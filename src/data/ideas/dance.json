{
  "theme": {
    "slug": "dance",
    "name": "Dance & Choreography",
    "emoji": "💃",
    "audience": "choreographers, dancers, dance teachers, movement directors",
    "market_anchor": "the global dance industry (~$5B; >2M studios worldwide)"
  },
  "ideas": [
    {
      "id": "dance-barre-whisperer-0",
      "theme": "dance",
      "title": "Barre Whisperer",
      "pitch": "Lovable AI generates personalized posture correction so students improve; ElevenLabs speaks it.",
      "subDiscipline": "ballet pedagogy",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "TTS provides hands-free audio cues while students hold difficult ballet positions.",
      "megaPrompt": "Build \"Barre Whisperer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI generates personalized posture correction so students improve; ElevenLabs speaks it.\nDiscipline: Dance & Choreography (ballet pedagogy).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: TTS provides hands-free audio cues while students hold difficult ballet positions.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the ballet pedagogy use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for ballet pedagogy. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for ballet pedagogy.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance studio market",
      "sam": "$1.2B — classical ballet training sector",
      "som": "$50M — elite ballet academy software"
    },
    {
      "id": "dance-floorwork-breath-guide-1",
      "theme": "dance",
      "title": "Floorwork Breath Guide",
      "pitch": "Lovable AI designs a breathing rhythm so dancers execute floorwork safely; ElevenLabs narrates it.",
      "subDiscipline": "contemporary somatic pacing",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Audio pacing allows dancers to synchronize breath with movement without looking at screens.",
      "megaPrompt": "Build \"Floorwork Breath Guide\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI designs a breathing rhythm so dancers execute floorwork safely; ElevenLabs narrates it.\nDiscipline: Dance & Choreography (contemporary somatic pacing).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Audio pacing allows dancers to synchronize breath with movement without looking at screens.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the contemporary somatic pacing use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for contemporary somatic pacing. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for contemporary somatic pacing.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$800M — contemporary dance education",
      "som": "$40M — university dance programs"
    },
    {
      "id": "dance-tap-cadence-caller-2",
      "theme": "dance",
      "title": "Tap Cadence Caller",
      "pitch": "Lovable AI creates complex syncopated counting so tap ensembles synchronize; ElevenLabs voices it.",
      "subDiscipline": "rhythmic ensemble counting",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "TTS delivers precise, customizable rhythmic vocalizations essential for teaching intricate tap routines.",
      "megaPrompt": "Build \"Tap Cadence Caller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI creates complex syncopated counting so tap ensembles synchronize; ElevenLabs voices it.\nDiscipline: Dance & Choreography (rhythmic ensemble counting).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: TTS delivers precise, customizable rhythmic vocalizations essential for teaching intricate tap routines.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the rhythmic ensemble counting use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for rhythmic ensemble counting. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for rhythmic ensemble counting.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance market",
      "sam": "$300M — tap dance studios",
      "som": "$15M — competitive tap teams"
    },
    {
      "id": "dance-cypher-hype-announcer-3",
      "theme": "dance",
      "title": "Cypher Hype Announcer",
      "pitch": "Lovable AI writes an energetic dancer introduction so performers feel hyped; ElevenLabs shouts it.",
      "subDiscipline": "street dance performance",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "High-energy TTS voices simulate a live MC, elevating the atmosphere of practice cyphers.",
      "megaPrompt": "Build \"Cypher Hype Announcer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI writes an energetic dancer introduction so performers feel hyped; ElevenLabs shouts it.\nDiscipline: Dance & Choreography (street dance performance).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: High-energy TTS voices simulate a live MC, elevating the atmosphere of practice cyphers.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the street dance performance use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for street dance performance. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for street dance performance.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$1B — hip-hop and street dance",
      "som": "$60M — urban dance competitions"
    },
    {
      "id": "dance-stage-blocking-narrator-4",
      "theme": "dance",
      "title": "Stage Blocking Narrator",
      "pitch": "Lovable AI calculates a stage pathway so choreographers visualize formations; ElevenLabs reads it.",
      "subDiscipline": "choreographic spatial staging",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Spoken spatial coordinates let choreographers close their eyes and mentally map stage geometry.",
      "megaPrompt": "Build \"Stage Blocking Narrator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI calculates a stage pathway so choreographers visualize formations; ElevenLabs reads it.\nDiscipline: Dance & Choreography (choreographic spatial staging).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Spoken spatial coordinates let choreographers close their eyes and mentally map stage geometry.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the choreographic spatial staging use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for choreographic spatial staging. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for choreographic spatial staging.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$500M — professional choreography services",
      "som": "$25M — commercial dance productions"
    },
    {
      "id": "dance-heritage-movement-storyteller-5",
      "theme": "dance",
      "title": "Heritage Movement Storyteller",
      "pitch": "Lovable AI scripts a historical dance anecdote so students learn context; ElevenLabs narrates it.",
      "subDiscipline": "dance history education",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "TTS transforms dry historical facts into engaging audio stories for studio warm-ups.",
      "megaPrompt": "Build \"Heritage Movement Storyteller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI scripts a historical dance anecdote so students learn context; ElevenLabs narrates it.\nDiscipline: Dance & Choreography (dance history education).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: TTS transforms dry historical facts into engaging audio stories for studio warm-ups.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the dance history education use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for dance history education. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for dance history education.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$600M — dance education and academia",
      "som": "$30M — dance history curriculums"
    },
    {
      "id": "dance-somatic-recovery-whisper-6",
      "theme": "dance",
      "title": "Somatic Recovery Whisper",
      "pitch": "Lovable AI drafts a targeted body scan so dancers prevent injuries; ElevenLabs whispers it.",
      "subDiscipline": "dancer injury prevention",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Soft TTS voices guide deep physical relaxation during critical post-rehearsal recovery windows.",
      "megaPrompt": "Build \"Somatic Recovery Whisper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI drafts a targeted body scan so dancers prevent injuries; ElevenLabs whispers it.\nDiscipline: Dance & Choreography (dancer injury prevention).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Soft TTS voices guide deep physical relaxation during critical post-rehearsal recovery windows.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the dancer injury prevention use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for dancer injury prevention. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for dancer injury prevention.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$400M — dancer wellness and physiotherapy",
      "som": "$20M — professional company healthcare"
    },
    {
      "id": "dance-quick-change-pacer-7",
      "theme": "dance",
      "title": "Quick Change Pacer",
      "pitch": "Lovable AI sequences a rapid costume swap so wardrobe crews succeed; ElevenLabs announces it.",
      "subDiscipline": "backstage production timing",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Clear TTS countdowns keep backstage crews perfectly synchronized during high-stress live performances.",
      "megaPrompt": "Build \"Quick Change Pacer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI sequences a rapid costume swap so wardrobe crews succeed; ElevenLabs announces it.\nDiscipline: Dance & Choreography (backstage production timing).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Clear TTS countdowns keep backstage crews perfectly synchronized during high-stress live performances.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the backstage production timing use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for backstage production timing. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for backstage production timing.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$700M — live dance production",
      "som": "$35M — theatrical dance wardrobe"
    },
    {
      "id": "dance-improv-muse-broadcaster-8",
      "theme": "dance",
      "title": "Improv Muse Broadcaster",
      "pitch": "Lovable AI invents an abstract physical prompt so dancers explore improvisation; ElevenLabs speaks it.",
      "subDiscipline": "movement improvisation",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Spontaneous audio prompts keep improvisers in a continuous flow state without visual distractions.",
      "megaPrompt": "Build \"Improv Muse Broadcaster\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI invents an abstract physical prompt so dancers explore improvisation; ElevenLabs speaks it.\nDiscipline: Dance & Choreography (movement improvisation).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Spontaneous audio prompts keep improvisers in a continuous flow state without visual distractions.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the movement improvisation use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for movement improvisation. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for movement improvisation.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$200M — experimental dance collectives",
      "som": "$10M — contact improvisation workshops"
    },
    {
      "id": "dance-pantomime-narrative-voice-9",
      "theme": "dance",
      "title": "Pantomime Narrative Voice",
      "pitch": "Lovable AI translates classical ballet mime so audiences understand plots; ElevenLabs voices it.",
      "subDiscipline": "classical ballet narrative",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "TTS provides accessible, real-time audio translations of silent gestures for visually impaired patrons.",
      "megaPrompt": "Build \"Pantomime Narrative Voice\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI translates classical ballet mime so audiences understand plots; ElevenLabs voices it.\nDiscipline: Dance & Choreography (classical ballet narrative).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: TTS provides accessible, real-time audio translations of silent gestures for visually impaired patrons.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the classical ballet narrative use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for classical ballet narrative. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for classical ballet narrative.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$1.2B — classical ballet performances",
      "som": "$40M — accessible arts initiatives"
    },
    {
      "id": "dance-studio-timekeeper-10",
      "theme": "dance",
      "title": "Studio Timekeeper",
      "pitch": "Lovable AI structures a daily rehearsal block so directors stay efficient; ElevenLabs calls it.",
      "subDiscipline": "rehearsal management",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Vocal timekeeping allows directors to manage studio schedules without constantly checking their phones.",
      "megaPrompt": "Build \"Studio Timekeeper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI structures a daily rehearsal block so directors stay efficient; ElevenLabs calls it.\nDiscipline: Dance & Choreography (rehearsal management).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Vocal timekeeping allows directors to manage studio schedules without constantly checking their phones.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the rehearsal management use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for rehearsal management. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for rehearsal management.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$800M — professional dance companies",
      "som": "$50M — company stage management"
    },
    {
      "id": "dance-constructive-critique-coach-11",
      "theme": "dance",
      "title": "Constructive Critique Coach",
      "pitch": "Lovable AI formulates gentle technical feedback so dancers improve gracefully; ElevenLabs reads it.",
      "subDiscipline": "performance evaluation",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Empathetic TTS delivery softens the blow of rigorous technical corrections during video review.",
      "megaPrompt": "Build \"Constructive Critique Coach\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI formulates gentle technical feedback so dancers improve gracefully; ElevenLabs reads it.\nDiscipline: Dance & Choreography (performance evaluation).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Empathetic TTS delivery softens the blow of rigorous technical corrections during video review.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the performance evaluation use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for performance evaluation. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for performance evaluation.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$300M — competitive dance circuit",
      "som": "$15M — elite youth dance training"
    },
    {
      "id": "dance-folkloric-rhythm-narrator-12",
      "theme": "dance",
      "title": "Folkloric Rhythm Narrator",
      "pitch": "Lovable AI explains a regional folk step so dancers honor traditions; ElevenLabs narrates it.",
      "subDiscipline": "cultural dance preservation",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Multilingual TTS preserves the authentic pronunciation and cultural context of traditional folk dances.",
      "megaPrompt": "Build \"Folkloric Rhythm Narrator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI explains a regional folk step so dancers honor traditions; ElevenLabs narrates it.\nDiscipline: Dance & Choreography (cultural dance preservation).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Multilingual TTS preserves the authentic pronunciation and cultural context of traditional folk dances.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the cultural dance preservation use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for cultural dance preservation. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for cultural dance preservation.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$400M — cultural and folkloric dance",
      "som": "$20M — heritage dance preservation"
    },
    {
      "id": "dance-ensemble-sync-metronome-13",
      "theme": "dance",
      "title": "Ensemble Sync Metronome",
      "pitch": "Lovable AI generates dynamic vocal counting so large ensembles unify; ElevenLabs speaks it.",
      "subDiscipline": "group synchronization",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "TTS replaces monotonous clicks with expressive vocal phrasing that matches the music's emotion.",
      "megaPrompt": "Build \"Ensemble Sync Metronome\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI generates dynamic vocal counting so large ensembles unify; ElevenLabs speaks it.\nDiscipline: Dance & Choreography (group synchronization).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: TTS replaces monotonous clicks with expressive vocal phrasing that matches the music's emotion.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the group synchronization use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for group synchronization. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for group synchronization.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$900M — large-scale dance productions",
      "som": "$45M — cruise ship dance casts"
    },
    {
      "id": "dance-emotional-arc-guide-14",
      "theme": "dance",
      "title": "Emotional Arc Guide",
      "pitch": "Lovable AI maps a character emotional shift so dancers act convincingly; ElevenLabs whispers it.",
      "subDiscipline": "dramatic expression",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Nuanced TTS voices act as an offstage prompter, guiding subtle facial and bodily expressions.",
      "megaPrompt": "Build \"Emotional Arc Guide\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI maps a character emotional shift so dancers act convincingly; ElevenLabs whispers it.\nDiscipline: Dance & Choreography (dramatic expression).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Nuanced TTS voices act as an offstage prompter, guiding subtle facial and bodily expressions.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the dramatic expression use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for dramatic expression. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for dramatic expression.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$600M — contemporary and modern dance",
      "som": "$30M — narrative dance theater"
    },
    {
      "id": "dance-kinetic-visualization-guru-15",
      "theme": "dance",
      "title": "Kinetic Visualization Guru",
      "pitch": "Lovable AI scripts a vivid movement visualization so dancers prepare mentally; ElevenLabs narrates it.",
      "subDiscipline": "mental preparation",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Immersive audio storytelling primes the nervous system for complex choreography before physical exertion.",
      "megaPrompt": "Build \"Kinetic Visualization Guru\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI scripts a vivid movement visualization so dancers prepare mentally; ElevenLabs narrates it.\nDiscipline: Dance & Choreography (mental preparation).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Immersive audio storytelling primes the nervous system for complex choreography before physical exertion.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the mental preparation use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for mental preparation. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for mental preparation.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$500M — sports and dance psychology",
      "som": "$25M — pre-professional dance academies"
    },
    {
      "id": "dance-curtain-call-anchor-16",
      "theme": "dance",
      "title": "Curtain Call Anchor",
      "pitch": "Lovable AI designs a grounding breathing exercise so anxious performers focus; ElevenLabs speaks it.",
      "subDiscipline": "performance anxiety",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Calming TTS voices provide a reliable, soothing presence in the chaotic backstage wings.",
      "megaPrompt": "Build \"Curtain Call Anchor\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI designs a grounding breathing exercise so anxious performers focus; ElevenLabs speaks it.\nDiscipline: Dance & Choreography (performance anxiety).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Calming TTS voices provide a reliable, soothing presence in the chaotic backstage wings.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the performance anxiety use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for performance anxiety. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for performance anxiety.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$400M — performer wellness programs",
      "som": "$20M — youth competition dancers"
    },
    {
      "id": "dance-motif-variation-prompter-17",
      "theme": "dance",
      "title": "Motif Variation Prompter",
      "pitch": "Lovable AI suggests a spatial motif variation so choreographers avoid repetition; ElevenLabs reads it.",
      "subDiscipline": "choreographic composition",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Spoken suggestions allow choreographers to immediately physically test new ideas without breaking flow.",
      "megaPrompt": "Build \"Motif Variation Prompter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI suggests a spatial motif variation so choreographers avoid repetition; ElevenLabs reads it.\nDiscipline: Dance & Choreography (choreographic composition).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Spoken suggestions allow choreographers to immediately physically test new ideas without breaking flow.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the choreographic composition use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for choreographic composition. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for choreographic composition.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$300M — independent choreographers",
      "som": "$15M — university dance departments"
    },
    {
      "id": "dance-accessible-movement-describer-18",
      "theme": "dance",
      "title": "Accessible Movement Describer",
      "pitch": "Lovable AI writes a vivid choreography description so blind patrons experience dance; ElevenLabs voices it.",
      "subDiscipline": "inclusive audience experience",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "High-quality TTS provides rich, real-time audio descriptions that capture the kinetic energy of live movement.",
      "megaPrompt": "Build \"Accessible Movement Describer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI writes a vivid choreography description so blind patrons experience dance; ElevenLabs voices it.\nDiscipline: Dance & Choreography (inclusive audience experience).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: High-quality TTS provides rich, real-time audio descriptions that capture the kinetic energy of live movement.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the inclusive audience experience use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for inclusive audience experience. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for inclusive audience experience.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$1B — live arts accessibility",
      "som": "$50M — inclusive theater initiatives"
    },
    {
      "id": "dance-battle-criteria-announcer-19",
      "theme": "dance",
      "title": "Battle Criteria Announcer",
      "pitch": "Lovable AI outlines a specific judging rubric so battle competitors strategize; ElevenLabs announces it.",
      "subDiscipline": "street dance competition",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Authoritative TTS voices simulate official judges, helping dancers practice under realistic competitive pressure.",
      "megaPrompt": "Build \"Battle Criteria Announcer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI outlines a specific judging rubric so battle competitors strategize; ElevenLabs announces it.\nDiscipline: Dance & Choreography (street dance competition).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Authoritative TTS voices simulate official judges, helping dancers practice under realistic competitive pressure.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the street dance competition use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for street dance competition. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for street dance competition.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$500M — dance battle events",
      "som": "$25M — professional street leagues"
    },
    {
      "id": "dance-post-show-reflection-20",
      "theme": "dance",
      "title": "Post Show Reflection",
      "pitch": "Lovable AI asks a targeted recovery question so dancers process performances; ElevenLabs speaks it.",
      "subDiscipline": "performance recovery",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Conversational TTS prompts guide exhausted dancers through mental cooldowns while they physically stretch.",
      "megaPrompt": "Build \"Post Show Reflection\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI asks a targeted recovery question so dancers process performances; ElevenLabs speaks it.\nDiscipline: Dance & Choreography (performance recovery).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Conversational TTS prompts guide exhausted dancers through mental cooldowns while they physically stretch.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the performance recovery use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for performance recovery. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for performance recovery.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$600M — professional dance companies",
      "som": "$30M — touring dance troupes"
    },
    {
      "id": "dance-lighting-cue-narrator-21",
      "theme": "dance",
      "title": "Lighting Cue Narrator",
      "pitch": "Lovable AI sequences a complex lighting state so tech crews anticipate cues; ElevenLabs calls it.",
      "subDiscipline": "technical rehearsal",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "TTS vocalizes upcoming lighting shifts, allowing technicians to focus on the board instead of notes.",
      "megaPrompt": "Build \"Lighting Cue Narrator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI sequences a complex lighting state so tech crews anticipate cues; ElevenLabs calls it.\nDiscipline: Dance & Choreography (technical rehearsal).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: TTS vocalizes upcoming lighting shifts, allowing technicians to focus on the board instead of notes.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the technical rehearsal use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for technical rehearsal. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for technical rehearsal.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$800M — theatrical production design",
      "som": "$40M — dance company tech crews"
    },
    {
      "id": "dance-pas-de-deux-pacer-22",
      "theme": "dance",
      "title": "Pas De Deux Pacer",
      "pitch": "Lovable AI calculates weight-transfer timing so partners execute lifts safely; ElevenLabs counts it.",
      "subDiscipline": "classical partnering",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Precise vocal counting ensures both partners initiate weight-sharing mechanics at the exact same millisecond.",
      "megaPrompt": "Build \"Pas De Deux Pacer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI calculates weight-transfer timing so partners execute lifts safely; ElevenLabs counts it.\nDiscipline: Dance & Choreography (classical partnering).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Precise vocal counting ensures both partners initiate weight-sharing mechanics at the exact same millisecond.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the classical partnering use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for classical partnering. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for classical partnering.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$1.2B — classical ballet training",
      "som": "$60M — professional ballet companies"
    },
    {
      "id": "dance-global-rhythm-pronouncer-23",
      "theme": "dance",
      "title": "Global Rhythm Pronouncer",
      "pitch": "Lovable AI translates indigenous dance terminology so students pronounce correctly; ElevenLabs voices it.",
      "subDiscipline": "world dance linguistics",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Multilingual TTS ensures respectful and accurate vocalization of non-English cultural dance vocabulary.",
      "megaPrompt": "Build \"Global Rhythm Pronouncer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI translates indigenous dance terminology so students pronounce correctly; ElevenLabs voices it.\nDiscipline: Dance & Choreography (world dance linguistics).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Multilingual TTS ensures respectful and accurate vocalization of non-English cultural dance vocabulary.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the world dance linguistics use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for world dance linguistics. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for world dance linguistics.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$400M — world dance education",
      "som": "$20M — cultural dance conservatories"
    },
    {
      "id": "dance-lobby-hype-broadcaster-24",
      "theme": "dance",
      "title": "Lobby Hype Broadcaster",
      "pitch": "Lovable AI writes a pre-show trivia fact so arriving audiences build anticipation; ElevenLabs broadcasts it.",
      "subDiscipline": "front of house",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "TTS transforms written program notes into an immersive, welcoming audio experience in the lobby.",
      "megaPrompt": "Build \"Lobby Hype Broadcaster\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI writes a pre-show trivia fact so arriving audiences build anticipation; ElevenLabs broadcasts it.\nDiscipline: Dance & Choreography (front of house).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: TTS transforms written program notes into an immersive, welcoming audio experience in the lobby.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the front of house use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for front of house. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for front of house.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$1B — live performance venues",
      "som": "$50M — regional dance theaters"
    },
    {
      "id": "dance-spontaneous-movement-muse-0",
      "theme": "dance",
      "title": "Spontaneous Movement Muse",
      "pitch": "Lovable AI generates surreal movement constraints so dancers explore; ElevenLabs speaks them continuously to guide improvisation.",
      "subDiscipline": "improvisation prompting",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Hands-free real-time audio is essential when dancers are physically moving and cannot look at a screen.",
      "megaPrompt": "Build \"Spontaneous Movement Muse\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI generates surreal movement constraints so dancers explore; ElevenLabs speaks them continuously to guide improvisation.\nDiscipline: Dance & Choreography (improvisation prompting).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Hands-free real-time audio is essential when dancers are physically moving and cannot look at a screen.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the improvisation prompting use case (e.g.\n\"You are a senior improvisation prompting mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for improvisation prompting.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance and movement industry",
      "sam": "$400M — contemporary and improvisational dance sectors",
      "som": "$15M — experimental choreographers and movement labs"
    },
    {
      "id": "dance-phantom-count-caller-1",
      "theme": "dance",
      "title": "Phantom Count Caller",
      "pitch": "Lovable AI analyzes choreography structure so dancers stay on track; ElevenLabs speaks counts and cues.",
      "subDiscipline": "rehearsal pacing",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Two-way WebRTC allows the AI to hear the dancer's breathing and adjust counts dynamically.",
      "megaPrompt": "Build \"Phantom Count Caller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI analyzes choreography structure so dancers stay on track; ElevenLabs speaks counts and cues.\nDiscipline: Dance & Choreography (rehearsal pacing).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Two-way WebRTC allows the AI to hear the dancer's breathing and adjust counts dynamically.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the rehearsal pacing use case (e.g.\n\"You are a senior rehearsal pacing mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for rehearsal pacing.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance studio market",
      "sam": "$1.2B — professional rehearsal spaces and companies",
      "som": "$40M — commercial and touring dance troupes"
    },
    {
      "id": "dance-green-room-anchor-2",
      "theme": "dance",
      "title": "Green Room Anchor",
      "pitch": "Lovable AI designs personalized focus routines so performers calm nerves; ElevenLabs speaks them through pre-show anxiety.",
      "subDiscipline": "performance psychology",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Conversational pacing mimics a real human therapist, which is crucial for calming pre-show nerves.",
      "megaPrompt": "Build \"Green Room Anchor\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI designs personalized focus routines so performers calm nerves; ElevenLabs speaks them through pre-show anxiety.\nDiscipline: Dance & Choreography (performance psychology).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Conversational pacing mimics a real human therapist, which is crucial for calming pre-show nerves.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the performance psychology use case (e.g.\n\"You are a senior performance psychology mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for performance psychology.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global performing arts industry",
      "sam": "$900M — professional stage performers and dancers",
      "som": "$30M — elite competition and company dancers"
    },
    {
      "id": "dance-somatic-triage-companion-3",
      "theme": "dance",
      "title": "Somatic Triage Companion",
      "pitch": "Lovable AI evaluates post-rehearsal physical feedback so dancers recover safely; ElevenLabs captures a verbal pain assessment.",
      "subDiscipline": "recovery assessment",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Real-time dialogue allows for nuanced, back-and-forth pain localization without requiring exhausted dancers to type.",
      "megaPrompt": "Build \"Somatic Triage Companion\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI evaluates post-rehearsal physical feedback so dancers recover safely; ElevenLabs captures a verbal pain assessment.\nDiscipline: Dance & Choreography (recovery assessment).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Real-time dialogue allows for nuanced, back-and-forth pain localization without requiring exhausted dancers to type.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the recovery assessment use case (e.g.\n\"You are a senior recovery assessment mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for recovery assessment.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance and wellness market",
      "sam": "$600M — dance medicine and physical therapy",
      "som": "$20M — professional athletes and elite dancers"
    },
    {
      "id": "dance-hands-free-notation-scribe-4",
      "theme": "dance",
      "title": "Hands-Free Notation Scribe",
      "pitch": "Lovable AI translates spoken movement descriptions so choreographers document ideas; ElevenLabs captures the spoken choreography.",
      "subDiscipline": "choreography documentation",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Capturing voice continuously via WebRTC lets choreographers dictate complex movements while physically demonstrating them.",
      "megaPrompt": "Build \"Hands-Free Notation Scribe\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI translates spoken movement descriptions so choreographers document ideas; ElevenLabs captures the spoken choreography.\nDiscipline: Dance & Choreography (choreography documentation).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Capturing voice continuously via WebRTC lets choreographers dictate complex movements while physically demonstrating them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the choreography documentation use case (e.g.\n\"You are a senior choreography documentation mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for choreography documentation.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance creation software market",
      "sam": "$300M — independent choreographers and directors",
      "som": "$10M — university dance faculty and residents"
    },
    {
      "id": "dance-phrasing-negotiator-5",
      "theme": "dance",
      "title": "Phrasing Negotiator",
      "pitch": "Lovable AI adjusts musical phrasing concepts so dancers find the groove; ElevenLabs speaks tempo changes conversationally.",
      "subDiscipline": "musicality rehearsal",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Interactive voice negotiation allows dancers to ask for tempo shifts mid-routine without stopping the music.",
      "megaPrompt": "Build \"Phrasing Negotiator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI adjusts musical phrasing concepts so dancers find the groove; ElevenLabs speaks tempo changes conversationally.\nDiscipline: Dance & Choreography (musicality rehearsal).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Interactive voice negotiation allows dancers to ask for tempo shifts mid-routine without stopping the music.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the musicality rehearsal use case (e.g.\n\"You are a senior musicality rehearsal mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for musicality rehearsal.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance and music education",
      "sam": "$500M — rhythm-based dance styles and studios",
      "som": "$18M — hip-hop and tap dance educators"
    },
    {
      "id": "dance-tech-week-maestro-6",
      "theme": "dance",
      "title": "Tech Week Maestro",
      "pitch": "Lovable AI tracks complex scene transitions so crews stay synchronized; ElevenLabs speaks cues over headsets.",
      "subDiscipline": "stage management",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Low-latency WebRTC ensures stage managers and dancers hear cues instantly during chaotic technical rehearsals.",
      "megaPrompt": "Build \"Tech Week Maestro\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI tracks complex scene transitions so crews stay synchronized; ElevenLabs speaks cues over headsets.\nDiscipline: Dance & Choreography (stage management).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Low-latency WebRTC ensures stage managers and dancers hear cues instantly during chaotic technical rehearsals.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the stage management use case (e.g.\n\"You are a senior stage management mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for stage management.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global live event production market",
      "sam": "$1.5B — theatrical stage management and crew",
      "som": "$50M — mid-size regional theater companies"
    },
    {
      "id": "dance-ballet-babel-guide-7",
      "theme": "dance",
      "title": "Ballet Babel Guide",
      "pitch": "Lovable AI translates nuanced dance terminology so diverse troupes understand; ElevenLabs speaks real-time bilingual coaching corrections.",
      "subDiscipline": "international coaching",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Real-time bilingual voice allows international guest choreographers to teach seamlessly without pausing for human translators.",
      "megaPrompt": "Build \"Ballet Babel Guide\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI translates nuanced dance terminology so diverse troupes understand; ElevenLabs speaks real-time bilingual coaching corrections.\nDiscipline: Dance & Choreography (international coaching).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Real-time bilingual voice allows international guest choreographers to teach seamlessly without pausing for human translators.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the international coaching use case (e.g.\n\"You are a senior international coaching mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for international coaching.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global classical dance industry",
      "sam": "$800M — international ballet companies and academies",
      "som": "$25M — touring international ballet troupes"
    },
    {
      "id": "dance-gravity-whisper-guide-8",
      "theme": "dance",
      "title": "Gravity Whisper Guide",
      "pitch": "Lovable AI sequences safe rolling mechanics so dancers avoid impact; ElevenLabs speaks somatic cues during practice.",
      "subDiscipline": "floorwork safety",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Soft, continuous voice guidance helps dancers maintain a meditative state while navigating dangerous floor transitions.",
      "megaPrompt": "Build \"Gravity Whisper Guide\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI sequences safe rolling mechanics so dancers avoid impact; ElevenLabs speaks somatic cues during practice.\nDiscipline: Dance & Choreography (floorwork safety).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Soft, continuous voice guidance helps dancers maintain a meditative state while navigating dangerous floor transitions.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the floorwork safety use case (e.g.\n\"You are a senior floorwork safety mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for floorwork safety.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global contemporary dance sector",
      "sam": "$200M — somatic and floorwork training",
      "som": "$8M — contemporary dance conservatories"
    },
    {
      "id": "dance-accessible-stage-narrator-9",
      "theme": "dance",
      "title": "Accessible Stage Narrator",
      "pitch": "Lovable AI composes vivid movement descriptions so blind audiences experience shows; ElevenLabs speaks the live narration.",
      "subDiscipline": "audio description",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Conversational AI can dynamically adjust narration pacing based on the live audio feed of the performance.",
      "megaPrompt": "Build \"Accessible Stage Narrator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI composes vivid movement descriptions so blind audiences experience shows; ElevenLabs speaks the live narration.\nDiscipline: Dance & Choreography (audio description).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Conversational AI can dynamically adjust narration pacing based on the live audio feed of the performance.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the audio description use case (e.g.\n\"You are a senior audio description mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for audio description.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global accessible arts market",
      "sam": "$400M — inclusive theater and dance venues",
      "som": "$12M — government-funded accessible arts programs"
    },
    {
      "id": "dance-cypher-hype-trainer-10",
      "theme": "dance",
      "title": "Cypher Hype Trainer",
      "pitch": "Lovable AI generates witty battle responses so b-boys build confidence; ElevenLabs speaks as a trash-talking opponent.",
      "subDiscipline": "battle preparation",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "An interactive, aggressive voice persona simulates the high-pressure auditory environment of a real street dance battle.",
      "megaPrompt": "Build \"Cypher Hype Trainer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI generates witty battle responses so b-boys build confidence; ElevenLabs speaks as a trash-talking opponent.\nDiscipline: Dance & Choreography (battle preparation).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: An interactive, aggressive voice persona simulates the high-pressure auditory environment of a real street dance battle.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the battle preparation use case (e.g.\n\"You are a senior battle preparation mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for battle preparation.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global street dance and culture",
      "sam": "$300M — competitive breaking and hip-hop events",
      "som": "$15M — professional battle league competitors"
    },
    {
      "id": "dance-classical-anatomy-tutor-11",
      "theme": "dance",
      "title": "Classical Anatomy Tutor",
      "pitch": "Lovable AI diagnoses technical flaws from verbal descriptions so dancers improve; ElevenLabs speaks anatomical corrections conversationally.",
      "subDiscipline": "ballet technique",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Dancers can describe physical sensations aloud at the barre and receive immediate biomechanical voice corrections.",
      "megaPrompt": "Build \"Classical Anatomy Tutor\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI diagnoses technical flaws from verbal descriptions so dancers improve; ElevenLabs speaks anatomical corrections conversationally.\nDiscipline: Dance & Choreography (ballet technique).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Dancers can describe physical sensations aloud at the barre and receive immediate biomechanical voice corrections.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the ballet technique use case (e.g.\n\"You are a senior ballet technique mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for ballet technique.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global classical ballet market",
      "sam": "$1B — ballet academies and pre-professional schools",
      "som": "$35M — vocational ballet boarding schools"
    },
    {
      "id": "dance-dynamic-pulse-weaver-12",
      "theme": "dance",
      "title": "Dynamic Pulse Weaver",
      "pitch": "Lovable AI customizes joint-mobility sequences so classes start safely; ElevenLabs speaks the vocal warmup interactively.",
      "subDiscipline": "physical warmup",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Teachers can verbally interrupt the AI to adjust the warmup intensity based on the class's energy.",
      "megaPrompt": "Build \"Dynamic Pulse Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI customizes joint-mobility sequences so classes start safely; ElevenLabs speaks the vocal warmup interactively.\nDiscipline: Dance & Choreography (physical warmup).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Teachers can verbally interrupt the AI to adjust the warmup intensity based on the class's energy.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the physical warmup use case (e.g.\n\"You are a senior physical warmup mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for physical warmup.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance studio software",
      "sam": "$2B — group class instructors and studios",
      "som": "$60M — boutique fitness and dance franchises"
    },
    {
      "id": "dance-musical-theater-persona-13",
      "theme": "dance",
      "title": "Musical Theater Persona",
      "pitch": "Lovable AI develops character backstories so musical theater dancers act authentically; ElevenLabs speaks in the character's voice.",
      "subDiscipline": "character acting",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Hearing the AI speak in character helps dancers internalize vocal mannerisms while rehearsing choreography.",
      "megaPrompt": "Build \"Musical Theater Persona\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI develops character backstories so musical theater dancers act authentically; ElevenLabs speaks in the character's voice.\nDiscipline: Dance & Choreography (character acting).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Hearing the AI speak in character helps dancers internalize vocal mannerisms while rehearsing choreography.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the character acting use case (e.g.\n\"You are a senior character acting mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for character acting.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global musical theater industry",
      "sam": "$1.5B — musical theater performers and triple-threats",
      "som": "$45M — Broadway and West End ensemble casts"
    },
    {
      "id": "dance-formation-spacing-referee-14",
      "theme": "dance",
      "title": "Formation Spacing Referee",
      "pitch": "Lovable AI calculates spatial geometry so ensembles perfect formations; ElevenLabs captures coordinates and speaks spacing corrections.",
      "subDiscipline": "stage blocking",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Dancers can call out their stage positions hands-free, and the AI instantly speaks necessary adjustments.",
      "megaPrompt": "Build \"Formation Spacing Referee\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI calculates spatial geometry so ensembles perfect formations; ElevenLabs captures coordinates and speaks spacing corrections.\nDiscipline: Dance & Choreography (stage blocking).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Dancers can call out their stage positions hands-free, and the AI instantly speaks necessary adjustments.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the stage blocking use case (e.g.\n\"You are a senior stage blocking mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for stage blocking.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global ensemble dance market",
      "sam": "$600M — competitive dance teams and show choirs",
      "som": "$20M — collegiate and high-school dance teams"
    },
    {
      "id": "dance-touring-fuel-coach-15",
      "theme": "dance",
      "title": "Touring Fuel Coach",
      "pitch": "Lovable AI plans high-energy meals so touring dancers stay fueled; ElevenLabs speaks dietary advice conversationally.",
      "subDiscipline": "tour nutrition",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Touring dancers can discuss local food options via voice while walking to venues, getting instant advice.",
      "megaPrompt": "Build \"Touring Fuel Coach\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI plans high-energy meals so touring dancers stay fueled; ElevenLabs speaks dietary advice conversationally.\nDiscipline: Dance & Choreography (tour nutrition).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Touring dancers can discuss local food options via voice while walking to venues, getting instant advice.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the tour nutrition use case (e.g.\n\"You are a senior tour nutrition mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for tour nutrition.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global sports and performance nutrition",
      "sam": "$400M — touring performers and athletic troupes",
      "som": "$10M — international touring dance companies"
    },
    {
      "id": "dance-arts-grant-interrogator-16",
      "theme": "dance",
      "title": "Arts Grant Interrogator",
      "pitch": "Lovable AI critiques artistic statements so choreographers win funding; ElevenLabs speaks questions during a mock interview.",
      "subDiscipline": "funding applications",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Simulating a spoken grant panel prepares choreographers for the verbal defense of their artistic vision.",
      "megaPrompt": "Build \"Arts Grant Interrogator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI critiques artistic statements so choreographers win funding; ElevenLabs speaks questions during a mock interview.\nDiscipline: Dance & Choreography (funding applications).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Simulating a spoken grant panel prepares choreographers for the verbal defense of their artistic vision.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the funding applications use case (e.g.\n\"You are a senior funding applications mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for funding applications.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global arts funding and grants",
      "sam": "$200M — independent choreographers and non-profits",
      "som": "$5M — grant-seeking contemporary dance artists"
    },
    {
      "id": "dance-lineage-socratic-tutor-17",
      "theme": "dance",
      "title": "Lineage Socratic Tutor",
      "pitch": "Lovable AI explores cultural roots so students respect dance lineage; ElevenLabs speaks history through interactive dialogue.",
      "subDiscipline": "dance history",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Socratic voice dialogue transforms passive history reading into an engaging, oral-tradition-style storytelling experience.",
      "megaPrompt": "Build \"Lineage Socratic Tutor\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI explores cultural roots so students respect dance lineage; ElevenLabs speaks history through interactive dialogue.\nDiscipline: Dance & Choreography (dance history).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Socratic voice dialogue transforms passive history reading into an engaging, oral-tradition-style storytelling experience.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the dance history use case (e.g.\n\"You are a senior dance history mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for dance history.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance education market",
      "sam": "$1B — university dance departments and academics",
      "som": "$30M — higher-education dance history courses"
    },
    {
      "id": "dance-twilight-somatic-journal-18",
      "theme": "dance",
      "title": "Twilight Somatic Journal",
      "pitch": "Lovable AI structures emotional debriefs so dancers process intense rehearsals; ElevenLabs captures a verbal cool-down journal.",
      "subDiscipline": "post-class reflection",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Speaking reflections aloud while stretching helps dancers emotionally decompress without the friction of writing.",
      "megaPrompt": "Build \"Twilight Somatic Journal\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI structures emotional debriefs so dancers process intense rehearsals; ElevenLabs captures a verbal cool-down journal.\nDiscipline: Dance & Choreography (post-class reflection).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Speaking reflections aloud while stretching helps dancers emotionally decompress without the friction of writing.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the post-class reflection use case (e.g.\n\"You are a senior post-class reflection mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for post-class reflection.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global mental health and wellness",
      "sam": "$300M — performer mental health initiatives",
      "som": "$12M — intensive dance training programs"
    },
    {
      "id": "dance-lighting-cue-builder-19",
      "theme": "dance",
      "title": "Lighting Cue Builder",
      "pitch": "Lovable AI organizes lighting states so designers build accurate plots; ElevenLabs captures and speaks cues.",
      "subDiscipline": "technical design",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Designers can sit in the dark auditorium and verbally build complex cue sheets with the AI.",
      "megaPrompt": "Build \"Lighting Cue Builder\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI organizes lighting states so designers build accurate plots; ElevenLabs captures and speaks cues.\nDiscipline: Dance & Choreography (technical design).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Designers can sit in the dark auditorium and verbally build complex cue sheets with the AI.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the technical design use case (e.g.\n\"You are a senior technical design mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for technical design.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global theatrical lighting design",
      "sam": "$800M — dance and theater technical crews",
      "som": "$25M — mid-tier regional dance festivals"
    },
    {
      "id": "dance-polyrhythm-scat-mentor-20",
      "theme": "dance",
      "title": "Polyrhythm Scat Mentor",
      "pitch": "Lovable AI generates complex syncopations so tap dancers master rhythm; ElevenLabs speaks the beats for imitation.",
      "subDiscipline": "rhythm training",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "The AI can scat and vocalize complex polyrhythms in real-time, adjusting speed based on the dancer's taps.",
      "megaPrompt": "Build \"Polyrhythm Scat Mentor\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI generates complex syncopations so tap dancers master rhythm; ElevenLabs speaks the beats for imitation.\nDiscipline: Dance & Choreography (rhythm training).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: The AI can scat and vocalize complex polyrhythms in real-time, adjusting speed based on the dancer's taps.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the rhythm training use case (e.g.\n\"You are a senior rhythm training mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for rhythm training.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global rhythm and percussion education",
      "sam": "$150M — tap and percussive dance communities",
      "som": "$5M — professional tap dance festivals"
    },
    {
      "id": "dance-prop-placement-tracker-21",
      "theme": "dance",
      "title": "Prop Placement Tracker",
      "pitch": "Lovable AI memorizes stage geography so crews avoid lost items; ElevenLabs speaks real-time prop locations.",
      "subDiscipline": "stage logistics",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Stagehands can quickly ask the AI where a prop was left during a chaotic, dark scene change.",
      "megaPrompt": "Build \"Prop Placement Tracker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI memorizes stage geography so crews avoid lost items; ElevenLabs speaks real-time prop locations.\nDiscipline: Dance & Choreography (stage logistics).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Stagehands can quickly ask the AI where a prop was left during a chaotic, dark scene change.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the stage logistics use case (e.g.\n\"You are a senior stage logistics mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for stage logistics.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global stagecraft and props market",
      "sam": "$500M — large-scale theatrical productions",
      "som": "$15M — touring mega-musicals and dance shows"
    },
    {
      "id": "dance-high-stakes-mock-auditor-22",
      "theme": "dance",
      "title": "High-Stakes Mock Auditor",
      "pitch": "Lovable AI simulates strict casting directors so dancers prepare mentally; ElevenLabs speaks during a high-pressure audition.",
      "subDiscipline": "audition prep",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "A realistic, intimidating voice agent helps dancers build resilience against harsh verbal audition environments.",
      "megaPrompt": "Build \"High-Stakes Mock Auditor\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI simulates strict casting directors so dancers prepare mentally; ElevenLabs speaks during a high-pressure audition.\nDiscipline: Dance & Choreography (audition prep).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: A realistic, intimidating voice agent helps dancers build resilience against harsh verbal audition environments.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the audition prep use case (e.g.\n\"You are a senior audition prep mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for audition prep.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global casting and talent agency market",
      "sam": "$1.2B — professional dancers seeking employment",
      "som": "$40M — commercial dance agency rosters"
    },
    {
      "id": "dance-thesis-concept-interrogator-23",
      "theme": "dance",
      "title": "Thesis Concept Interrogator",
      "pitch": "Lovable AI challenges thematic assumptions so movement directors refine visions; ElevenLabs speaks deep Socratic questions aloud.",
      "subDiscipline": "artistic direction",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Walking and talking with an AI sounding board helps directors untangle complex choreographic concepts physically.",
      "megaPrompt": "Build \"Thesis Concept Interrogator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI challenges thematic assumptions so movement directors refine visions; ElevenLabs speaks deep Socratic questions aloud.\nDiscipline: Dance & Choreography (artistic direction).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Walking and talking with an AI sounding board helps directors untangle complex choreographic concepts physically.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the artistic direction use case (e.g.\n\"You are a senior artistic direction mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for artistic direction.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global contemporary arts direction",
      "sam": "$250M — established choreographers and creators",
      "som": "$8M — artist-in-residence program directors"
    },
    {
      "id": "dance-studio-parent-liaison-24",
      "theme": "dance",
      "title": "Studio Parent Liaison",
      "pitch": "Lovable AI resolves scheduling conflicts so studio owners save time; ElevenLabs speaks to parents about recitals.",
      "subDiscipline": "studio administration",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "A friendly voice agent can handle anxious parent phone calls after hours, reducing studio admin burnout.",
      "megaPrompt": "Build \"Studio Parent Liaison\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI resolves scheduling conflicts so studio owners save time; ElevenLabs speaks to parents about recitals.\nDiscipline: Dance & Choreography (studio administration).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: A friendly voice agent can handle anxious parent phone calls after hours, reducing studio admin burnout.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the studio administration use case (e.g.\n\"You are a senior studio administration mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for studio administration.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance studio management",
      "sam": "$2.5B — recreational youth dance studios",
      "som": "$80M — large multi-branch dance academies"
    },
    {
      "id": "dance-rehearsal-whisper-scribe-0",
      "theme": "dance",
      "title": "Rehearsal Whisper Scribe",
      "pitch": "Lovable AI organizes notes so choreographers focus; ElevenLabs captures it.",
      "subDiscipline": "choreographic notation",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT allows choreographers to dictate corrections without stopping the dancers.",
      "megaPrompt": "Build \"Rehearsal Whisper Scribe\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI organizes notes so choreographers focus; ElevenLabs captures it.\nDiscipline: Dance & Choreography (choreographic notation).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT allows choreographers to dictate corrections without stopping the dancers.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for choreographic notation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken choreographic notation notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for choreographic notation.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$500M — professional dance companies",
      "som": "$50M — contemporary choreographers"
    },
    {
      "id": "dance-groove-count-transcriber-1",
      "theme": "dance",
      "title": "Groove Count Transcriber",
      "pitch": "Lovable AI maps rhythms so dancers learn musicality; ElevenLabs transcribes it.",
      "subDiscipline": "musicality training",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Word timestamps align spoken counts precisely with musical beats for analysis.",
      "megaPrompt": "Build \"Groove Count Transcriber\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI maps rhythms so dancers learn musicality; ElevenLabs transcribes it.\nDiscipline: Dance & Choreography (musicality training).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Word timestamps align spoken counts precisely with musical beats for analysis.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for musicality training:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken musicality training notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for musicality training.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$1B — dance education market",
      "som": "$100M — hip-hop studios"
    },
    {
      "id": "dance-somatic-echo-journal-2",
      "theme": "dance",
      "title": "Somatic Echo Journal",
      "pitch": "Lovable AI designs recovery so dancers heal; ElevenLabs captures it.",
      "subDiscipline": "dancer recovery",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime transcription lets exhausted dancers verbally log physical sensations immediately post-rehearsal.",
      "megaPrompt": "Build \"Somatic Echo Journal\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI designs recovery so dancers heal; ElevenLabs captures it.\nDiscipline: Dance & Choreography (dancer recovery).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime transcription lets exhausted dancers verbally log physical sensations immediately post-rehearsal.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for dancer recovery:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken dancer recovery notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for dancer recovery.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$800M — dance wellness and physio",
      "som": "$80M — elite ballet dancers"
    },
    {
      "id": "dance-improv-motif-catcher-3",
      "theme": "dance",
      "title": "Improv Motif Catcher",
      "pitch": "Lovable AI extracts motifs so creators build routines; ElevenLabs transcribes it.",
      "subDiscipline": "movement generation",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Capturing stream-of-consciousness speech during improv helps anchor fleeting physical ideas into text.",
      "megaPrompt": "Build \"Improv Motif Catcher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI extracts motifs so creators build routines; ElevenLabs transcribes it.\nDiscipline: Dance & Choreography (movement generation).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Capturing stream-of-consciousness speech during improv helps anchor fleeting physical ideas into text.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for movement generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken movement generation notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for movement generation.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$300M — experimental dance scene",
      "som": "$30M — solo improvisers"
    },
    {
      "id": "dance-studio-cue-weaver-4",
      "theme": "dance",
      "title": "Studio Cue Weaver",
      "pitch": "Lovable AI generates handouts so teachers instruct; ElevenLabs captures it.",
      "subDiscipline": "dance pedagogy",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "STT records rapid-fire teaching cues to later distill into structured learning materials.",
      "megaPrompt": "Build \"Studio Cue Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI generates handouts so teachers instruct; ElevenLabs captures it.\nDiscipline: Dance & Choreography (dance pedagogy).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: STT records rapid-fire teaching cues to later distill into structured learning materials.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for dance pedagogy:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken dance pedagogy notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for dance pedagogy.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$2B — global dance studios",
      "som": "$200M — children's dance academies"
    },
    {
      "id": "dance-libretto-breath-drafter-5",
      "theme": "dance",
      "title": "Libretto Breath Drafter",
      "pitch": "Lovable AI writes story beats so directors stage ballets; ElevenLabs captures it.",
      "subDiscipline": "narrative ballet",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT lets directors pace their storytelling aloud, matching the theatrical rhythm.",
      "megaPrompt": "Build \"Libretto Breath Drafter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI writes story beats so directors stage ballets; ElevenLabs captures it.\nDiscipline: Dance & Choreography (narrative ballet).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT lets directors pace their storytelling aloud, matching the theatrical rhythm.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for narrative ballet:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken narrative ballet notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for narrative ballet.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$400M — classical ballet companies",
      "som": "$40M — narrative choreographers"
    },
    {
      "id": "dance-stage-block-mapper-6",
      "theme": "dance",
      "title": "Stage Block Mapper",
      "pitch": "Lovable AI maps positions so directors block scenes; ElevenLabs transcribes it.",
      "subDiscipline": "spatial choreography",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Spoken spatial cues are instantly transcribed to generate visual blocking diagrams.",
      "megaPrompt": "Build \"Stage Block Mapper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI maps positions so directors block scenes; ElevenLabs transcribes it.\nDiscipline: Dance & Choreography (spatial choreography).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Spoken spatial cues are instantly transcribed to generate visual blocking diagrams.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for spatial choreography:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken spatial choreography notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for spatial choreography.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$600M — musical theater dance",
      "som": "$60M — Broadway movement directors"
    },
    {
      "id": "dance-stamina-pacing-coach-7",
      "theme": "dance",
      "title": "Stamina Pacing Coach",
      "pitch": "Lovable AI designs stamina training so dancers endure sets; ElevenLabs captures it.",
      "subDiscipline": "performance conditioning",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Timestamped breath vocalizations allow the AI to analyze respiratory pacing during routines.",
      "megaPrompt": "Build \"Stamina Pacing Coach\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI designs stamina training so dancers endure sets; ElevenLabs captures it.\nDiscipline: Dance & Choreography (performance conditioning).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Timestamped breath vocalizations allow the AI to analyze respiratory pacing during routines.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for performance conditioning:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken performance conditioning notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for performance conditioning.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$200M — dance fitness",
      "som": "$20M — commercial backup dancers"
    },
    {
      "id": "dance-audition-critique-distiller-8",
      "theme": "dance",
      "title": "Audition Critique Distiller",
      "pitch": "Lovable AI summarizes feedback so dancers improve; ElevenLabs captures it.",
      "subDiscipline": "talent evaluation",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT records nuanced verbal auditions feedback for immediate, structured review.",
      "megaPrompt": "Build \"Audition Critique Distiller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI summarizes feedback so dancers improve; ElevenLabs captures it.\nDiscipline: Dance & Choreography (talent evaluation).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT records nuanced verbal auditions feedback for immediate, structured review.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for talent evaluation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken talent evaluation notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for talent evaluation.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$150M — dance competitions",
      "som": "$15M — competition judges"
    },
    {
      "id": "dance-heritage-step-archivist-9",
      "theme": "dance",
      "title": "Heritage Step Archivist",
      "pitch": "Lovable AI creates study guides so students learn history; ElevenLabs transcribes it.",
      "subDiscipline": "cultural dance preservation",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "STT accurately captures the oral traditions and nuanced histories of folk dances.",
      "megaPrompt": "Build \"Heritage Step Archivist\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI creates study guides so students learn history; ElevenLabs transcribes it.\nDiscipline: Dance & Choreography (cultural dance preservation).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: STT accurately captures the oral traditions and nuanced histories of folk dances.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for cultural dance preservation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken cultural dance preservation notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for cultural dance preservation.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$100M — cultural dance troupes",
      "som": "$10M — folkloric educators"
    },
    {
      "id": "dance-costume-friction-logger-10",
      "theme": "dance",
      "title": "Costume Friction Logger",
      "pitch": "Lovable AI drafts tailor notes so designers adjust fits; ElevenLabs captures it.",
      "subDiscipline": "wardrobe design",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Dancers can verbally report garment restrictions mid-rehearsal without breaking physical flow.",
      "megaPrompt": "Build \"Costume Friction Logger\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI drafts tailor notes so designers adjust fits; ElevenLabs captures it.\nDiscipline: Dance & Choreography (wardrobe design).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Dancers can verbally report garment restrictions mid-rehearsal without breaking physical flow.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for wardrobe design:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken wardrobe design notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for wardrobe design.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$50M — dancewear manufacturing",
      "som": "$5M — theatrical costumers"
    },
    {
      "id": "dance-lighting-state-caller-11",
      "theme": "dance",
      "title": "Lighting State Caller",
      "pitch": "Lovable AI writes cue sheets so technicians run shows; ElevenLabs transcribes it.",
      "subDiscipline": "technical production",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "STT captures the lighting designer's spoken cues during tech rehearsals in real-time.",
      "megaPrompt": "Build \"Lighting State Caller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI writes cue sheets so technicians run shows; ElevenLabs transcribes it.\nDiscipline: Dance & Choreography (technical production).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: STT captures the lighting designer's spoken cues during tech rehearsals in real-time.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for technical production:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken technical production notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for technical production.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$800M — live event production",
      "som": "$80M — dance festival techs"
    },
    {
      "id": "dance-mindset-script-refiner-12",
      "theme": "dance",
      "title": "Mindset Script Refiner",
      "pitch": "Lovable AI refines visualization scripts so dancers focus; ElevenLabs captures it.",
      "subDiscipline": "sports psychology",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Speaking visualizations aloud helps dancers externalize focus, which STT captures for refinement.",
      "megaPrompt": "Build \"Mindset Script Refiner\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI refines visualization scripts so dancers focus; ElevenLabs captures it.\nDiscipline: Dance & Choreography (sports psychology).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Speaking visualizations aloud helps dancers externalize focus, which STT captures for refinement.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for sports psychology:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken sports psychology notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for sports psychology.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$50M — performance psychology",
      "som": "$5M — elite soloists"
    },
    {
      "id": "dance-tap-rhythm-notator-13",
      "theme": "dance",
      "title": "Tap Rhythm Notator",
      "pitch": "Lovable AI transcribes footwork so tap dancers teach; ElevenLabs captures it.",
      "subDiscipline": "percussive dance",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "STT captures the complex, rapid vocalizations of tap rhythms for structural analysis.",
      "megaPrompt": "Build \"Tap Rhythm Notator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI transcribes footwork so tap dancers teach; ElevenLabs captures it.\nDiscipline: Dance & Choreography (percussive dance).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: STT captures the complex, rapid vocalizations of tap rhythms for structural analysis.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for percussive dance:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken percussive dance notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for percussive dance.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$75M — tap dance studios",
      "som": "$7.5M — tap educators"
    },
    {
      "id": "dance-partner-weight-checker-14",
      "theme": "dance",
      "title": "Partner Weight Checker",
      "pitch": "Lovable AI creates safety checklists so lifters prevent injury; ElevenLabs captures it.",
      "subDiscipline": "pas de deux safety",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT logs the technical negotiations between partners regarding weight distribution.",
      "megaPrompt": "Build \"Partner Weight Checker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI creates safety checklists so lifters prevent injury; ElevenLabs captures it.\nDiscipline: Dance & Choreography (pas de deux safety).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT logs the technical negotiations between partners regarding weight distribution.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for pas de deux safety:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken pas de deux safety notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for pas de deux safety.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$400M — classical ballet",
      "som": "$40M — partnering coaches"
    },
    {
      "id": "dance-floor-texture-assessor-15",
      "theme": "dance",
      "title": "Floor Texture Assessor",
      "pitch": "Lovable AI recommends joint care so dancers adapt; ElevenLabs captures it.",
      "subDiscipline": "environmental adaptation",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Dancers verbally describe floor friction and shock absorption immediately upon stepping onstage.",
      "megaPrompt": "Build \"Floor Texture Assessor\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI recommends joint care so dancers adapt; ElevenLabs captures it.\nDiscipline: Dance & Choreography (environmental adaptation).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Dancers verbally describe floor friction and shock absorption immediately upon stepping onstage.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for environmental adaptation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken environmental adaptation notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for environmental adaptation.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$100M — dance flooring",
      "som": "$10M — touring companies"
    },
    {
      "id": "dance-crowd-vibe-adjuster-16",
      "theme": "dance",
      "title": "Crowd Vibe Adjuster",
      "pitch": "Lovable AI adjusts setlist pacing so performers engage audiences; ElevenLabs captures it.",
      "subDiscipline": "live performance pacing",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "STT monitors ambient crowd noise and performer ad-libs to gauge live energy.",
      "megaPrompt": "Build \"Crowd Vibe Adjuster\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI adjusts setlist pacing so performers engage audiences; ElevenLabs captures it.\nDiscipline: Dance & Choreography (live performance pacing).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: STT monitors ambient crowd noise and performer ad-libs to gauge live energy.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for live performance pacing:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken live performance pacing notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for live performance pacing.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$1B — commercial dance tours",
      "som": "$100M — club performers"
    },
    {
      "id": "dance-audio-description-weaver-17",
      "theme": "dance",
      "title": "Audio Description Weaver",
      "pitch": "Lovable AI writes descriptive scripts so blind audiences enjoy shows; ElevenLabs captures it.",
      "subDiscipline": "accessible performance",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Choreographers verbally describe their work, which STT captures to draft accessibility audio.",
      "megaPrompt": "Build \"Audio Description Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI writes descriptive scripts so blind audiences enjoy shows; ElevenLabs captures it.\nDiscipline: Dance & Choreography (accessible performance).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Choreographers verbally describe their work, which STT captures to draft accessibility audio.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for accessible performance:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken accessible performance notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for accessible performance.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$200M — accessible arts initiatives",
      "som": "$20M — inclusive dance troupes"
    },
    {
      "id": "dance-dance-film-scoper-18",
      "theme": "dance",
      "title": "Dance Film Scoper",
      "pitch": "Lovable AI drafts shot lists so directors film routines; ElevenLabs captures it.",
      "subDiscipline": "screen choreography",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "STT records the director's spontaneous camera framing instructions during location scouting.",
      "megaPrompt": "Build \"Dance Film Scoper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI drafts shot lists so directors film routines; ElevenLabs captures it.\nDiscipline: Dance & Choreography (screen choreography).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: STT records the director's spontaneous camera framing instructions during location scouting.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for screen choreography:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken screen choreography notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for screen choreography.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$300M — dance media production",
      "som": "$30M — screen choreographers"
    },
    {
      "id": "dance-call-sheet-dictator-19",
      "theme": "dance",
      "title": "Call Sheet Dictator",
      "pitch": "Lovable AI updates schedules so stage managers organize rehearsals; ElevenLabs captures it.",
      "subDiscipline": "production management",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT allows managers to dictate schedule shifts hands-free during chaotic tech weeks.",
      "megaPrompt": "Build \"Call Sheet Dictator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI updates schedules so stage managers organize rehearsals; ElevenLabs captures it.\nDiscipline: Dance & Choreography (production management).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT allows managers to dictate schedule shifts hands-free during chaotic tech weeks.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for production management:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken production management notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for production management.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$500M — dance production management",
      "som": "$50M — company managers"
    },
    {
      "id": "dance-warmup-muscle-logger-20",
      "theme": "dance",
      "title": "Warmup Muscle Logger",
      "pitch": "Lovable AI logs targeted muscles so captains lead warmups; ElevenLabs captures it.",
      "subDiscipline": "company class",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "STT transcribes the anatomical cues given during company class for physiological tracking.",
      "megaPrompt": "Build \"Warmup Muscle Logger\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI logs targeted muscles so captains lead warmups; ElevenLabs captures it.\nDiscipline: Dance & Choreography (company class).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: STT transcribes the anatomical cues given during company class for physiological tracking.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for company class:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken company class notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for company class.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$800M — professional dance companies",
      "som": "$80M — dance captains"
    },
    {
      "id": "dance-structure-block-namer-21",
      "theme": "dance",
      "title": "Structure Block Namer",
      "pitch": "Lovable AI builds structural maps so choreographers organize pieces; ElevenLabs captures it.",
      "subDiscipline": "choreographic architecture",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Creators verbally name and sequence dance blocks, which STT logs into a timeline.",
      "megaPrompt": "Build \"Structure Block Namer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI builds structural maps so choreographers organize pieces; ElevenLabs captures it.\nDiscipline: Dance & Choreography (choreographic architecture).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Creators verbally name and sequence dance blocks, which STT logs into a timeline.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for choreographic architecture:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken choreographic architecture notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for choreographic architecture.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$300M — contemporary dance",
      "som": "$30M — independent choreographers"
    },
    {
      "id": "dance-injury-modification-drafter-22",
      "theme": "dance",
      "title": "Injury Modification Drafter",
      "pitch": "Lovable AI rewrites choreography so injured dancers participate; ElevenLabs captures it.",
      "subDiscipline": "adaptive choreography",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Dancers explain their physical restrictions aloud, allowing AI to suggest safe movement alternatives.",
      "megaPrompt": "Build \"Injury Modification Drafter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI rewrites choreography so injured dancers participate; ElevenLabs captures it.\nDiscipline: Dance & Choreography (adaptive choreography).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Dancers explain their physical restrictions aloud, allowing AI to suggest safe movement alternatives.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for adaptive choreography:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken adaptive choreography notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for adaptive choreography.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$150M — dance medicine",
      "som": "$15M — rehabilitating dancers"
    },
    {
      "id": "dance-post-show-echo-23",
      "theme": "dance",
      "title": "Post Show Echo",
      "pitch": "Lovable AI extracts morale insights so directors support casts; ElevenLabs captures it.",
      "subDiscipline": "company culture",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "STT records the emotional and verbal debriefs after a performance for sentiment analysis.",
      "megaPrompt": "Build \"Post Show Echo\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI extracts morale insights so directors support casts; ElevenLabs captures it.\nDiscipline: Dance & Choreography (company culture).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: STT records the emotional and verbal debriefs after a performance for sentiment analysis.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for company culture:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken company culture notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for company culture.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$500M — professional dance companies",
      "som": "$50M — artistic directors"
    },
    {
      "id": "dance-grant-pitch-polisher-24",
      "theme": "dance",
      "title": "Grant Pitch Polisher",
      "pitch": "Lovable AI refines proposal text so artists secure funding; ElevenLabs captures it.",
      "subDiscipline": "arts funding",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "STT records the artist's passionate verbal pitches, transforming spoken ideas into formal grants.",
      "megaPrompt": "Build \"Grant Pitch Polisher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI refines proposal text so artists secure funding; ElevenLabs captures it.\nDiscipline: Dance & Choreography (arts funding).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: STT records the artist's passionate verbal pitches, transforming spoken ideas into formal grants.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for arts funding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken arts funding notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for arts funding.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry",
      "sam": "$2B — arts grants and funding",
      "som": "$200M — dance non-profits"
    },
    {
      "id": "dance-ballet-cue-weaver-0",
      "theme": "dance",
      "title": "Ballet Cue Weaver",
      "pitch": "Lovable AI sequences ballet corrections so dancers improve; ElevenLabs generates rhythmic SFX to guide pirouettes.",
      "subDiscipline": "rehearsal timing",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation provides precise, non-distracting audio cues for physical timing without needing full musical tracks.",
      "megaPrompt": "Build \"Ballet Cue Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI sequences ballet corrections so dancers improve; ElevenLabs generates rhythmic SFX to guide pirouettes.\nDiscipline: Dance & Choreography (rehearsal timing).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation provides precise, non-distracting audio cues for physical timing without needing full musical tracks.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for rehearsal timing. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for rehearsal timing.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-tap-foley-studio-1",
      "theme": "dance",
      "title": "Tap Foley Studio",
      "pitch": "Lovable AI designs tap routines so choreographers experiment; ElevenLabs synthesizes custom floor-strike SFX for rapid prototyping.",
      "subDiscipline": "tap percussion",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation perfectly mimics specific tap shoe and floor material interactions for instant acoustic feedback.",
      "megaPrompt": "Build \"Tap Foley Studio\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI designs tap routines so choreographers experiment; ElevenLabs synthesizes custom floor-strike SFX for rapid prototyping.\nDiscipline: Dance & Choreography (tap percussion).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation perfectly mimics specific tap shoe and floor material interactions for instant acoustic feedback.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for tap percussion. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for tap percussion.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-contemporary-breath-scorer-2",
      "theme": "dance",
      "title": "Contemporary Breath Scorer",
      "pitch": "Lovable AI maps contemporary floorwork so dancers connect; ElevenLabs generates ambient breathing SFX to match descents.",
      "subDiscipline": "movement scoring",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Short SFX generation captures the visceral, intimate sounds of breath and friction essential to contemporary grounding.",
      "megaPrompt": "Build \"Contemporary Breath Scorer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI maps contemporary floorwork so dancers connect; ElevenLabs generates ambient breathing SFX to match descents.\nDiscipline: Dance & Choreography (movement scoring).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Short SFX generation captures the visceral, intimate sounds of breath and friction essential to contemporary grounding.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for movement scoring. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for movement scoring.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-cypher-beat-drafter-3",
      "theme": "dance",
      "title": "Cypher Beat Drafter",
      "pitch": "Lovable AI structures hip-hop sets so crews practice; ElevenLabs generates heavy bass studio tracks to fuel energy.",
      "subDiscipline": "hip-hop choreography",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Music generation creates full, high-energy studio tracks tailored to the specific BPM and mood of street dance.",
      "megaPrompt": "Build \"Cypher Beat Drafter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI structures hip-hop sets so crews practice; ElevenLabs generates heavy bass studio tracks to fuel energy.\nDiscipline: Dance & Choreography (hip-hop choreography).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Music generation creates full, high-energy studio tracks tailored to the specific BPM and mood of street dance.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for hip-hop choreography. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for hip-hop choreography.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-flamenco-clap-synthesizer-4",
      "theme": "dance",
      "title": "Flamenco Clap Synthesizer",
      "pitch": "Lovable AI charts complex palmas so students learn; ElevenLabs produces layered handclap SFX to reinforce compás.",
      "subDiscipline": "flamenco rhythm",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation accurately reproduces the sharp, syncopated acoustic transients of traditional flamenco handclapping.",
      "megaPrompt": "Build \"Flamenco Clap Synthesizer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI charts complex palmas so students learn; ElevenLabs produces layered handclap SFX to reinforce compás.\nDiscipline: Dance & Choreography (flamenco rhythm).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation accurately reproduces the sharp, syncopated acoustic transients of traditional flamenco handclapping.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for flamenco rhythm. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for flamenco rhythm.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-somatic-resonance-guide-5",
      "theme": "dance",
      "title": "Somatic Resonance Guide",
      "pitch": "Lovable AI designs somatic flows so patients heal; ElevenLabs generates gentle nature SFX to deepen grounding.",
      "subDiscipline": "dance therapy",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation creates soothing, non-rhythmic environmental audio that supports emotional regulation during therapeutic movement.",
      "megaPrompt": "Build \"Somatic Resonance Guide\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI designs somatic flows so patients heal; ElevenLabs generates gentle nature SFX to deepen grounding.\nDiscipline: Dance & Choreography (dance therapy).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation creates soothing, non-rhythmic environmental audio that supports emotional regulation during therapeutic movement.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for dance therapy. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for dance therapy.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-pole-friction-cues-6",
      "theme": "dance",
      "title": "Pole Friction Cues",
      "pitch": "Lovable AI plans pole transitions so athletes flow; ElevenLabs generates subtle grip SFX to cue placements.",
      "subDiscipline": "pole choreography",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation provides micro-audio feedback mimicking skin-on-metal friction, aiding spatial awareness during inverted spins.",
      "megaPrompt": "Build \"Pole Friction Cues\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI plans pole transitions so athletes flow; ElevenLabs generates subtle grip SFX to cue placements.\nDiscipline: Dance & Choreography (pole choreography).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation provides micro-audio feedback mimicking skin-on-metal friction, aiding spatial awareness during inverted spins.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for pole choreography. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for pole choreography.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-ballroom-tempo-ghost-7",
      "theme": "dance",
      "title": "Ballroom Tempo Ghost",
      "pitch": "Lovable AI breaks down waltz timing so couples sync; ElevenLabs generates sweeping orchestral SFX highlighting downbeats.",
      "subDiscipline": "partner dancing",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation isolates specific orchestral swells to emphasize musicality and phrasing without overwhelming the dancers.",
      "megaPrompt": "Build \"Ballroom Tempo Ghost\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI breaks down waltz timing so couples sync; ElevenLabs generates sweeping orchestral SFX highlighting downbeats.\nDiscipline: Dance & Choreography (partner dancing).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation isolates specific orchestral swells to emphasize musicality and phrasing without overwhelming the dancers.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for partner dancing. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for partner dancing.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-prosthetic-servo-scorer-8",
      "theme": "dance",
      "title": "Prosthetic Servo Scorer",
      "pitch": "Lovable AI choreographs integrated routines so performers collaborate; ElevenLabs generates rhythmic servo SFX blending with movements.",
      "subDiscipline": "integrated dance",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation creates mechanical soundscapes that celebrate and rhythmically integrate the acoustic reality of prosthetic limbs.",
      "megaPrompt": "Build \"Prosthetic Servo Scorer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI choreographs integrated routines so performers collaborate; ElevenLabs generates rhythmic servo SFX blending with movements.\nDiscipline: Dance & Choreography (integrated dance).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation creates mechanical soundscapes that celebrate and rhythmically integrate the acoustic reality of prosthetic limbs.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for integrated dance. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for integrated dance.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-storybook-step-maker-9",
      "theme": "dance",
      "title": "Storybook Step Maker",
      "pitch": "Lovable AI invents animal dances so kids engage; ElevenLabs generates playful creature SFX sparking imagination.",
      "subDiscipline": "children's dance",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation produces fun, recognizable animal and magic sounds that keep young dancers entertained and moving.",
      "megaPrompt": "Build \"Storybook Step Maker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI invents animal dances so kids engage; ElevenLabs generates playful creature SFX sparking imagination.\nDiscipline: Dance & Choreography (children's dance).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation produces fun, recognizable animal and magic sounds that keep young dancers entertained and moving.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for children's dance. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for children's dance.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-screendance-audio-weaver-10",
      "theme": "dance",
      "title": "Screendance Audio Weaver",
      "pitch": "Lovable AI storyboards screendance scenes so directors shoot; ElevenLabs generates cinematic foley SFX enhancing visual edits.",
      "subDiscipline": "dance film",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation provides bespoke environmental and movement foley crucial for the post-production of dance films.",
      "megaPrompt": "Build \"Screendance Audio Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI storyboards screendance scenes so directors shoot; ElevenLabs generates cinematic foley SFX enhancing visual edits.\nDiscipline: Dance & Choreography (dance film).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation provides bespoke environmental and movement foley crucial for the post-production of dance films.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for dance film. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for dance film.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-silk-tension-sonifier-11",
      "theme": "dance",
      "title": "Silk Tension Sonifier",
      "pitch": "Lovable AI maps aerial drops so riggers check safety; ElevenLabs generates fabric-stretch SFX indicating tension levels.",
      "subDiscipline": "aerial arts",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation translates physical tension into auditory cues, helping aerialists anticipate the snap and sway of silk.",
      "megaPrompt": "Build \"Silk Tension Sonifier\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI maps aerial drops so riggers check safety; ElevenLabs generates fabric-stretch SFX indicating tension levels.\nDiscipline: Dance & Choreography (aerial arts).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation translates physical tension into auditory cues, helping aerialists anticipate the snap and sway of silk.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for aerial arts. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for aerial arts.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-bboy-windmill-metronome-12",
      "theme": "dance",
      "title": "Bboy Windmill Metronome",
      "pitch": "Lovable AI sequences power moves so breakers build stamina; ElevenLabs generates sweeping whoosh SFX pacing rotations.",
      "subDiscipline": "breaking",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation creates continuous, looping aerodynamic sounds that help breakers maintain momentum during continuous floor rotations.",
      "megaPrompt": "Build \"Bboy Windmill Metronome\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI sequences power moves so breakers build stamina; ElevenLabs generates sweeping whoosh SFX pacing rotations.\nDiscipline: Dance & Choreography (breaking).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation creates continuous, looping aerodynamic sounds that help breakers maintain momentum during continuous floor rotations.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for breaking. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for breaking.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-labanotation-sound-translator-13",
      "theme": "dance",
      "title": "Labanotation Sound Translator",
      "pitch": "Lovable AI reads Labanotation scores so archivists preserve history; ElevenLabs generates abstract tonal SFX representing pathways.",
      "subDiscipline": "dance notation",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation maps abstract choreographic symbols to distinct audio textures, making historical notation accessible via sound.",
      "megaPrompt": "Build \"Labanotation Sound Translator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI reads Labanotation scores so archivists preserve history; ElevenLabs generates abstract tonal SFX representing pathways.\nDiscipline: Dance & Choreography (dance notation).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation maps abstract choreographic symbols to distinct audio textures, making historical notation accessible via sound.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for dance notation. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for dance notation.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-wheelchair-drift-scorer-14",
      "theme": "dance",
      "title": "Wheelchair Drift Scorer",
      "pitch": "Lovable AI designs wheelchair formations so troupes synchronize; ElevenLabs generates rhythmic tire-drift SFX accentuating floor patterns.",
      "subDiscipline": "wheelchair dance",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation captures the unique acoustic signature of wheelchair movement, turning mechanical friction into a percussive instrument.",
      "megaPrompt": "Build \"Wheelchair Drift Scorer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI designs wheelchair formations so troupes synchronize; ElevenLabs generates rhythmic tire-drift SFX accentuating floor patterns.\nDiscipline: Dance & Choreography (wheelchair dance).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation captures the unique acoustic signature of wheelchair movement, turning mechanical friction into a percussive instrument.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for wheelchair dance. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for wheelchair dance.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-studio-warmup-pulse-15",
      "theme": "dance",
      "title": "Studio Warmup Pulse",
      "pitch": "Lovable AI builds dynamic warmups so teachers save time; ElevenLabs generates escalating heartbeat SFX raising pulses.",
      "subDiscipline": "class instruction",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation uses biological rhythms to subconsciously guide dancers' heart rates and breathing during physical preparation.",
      "megaPrompt": "Build \"Studio Warmup Pulse\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI builds dynamic warmups so teachers save time; ElevenLabs generates escalating heartbeat SFX raising pulses.\nDiscipline: Dance & Choreography (class instruction).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation uses biological rhythms to subconsciously guide dancers' heart rates and breathing during physical preparation.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for class instruction. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for class instruction.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-haka-stomp-archiver-16",
      "theme": "dance",
      "title": "Haka Stomp Archiver",
      "pitch": "Lovable AI documents traditional stomps so elders teach; ElevenLabs generates deep earth-shaking SFX honoring ancestral impact.",
      "subDiscipline": "indigenous dance",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation replicates the low-frequency acoustic resonance of mass foot-stomping essential to cultural war dances.",
      "megaPrompt": "Build \"Haka Stomp Archiver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI documents traditional stomps so elders teach; ElevenLabs generates deep earth-shaking SFX honoring ancestral impact.\nDiscipline: Dance & Choreography (indigenous dance).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation replicates the low-frequency acoustic resonance of mass foot-stomping essential to cultural war dances.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for indigenous dance. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for indigenous dance.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-tutu-rustle-designer-17",
      "theme": "dance",
      "title": "Tutu Rustle Designer",
      "pitch": "Lovable AI selects costume fabrics so designers visualize; ElevenLabs generates specific material-rustle SFX previewing acoustic drape.",
      "subDiscipline": "costume integration",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation allows designers to hear how different textiles will sound during choreography before cutting the fabric.",
      "megaPrompt": "Build \"Tutu Rustle Designer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI selects costume fabrics so designers visualize; ElevenLabs generates specific material-rustle SFX previewing acoustic drape.\nDiscipline: Dance & Choreography (costume integration).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation allows designers to hear how different textiles will sound during choreography before cutting the fabric.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for costume integration. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for costume integration.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-contact-jam-resonator-18",
      "theme": "dance",
      "title": "Contact Jam Resonator",
      "pitch": "Lovable AI suggests weight-sharing prompts so improvisers explore; ElevenLabs generates sustained drone music holding their focus.",
      "subDiscipline": "contact improvisation",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Music generation provides continuous, evolving drone tracks that support the unpredictable, flowing nature of contact improvisation.",
      "megaPrompt": "Build \"Contact Jam Resonator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI suggests weight-sharing prompts so improvisers explore; ElevenLabs generates sustained drone music holding their focus.\nDiscipline: Dance & Choreography (contact improvisation).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Music generation provides continuous, evolving drone tracks that support the unpredictable, flowing nature of contact improvisation.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for contact improvisation. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for contact improvisation.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-stage-cue-sonifier-19",
      "theme": "dance",
      "title": "Stage Cue Sonifier",
      "pitch": "Lovable AI times lighting shifts so technicians rehearse; ElevenLabs generates subtle electrical hum SFX signaling cues.",
      "subDiscipline": "theatrical lighting",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation provides discreet audio markers for lighting operators to sync their board transitions with stage movement.",
      "megaPrompt": "Build \"Stage Cue Sonifier\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI times lighting shifts so technicians rehearse; ElevenLabs generates subtle electrical hum SFX signaling cues.\nDiscipline: Dance & Choreography (theatrical lighting).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation provides discreet audio markers for lighting operators to sync their board transitions with stage movement.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for theatrical lighting. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for theatrical lighting.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-cheer-stunt-metronome-20",
      "theme": "dance",
      "title": "Cheer Stunt Metronome",
      "pitch": "Lovable AI counts complex pyramids so flyers stay safe; ElevenLabs generates sharp whistle SFX marking releases.",
      "subDiscipline": "cheerleading",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation produces piercing, high-transient audio cues that cut through loud gymnasiums during critical stunt releases.",
      "megaPrompt": "Build \"Cheer Stunt Metronome\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI counts complex pyramids so flyers stay safe; ElevenLabs generates sharp whistle SFX marking releases.\nDiscipline: Dance & Choreography (cheerleading).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation produces piercing, high-transient audio cues that cut through loud gymnasiums during critical stunt releases.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for cheerleading. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for cheerleading.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-battle-crowd-simulator-21",
      "theme": "dance",
      "title": "Battle Crowd Simulator",
      "pitch": "Lovable AI scripts battle reactions so MCs practice; ElevenLabs generates dynamic crowd-cheer SFX simulating cypher energy.",
      "subDiscipline": "battle culture",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation creates realistic, reactive audience noise to help dancers and MCs practice feeding off crowd energy.",
      "megaPrompt": "Build \"Battle Crowd Simulator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI scripts battle reactions so MCs practice; ElevenLabs generates dynamic crowd-cheer SFX simulating cypher energy.\nDiscipline: Dance & Choreography (battle culture).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation creates realistic, reactive audience noise to help dancers and MCs practice feeding off crowd energy.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for battle culture. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for battle culture.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-pointe-box-molder-22",
      "theme": "dance",
      "title": "Pointe Box Molder",
      "pitch": "Lovable AI guides shoe preparation so dancers avoid injury; ElevenLabs generates satisfying crackle SFX indicating flexibility.",
      "subDiscipline": "ballet preparation",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation provides auditory feedback on the structural breakdown of pointe shoes, ensuring optimal support and articulation.",
      "megaPrompt": "Build \"Pointe Box Molder\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI guides shoe preparation so dancers avoid injury; ElevenLabs generates satisfying crackle SFX indicating flexibility.\nDiscipline: Dance & Choreography (ballet preparation).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation provides auditory feedback on the structural breakdown of pointe shoes, ensuring optimal support and articulation.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for ballet preparation. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for ballet preparation.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-flashmob-sync-pulse-23",
      "theme": "dance",
      "title": "Flashmob Sync Pulse",
      "pitch": "Lovable AI coordinates public formations so amateurs align; ElevenLabs generates low-frequency sub SFX keeping crowds synchronized.",
      "subDiscipline": "public performance",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation utilizes sub-bass frequencies that can be felt physically, helping large groups stay in time outdoors.",
      "megaPrompt": "Build \"Flashmob Sync Pulse\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI coordinates public formations so amateurs align; ElevenLabs generates low-frequency sub SFX keeping crowds synchronized.\nDiscipline: Dance & Choreography (public performance).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation utilizes sub-bass frequencies that can be felt physically, helping large groups stay in time outdoors.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for public performance. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for public performance.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-rehab-gait-scorer-24",
      "theme": "dance",
      "title": "Rehab Gait Scorer",
      "pitch": "Lovable AI analyzes recovery steps so dancers heal; ElevenLabs generates rhythmic chime SFX encouraging symmetrical transfer.",
      "subDiscipline": "physical therapy",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation provides gentle, positive auditory reinforcement to help dancers rebuild confidence and symmetry after lower-body injuries.",
      "megaPrompt": "Build \"Rehab Gait Scorer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI analyzes recovery steps so dancers heal; ElevenLabs generates rhythmic chime SFX encouraging symmetrical transfer.\nDiscipline: Dance & Choreography (physical therapy).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation provides gentle, positive auditory reinforcement to help dancers rebuild confidence and symmetry after lower-body injuries.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for physical therapy. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for physical therapy.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    }
  ]
}