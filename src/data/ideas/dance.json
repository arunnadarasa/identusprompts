{
  "theme": {
    "slug": "dance",
    "name": "Dance & Choreography",
    "emoji": "💃",
    "audience": "choreographers, dancers, dance teachers, movement directors",
    "market_anchor": "the global dance industry (~$5B; >2M studios worldwide)"
  },
  "ideas": [
    {
      "id": "dance-choreographer-credentials-0",
      "theme": "dance",
      "title": "Choreographer Credentials",
      "pitch": "Choreographer Credentials mints a did:prism for dance education so identity travels with the person, not the platform.",
      "concept": "Choreographers publish their credentials to verify their expertise to studios looking to hire qualified teachers.",
      "subDiscipline": "dance education",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits dance education in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Choreographer Credentials\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nChoreographers publish their credentials to verify their expertise to studios looking to hire qualified teachers.\nDiscipline: Dance & Choreography (dance education).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits dance education in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance studio market · SAM $1.2B — classical ballet training sector · SOM $50M — elite ballet academy software\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance studio market",
      "sam": "$1.2B — classical ballet training sector",
      "som": "$50M — elite ballet academy software"
    },
    {
      "id": "dance-collaboration-hub-0",
      "theme": "dance",
      "title": "Collaboration Hub",
      "pitch": "Collaboration Hub pairs two wallets over DIDComm so choreographer connections can exchange trusted claims on a private channel.",
      "concept": "Choreographers create and manage joint projects by proving their identity and experience to potential collaborators.",
      "subDiscipline": "choreographer connections",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits choreographer connections in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Collaboration Hub\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nChoreographers create and manage joint projects by proving their identity and experience to potential collaborators.\nDiscipline: Dance & Choreography (choreographer connections).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits choreographer connections in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $800M — contemporary dance education · SOM $40M — university dance programs\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$800M — contemporary dance education",
      "som": "$40M — university dance programs"
    },
    {
      "id": "dance-choreographer-credentials-1",
      "theme": "dance",
      "title": "Choreographer Credentials",
      "pitch": "Choreographer Credentials issues a signed verifiable credential for dance education that the holder keeps and reuses anywhere.",
      "concept": "Choreographers issue verified credentials proving their training and experience to dance studios seeking qualified instructors.",
      "subDiscipline": "dance education",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches dance education in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Choreographer Credentials\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nChoreographers issue verified credentials proving their training and experience to dance studios seeking qualified instructors.\nDiscipline: Dance & Choreography (dance education).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches dance education in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance market · SAM $300M — tap dance studios · SOM $15M — competitive tap teams\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance market",
      "sam": "$300M — tap dance studios",
      "som": "$15M — competitive tap teams"
    },
    {
      "id": "dance-choreographer-credentials-2",
      "theme": "dance",
      "title": "Choreographer Credentials",
      "pitch": "Choreographer Credentials verifies a credential at the gate for choreographer accreditation — proof without a phone call to the issuer.",
      "concept": "Dancers prove their choreographer's credentials to studios seeking verified choreographers for projects.",
      "subDiscipline": "choreographer accreditation",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for choreographer accreditation in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Choreographer Credentials\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers prove their choreographer's credentials to studios seeking verified choreographers for projects.\nDiscipline: Dance & Choreography (choreographer accreditation).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for choreographer accreditation in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $1B — hip-hop and street dance · SOM $60M — urban dance competitions\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$1B — hip-hop and street dance",
      "som": "$60M — urban dance competitions"
    },
    {
      "id": "dance-performance-access-pass-0",
      "theme": "dance",
      "title": "Performance Access Pass",
      "pitch": "Performance Access Pass mints a did:prism for event management so identity travels with the person, not the platform.",
      "concept": "Dancers verify their identity to gain secure access to backstage areas and rehearsals during performances.",
      "subDiscipline": "event management",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits event management in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Performance Access Pass\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers verify their identity to gain secure access to backstage areas and rehearsals during performances.\nDiscipline: Dance & Choreography (event management).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits event management in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $500M — professional choreography services · SOM $25M — commercial dance productions\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$500M — professional choreography services",
      "som": "$25M — commercial dance productions"
    },
    {
      "id": "dance-masterclass-access-0",
      "theme": "dance",
      "title": "Masterclass Access",
      "pitch": "Masterclass Access pairs two wallets over DIDComm so dance education can exchange trusted claims on a private channel.",
      "concept": "Dance teachers verify their credentials to grant access to exclusive masterclasses for aspiring dancers.",
      "subDiscipline": "dance education",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits dance education in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Masterclass Access\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDance teachers verify their credentials to grant access to exclusive masterclasses for aspiring dancers.\nDiscipline: Dance & Choreography (dance education).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits dance education in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $600M — dance education and academia · SOM $30M — dance history curriculums\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$600M — dance education and academia",
      "som": "$30M — dance history curriculums"
    },
    {
      "id": "dance-workshop-participation-proof-0",
      "theme": "dance",
      "title": "Workshop Participation Proof",
      "pitch": "Workshop Participation Proof issues a signed verifiable credential for dance workshops that the holder keeps and reuses anywhere.",
      "concept": "Participants receive verifiable credentials confirming attendance at workshops, which they can share with studios for future opportunities.",
      "subDiscipline": "dance workshops",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches dance workshops in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Workshop Participation Proof\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nParticipants receive verifiable credentials confirming attendance at workshops, which they can share with studios for future opportunities.\nDiscipline: Dance & Choreography (dance workshops).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches dance workshops in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $400M — dancer wellness and physiotherapy · SOM $20M — professional company healthcare\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$400M — dancer wellness and physiotherapy",
      "som": "$20M — professional company healthcare"
    },
    {
      "id": "dance-dance-teacher-licensing-0",
      "theme": "dance",
      "title": "Dance Teacher Licensing",
      "pitch": "Dance Teacher Licensing verifies a credential at the gate for dance education — proof without a phone call to the issuer.",
      "concept": "Teachers present proof of their certifications and ongoing education to schools hiring instructors.",
      "subDiscipline": "dance education",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for dance education in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Dance Teacher Licensing\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTeachers present proof of their certifications and ongoing education to schools hiring instructors.\nDiscipline: Dance & Choreography (dance education).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for dance education in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $700M — live dance production · SOM $35M — theatrical dance wardrobe\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$700M — live dance production",
      "som": "$35M — theatrical dance wardrobe"
    },
    {
      "id": "dance-collaboration-authenticator-0",
      "theme": "dance",
      "title": "Collaboration Authenticator",
      "pitch": "Collaboration Authenticator mints a did:prism for creative collaborations so identity travels with the person, not the platform.",
      "concept": "Dance creators validate their contributions to a project, ensuring all collaborators receive proper credit and attribution.",
      "subDiscipline": "creative collaborations",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits creative collaborations in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Collaboration Authenticator\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDance creators validate their contributions to a project, ensuring all collaborators receive proper credit and attribution.\nDiscipline: Dance & Choreography (creative collaborations).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits creative collaborations in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $200M — experimental dance collectives · SOM $10M — contact improvisation workshops\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$200M — experimental dance collectives",
      "som": "$10M — contact improvisation workshops"
    },
    {
      "id": "dance-credential-validator-0",
      "theme": "dance",
      "title": "Credential Validator",
      "pitch": "Credential Validator pairs two wallets over DIDComm so dance certifications can exchange trusted claims on a private channel.",
      "concept": "Dancers submit their training credentials to studios for validation, ensuring only qualified individuals receive performance opportunities.",
      "subDiscipline": "dance certifications",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits dance certifications in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Credential Validator\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers submit their training credentials to studios for validation, ensuring only qualified individuals receive performance opportunities.\nDiscipline: Dance & Choreography (dance certifications).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits dance certifications in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $1.2B — classical ballet performances · SOM $40M — accessible arts initiatives\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$1.2B — classical ballet performances",
      "som": "$40M — accessible arts initiatives"
    },
    {
      "id": "dance-performance-rights-tracker-0",
      "theme": "dance",
      "title": "Performance Rights Tracker",
      "pitch": "Performance Rights Tracker issues a signed verifiable credential for licensing that the holder keeps and reuses anywhere.",
      "concept": "Dancers can prove their rights to use choreography by sharing verified credentials from the original choreographer.",
      "subDiscipline": "licensing",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches licensing in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Performance Rights Tracker\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers can prove their rights to use choreography by sharing verified credentials from the original choreographer.\nDiscipline: Dance & Choreography (licensing).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches licensing in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $800M — professional dance companies · SOM $50M — company stage management\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$800M — professional dance companies",
      "som": "$50M — company stage management"
    },
    {
      "id": "dance-performance-verification-0",
      "theme": "dance",
      "title": "Performance Verification",
      "pitch": "Performance Verification verifies a credential at the gate for stage productions — proof without a phone call to the issuer.",
      "concept": "Dancers validate their participation in past performances to gain access to exclusive auditions.",
      "subDiscipline": "stage productions",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for stage productions in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Performance Verification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers validate their participation in past performances to gain access to exclusive auditions.\nDiscipline: Dance & Choreography (stage productions).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for stage productions in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $300M — competitive dance circuit · SOM $15M — elite youth dance training\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$300M — competitive dance circuit",
      "som": "$15M — elite youth dance training"
    },
    {
      "id": "dance-dance-teacher-registry-0",
      "theme": "dance",
      "title": "Dance Teacher Registry",
      "pitch": "Dance Teacher Registry mints a did:prism for teacher accreditation so identity travels with the person, not the platform.",
      "concept": "Dance educators register their qualifications, enabling studios to verify their teaching credentials for hiring.",
      "subDiscipline": "teacher accreditation",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits teacher accreditation in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Dance Teacher Registry\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDance educators register their qualifications, enabling studios to verify their teaching credentials for hiring.\nDiscipline: Dance & Choreography (teacher accreditation).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits teacher accreditation in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $400M — cultural and folkloric dance · SOM $20M — heritage dance preservation\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$400M — cultural and folkloric dance",
      "som": "$20M — heritage dance preservation"
    },
    {
      "id": "dance-event-registration-0",
      "theme": "dance",
      "title": "Event Registration",
      "pitch": "Event Registration pairs two wallets over DIDComm so dance festivals can exchange trusted claims on a private channel.",
      "concept": "Participants can prove their eligibility and previous experience to register for dance festivals.",
      "subDiscipline": "dance festivals",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits dance festivals in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Event Registration\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nParticipants can prove their eligibility and previous experience to register for dance festivals.\nDiscipline: Dance & Choreography (dance festivals).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits dance festivals in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $900M — large-scale dance productions · SOM $45M — cruise ship dance casts\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$900M — large-scale dance productions",
      "som": "$45M — cruise ship dance casts"
    },
    {
      "id": "dance-dance-fitness-certifications-0",
      "theme": "dance",
      "title": "Dance Fitness Certifications",
      "pitch": "Dance Fitness Certifications issues a signed verifiable credential for fitness dance that the holder keeps and reuses anywhere.",
      "concept": "Dance fitness instructors can verify their certifications to gyms and studios using signed credentials issued by accredited organizations.",
      "subDiscipline": "fitness dance",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches fitness dance in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Dance Fitness Certifications\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDance fitness instructors can verify their certifications to gyms and studios using signed credentials issued by accredited organizations.\nDiscipline: Dance & Choreography (fitness dance).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches fitness dance in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $600M — contemporary and modern dance · SOM $30M — narrative dance theater\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$600M — contemporary and modern dance",
      "som": "$30M — narrative dance theater"
    },
    {
      "id": "dance-membership-access-0",
      "theme": "dance",
      "title": "Membership Access",
      "pitch": "Membership Access verifies a credential at the gate for dance unions — proof without a phone call to the issuer.",
      "concept": "Dancers prove their union membership status to access benefits and exclusive community events.",
      "subDiscipline": "dance unions",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for dance unions in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Membership Access\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers prove their union membership status to access benefits and exclusive community events.\nDiscipline: Dance & Choreography (dance unions).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for dance unions in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $500M — sports and dance psychology · SOM $25M — pre-professional dance academies\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$500M — sports and dance psychology",
      "som": "$25M — pre-professional dance academies"
    },
    {
      "id": "dance-competition-eligibility-checker-0",
      "theme": "dance",
      "title": "Competition Eligibility Checker",
      "pitch": "Competition Eligibility Checker mints a did:prism for dance competitions so identity travels with the person, not the platform.",
      "concept": "Participants confirm their age and eligibility status through verified credentials before entering dance competitions.",
      "subDiscipline": "dance competitions",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits dance competitions in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Competition Eligibility Checker\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nParticipants confirm their age and eligibility status through verified credentials before entering dance competitions.\nDiscipline: Dance & Choreography (dance competitions).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits dance competitions in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $400M — performer wellness programs · SOM $20M — youth competition dancers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$400M — performer wellness programs",
      "som": "$20M — youth competition dancers"
    },
    {
      "id": "dance-choreography-portfolio-0",
      "theme": "dance",
      "title": "Choreography Portfolio",
      "pitch": "Choreography Portfolio pairs two wallets over DIDComm so dance showcases can exchange trusted claims on a private channel.",
      "concept": "Dancers showcase their verified works and receive feedback from peers and critics based on their credentials.",
      "subDiscipline": "dance showcases",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits dance showcases in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Choreography Portfolio\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers showcase their verified works and receive feedback from peers and critics based on their credentials.\nDiscipline: Dance & Choreography (dance showcases).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits dance showcases in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $300M — independent choreographers · SOM $15M — university dance departments\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$300M — independent choreographers",
      "som": "$15M — university dance departments"
    },
    {
      "id": "dance-dance-guild-membership-0",
      "theme": "dance",
      "title": "Dance Guild Membership",
      "pitch": "Dance Guild Membership issues a signed verifiable credential for union status that the holder keeps and reuses anywhere.",
      "concept": "Members can obtain verifiable credentials as proof of their membership in dance guilds or unions for eligibility in performances.",
      "subDiscipline": "union status",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches union status in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Dance Guild Membership\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nMembers can obtain verifiable credentials as proof of their membership in dance guilds or unions for eligibility in performances.\nDiscipline: Dance & Choreography (union status).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches union status in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $1B — live arts accessibility · SOM $50M — inclusive theater initiatives\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$1B — live arts accessibility",
      "som": "$50M — inclusive theater initiatives"
    },
    {
      "id": "dance-age-eligibility-checker-0",
      "theme": "dance",
      "title": "Age Eligibility Checker",
      "pitch": "Age Eligibility Checker verifies a credential at the gate for youth dance — proof without a phone call to the issuer.",
      "concept": "Youth dancers can present age verification to enter competitions or classes restricted by age.",
      "subDiscipline": "youth dance",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for youth dance in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Age Eligibility Checker\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nYouth dancers can present age verification to enter competitions or classes restricted by age.\nDiscipline: Dance & Choreography (youth dance).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for youth dance in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $500M — dance battle events · SOM $25M — professional street leagues\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$500M — dance battle events",
      "som": "$25M — professional street leagues"
    },
    {
      "id": "dance-artistic-license-manager-0",
      "theme": "dance",
      "title": "Artistic License Manager",
      "pitch": "Artistic License Manager mints a did:prism for rights management so identity travels with the person, not the platform.",
      "concept": "Choreographers establish and share their licensing agreements with venues to ensure proper rights management.",
      "subDiscipline": "rights management",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits rights management in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Artistic License Manager\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nChoreographers establish and share their licensing agreements with venues to ensure proper rights management.\nDiscipline: Dance & Choreography (rights management).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits rights management in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $600M — professional dance companies · SOM $30M — touring dance troupes\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$600M — professional dance companies",
      "som": "$30M — touring dance troupes"
    },
    {
      "id": "dance-rehearsal-access-0",
      "theme": "dance",
      "title": "Rehearsal Access",
      "pitch": "Rehearsal Access pairs two wallets over DIDComm so casting can exchange trusted claims on a private channel.",
      "concept": "Dancers prove their union membership to gain access to rehearsals and contracts for professional productions.",
      "subDiscipline": "casting",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits casting in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Rehearsal Access\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers prove their union membership to gain access to rehearsals and contracts for professional productions.\nDiscipline: Dance & Choreography (casting).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits casting in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $800M — theatrical production design · SOM $40M — dance company tech crews\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$800M — theatrical production design",
      "som": "$40M — dance company tech crews"
    },
    {
      "id": "dance-age-verification-for-performances-0",
      "theme": "dance",
      "title": "Age Verification for Performances",
      "pitch": "Age Verification for Performances issues a signed verifiable credential for youth dance that the holder keeps and reuses anywhere.",
      "concept": "Parents can digitally attest to their children's ages using credentials for eligibility in youth dance competitions.",
      "subDiscipline": "youth dance",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches youth dance in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Age Verification for Performances\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nParents can digitally attest to their children's ages using credentials for eligibility in youth dance competitions.\nDiscipline: Dance & Choreography (youth dance).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches youth dance in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $1.2B — classical ballet training · SOM $60M — professional ballet companies\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$1.2B — classical ballet training",
      "som": "$60M — professional ballet companies"
    },
    {
      "id": "dance-dance-rights-tracker-0",
      "theme": "dance",
      "title": "Dance Rights Tracker",
      "pitch": "Dance Rights Tracker verifies a credential at the gate for intellectual property — proof without a phone call to the issuer.",
      "concept": "Choreographers present proof of original choreography to protect their creative ownership in disputes.",
      "subDiscipline": "intellectual property",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for intellectual property in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Dance Rights Tracker\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nChoreographers present proof of original choreography to protect their creative ownership in disputes.\nDiscipline: Dance & Choreography (intellectual property).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for intellectual property in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $400M — world dance education · SOM $20M — cultural dance conservatories\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$400M — world dance education",
      "som": "$20M — cultural dance conservatories"
    },
    {
      "id": "dance-dance-history-archive-0",
      "theme": "dance",
      "title": "Dance History Archive",
      "pitch": "Dance History Archive mints a did:prism for archival curation so identity travels with the person, not the platform.",
      "concept": "Dancers authenticate their participation in historical performances, ensuring accurate documentation for archival purposes.",
      "subDiscipline": "archival curation",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits archival curation in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Dance History Archive\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers authenticate their participation in historical performances, ensuring accurate documentation for archival purposes.\nDiscipline: Dance & Choreography (archival curation).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits archival curation in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $1B — live performance venues · SOM $50M — regional dance theaters\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$1B — live performance venues",
      "som": "$50M — regional dance theaters"
    },
    {
      "id": "dance-performance-rights-0",
      "theme": "dance",
      "title": "Performance Rights",
      "pitch": "Performance Rights pairs two wallets over DIDComm so licensing can exchange trusted claims on a private channel.",
      "concept": "Choreographers verify their identity to claim performance rights for their original dances from venues.",
      "subDiscipline": "licensing",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits licensing in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Performance Rights\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nChoreographers verify their identity to claim performance rights for their original dances from venues.\nDiscipline: Dance & Choreography (licensing).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits licensing in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance and movement industry · SAM $400M — contemporary and improvisational dance sectors · SOM $15M — experimental choreographers and movement labs\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance and movement industry",
      "sam": "$400M — contemporary and improvisational dance sectors",
      "som": "$15M — experimental choreographers and movement labs"
    },
    {
      "id": "dance-choreography-collaboration-verification-0",
      "theme": "dance",
      "title": "Choreography Collaboration Verification",
      "pitch": "Choreography Collaboration Verification issues a signed verifiable credential for collaborative projects that the holder keeps and reuses anywhere.",
      "concept": "Dancers can verify their contributions to collaborative choreographic works with credentials shared among the team members.",
      "subDiscipline": "collaborative projects",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches collaborative projects in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Choreography Collaboration Verification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers can verify their contributions to collaborative choreographic works with credentials shared among the team members.\nDiscipline: Dance & Choreography (collaborative projects).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches collaborative projects in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance studio market · SAM $1.2B — professional rehearsal spaces and companies · SOM $40M — commercial and touring dance troupes\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance studio market",
      "sam": "$1.2B — professional rehearsal spaces and companies",
      "som": "$40M — commercial and touring dance troupes"
    },
    {
      "id": "dance-consent-management-0",
      "theme": "dance",
      "title": "Consent Management",
      "pitch": "Consent Management verifies a credential at the gate for performer rights — proof without a phone call to the issuer.",
      "concept": "Dancers provide evidence of consent for video recordings to producers for various projects.",
      "subDiscipline": "performer rights",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for performer rights in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Consent Management\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers provide evidence of consent for video recordings to producers for various projects.\nDiscipline: Dance & Choreography (performer rights).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for performer rights in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global performing arts industry · SAM $900M — professional stage performers and dancers · SOM $30M — elite competition and company dancers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global performing arts industry",
      "sam": "$900M — professional stage performers and dancers",
      "som": "$30M — elite competition and company dancers"
    },
    {
      "id": "dance-provenance-for-choreographies-0",
      "theme": "dance",
      "title": "Provenance for Choreographies",
      "pitch": "Provenance for Choreographies mints a did:prism for dance documentation so identity travels with the person, not the platform.",
      "concept": "Choreographers assert ownership of original works, providing proof of authorship to prevent unauthorized reproductions.",
      "subDiscipline": "dance documentation",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits dance documentation in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Provenance for Choreographies\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nChoreographers assert ownership of original works, providing proof of authorship to prevent unauthorized reproductions.\nDiscipline: Dance & Choreography (dance documentation).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits dance documentation in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance and wellness market · SAM $600M — dance medicine and physical therapy · SOM $20M — professional athletes and elite dancers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance and wellness market",
      "sam": "$600M — dance medicine and physical therapy",
      "som": "$20M — professional athletes and elite dancers"
    },
    {
      "id": "dance-age-verification-0",
      "theme": "dance",
      "title": "Age Verification",
      "pitch": "Age Verification pairs two wallets over DIDComm so youth dance can exchange trusted claims on a private channel.",
      "concept": "Dance schools can verify the ages of applicants to ensure compliance with age restrictions.",
      "subDiscipline": "youth dance",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits youth dance in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Age Verification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDance schools can verify the ages of applicants to ensure compliance with age restrictions.\nDiscipline: Dance & Choreography (youth dance).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits youth dance in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance creation software market · SAM $300M — independent choreographers and directors · SOM $10M — university dance faculty and residents\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance creation software market",
      "sam": "$300M — independent choreographers and directors",
      "som": "$10M — university dance faculty and residents"
    },
    {
      "id": "dance-student-dance-accreditations-0",
      "theme": "dance",
      "title": "Student Dance Accreditations",
      "pitch": "Student Dance Accreditations issues a signed verifiable credential for dance education that the holder keeps and reuses anywhere.",
      "concept": "Dance schools issue credentials to students upon completion of courses, which can be shared with future employers.",
      "subDiscipline": "dance education",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches dance education in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Student Dance Accreditations\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDance schools issue credentials to students upon completion of courses, which can be shared with future employers.\nDiscipline: Dance & Choreography (dance education).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches dance education in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance and music education · SAM $500M — rhythm-based dance styles and studios · SOM $18M — hip-hop and tap dance educators\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance and music education",
      "sam": "$500M — rhythm-based dance styles and studios",
      "som": "$18M — hip-hop and tap dance educators"
    },
    {
      "id": "dance-dance-workshop-credentials-0",
      "theme": "dance",
      "title": "Dance Workshop Credentials",
      "pitch": "Dance Workshop Credentials verifies a credential at the gate for workshops and training — proof without a phone call to the issuer.",
      "concept": "Instructors prove their qualifications before leading workshops at dance festivals.",
      "subDiscipline": "workshops and training",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for workshops and training in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Dance Workshop Credentials\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nInstructors prove their qualifications before leading workshops at dance festivals.\nDiscipline: Dance & Choreography (workshops and training).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for workshops and training in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global live event production market · SAM $1.5B — theatrical stage management and crew · SOM $50M — mid-size regional theater companies\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global live event production market",
      "sam": "$1.5B — theatrical stage management and crew",
      "som": "$50M — mid-size regional theater companies"
    },
    {
      "id": "dance-collaboration-consent-log-0",
      "theme": "dance",
      "title": "Collaboration Consent Log",
      "pitch": "Collaboration Consent Log mints a did:prism for creative consent so identity travels with the person, not the platform.",
      "concept": "Dancers document consent for collaboration on choreography, ensuring transparency and respect for creative contributions.",
      "subDiscipline": "creative consent",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits creative consent in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Collaboration Consent Log\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers document consent for collaboration on choreography, ensuring transparency and respect for creative contributions.\nDiscipline: Dance & Choreography (creative consent).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits creative consent in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global classical dance industry · SAM $800M — international ballet companies and academies · SOM $25M — touring international ballet troupes\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global classical dance industry",
      "sam": "$800M — international ballet companies and academies",
      "som": "$25M — touring international ballet troupes"
    },
    {
      "id": "dance-dance-collaborator-0",
      "theme": "dance",
      "title": "Dance Collaborator",
      "pitch": "Dance Collaborator pairs two wallets over DIDComm so movement partnerships can exchange trusted claims on a private channel.",
      "concept": "Dancers use verified identities to form partnerships and co-create choreographies with other verified artists.",
      "subDiscipline": "movement partnerships",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits movement partnerships in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Dance Collaborator\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers use verified identities to form partnerships and co-create choreographies with other verified artists.\nDiscipline: Dance & Choreography (movement partnerships).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits movement partnerships in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global contemporary dance sector · SAM $200M — somatic and floorwork training · SOM $8M — contemporary dance conservatories\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global contemporary dance sector",
      "sam": "$200M — somatic and floorwork training",
      "som": "$8M — contemporary dance conservatories"
    },
    {
      "id": "dance-costume-authenticity-check-0",
      "theme": "dance",
      "title": "Costume Authenticity Check",
      "pitch": "Costume Authenticity Check issues a signed verifiable credential for costume archives that the holder keeps and reuses anywhere.",
      "concept": "Designers issue credentials that verify the authenticity of costumes for performances, ensuring provenance is documented.",
      "subDiscipline": "costume archives",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches costume archives in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Costume Authenticity Check\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDesigners issue credentials that verify the authenticity of costumes for performances, ensuring provenance is documented.\nDiscipline: Dance & Choreography (costume archives).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches costume archives in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global accessible arts market · SAM $400M — inclusive theater and dance venues · SOM $12M — government-funded accessible arts programs\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global accessible arts market",
      "sam": "$400M — inclusive theater and dance venues",
      "som": "$12M — government-funded accessible arts programs"
    },
    {
      "id": "dance-festival-participant-verification-0",
      "theme": "dance",
      "title": "Festival Participant Verification",
      "pitch": "Festival Participant Verification verifies a credential at the gate for dance festivals — proof without a phone call to the issuer.",
      "concept": "Participants present proof of eligibility to attend specific dance festivals or events.",
      "subDiscipline": "dance festivals",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for dance festivals in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Festival Participant Verification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nParticipants present proof of eligibility to attend specific dance festivals or events.\nDiscipline: Dance & Choreography (dance festivals).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for dance festivals in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global street dance and culture · SAM $300M — competitive breaking and hip-hop events · SOM $15M — professional battle league competitors\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global street dance and culture",
      "sam": "$300M — competitive breaking and hip-hop events",
      "som": "$15M — professional battle league competitors"
    },
    {
      "id": "dance-membership-verification-hub-0",
      "theme": "dance",
      "title": "Membership Verification Hub",
      "pitch": "Membership Verification Hub mints a did:prism for dance unions so identity travels with the person, not the platform.",
      "concept": "Dance professionals verify their membership status with unions or guilds to obtain industry benefits and protections.",
      "subDiscipline": "dance unions",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits dance unions in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Membership Verification Hub\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDance professionals verify their membership status with unions or guilds to obtain industry benefits and protections.\nDiscipline: Dance & Choreography (dance unions).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits dance unions in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global classical ballet market · SAM $1B — ballet academies and pre-professional schools · SOM $35M — vocational ballet boarding schools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global classical ballet market",
      "sam": "$1B — ballet academies and pre-professional schools",
      "som": "$35M — vocational ballet boarding schools"
    },
    {
      "id": "dance-feedback-loop-0",
      "theme": "dance",
      "title": "Feedback Loop",
      "pitch": "Feedback Loop pairs two wallets over DIDComm so dance criticism can exchange trusted claims on a private channel.",
      "concept": "Dancers receive personalized feedback by proving their performance credentials to experienced reviewers.",
      "subDiscipline": "dance criticism",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits dance criticism in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Feedback Loop\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers receive personalized feedback by proving their performance credentials to experienced reviewers.\nDiscipline: Dance & Choreography (dance criticism).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits dance criticism in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance studio software · SAM $2B — group class instructors and studios · SOM $60M — boutique fitness and dance franchises\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance studio software",
      "sam": "$2B — group class instructors and studios",
      "som": "$60M — boutique fitness and dance franchises"
    },
    {
      "id": "dance-backstage-access-passes-0",
      "theme": "dance",
      "title": "Backstage Access Passes",
      "pitch": "Backstage Access Passes issues a signed verifiable credential for event management that the holder keeps and reuses anywhere.",
      "concept": "Event organizers issue verifiable backstage access passes to dancers and crew, ensuring secure entry based on credentials.",
      "subDiscipline": "event management",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches event management in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Backstage Access Passes\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nEvent organizers issue verifiable backstage access passes to dancers and crew, ensuring secure entry based on credentials.\nDiscipline: Dance & Choreography (event management).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches event management in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global musical theater industry · SAM $1.5B — musical theater performers and triple-threats · SOM $45M — Broadway and West End ensemble casts\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global musical theater industry",
      "sam": "$1.5B — musical theater performers and triple-threats",
      "som": "$45M — Broadway and West End ensemble casts"
    },
    {
      "id": "dance-choreography-attribution-0",
      "theme": "dance",
      "title": "Choreography Attribution",
      "pitch": "Choreography Attribution verifies a credential at the gate for crediting — proof without a phone call to the issuer.",
      "concept": "Dancers can provide verified attribution to their choreographers when submitting videos online.",
      "subDiscipline": "crediting",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for crediting in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Choreography Attribution\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers can provide verified attribution to their choreographers when submitting videos online.\nDiscipline: Dance & Choreography (crediting).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for crediting in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global ensemble dance market · SAM $600M — competitive dance teams and show choirs · SOM $20M — collegiate and high-school dance teams\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global ensemble dance market",
      "sam": "$600M — competitive dance teams and show choirs",
      "som": "$20M — collegiate and high-school dance teams"
    },
    {
      "id": "dance-skill-development-tracker-0",
      "theme": "dance",
      "title": "Skill Development Tracker",
      "pitch": "Skill Development Tracker mints a did:prism for personal development so identity travels with the person, not the platform.",
      "concept": "Dancers validate their training progress and workshop attendance to showcase their skills to potential employers.",
      "subDiscipline": "personal development",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits personal development in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Skill Development Tracker\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers validate their training progress and workshop attendance to showcase their skills to potential employers.\nDiscipline: Dance & Choreography (personal development).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits personal development in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global sports and performance nutrition · SAM $400M — touring performers and athletic troupes · SOM $10M — international touring dance companies\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global sports and performance nutrition",
      "sam": "$400M — touring performers and athletic troupes",
      "som": "$10M — international touring dance companies"
    },
    {
      "id": "dance-costume-ownership-0",
      "theme": "dance",
      "title": "Costume Ownership",
      "pitch": "Costume Ownership pairs two wallets over DIDComm so costume design can exchange trusted claims on a private channel.",
      "concept": "Designers prove ownership and authenticity of costumes to performers for rental or sale.",
      "subDiscipline": "costume design",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits costume design in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Costume Ownership\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDesigners prove ownership and authenticity of costumes to performers for rental or sale.\nDiscipline: Dance & Choreography (costume design).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits costume design in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global arts funding and grants · SAM $200M — independent choreographers and non-profits · SOM $5M — grant-seeking contemporary dance artists\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global arts funding and grants",
      "sam": "$200M — independent choreographers and non-profits",
      "som": "$5M — grant-seeking contemporary dance artists"
    },
    {
      "id": "dance-choreography-innovation-claim-0",
      "theme": "dance",
      "title": "Choreography Innovation Claim",
      "pitch": "Choreography Innovation Claim issues a signed verifiable credential for intellectual property that the holder keeps and reuses anywhere.",
      "concept": "Choreographers can claim and share their innovative works with verifiable credentials, establishing authorship and protecting their rights.",
      "subDiscipline": "intellectual property",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches intellectual property in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Choreography Innovation Claim\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nChoreographers can claim and share their innovative works with verifiable credentials, establishing authorship and protecting their rights.\nDiscipline: Dance & Choreography (intellectual property).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches intellectual property in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance education market · SAM $1B — university dance departments and academics · SOM $30M — higher-education dance history courses\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance education market",
      "sam": "$1B — university dance departments and academics",
      "som": "$30M — higher-education dance history courses"
    },
    {
      "id": "dance-collaboration-consent-0",
      "theme": "dance",
      "title": "Collaboration Consent",
      "pitch": "Collaboration Consent verifies a credential at the gate for collaborative projects — proof without a phone call to the issuer.",
      "concept": "Dancers verify consent agreements with collaborators to share creative works.",
      "subDiscipline": "collaborative projects",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for collaborative projects in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Collaboration Consent\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers verify consent agreements with collaborators to share creative works.\nDiscipline: Dance & Choreography (collaborative projects).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for collaborative projects in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global mental health and wellness · SAM $300M — performer mental health initiatives · SOM $12M — intensive dance training programs\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global mental health and wellness",
      "sam": "$300M — performer mental health initiatives",
      "som": "$12M — intensive dance training programs"
    },
    {
      "id": "dance-choreography-feedback-loop-0",
      "theme": "dance",
      "title": "Choreography Feedback Loop",
      "pitch": "Choreography Feedback Loop mints a did:prism for feedback systems so identity travels with the person, not the platform.",
      "concept": "Dancers receive verified feedback on their performances from peers and mentors to improve their craft.",
      "subDiscipline": "feedback systems",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits feedback systems in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Choreography Feedback Loop\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers receive verified feedback on their performances from peers and mentors to improve their craft.\nDiscipline: Dance & Choreography (feedback systems).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits feedback systems in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global theatrical lighting design · SAM $800M — dance and theater technical crews · SOM $25M — mid-tier regional dance festivals\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global theatrical lighting design",
      "sam": "$800M — dance and theater technical crews",
      "som": "$25M — mid-tier regional dance festivals"
    },
    {
      "id": "dance-workshop-participation-0",
      "theme": "dance",
      "title": "Workshop Participation",
      "pitch": "Workshop Participation pairs two wallets over DIDComm so dance workshops can exchange trusted claims on a private channel.",
      "concept": "Participants prove their previous experience to enroll in specialized dance workshops.",
      "subDiscipline": "dance workshops",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits dance workshops in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Workshop Participation\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nParticipants prove their previous experience to enroll in specialized dance workshops.\nDiscipline: Dance & Choreography (dance workshops).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits dance workshops in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global rhythm and percussion education · SAM $150M — tap and percussive dance communities · SOM $5M — professional tap dance festivals\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global rhythm and percussion education",
      "sam": "$150M — tap and percussive dance communities",
      "som": "$5M — professional tap dance festivals"
    },
    {
      "id": "dance-safe-space-consent-verification-0",
      "theme": "dance",
      "title": "Safe Space Consent Verification",
      "pitch": "Safe Space Consent Verification issues a signed verifiable credential for safeguarding that the holder keeps and reuses anywhere.",
      "concept": "Dancers can provide verifiable consent credentials to ensure safe working environments during rehearsals and performances.",
      "subDiscipline": "safeguarding",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches safeguarding in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Safe Space Consent Verification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers can provide verifiable consent credentials to ensure safe working environments during rehearsals and performances.\nDiscipline: Dance & Choreography (safeguarding).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches safeguarding in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global stagecraft and props market · SAM $500M — large-scale theatrical productions · SOM $15M — touring mega-musicals and dance shows\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global stagecraft and props market",
      "sam": "$500M — large-scale theatrical productions",
      "som": "$15M — touring mega-musicals and dance shows"
    },
    {
      "id": "dance-dance-awards-eligibility-0",
      "theme": "dance",
      "title": "Dance Awards Eligibility",
      "pitch": "Dance Awards Eligibility verifies a credential at the gate for awards and recognitions — proof without a phone call to the issuer.",
      "concept": "Dancers present proof of previous awards to qualify for prestigious dance competitions.",
      "subDiscipline": "awards and recognitions",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for awards and recognitions in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Dance Awards Eligibility\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers present proof of previous awards to qualify for prestigious dance competitions.\nDiscipline: Dance & Choreography (awards and recognitions).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for awards and recognitions in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global casting and talent agency market · SAM $1.2B — professional dancers seeking employment · SOM $40M — commercial dance agency rosters\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global casting and talent agency market",
      "sam": "$1.2B — professional dancers seeking employment",
      "som": "$40M — commercial dance agency rosters"
    },
    {
      "id": "dance-exclusive-workshop-invitations-0",
      "theme": "dance",
      "title": "Exclusive Workshop Invitations",
      "pitch": "Exclusive Workshop Invitations mints a did:prism for professional development so identity travels with the person, not the platform.",
      "concept": "Verified dance professionals receive invitations to exclusive workshops, ensuring attendees are qualified for advanced training.",
      "subDiscipline": "professional development",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits professional development in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Exclusive Workshop Invitations\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nVerified dance professionals receive invitations to exclusive workshops, ensuring attendees are qualified for advanced training.\nDiscipline: Dance & Choreography (professional development).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits professional development in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global contemporary arts direction · SAM $250M — established choreographers and creators · SOM $8M — artist-in-residence program directors\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global contemporary arts direction",
      "sam": "$250M — established choreographers and creators",
      "som": "$8M — artist-in-residence program directors"
    },
    {
      "id": "dance-legacy-documentation-0",
      "theme": "dance",
      "title": "Legacy Documentation",
      "pitch": "Legacy Documentation pairs two wallets over DIDComm so dance history can exchange trusted claims on a private channel.",
      "concept": "Dancers can document and verify their dance lineage to preserve choreographic heritage.",
      "subDiscipline": "dance history",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits dance history in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Legacy Documentation\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers can document and verify their dance lineage to preserve choreographic heritage.\nDiscipline: Dance & Choreography (dance history).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits dance history in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance studio management · SAM $2.5B — recreational youth dance studios · SOM $80M — large multi-branch dance academies\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance studio management",
      "sam": "$2.5B — recreational youth dance studios",
      "som": "$80M — large multi-branch dance academies"
    },
    {
      "id": "dance-performance-feedback-validation-0",
      "theme": "dance",
      "title": "Performance Feedback Validation",
      "pitch": "Performance Feedback Validation issues a signed verifiable credential for dance mentorship that the holder keeps and reuses anywhere.",
      "concept": "Mentors issue verified feedback credentials to dancers, validating their growth and achievements for future opportunities.",
      "subDiscipline": "dance mentorship",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches dance mentorship in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Performance Feedback Validation\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nMentors issue verified feedback credentials to dancers, validating their growth and achievements for future opportunities.\nDiscipline: Dance & Choreography (dance mentorship).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches dance mentorship in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $500M — professional dance companies · SOM $50M — contemporary choreographers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$500M — professional dance companies",
      "som": "$50M — contemporary choreographers"
    },
    {
      "id": "dance-studio-membership-proof-0",
      "theme": "dance",
      "title": "Studio Membership Proof",
      "pitch": "Studio Membership Proof verifies a credential at the gate for dance studios — proof without a phone call to the issuer.",
      "concept": "Dancers provide proof of membership status to receive studio discounts or promotions.",
      "subDiscipline": "dance studios",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for dance studios in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Studio Membership Proof\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers provide proof of membership status to receive studio discounts or promotions.\nDiscipline: Dance & Choreography (dance studios).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for dance studios in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $1B — dance education market · SOM $100M — hip-hop studios\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$1B — dance education market",
      "som": "$100M — hip-hop studios"
    },
    {
      "id": "dance-safe-space-credentials-0",
      "theme": "dance",
      "title": "Safe Space Credentials",
      "pitch": "Safe Space Credentials mints a did:prism for safeguarding so identity travels with the person, not the platform.",
      "concept": "Dancers prove their completion of safety and safeguarding training before participating in sensitive projects.",
      "subDiscipline": "safeguarding",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits safeguarding in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Safe Space Credentials\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers prove their completion of safety and safeguarding training before participating in sensitive projects.\nDiscipline: Dance & Choreography (safeguarding).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits safeguarding in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $800M — dance wellness and physio · SOM $80M — elite ballet dancers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$800M — dance wellness and physio",
      "som": "$80M — elite ballet dancers"
    },
    {
      "id": "dance-choreography-validation-0",
      "theme": "dance",
      "title": "Choreography Validation",
      "pitch": "Choreography Validation pairs two wallets over DIDComm so dance competitions can exchange trusted claims on a private channel.",
      "concept": "Competitors prove the originality of their works to judges through verified choreography credentials.",
      "subDiscipline": "dance competitions",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits dance competitions in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Choreography Validation\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCompetitors prove the originality of their works to judges through verified choreography credentials.\nDiscipline: Dance & Choreography (dance competitions).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits dance competitions in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $300M — experimental dance scene · SOM $30M — solo improvisers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$300M — experimental dance scene",
      "som": "$30M — solo improvisers"
    },
    {
      "id": "dance-dance-therapy-credentials-0",
      "theme": "dance",
      "title": "Dance Therapy Credentials",
      "pitch": "Dance Therapy Credentials issues a signed verifiable credential for dance therapy that the holder keeps and reuses anywhere.",
      "concept": "Therapists receive credentials from accredited organizations to verify their qualifications to conduct dance therapy sessions.",
      "subDiscipline": "dance therapy",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches dance therapy in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Dance Therapy Credentials\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTherapists receive credentials from accredited organizations to verify their qualifications to conduct dance therapy sessions.\nDiscipline: Dance & Choreography (dance therapy).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches dance therapy in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $2B — global dance studios · SOM $200M — children's dance academies\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$2B — global dance studios",
      "som": "$200M — children's dance academies"
    },
    {
      "id": "dance-authenticity-of-editions-0",
      "theme": "dance",
      "title": "Authenticity of Editions",
      "pitch": "Authenticity of Editions verifies a credential at the gate for choreography archives — proof without a phone call to the issuer.",
      "concept": "Choreographers present proof of edition authenticity for their works to enhance credibility.",
      "subDiscipline": "choreography archives",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for choreography archives in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Authenticity of Editions\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nChoreographers present proof of edition authenticity for their works to enhance credibility.\nDiscipline: Dance & Choreography (choreography archives).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for choreography archives in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $400M — classical ballet companies · SOM $40M — narrative choreographers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$400M — classical ballet companies",
      "som": "$40M — narrative choreographers"
    },
    {
      "id": "dance-event-role-verification-0",
      "theme": "dance",
      "title": "Event Role Verification",
      "pitch": "Event Role Verification mints a did:prism for show production so identity travels with the person, not the platform.",
      "concept": "Production teams verify the roles of dancers in events to ensure accurate representation in promotional materials.",
      "subDiscipline": "show production",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits show production in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Event Role Verification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nProduction teams verify the roles of dancers in events to ensure accurate representation in promotional materials.\nDiscipline: Dance & Choreography (show production).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits show production in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $600M — musical theater dance · SOM $60M — Broadway movement directors\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$600M — musical theater dance",
      "som": "$60M — Broadway movement directors"
    },
    {
      "id": "dance-performance-tracking-0",
      "theme": "dance",
      "title": "Performance Tracking",
      "pitch": "Performance Tracking pairs two wallets over DIDComm so dance career can exchange trusted claims on a private channel.",
      "concept": "Dancers track and verify their performance history to enhance their career opportunities.",
      "subDiscipline": "dance career",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits dance career in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Performance Tracking\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers track and verify their performance history to enhance their career opportunities.\nDiscipline: Dance & Choreography (dance career).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits dance career in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $200M — dance fitness · SOM $20M — commercial backup dancers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$200M — dance fitness",
      "som": "$20M — commercial backup dancers"
    },
    {
      "id": "dance-choreographer-s-portfolio-access-0",
      "theme": "dance",
      "title": "Choreographer's Portfolio Access",
      "pitch": "Choreographer's Portfolio Access issues a signed verifiable credential for creative portfolio that the holder keeps and reuses anywhere.",
      "concept": "Choreographers can share credentials that provide access to their verified portfolios for hiring committees in studios.",
      "subDiscipline": "creative portfolio",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches creative portfolio in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Choreographer's Portfolio Access\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nChoreographers can share credentials that provide access to their verified portfolios for hiring committees in studios.\nDiscipline: Dance & Choreography (creative portfolio).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches creative portfolio in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $150M — dance competitions · SOM $15M — competition judges\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$150M — dance competitions",
      "som": "$15M — competition judges"
    },
    {
      "id": "dance-performance-participation-proof-0",
      "theme": "dance",
      "title": "Performance Participation Proof",
      "pitch": "Performance Participation Proof verifies a credential at the gate for casting — proof without a phone call to the issuer.",
      "concept": "Dancers validate their past participations to obtain roles in future performances.",
      "subDiscipline": "casting",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for casting in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Performance Participation Proof\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers validate their past participations to obtain roles in future performances.\nDiscipline: Dance & Choreography (casting).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for casting in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $100M — cultural dance troupes · SOM $10M — folkloric educators\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$100M — cultural dance troupes",
      "som": "$10M — folkloric educators"
    },
    {
      "id": "dance-masterclass-authenticator-0",
      "theme": "dance",
      "title": "Masterclass Authenticator",
      "pitch": "Masterclass Authenticator mints a did:prism for guest instruction so identity travels with the person, not the platform.",
      "concept": "Dancers verify their attendance in masterclasses to prove their professional development to studios and peers.",
      "subDiscipline": "guest instruction",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits guest instruction in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Masterclass Authenticator\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers verify their attendance in masterclasses to prove their professional development to studios and peers.\nDiscipline: Dance & Choreography (guest instruction).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits guest instruction in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $50M — dancewear manufacturing · SOM $5M — theatrical costumers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$50M — dancewear manufacturing",
      "som": "$5M — theatrical costumers"
    },
    {
      "id": "dance-accredited-dance-teachers-0",
      "theme": "dance",
      "title": "Accredited Dance Teachers",
      "pitch": "Accredited Dance Teachers pairs two wallets over DIDComm so dance education can exchange trusted claims on a private channel.",
      "concept": "Students confirm their teachers' qualifications to ensure quality education in dance.",
      "subDiscipline": "dance education",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits dance education in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Accredited Dance Teachers\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nStudents confirm their teachers' qualifications to ensure quality education in dance.\nDiscipline: Dance & Choreography (dance education).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits dance education in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $800M — live event production · SOM $80M — dance festival techs\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$800M — live event production",
      "som": "$80M — dance festival techs"
    },
    {
      "id": "dance-competition-entry-verification-0",
      "theme": "dance",
      "title": "Competition Entry Verification",
      "pitch": "Competition Entry Verification issues a signed verifiable credential for dance competitions that the holder keeps and reuses anywhere.",
      "concept": "Dancers can present verified credentials proving their eligibility for competitions, ensuring adherence to age and experience criteria.",
      "subDiscipline": "dance competitions",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches dance competitions in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Competition Entry Verification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers can present verified credentials proving their eligibility for competitions, ensuring adherence to age and experience criteria.\nDiscipline: Dance & Choreography (dance competitions).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches dance competitions in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $50M — performance psychology · SOM $5M — elite soloists\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$50M — performance psychology",
      "som": "$5M — elite soloists"
    },
    {
      "id": "dance-educational-workshop-proof-0",
      "theme": "dance",
      "title": "Educational Workshop Proof",
      "pitch": "Educational Workshop Proof verifies a credential at the gate for education — proof without a phone call to the issuer.",
      "concept": "Dance educators present proof of completed workshops to maintain their teaching certifications.",
      "subDiscipline": "education",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for education in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Educational Workshop Proof\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDance educators present proof of completed workshops to maintain their teaching certifications.\nDiscipline: Dance & Choreography (education).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for education in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $75M — tap dance studios · SOM $7.5M — tap educators\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$75M — tap dance studios",
      "som": "$7.5M — tap educators"
    },
    {
      "id": "dance-choreography-edition-claims-0",
      "theme": "dance",
      "title": "Choreography Edition Claims",
      "pitch": "Choreography Edition Claims mints a did:prism for performance rights so identity travels with the person, not the platform.",
      "concept": "Choreographers validate their claims to specific performances or adaptations, securing their rights to original works.",
      "subDiscipline": "performance rights",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits performance rights in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Choreography Edition Claims\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nChoreographers validate their claims to specific performances or adaptations, securing their rights to original works.\nDiscipline: Dance & Choreography (performance rights).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits performance rights in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $400M — classical ballet · SOM $40M — partnering coaches\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$400M — classical ballet",
      "som": "$40M — partnering coaches"
    },
    {
      "id": "dance-safe-spaces-0",
      "theme": "dance",
      "title": "Safe Spaces",
      "pitch": "Safe Spaces pairs two wallets over DIDComm so dance safety can exchange trusted claims on a private channel.",
      "concept": "Dancers prove their safety training to access sensitive rehearsal spaces.",
      "subDiscipline": "dance safety",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits dance safety in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Safe Spaces\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers prove their safety training to access sensitive rehearsal spaces.\nDiscipline: Dance & Choreography (dance safety).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits dance safety in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $100M — dance flooring · SOM $10M — touring companies\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$100M — dance flooring",
      "som": "$10M — touring companies"
    },
    {
      "id": "dance-dance-event-ticket-transfers-0",
      "theme": "dance",
      "title": "Dance Event Ticket Transfers",
      "pitch": "Dance Event Ticket Transfers issues a signed verifiable credential for event access that the holder keeps and reuses anywhere.",
      "concept": "Attendees receive verifiable credentials for tickets that can be transferred securely while ensuring authenticity.",
      "subDiscipline": "event access",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches event access in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Dance Event Ticket Transfers\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAttendees receive verifiable credentials for tickets that can be transferred securely while ensuring authenticity.\nDiscipline: Dance & Choreography (event access).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches event access in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $1B — commercial dance tours · SOM $100M — club performers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$1B — commercial dance tours",
      "som": "$100M — club performers"
    },
    {
      "id": "dance-rehearsal-access-pass-0",
      "theme": "dance",
      "title": "Rehearsal Access Pass",
      "pitch": "Rehearsal Access Pass verifies a credential at the gate for backstage access — proof without a phone call to the issuer.",
      "concept": "Dancers provide proof of rehearsal participation to gain backstage access during performances.",
      "subDiscipline": "backstage access",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for backstage access in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Rehearsal Access Pass\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers provide proof of rehearsal participation to gain backstage access during performances.\nDiscipline: Dance & Choreography (backstage access).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for backstage access in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $200M — accessible arts initiatives · SOM $20M — inclusive dance troupes\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$200M — accessible arts initiatives",
      "som": "$20M — inclusive dance troupes"
    },
    {
      "id": "dance-dance-project-archives-0",
      "theme": "dance",
      "title": "Dance Project Archives",
      "pitch": "Dance Project Archives mints a did:prism for project management so identity travels with the person, not the platform.",
      "concept": "Dance projects maintain verified archives of contributions, ensuring accurate record-keeping for future references.",
      "subDiscipline": "project management",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits project management in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Dance Project Archives\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDance projects maintain verified archives of contributions, ensuring accurate record-keeping for future references.\nDiscipline: Dance & Choreography (project management).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits project management in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $300M — dance media production · SOM $30M — screen choreographers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$300M — dance media production",
      "som": "$30M — screen choreographers"
    },
    {
      "id": "dance-scholarship-eligibility-0",
      "theme": "dance",
      "title": "Scholarship Eligibility",
      "pitch": "Scholarship Eligibility pairs two wallets over DIDComm so dance funding can exchange trusted claims on a private channel.",
      "concept": "Aspiring dancers prove their qualifications to apply for scholarships and funding opportunities.",
      "subDiscipline": "dance funding",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits dance funding in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Scholarship Eligibility\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAspiring dancers prove their qualifications to apply for scholarships and funding opportunities.\nDiscipline: Dance & Choreography (dance funding).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits dance funding in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $500M — dance production management · SOM $50M — company managers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$500M — dance production management",
      "som": "$50M — company managers"
    },
    {
      "id": "dance-dance-class-completion-proof-0",
      "theme": "dance",
      "title": "Dance Class Completion Proof",
      "pitch": "Dance Class Completion Proof issues a signed verifiable credential for dance education that the holder keeps and reuses anywhere.",
      "concept": "Dance class instructors issue credentials confirming completion to students, proving their participation in specific classes.",
      "subDiscipline": "dance education",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches dance education in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Dance Class Completion Proof\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDance class instructors issue credentials confirming completion to students, proving their participation in specific classes.\nDiscipline: Dance & Choreography (dance education).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches dance education in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $800M — professional dance companies · SOM $80M — dance captains\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$800M — professional dance companies",
      "som": "$80M — dance captains"
    },
    {
      "id": "dance-rights-ownership-verification-0",
      "theme": "dance",
      "title": "Rights Ownership Verification",
      "pitch": "Rights Ownership Verification verifies a credential at the gate for licensing — proof without a phone call to the issuer.",
      "concept": "Choreographers validate their rights ownership to license their works for use.",
      "subDiscipline": "licensing",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for licensing in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Rights Ownership Verification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nChoreographers validate their rights ownership to license their works for use.\nDiscipline: Dance & Choreography (licensing).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for licensing in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $300M — contemporary dance · SOM $30M — independent choreographers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$300M — contemporary dance",
      "som": "$30M — independent choreographers"
    },
    {
      "id": "dance-talent-showcase-validator-0",
      "theme": "dance",
      "title": "Talent Showcase Validator",
      "pitch": "Talent Showcase Validator mints a did:prism for auditions so identity travels with the person, not the platform.",
      "concept": "Dancers confirm their participation in talent showcases to attract industry opportunities and validate their experience.",
      "subDiscipline": "auditions",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits auditions in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Talent Showcase Validator\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers confirm their participation in talent showcases to attract industry opportunities and validate their experience.\nDiscipline: Dance & Choreography (auditions).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits auditions in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $150M — dance medicine · SOM $15M — rehabilitating dancers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$150M — dance medicine",
      "som": "$15M — rehabilitating dancers"
    },
    {
      "id": "dance-agent-verification-0",
      "theme": "dance",
      "title": "Agent Verification",
      "pitch": "Agent Verification pairs two wallets over DIDComm so dance representation can exchange trusted claims on a private channel.",
      "concept": "Dancers verify their agents' credentials to ensure legitimate representation in negotiations.",
      "subDiscipline": "dance representation",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits dance representation in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Agent Verification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers verify their agents' credentials to ensure legitimate representation in negotiations.\nDiscipline: Dance & Choreography (dance representation).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits dance representation in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $500M — professional dance companies · SOM $50M — artistic directors\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$500M — professional dance companies",
      "som": "$50M — artistic directors"
    },
    {
      "id": "dance-teacher-accreditation-proof-0",
      "theme": "dance",
      "title": "Teacher Accreditation Proof",
      "pitch": "Teacher Accreditation Proof issues a signed verifiable credential for dance education that the holder keeps and reuses anywhere.",
      "concept": "Dance teachers can obtain verifiable credentials from education boards, proving their teaching qualifications to potential employers.",
      "subDiscipline": "dance education",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches dance education in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Teacher Accreditation Proof\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDance teachers can obtain verifiable credentials from education boards, proving their teaching qualifications to potential employers.\nDiscipline: Dance & Choreography (dance education).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches dance education in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry · SAM $2B — arts grants and funding · SOM $200M — dance non-profits\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry",
      "sam": "$2B — arts grants and funding",
      "som": "$200M — dance non-profits"
    },
    {
      "id": "dance-choreographic-collaboration-0",
      "theme": "dance",
      "title": "Choreographic Collaboration",
      "pitch": "Choreographic Collaboration verifies a credential at the gate for collaborative choreography — proof without a phone call to the issuer.",
      "concept": "Dancers prove their collaboration history to attract potential partners for projects.",
      "subDiscipline": "collaborative choreography",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for collaborative choreography in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Choreographic Collaboration\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers prove their collaboration history to attract potential partners for projects.\nDiscipline: Dance & Choreography (collaborative choreography).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for collaborative choreography in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry and studio market · SAM $800M — digital tools for choreographers and teachers · SOM $50M — AI audio generation for movement professionals\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-festival-participation-proof-0",
      "theme": "dance",
      "title": "Festival Participation Proof",
      "pitch": "Festival Participation Proof mints a did:prism for event access so identity travels with the person, not the platform.",
      "concept": "Dancers confirm their participation in dance festivals, obtaining credentials needed for entry and recognition.",
      "subDiscipline": "event access",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits event access in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Festival Participation Proof\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers confirm their participation in dance festivals, obtaining credentials needed for entry and recognition.\nDiscipline: Dance & Choreography (event access).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits event access in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry and studio market · SAM $800M — digital tools for choreographers and teachers · SOM $50M — AI audio generation for movement professionals\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-choreographic-rights-0",
      "theme": "dance",
      "title": "Choreographic Rights",
      "pitch": "Choreographic Rights pairs two wallets over DIDComm so dance licensing can exchange trusted claims on a private channel.",
      "concept": "Choreographers prove ownership of their work to ensure proper licensing and royalties.",
      "subDiscipline": "dance licensing",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits dance licensing in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Choreographic Rights\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nChoreographers prove ownership of their work to ensure proper licensing and royalties.\nDiscipline: Dance & Choreography (dance licensing).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits dance licensing in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry and studio market · SAM $800M — digital tools for choreographers and teachers · SOM $50M — AI audio generation for movement professionals\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-choreography-licensing-permissions-0",
      "theme": "dance",
      "title": "Choreography Licensing Permissions",
      "pitch": "Choreography Licensing Permissions issues a signed verifiable credential for licensing that the holder keeps and reuses anywhere.",
      "concept": "Choreographers can issue credentials that grant permission for their works to be performed by dancers and companies.",
      "subDiscipline": "licensing",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches licensing in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Choreography Licensing Permissions\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nChoreographers can issue credentials that grant permission for their works to be performed by dancers and companies.\nDiscipline: Dance & Choreography (licensing).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches licensing in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry and studio market · SAM $800M — digital tools for choreographers and teachers · SOM $50M — AI audio generation for movement professionals\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-dance-fitness-certification-0",
      "theme": "dance",
      "title": "Dance Fitness Certification",
      "pitch": "Dance Fitness Certification verifies a credential at the gate for dance fitness — proof without a phone call to the issuer.",
      "concept": "Instructors present proof of dance fitness certifications to teach specialized classes.",
      "subDiscipline": "dance fitness",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for dance fitness in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Dance Fitness Certification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nInstructors present proof of dance fitness certifications to teach specialized classes.\nDiscipline: Dance & Choreography (dance fitness).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for dance fitness in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry and studio market · SAM $800M — digital tools for choreographers and teachers · SOM $50M — AI audio generation for movement professionals\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-choreographer-showreel-identifier-0",
      "theme": "dance",
      "title": "Choreographer Showreel Identifier",
      "pitch": "Choreographer Showreel Identifier mints a did:prism for portfolio curation so identity travels with the person, not the platform.",
      "concept": "Choreographers publish a verified identifier for their showreels, providing access to potential employers.",
      "subDiscipline": "portfolio curation",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits portfolio curation in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Choreographer Showreel Identifier\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nChoreographers publish a verified identifier for their showreels, providing access to potential employers.\nDiscipline: Dance & Choreography (portfolio curation).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits portfolio curation in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry and studio market · SAM $800M — digital tools for choreographers and teachers · SOM $50M — AI audio generation for movement professionals\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-audition-readiness-0",
      "theme": "dance",
      "title": "Audition Readiness",
      "pitch": "Audition Readiness pairs two wallets over DIDComm so dance auditions can exchange trusted claims on a private channel.",
      "concept": "Dancers verify their readiness and qualifications for upcoming auditions through a secure profile.",
      "subDiscipline": "dance auditions",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits dance auditions in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Audition Readiness\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers verify their readiness and qualifications for upcoming auditions through a secure profile.\nDiscipline: Dance & Choreography (dance auditions).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits dance auditions in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry and studio market · SAM $800M — digital tools for choreographers and teachers · SOM $50M — AI audio generation for movement professionals\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-masterclass-attendance-verification-0",
      "theme": "dance",
      "title": "Masterclass Attendance Verification",
      "pitch": "Masterclass Attendance Verification issues a signed verifiable credential for specialty workshops that the holder keeps and reuses anywhere.",
      "concept": "Participants receive credentials verifying attendance at masterclasses, which they can share with studios to prove experience.",
      "subDiscipline": "specialty workshops",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches specialty workshops in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Masterclass Attendance Verification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nParticipants receive credentials verifying attendance at masterclasses, which they can share with studios to prove experience.\nDiscipline: Dance & Choreography (specialty workshops).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches specialty workshops in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry and studio market · SAM $800M — digital tools for choreographers and teachers · SOM $50M — AI audio generation for movement professionals\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-event-participation-proof-0",
      "theme": "dance",
      "title": "Event Participation Proof",
      "pitch": "Event Participation Proof verifies a credential at the gate for community events — proof without a phone call to the issuer.",
      "concept": "Dancers validate their attendance at community dance events for future networking opportunities.",
      "subDiscipline": "community events",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for community events in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Event Participation Proof\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers validate their attendance at community dance events for future networking opportunities.\nDiscipline: Dance & Choreography (community events).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for community events in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry and studio market · SAM $800M — digital tools for choreographers and teachers · SOM $50M — AI audio generation for movement professionals\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-movement-research-sharing-0",
      "theme": "dance",
      "title": "Movement Research Sharing",
      "pitch": "Movement Research Sharing mints a did:prism for research collaboration so identity travels with the person, not the platform.",
      "concept": "Dancers authenticate their research contributions to encourage collaborative projects and ensure proper recognition.",
      "subDiscipline": "research collaboration",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits research collaboration in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Movement Research Sharing\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers authenticate their research contributions to encourage collaborative projects and ensure proper recognition.\nDiscipline: Dance & Choreography (research collaboration).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits research collaboration in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry and studio market · SAM $800M — digital tools for choreographers and teachers · SOM $50M — AI audio generation for movement professionals\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-provenance-tracker-0",
      "theme": "dance",
      "title": "Provenance Tracker",
      "pitch": "Provenance Tracker pairs two wallets over DIDComm so dance authenticity can exchange trusted claims on a private channel.",
      "concept": "Choreographers log and validate the history of their works to ensure authenticity and provenance.",
      "subDiscipline": "dance authenticity",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits dance authenticity in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Provenance Tracker\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nChoreographers log and validate the history of their works to ensure authenticity and provenance.\nDiscipline: Dance & Choreography (dance authenticity).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits dance authenticity in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry and studio market · SAM $800M — digital tools for choreographers and teachers · SOM $50M — AI audio generation for movement professionals\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-choreographic-inspiration-credits-0",
      "theme": "dance",
      "title": "Choreographic Inspiration Credits",
      "pitch": "Choreographic Inspiration Credits issues a signed verifiable credential for artistic attribution that the holder keeps and reuses anywhere.",
      "concept": "Artists can issue credentials that provide attribution for inspirations and influences in their choreographic works.",
      "subDiscipline": "artistic attribution",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches artistic attribution in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Choreographic Inspiration Credits\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nArtists can issue credentials that provide attribution for inspirations and influences in their choreographic works.\nDiscipline: Dance & Choreography (artistic attribution).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches artistic attribution in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry and studio market · SAM $800M — digital tools for choreographers and teachers · SOM $50M — AI audio generation for movement professionals\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-reputation-management-0",
      "theme": "dance",
      "title": "Reputation Management",
      "pitch": "Reputation Management verifies a credential at the gate for professional reputation — proof without a phone call to the issuer.",
      "concept": "Dancers can present proof of endorsements from credible sources to enhance their professional reputation.",
      "subDiscipline": "professional reputation",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for professional reputation in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Reputation Management\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers can present proof of endorsements from credible sources to enhance their professional reputation.\nDiscipline: Dance & Choreography (professional reputation).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for professional reputation in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry and studio market · SAM $800M — digital tools for choreographers and teachers · SOM $50M — AI audio generation for movement professionals\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-health-and-wellness-credentials-0",
      "theme": "dance",
      "title": "Health and Wellness Credentials",
      "pitch": "Health and Wellness Credentials mints a did:prism for wellness in dance so identity travels with the person, not the platform.",
      "concept": "Dancers verify their health and wellness training to promote safe practices within studios and performances.",
      "subDiscipline": "wellness in dance",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits wellness in dance in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Health and Wellness Credentials\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers verify their health and wellness training to promote safe practices within studios and performances.\nDiscipline: Dance & Choreography (wellness in dance).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits wellness in dance in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry and studio market · SAM $800M — digital tools for choreographers and teachers · SOM $50M — AI audio generation for movement professionals\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-accessibility-pass-0",
      "theme": "dance",
      "title": "Accessibility Pass",
      "pitch": "Accessibility Pass pairs two wallets over DIDComm so inclusive dance can exchange trusted claims on a private channel.",
      "concept": "Dancers with disabilities prove their eligibility for specialized programs and accessibility services.",
      "subDiscipline": "inclusive dance",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits inclusive dance in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Accessibility Pass\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers with disabilities prove their eligibility for specialized programs and accessibility services.\nDiscipline: Dance & Choreography (inclusive dance).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits inclusive dance in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry and studio market · SAM $800M — digital tools for choreographers and teachers · SOM $50M — AI audio generation for movement professionals\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-performance-health-ratings-0",
      "theme": "dance",
      "title": "Performance Health Ratings",
      "pitch": "Performance Health Ratings issues a signed verifiable credential for health and wellness that the holder keeps and reuses anywhere.",
      "concept": "Dancers can obtain credentials confirming health and readiness statuses that studios can verify before casting.",
      "subDiscipline": "health and wellness",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches health and wellness in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Performance Health Ratings\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers can obtain credentials confirming health and readiness statuses that studios can verify before casting.\nDiscipline: Dance & Choreography (health and wellness).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches health and wellness in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry and studio market · SAM $800M — digital tools for choreographers and teachers · SOM $50M — AI audio generation for movement professionals\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-access-to-exclusive-classes-0",
      "theme": "dance",
      "title": "Access to Exclusive Classes",
      "pitch": "Access to Exclusive Classes verifies a credential at the gate for dance classes — proof without a phone call to the issuer.",
      "concept": "Dancers prove their experience level to gain entry to advanced or exclusive dance classes.",
      "subDiscipline": "dance classes",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for dance classes in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Access to Exclusive Classes\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers prove their experience level to gain entry to advanced or exclusive dance classes.\nDiscipline: Dance & Choreography (dance classes).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for dance classes in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry and studio market · SAM $800M — digital tools for choreographers and teachers · SOM $50M — AI audio generation for movement professionals\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-performance-rights-validator-0",
      "theme": "dance",
      "title": "Performance Rights Validator",
      "pitch": "Performance Rights Validator mints a did:prism for intellectual property so identity travels with the person, not the platform.",
      "concept": "Choreographers prove ownership of their works to negotiate performance rights with theaters and dance companies.",
      "subDiscipline": "intellectual property",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits intellectual property in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Performance Rights Validator\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nChoreographers prove ownership of their works to negotiate performance rights with theaters and dance companies.\nDiscipline: Dance & Choreography (intellectual property).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits intellectual property in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry and studio market · SAM $800M — digital tools for choreographers and teachers · SOM $50M — AI audio generation for movement professionals\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-choreography-collaborations-0",
      "theme": "dance",
      "title": "Choreography Collaborations",
      "pitch": "Choreography Collaborations pairs two wallets over DIDComm so creative partnerships can exchange trusted claims on a private channel.",
      "concept": "Artists create and manage collaborative projects by proving their identity and previous work to each other.",
      "subDiscipline": "creative partnerships",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits creative partnerships in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Choreography Collaborations\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nArtists create and manage collaborative projects by proving their identity and previous work to each other.\nDiscipline: Dance & Choreography (creative partnerships).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits creative partnerships in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry and studio market · SAM $800M — digital tools for choreographers and teachers · SOM $50M — AI audio generation for movement professionals\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-workshop-instructor-proof-0",
      "theme": "dance",
      "title": "Workshop Instructor Proof",
      "pitch": "Workshop Instructor Proof issues a signed verifiable credential for community workshops that the holder keeps and reuses anywhere.",
      "concept": "Workshop facilitators issue credentials verifying their expertise to attract participants and build trust in the community.",
      "subDiscipline": "community workshops",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches community workshops in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Workshop Instructor Proof\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nWorkshop facilitators issue credentials verifying their expertise to attract participants and build trust in the community.\nDiscipline: Dance & Choreography (community workshops).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches community workshops in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry and studio market · SAM $800M — digital tools for choreographers and teachers · SOM $50M — AI audio generation for movement professionals\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-choreography-submission-verification-0",
      "theme": "dance",
      "title": "Choreography Submission Verification",
      "pitch": "Choreography Submission Verification verifies a credential at the gate for submission platforms — proof without a phone call to the issuer.",
      "concept": "Dancers present proof of choreography to secure spots in competitions or showcases.",
      "subDiscipline": "submission platforms",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for submission platforms in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Choreography Submission Verification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers present proof of choreography to secure spots in competitions or showcases.\nDiscipline: Dance & Choreography (submission platforms).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for submission platforms in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry and studio market · SAM $800M — digital tools for choreographers and teachers · SOM $50M — AI audio generation for movement professionals\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-legacy-choreographer-registry-0",
      "theme": "dance",
      "title": "Legacy Choreographer Registry",
      "pitch": "Legacy Choreographer Registry mints a did:prism for historical dance so identity travels with the person, not the platform.",
      "concept": "Established choreographers document their legacies, allowing new generations to verify their influences and teachings.",
      "subDiscipline": "historical dance",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits historical dance in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Legacy Choreographer Registry\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nEstablished choreographers document their legacies, allowing new generations to verify their influences and teachings.\nDiscipline: Dance & Choreography (historical dance).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits historical dance in Dance & Choreography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry and studio market · SAM $800M — digital tools for choreographers and teachers · SOM $50M — AI audio generation for movement professionals\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-festival-credentials-0",
      "theme": "dance",
      "title": "Festival Credentials",
      "pitch": "Festival Credentials pairs two wallets over DIDComm so dance showcases can exchange trusted claims on a private channel.",
      "concept": "Dancers prove their past performances to gain entry to exclusive dance festivals and showcases.",
      "subDiscipline": "dance showcases",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits dance showcases in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Festival Credentials\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDancers prove their past performances to gain entry to exclusive dance festivals and showcases.\nDiscipline: Dance & Choreography (dance showcases).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits dance showcases in Dance & Choreography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry and studio market · SAM $800M — digital tools for choreographers and teachers · SOM $50M — AI audio generation for movement professionals\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-dance-film-credentials-0",
      "theme": "dance",
      "title": "Dance Film Credentials",
      "pitch": "Dance Film Credentials issues a signed verifiable credential for film production that the holder keeps and reuses anywhere.",
      "concept": "Filmmakers can obtain verifiable credentials for dance sequences to prove authenticity and rights during productions.",
      "subDiscipline": "film production",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches film production in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Dance Film Credentials\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFilmmakers can obtain verifiable credentials for dance sequences to prove authenticity and rights during productions.\nDiscipline: Dance & Choreography (film production).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches film production in Dance & Choreography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry and studio market · SAM $800M — digital tools for choreographers and teachers · SOM $50M — AI audio generation for movement professionals\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    },
    {
      "id": "dance-dance-technique-certification-0",
      "theme": "dance",
      "title": "Dance Technique Certification",
      "pitch": "Dance Technique Certification verifies a credential at the gate for dance techniques — proof without a phone call to the issuer.",
      "concept": "Teachers verify their mastery of specific dance techniques to lead classes or workshops.",
      "subDiscipline": "dance techniques",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for dance techniques in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Dance Technique Certification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTeachers verify their mastery of specific dance techniques to lead classes or workshops.\nDiscipline: Dance & Choreography (dance techniques).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for dance techniques in Dance & Choreography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\n<<MODE_BLOCK>>\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — global dance industry and studio market · SAM $800M — digital tools for choreographers and teachers · SOM $50M — AI audio generation for movement professionals\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — global dance industry and studio market",
      "sam": "$800M — digital tools for choreographers and teachers",
      "som": "$50M — AI audio generation for movement professionals"
    }
  ]
}
