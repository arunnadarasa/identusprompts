{
  "theme": {
    "slug": "film-animation",
    "name": "Filmmaking & Animation",
    "emoji": "🎬",
    "audience": "filmmakers, animators, motion designers, storyboard artists",
    "market_anchor": "the animation industry (~$400B incl. film/TV) with >500K working animators"
  },
  "ideas": [
    {
      "id": "film-animation-credits-verifier-0",
      "theme": "film-animation",
      "title": "Credits Verifier",
      "pitch": "Credits Verifier mints a did:prism for film credits so identity travels with the person, not the platform.",
      "concept": "Filmmakers can publish their contributions as verified DIDs, allowing others to confirm their credits in film projects.",
      "subDiscipline": "film credits",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits film credits in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Credits Verifier\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFilmmakers can publish their contributions as verified DIDs, allowing others to confirm their credits in film projects.\nDiscipline: Filmmaking & Animation (film credits).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits film credits in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $15B — pre-production and storyboarding software · SOM $50M — indie animation studio tooling\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$15B — pre-production and storyboarding software",
      "som": "$50M — indie animation studio tooling"
    },
    {
      "id": "film-animation-collaborative-credits-0",
      "theme": "film-animation",
      "title": "Collaborative Credits",
      "pitch": "Collaborative Credits pairs two wallets over DIDComm so film production can exchange trusted claims on a private channel.",
      "concept": "Filmmakers can invite collaborators to verify their contributions, ensuring proper credit allocation in the final film project.",
      "subDiscipline": "film production",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits film production in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Collaborative Credits\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFilmmakers can invite collaborators to verify their contributions, ensuring proper credit allocation in the final film project.\nDiscipline: Filmmaking & Animation (film production).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits film production in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $5B — voice acting and casting services · SOM $20M — freelance voice actor coaching\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$5B — voice acting and casting services",
      "som": "$20M — freelance voice actor coaching"
    },
    {
      "id": "film-animation-credited-collaborators-0",
      "theme": "film-animation",
      "title": "Credited Collaborators",
      "pitch": "Credited Collaborators issues a signed verifiable credential for crew roles that the holder keeps and reuses anywhere.",
      "concept": "Proves crew roles and contributions in projects to ensure proper credit attribution for filmmakers and animators.",
      "subDiscipline": "crew roles",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches crew roles in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Credited Collaborators\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nProves crew roles and contributions in projects to ensure proper credit attribution for filmmakers and animators.\nDiscipline: Filmmaking & Animation (crew roles).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches crew roles in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $12B — 3D animation software and plugins · SOM $40M — freelance 3D animator utilities\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$12B — 3D animation software and plugins",
      "som": "$40M — freelance 3D animator utilities"
    },
    {
      "id": "film-animation-identity-of-creators-0",
      "theme": "film-animation",
      "title": "Identity of Creators",
      "pitch": "Identity of Creators verifies a credential at the gate for film credits — proof without a phone call to the issuer.",
      "concept": "Filmmakers prove their contributions to a project, ensuring accurate credits to producers and studios without intermediaries.",
      "subDiscipline": "film credits",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for film credits in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Identity of Creators\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFilmmakers prove their contributions to a project, ensuring accurate credits to producers and studios without intermediaries.\nDiscipline: Filmmaking & Animation (film credits).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for film credits in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $8B — screenwriting and development tools · SOM $30M — independent screenwriter software\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$8B — screenwriting and development tools",
      "som": "$30M — independent screenwriter software"
    },
    {
      "id": "film-animation-rights-tracker-0",
      "theme": "film-animation",
      "title": "Rights Tracker",
      "pitch": "Rights Tracker mints a did:prism for licensing so identity travels with the person, not the platform.",
      "concept": "Animators register their work with DIDs to prove ownership and licensing rights to potential buyers or collaborators.",
      "subDiscipline": "licensing",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits licensing in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Rights Tracker\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAnimators register their work with DIDs to prove ownership and licensing rights to potential buyers or collaborators.\nDiscipline: Filmmaking & Animation (licensing).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits licensing in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $6B — post-production audio services · SOM $25M — boutique foley studio tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$6B — post-production audio services",
      "som": "$25M — boutique foley studio tools"
    },
    {
      "id": "film-animation-rights-tracker-1",
      "theme": "film-animation",
      "title": "Rights Tracker",
      "pitch": "Rights Tracker pairs two wallets over DIDComm so licensing can exchange trusted claims on a private channel.",
      "concept": "Animators can create and share verified licenses for their work, proving ownership and usage rights to potential clients.",
      "subDiscipline": "licensing",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits licensing in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Rights Tracker\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAnimators can create and share verified licenses for their work, proving ownership and usage rights to potential clients.\nDiscipline: Filmmaking & Animation (licensing).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits licensing in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $10B — motion design and VFX software · SOM $35M — freelance motion designer plugins\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$10B — motion design and VFX software",
      "som": "$35M — freelance motion designer plugins"
    },
    {
      "id": "film-animation-rightful-owners-0",
      "theme": "film-animation",
      "title": "Rightful Owners",
      "pitch": "Rightful Owners issues a signed verifiable credential for licensing that the holder keeps and reuses anywhere.",
      "concept": "Allows animators to prove ownership and licensing rights of their work to studios and clients.",
      "subDiscipline": "licensing",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches licensing in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Rightful Owners\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAllows animators to prove ownership and licensing rights of their work to studios and clients.\nDiscipline: Filmmaking & Animation (licensing).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches licensing in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $4B — digital intermediate and color grading · SOM $15M — independent colorist workstations\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$4B — digital intermediate and color grading",
      "som": "$15M — independent colorist workstations"
    },
    {
      "id": "film-animation-age-verification-tool-0",
      "theme": "film-animation",
      "title": "Age Verification Tool",
      "pitch": "Age Verification Tool verifies a credential at the gate for animation festivals — proof without a phone call to the issuer.",
      "concept": "Animators prove their age to qualify for youth competitions, with only age disclosed to event organizers.",
      "subDiscipline": "animation festivals",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for animation festivals in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Age Verification Tool\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAnimators prove their age to qualify for youth competitions, with only age disclosed to event organizers.\nDiscipline: Filmmaking & Animation (animation festivals).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for animation festivals in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $18B — visual effects production services · SOM $60M — mid-tier VFX house software\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$18B — visual effects production services",
      "som": "$60M — mid-tier VFX house software"
    },
    {
      "id": "film-animation-accredited-guild-pass-0",
      "theme": "film-animation",
      "title": "Accredited Guild Pass",
      "pitch": "Accredited Guild Pass mints a did:prism for guild membership so identity travels with the person, not the platform.",
      "concept": "Professionals can verifiably prove their guild membership status to access exclusive industry events and resources.",
      "subDiscipline": "guild membership",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits guild membership in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Accredited Guild Pass\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nProfessionals can verifiably prove their guild membership status to access exclusive industry events and resources.\nDiscipline: Filmmaking & Animation (guild membership).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits guild membership in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $2B — entertainment pitching and development · SOM $10M — indie producer presentation tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$2B — entertainment pitching and development",
      "som": "$10M — indie producer presentation tools"
    },
    {
      "id": "film-animation-authenticity-dashboard-0",
      "theme": "film-animation",
      "title": "Authenticity Dashboard",
      "pitch": "Authenticity Dashboard pairs two wallets over DIDComm so archival preservation can exchange trusted claims on a private channel.",
      "concept": "Artists can authenticate their past works and share provenance details with galleries, collectors, and audiences to establish trust.",
      "subDiscipline": "archival preservation",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits archival preservation in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Authenticity Dashboard\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nArtists can authenticate their past works and share provenance details with galleries, collectors, and audiences to establish trust.\nDiscipline: Filmmaking & Animation (archival preservation).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits archival preservation in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $7B — art direction and concept art · SOM $25M — freelance concept artist utilities\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$7B — art direction and concept art",
      "som": "$25M — freelance concept artist utilities"
    },
    {
      "id": "film-animation-verified-festivals-0",
      "theme": "film-animation",
      "title": "Verified Festivals",
      "pitch": "Verified Festivals issues a signed verifiable credential for film festivals that the holder keeps and reuses anywhere.",
      "concept": "Enables filmmakers to prove eligibility and participation in festivals to streamline submission and accreditation processes.",
      "subDiscipline": "film festivals",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches film festivals in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Verified Festivals\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nEnables filmmakers to prove eligibility and participation in festivals to streamline submission and accreditation processes.\nDiscipline: Filmmaking & Animation (film festivals).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches film festivals in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $9B — character design and modeling · SOM $30M — boutique animation studio tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$9B — character design and modeling",
      "som": "$30M — boutique animation studio tools"
    },
    {
      "id": "film-animation-membership-validator-0",
      "theme": "film-animation",
      "title": "Membership Validator",
      "pitch": "Membership Validator verifies a credential at the gate for guild status — proof without a phone call to the issuer.",
      "concept": "Animators present their guild membership status to access exclusive workshops, revealing only necessary membership details.",
      "subDiscipline": "guild status",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for guild status in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Membership Validator\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAnimators present their guild membership status to access exclusive workshops, revealing only necessary membership details.\nDiscipline: Filmmaking & Animation (guild status).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for guild status in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $11B — video editing and post-production · SOM $45M — independent film editor software\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$11B — video editing and post-production",
      "som": "$45M — independent film editor software"
    },
    {
      "id": "film-animation-film-provenance-0",
      "theme": "film-animation",
      "title": "Film Provenance",
      "pitch": "Film Provenance mints a did:prism for film archives so identity travels with the person, not the platform.",
      "concept": "Filmmakers document the provenance of their films via DIDs to enhance authenticity when submitting to festivals.",
      "subDiscipline": "film archives",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits film archives in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Film Provenance\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFilmmakers document the provenance of their films via DIDs to enhance authenticity when submitting to festivals.\nDiscipline: Filmmaking & Animation (film archives).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits film archives in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $3B — stop motion and practical animation · SOM $12M — indie stop motion studio gear\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$3B — stop motion and practical animation",
      "som": "$12M — indie stop motion studio gear"
    },
    {
      "id": "film-animation-project-memberships-0",
      "theme": "film-animation",
      "title": "Project Memberships",
      "pitch": "Project Memberships pairs two wallets over DIDComm so film guilds can exchange trusted claims on a private channel.",
      "concept": "Members of filmmaking guilds can verify their status and qualifications to access exclusive resources and opportunities.",
      "subDiscipline": "film guilds",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits film guilds in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Project Memberships\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nMembers of filmmaking guilds can verify their status and qualifications to access exclusive resources and opportunities.\nDiscipline: Filmmaking & Animation (film guilds).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits film guilds in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $14B — camera and lighting equipment services · SOM $50M — freelance gaffer and grip tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$14B — camera and lighting equipment services",
      "som": "$50M — freelance gaffer and grip tools"
    },
    {
      "id": "film-animation-skill-verified-0",
      "theme": "film-animation",
      "title": "Skill Verified",
      "pitch": "Skill Verified issues a signed verifiable credential for talent credentials that the holder keeps and reuses anywhere.",
      "concept": "Certifies skills and qualifications of animators to hiring managers or agencies through issued verifiable credentials.",
      "subDiscipline": "talent credentials",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches talent credentials in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Skill Verified\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCertifies skills and qualifications of animators to hiring managers or agencies through issued verifiable credentials.\nDiscipline: Filmmaking & Animation (talent credentials).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches talent credentials in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $5B — location scouting and management · SOM $18M — independent location manager apps\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$5B — location scouting and management",
      "som": "$18M — independent location manager apps"
    },
    {
      "id": "film-animation-provenance-finder-0",
      "theme": "film-animation",
      "title": "Provenance Finder",
      "pitch": "Provenance Finder verifies a credential at the gate for original works — proof without a phone call to the issuer.",
      "concept": "Animators verify the authenticity of their original artworks to buyers, showing only relevant ownership history.",
      "subDiscipline": "original works",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for original works in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Provenance Finder\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAnimators verify the authenticity of their original artworks to buyers, showing only relevant ownership history.\nDiscipline: Filmmaking & Animation (original works).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for original works in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $4B — stunt coordination and action design · SOM $15M — specialized stunt team software\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$4B — stunt coordination and action design",
      "som": "$15M — specialized stunt team software"
    },
    {
      "id": "film-animation-age-gate-access-0",
      "theme": "film-animation",
      "title": "Age Gate Access",
      "pitch": "Age Gate Access mints a did:prism for content ratings so identity travels with the person, not the platform.",
      "concept": "Animated content creators verify age restrictions using DIDs to control access based on viewer eligibility.",
      "subDiscipline": "content ratings",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits content ratings in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Age Gate Access\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAnimated content creators verify age restrictions using DIDs to control access based on viewer eligibility.\nDiscipline: Filmmaking & Animation (content ratings).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits content ratings in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $2B — script supervision and on-set data · SOM $8M — freelance script supervisor tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$2B — script supervision and on-set data",
      "som": "$8M — freelance script supervisor tools"
    },
    {
      "id": "film-animation-consent-manager-0",
      "theme": "film-animation",
      "title": "Consent Manager",
      "pitch": "Consent Manager pairs two wallets over DIDComm so casting can exchange trusted claims on a private channel.",
      "concept": "Actors can manage and share verified consent for specific projects, ensuring clear agreements with producers and directors.",
      "subDiscipline": "casting",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits casting in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Consent Manager\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nActors can manage and share verified consent for specific projects, ensuring clear agreements with producers and directors.\nDiscipline: Filmmaking & Animation (casting).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits casting in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $6B — post-production audio services · SOM $22M — boutique ADR studio software\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$6B — post-production audio services",
      "som": "$22M — boutique ADR studio software"
    },
    {
      "id": "film-animation-authentic-editions-0",
      "theme": "film-animation",
      "title": "Authentic Editions",
      "pitch": "Authentic Editions issues a signed verifiable credential for animation series that the holder keeps and reuses anywhere.",
      "concept": "Verifies authenticity and provenance of limited edition animation frames for collectors and galleries.",
      "subDiscipline": "animation series",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches animation series in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Authentic Editions\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nVerifies authenticity and provenance of limited edition animation frames for collectors and galleries.\nDiscipline: Filmmaking & Animation (animation series).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches animation series in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $3B — film festival and distribution prep · SOM $10M — indie filmmaker career tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$3B — film festival and distribution prep",
      "som": "$10M — indie filmmaker career tools"
    },
    {
      "id": "film-animation-consent-tracker-0",
      "theme": "film-animation",
      "title": "Consent Tracker",
      "pitch": "Consent Tracker verifies a credential at the gate for collaboration — proof without a phone call to the issuer.",
      "concept": "Filmmakers present proof of consent for using actors' likenesses to production teams, ensuring privacy while confirming permissions.",
      "subDiscipline": "collaboration",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for collaboration in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Consent Tracker\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFilmmakers present proof of consent for using actors' likenesses to production teams, ensuring privacy while confirming permissions.\nDiscipline: Filmmaking & Animation (collaboration).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for collaboration in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $16B — production management and scheduling · SOM $55M — indie production company software\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$16B — production management and scheduling",
      "som": "$55M — indie production company software"
    },
    {
      "id": "film-animation-storyboard-credentials-0",
      "theme": "film-animation",
      "title": "Storyboard Credentials",
      "pitch": "Storyboard Credentials mints a did:prism for storyboards so identity travels with the person, not the platform.",
      "concept": "Storyboard artists publish their credentials and past work DIDs to prove their expertise to potential employers.",
      "subDiscipline": "storyboards",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits storyboards in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Storyboard Credentials\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nStoryboard artists publish their credentials and past work DIDs to prove their expertise to potential employers.\nDiscipline: Filmmaking & Animation (storyboards).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits storyboards in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $8B — technical animation and simulation · SOM $30M — VFX technical director tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$8B — technical animation and simulation",
      "som": "$30M — VFX technical director tools"
    },
    {
      "id": "film-animation-backstage-pass-validator-0",
      "theme": "film-animation",
      "title": "Backstage Pass Validator",
      "pitch": "Backstage Pass Validator pairs two wallets over DIDComm so event access can exchange trusted claims on a private channel.",
      "concept": "Event organizers can invite crew members to receive verified backstage access for events and screenings, ensuring only authorized personnel enter.",
      "subDiscipline": "event access",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits event access in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Backstage Pass Validator\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nEvent organizers can invite crew members to receive verified backstage access for events and screenings, ensuring only authorized personnel enter.\nDiscipline: Filmmaking & Animation (event access).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits event access in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $7B — 3D rigging and pipeline tools · SOM $25M — freelance character rigger plugins\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$7B — 3D rigging and pipeline tools",
      "som": "$25M — freelance character rigger plugins"
    },
    {
      "id": "film-animation-union-access-0",
      "theme": "film-animation",
      "title": "Union Access",
      "pitch": "Union Access issues a signed verifiable credential for guild memberships that the holder keeps and reuses anywhere.",
      "concept": "Confirms union membership status for animators and filmmakers to access industry benefits and protections.",
      "subDiscipline": "guild memberships",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches guild memberships in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Union Access\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nConfirms union membership status for animators and filmmakers to access industry benefits and protections.\nDiscipline: Filmmaking & Animation (guild memberships).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches guild memberships in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $10B — render farm and pipeline management · SOM $40M — mid-tier studio pipeline tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$10B — render farm and pipeline management",
      "som": "$40M — mid-tier studio pipeline tools"
    },
    {
      "id": "film-animation-eligibility-checker-0",
      "theme": "film-animation",
      "title": "Eligibility Checker",
      "pitch": "Eligibility Checker verifies a credential at the gate for grants — proof without a phone call to the issuer.",
      "concept": "Filmmakers prove eligibility for funding grants to institutions, only revealing qualifying criteria without personal data.",
      "subDiscipline": "grants",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for grants in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Eligibility Checker\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFilmmakers prove eligibility for funding grants to institutions, only revealing qualifying criteria without personal data.\nDiscipline: Filmmaking & Animation (grants).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for grants in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $5B — art department and set decoration · SOM $18M — freelance set dresser utilities\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$5B — art department and set decoration",
      "som": "$18M — freelance set dresser utilities"
    },
    {
      "id": "film-animation-consent-manager-1",
      "theme": "film-animation",
      "title": "Consent Manager",
      "pitch": "Consent Manager mints a did:prism for talent consent so identity travels with the person, not the platform.",
      "concept": "Actors and contributors register DIDs to provide verifiable consent for the use of their likeness or contributions in projects.",
      "subDiscipline": "talent consent",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits talent consent in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Consent Manager\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nActors and contributors register DIDs to provide verifiable consent for the use of their likeness or contributions in projects.\nDiscipline: Filmmaking & Animation (talent consent).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits talent consent in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $4B — practical effects and makeup services · SOM $15M — freelance SFX makeup artist tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$4B — practical effects and makeup services",
      "som": "$15M — freelance SFX makeup artist tools"
    },
    {
      "id": "film-animation-storyboard-collaborator-0",
      "theme": "film-animation",
      "title": "Storyboard Collaborator",
      "pitch": "Storyboard Collaborator pairs two wallets over DIDComm so storyboarding can exchange trusted claims on a private channel.",
      "concept": "Storyboard artists can connect with directors to verify ideas and drafts, streamlining the creative feedback process.",
      "subDiscipline": "storyboarding",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits storyboarding in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Storyboard Collaborator\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nStoryboard artists can connect with directors to verify ideas and drafts, streamlining the creative feedback process.\nDiscipline: Filmmaking & Animation (storyboarding).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits storyboarding in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $12B — pre-production and storyboarding software · SOM $150M — indie and studio storyboard artists\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$12B — pre-production and storyboarding software",
      "som": "$150M — indie and studio storyboard artists"
    },
    {
      "id": "film-animation-storyboard-approval-0",
      "theme": "film-animation",
      "title": "Storyboard Approval",
      "pitch": "Storyboard Approval issues a signed verifiable credential for storyboarding that the holder keeps and reuses anywhere.",
      "concept": "Validates storyboard artist credentials to directors and producers for project approvals and collaborations.",
      "subDiscipline": "storyboarding",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches storyboarding in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Storyboard Approval\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nValidates storyboard artist credentials to directors and producers for project approvals and collaborations.\nDiscipline: Filmmaking & Animation (storyboarding).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches storyboarding in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global entertainment production · SAM $8B — voice acting and casting services · SOM $90M — freelance voice actors and coaches\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global entertainment production",
      "sam": "$8B — voice acting and casting services",
      "som": "$90M — freelance voice actors and coaches"
    },
    {
      "id": "film-animation-access-control-app-0",
      "theme": "film-animation",
      "title": "Access Control App",
      "pitch": "Access Control App verifies a credential at the gate for film festivals — proof without a phone call to the issuer.",
      "concept": "Event planners verify attendees' VIP statuses without revealing identities, ensuring secure access to behind-the-scenes areas.",
      "subDiscipline": "film festivals",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for film festivals in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Access Control App\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nEvent planners verify attendees' VIP statuses without revealing identities, ensuring secure access to behind-the-scenes areas.\nDiscipline: Filmmaking & Animation (film festivals).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for film festivals in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — film and television production · SAM $15B — production management and set operations · SOM $200M — independent and commercial directors\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — film and television production",
      "sam": "$15B — production management and set operations",
      "som": "$200M — independent and commercial directors"
    },
    {
      "id": "film-animation-payout-assurance-0",
      "theme": "film-animation",
      "title": "Payout Assurance",
      "pitch": "Payout Assurance mints a did:prism for royalty distribution so identity travels with the person, not the platform.",
      "concept": "Creators can verify their identity and contribution DIDs to ensure correct royalty payouts from productions.",
      "subDiscipline": "royalty distribution",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits royalty distribution in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Payout Assurance\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCreators can verify their identity and contribution DIDs to ensure correct royalty payouts from productions.\nDiscipline: Filmmaking & Animation (royalty distribution).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits royalty distribution in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation industry · SAM $20B — 3D and 2D character animation · SOM $300M — character animators and riggers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation industry",
      "sam": "$20B — 3D and 2D character animation",
      "som": "$300M — character animators and riggers"
    },
    {
      "id": "film-animation-payout-authority-0",
      "theme": "film-animation",
      "title": "Payout Authority",
      "pitch": "Payout Authority pairs two wallets over DIDComm so royalties can exchange trusted claims on a private channel.",
      "concept": "Artists can verify their contributions to projects, allowing them to claim rightful payouts from revenue generated.",
      "subDiscipline": "royalties",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits royalties in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Payout Authority\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nArtists can verify their contributions to projects, allowing them to claim rightful payouts from revenue generated.\nDiscipline: Filmmaking & Animation (royalties).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits royalties in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global film and TV market · SAM $5B — screenwriting software and services · SOM $75M — professional and aspiring screenwriters\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global film and TV market",
      "sam": "$5B — screenwriting software and services",
      "som": "$75M — professional and aspiring screenwriters"
    },
    {
      "id": "film-animation-age-restricted-content-0",
      "theme": "film-animation",
      "title": "Age Restricted Content",
      "pitch": "Age Restricted Content issues a signed verifiable credential for content ratings that the holder keeps and reuses anywhere.",
      "concept": "Proves age eligibility for creators producing content aimed at specific demographic groups.",
      "subDiscipline": "content ratings",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches content ratings in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Age Restricted Content\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nProves age eligibility for creators producing content aimed at specific demographic groups.\nDiscipline: Filmmaking & Animation (content ratings).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches content ratings in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — digital media and animation · SAM $10B — motion graphics and VFX software · SOM $120M — freelance motion designers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — digital media and animation",
      "sam": "$10B — motion graphics and VFX software",
      "som": "$120M — freelance motion designers"
    },
    {
      "id": "film-animation-accreditation-hub-0",
      "theme": "film-animation",
      "title": "Accreditation Hub",
      "pitch": "Accreditation Hub verifies a credential at the gate for educational programs — proof without a phone call to the issuer.",
      "concept": "Aspiring animators present their completed courses to potential employers, confirming qualifications without disclosing sensitive information.",
      "subDiscipline": "educational programs",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for educational programs in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Accreditation Hub\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAspiring animators present their completed courses to potential employers, confirming qualifications without disclosing sensitive information.\nDiscipline: Filmmaking & Animation (educational programs).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for educational programs in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — post-production audio market · SAM $3B — foley and sound effects creation · SOM $45M — foley stages and independent artists\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — post-production audio market",
      "sam": "$3B — foley and sound effects creation",
      "som": "$45M — foley stages and independent artists"
    },
    {
      "id": "film-animation-agent-delegation-0",
      "theme": "film-animation",
      "title": "Agent Delegation",
      "pitch": "Agent Delegation mints a did:prism for talent representation so identity travels with the person, not the platform.",
      "concept": "Artists can use DIDs to securely delegate authority to agents for contract negotiations and project approvals.",
      "subDiscipline": "talent representation",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits talent representation in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Agent Delegation\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nArtists can use DIDs to securely delegate authority to agents for contract negotiations and project approvals.\nDiscipline: Filmmaking & Animation (talent representation).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits talent representation in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — film post-production · SAM $6B — color grading and finishing · SOM $80M — freelance colorists and boutique suites\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — film post-production",
      "sam": "$6B — color grading and finishing",
      "som": "$80M — freelance colorists and boutique suites"
    },
    {
      "id": "film-animation-agent-verification-0",
      "theme": "film-animation",
      "title": "Agent Verification",
      "pitch": "Agent Verification pairs two wallets over DIDComm so representation can exchange trusted claims on a private channel.",
      "concept": "Actors can verify their representation by agents to enhance trust during casting calls and negotiations.",
      "subDiscipline": "representation",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits representation in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Agent Verification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nActors can verify their representation by agents to enhance trust during casting calls and negotiations.\nDiscipline: Filmmaking & Animation (representation).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits representation in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — visual effects industry · SAM $14B — VFX compositing and rendering · SOM $210M — VFX artists and studios\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — visual effects industry",
      "sam": "$14B — VFX compositing and rendering",
      "som": "$210M — VFX artists and studios"
    },
    {
      "id": "film-animation-project-backstage-pass-0",
      "theme": "film-animation",
      "title": "Project Backstage Pass",
      "pitch": "Project Backstage Pass issues a signed verifiable credential for event access that the holder keeps and reuses anywhere.",
      "concept": "Issues backstage passes that validate access for crew members based on their role in a production.",
      "subDiscipline": "event access",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches event access in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Project Backstage Pass\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nIssues backstage passes that validate access for crew members based on their role in a production.\nDiscipline: Filmmaking & Animation (event access).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches event access in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — film production management · SAM $4B — production scheduling software · SOM $60M — line producers and UPMs\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — film production management",
      "sam": "$4B — production scheduling software",
      "som": "$60M — line producers and UPMs"
    },
    {
      "id": "film-animation-collaboration-verifier-0",
      "theme": "film-animation",
      "title": "Collaboration Verifier",
      "pitch": "Collaboration Verifier verifies a credential at the gate for team projects — proof without a phone call to the issuer.",
      "concept": "Filmmakers prove team member roles in collaborative projects, allowing studios to assess contributions without full disclosure.",
      "subDiscipline": "team projects",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for team projects in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Collaboration Verifier\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFilmmakers prove team member roles in collaborative projects, allowing studios to assess contributions without full disclosure.\nDiscipline: Filmmaking & Animation (team projects).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for team projects in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — physical production · SAM $2B — location scouting and management · SOM $30M — location managers and scouts\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — physical production",
      "sam": "$2B — location scouting and management",
      "som": "$30M — location managers and scouts"
    },
    {
      "id": "film-animation-screening-invitations-0",
      "theme": "film-animation",
      "title": "Screening Invitations",
      "pitch": "Screening Invitations mints a did:prism for exclusive events so identity travels with the person, not the platform.",
      "concept": "Filmmakers generate DIDs to distribute verifiable invitations for exclusive film screenings to selected audience members.",
      "subDiscipline": "exclusive events",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits exclusive events in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Screening Invitations\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFilmmakers generate DIDs to distribute verifiable invitations for exclusive film screenings to selected audience members.\nDiscipline: Filmmaking & Animation (exclusive events).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits exclusive events in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — animation and VFX · SAM $5B — creature design and audio · SOM $75M — creature FX artists and sound designers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — animation and VFX",
      "sam": "$5B — creature design and audio",
      "som": "$75M — creature FX artists and sound designers"
    },
    {
      "id": "film-animation-age-verification-tool-1",
      "theme": "film-animation",
      "title": "Age Verification Tool",
      "pitch": "Age Verification Tool pairs two wallets over DIDComm so youth productions can exchange trusted claims on a private channel.",
      "concept": "Producers can invite young actors to verify their age for compliance with child labor laws in film production.",
      "subDiscipline": "youth productions",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits youth productions in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Age Verification Tool\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nProducers can invite young actors to verify their age for compliance with child labor laws in film production.\nDiscipline: Filmmaking & Animation (youth productions).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits youth productions in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — post-production editing · SAM $8B — video editing software and services · SOM $100M — freelance and studio editors\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — post-production editing",
      "sam": "$8B — video editing software and services",
      "som": "$100M — freelance and studio editors"
    },
    {
      "id": "film-animation-creative-consent-0",
      "theme": "film-animation",
      "title": "Creative Consent",
      "pitch": "Creative Consent issues a signed verifiable credential for collaboration approvals that the holder keeps and reuses anywhere.",
      "concept": "Tracks and validates consent from collaborators on usage rights for creative works in projects.",
      "subDiscipline": "collaboration approvals",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches collaboration approvals in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Creative Consent\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTracks and validates consent from collaborators on usage rights for creative works in projects.\nDiscipline: Filmmaking & Animation (collaboration approvals).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches collaboration approvals in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — TV and film development · SAM $1B — pitch prep and consulting · SOM $15M — showrunners and indie producers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — TV and film development",
      "sam": "$1B — pitch prep and consulting",
      "som": "$15M — showrunners and indie producers"
    },
    {
      "id": "film-animation-artwork-provenance-0",
      "theme": "film-animation",
      "title": "Artwork Provenance",
      "pitch": "Artwork Provenance verifies a credential at the gate for gallery shows — proof without a phone call to the issuer.",
      "concept": "Artists present proof of their art's history to galleries, ensuring authenticity while sharing minimal data.",
      "subDiscipline": "gallery shows",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for gallery shows in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Artwork Provenance\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nArtists present proof of their art's history to galleries, ensuring authenticity while sharing minimal data.\nDiscipline: Filmmaking & Animation (gallery shows).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for gallery shows in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — media localization and accessibility · SAM $2B — audio description services · SOM $30M — accessibility coordinators and narrators\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — media localization and accessibility",
      "sam": "$2B — audio description services",
      "som": "$30M — accessibility coordinators and narrators"
    },
    {
      "id": "film-animation-animation-attribution-0",
      "theme": "film-animation",
      "title": "Animation Attribution",
      "pitch": "Animation Attribution mints a did:prism for character design so identity travels with the person, not the platform.",
      "concept": "Animators publish DIDs for their character designs, allowing others to verify and attribute them correctly.",
      "subDiscipline": "character design",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits character design in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Animation Attribution\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAnimators publish DIDs for their character designs, allowing others to verify and attribute them correctly.\nDiscipline: Filmmaking & Animation (character design).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits character design in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — physical production · SAM $3B — stunt coordination and rigging · SOM $45M — stunt coordinators and riggers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — physical production",
      "sam": "$3B — stunt coordination and rigging",
      "som": "$45M — stunt coordinators and riggers"
    },
    {
      "id": "film-animation-edition-authenticity-0",
      "theme": "film-animation",
      "title": "Edition Authenticity",
      "pitch": "Edition Authenticity pairs two wallets over DIDComm so animation can exchange trusted claims on a private channel.",
      "concept": "Animators can invite collectors to verify authenticity of limited editions, ensuring original artwork is recognized and valued.",
      "subDiscipline": "animation",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits animation in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Edition Authenticity\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAnimators can invite collectors to verify authenticity of limited editions, ensuring original artwork is recognized and valued.\nDiscipline: Filmmaking & Animation (animation).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits animation in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — art department and set construction · SAM $5B — production design software · SOM $75M — production designers and art directors\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — art department and set construction",
      "sam": "$5B — production design software",
      "som": "$75M — production designers and art directors"
    },
    {
      "id": "film-animation-payout-confirmations-0",
      "theme": "film-animation",
      "title": "Payout Confirmations",
      "pitch": "Payout Confirmations issues a signed verifiable credential for royalty distribution that the holder keeps and reuses anywhere.",
      "concept": "Provides verifiable credentials for animators to confirm payout eligibility based on contributions and contracts.",
      "subDiscipline": "royalty distribution",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches royalty distribution in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Payout Confirmations\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nProvides verifiable credentials for animators to confirm payout eligibility based on contributions and contracts.\nDiscipline: Filmmaking & Animation (royalty distribution).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches royalty distribution in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — acting and performance · SAM $4B — acting coaches and workshops · SOM $60M — professional actors and coaches\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — acting and performance",
      "sam": "$4B — acting coaches and workshops",
      "som": "$60M — professional actors and coaches"
    },
    {
      "id": "film-animation-rights-management-app-0",
      "theme": "film-animation",
      "title": "Rights Management App",
      "pitch": "Rights Management App verifies a credential at the gate for content distribution — proof without a phone call to the issuer.",
      "concept": "Content creators prove their rights to distribute specific works to platforms, disclosing only necessary rights information.",
      "subDiscipline": "content distribution",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for content distribution in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Rights Management App\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nContent creators prove their rights to distribute specific works to platforms, disclosing only necessary rights information.\nDiscipline: Filmmaking & Animation (content distribution).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for content distribution in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — broadcast design and packaging · SAM $2B — title sequence production · SOM $30M — motion designers and broadcast studios\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — broadcast design and packaging",
      "sam": "$2B — title sequence production",
      "som": "$30M — motion designers and broadcast studios"
    },
    {
      "id": "film-animation-project-collaboration-hub-0",
      "theme": "film-animation",
      "title": "Project Collaboration Hub",
      "pitch": "Project Collaboration Hub mints a did:prism for team projects so identity travels with the person, not the platform.",
      "concept": "Creative teams establish DIDs for members to verify roles and contributions in collaborative filmmaking projects.",
      "subDiscipline": "team projects",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits team projects in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Project Collaboration Hub\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCreative teams establish DIDs for members to verify roles and contributions in collaborative filmmaking projects.\nDiscipline: Filmmaking & Animation (team projects).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits team projects in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — animation industry · SAM $1B — stop-motion production · SOM $15M — stop-motion studios and indie animators\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — animation industry",
      "sam": "$1B — stop-motion production",
      "som": "$15M — stop-motion studios and indie animators"
    },
    {
      "id": "film-animation-creative-fellowship-0",
      "theme": "film-animation",
      "title": "Creative Fellowship",
      "pitch": "Creative Fellowship pairs two wallets over DIDComm so film education can exchange trusted claims on a private channel.",
      "concept": "Filmmakers can invite peers to verify their fellowship status, accessing educational resources and funding opportunities.",
      "subDiscipline": "film education",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits film education in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Creative Fellowship\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFilmmakers can invite peers to verify their fellowship status, accessing educational resources and funding opportunities.\nDiscipline: Filmmaking & Animation (film education).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits film education in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — hair, makeup, and wardrobe · SAM $2B — special effects makeup · SOM $30M — SFX makeup artists and shops\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — hair, makeup, and wardrobe",
      "sam": "$2B — special effects makeup",
      "som": "$30M — SFX makeup artists and shops"
    },
    {
      "id": "film-animation-festival-participation-0",
      "theme": "film-animation",
      "title": "Festival Participation",
      "pitch": "Festival Participation issues a signed verifiable credential for event participation that the holder keeps and reuses anywhere.",
      "concept": "Verifies an animator’s past festival participation to enhance their professional credibility.",
      "subDiscipline": "event participation",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches event participation in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Festival Participation\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nVerifies an animator’s past festival participation to enhance their professional credibility.\nDiscipline: Filmmaking & Animation (event participation).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches event participation in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — casting and talent acquisition · SAM $3B — casting software and services · SOM $45M — casting directors and associates\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — casting and talent acquisition",
      "sam": "$3B — casting software and services",
      "som": "$45M — casting directors and associates"
    },
    {
      "id": "film-animation-background-check-service-0",
      "theme": "film-animation",
      "title": "Background Check Service",
      "pitch": "Background Check Service verifies a credential at the gate for hiring — proof without a phone call to the issuer.",
      "concept": "Employers verify applicants' previous work experiences without accessing their full history, only confirming credentials.",
      "subDiscipline": "hiring",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for hiring in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Background Check Service\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nEmployers verify applicants' previous work experiences without accessing their full history, only confirming credentials.\nDiscipline: Filmmaking & Animation (hiring).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for hiring in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — film distribution and marketing · SAM $6B — trailer houses and marketing · SOM $90M — trailer producers and copywriters\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — film distribution and marketing",
      "sam": "$6B — trailer houses and marketing",
      "som": "$90M — trailer producers and copywriters"
    },
    {
      "id": "film-animation-festival-submission-id-0",
      "theme": "film-animation",
      "title": "Festival Submission ID",
      "pitch": "Festival Submission ID mints a did:prism for film festivals so identity travels with the person, not the platform.",
      "concept": "Filmmakers create DIDs to submit films to festivals, ensuring their submission data is verifiable and linked to their identity.",
      "subDiscipline": "film festivals",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits film festivals in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Festival Submission ID\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFilmmakers create DIDs to submit films to festivals, ensuring their submission data is verifiable and linked to their identity.\nDiscipline: Filmmaking & Animation (film festivals).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits film festivals in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — audio post-production · SAM $4B — ADR and loop group services · SOM $60M — re-recording mixers and ADR supervisors\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — audio post-production",
      "sam": "$4B — ADR and loop group services",
      "som": "$60M — re-recording mixers and ADR supervisors"
    },
    {
      "id": "film-animation-collab-invitation-hub-0",
      "theme": "film-animation",
      "title": "Collab Invitation Hub",
      "pitch": "Collab Invitation Hub pairs two wallets over DIDComm so animation can exchange trusted claims on a private channel.",
      "concept": "Animators can invite collaborators to verify participation in joint projects, fostering transparent teamwork.",
      "subDiscipline": "animation",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits animation in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Collab Invitation Hub\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAnimators can invite collaborators to verify participation in joint projects, fostering transparent teamwork.\nDiscipline: Filmmaking & Animation (animation).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits animation in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — television development · SAM $2B — writers room operations · SOM $30M — showrunners and staff writers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — television development",
      "sam": "$2B — writers room operations",
      "som": "$30M — showrunners and staff writers"
    },
    {
      "id": "film-animation-content-ownership-0",
      "theme": "film-animation",
      "title": "Content Ownership",
      "pitch": "Content Ownership issues a signed verifiable credential for intellectual property that the holder keeps and reuses anywhere.",
      "concept": "Allows filmmakers to prove ownership of original content to protect against unauthorized use.",
      "subDiscipline": "intellectual property",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches intellectual property in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Content Ownership\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAllows filmmakers to prove ownership of original content to protect against unauthorized use.\nDiscipline: Filmmaking & Animation (intellectual property).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches intellectual property in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global film and animation production · SAM $15B — pre-production and storyboarding software · SOM $120M — indie animation studio tooling\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global film and animation production",
      "sam": "$15B — pre-production and storyboarding software",
      "som": "$120M — indie animation studio tooling"
    },
    {
      "id": "film-animation-festival-submission-validator-0",
      "theme": "film-animation",
      "title": "Festival Submission Validator",
      "pitch": "Festival Submission Validator verifies a credential at the gate for entry requirements — proof without a phone call to the issuer.",
      "concept": "Filmmakers prove compliance with submission criteria for film festivals, revealing only necessary qualifications.",
      "subDiscipline": "entry requirements",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for entry requirements in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Festival Submission Validator\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFilmmakers prove compliance with submission criteria for film festivals, revealing only necessary qualifications.\nDiscipline: Filmmaking & Animation (entry requirements).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for entry requirements in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global film and animation production · SAM $8B — post-production audio software · SOM $50M — boutique foley stage tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global film and animation production",
      "sam": "$8B — post-production audio software",
      "som": "$50M — boutique foley stage tools"
    },
    {
      "id": "film-animation-credit-claim-api-0",
      "theme": "film-animation",
      "title": "Credit Claim API",
      "pitch": "Credit Claim API mints a did:prism for post-production so identity travels with the person, not the platform.",
      "concept": "Post-production teams use DIDs to claim and verify credits for editors and visual effects artists.",
      "subDiscipline": "post-production",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits post-production in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Credit Claim API\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPost-production teams use DIDs to claim and verify credits for editors and visual effects artists.\nDiscipline: Filmmaking & Animation (post-production).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits post-production in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global film and animation production · SAM $12B — narrative and casting platforms · SOM $80M — animation voice recording studios\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global film and animation production",
      "sam": "$12B — narrative and casting platforms",
      "som": "$80M — animation voice recording studios"
    },
    {
      "id": "film-animation-skill-accreditation-0",
      "theme": "film-animation",
      "title": "Skill Accreditation",
      "pitch": "Skill Accreditation pairs two wallets over DIDComm so workshops can exchange trusted claims on a private channel.",
      "concept": "Participants can invite instructors to verify their attendance and skills gained in filmmaking workshops for future employment.",
      "subDiscipline": "workshops",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits workshops in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Skill Accreditation\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nParticipants can invite instructors to verify their attendance and skills gained in filmmaking workshops for future employment.\nDiscipline: Filmmaking & Animation (workshops).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits workshops in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global film and animation production · SAM $10B — 2D animation software market · SOM $60M — traditional animation studios\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global film and animation production",
      "sam": "$10B — 2D animation software market",
      "som": "$60M — traditional animation studios"
    },
    {
      "id": "film-animation-animation-mentorship-0",
      "theme": "film-animation",
      "title": "Animation Mentorship",
      "pitch": "Animation Mentorship issues a signed verifiable credential for education that the holder keeps and reuses anywhere.",
      "concept": "Confirms mentorship credentials of experienced animators to students seeking guidance in their careers.",
      "subDiscipline": "education",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches education in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Animation Mentorship\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nConfirms mentorship credentials of experienced animators to students seeking guidance in their careers.\nDiscipline: Filmmaking & Animation (education).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches education in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global film and animation production · SAM $20B — live-action production management · SOM $150M — commercial directing software\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global film and animation production",
      "sam": "$20B — live-action production management",
      "som": "$150M — commercial directing software"
    },
    {
      "id": "film-animation-credits-confirmation-0",
      "theme": "film-animation",
      "title": "Credits Confirmation",
      "pitch": "Credits Confirmation verifies a credential at the gate for documentaries — proof without a phone call to the issuer.",
      "concept": "Producers verify crew contributions in documentaries, ensuring each member gets recognized without revealing personal details.",
      "subDiscipline": "documentaries",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for documentaries in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Credits Confirmation\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nProducers verify crew contributions in documentaries, ensuring each member gets recognized without revealing personal details.\nDiscipline: Filmmaking & Animation (documentaries).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for documentaries in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global film and animation production · SAM $18B — video editing and VFX software · SOM $90M — freelance editor productivity tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global film and animation production",
      "sam": "$18B — video editing and VFX software",
      "som": "$90M — freelance editor productivity tools"
    },
    {
      "id": "film-animation-animation-portfolio-0",
      "theme": "film-animation",
      "title": "Animation Portfolio",
      "pitch": "Animation Portfolio mints a did:prism for showreels so identity travels with the person, not the platform.",
      "concept": "Animators can publish their portfolios as DIDs, making it easier for recruiters and clients to verify their skills.",
      "subDiscipline": "showreels",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits showreels in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Animation Portfolio\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAnimators can publish their portfolios as DIDs, making it easier for recruiters and clients to verify their skills.\nDiscipline: Filmmaking & Animation (showreels).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits showreels in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global film and animation production · SAM $5B — screenwriting and development tools · SOM $40M — television showrunner coaching\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global film and animation production",
      "sam": "$5B — screenwriting and development tools",
      "som": "$40M — television showrunner coaching"
    },
    {
      "id": "film-animation-production-role-certifier-0",
      "theme": "film-animation",
      "title": "Production Role Certifier",
      "pitch": "Production Role Certifier pairs two wallets over DIDComm so film jobs can exchange trusted claims on a private channel.",
      "concept": "Crew members can verify their roles on film projects, showcasing experience to potential employers.",
      "subDiscipline": "film jobs",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits film jobs in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Production Role Certifier\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCrew members can verify their roles on film projects, showcasing experience to potential employers.\nDiscipline: Filmmaking & Animation (film jobs).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits film jobs in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global film and animation production · SAM $6B — writers room collaboration software · SOM $30M — animated series development\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global film and animation production",
      "sam": "$6B — writers room collaboration software",
      "som": "$30M — animated series development"
    },
    {
      "id": "film-animation-frame-authenticity-0",
      "theme": "film-animation",
      "title": "Frame Authenticity",
      "pitch": "Frame Authenticity issues a signed verifiable credential for art collection that the holder keeps and reuses anywhere.",
      "concept": "Proves authenticity of individual animation frames for collectors and art institutions.",
      "subDiscipline": "art collection",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches art collection in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Frame Authenticity\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nProves authenticity of individual animation frames for collectors and art institutions.\nDiscipline: Filmmaking & Animation (art collection).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches art collection in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global film and animation production · SAM $14B — cinematography and grip equipment · SOM $70M — digital cinematography planning apps\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global film and animation production",
      "sam": "$14B — cinematography and grip equipment",
      "som": "$70M — digital cinematography planning apps"
    },
    {
      "id": "film-animation-screenplay-authenticator-0",
      "theme": "film-animation",
      "title": "Screenplay Authenticator",
      "pitch": "Screenplay Authenticator verifies a credential at the gate for script rights — proof without a phone call to the issuer.",
      "concept": "Writers prove ownership of scripts to producers, disclosing only relevant details without exposing full texts.",
      "subDiscipline": "script rights",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for script rights in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Screenplay Authenticator\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nWriters prove ownership of scripts to producers, disclosing only relevant details without exposing full texts.\nDiscipline: Filmmaking & Animation (script rights).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for script rights in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global film and animation production · SAM $9B — digital illustration and storyboarding · SOM $45M — remote storyboard artist tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global film and animation production",
      "sam": "$9B — digital illustration and storyboarding",
      "som": "$45M — remote storyboard artist tools"
    },
    {
      "id": "film-animation-edition-registry-0",
      "theme": "film-animation",
      "title": "Edition Registry",
      "pitch": "Edition Registry mints a did:prism for limited releases so identity travels with the person, not the platform.",
      "concept": "Filmmakers register limited edition releases with DIDs to prove authenticity and traceability of copies.",
      "subDiscipline": "limited releases",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits limited releases in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Edition Registry\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFilmmakers register limited edition releases with DIDs to prove authenticity and traceability of copies.\nDiscipline: Filmmaking & Animation (limited releases).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits limited releases in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global film and animation production · SAM $25B — VFX and post-production services · SOM $200M — on-set VFX supervision tech\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global film and animation production",
      "sam": "$25B — VFX and post-production services",
      "som": "$200M — on-set VFX supervision tech"
    },
    {
      "id": "film-animation-casting-call-proof-0",
      "theme": "film-animation",
      "title": "Casting Call Proof",
      "pitch": "Casting Call Proof pairs two wallets over DIDComm so casting can exchange trusted claims on a private channel.",
      "concept": "Casting directors can verify candidates' past roles and experiences, enhancing trust during audition processes.",
      "subDiscipline": "casting",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits casting in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Casting Call Proof\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCasting directors can verify candidates' past roles and experiences, enhancing trust during audition processes.\nDiscipline: Filmmaking & Animation (casting).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits casting in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global film and animation production · SAM $11B — 3D animation and mo-cap software · SOM $85M — AAA game animation pipelines\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global film and animation production",
      "sam": "$11B — 3D animation and mo-cap software",
      "som": "$85M — AAA game animation pipelines"
    },
    {
      "id": "film-animation-project-completion-0",
      "theme": "film-animation",
      "title": "Project Completion",
      "pitch": "Project Completion issues a signed verifiable credential for production milestones that the holder keeps and reuses anywhere.",
      "concept": "Properly certifies project completion statuses for animators to confirm deliverables to clients.",
      "subDiscipline": "production milestones",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches production milestones in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Project Completion\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nProperly certifies project completion statuses for animators to confirm deliverables to clients.\nDiscipline: Filmmaking & Animation (production milestones).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches production milestones in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global film and animation production · SAM $7B — digital audio workstation ecosystems · SOM $55M — film sound design studios\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global film and animation production",
      "sam": "$7B — digital audio workstation ecosystems",
      "som": "$55M — film sound design studios"
    },
    {
      "id": "film-animation-casting-call-prover-0",
      "theme": "film-animation",
      "title": "Casting Call Prover",
      "pitch": "Casting Call Prover verifies a credential at the gate for auditions — proof without a phone call to the issuer.",
      "concept": "Actors present verified audition records to casting directors, ensuring trust without sharing complete audition videos.",
      "subDiscipline": "auditions",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for auditions in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Casting Call Prover\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nActors present verified audition records to casting directors, ensuring trust without sharing complete audition videos.\nDiscipline: Filmmaking & Animation (auditions).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for auditions in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global film and animation production · SAM $4B — script reading and casting platforms · SOM $25M — comedy writers room tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global film and animation production",
      "sam": "$4B — script reading and casting platforms",
      "som": "$25M — comedy writers room tools"
    },
    {
      "id": "film-animation-talent-verification-0",
      "theme": "film-animation",
      "title": "Talent Verification",
      "pitch": "Talent Verification mints a did:prism for casting so identity travels with the person, not the platform.",
      "concept": "Producers verify actor identities and qualifications through a DID system, simplifying the audition and casting process.",
      "subDiscipline": "casting",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits casting in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Talent Verification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nProducers verify actor identities and qualifications through a DID system, simplifying the audition and casting process.\nDiscipline: Filmmaking & Animation (casting).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits casting in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global film and animation production · SAM $3B — stop-motion and claymation studios · SOM $15M — indie stop-motion production\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global film and animation production",
      "sam": "$3B — stop-motion and claymation studios",
      "som": "$15M — indie stop-motion production"
    },
    {
      "id": "film-animation-location-access-mentor-0",
      "theme": "film-animation",
      "title": "Location Access Mentor",
      "pitch": "Location Access Mentor pairs two wallets over DIDComm so scouting can exchange trusted claims on a private channel.",
      "concept": "Location scouts can verify permissions for specific sites, ensuring compliance with filming regulations.",
      "subDiscipline": "scouting",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits scouting in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Location Access Mentor\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLocation scouts can verify permissions for specific sites, ensuring compliance with filming regulations.\nDiscipline: Filmmaking & Animation (scouting).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits scouting in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global film and animation production · SAM $10B — color grading and finishing software · SOM $65M — boutique post-production houses\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global film and animation production",
      "sam": "$10B — color grading and finishing software",
      "som": "$65M — boutique post-production houses"
    },
    {
      "id": "film-animation-script-validation-0",
      "theme": "film-animation",
      "title": "Script Validation",
      "pitch": "Script Validation issues a signed verifiable credential for script supervision that the holder keeps and reuses anywhere.",
      "concept": "Issues credits to script supervisors to validate their contributions and expertise in productions.",
      "subDiscipline": "script supervision",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches script supervision in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Script Validation\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nIssues credits to script supervisors to validate their contributions and expertise in productions.\nDiscipline: Filmmaking & Animation (script supervision).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches script supervision in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global film and animation production · SAM $12B — motion graphics and broadcast design · SOM $90M — title sequence agencies\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global film and animation production",
      "sam": "$12B — motion graphics and broadcast design",
      "som": "$90M — title sequence agencies"
    },
    {
      "id": "film-animation-location-release-validator-0",
      "theme": "film-animation",
      "title": "Location Release Validator",
      "pitch": "Location Release Validator verifies a credential at the gate for shoot permissions — proof without a phone call to the issuer.",
      "concept": "Filmmakers verify location permissions quickly, sharing only essential approval information without entire agreements.",
      "subDiscipline": "shoot permissions",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for shoot permissions in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Location Release Validator\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFilmmakers verify location permissions quickly, sharing only essential approval information without entire agreements.\nDiscipline: Filmmaking & Animation (shoot permissions).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for shoot permissions in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global film and animation production · SAM $6B — dialogue editing and ADR services · SOM $40M — television dubbing studios\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global film and animation production",
      "sam": "$6B — dialogue editing and ADR services",
      "som": "$40M — television dubbing studios"
    },
    {
      "id": "film-animation-collaborator-network-0",
      "theme": "film-animation",
      "title": "Collaborator Network",
      "pitch": "Collaborator Network mints a did:prism for networking so identity travels with the person, not the platform.",
      "concept": "Filmmakers register DIDs to connect with verified collaborators for projects, ensuring trusted partnerships.",
      "subDiscipline": "networking",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits networking in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Collaborator Network\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFilmmakers register DIDs to connect with verified collaborators for projects, ensuring trusted partnerships.\nDiscipline: Filmmaking & Animation (networking).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits networking in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global film and animation production · SAM $8B — production logistics and scouting · SOM $50M — commercial location management\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global film and animation production",
      "sam": "$8B — production logistics and scouting",
      "som": "$50M — commercial location management"
    },
    {
      "id": "film-animation-virtual-review-board-0",
      "theme": "film-animation",
      "title": "Virtual Review Board",
      "pitch": "Virtual Review Board pairs two wallets over DIDComm so film festivals can exchange trusted claims on a private channel.",
      "concept": "Filmmakers can invite reviewers to verify attendance, granting access to festival selections and feedback.",
      "subDiscipline": "film festivals",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits film festivals in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Virtual Review Board\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFilmmakers can invite reviewers to verify attendance, granting access to festival selections and feedback.\nDiscipline: Filmmaking & Animation (film festivals).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits film festivals in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global film and animation production · SAM $5B — on-set production management software · SOM $35M — script supervisor digital tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global film and animation production",
      "sam": "$5B — on-set production management software",
      "som": "$35M — script supervisor digital tools"
    },
    {
      "id": "film-animation-equipment-access-0",
      "theme": "film-animation",
      "title": "Equipment Access",
      "pitch": "Equipment Access issues a signed verifiable credential for production gear that the holder keeps and reuses anywhere.",
      "concept": "Verifies credentials for access to specialized production equipment needed by filmmakers.",
      "subDiscipline": "production gear",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches production gear in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Equipment Access\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nVerifies credentials for access to specialized production equipment needed by filmmakers.\nDiscipline: Filmmaking & Animation (production gear).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches production gear in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global film and animation production · SAM $15B — 3D modeling and rigging pipelines · SOM $100M — AAA game character art\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global film and animation production",
      "sam": "$15B — 3D modeling and rigging pipelines",
      "som": "$100M — AAA game character art"
    },
    {
      "id": "film-animation-animation-credits-app-0",
      "theme": "film-animation",
      "title": "Animation Credits App",
      "pitch": "Animation Credits App verifies a credential at the gate for short films — proof without a phone call to the issuer.",
      "concept": "Animators verify their roles in short films, ensuring proper credit distribution while protecting personal identities.",
      "subDiscipline": "short films",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for short films in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Animation Credits App\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAnimators verify their roles in short films, ensuring proper credit distribution while protecting personal identities.\nDiscipline: Filmmaking & Animation (short films).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for short films in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global film and animation production · SAM $9B — digital matte painting and concept art · SOM $60M — VFX environment art departments\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global film and animation production",
      "sam": "$9B — digital matte painting and concept art",
      "som": "$60M — VFX environment art departments"
    },
    {
      "id": "film-animation-behind-the-scenes-access-0",
      "theme": "film-animation",
      "title": "Behind-the-Scenes Access",
      "pitch": "Behind-the-Scenes Access mints a did:prism for exclusive content so identity travels with the person, not the platform.",
      "concept": "Crew members use DIDs to grant exclusive access to behind-the-scenes content for verified fans.",
      "subDiscipline": "exclusive content",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits exclusive content in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Behind-the-Scenes Access\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCrew members use DIDs to grant exclusive access to behind-the-scenes content for verified fans.\nDiscipline: Filmmaking & Animation (exclusive content).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits exclusive content in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global film and animation production · SAM $7B — trailer house and marketing post · SOM $45M — theatrical trailer editing\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global film and animation production",
      "sam": "$7B — trailer house and marketing post",
      "som": "$45M — theatrical trailer editing"
    },
    {
      "id": "film-animation-documentary-credits-journal-0",
      "theme": "film-animation",
      "title": "Documentary Credits Journal",
      "pitch": "Documentary Credits Journal pairs two wallets over DIDComm so documentary film can exchange trusted claims on a private channel.",
      "concept": "Documentarians can invite contributors to verify their roles and input, preserving accurate credits for future screenings.",
      "subDiscipline": "documentary film",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits documentary film in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Documentary Credits Journal\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDocumentarians can invite contributors to verify their roles and input, preserving accurate credits for future screenings.\nDiscipline: Filmmaking & Animation (documentary film).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits documentary film in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global film and animation production · SAM $11B — physical production and stunt services · SOM $75M — action film safety management\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global film and animation production",
      "sam": "$11B — physical production and stunt services",
      "som": "$75M — action film safety management"
    },
    {
      "id": "film-animation-animation-awards-validation-0",
      "theme": "film-animation",
      "title": "Animation Awards Validation",
      "pitch": "Animation Awards Validation issues a signed verifiable credential for awards that the holder keeps and reuses anywhere.",
      "concept": "Proves awards won by animators to potential employers or collaborators as a mark of distinction.",
      "subDiscipline": "awards",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches awards in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Animation Awards Validation\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nProves awards won by animators to potential employers or collaborators as a mark of distinction.\nDiscipline: Filmmaking & Animation (awards).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches awards in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global film and animation production · SAM $13B — character animation and lip-sync tools · SOM $85M — animated feature lip-sync pipelines\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global film and animation production",
      "sam": "$13B — character animation and lip-sync tools",
      "som": "$85M — animated feature lip-sync pipelines"
    },
    {
      "id": "film-animation-license-verification-tool-0",
      "theme": "film-animation",
      "title": "License Verification Tool",
      "pitch": "License Verification Tool verifies a credential at the gate for music rights — proof without a phone call to the issuer.",
      "concept": "Musicians prove rights to use tracks in films, revealing only necessary licensing information to filmmakers.",
      "subDiscipline": "music rights",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for music rights in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"License Verification Tool\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nMusicians prove rights to use tracks in films, revealing only necessary licensing information to filmmakers.\nDiscipline: Filmmaking & Animation (music rights).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for music rights in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $12B — post-production audio services · SOM $500M — indie foley and sound design software\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$12B — post-production audio services",
      "som": "$500M — indie foley and sound design software"
    },
    {
      "id": "film-animation-content-ownership-1",
      "theme": "film-animation",
      "title": "Content Ownership",
      "pitch": "Content Ownership mints a did:prism for digital rights so identity travels with the person, not the platform.",
      "concept": "Animators can license their work by proving ownership with DIDs during negotiations with distributors.",
      "subDiscipline": "digital rights",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits digital rights in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Content Ownership\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAnimators can license their work by proving ownership with DIDs during negotiations with distributors.\nDiscipline: Filmmaking & Animation (digital rights).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits digital rights in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $15B — pre-production and storyboarding tools · SOM $800M — digital storyboard software market\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$15B — pre-production and storyboarding tools",
      "som": "$800M — digital storyboard software market"
    },
    {
      "id": "film-animation-artistic-license-grant-0",
      "theme": "film-animation",
      "title": "Artistic License Grant",
      "pitch": "Artistic License Grant pairs two wallets over DIDComm so animation can exchange trusted claims on a private channel.",
      "concept": "Animators can invite clients to verify commissioned work, ensuring agreed terms are respected and honored.",
      "subDiscipline": "animation",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits animation in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Artistic License Grant\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAnimators can invite clients to verify commissioned work, ensuring agreed terms are respected and honored.\nDiscipline: Filmmaking & Animation (animation).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits animation in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $10B — video editing and animatic software · SOM $400M — freelance animation editing tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$10B — video editing and animatic software",
      "som": "$400M — freelance animation editing tools"
    },
    {
      "id": "film-animation-talent-pool-registry-0",
      "theme": "film-animation",
      "title": "Talent Pool Registry",
      "pitch": "Talent Pool Registry issues a signed verifiable credential for talent recruitment that the holder keeps and reuses anywhere.",
      "concept": "Confirms and catalogs animator skills and specializations for recruiters seeking talent for projects.",
      "subDiscipline": "talent recruitment",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches talent recruitment in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Talent Pool Registry\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nConfirms and catalogs animator skills and specializations for recruiters seeking talent for projects.\nDiscipline: Filmmaking & Animation (talent recruitment).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches talent recruitment in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $20B — VFX and creature design market · SOM $600M — creature audio and vocal design\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$20B — VFX and creature design market",
      "som": "$600M — creature audio and vocal design"
    },
    {
      "id": "film-animation-integrity-checker-0",
      "theme": "film-animation",
      "title": "Integrity Checker",
      "pitch": "Integrity Checker verifies a credential at the gate for animated series — proof without a phone call to the issuer.",
      "concept": "Creators verify the integrity of their episodes against content changes, ensuring authenticity without revealing entire scripts.",
      "subDiscipline": "animated series",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for animated series in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Integrity Checker\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCreators verify the integrity of their episodes against content changes, ensuring authenticity without revealing entire scripts.\nDiscipline: Filmmaking & Animation (animated series).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for animated series in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $18B — sci-fi and fantasy production design · SOM $300M — ambient soundscape licensing\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$18B — sci-fi and fantasy production design",
      "som": "$300M — ambient soundscape licensing"
    },
    {
      "id": "film-animation-lifetime-achievement-id-0",
      "theme": "film-animation",
      "title": "Lifetime Achievement ID",
      "pitch": "Lifetime Achievement ID mints a did:prism for recognition so identity travels with the person, not the platform.",
      "concept": "Veteran filmmakers receive DIDs to authenticate their lifetime achievements and contributions to the industry.",
      "subDiscipline": "recognition",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits recognition in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Lifetime Achievement ID\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nVeteran filmmakers receive DIDs to authenticate their lifetime achievements and contributions to the industry.\nDiscipline: Filmmaking & Animation (recognition).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits recognition in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $25B — action and VFX sequence production · SOM $450M — fight choreography and animation pre-vis\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$25B — action and VFX sequence production",
      "som": "$450M — fight choreography and animation pre-vis"
    },
    {
      "id": "film-animation-festival-submission-check-0",
      "theme": "film-animation",
      "title": "Festival Submission Check",
      "pitch": "Festival Submission Check pairs two wallets over DIDComm so film festivals can exchange trusted claims on a private channel.",
      "concept": "Filmmakers can verify submission details to festivals, preventing submission errors and ensuring proper entry.",
      "subDiscipline": "film festivals",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits film festivals in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Festival Submission Check\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFilmmakers can verify submission details to festivals, preventing submission errors and ensuring proper entry.\nDiscipline: Filmmaking & Animation (film festivals).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits film festivals in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $14B — screenwriting and narrative design tools · SOM $700M — indie film scoring and composition\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$14B — screenwriting and narrative design tools",
      "som": "$700M — indie film scoring and composition"
    },
    {
      "id": "film-animation-rights-transfer-0",
      "theme": "film-animation",
      "title": "Rights Transfer",
      "pitch": "Rights Transfer issues a signed verifiable credential for copyright that the holder keeps and reuses anywhere.",
      "concept": "Facilitates the transfer of rights for animated works ensuring both parties can verify ownership changes.",
      "subDiscipline": "copyright",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches copyright in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Rights Transfer\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFacilitates the transfer of rights for animated works ensuring both parties can verify ownership changes.\nDiscipline: Filmmaking & Animation (copyright).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches copyright in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $30B — fantasy VFX and animation · SOM $500M — magic system audio design\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$30B — fantasy VFX and animation",
      "som": "$500M — magic system audio design"
    },
    {
      "id": "film-animation-artistic-collaboration-proof-0",
      "theme": "film-animation",
      "title": "Artistic Collaboration Proof",
      "pitch": "Artistic Collaboration Proof verifies a credential at the gate for projects — proof without a phone call to the issuer.",
      "concept": "Filmmakers prove collaborative ownership of projects, maintaining privacy while confirming contributions from all parties.",
      "subDiscipline": "projects",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for projects in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Artistic Collaboration Proof\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFilmmakers prove collaborative ownership of projects, maintaining privacy while confirming contributions from all parties.\nDiscipline: Filmmaking & Animation (projects).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for projects in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $8B — television and film pitching market · SOM $200M — pitch deck media production\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$8B — television and film pitching market",
      "som": "$200M — pitch deck media production"
    },
    {
      "id": "film-animation-creative-commons-verification-0",
      "theme": "film-animation",
      "title": "Creative Commons Verification",
      "pitch": "Creative Commons Verification mints a did:prism for resource sharing so identity travels with the person, not the platform.",
      "concept": "Content creators can register their work under Creative Commons with DIDs to verify usage permissions.",
      "subDiscipline": "resource sharing",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits resource sharing in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Creative Commons Verification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nContent creators can register their work under Creative Commons with DIDs to verify usage permissions.\nDiscipline: Filmmaking & Animation (resource sharing).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits resource sharing in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $5B — stop-motion and practical animation · SOM $150M — stop-motion studio sound design\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$5B — stop-motion and practical animation",
      "som": "$150M — stop-motion studio sound design"
    },
    {
      "id": "film-animation-character-design-authenticator-0",
      "theme": "film-animation",
      "title": "Character Design Authenticator",
      "pitch": "Character Design Authenticator pairs two wallets over DIDComm so character design can exchange trusted claims on a private channel.",
      "concept": "Character designers can invite teams to verify the originality of their work and reduce plagiarism risks.",
      "subDiscipline": "character design",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits character design in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Character Design Authenticator\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCharacter designers can invite teams to verify the originality of their work and reduce plagiarism risks.\nDiscipline: Filmmaking & Animation (character design).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits character design in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $22B — motion graphics and broadcast design · SOM $900M — UI/UX motion design audio\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$22B — motion graphics and broadcast design",
      "som": "$900M — UI/UX motion design audio"
    },
    {
      "id": "film-animation-workshop-participation-0",
      "theme": "film-animation",
      "title": "Workshop Participation",
      "pitch": "Workshop Participation issues a signed verifiable credential for professional development that the holder keeps and reuses anywhere.",
      "concept": "Certifies participation in professional development workshops for animators to boost their credentials.",
      "subDiscipline": "professional development",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches professional development in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Workshop Participation\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCertifies participation in professional development workshops for animators to boost their credentials.\nDiscipline: Filmmaking & Animation (professional development).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches professional development in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $12B — horror film and animation production · SOM $350M — horror audio post-production\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$12B — horror film and animation production",
      "som": "$350M — horror audio post-production"
    },
    {
      "id": "film-animation-background-eligibility-verifier-0",
      "theme": "film-animation",
      "title": "Background Eligibility Verifier",
      "pitch": "Background Eligibility Verifier verifies a credential at the gate for youth programs — proof without a phone call to the issuer.",
      "concept": "Young animators present proof of eligibility for youth programs, sharing only age and relevant details.",
      "subDiscipline": "youth programs",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for youth programs in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Background Eligibility Verifier\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nYoung animators present proof of eligibility for youth programs, sharing only age and relevant details.\nDiscipline: Filmmaking & Animation (youth programs).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for youth programs in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $10B — historical and period-piece production · SOM $250M — historical foley and sound libraries\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$10B — historical and period-piece production",
      "som": "$250M — historical foley and sound libraries"
    },
    {
      "id": "film-animation-union-status-check-0",
      "theme": "film-animation",
      "title": "Union Status Check",
      "pitch": "Union Status Check mints a did:prism for labor rights so identity travels with the person, not the platform.",
      "concept": "Crew members verify their union status via DIDs to ensure compliance with labor regulations when taking jobs.",
      "subDiscipline": "labor rights",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits labor rights in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Union Status Check\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCrew members verify their union status via DIDs to ensure compliance with labor regulations when taking jobs.\nDiscipline: Filmmaking & Animation (labor rights).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits labor rights in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $18B — vehicle and mecha design · SOM $400M — sci-fi vehicle audio design\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$18B — vehicle and mecha design",
      "som": "$400M — sci-fi vehicle audio design"
    },
    {
      "id": "film-animation-diversity-certification-0",
      "theme": "film-animation",
      "title": "Diversity Certification",
      "pitch": "Diversity Certification pairs two wallets over DIDComm so inclusive filmmaking can exchange trusted claims on a private channel.",
      "concept": "Production companies can invite stakeholders to verify diversity certifications, fostering accountability in hiring practices.",
      "subDiscipline": "inclusive filmmaking",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits inclusive filmmaking in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Diversity Certification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nProduction companies can invite stakeholders to verify diversity certifications, fostering accountability in hiring practices.\nDiscipline: Filmmaking & Animation (inclusive filmmaking).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits inclusive filmmaking in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $25B — environment and background animation · SOM $600M — ambient environmental audio tracks\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$25B — environment and background animation",
      "som": "$600M — ambient environmental audio tracks"
    },
    {
      "id": "film-animation-accredited-collaborators-0",
      "theme": "film-animation",
      "title": "Accredited Collaborators",
      "pitch": "Accredited Collaborators issues a signed verifiable credential for collaborative projects that the holder keeps and reuses anywhere.",
      "concept": "Validates the roles and contributions of collaborators in co-created projects to ensure accurate credits.",
      "subDiscipline": "collaborative projects",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches collaborative projects in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Accredited Collaborators\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nValidates the roles and contributions of collaborators in co-created projects to ensure accurate credits.\nDiscipline: Filmmaking & Animation (collaborative projects).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches collaborative projects in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $15B — crowd simulation and background animation · SOM $300M — background walla and crowd audio\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$15B — crowd simulation and background animation",
      "som": "$300M — background walla and crowd audio"
    },
    {
      "id": "film-animation-visual-rights-app-0",
      "theme": "film-animation",
      "title": "Visual Rights App",
      "pitch": "Visual Rights App verifies a credential at the gate for stock footage — proof without a phone call to the issuer.",
      "concept": "Video producers prove their rights to use stock footage, verifying permissions without disclosing additional data.",
      "subDiscipline": "stock footage",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for stock footage in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Visual Rights App\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nVideo producers prove their rights to use stock footage, verifying permissions without disclosing additional data.\nDiscipline: Filmmaking & Animation (stock footage).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for stock footage in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $20B — comedy animation and television · SOM $500M — comedic sound effects licensing\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$20B — comedy animation and television",
      "som": "$500M — comedic sound effects licensing"
    },
    {
      "id": "film-animation-backstage-access-control-0",
      "theme": "film-animation",
      "title": "Backstage Access Control",
      "pitch": "Backstage Access Control mints a did:prism for event management so identity travels with the person, not the platform.",
      "concept": "Event organizers use DIDs to manage and authenticate backstage access for crew and talent during events.",
      "subDiscipline": "event management",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits event management in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Backstage Access Control\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nEvent organizers use DIDs to manage and authenticate backstage access for crew and talent during events.\nDiscipline: Filmmaking & Animation (event management).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits event management in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $8B — film marketing and trailer houses · SOM $400M — trailer music composition market\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$8B — film marketing and trailer houses",
      "som": "$400M — trailer music composition market"
    },
    {
      "id": "film-animation-legacy-archive-access-0",
      "theme": "film-animation",
      "title": "Legacy Archive Access",
      "pitch": "Legacy Archive Access pairs two wallets over DIDComm so film history can exchange trusted claims on a private channel.",
      "concept": "Academics can invite institutions to verify access to historical film archives for research purposes.",
      "subDiscipline": "film history",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits film history in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Legacy Archive Access\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAcademics can invite institutions to verify access to historical film archives for research purposes.\nDiscipline: Filmmaking & Animation (film history).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits film history in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $2B — film restoration and archiving · SOM $100M — silent film exhibition and scoring\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$2B — film restoration and archiving",
      "som": "$100M — silent film exhibition and scoring"
    },
    {
      "id": "film-animation-creative-commons-proof-0",
      "theme": "film-animation",
      "title": "Creative Commons Proof",
      "pitch": "Creative Commons Proof issues a signed verifiable credential for licensing that the holder keeps and reuses anywhere.",
      "concept": "Confirms adherence to Creative Commons licenses for animators sharing their work publicly.",
      "subDiscipline": "licensing",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches licensing in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Creative Commons Proof\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nConfirms adherence to Creative Commons licenses for animators sharing their work publicly.\nDiscipline: Filmmaking & Animation (licensing).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches licensing in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $12B — set decoration and prop design · SOM $250M — diegetic audio and source music\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$12B — set decoration and prop design",
      "som": "$250M — diegetic audio and source music"
    },
    {
      "id": "film-animation-script-registration-tool-0",
      "theme": "film-animation",
      "title": "Script Registration Tool",
      "pitch": "Script Registration Tool verifies a credential at the gate for writing — proof without a phone call to the issuer.",
      "concept": "Writers confirm the registration of their scripts to peers, ensuring originality while keeping full content private.",
      "subDiscipline": "writing",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for writing in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Script Registration Tool\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nWriters confirm the registration of their scripts to peers, ensuring originality while keeping full content private.\nDiscipline: Filmmaking & Animation (writing).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for writing in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $15B — surreal and experimental film · SOM $300M — experimental sound design market\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$15B — surreal and experimental film",
      "som": "$300M — experimental sound design market"
    },
    {
      "id": "film-animation-visual-effects-approval-0",
      "theme": "film-animation",
      "title": "Visual Effects Approval",
      "pitch": "Visual Effects Approval mints a did:prism for post-production so identity travels with the person, not the platform.",
      "concept": "VFX artists verify their contributions through DIDs to ensure credited recognition in final productions.",
      "subDiscipline": "post-production",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits post-production in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Visual Effects Approval\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nVFX artists verify their contributions through DIDs to ensure credited recognition in final productions.\nDiscipline: Filmmaking & Animation (post-production).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits post-production in Filmmaking & Animation because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $22B — hard-surface modeling and rigging · SOM $450M — mechanical and robotics audio design\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$22B — hard-surface modeling and rigging",
      "som": "$450M — mechanical and robotics audio design"
    },
    {
      "id": "film-animation-ledger-credit-0",
      "theme": "film-animation",
      "title": "Ledger Credit",
      "pitch": "Ledger Credit pairs two wallets over DIDComm so credit attribution can exchange trusted claims on a private channel.",
      "concept": "A filmmaking app where filmmakers use DIDComm Connection to establish who made which part of the work.",
      "subDiscipline": "credit attribution",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits credit attribution in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Ledger Credit\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nA filmmaking app where filmmakers use DIDComm Connection to establish who made which part of the work.\nDiscipline: Filmmaking & Animation (credit attribution).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits credit attribution in Filmmaking & Animation because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $10B — underwater and maritime production · SOM $200M — aquatic soundscapes and foley\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$10B — underwater and maritime production",
      "som": "$200M — aquatic soundscapes and foley"
    },
    {
      "id": "film-animation-production-credits-app-0",
      "theme": "film-animation",
      "title": "Production Credits App",
      "pitch": "Production Credits App issues a signed verifiable credential for credits management that the holder keeps and reuses anywhere.",
      "concept": "Simplifies the process of issuing production credits to all contributors based on authenticated roles.",
      "subDiscipline": "credits management",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches credits management in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Production Credits App\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSimplifies the process of issuing production credits to all contributors based on authenticated roles.\nDiscipline: Filmmaking & Animation (credits management).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches credits management in Filmmaking & Animation because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $5B — title sequence and broadcast packaging · SOM $300M — main title theme composition\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$5B — title sequence and broadcast packaging",
      "som": "$300M — main title theme composition"
    },
    {
      "id": "film-animation-talent-agency-validator-0",
      "theme": "film-animation",
      "title": "Talent Agency Validator",
      "pitch": "Talent Agency Validator verifies a credential at the gate for representation — proof without a phone call to the issuer.",
      "concept": "Actors verify their representation status with agencies, sharing only necessary information without revealing personal contracts.",
      "subDiscipline": "representation",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for representation in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Talent Agency Validator\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nActors verify their representation status with agencies, sharing only necessary information without revealing personal contracts.\nDiscipline: Filmmaking & Animation (representation).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for representation in Filmmaking & Animation because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $400B — global animation and film production · SAM $15B — franchise and sequel production · SOM $350M — cinematic stinger and transition audio\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$400B — global animation and film production",
      "sam": "$15B — franchise and sequel production",
      "som": "$350M — cinematic stinger and transition audio"
    }
  ]
}
