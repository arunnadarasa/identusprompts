{
  "theme": {
    "slug": "visual-art",
    "name": "Visual Art",
    "emoji": "🎨",
    "audience": "painters, illustrators, generative artists, gallerists",
    "market_anchor": "the global art market (~$65B; >300K working visual artists)"
  },
  "ideas": [
    {
      "id": "visual-art-canvas-calm-narrator-0",
      "theme": "visual-art",
      "title": "Canvas Calm Narrator",
      "pitch": "Canvas Calm Narrator spins up a fresh public Sprite for each session so studio mindfulness gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "studio mindfulness",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Text-to-Speech provides a soothing, hands-free audio guide for artists whose hands are covered in paint or charcoal.",
      "megaPrompt": "Build \"Canvas Calm Narrator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCanvas Calm Narrator spins up a fresh public Sprite for each session so studio mindfulness gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (studio mindfulness).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Text-to-Speech provides a soothing, hands-free audio guide for artists whose hands are covered in paint or charcoal.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `studio-mindfulness-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for studio mindfulness.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — global wellness and meditation app market",
      "sam": "$500M — creative professional mindfulness tools",
      "som": "$15M — visual artist studio wellness subscriptions"
    },
    {
      "id": "visual-art-palette-whisper-guide-1",
      "theme": "visual-art",
      "title": "Palette Whisper Guide",
      "pitch": "Palette Whisper Guide spins up a fresh public Sprite for each session so color theory study gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "color theory study",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Hands-free Text-to-Speech allows painters to hear exact pigment drops without looking away from their physical palette.",
      "megaPrompt": "Build \"Palette Whisper Guide\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPalette Whisper Guide spins up a fresh public Sprite for each session so color theory study gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (color theory study).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Hands-free Text-to-Speech allows painters to hear exact pigment drops without looking away from their physical palette.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `color-theory-study-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for color theory study.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$12B — global art supplies and education market",
      "sam": "$2B — digital and traditional color theory learning",
      "som": "$40M — professional painter masterclass subscriptions"
    },
    {
      "id": "visual-art-exhibition-echo-docent-2",
      "theme": "visual-art",
      "title": "Exhibition Echo Docent",
      "pitch": "Exhibition Echo Docent spins up a fresh public Sprite for each session so gallery audio tours gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "gallery audio tours",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "High-quality multilingual Text-to-Speech creates immersive, localized audio guides without hiring live docents for every exhibition.",
      "megaPrompt": "Build \"Exhibition Echo Docent\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nExhibition Echo Docent spins up a fresh public Sprite for each session so gallery audio tours gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (gallery audio tours).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: High-quality multilingual Text-to-Speech creates immersive, localized audio guides without hiring live docents for every exhibition.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `gallery-audio-tours-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for gallery audio tours.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global commercial art gallery market",
      "sam": "$5B — gallery visitor experience and audio tech",
      "som": "$100M — independent gallery audio guide software"
    },
    {
      "id": "visual-art-easel-posture-prompter-3",
      "theme": "visual-art",
      "title": "Easel Posture Prompter",
      "pitch": "Easel Posture Prompter spins up a fresh public Sprite for each session so studio ergonomics gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "studio ergonomics",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Audio cues ensure artists remember to stretch and correct posture without breaking their visual flow state.",
      "megaPrompt": "Build \"Easel Posture Prompter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nEasel Posture Prompter spins up a fresh public Sprite for each session so studio ergonomics gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (studio ergonomics).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Audio cues ensure artists remember to stretch and correct posture without breaking their visual flow state.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `studio-ergonomics-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for studio ergonomics.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — occupational health and ergonomics market",
      "sam": "$800M — freelance and studio worker health tools",
      "som": "$20M — physical artist wellness applications"
    },
    {
      "id": "visual-art-prompt-alchemy-broadcaster-4",
      "theme": "visual-art",
      "title": "Prompt Alchemy Broadcaster",
      "pitch": "Prompt Alchemy Broadcaster spins up a fresh public Sprite for each session so generative prompt engineering gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "generative prompt engineering",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Hearing prompts spoken aloud via Text-to-Speech stimulates different cognitive pathways for generative artists seeking unexpected visual combinations.",
      "megaPrompt": "Build \"Prompt Alchemy Broadcaster\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPrompt Alchemy Broadcaster spins up a fresh public Sprite for each session so generative prompt engineering gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (generative prompt engineering).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Hearing prompts spoken aloud via Text-to-Speech stimulates different cognitive pathways for generative artists seeking unexpected visual combinations.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `generative-prompt-engine-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for generative prompt engineering.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — digital art and generative AI software",
      "sam": "$1.5B — prompt engineering and workflow tools",
      "som": "$30M — indie generative artist utility apps"
    },
    {
      "id": "visual-art-sketchbook-lore-keeper-5",
      "theme": "visual-art",
      "title": "Sketchbook Lore Keeper",
      "pitch": "Sketchbook Lore Keeper spins up a fresh public Sprite for each session so visual journaling gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "visual journaling",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Text-to-Speech turns quiet journaling into an engaging, personalized art history podcast for the creator's own reflection.",
      "megaPrompt": "Build \"Sketchbook Lore Keeper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSketchbook Lore Keeper spins up a fresh public Sprite for each session so visual journaling gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (visual journaling).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Text-to-Speech turns quiet journaling into an engaging, personalized art history podcast for the creator's own reflection.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `visual-journaling-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for visual journaling.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — digital journaling and note-taking market",
      "sam": "$400M — creative professional reflection tools",
      "som": "$10M — visual art student and hobbyist apps"
    },
    {
      "id": "visual-art-composition-critique-chorus-6",
      "theme": "visual-art",
      "title": "Composition Critique Chorus",
      "pitch": "Composition Critique Chorus spins up a fresh public Sprite for each session so visual composition analysis gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "visual composition analysis",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Spoken critique mimics a traditional studio visit, allowing students to absorb feedback while physically stepping back from their work.",
      "megaPrompt": "Build \"Composition Critique Chorus\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nComposition Critique Chorus spins up a fresh public Sprite for each session so visual composition analysis gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (visual composition analysis).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Spoken critique mimics a traditional studio visit, allowing students to absorb feedback while physically stepping back from their work.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `visual-composition-analy-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for visual composition analysis.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$15B — global art education and tutoring market",
      "sam": "$3B — digital art critique and portfolio review",
      "som": "$60M — art school and independent student tools"
    },
    {
      "id": "visual-art-lumens-rhythm-director-7",
      "theme": "visual-art",
      "title": "Lumens Rhythm Director",
      "pitch": "Lumens Rhythm Director spins up a fresh public Sprite for each session so studio lighting management gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "studio lighting management",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits studio lighting management in Visual Art because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Lumens Rhythm Director\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLumens Rhythm Director spins up a fresh public Sprite for each session so studio lighting management gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (studio lighting management).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits studio lighting management in Visual Art because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `studio-lighting-manageme-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for studio lighting management.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$25B — smart home and studio lighting market",
      "sam": "$2B — specialized creative workspace automation",
      "som": "$25M — professional painter studio setups"
    },
    {
      "id": "visual-art-pigment-scout-announcer-8",
      "theme": "visual-art",
      "title": "Pigment Scout Announcer",
      "pitch": "Pigment Scout Announcer spins up a fresh public Sprite for each session so art material sourcing gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "art material sourcing",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Audio alerts keep artists informed about niche supply chains without forcing them to constantly check inventory websites.",
      "megaPrompt": "Build \"Pigment Scout Announcer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPigment Scout Announcer spins up a fresh public Sprite for each session so art material sourcing gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (art material sourcing).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Audio alerts keep artists informed about niche supply chains without forcing them to constantly check inventory websites.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `art-material-sourcing-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for art material sourcing.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$12B — global fine art materials market",
      "sam": "$1B — specialized pigment and canvas supply chain",
      "som": "$15M — professional oil painter procurement tools"
    },
    {
      "id": "visual-art-curation-flow-narrator-9",
      "theme": "visual-art",
      "title": "Curation Flow Narrator",
      "pitch": "Curation Flow Narrator spins up a fresh public Sprite for each session so exhibition spatial planning gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "exhibition spatial planning",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Hearing the physical walkthrough helps curators mentally simulate visitor movement through a space before installation begins.",
      "megaPrompt": "Build \"Curation Flow Narrator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCuration Flow Narrator spins up a fresh public Sprite for each session so exhibition spatial planning gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (exhibition spatial planning).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Hearing the physical walkthrough helps curators mentally simulate visitor movement through a space before installation begins.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `exhibition-spatial-plann-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for exhibition spatial planning.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market and exhibition management",
      "sam": "$4B — gallery curation and spatial design software",
      "som": "$80M — independent curator and museum planning tools"
    },
    {
      "id": "visual-art-valuation-compass-voice-10",
      "theme": "visual-art",
      "title": "Valuation Compass Voice",
      "pitch": "Valuation Compass Voice spins up a fresh public Sprite for each session so art market pricing gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "art market pricing",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Spoken rationale acts as a confidence coach, helping artists internalize their worth before discussing prices with collectors.",
      "megaPrompt": "Build \"Valuation Compass Voice\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nValuation Compass Voice spins up a fresh public Sprite for each session so art market pricing gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (art market pricing).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Spoken rationale acts as a confidence coach, helping artists internalize their worth before discussing prices with collectors.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `art-market-pricing-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for art market pricing.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global commercial art sales market",
      "sam": "$5B — art advisory and pricing analytics",
      "som": "$50M — emerging artist career management platforms"
    },
    {
      "id": "visual-art-acid-bath-timer-11",
      "theme": "visual-art",
      "title": "Acid Bath Timer",
      "pitch": "Acid Bath Timer spins up a fresh public Sprite for each session so printmaking etching gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "printmaking etching",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Hands-free audio timing is critical when an artist's hands are covered in hazardous chemicals and wax grounds.",
      "megaPrompt": "Build \"Acid Bath Timer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAcid Bath Timer spins up a fresh public Sprite for each session so printmaking etching gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (printmaking etching).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Hands-free audio timing is critical when an artist's hands are covered in hazardous chemicals and wax grounds.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `printmaking-etching-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for printmaking etching.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$12B — global printmaking and fine art supplies",
      "sam": "$800M — professional printmaking studio equipment",
      "som": "$12M — academic and indie printmaker software"
    },
    {
      "id": "visual-art-grid-projection-caller-12",
      "theme": "visual-art",
      "title": "Grid Projection Caller",
      "pitch": "Grid Projection Caller spins up a fresh public Sprite for each session so mural scaling and mapping gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "mural scaling and mapping",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Calling out coordinates allows muralists to maintain their physical momentum on scaffolding without constantly checking a digital screen.",
      "megaPrompt": "Build \"Grid Projection Caller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nGrid Projection Caller spins up a fresh public Sprite for each session so mural scaling and mapping gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (mural scaling and mapping).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Calling out coordinates allows muralists to maintain their physical momentum on scaffolding without constantly checking a digital screen.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `mural-scaling-and-mappin-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for mural scaling and mapping.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — public art and mural commissioning market",
      "sam": "$1B — large-scale art projection and mapping tools",
      "som": "$20M — professional muralist and street artist utilities"
    },
    {
      "id": "visual-art-portfolio-pitch-rehearser-13",
      "theme": "visual-art",
      "title": "Portfolio Pitch Rehearser",
      "pitch": "Portfolio Pitch Rehearser spins up a fresh public Sprite for each session so artist statement delivery gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "artist statement delivery",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Text-to-Speech provides a rhythmic baseline for artists to practice speaking about their work confidently for grants and residencies.",
      "megaPrompt": "Build \"Portfolio Pitch Rehearser\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPortfolio Pitch Rehearser spins up a fresh public Sprite for each session so artist statement delivery gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (artist statement delivery).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Text-to-Speech provides a rhythmic baseline for artists to practice speaking about their work confidently for grants and residencies.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `artist-statement-deliver-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for artist statement delivery.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — arts grant and residency funding market",
      "sam": "$500M — artist career development and coaching",
      "som": "$15M — portfolio presentation and pitch software"
    },
    {
      "id": "visual-art-kiln-firing-announcer-14",
      "theme": "visual-art",
      "title": "Kiln Firing Announcer",
      "pitch": "Kiln Firing Announcer spins up a fresh public Sprite for each session so ceramic glaze chemistry gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "ceramic glaze chemistry",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Audio instructions prevent cross-contamination by allowing potters to listen to recipes while their hands are deep in chemical buckets.",
      "megaPrompt": "Build \"Kiln Firing Announcer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nKiln Firing Announcer spins up a fresh public Sprite for each session so ceramic glaze chemistry gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (ceramic glaze chemistry).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Audio instructions prevent cross-contamination by allowing potters to listen to recipes while their hands are deep in chemical buckets.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `ceramic-glaze-chemistry-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for ceramic glaze chemistry.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — global ceramics and pottery supplies market",
      "sam": "$1.5B — advanced ceramic glaze formulation tools",
      "som": "$25M — studio potter and ceramicist software"
    },
    {
      "id": "visual-art-alt-text-artdescriber-15",
      "theme": "visual-art",
      "title": "Alt Text ArtDescriber",
      "pitch": "Alt Text ArtDescriber spins up a fresh public Sprite for each session so digital art accessibility gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "digital art accessibility",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Hearing the alt-text read aloud helps artists ensure the emotional tone matches their visual intent for screen-reader users.",
      "megaPrompt": "Build \"Alt Text ArtDescriber\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAlt Text ArtDescriber spins up a fresh public Sprite for each session so digital art accessibility gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (digital art accessibility).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Hearing the alt-text read aloud helps artists ensure the emotional tone matches their visual intent for screen-reader users.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `digital-art-accessibilit-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for digital art accessibility.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$15B — digital accessibility and compliance software",
      "sam": "$2B — creative media accessibility tools",
      "som": "$30M — digital illustrator and web comic platforms"
    },
    {
      "id": "visual-art-varnish-cure-narrator-16",
      "theme": "visual-art",
      "title": "Varnish Cure Narrator",
      "pitch": "Varnish Cure Narrator spins up a fresh public Sprite for each session so painting conservation gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "painting conservation",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits painting conservation in Visual Art because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Varnish Cure Narrator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nVarnish Cure Narrator spins up a fresh public Sprite for each session so painting conservation gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (painting conservation).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits painting conservation in Visual Art because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `painting-conservation-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for painting conservation.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — art conservation and museum preservation market",
      "sam": "$1B — fine art storage and climate monitoring",
      "som": "$20M — private conservation lab management software"
    },
    {
      "id": "visual-art-gesture-pose-caller-17",
      "theme": "visual-art",
      "title": "Gesture Pose Caller",
      "pitch": "Gesture Pose Caller spins up a fresh public Sprite for each session so life drawing sessions gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "life drawing sessions",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Text-to-Speech acts as an automated, unbiased session director, calling out timed gesture changes to keep the drawing class flowing.",
      "megaPrompt": "Build \"Gesture Pose Caller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nGesture Pose Caller spins up a fresh public Sprite for each session so life drawing sessions gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (life drawing sessions).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Text-to-Speech acts as an automated, unbiased session director, calling out timed gesture changes to keep the drawing class flowing.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `life-drawing-sessions-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for life drawing sessions.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$15B — global art education and life drawing market",
      "sam": "$2B — figure drawing session management tools",
      "som": "$40M — independent sketch group and studio apps"
    },
    {
      "id": "visual-art-crate-packing-coach-18",
      "theme": "visual-art",
      "title": "Crate Packing Coach",
      "pitch": "Crate Packing Coach spins up a fresh public Sprite for each session so fine art logistics gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "fine art logistics",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Spoken measurements allow artists to use power tools and cut materials safely without taking their eyes off the work.",
      "megaPrompt": "Build \"Crate Packing Coach\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCrate Packing Coach spins up a fresh public Sprite for each session so fine art logistics gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (fine art logistics).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Spoken measurements allow artists to use power tools and cut materials safely without taking their eyes off the work.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `fine-art-logistics-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for fine art logistics.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$20B — fine art logistics and shipping market",
      "sam": "$3B — independent artist packaging and freight tools",
      "som": "$50M — studio-to-gallery shipping software"
    },
    {
      "id": "visual-art-wash-timing-whisperer-19",
      "theme": "visual-art",
      "title": "Wash Timing Whisperer",
      "pitch": "Wash Timing Whisperer spins up a fresh public Sprite for each session so watercolor technique gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "watercolor technique",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Gentle audio cues help painters time their wet-on-wet techniques perfectly without hovering anxiously over the damp paper.",
      "megaPrompt": "Build \"Wash Timing Whisperer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nWash Timing Whisperer spins up a fresh public Sprite for each session so watercolor technique gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (watercolor technique).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Gentle audio cues help painters time their wet-on-wet techniques perfectly without hovering anxiously over the damp paper.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `watercolor-technique-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for watercolor technique.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$12B — global watercolor and fine art supplies",
      "sam": "$1B — watercolor technique education and tools",
      "som": "$15M — traditional watercolorist utility apps"
    },
    {
      "id": "visual-art-canvas-stretcher-counter-20",
      "theme": "visual-art",
      "title": "Canvas Stretcher Counter",
      "pitch": "Canvas Stretcher Counter spins up a fresh public Sprite for each session so studio inventory management gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "studio inventory management",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Audio readouts turn tedious physical inventory counts into a hands-free, conversational studio cleanup experience.",
      "megaPrompt": "Build \"Canvas Stretcher Counter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCanvas Stretcher Counter spins up a fresh public Sprite for each session so studio inventory management gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (studio inventory management).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Audio readouts turn tedious physical inventory counts into a hands-free, conversational studio cleanup experience.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `studio-inventory-managem-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for studio inventory management.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$12B — global art materials and inventory market",
      "sam": "$1.5B — studio management and supply tracking",
      "som": "$25M — professional painter studio organization tools"
    },
    {
      "id": "visual-art-patron-update-broadcaster-21",
      "theme": "visual-art",
      "title": "Patron Update Broadcaster",
      "pitch": "Patron Update Broadcaster spins up a fresh public Sprite for each session so commission client relations gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "commission client relations",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Hearing the diplomatic phrasing aloud helps artists navigate difficult commission conversations with wealthy patrons confidently.",
      "megaPrompt": "Build \"Patron Update Broadcaster\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPatron Update Broadcaster spins up a fresh public Sprite for each session so commission client relations gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (commission client relations).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Hearing the diplomatic phrasing aloud helps artists navigate difficult commission conversations with wealthy patrons confidently.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `commission-client-relati-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for commission client relations.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global commissioned art and portrait market",
      "sam": "$4B — freelance artist client management software",
      "som": "$60M — independent portrait and commission platforms"
    },
    {
      "id": "visual-art-sunlight-shift-spotter-22",
      "theme": "visual-art",
      "title": "Sunlight Shift Spotter",
      "pitch": "Sunlight Shift Spotter spins up a fresh public Sprite for each session so plein air landscape gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "plein air landscape",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Audio alerts notify plein air artists of fleeting light changes without forcing them to look away from the landscape.",
      "megaPrompt": "Build \"Sunlight Shift Spotter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSunlight Shift Spotter spins up a fresh public Sprite for each session so plein air landscape gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (plein air landscape).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Audio alerts notify plein air artists of fleeting light changes without forcing them to look away from the landscape.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `plein-air-landscape-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for plein air landscape.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — outdoor recreation and plein air market",
      "sam": "$1B — landscape painting gear and accessories",
      "som": "$15M — plein air painter utility applications"
    },
    {
      "id": "visual-art-residency-essay-echo-23",
      "theme": "visual-art",
      "title": "Residency Essay Echo",
      "pitch": "Residency Essay Echo spins up a fresh public Sprite for each session so grant writing and applications gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "grant writing and applications",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Listening to the essay read aloud reveals clunky phrasing and pacing issues that silent reading often misses.",
      "megaPrompt": "Build \"Residency Essay Echo\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nResidency Essay Echo spins up a fresh public Sprite for each session so grant writing and applications gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (grant writing and applications).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Listening to the essay read aloud reveals clunky phrasing and pacing issues that silent reading often misses.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `grant-writing-and-applic-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for grant writing and applications.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — arts grant and residency funding market",
      "sam": "$600M — artist application and grant writing tools",
      "som": "$20M — emerging artist career advancement software"
    },
    {
      "id": "visual-art-chroma-chord-translator-24",
      "theme": "visual-art",
      "title": "Chroma Chord Translator",
      "pitch": "Chroma Chord Translator spins up a fresh public Sprite for each session so synesthetic art exploration gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "synesthetic art exploration",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Spoken musical descriptions help visual artists conceptualize synesthesia, translating color palettes into audible rhythms for cross-sensory inspiration.",
      "megaPrompt": "Build \"Chroma Chord Translator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nChroma Chord Translator spins up a fresh public Sprite for each session so synesthetic art exploration gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (synesthetic art exploration).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Spoken musical descriptions help visual artists conceptualize synesthesia, translating color palettes into audible rhythms for cross-sensory inspiration.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `synesthetic-art-explorat-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for synesthetic art exploration.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — digital art and cross-sensory software",
      "sam": "$1B — experimental art and synesthesia tools",
      "som": "$15M — avant-garde and generative artist apps"
    },
    {
      "id": "visual-art-palette-whisperer-0",
      "theme": "visual-art",
      "title": "Palette Whisperer",
      "pitch": "Palette Whisperer spins up a fresh public Sprite for each session so color mixing gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "color mixing",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Hands covered in wet paint require a real-time conversational agent to adjust ratios verbally without touching screens.",
      "megaPrompt": "Build \"Palette Whisperer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPalette Whisperer spins up a fresh public Sprite for each session so color mixing gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (color mixing).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Hands covered in wet paint require a real-time conversational agent to adjust ratios verbally without touching screens.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `color-mixing-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for color mixing.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$12B — fine art painting supplies",
      "som": "$150M — digital tools for traditional painters"
    },
    {
      "id": "visual-art-gallery-floor-coach-1",
      "theme": "visual-art",
      "title": "Gallery Floor Coach",
      "pitch": "Gallery Floor Coach spins up a fresh public Sprite for each session so exhibition sales gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "exhibition sales",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Roleplaying objections requires a low-latency conversational agent to simulate natural, unpredictable buyer interruptions.",
      "megaPrompt": "Build \"Gallery Floor Coach\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nGallery Floor Coach spins up a fresh public Sprite for each session so exhibition sales gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (exhibition sales).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Roleplaying objections requires a low-latency conversational agent to simulate natural, unpredictable buyer interruptions.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `exhibition-sales-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for exhibition sales.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$8B — commercial gallery operations",
      "som": "$200M — gallery sales training software"
    },
    {
      "id": "visual-art-storyboard-narrator-2",
      "theme": "visual-art",
      "title": "Storyboard Narrator",
      "pitch": "Storyboard Narrator spins up a fresh public Sprite for each session so visual storytelling gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "visual storytelling",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Testing narrative pacing demands a conversational agent that can pause, rewind, and discuss emotional beats interactively.",
      "megaPrompt": "Build \"Storyboard Narrator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nStoryboard Narrator spins up a fresh public Sprite for each session so visual storytelling gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (visual storytelling).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Testing narrative pacing demands a conversational agent that can pause, rewind, and discuss emotional beats interactively.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `visual-storytelling-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for visual storytelling.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$5B — commercial illustration and publishing",
      "som": "$120M — storyboarding and pre-visualization tools"
    },
    {
      "id": "visual-art-code-canvas-companion-3",
      "theme": "visual-art",
      "title": "Code Canvas Companion",
      "pitch": "Code Canvas Companion spins up a fresh public Sprite for each session so creative coding gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "creative coding",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Debugging code hands-free requires a conversational agent to listen to error descriptions and talk through logic fixes.",
      "megaPrompt": "Build \"Code Canvas Companion\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCode Canvas Companion spins up a fresh public Sprite for each session so creative coding gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (creative coding).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Debugging code hands-free requires a conversational agent to listen to error descriptions and talk through logic fixes.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `creative-coding-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for creative coding.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$3B — digital and generative art software",
      "som": "$80M — creative coding environments and plugins"
    },
    {
      "id": "visual-art-easel-posture-guide-4",
      "theme": "visual-art",
      "title": "Easel Posture Guide",
      "pitch": "Easel Posture Guide spins up a fresh public Sprite for each session so studio ergonomics gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "studio ergonomics",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Stretching routines need a conversational agent to listen to pain feedback and adjust physical cues dynamically.",
      "megaPrompt": "Build \"Easel Posture Guide\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nEasel Posture Guide spins up a fresh public Sprite for each session so studio ergonomics gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (studio ergonomics).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Stretching routines need a conversational agent to listen to pain feedback and adjust physical cues dynamically.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `studio-ergonomics-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for studio ergonomics.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$2B — artist wellness and studio equipment",
      "som": "$40M — ergonomic aids for fine artists"
    },
    {
      "id": "visual-art-canvas-critic-5",
      "theme": "visual-art",
      "title": "Canvas Critic",
      "pitch": "Canvas Critic spins up a fresh public Sprite for each session so peer review gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "peer review",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Critiquing art is a dialogue, requiring a conversational agent to answer follow-up questions about specific compositional choices.",
      "megaPrompt": "Build \"Canvas Critic\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCanvas Critic spins up a fresh public Sprite for each session so peer review gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (peer review).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Critiquing art is a dialogue, requiring a conversational agent to answer follow-up questions about specific compositional choices.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `peer-review-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for peer review.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$10B — art education and critique services",
      "som": "$250M — digital mentorship and portfolio review platforms"
    },
    {
      "id": "visual-art-white-cube-walker-6",
      "theme": "visual-art",
      "title": "White Cube Walker",
      "pitch": "White Cube Walker spins up a fresh public Sprite for each session so spatial curation gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "spatial curation",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Measuring walls requires a conversational agent to listen to spatial dimensions and recalculate placements on the fly.",
      "megaPrompt": "Build \"White Cube Walker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nWhite Cube Walker spins up a fresh public Sprite for each session so spatial curation gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (spatial curation).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Measuring walls requires a conversational agent to listen to spatial dimensions and recalculate placements on the fly.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `spatial-curation-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for spatial curation.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$4B — museum and gallery exhibition design",
      "som": "$90M — curatorial spatial planning software"
    },
    {
      "id": "visual-art-statement-sculptor-7",
      "theme": "visual-art",
      "title": "Statement Sculptor",
      "pitch": "Statement Sculptor spins up a fresh public Sprite for each session so arts writing gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "arts writing",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Drafting essays benefits from a conversational agent that can debate word choices and refine tone interactively.",
      "megaPrompt": "Build \"Statement Sculptor\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nStatement Sculptor spins up a fresh public Sprite for each session so arts writing gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (arts writing).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Drafting essays benefits from a conversational agent that can debate word choices and refine tone interactively.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `arts-writing-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for arts writing.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$1B — art PR and copywriting services",
      "som": "$30M — AI writing assistants for creatives"
    },
    {
      "id": "visual-art-plein-air-spotter-8",
      "theme": "visual-art",
      "title": "Plein Air Spotter",
      "pitch": "Plein Air Spotter spins up a fresh public Sprite for each session so outdoor painting gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "outdoor painting",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Outdoor painting requires a conversational agent to listen to environmental changes and update shadow warnings dynamically.",
      "megaPrompt": "Build \"Plein Air Spotter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPlein Air Spotter spins up a fresh public Sprite for each session so outdoor painting gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (outdoor painting).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Outdoor painting requires a conversational agent to listen to environmental changes and update shadow warnings dynamically.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `outdoor-painting-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for outdoor painting.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$6B — landscape and plein art materials",
      "som": "$110M — outdoor painting accessories and aids"
    },
    {
      "id": "visual-art-press-timer-talker-9",
      "theme": "visual-art",
      "title": "Press Timer Talker",
      "pitch": "Press Timer Talker spins up a fresh public Sprite for each session so printmaking gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "printmaking",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Printmaking involves hazardous chemicals, needing a conversational agent to answer urgent safety questions while hands are busy.",
      "megaPrompt": "Build \"Press Timer Talker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPress Timer Talker spins up a fresh public Sprite for each session so printmaking gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (printmaking).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Printmaking involves hazardous chemicals, needing a conversational agent to answer urgent safety questions while hands are busy.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `printmaking-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for printmaking.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$2B — fine art printmaking supplies",
      "som": "$45M — printmaking studio management tools"
    },
    {
      "id": "visual-art-muse-archivist-10",
      "theme": "visual-art",
      "title": "Muse Archivist",
      "pitch": "Muse Archivist spins up a fresh public Sprite for each session so art history gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "art history",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Studying art history requires a conversational agent to engage in Socratic dialogue about historical techniques and contexts.",
      "megaPrompt": "Build \"Muse Archivist\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nMuse Archivist spins up a fresh public Sprite for each session so art history gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (art history).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Studying art history requires a conversational agent to engage in Socratic dialogue about historical techniques and contexts.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `art-history-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for art history.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$7B — art history research and academia",
      "som": "$150M — digital archives and study aids for artists"
    },
    {
      "id": "visual-art-brief-interrogator-11",
      "theme": "visual-art",
      "title": "Brief Interrogator",
      "pitch": "Brief Interrogator spins up a fresh public Sprite for each session so commercial illustration gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "commercial illustration",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Clarifying briefs requires a conversational agent to simulate the client and respond to the illustrator's probing questions.",
      "megaPrompt": "Build \"Brief Interrogator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nBrief Interrogator spins up a fresh public Sprite for each session so commercial illustration gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (commercial illustration).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Clarifying briefs requires a conversational agent to simulate the client and respond to the illustrator's probing questions.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `commercial-illustration-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for commercial illustration.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$15B — commercial art and advertising illustration",
      "som": "$300M — freelance illustrator business management tools"
    },
    {
      "id": "visual-art-canvas-voicer-12",
      "theme": "visual-art",
      "title": "Canvas Voicer",
      "pitch": "Canvas Voicer spins up a fresh public Sprite for each session so visual accessibility gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "visual accessibility",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Creating audio guides requires a conversational agent to interact with visually impaired users to answer specific artwork questions.",
      "megaPrompt": "Build \"Canvas Voicer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCanvas Voicer spins up a fresh public Sprite for each session so visual accessibility gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (visual accessibility).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Creating audio guides requires a conversational agent to interact with visually impaired users to answer specific artwork questions.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `visual-accessibility-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for visual accessibility.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$3B — museum accessibility and inclusion services",
      "som": "$75M — audio description software for cultural institutions"
    },
    {
      "id": "visual-art-pigment-forager-13",
      "theme": "visual-art",
      "title": "Pigment Forager",
      "pitch": "Pigment Forager spins up a fresh public Sprite for each session so material sourcing gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "material sourcing",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Browsing stores requires a conversational agent to listen to pigment names and verify vendor stock interactively.",
      "megaPrompt": "Build \"Pigment Forager\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPigment Forager spins up a fresh public Sprite for each session so material sourcing gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (material sourcing).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Browsing stores requires a conversational agent to listen to pigment names and verify vendor stock interactively.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `material-sourcing-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for material sourcing.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$5B — specialty art material retail",
      "som": "$100M — supply chain tools for independent art shops"
    },
    {
      "id": "visual-art-prompt-alchemist-14",
      "theme": "visual-art",
      "title": "Prompt Alchemist",
      "pitch": "Prompt Alchemist spins up a fresh public Sprite for each session so AI image generation gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "AI image generation",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Iterating prompts requires a conversational agent to listen to visual feedback and conversationally tweak descriptive parameters.",
      "megaPrompt": "Build \"Prompt Alchemist\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPrompt Alchemist spins up a fresh public Sprite for each session so AI image generation gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (AI image generation).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Iterating prompts requires a conversational agent to listen to visual feedback and conversationally tweak descriptive parameters.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `ai-image-generation-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for AI image generation.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$4B — generative AI software subscriptions",
      "som": "$200M — prompt engineering and workflow tools"
    },
    {
      "id": "visual-art-fair-floor-wingman-15",
      "theme": "visual-art",
      "title": "Fair Floor Wingman",
      "pitch": "Fair Floor Wingman spins up a fresh public Sprite for each session so art networking gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "art networking",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Networking requires a conversational agent to listen to ambient introductions and whisper contextual follow-up questions to the gallerist.",
      "megaPrompt": "Build \"Fair Floor Wingman\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFair Floor Wingman spins up a fresh public Sprite for each session so art networking gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (art networking).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Networking requires a conversational agent to listen to ambient introductions and whisper contextual follow-up questions to the gallerist.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `art-networking-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for art networking.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$9B — art fair operations and VIP relations",
      "som": "$180M — CRM and networking tools for art dealers"
    },
    {
      "id": "visual-art-mural-grid-caller-16",
      "theme": "visual-art",
      "title": "Mural Grid Caller",
      "pitch": "Mural Grid Caller spins up a fresh public Sprite for each session so large-scale painting gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "large-scale painting",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Painting on scaffolding requires a conversational agent to listen to grid confirmations and call out the next coordinates.",
      "megaPrompt": "Build \"Mural Grid Caller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nMural Grid Caller spins up a fresh public Sprite for each session so large-scale painting gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (large-scale painting).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Painting on scaffolding requires a conversational agent to listen to grid confirmations and call out the next coordinates.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `large-scale-painting-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for large-scale painting.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$2B — public art and mural commissions",
      "som": "$60M — large-scale painting planning software"
    },
    {
      "id": "visual-art-kiln-watcher-17",
      "theme": "visual-art",
      "title": "Kiln Watcher",
      "pitch": "Kiln Watcher spins up a fresh public Sprite for each session so ceramic firing gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "ceramic firing",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Monitoring kilns requires a conversational agent to listen to temperature overrides and discuss firing adjustments in real time.",
      "megaPrompt": "Build \"Kiln Watcher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nKiln Watcher spins up a fresh public Sprite for each session so ceramic firing gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (ceramic firing).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Monitoring kilns requires a conversational agent to listen to temperature overrides and discuss firing adjustments in real time.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `ceramic-firing-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for ceramic firing.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$3B — ceramics and sculptural arts supplies",
      "som": "$85M — kiln monitoring and studio safety tech"
    },
    {
      "id": "visual-art-wash-water-guide-18",
      "theme": "visual-art",
      "title": "Wash Water Guide",
      "pitch": "Wash Water Guide spins up a fresh public Sprite for each session so watercolor technique gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "watercolor technique",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Watercolor layering requires a conversational agent to listen to paper wetness observations and advise on the next wash.",
      "megaPrompt": "Build \"Wash Water Guide\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nWash Water Guide spins up a fresh public Sprite for each session so watercolor technique gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (watercolor technique).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Watercolor layering requires a conversational agent to listen to paper wetness observations and advise on the next wash.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `watercolor-technique-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for watercolor technique.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$4B — watercolor and paper art materials",
      "som": "$90M — technique aids for watercolor painters"
    },
    {
      "id": "visual-art-price-tag-whisper-19",
      "theme": "visual-art",
      "title": "Price Tag Whisper",
      "pitch": "Price Tag Whisper spins up a fresh public Sprite for each session so art valuation gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "art valuation",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Pricing art requires a conversational agent to debate market variables and negotiate final numbers with the artist.",
      "megaPrompt": "Build \"Price Tag Whisper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPrice Tag Whisper spins up a fresh public Sprite for each session so art valuation gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (art valuation).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Pricing art requires a conversational agent to debate market variables and negotiate final numbers with the artist.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `art-valuation-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for art valuation.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$11B — art advisory and valuation services",
      "som": "$220M — pricing analytics for independent artists"
    },
    {
      "id": "visual-art-sketchbook-confidant-20",
      "theme": "visual-art",
      "title": "Sketchbook Confidant",
      "pitch": "Sketchbook Confidant spins up a fresh public Sprite for each session so visual journaling gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "visual journaling",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Journaling requires a conversational agent to listen to emotional reflections and ask empathetic, probing follow-up questions.",
      "megaPrompt": "Build \"Sketchbook Confidant\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSketchbook Confidant spins up a fresh public Sprite for each session so visual journaling gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (visual journaling).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Journaling requires a conversational agent to listen to emotional reflections and ask empathetic, probing follow-up questions.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `visual-journaling-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for visual journaling.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$2B — art therapy and creative wellness",
      "som": "$50M — digital journaling and mindfulness apps for creatives"
    },
    {
      "id": "visual-art-nib-rhythm-keeper-21",
      "theme": "visual-art",
      "title": "Nib Rhythm Keeper",
      "pitch": "Nib Rhythm Keeper spins up a fresh public Sprite for each session so calligraphy gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "calligraphy",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Calligraphy requires a conversational agent to listen to stroke rhythm feedback and adjust the spoken metronome dynamically.",
      "megaPrompt": "Build \"Nib Rhythm Keeper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nNib Rhythm Keeper spins up a fresh public Sprite for each session so calligraphy gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (calligraphy).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Calligraphy requires a conversational agent to listen to stroke rhythm feedback and adjust the spoken metronome dynamically.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `calligraphy-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for calligraphy.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$1B — calligraphy and lettering supplies",
      "som": "$25M — practice aids for typographic artists"
    },
    {
      "id": "visual-art-light-meter-muse-22",
      "theme": "visual-art",
      "title": "Light Meter Muse",
      "pitch": "Light Meter Muse spins up a fresh public Sprite for each session so studio lighting gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "studio lighting",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Adjusting lights requires a conversational agent to listen to shadow descriptions and suggest real-time modifier changes.",
      "megaPrompt": "Build \"Light Meter Muse\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLight Meter Muse spins up a fresh public Sprite for each session so studio lighting gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (studio lighting).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Adjusting lights requires a conversational agent to listen to shadow descriptions and suggest real-time modifier changes.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `studio-lighting-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for studio lighting.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$14B — photography and lighting equipment",
      "som": "$350M — studio lighting calculation and planning tools"
    },
    {
      "id": "visual-art-varnish-doctor-23",
      "theme": "visual-art",
      "title": "Varnish Doctor",
      "pitch": "Varnish Doctor spins up a fresh public Sprite for each session so art conservation gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "art conservation",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Restoration requires a conversational agent to listen to solvent reactions and instantly advise on chemical neutralization.",
      "megaPrompt": "Build \"Varnish Doctor\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nVarnish Doctor spins up a fresh public Sprite for each session so art conservation gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (art conservation).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Restoration requires a conversational agent to listen to solvent reactions and instantly advise on chemical neutralization.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `art-conservation-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for art conservation.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$2B — art conservation and restoration services",
      "som": "$70M — diagnostic software for art conservators"
    },
    {
      "id": "visual-art-space-wire-rigger-24",
      "theme": "visual-art",
      "title": "Space Wire Rigger",
      "pitch": "Space Wire Rigger spins up a fresh public Sprite for each session so installation art gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "installation art",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Rigging requires a conversational agent to listen to tension feedback from the artist and adjust load calculations verbally.",
      "megaPrompt": "Build \"Space Wire Rigger\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSpace Wire Rigger spins up a fresh public Sprite for each session so installation art gets its own shareable sandbox URL in seconds.\nDiscipline: Visual Art (installation art).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Rigging requires a conversational agent to listen to tension feedback from the artist and adjust load calculations verbally.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `installation-art-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for installation art.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$3B — contemporary installation and spatial art",
      "som": "$65M — structural planning tools for installation artists"
    },
    {
      "id": "visual-art-canvas-whisper-journal-0",
      "theme": "visual-art",
      "title": "Canvas Whisper Journal",
      "pitch": "Canvas Whisper Journal runs a one-shot shell command inside a Sprite for studio journaling and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "studio journaling",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "Realtime STT allows painters to journal continuously without washing paint off their hands.",
      "megaPrompt": "Build \"Canvas Whisper Journal\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCanvas Whisper Journal runs a one-shot shell command inside a Sprite for studio journaling and streams the stdout back so the user watches the work happen live.\nDiscipline: Visual Art (studio journaling).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: Realtime STT allows painters to journal continuously without washing paint off their hands.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `studio-journaling-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a studio journaling-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for studio journaling.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global art supply and studio software market",
      "sam": "$500M — professional painter productivity tools",
      "som": "$15M — indie painter journaling apps"
    },
    {
      "id": "visual-art-prompt-alchemy-dictation-1",
      "theme": "visual-art",
      "title": "Prompt Alchemy Dictation",
      "pitch": "Prompt Alchemy Dictation runs a one-shot shell command inside a Sprite for generative art prompting and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "generative art prompting",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "Realtime STT lets artists speak complex visual concepts much faster than typing them.",
      "megaPrompt": "Build \"Prompt Alchemy Dictation\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPrompt Alchemy Dictation runs a one-shot shell command inside a Sprite for generative art prompting and streams the stdout back so the user watches the work happen live.\nDiscipline: Visual Art (generative art prompting).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: Realtime STT lets artists speak complex visual concepts much faster than typing them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `generative-art-prompting-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a generative art prompting-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for generative art prompting.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global generative AI software market",
      "sam": "$800M — AI image generation tools",
      "som": "$25M — prompt engineering utilities"
    },
    {
      "id": "visual-art-placard-prose-weaver-2",
      "theme": "visual-art",
      "title": "Placard Prose Weaver",
      "pitch": "Placard Prose Weaver runs a one-shot shell command inside a Sprite for gallery curation and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "gallery curation",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "Realtime STT captures the gallerist's authentic, off-the-cuff passion for immediate text refinement.",
      "megaPrompt": "Build \"Placard Prose Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPlacard Prose Weaver runs a one-shot shell command inside a Sprite for gallery curation and streams the stdout back so the user watches the work happen live.\nDiscipline: Visual Art (gallery curation).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: Realtime STT captures the gallerist's authentic, off-the-cuff passion for immediate text refinement.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `gallery-curation-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a gallery curation-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for gallery curation.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global fine art market",
      "sam": "$4B — gallery management and exhibition software",
      "som": "$50M — exhibition catalog publishing tools"
    },
    {
      "id": "visual-art-studio-critique-catcher-3",
      "theme": "visual-art",
      "title": "Studio Critique Catcher",
      "pitch": "Studio Critique Catcher runs a one-shot shell command inside a Sprite for art education and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "art education",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "Realtime STT allows teachers to maintain eye contact with students while dictating feedback.",
      "megaPrompt": "Build \"Studio Critique Catcher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nStudio Critique Catcher runs a one-shot shell command inside a Sprite for art education and streams the stdout back so the user watches the work happen live.\nDiscipline: Visual Art (art education).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: Realtime STT allows teachers to maintain eye contact with students while dictating feedback.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `art-education-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a art education-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for art education.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — global art education and edtech market",
      "sam": "$2B — higher education art department software",
      "som": "$40M — studio critique management platforms"
    },
    {
      "id": "visual-art-pigment-ratio-scribe-4",
      "theme": "visual-art",
      "title": "Pigment Ratio Live Notes",
      "pitch": "Pigment Ratio Live Notes runs a one-shot shell command inside a Sprite for color mixing and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "color mixing",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "Realtime STT is essential because painters cannot touch keyboards while mixing oils.",
      "megaPrompt": "Build \"Pigment Ratio Live Notes\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPigment Ratio Live Notes runs a one-shot shell command inside a Sprite for color mixing and streams the stdout back so the user watches the work happen live.\nDiscipline: Visual Art (color mixing).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: Realtime STT is essential because painters cannot touch keyboards while mixing oils.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `color-mixing-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a color mixing-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for color mixing.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$12B — global paint and coating manufacturing market",
      "sam": "$1B — professional artist paint and accessory sales",
      "som": "$20M — color theory and mixing software"
    },
    {
      "id": "visual-art-brief-extraction-whisper-5",
      "theme": "visual-art",
      "title": "Brief Extraction Whisper",
      "pitch": "Brief Extraction Whisper runs a one-shot shell command inside a Sprite for illustration commissions and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "illustration commissions",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "Realtime STT accurately logs nuanced client requests during fast-paced verbal negotiations.",
      "megaPrompt": "Build \"Brief Extraction Whisper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nBrief Extraction Whisper runs a one-shot shell command inside a Sprite for illustration commissions and streams the stdout back so the user watches the work happen live.\nDiscipline: Visual Art (illustration commissions).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: Realtime STT accurately logs nuanced client requests during fast-paced verbal negotiations.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `illustration-commissions-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a illustration commissions-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for illustration commissions.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — global freelance and gig economy platforms",
      "sam": "$1.5B — commercial illustration and design market",
      "som": "$30M — freelance illustrator CRM tools"
    },
    {
      "id": "visual-art-plein-air-scene-scribe-6",
      "theme": "visual-art",
      "title": "Plein Air Scene Live Notes",
      "pitch": "Plein Air Scene Live Notes runs a one-shot shell command inside a Sprite for landscape painting and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "landscape painting",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "Realtime STT lets artists dictate shifting light conditions without looking away from the canvas.",
      "megaPrompt": "Build \"Plein Air Scene Live Notes\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPlein Air Scene Live Notes runs a one-shot shell command inside a Sprite for landscape painting and streams the stdout back so the user watches the work happen live.\nDiscipline: Visual Art (landscape painting).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: Realtime STT lets artists dictate shifting light conditions without looking away from the canvas.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `landscape-painting-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a landscape painting-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for landscape painting.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — global outdoor recreation and plein air market",
      "sam": "$500M — landscape art supplies and workshops",
      "som": "$10M — plein air painting companion apps"
    },
    {
      "id": "visual-art-armature-blueprint-dictation-7",
      "theme": "visual-art",
      "title": "Armature Blueprint Dictation",
      "pitch": "Armature Blueprint Dictation runs a one-shot shell command inside a Sprite for sculpture planning and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "sculpture planning",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "Realtime STT allows sculptors to call out dimensions while physically manipulating heavy materials.",
      "megaPrompt": "Build \"Armature Blueprint Dictation\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nArmature Blueprint Dictation runs a one-shot shell command inside a Sprite for sculpture planning and streams the stdout back so the user watches the work happen live.\nDiscipline: Visual Art (sculpture planning).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: Realtime STT allows sculptors to call out dimensions while physically manipulating heavy materials.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `sculpture-planning-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a sculpture planning-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for sculpture planning.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — global sculpture and public art market",
      "sam": "$600M — sculpting tools and fabrication services",
      "som": "$15M — 3D fabrication and sculpting planning software"
    },
    {
      "id": "visual-art-booth-pitch-rehearsal-8",
      "theme": "visual-art",
      "title": "Booth Pitch Rehearsal",
      "pitch": "Booth Pitch Rehearsal runs a one-shot shell command inside a Sprite for art fair sales and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "art fair sales",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "Realtime STT captures the exact cadence and pauses of a spoken sales pitch.",
      "megaPrompt": "Build \"Booth Pitch Rehearsal\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nBooth Pitch Rehearsal runs a one-shot shell command inside a Sprite for art fair sales and streams the stdout back so the user watches the work happen live.\nDiscipline: Visual Art (art fair sales).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: Realtime STT captures the exact cadence and pauses of a spoken sales pitch.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `art-fair-sales-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a art fair sales-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for art fair sales.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global art fair and exhibition market",
      "sam": "$400M — art fair booth management and sales",
      "som": "$12M — artist sales coaching and pitch tools"
    },
    {
      "id": "visual-art-canvas-inventory-scribe-9",
      "theme": "visual-art",
      "title": "Canvas Inventory Live Notes",
      "pitch": "Canvas Inventory Live Notes runs a one-shot shell command inside a Sprite for studio management and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "studio management",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "Realtime STT enables hands-free logging of physical artwork dimensions and framing supplies.",
      "megaPrompt": "Build \"Canvas Inventory Live Notes\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCanvas Inventory Live Notes runs a one-shot shell command inside a Sprite for studio management and streams the stdout back so the user watches the work happen live.\nDiscipline: Visual Art (studio management).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: Realtime STT enables hands-free logging of physical artwork dimensions and framing supplies.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `studio-management-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a studio management-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for studio management.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — global art inventory and collection management market",
      "sam": "$800M — working artist studio management software",
      "som": "$25M — independent artist inventory trackers"
    },
    {
      "id": "visual-art-mural-wall-mapper-10",
      "theme": "visual-art",
      "title": "Mural Wall Mapper",
      "pitch": "Mural Wall Mapper runs a one-shot shell command inside a Sprite for public art prep and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "public art prep",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "Realtime STT lets artists dictate structural flaws while climbing scaffolding or lifts.",
      "megaPrompt": "Build \"Mural Wall Mapper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nMural Wall Mapper runs a one-shot shell command inside a Sprite for public art prep and streams the stdout back so the user watches the work happen live.\nDiscipline: Visual Art (public art prep).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: Realtime STT lets artists dictate structural flaws while climbing scaffolding or lifts.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `public-art-prep-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a public art prep-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for public art prep.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global mural and street art commission market",
      "sam": "$300M — public art project management software",
      "som": "$8M — muralist site prep and logistics tools"
    },
    {
      "id": "visual-art-proofing-state-logger-11",
      "theme": "visual-art",
      "title": "Proofing State Logger",
      "pitch": "Proofing State Logger runs a one-shot shell command inside a Sprite for printmaking and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "printmaking",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "Realtime STT allows printmakers to dictate ink viscosity changes while operating heavy presses.",
      "megaPrompt": "Build \"Proofing State Logger\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nProofing State Logger runs a one-shot shell command inside a Sprite for printmaking and streams the stdout back so the user watches the work happen live.\nDiscipline: Visual Art (printmaking).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: Realtime STT allows printmakers to dictate ink viscosity changes while operating heavy presses.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `printmaking-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a printmaking-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for printmaking.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$900M — global fine art printmaking market",
      "sam": "$200M — printmaking studio equipment and software",
      "som": "$5M — edition tracking and print state logs"
    },
    {
      "id": "visual-art-wash-layer-timeline-12",
      "theme": "visual-art",
      "title": "Wash Layer Timeline",
      "pitch": "Wash Layer Timeline runs a one-shot shell command inside a Sprite for watercolor technique and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "watercolor technique",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "Realtime STT captures immediate visual feedback on water absorption without interrupting the brushstroke.",
      "megaPrompt": "Build \"Wash Layer Timeline\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nWash Layer Timeline runs a one-shot shell command inside a Sprite for watercolor technique and streams the stdout back so the user watches the work happen live.\nDiscipline: Visual Art (watercolor technique).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: Realtime STT captures immediate visual feedback on water absorption without interrupting the brushstroke.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `watercolor-technique-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a watercolor technique-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for watercolor technique.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.2B — global watercolor and paper art market",
      "sam": "$300M — watercolor instruction and technique software",
      "som": "$7M — watercolor drying and timing aids"
    },
    {
      "id": "visual-art-exhibition-thread-weaver-13",
      "theme": "visual-art",
      "title": "Exhibition Thread Weaver",
      "pitch": "Exhibition Thread Weaver runs a one-shot shell command inside a Sprite for curatorial writing and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "curatorial writing",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "Realtime STT captures the rapid, associative thinking required to connect disparate visual artworks.",
      "megaPrompt": "Build \"Exhibition Thread Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nExhibition Thread Weaver runs a one-shot shell command inside a Sprite for curatorial writing and streams the stdout back so the user watches the work happen live.\nDiscipline: Visual Art (curatorial writing).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: Realtime STT captures the rapid, associative thinking required to connect disparate visual artworks.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `curatorial-writing-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a curatorial writing-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for curatorial writing.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global museum and curatorial services market",
      "sam": "$1B — exhibition design and catalog publishing",
      "som": "$30M — curatorial brainstorming and essay drafting tools"
    },
    {
      "id": "visual-art-gesture-anatomy-scribe-14",
      "theme": "visual-art",
      "title": "Gesture Anatomy Live Notes",
      "pitch": "Gesture Anatomy Live Notes runs a one-shot shell command inside a Sprite for figure drawing and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "figure drawing",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "Realtime STT lets artists dictate muscle tensions during fleeting, short-duration live model poses.",
      "megaPrompt": "Build \"Gesture Anatomy Live Notes\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nGesture Anatomy Live Notes runs a one-shot shell command inside a Sprite for figure drawing and streams the stdout back so the user watches the work happen live.\nDiscipline: Visual Art (figure drawing).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: Realtime STT lets artists dictate muscle tensions during fleeting, short-duration live model poses.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `figure-drawing-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a figure drawing-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for figure drawing.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global life drawing and figure art market",
      "sam": "$400M — anatomy instruction and reference software",
      "som": "$10M — live figure drawing study aids"
    },
    {
      "id": "visual-art-kiln-schedule-dictation-15",
      "theme": "visual-art",
      "title": "Kiln Schedule Dictation",
      "pitch": "Kiln Schedule Dictation runs a one-shot shell command inside a Sprite for ceramic glazing and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "ceramic glazing",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "Realtime STT allows potters to dictate chemical ratios while their hands are covered in clay.",
      "megaPrompt": "Build \"Kiln Schedule Dictation\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nKiln Schedule Dictation runs a one-shot shell command inside a Sprite for ceramic glazing and streams the stdout back so the user watches the work happen live.\nDiscipline: Visual Art (ceramic glazing).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: Realtime STT allows potters to dictate chemical ratios while their hands are covered in clay.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `ceramic-glazing-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a ceramic glazing-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for ceramic glazing.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — global ceramics and pottery equipment market",
      "sam": "$500M — ceramic studio management and glaze software",
      "som": "$12M — independent potter firing schedule tools"
    },
    {
      "id": "visual-art-blockage-breaker-coach-16",
      "theme": "visual-art",
      "title": "Blockage Breaker Coach",
      "pitch": "Blockage Breaker Coach runs a one-shot shell command inside a Sprite for artist mindset and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "artist mindset",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "Realtime STT captures the raw, emotional venting that precedes a creative breakthrough.",
      "megaPrompt": "Build \"Blockage Breaker Coach\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nBlockage Breaker Coach runs a one-shot shell command inside a Sprite for artist mindset and streams the stdout back so the user watches the work happen live.\nDiscipline: Visual Art (artist mindset).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: Realtime STT captures the raw, emotional venting that precedes a creative breakthrough.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `artist-mindset-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a artist mindset-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for artist mindset.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global creative therapy and coaching market",
      "sam": "$300M — artist wellness and mindset programs",
      "som": "$8M — creative block journaling and coaching apps"
    },
    {
      "id": "visual-art-grant-vision-drafter-17",
      "theme": "visual-art",
      "title": "Grant Vision Drafter",
      "pitch": "Grant Vision Drafter runs a one-shot shell command inside a Sprite for arts funding and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "arts funding",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "Realtime STT lets artists passionately articulate their vision without being constrained by formal typing.",
      "megaPrompt": "Build \"Grant Vision Drafter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nGrant Vision Drafter runs a one-shot shell command inside a Sprite for arts funding and streams the stdout back so the user watches the work happen live.\nDiscipline: Visual Art (arts funding).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: Realtime STT lets artists passionately articulate their vision without being constrained by formal typing.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `arts-funding-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a arts funding-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for arts funding.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — global arts grant and foundation funding market",
      "sam": "$800M — grant writing and arts administration software",
      "som": "$20M — independent artist grant application tools"
    },
    {
      "id": "visual-art-residency-interview-prep-18",
      "theme": "visual-art",
      "title": "Residency Interview Prep",
      "pitch": "Residency Interview Prep runs a one-shot shell command inside a Sprite for career development and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "career development",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "Realtime STT captures the natural hesitations and verbal tics that need refining for interviews.",
      "megaPrompt": "Build \"Residency Interview Prep\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nResidency Interview Prep runs a one-shot shell command inside a Sprite for career development and streams the stdout back so the user watches the work happen live.\nDiscipline: Visual Art (career development).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: Realtime STT captures the natural hesitations and verbal tics that need refining for interviews.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `career-development-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a career development-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for career development.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$500M — global artist residency and fellowship market",
      "sam": "$100M — arts career development and coaching",
      "som": "$3M — residency interview rehearsal platforms"
    },
    {
      "id": "visual-art-slide-lecture-catcher-19",
      "theme": "visual-art",
      "title": "Slide Lecture Catcher",
      "pitch": "Slide Lecture Catcher runs a one-shot shell command inside a Sprite for art history teaching and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "art history teaching",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "Realtime STT captures the unscripted, passionate tangents that make art history lectures engaging.",
      "megaPrompt": "Build \"Slide Lecture Catcher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSlide Lecture Catcher runs a one-shot shell command inside a Sprite for art history teaching and streams the stdout back so the user watches the work happen live.\nDiscipline: Visual Art (art history teaching).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: Realtime STT captures the unscripted, passionate tangents that make art history lectures engaging.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `art-history-teaching-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a art history teaching-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for art history teaching.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — global higher education humanities market",
      "sam": "$1.5B — university art history department software",
      "som": "$35M — lecture capture and study guide generators"
    },
    {
      "id": "visual-art-value-proposition-scribe-20",
      "theme": "visual-art",
      "title": "Value Proposition Live Notes",
      "pitch": "Value Proposition Live Notes runs a one-shot shell command inside a Sprite for art pricing and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "art pricing",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "Realtime STT helps artists verbalize their self-worth and pricing logic before sending formal quotes.",
      "megaPrompt": "Build \"Value Proposition Live Notes\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nValue Proposition Live Notes runs a one-shot shell command inside a Sprite for art pricing and streams the stdout back so the user watches the work happen live.\nDiscipline: Visual Art (art pricing).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: Realtime STT helps artists verbalize their self-worth and pricing logic before sending formal quotes.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `art-pricing-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a art pricing-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for art pricing.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$6B — global art advisory and private sales market",
      "sam": "$1.2B — independent artist sales and pricing tools",
      "som": "$25M — art commission negotiation and invoicing apps"
    },
    {
      "id": "visual-art-storyboard-arc-dictation-21",
      "theme": "visual-art",
      "title": "Storyboard Arc Dictation",
      "pitch": "Storyboard Arc Dictation runs a one-shot shell command inside a Sprite for graphic narrative and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "graphic narrative",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "Realtime STT allows illustrators to act out dialogue and pacing while sketching rough panels.",
      "megaPrompt": "Build \"Storyboard Arc Dictation\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nStoryboard Arc Dictation runs a one-shot shell command inside a Sprite for graphic narrative and streams the stdout back so the user watches the work happen live.\nDiscipline: Visual Art (graphic narrative).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: Realtime STT allows illustrators to act out dialogue and pacing while sketching rough panels.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `graphic-narrative-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a graphic narrative-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for graphic narrative.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — global comic and graphic novel publishing market",
      "sam": "$600M — digital illustration and storyboarding software",
      "som": "$15M — graphic novel plotting and pacing tools"
    },
    {
      "id": "visual-art-rigging-safety-scribe-22",
      "theme": "visual-art",
      "title": "Rigging Safety Live Notes",
      "pitch": "Rigging Safety Live Notes runs a one-shot shell command inside a Sprite for installation art and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "installation art",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "Realtime STT lets installation artists dictate safety protocols while physically inspecting heavy rigging.",
      "megaPrompt": "Build \"Rigging Safety Live Notes\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nRigging Safety Live Notes runs a one-shot shell command inside a Sprite for installation art and streams the stdout back so the user watches the work happen live.\nDiscipline: Visual Art (installation art).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: Realtime STT lets installation artists dictate safety protocols while physically inspecting heavy rigging.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `installation-art-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a installation art-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for installation art.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — global large-scale installation and public art market",
      "sam": "$200M — art logistics and installation management",
      "som": "$5M — installation safety and compliance checklists"
    },
    {
      "id": "visual-art-crate-handler-guide-23",
      "theme": "visual-art",
      "title": "Crate Handler Guide",
      "pitch": "Crate Handler Guide runs a one-shot shell command inside a Sprite for art shipping and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "art shipping",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "Realtime STT allows artists to dictate fragile points while actively building custom shipping crates.",
      "megaPrompt": "Build \"Crate Handler Guide\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCrate Handler Guide runs a one-shot shell command inside a Sprite for art shipping and streams the stdout back so the user watches the work happen live.\nDiscipline: Visual Art (art shipping).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: Realtime STT allows artists to dictate fragile points while actively building custom shipping crates.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `art-shipping-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a art shipping-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for art shipping.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global fine art logistics and shipping market",
      "sam": "$400M — art packaging and crating services",
      "som": "$10M — independent artist shipping instruction generators"
    },
    {
      "id": "visual-art-portfolio-thread-weaver-24",
      "theme": "visual-art",
      "title": "Portfolio Thread Weaver",
      "pitch": "Portfolio Thread Weaver runs a one-shot shell command inside a Sprite for portfolio review and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "portfolio review",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "Realtime STT captures the overarching conceptual story an artist tells when reviewing their body of work.",
      "megaPrompt": "Build \"Portfolio Thread Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPortfolio Thread Weaver runs a one-shot shell command inside a Sprite for portfolio review and streams the stdout back so the user watches the work happen live.\nDiscipline: Visual Art (portfolio review).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: Realtime STT captures the overarching conceptual story an artist tells when reviewing their body of work.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `portfolio-review-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a portfolio review-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for portfolio review.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global art portfolio and admissions market",
      "sam": "$300M — professional artist website and portfolio builders",
      "som": "$8M — portfolio narrative and review prep tools"
    },
    {
      "id": "visual-art-canvas-breeze-synth-0",
      "theme": "visual-art",
      "title": "Canvas Breeze Synth",
      "pitch": "Canvas Breeze Synth composes plein air painting in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "plein air painting",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Generative SFX creates immersive environmental audio matching the painted scenery.",
      "megaPrompt": "Build \"Canvas Breeze Synth\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCanvas Breeze Synth composes plein air painting in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Visual Art (plein air painting).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Generative SFX creates immersive environmental audio matching the painted scenery.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `plein-air-painting-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for plein air painting (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for plein air painting.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global outdoor art supplies",
      "sam": "$500M — professional plein air painters",
      "som": "$15M — digital companion apps for painters"
    },
    {
      "id": "visual-art-brushstroke-tempo-engine-1",
      "theme": "visual-art",
      "title": "Brushstroke Tempo Engine",
      "pitch": "Brushstroke Tempo Engine composes studio flow state in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "studio flow state",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Generative music adapts BPM to the artist's physical brushstroke rhythm.",
      "megaPrompt": "Build \"Brushstroke Tempo Engine\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nBrushstroke Tempo Engine composes studio flow state in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Visual Art (studio flow state).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Generative music adapts BPM to the artist's physical brushstroke rhythm.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `studio-flow-state-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for studio flow state (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for studio flow state.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global art studio equipment",
      "sam": "$1B — professional studio painters",
      "som": "$20M — focus and productivity audio tools"
    },
    {
      "id": "visual-art-chromatic-chord-mapper-2",
      "theme": "visual-art",
      "title": "Chromatic Chord Mapper",
      "pitch": "Chromatic Chord Mapper composes color theory study in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "color theory study",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "SFX generation maps specific color frequencies and temperatures to audio chords.",
      "megaPrompt": "Build \"Chromatic Chord Mapper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nChromatic Chord Mapper composes color theory study in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Visual Art (color theory study).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: SFX generation maps specific color frequencies and temperatures to audio chords.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `color-theory-study-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for color theory study (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for color theory study.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — art education software",
      "sam": "$200M — color theory students",
      "som": "$5M — synesthesia learning tools"
    },
    {
      "id": "visual-art-white-cube-resonator-3",
      "theme": "visual-art",
      "title": "White Cube Resonator",
      "pitch": "White Cube Resonator composes gallery exhibition curation in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "gallery exhibition curation",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Studio track generation provides room-filling ambient soundscapes for physical art spaces.",
      "megaPrompt": "Build \"White Cube Resonator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nWhite Cube Resonator composes gallery exhibition curation in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Visual Art (gallery exhibition curation).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Studio track generation provides room-filling ambient soundscapes for physical art spaces.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `gallery-exhibition-curat-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for gallery exhibition curation (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for gallery exhibition curation.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — global art gallery operations",
      "sam": "$2B — contemporary exhibition spaces",
      "som": "$30M — gallery sensory enhancement tech"
    },
    {
      "id": "visual-art-splatter-impact-foley-4",
      "theme": "visual-art",
      "title": "Splatter Impact Foley",
      "pitch": "Splatter Impact Foley composes abstract expressionism in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "abstract expressionism",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Short SFX generation provides percussive feedback for aggressive, physical paint application.",
      "megaPrompt": "Build \"Splatter Impact Foley\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSplatter Impact Foley composes abstract expressionism in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Visual Art (abstract expressionism).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Short SFX generation provides percussive feedback for aggressive, physical paint application.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `abstract-expressionism-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for abstract expressionism (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for abstract expressionism.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — modern art materials",
      "sam": "$500M — abstract expressionist painters",
      "som": "$10M — interactive studio feedback tools"
    },
    {
      "id": "visual-art-kiln-rhythm-spinner-5",
      "theme": "visual-art",
      "title": "Kiln Rhythm Spinner",
      "pitch": "Kiln Rhythm Spinner composes ceramic sculpting in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "ceramic sculpting",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Generative music creates hypnotic, looping beats synchronized to pottery wheel rotation.",
      "megaPrompt": "Build \"Kiln Rhythm Spinner\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nKiln Rhythm Spinner composes ceramic sculpting in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Visual Art (ceramic sculpting).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Generative music creates hypnotic, looping beats synchronized to pottery wheel rotation.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `ceramic-sculpting-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for ceramic sculpting (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for ceramic sculpting.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — ceramics and sculpting supplies",
      "sam": "$800M — professional ceramicists",
      "som": "$12M — studio pacing and rhythm aids"
    },
    {
      "id": "visual-art-layered-canvas-crescendo-6",
      "theme": "visual-art",
      "title": "Layered Canvas Crescendo",
      "pitch": "Layered Canvas Crescendo composes digital illustration in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "digital illustration",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Studio music generation builds dynamic soundtracks that evolve with digital drawing complexity.",
      "megaPrompt": "Build \"Layered Canvas Crescendo\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLayered Canvas Crescendo composes digital illustration in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Visual Art (digital illustration).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Studio music generation builds dynamic soundtracks that evolve with digital drawing complexity.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `digital-illustration-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for digital illustration (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for digital illustration.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — digital art software market",
      "sam": "$2B — professional digital illustrators",
      "som": "$40M — timelapse and portfolio video tools"
    },
    {
      "id": "visual-art-critique-tension-resolver-7",
      "theme": "visual-art",
      "title": "Critique Tension Resolver",
      "pitch": "Critique Tension Resolver composes portfolio review in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "portfolio review",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Music generation uses tension and resolution to emotionally frame the critique process.",
      "megaPrompt": "Build \"Critique Tension Resolver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCritique Tension Resolver composes portfolio review in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Visual Art (portfolio review).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Music generation uses tension and resolution to emotionally frame the critique process.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `portfolio-review-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for portfolio review (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for portfolio review.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — art school and mentoring",
      "sam": "$400M — emerging visual artists",
      "som": "$8M — AI portfolio coaching platforms"
    },
    {
      "id": "visual-art-prompt-harmony-checker-8",
      "theme": "visual-art",
      "title": "Prompt Harmony Checker",
      "pitch": "Prompt Harmony Checker composes generative AI art in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "generative AI art",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "SFX provides immediate, non-verbal audio cues indicating prompt balance and complexity.",
      "megaPrompt": "Build \"Prompt Harmony Checker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPrompt Harmony Checker composes generative AI art in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Visual Art (generative AI art).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: SFX provides immediate, non-verbal audio cues indicating prompt balance and complexity.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `generative-ai-art-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for generative AI art (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for generative AI art.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — generative AI software",
      "sam": "$1B — AI visual artists",
      "som": "$25M — prompt engineering helper tools"
    },
    {
      "id": "visual-art-figure-pose-metronome-9",
      "theme": "visual-art",
      "title": "Figure Pose Metronome",
      "pitch": "Figure Pose Metronome composes life drawing in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "life drawing",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Ambient music generation provides subtle, shifting audio cues for timed drawing sessions.",
      "megaPrompt": "Build \"Figure Pose Metronome\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFigure Pose Metronome composes life drawing in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Visual Art (life drawing).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Ambient music generation provides subtle, shifting audio cues for timed drawing sessions.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `life-drawing-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for life drawing (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for life drawing.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — life drawing studios",
      "sam": "$200M — figure drawing students",
      "som": "$4M — timed session management apps"
    },
    {
      "id": "visual-art-acid-bath-industrialist-10",
      "theme": "visual-art",
      "title": "Acid Bath Industrialist",
      "pitch": "Acid Bath Industrialist composes etching and printmaking in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "etching and printmaking",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "SFX generation creates rhythmic, industrial audio matching the physical printmaking process.",
      "megaPrompt": "Build \"Acid Bath Industrialist\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAcid Bath Industrialist composes etching and printmaking in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Visual Art (etching and printmaking).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: SFX generation creates rhythmic, industrial audio matching the physical printmaking process.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `etching-and-printmaking-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for etching and printmaking (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for etching and printmaking.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$500M — printmaking equipment",
      "sam": "$100M — professional printmakers",
      "som": "$2M — traditional craft workflow aids"
    },
    {
      "id": "visual-art-wet-wash-fluidity-11",
      "theme": "visual-art",
      "title": "Wet Wash Fluidity",
      "pitch": "Wet Wash Fluidity composes watercolor painting in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "watercolor painting",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Generative music uses fluid, harp-like tones to guide wet-on-wet timing.",
      "megaPrompt": "Build \"Wet Wash Fluidity\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nWet Wash Fluidity composes watercolor painting in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Visual Art (watercolor painting).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Generative music uses fluid, harp-like tones to guide wet-on-wet timing.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `watercolor-painting-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for watercolor painting (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for watercolor painting.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — watercolor supplies",
      "sam": "$300M — watercolor specialists",
      "som": "$6M — medium-specific painting guides"
    },
    {
      "id": "visual-art-epoch-echo-immersion-12",
      "theme": "visual-art",
      "title": "Epoch Echo Immersion",
      "pitch": "Epoch Echo Immersion composes art history research in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "art history research",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Studio track generation produces era-accurate ambient music for historical immersion.",
      "megaPrompt": "Build \"Epoch Echo Immersion\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nEpoch Echo Immersion composes art history research in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Visual Art (art history research).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Studio track generation produces era-accurate ambient music for historical immersion.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `art-history-research-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for art history research (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for art history research.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — academic art history",
      "sam": "$500M — historical painters and researchers",
      "som": "$10M — immersive research companions"
    },
    {
      "id": "visual-art-stretcher-bar-asmr-13",
      "theme": "visual-art",
      "title": "Stretcher Bar ASMR",
      "pitch": "Stretcher Bar ASMR composes canvas preparation in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "canvas preparation",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "SFX generation provides satisfying, tactile audio feedback for physical studio labor.",
      "megaPrompt": "Build \"Stretcher Bar ASMR\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nStretcher Bar ASMR composes canvas preparation in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Visual Art (canvas preparation).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: SFX generation provides satisfying, tactile audio feedback for physical studio labor.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `canvas-preparation-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for canvas preparation (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for canvas preparation.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$800M — canvas and framing supplies",
      "sam": "$150M — painters who prep their own canvases",
      "som": "$3M — studio setup and prep guides"
    },
    {
      "id": "visual-art-scaffolding-epic-scorer-14",
      "theme": "visual-art",
      "title": "Scaffolding Epic Scorer",
      "pitch": "Scaffolding Epic Scorer composes mural painting in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "mural painting",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Cinematic music generation provides motivating, large-scale audio for monumental physical work.",
      "megaPrompt": "Build \"Scaffolding Epic Scorer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nScaffolding Epic Scorer composes mural painting in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Visual Art (mural painting).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Cinematic music generation provides motivating, large-scale audio for monumental physical work.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `mural-painting-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for mural painting (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for mural painting.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — public art and murals",
      "sam": "$200M — professional muralists",
      "som": "$5M — large-scale project management tools"
    },
    {
      "id": "visual-art-pigment-ratio-tuner-15",
      "theme": "visual-art",
      "title": "Pigment Ratio Tuner",
      "pitch": "Pigment Ratio Tuner composes palette mixing in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "palette mixing",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Generative SFX uses resolving audio chords to signal when color ratios are balanced.",
      "megaPrompt": "Build \"Pigment Ratio Tuner\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPigment Ratio Tuner composes palette mixing in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Visual Art (palette mixing).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Generative SFX uses resolving audio chords to signal when color ratios are balanced.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `palette-mixing-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for palette mixing (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for palette mixing.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — artist paints and pigments",
      "sam": "$400M — traditional painters",
      "som": "$8M — color mixing utility apps"
    },
    {
      "id": "visual-art-blank-canvas-unwinder-16",
      "theme": "visual-art",
      "title": "Blank Canvas Unwinder",
      "pitch": "Blank Canvas Unwinder composes creative mindset in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "creative mindset",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Meditative music generation creates calming soundscapes derived from journaling prompts.",
      "megaPrompt": "Build \"Blank Canvas Unwinder\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nBlank Canvas Unwinder composes creative mindset in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Visual Art (creative mindset).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Meditative music generation creates calming soundscapes derived from journaling prompts.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `creative-mindset-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for creative mindset (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for creative mindset.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — mental wellness and coaching",
      "sam": "$800M — creative professionals",
      "som": "$15M — mindfulness tools for artists"
    },
    {
      "id": "visual-art-graphite-texture-scraper-17",
      "theme": "visual-art",
      "title": "Graphite Texture Scraper",
      "pitch": "Graphite Texture Scraper composes charcoal drawing in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "charcoal drawing",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "SFX generation produces scratchy, textured audio responding to shading pressure and density.",
      "megaPrompt": "Build \"Graphite Texture Scraper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nGraphite Texture Scraper composes charcoal drawing in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Visual Art (charcoal drawing).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: SFX generation produces scratchy, textured audio responding to shading pressure and density.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `charcoal-drawing-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for charcoal drawing (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for charcoal drawing.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$600M — drawing and sketching supplies",
      "sam": "$120M — charcoal and graphite artists",
      "som": "$2.5M — drawing technique feedback tools"
    },
    {
      "id": "visual-art-vernissage-vibe-weaver-18",
      "theme": "visual-art",
      "title": "Vernissage Vibe Weaver",
      "pitch": "Vernissage Vibe Weaver composes gallery opening events in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "gallery opening events",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Studio track generation creates custom, sophisticated lobby music for exhibition openings.",
      "megaPrompt": "Build \"Vernissage Vibe Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nVernissage Vibe Weaver composes gallery opening events in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Visual Art (gallery opening events).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Studio track generation creates custom, sophisticated lobby music for exhibition openings.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `gallery-opening-events-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for gallery opening events (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for gallery opening events.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — event and hospitality management",
      "sam": "$1B — art gallery events",
      "som": "$20M — bespoke event atmosphere tools"
    },
    {
      "id": "visual-art-micro-detail-focus-19",
      "theme": "visual-art",
      "title": "Micro Detail Focus",
      "pitch": "Micro Detail Focus composes art conservation in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "art conservation",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Delicate ambient music generation maintains deep, unbroken focus during microscopic work.",
      "megaPrompt": "Build \"Micro Detail Focus\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nMicro Detail Focus composes art conservation in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Visual Art (art conservation).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Delicate ambient music generation maintains deep, unbroken focus during microscopic work.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `art-conservation-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for art conservation (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for art conservation.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — art conservation and restoration",
      "sam": "$400M — professional conservators",
      "som": "$8M — precision focus and analysis tools"
    },
    {
      "id": "visual-art-worldbuilding-ambience-forge-20",
      "theme": "visual-art",
      "title": "Worldbuilding Ambience Forge",
      "pitch": "Worldbuilding Ambience Forge composes concept art in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "concept art",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Environmental SFX generation builds the auditory world before the artist draws it.",
      "megaPrompt": "Build \"Worldbuilding Ambience Forge\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nWorldbuilding Ambience Forge composes concept art in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Visual Art (concept art).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Environmental SFX generation builds the auditory world before the artist draws it.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `concept-art-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for concept art (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for concept art.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — game and film pre-production",
      "sam": "$2B — concept artists",
      "som": "$40M — worldbuilding and ideation software"
    },
    {
      "id": "visual-art-kerning-cadence-tracker-21",
      "theme": "visual-art",
      "title": "Kerning Cadence Tracker",
      "pitch": "Kerning Cadence Tracker composes hand lettering in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "hand lettering",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Rhythmic music generation matches the physical cadence and flow of pen strokes.",
      "megaPrompt": "Build \"Kerning Cadence Tracker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nKerning Cadence Tracker composes hand lettering in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Visual Art (hand lettering).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Rhythmic music generation matches the physical cadence and flow of pen strokes.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `hand-lettering-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for hand lettering (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for hand lettering.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — typography and calligraphy",
      "sam": "$300M — hand lettering artists",
      "som": "$6M — lettering practice and pacing aids"
    },
    {
      "id": "visual-art-mixed-media-organizer-22",
      "theme": "visual-art",
      "title": "Mixed Media Organizer",
      "pitch": "Mixed Media Organizer composes collage art in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "collage art",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Layered SFX generation starts chaotic and organizes into harmony as the composition balances.",
      "megaPrompt": "Build \"Mixed Media Organizer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nMixed Media Organizer composes collage art in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Visual Art (collage art).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Layered SFX generation starts chaotic and organizes into harmony as the composition balances.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `collage-art-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for collage art (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for collage art.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$800M — mixed media and craft supplies",
      "sam": "$150M — collage and assemblage artists",
      "som": "$3M — compositional layout tools"
    },
    {
      "id": "visual-art-mortar-pestle-muller-23",
      "theme": "visual-art",
      "title": "Mortar Pestle Muller",
      "pitch": "Mortar Pestle Muller composes paint making in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "paint making",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Raw, earthy SFX generation complements the tactile, physical process of milling pigments.",
      "megaPrompt": "Build \"Mortar Pestle Muller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nMortar Pestle Muller composes paint making in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Visual Art (paint making).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Raw, earthy SFX generation complements the tactile, physical process of milling pigments.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `paint-making-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for paint making (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for paint making.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$300M — raw art pigments and binders",
      "sam": "$50M — bespoke paint makers",
      "som": "$1M — traditional material crafting aids"
    },
    {
      "id": "visual-art-final-coat-triumph-24",
      "theme": "visual-art",
      "title": "Final Coat Triumph",
      "pitch": "Final Coat Triumph composes painting finishing in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "painting finishing",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Triumphant studio track generation provides a satisfying audio reward for completing a piece.",
      "megaPrompt": "Build \"Final Coat Triumph\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFinal Coat Triumph composes painting finishing in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Visual Art (painting finishing).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Triumphant studio track generation provides a satisfying audio reward for completing a piece.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `painting-finishing-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for painting finishing (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for painting finishing.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$500M — art finishing and framing supplies",
      "sam": "$100M — professional painters",
      "som": "$2M — studio workflow completion tools"
    }
  ]
}