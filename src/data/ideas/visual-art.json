{
  "theme": {
    "slug": "visual-art",
    "name": "Visual Art",
    "emoji": "🎨",
    "audience": "painters, illustrators, generative artists, gallerists",
    "market_anchor": "the global art market (~$65B; >300K working visual artists)"
  },
  "ideas": [
    {
      "id": "visual-art-canvas-calm-narrator-0",
      "theme": "visual-art",
      "title": "Canvas Calm Narrator",
      "pitch": "Lovable AI writes pre-painting meditation scripts so painters focus; ElevenLabs speaks them to ground your mind before touching the canvas.",
      "subDiscipline": "studio mindfulness",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Text-to-Speech provides a soothing, hands-free audio guide for artists whose hands are covered in paint or charcoal.",
      "megaPrompt": "Build \"Canvas Calm Narrator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI writes pre-painting meditation scripts so painters focus; ElevenLabs speaks them to ground your mind before touching the canvas.\nDiscipline: Visual Art (studio mindfulness).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Text-to-Speech provides a soothing, hands-free audio guide for artists whose hands are covered in paint or charcoal.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the studio mindfulness use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for studio mindfulness. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for studio mindfulness.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — global wellness and meditation app market",
      "sam": "$500M — creative professional mindfulness tools",
      "som": "$15M — visual artist studio wellness subscriptions"
    },
    {
      "id": "visual-art-palette-whisper-guide-1",
      "theme": "visual-art",
      "title": "Palette Whisper Guide",
      "pitch": "Lovable AI generates complex color mixing ratios so illustrators learn; ElevenLabs reads the recipes aloud while you mix oils.",
      "subDiscipline": "color theory study",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Hands-free Text-to-Speech allows painters to hear exact pigment drops without looking away from their physical palette.",
      "megaPrompt": "Build \"Palette Whisper Guide\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI generates complex color mixing ratios so illustrators learn; ElevenLabs reads the recipes aloud while you mix oils.\nDiscipline: Visual Art (color theory study).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Hands-free Text-to-Speech allows painters to hear exact pigment drops without looking away from their physical palette.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the color theory study use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for color theory study. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for color theory study.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$12B — global art supplies and education market",
      "sam": "$2B — digital and traditional color theory learning",
      "som": "$40M — professional painter masterclass subscriptions"
    },
    {
      "id": "visual-art-exhibition-echo-docent-2",
      "theme": "visual-art",
      "title": "Exhibition Echo Docent",
      "pitch": "Lovable AI drafts engaging artwork narratives so gallerists captivate; ElevenLabs speaks the tour to visitors through spatial room speakers.",
      "subDiscipline": "gallery audio tours",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "High-quality multilingual Text-to-Speech creates immersive, localized audio guides without hiring live docents for every exhibition.",
      "megaPrompt": "Build \"Exhibition Echo Docent\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI drafts engaging artwork narratives so gallerists captivate; ElevenLabs speaks the tour to visitors through spatial room speakers.\nDiscipline: Visual Art (gallery audio tours).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: High-quality multilingual Text-to-Speech creates immersive, localized audio guides without hiring live docents for every exhibition.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the gallery audio tours use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for gallery audio tours. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for gallery audio tours.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global commercial art gallery market",
      "sam": "$5B — gallery visitor experience and audio tech",
      "som": "$100M — independent gallery audio guide software"
    },
    {
      "id": "visual-art-easel-posture-prompter-3",
      "theme": "visual-art",
      "title": "Easel Posture Prompter",
      "pitch": "Lovable AI schedules physical stretch routines so sculptors avoid injury; ElevenLabs narrates the movements while you work at the easel.",
      "subDiscipline": "studio ergonomics",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Audio cues ensure artists remember to stretch and correct posture without breaking their visual flow state.",
      "megaPrompt": "Build \"Easel Posture Prompter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI schedules physical stretch routines so sculptors avoid injury; ElevenLabs narrates the movements while you work at the easel.\nDiscipline: Visual Art (studio ergonomics).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Audio cues ensure artists remember to stretch and correct posture without breaking their visual flow state.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the studio ergonomics use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for studio ergonomics. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for studio ergonomics.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — occupational health and ergonomics market",
      "sam": "$800M — freelance and studio worker health tools",
      "som": "$20M — physical artist wellness applications"
    },
    {
      "id": "visual-art-prompt-alchemy-broadcaster-4",
      "theme": "visual-art",
      "title": "Prompt Alchemy Broadcaster",
      "pitch": "Lovable AI brainstorms surreal image prompts so digital artists iterate; ElevenLabs speaks the variations to inspire your next visual generation.",
      "subDiscipline": "generative prompt engineering",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Hearing prompts spoken aloud via Text-to-Speech stimulates different cognitive pathways for generative artists seeking unexpected visual combinations.",
      "megaPrompt": "Build \"Prompt Alchemy Broadcaster\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI brainstorms surreal image prompts so digital artists iterate; ElevenLabs speaks the variations to inspire your next visual generation.\nDiscipline: Visual Art (generative prompt engineering).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Hearing prompts spoken aloud via Text-to-Speech stimulates different cognitive pathways for generative artists seeking unexpected visual combinations.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the generative prompt engineering use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for generative prompt engineering. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for generative prompt engineering.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — digital art and generative AI software",
      "sam": "$1.5B — prompt engineering and workflow tools",
      "som": "$30M — indie generative artist utility apps"
    },
    {
      "id": "visual-art-sketchbook-lore-keeper-5",
      "theme": "visual-art",
      "title": "Sketchbook Lore Keeper",
      "pitch": "Lovable AI contextualizes your daily sketches so illustrators reflect; ElevenLabs reads the historical parallels back as an audio diary.",
      "subDiscipline": "visual journaling",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Text-to-Speech turns quiet journaling into an engaging, personalized art history podcast for the creator's own reflection.",
      "megaPrompt": "Build \"Sketchbook Lore Keeper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI contextualizes your daily sketches so illustrators reflect; ElevenLabs reads the historical parallels back as an audio diary.\nDiscipline: Visual Art (visual journaling).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Text-to-Speech turns quiet journaling into an engaging, personalized art history podcast for the creator's own reflection.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the visual journaling use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for visual journaling. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for visual journaling.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — digital journaling and note-taking market",
      "sam": "$400M — creative professional reflection tools",
      "som": "$10M — visual art student and hobbyist apps"
    },
    {
      "id": "visual-art-composition-critique-chorus-6",
      "theme": "visual-art",
      "title": "Composition Critique Chorus",
      "pitch": "Lovable AI analyzes uploaded paintings so students improve; ElevenLabs speaks constructive feedback on focal points while you review the canvas.",
      "subDiscipline": "visual composition analysis",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Spoken critique mimics a traditional studio visit, allowing students to absorb feedback while physically stepping back from their work.",
      "megaPrompt": "Build \"Composition Critique Chorus\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI analyzes uploaded paintings so students improve; ElevenLabs speaks constructive feedback on focal points while you review the canvas.\nDiscipline: Visual Art (visual composition analysis).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Spoken critique mimics a traditional studio visit, allowing students to absorb feedback while physically stepping back from their work.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the visual composition analysis use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for visual composition analysis. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for visual composition analysis.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$15B — global art education and tutoring market",
      "sam": "$3B — digital art critique and portfolio review",
      "som": "$60M — art school and independent student tools"
    },
    {
      "id": "visual-art-lumens-rhythm-director-7",
      "theme": "visual-art",
      "title": "Lumens Rhythm Director",
      "pitch": "Lovable AI plans circadian lighting shifts so painters protect vision; ElevenLabs announces the transitions to adjust your studio smart bulbs.",
      "subDiscipline": "studio lighting management",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Voice announcements prepare the artist for lighting changes, preventing sudden visual shocks that disrupt color perception.",
      "megaPrompt": "Build \"Lumens Rhythm Director\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI plans circadian lighting shifts so painters protect vision; ElevenLabs announces the transitions to adjust your studio smart bulbs.\nDiscipline: Visual Art (studio lighting management).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Voice announcements prepare the artist for lighting changes, preventing sudden visual shocks that disrupt color perception.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the studio lighting management use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for studio lighting management. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for studio lighting management.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$25B — smart home and studio lighting market",
      "sam": "$2B — specialized creative workspace automation",
      "som": "$25M — professional painter studio setups"
    },
    {
      "id": "visual-art-pigment-scout-announcer-8",
      "theme": "visual-art",
      "title": "Pigment Scout Announcer",
      "pitch": "Lovable AI tracks rare earth pigment availability so oil painters prepare; ElevenLabs speaks the restock alerts while you clean brushes.",
      "subDiscipline": "art material sourcing",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Audio alerts keep artists informed about niche supply chains without forcing them to constantly check inventory websites.",
      "megaPrompt": "Build \"Pigment Scout Announcer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI tracks rare earth pigment availability so oil painters prepare; ElevenLabs speaks the restock alerts while you clean brushes.\nDiscipline: Visual Art (art material sourcing).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Audio alerts keep artists informed about niche supply chains without forcing them to constantly check inventory websites.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the art material sourcing use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for art material sourcing. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for art material sourcing.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$12B — global fine art materials market",
      "sam": "$1B — specialized pigment and canvas supply chain",
      "som": "$15M — professional oil painter procurement tools"
    },
    {
      "id": "visual-art-curation-flow-narrator-9",
      "theme": "visual-art",
      "title": "Curation Flow Narrator",
      "pitch": "Lovable AI sequences artwork placement so curators optimize flow; ElevenLabs reads the spatial walkthrough to visualize the gallery layout.",
      "subDiscipline": "exhibition spatial planning",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Hearing the physical walkthrough helps curators mentally simulate visitor movement through a space before installation begins.",
      "megaPrompt": "Build \"Curation Flow Narrator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI sequences artwork placement so curators optimize flow; ElevenLabs reads the spatial walkthrough to visualize the gallery layout.\nDiscipline: Visual Art (exhibition spatial planning).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Hearing the physical walkthrough helps curators mentally simulate visitor movement through a space before installation begins.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the exhibition spatial planning use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for exhibition spatial planning. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for exhibition spatial planning.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market and exhibition management",
      "sam": "$4B — gallery curation and spatial design software",
      "som": "$80M — independent curator and museum planning tools"
    },
    {
      "id": "visual-art-valuation-compass-voice-10",
      "theme": "visual-art",
      "title": "Valuation Compass Voice",
      "pitch": "Lovable AI calculates fair market prices so emerging artists sell; ElevenLabs speaks the pricing rationale to build your negotiation confidence.",
      "subDiscipline": "art market pricing",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Spoken rationale acts as a confidence coach, helping artists internalize their worth before discussing prices with collectors.",
      "megaPrompt": "Build \"Valuation Compass Voice\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI calculates fair market prices so emerging artists sell; ElevenLabs speaks the pricing rationale to build your negotiation confidence.\nDiscipline: Visual Art (art market pricing).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Spoken rationale acts as a confidence coach, helping artists internalize their worth before discussing prices with collectors.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the art market pricing use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for art market pricing. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for art market pricing.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global commercial art sales market",
      "sam": "$5B — art advisory and pricing analytics",
      "som": "$50M — emerging artist career management platforms"
    },
    {
      "id": "visual-art-acid-bath-timer-11",
      "theme": "visual-art",
      "title": "Acid Bath Timer",
      "pitch": "Lovable AI calculates precise etching durations so printmakers execute; ElevenLabs speaks the countdowns to coordinate multiple plates in the acid.",
      "subDiscipline": "printmaking etching",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Hands-free audio timing is critical when an artist's hands are covered in hazardous chemicals and wax grounds.",
      "megaPrompt": "Build \"Acid Bath Timer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI calculates precise etching durations so printmakers execute; ElevenLabs speaks the countdowns to coordinate multiple plates in the acid.\nDiscipline: Visual Art (printmaking etching).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Hands-free audio timing is critical when an artist's hands are covered in hazardous chemicals and wax grounds.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the printmaking etching use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for printmaking etching. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for printmaking etching.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$12B — global printmaking and fine art supplies",
      "sam": "$800M — professional printmaking studio equipment",
      "som": "$12M — academic and indie printmaker software"
    },
    {
      "id": "visual-art-grid-projection-caller-12",
      "theme": "visual-art",
      "title": "Grid Projection Caller",
      "pitch": "Lovable AI generates proportional grid coordinates so muralists scale; ElevenLabs reads the wall measurements aloud while you draw chalk lines.",
      "subDiscipline": "mural scaling and mapping",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Calling out coordinates allows muralists to maintain their physical momentum on scaffolding without constantly checking a digital screen.",
      "megaPrompt": "Build \"Grid Projection Caller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI generates proportional grid coordinates so muralists scale; ElevenLabs reads the wall measurements aloud while you draw chalk lines.\nDiscipline: Visual Art (mural scaling and mapping).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Calling out coordinates allows muralists to maintain their physical momentum on scaffolding without constantly checking a digital screen.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the mural scaling and mapping use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for mural scaling and mapping. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for mural scaling and mapping.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — public art and mural commissioning market",
      "sam": "$1B — large-scale art projection and mapping tools",
      "som": "$20M — professional muralist and street artist utilities"
    },
    {
      "id": "visual-art-portfolio-pitch-rehearser-13",
      "theme": "visual-art",
      "title": "Portfolio Pitch Rehearser",
      "pitch": "Lovable AI rewrites clunky artist statements so creators persuade; ElevenLabs speaks the polished pitch so you can rehearse the cadence.",
      "subDiscipline": "artist statement delivery",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Text-to-Speech provides a rhythmic baseline for artists to practice speaking about their work confidently for grants and residencies.",
      "megaPrompt": "Build \"Portfolio Pitch Rehearser\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI rewrites clunky artist statements so creators persuade; ElevenLabs speaks the polished pitch so you can rehearse the cadence.\nDiscipline: Visual Art (artist statement delivery).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Text-to-Speech provides a rhythmic baseline for artists to practice speaking about their work confidently for grants and residencies.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the artist statement delivery use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for artist statement delivery. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for artist statement delivery.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — arts grant and residency funding market",
      "sam": "$500M — artist career development and coaching",
      "som": "$15M — portfolio presentation and pitch software"
    },
    {
      "id": "visual-art-kiln-firing-announcer-14",
      "theme": "visual-art",
      "title": "Kiln Firing Announcer",
      "pitch": "Lovable AI formulates cone-ten glaze recipes so ceramicists experiment; ElevenLabs reads the chemical percentages while you weigh dry materials.",
      "subDiscipline": "ceramic glaze chemistry",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Audio instructions prevent cross-contamination by allowing potters to listen to recipes while their hands are deep in chemical buckets.",
      "megaPrompt": "Build \"Kiln Firing Announcer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI formulates cone-ten glaze recipes so ceramicists experiment; ElevenLabs reads the chemical percentages while you weigh dry materials.\nDiscipline: Visual Art (ceramic glaze chemistry).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Audio instructions prevent cross-contamination by allowing potters to listen to recipes while their hands are deep in chemical buckets.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the ceramic glaze chemistry use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for ceramic glaze chemistry. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for ceramic glaze chemistry.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — global ceramics and pottery supplies market",
      "sam": "$1.5B — advanced ceramic glaze formulation tools",
      "som": "$25M — studio potter and ceramicist software"
    },
    {
      "id": "visual-art-alt-text-artdescriber-15",
      "theme": "visual-art",
      "title": "Alt Text ArtDescriber",
      "pitch": "Lovable AI writes poetic alt-text so digital illustrators include; ElevenLabs speaks the descriptions to verify they capture visual essence.",
      "subDiscipline": "digital art accessibility",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Hearing the alt-text read aloud helps artists ensure the emotional tone matches their visual intent for screen-reader users.",
      "megaPrompt": "Build \"Alt Text ArtDescriber\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI writes poetic alt-text so digital illustrators include; ElevenLabs speaks the descriptions to verify they capture visual essence.\nDiscipline: Visual Art (digital art accessibility).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Hearing the alt-text read aloud helps artists ensure the emotional tone matches their visual intent for screen-reader users.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the digital art accessibility use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for digital art accessibility. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for digital art accessibility.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$15B — digital accessibility and compliance software",
      "sam": "$2B — creative media accessibility tools",
      "som": "$30M — digital illustrator and web comic platforms"
    },
    {
      "id": "visual-art-varnish-cure-narrator-16",
      "theme": "visual-art",
      "title": "Varnish Cure Narrator",
      "pitch": "Lovable AI tracks environmental humidity data so conservators protect; ElevenLabs speaks the climate warnings to safeguard drying oil paintings.",
      "subDiscipline": "painting conservation",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Ambient voice alerts notify conservators of micro-climate shifts without requiring them to stare at data dashboards in the lab.",
      "megaPrompt": "Build \"Varnish Cure Narrator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI tracks environmental humidity data so conservators protect; ElevenLabs speaks the climate warnings to safeguard drying oil paintings.\nDiscipline: Visual Art (painting conservation).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Ambient voice alerts notify conservators of micro-climate shifts without requiring them to stare at data dashboards in the lab.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the painting conservation use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for painting conservation. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for painting conservation.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — art conservation and museum preservation market",
      "sam": "$1B — fine art storage and climate monitoring",
      "som": "$20M — private conservation lab management software"
    },
    {
      "id": "visual-art-gesture-pose-caller-17",
      "theme": "visual-art",
      "title": "Gesture Pose Caller",
      "pitch": "Lovable AI designs dynamic anatomy poses so sketch groups practice; ElevenLabs speaks the pose instructions to the live model.",
      "subDiscipline": "life drawing sessions",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Text-to-Speech acts as an automated, unbiased session director, calling out timed gesture changes to keep the drawing class flowing.",
      "megaPrompt": "Build \"Gesture Pose Caller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI designs dynamic anatomy poses so sketch groups practice; ElevenLabs speaks the pose instructions to the live model.\nDiscipline: Visual Art (life drawing sessions).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Text-to-Speech acts as an automated, unbiased session director, calling out timed gesture changes to keep the drawing class flowing.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the life drawing sessions use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for life drawing sessions. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for life drawing sessions.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$15B — global art education and life drawing market",
      "sam": "$2B — figure drawing session management tools",
      "som": "$40M — independent sketch group and studio apps"
    },
    {
      "id": "visual-art-crate-packing-coach-18",
      "theme": "visual-art",
      "title": "Crate Packing Coach",
      "pitch": "Lovable AI generates custom crating instructions so artists ship; ElevenLabs reads the foam-cutting measurements while you build the wooden box.",
      "subDiscipline": "fine art logistics",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Spoken measurements allow artists to use power tools and cut materials safely without taking their eyes off the work.",
      "megaPrompt": "Build \"Crate Packing Coach\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI generates custom crating instructions so artists ship; ElevenLabs reads the foam-cutting measurements while you build the wooden box.\nDiscipline: Visual Art (fine art logistics).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Spoken measurements allow artists to use power tools and cut materials safely without taking their eyes off the work.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the fine art logistics use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for fine art logistics. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for fine art logistics.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$20B — fine art logistics and shipping market",
      "sam": "$3B — independent artist packaging and freight tools",
      "som": "$50M — studio-to-gallery shipping software"
    },
    {
      "id": "visual-art-wash-timing-whisperer-19",
      "theme": "visual-art",
      "title": "Wash Timing Whisperer",
      "pitch": "Lovable AI calculates paper drying times so watercolorists layer; ElevenLabs whispers the wait durations so you avoid muddying the wash.",
      "subDiscipline": "watercolor technique",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Gentle audio cues help painters time their wet-on-wet techniques perfectly without hovering anxiously over the damp paper.",
      "megaPrompt": "Build \"Wash Timing Whisperer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI calculates paper drying times so watercolorists layer; ElevenLabs whispers the wait durations so you avoid muddying the wash.\nDiscipline: Visual Art (watercolor technique).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Gentle audio cues help painters time their wet-on-wet techniques perfectly without hovering anxiously over the damp paper.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the watercolor technique use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for watercolor technique. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for watercolor technique.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$12B — global watercolor and fine art supplies",
      "sam": "$1B — watercolor technique education and tools",
      "som": "$15M — traditional watercolorist utility apps"
    },
    {
      "id": "visual-art-canvas-stretcher-counter-20",
      "theme": "visual-art",
      "title": "Canvas Stretcher Counter",
      "pitch": "Lovable AI audits your physical art supplies so painters organize; ElevenLabs reads the restocking list while you sort the studio.",
      "subDiscipline": "studio inventory management",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Audio readouts turn tedious physical inventory counts into a hands-free, conversational studio cleanup experience.",
      "megaPrompt": "Build \"Canvas Stretcher Counter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI audits your physical art supplies so painters organize; ElevenLabs reads the restocking list while you sort the studio.\nDiscipline: Visual Art (studio inventory management).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Audio readouts turn tedious physical inventory counts into a hands-free, conversational studio cleanup experience.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the studio inventory management use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for studio inventory management. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for studio inventory management.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$12B — global art materials and inventory market",
      "sam": "$1.5B — studio management and supply tracking",
      "som": "$25M — professional painter studio organization tools"
    },
    {
      "id": "visual-art-patron-update-broadcaster-21",
      "theme": "visual-art",
      "title": "Patron Update Broadcaster",
      "pitch": "Lovable AI drafts polite progress updates so portrait artists manage; ElevenLabs speaks the scripts to rehearse your client phone calls.",
      "subDiscipline": "commission client relations",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Hearing the diplomatic phrasing aloud helps artists navigate difficult commission conversations with wealthy patrons confidently.",
      "megaPrompt": "Build \"Patron Update Broadcaster\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI drafts polite progress updates so portrait artists manage; ElevenLabs speaks the scripts to rehearse your client phone calls.\nDiscipline: Visual Art (commission client relations).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Hearing the diplomatic phrasing aloud helps artists navigate difficult commission conversations with wealthy patrons confidently.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the commission client relations use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for commission client relations. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for commission client relations.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global commissioned art and portrait market",
      "sam": "$4B — freelance artist client management software",
      "som": "$60M — independent portrait and commission platforms"
    },
    {
      "id": "visual-art-sunlight-shift-spotter-22",
      "theme": "visual-art",
      "title": "Sunlight Shift Spotter",
      "pitch": "Lovable AI predicts golden hour shadow angles so landscape painters capture; ElevenLabs speaks the light warnings while you paint outdoors.",
      "subDiscipline": "plein air landscape",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Audio alerts notify plein air artists of fleeting light changes without forcing them to look away from the landscape.",
      "megaPrompt": "Build \"Sunlight Shift Spotter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI predicts golden hour shadow angles so landscape painters capture; ElevenLabs speaks the light warnings while you paint outdoors.\nDiscipline: Visual Art (plein air landscape).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Audio alerts notify plein air artists of fleeting light changes without forcing them to look away from the landscape.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the plein air landscape use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for plein air landscape. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for plein air landscape.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — outdoor recreation and plein air market",
      "sam": "$1B — landscape painting gear and accessories",
      "som": "$15M — plein air painter utility applications"
    },
    {
      "id": "visual-art-residency-essay-echo-23",
      "theme": "visual-art",
      "title": "Residency Essay Echo",
      "pitch": "Lovable AI structures compelling residency essays so applicants persuade; ElevenLabs reads the drafts back to check narrative flow and tone.",
      "subDiscipline": "grant writing and applications",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Listening to the essay read aloud reveals clunky phrasing and pacing issues that silent reading often misses.",
      "megaPrompt": "Build \"Residency Essay Echo\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI structures compelling residency essays so applicants persuade; ElevenLabs reads the drafts back to check narrative flow and tone.\nDiscipline: Visual Art (grant writing and applications).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Listening to the essay read aloud reveals clunky phrasing and pacing issues that silent reading often misses.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the grant writing and applications use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for grant writing and applications. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for grant writing and applications.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — arts grant and residency funding market",
      "sam": "$600M — artist application and grant writing tools",
      "som": "$20M — emerging artist career advancement software"
    },
    {
      "id": "visual-art-chroma-chord-translator-24",
      "theme": "visual-art",
      "title": "Chroma Chord Translator",
      "pitch": "Lovable AI maps color hex codes to musical theory so painters explore; ElevenLabs speaks the sonic equivalents to inspire visual rhythms.",
      "subDiscipline": "synesthetic art exploration",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Spoken musical descriptions help visual artists conceptualize synesthesia, translating color palettes into audible rhythms for cross-sensory inspiration.",
      "megaPrompt": "Build \"Chroma Chord Translator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI maps color hex codes to musical theory so painters explore; ElevenLabs speaks the sonic equivalents to inspire visual rhythms.\nDiscipline: Visual Art (synesthetic art exploration).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Spoken musical descriptions help visual artists conceptualize synesthesia, translating color palettes into audible rhythms for cross-sensory inspiration.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the synesthetic art exploration use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for synesthetic art exploration. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for synesthetic art exploration.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — digital art and cross-sensory software",
      "sam": "$1B — experimental art and synesthesia tools",
      "som": "$15M — avant-garde and generative artist apps"
    },
    {
      "id": "visual-art-palette-whisperer-0",
      "theme": "visual-art",
      "title": "Palette Whisperer",
      "pitch": "Lovable AI calculates pigment ratios so painters mix accurately; ElevenLabs speaks the recipe while hands are covered in paint.",
      "subDiscipline": "color mixing",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Hands covered in wet paint require a real-time conversational agent to adjust ratios verbally without touching screens.",
      "megaPrompt": "Build \"Palette Whisperer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI calculates pigment ratios so painters mix accurately; ElevenLabs speaks the recipe while hands are covered in paint.\nDiscipline: Visual Art (color mixing).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Hands covered in wet paint require a real-time conversational agent to adjust ratios verbally without touching screens.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the color mixing use case (e.g.\n\"You are a senior color mixing mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for color mixing.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$12B — fine art painting supplies",
      "som": "$150M — digital tools for traditional painters"
    },
    {
      "id": "visual-art-gallery-floor-coach-1",
      "theme": "visual-art",
      "title": "Gallery Floor Coach",
      "pitch": "Lovable AI simulates collector objections so gallerists practice pitching; ElevenLabs speaks the buyer persona in real-time roleplay.",
      "subDiscipline": "exhibition sales",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Roleplaying objections requires a low-latency conversational agent to simulate natural, unpredictable buyer interruptions.",
      "megaPrompt": "Build \"Gallery Floor Coach\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI simulates collector objections so gallerists practice pitching; ElevenLabs speaks the buyer persona in real-time roleplay.\nDiscipline: Visual Art (exhibition sales).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Roleplaying objections requires a low-latency conversational agent to simulate natural, unpredictable buyer interruptions.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the exhibition sales use case (e.g.\n\"You are a senior exhibition sales mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for exhibition sales.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$8B — commercial gallery operations",
      "som": "$200M — gallery sales training software"
    },
    {
      "id": "visual-art-storyboard-narrator-2",
      "theme": "visual-art",
      "title": "Storyboard Narrator",
      "pitch": "Lovable AI generates scene descriptions so illustrators visualize pacing; ElevenLabs speaks the narrative aloud to test emotional flow.",
      "subDiscipline": "visual storytelling",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Testing narrative pacing demands a conversational agent that can pause, rewind, and discuss emotional beats interactively.",
      "megaPrompt": "Build \"Storyboard Narrator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI generates scene descriptions so illustrators visualize pacing; ElevenLabs speaks the narrative aloud to test emotional flow.\nDiscipline: Visual Art (visual storytelling).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Testing narrative pacing demands a conversational agent that can pause, rewind, and discuss emotional beats interactively.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the visual storytelling use case (e.g.\n\"You are a senior visual storytelling mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for visual storytelling.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$5B — commercial illustration and publishing",
      "som": "$120M — storyboarding and pre-visualization tools"
    },
    {
      "id": "visual-art-code-canvas-companion-3",
      "theme": "visual-art",
      "title": "Code Canvas Companion",
      "pitch": "Lovable AI debugs shader logic so generative artists fix errors; ElevenLabs speaks the corrected syntax while eyes stay onscreen.",
      "subDiscipline": "creative coding",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Debugging code hands-free requires a conversational agent to listen to error descriptions and talk through logic fixes.",
      "megaPrompt": "Build \"Code Canvas Companion\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI debugs shader logic so generative artists fix errors; ElevenLabs speaks the corrected syntax while eyes stay onscreen.\nDiscipline: Visual Art (creative coding).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Debugging code hands-free requires a conversational agent to listen to error descriptions and talk through logic fixes.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the creative coding use case (e.g.\n\"You are a senior creative coding mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for creative coding.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$3B — digital and generative art software",
      "som": "$80M — creative coding environments and plugins"
    },
    {
      "id": "visual-art-easel-posture-guide-4",
      "theme": "visual-art",
      "title": "Easel Posture Guide",
      "pitch": "Lovable AI designs stretching routines so painters avoid repetitive strain; ElevenLabs speaks the physical cues during long studio sessions.",
      "subDiscipline": "studio ergonomics",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Stretching routines need a conversational agent to listen to pain feedback and adjust physical cues dynamically.",
      "megaPrompt": "Build \"Easel Posture Guide\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI designs stretching routines so painters avoid repetitive strain; ElevenLabs speaks the physical cues during long studio sessions.\nDiscipline: Visual Art (studio ergonomics).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Stretching routines need a conversational agent to listen to pain feedback and adjust physical cues dynamically.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the studio ergonomics use case (e.g.\n\"You are a senior studio ergonomics mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for studio ergonomics.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$2B — artist wellness and studio equipment",
      "som": "$40M — ergonomic aids for fine artists"
    },
    {
      "id": "visual-art-canvas-critic-5",
      "theme": "visual-art",
      "title": "Canvas Critic",
      "pitch": "Lovable AI analyzes composition balance so artists refine their work; ElevenLabs speaks constructive feedback like a supportive mentor.",
      "subDiscipline": "peer review",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Critiquing art is a dialogue, requiring a conversational agent to answer follow-up questions about specific compositional choices.",
      "megaPrompt": "Build \"Canvas Critic\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI analyzes composition balance so artists refine their work; ElevenLabs speaks constructive feedback like a supportive mentor.\nDiscipline: Visual Art (peer review).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Critiquing art is a dialogue, requiring a conversational agent to answer follow-up questions about specific compositional choices.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the peer review use case (e.g.\n\"You are a senior peer review mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for peer review.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$10B — art education and critique services",
      "som": "$250M — digital mentorship and portfolio review platforms"
    },
    {
      "id": "visual-art-white-cube-walker-6",
      "theme": "visual-art",
      "title": "White Cube Walker",
      "pitch": "Lovable AI plans artwork placement so curators optimize flow; ElevenLabs speaks the walkthrough directions while measuring physical walls.",
      "subDiscipline": "spatial curation",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Measuring walls requires a conversational agent to listen to spatial dimensions and recalculate placements on the fly.",
      "megaPrompt": "Build \"White Cube Walker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI plans artwork placement so curators optimize flow; ElevenLabs speaks the walkthrough directions while measuring physical walls.\nDiscipline: Visual Art (spatial curation).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Measuring walls requires a conversational agent to listen to spatial dimensions and recalculate placements on the fly.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the spatial curation use case (e.g.\n\"You are a senior spatial curation mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for spatial curation.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$4B — museum and gallery exhibition design",
      "som": "$90M — curatorial spatial planning software"
    },
    {
      "id": "visual-art-statement-sculptor-7",
      "theme": "visual-art",
      "title": "Statement Sculptor",
      "pitch": "Lovable AI drafts exhibition essays so artists articulate their vision; ElevenLabs speaks the text to test its conversational tone.",
      "subDiscipline": "arts writing",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Drafting essays benefits from a conversational agent that can debate word choices and refine tone interactively.",
      "megaPrompt": "Build \"Statement Sculptor\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI drafts exhibition essays so artists articulate their vision; ElevenLabs speaks the text to test its conversational tone.\nDiscipline: Visual Art (arts writing).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Drafting essays benefits from a conversational agent that can debate word choices and refine tone interactively.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the arts writing use case (e.g.\n\"You are a senior arts writing mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for arts writing.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$1B — art PR and copywriting services",
      "som": "$30M — AI writing assistants for creatives"
    },
    {
      "id": "visual-art-plein-air-spotter-8",
      "theme": "visual-art",
      "title": "Plein Air Spotter",
      "pitch": "Lovable AI tracks shifting sunlight so landscape painters capture golden hour; ElevenLabs speaks time and shadow warnings outdoors.",
      "subDiscipline": "outdoor painting",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Outdoor painting requires a conversational agent to listen to environmental changes and update shadow warnings dynamically.",
      "megaPrompt": "Build \"Plein Air Spotter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI tracks shifting sunlight so landscape painters capture golden hour; ElevenLabs speaks time and shadow warnings outdoors.\nDiscipline: Visual Art (outdoor painting).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Outdoor painting requires a conversational agent to listen to environmental changes and update shadow warnings dynamically.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the outdoor painting use case (e.g.\n\"You are a senior outdoor painting mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for outdoor painting.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$6B — landscape and plein art materials",
      "som": "$110M — outdoor painting accessories and aids"
    },
    {
      "id": "visual-art-press-timer-talker-9",
      "theme": "visual-art",
      "title": "Press Timer Talker",
      "pitch": "Lovable AI calculates acid bath durations so printmakers etch perfectly; ElevenLabs speaks countdowns and safety reminders over studio noise.",
      "subDiscipline": "printmaking",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Printmaking involves hazardous chemicals, needing a conversational agent to answer urgent safety questions while hands are busy.",
      "megaPrompt": "Build \"Press Timer Talker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI calculates acid bath durations so printmakers etch perfectly; ElevenLabs speaks countdowns and safety reminders over studio noise.\nDiscipline: Visual Art (printmaking).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Printmaking involves hazardous chemicals, needing a conversational agent to answer urgent safety questions while hands are busy.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the printmaking use case (e.g.\n\"You are a senior printmaking mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for printmaking.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$2B — fine art printmaking supplies",
      "som": "$45M — printmaking studio management tools"
    },
    {
      "id": "visual-art-muse-archivist-10",
      "theme": "visual-art",
      "title": "Muse Archivist",
      "pitch": "Lovable AI retrieves historical techniques so painters replicate old masters; ElevenLabs speaks the historical context during study sessions.",
      "subDiscipline": "art history",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Studying art history requires a conversational agent to engage in Socratic dialogue about historical techniques and contexts.",
      "megaPrompt": "Build \"Muse Archivist\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI retrieves historical techniques so painters replicate old masters; ElevenLabs speaks the historical context during study sessions.\nDiscipline: Visual Art (art history).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Studying art history requires a conversational agent to engage in Socratic dialogue about historical techniques and contexts.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the art history use case (e.g.\n\"You are a senior art history mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for art history.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$7B — art history research and academia",
      "som": "$150M — digital archives and study aids for artists"
    },
    {
      "id": "visual-art-brief-interrogator-11",
      "theme": "visual-art",
      "title": "Brief Interrogator",
      "pitch": "Lovable AI extracts core requirements so illustrators clarify vague requests; ElevenLabs speaks the follow-up questions during discovery calls.",
      "subDiscipline": "commercial illustration",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Clarifying briefs requires a conversational agent to simulate the client and respond to the illustrator's probing questions.",
      "megaPrompt": "Build \"Brief Interrogator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI extracts core requirements so illustrators clarify vague requests; ElevenLabs speaks the follow-up questions during discovery calls.\nDiscipline: Visual Art (commercial illustration).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Clarifying briefs requires a conversational agent to simulate the client and respond to the illustrator's probing questions.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the commercial illustration use case (e.g.\n\"You are a senior commercial illustration mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for commercial illustration.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$15B — commercial art and advertising illustration",
      "som": "$300M — freelance illustrator business management tools"
    },
    {
      "id": "visual-art-canvas-voicer-12",
      "theme": "visual-art",
      "title": "Canvas Voicer",
      "pitch": "Lovable AI writes rich visual descriptions so blind audiences experience art; ElevenLabs speaks the audio guide for gallery installations.",
      "subDiscipline": "visual accessibility",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Creating audio guides requires a conversational agent to interact with visually impaired users to answer specific artwork questions.",
      "megaPrompt": "Build \"Canvas Voicer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI writes rich visual descriptions so blind audiences experience art; ElevenLabs speaks the audio guide for gallery installations.\nDiscipline: Visual Art (visual accessibility).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Creating audio guides requires a conversational agent to interact with visually impaired users to answer specific artwork questions.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the visual accessibility use case (e.g.\n\"You are a senior visual accessibility mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for visual accessibility.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$3B — museum accessibility and inclusion services",
      "som": "$75M — audio description software for cultural institutions"
    },
    {
      "id": "visual-art-pigment-forager-13",
      "theme": "visual-art",
      "title": "Pigment Forager",
      "pitch": "Lovable AI identifies rare earth pigments so painters find authentic supplies; ElevenLabs speaks vendor details while browsing art stores.",
      "subDiscipline": "material sourcing",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Browsing stores requires a conversational agent to listen to pigment names and verify vendor stock interactively.",
      "megaPrompt": "Build \"Pigment Forager\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI identifies rare earth pigments so painters find authentic supplies; ElevenLabs speaks vendor details while browsing art stores.\nDiscipline: Visual Art (material sourcing).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Browsing stores requires a conversational agent to listen to pigment names and verify vendor stock interactively.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the material sourcing use case (e.g.\n\"You are a senior material sourcing mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for material sourcing.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$5B — specialty art material retail",
      "som": "$100M — supply chain tools for independent art shops"
    },
    {
      "id": "visual-art-prompt-alchemist-14",
      "theme": "visual-art",
      "title": "Prompt Alchemist",
      "pitch": "Lovable AI refines descriptive keywords so generative artists steer outputs; ElevenLabs speaks the optimized prompts for rapid vocal iteration.",
      "subDiscipline": "AI image generation",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Iterating prompts requires a conversational agent to listen to visual feedback and conversationally tweak descriptive parameters.",
      "megaPrompt": "Build \"Prompt Alchemist\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI refines descriptive keywords so generative artists steer outputs; ElevenLabs speaks the optimized prompts for rapid vocal iteration.\nDiscipline: Visual Art (AI image generation).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Iterating prompts requires a conversational agent to listen to visual feedback and conversationally tweak descriptive parameters.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the AI image generation use case (e.g.\n\"You are a senior AI image generation mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for AI image generation.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$4B — generative AI software subscriptions",
      "som": "$200M — prompt engineering and workflow tools"
    },
    {
      "id": "visual-art-fair-floor-wingman-15",
      "theme": "visual-art",
      "title": "Fair Floor Wingman",
      "pitch": "Lovable AI memorizes attendee backgrounds so gallerists network effectively; ElevenLabs speaks personalized icebreakers discreetly via earpiece at fairs.",
      "subDiscipline": "art networking",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Networking requires a conversational agent to listen to ambient introductions and whisper contextual follow-up questions to the gallerist.",
      "megaPrompt": "Build \"Fair Floor Wingman\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI memorizes attendee backgrounds so gallerists network effectively; ElevenLabs speaks personalized icebreakers discreetly via earpiece at fairs.\nDiscipline: Visual Art (art networking).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Networking requires a conversational agent to listen to ambient introductions and whisper contextual follow-up questions to the gallerist.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the art networking use case (e.g.\n\"You are a senior art networking mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for art networking.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$9B — art fair operations and VIP relations",
      "som": "$180M — CRM and networking tools for art dealers"
    },
    {
      "id": "visual-art-mural-grid-caller-16",
      "theme": "visual-art",
      "title": "Mural Grid Caller",
      "pitch": "Lovable AI calculates projection grids so muralists scale designs accurately; ElevenLabs speaks coordinate pairs while artists paint on scaffolding.",
      "subDiscipline": "large-scale painting",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Painting on scaffolding requires a conversational agent to listen to grid confirmations and call out the next coordinates.",
      "megaPrompt": "Build \"Mural Grid Caller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI calculates projection grids so muralists scale designs accurately; ElevenLabs speaks coordinate pairs while artists paint on scaffolding.\nDiscipline: Visual Art (large-scale painting).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Painting on scaffolding requires a conversational agent to listen to grid confirmations and call out the next coordinates.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the large-scale painting use case (e.g.\n\"You are a senior large-scale painting mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for large-scale painting.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$2B — public art and mural commissions",
      "som": "$60M — large-scale painting planning software"
    },
    {
      "id": "visual-art-kiln-watcher-17",
      "theme": "visual-art",
      "title": "Kiln Watcher",
      "pitch": "Lovable AI monitors temperature curves so ceramicists prevent cracking; ElevenLabs speaks thermal alerts during overnight kiln firings.",
      "subDiscipline": "ceramic firing",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Monitoring kilns requires a conversational agent to listen to temperature overrides and discuss firing adjustments in real time.",
      "megaPrompt": "Build \"Kiln Watcher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI monitors temperature curves so ceramicists prevent cracking; ElevenLabs speaks thermal alerts during overnight kiln firings.\nDiscipline: Visual Art (ceramic firing).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Monitoring kilns requires a conversational agent to listen to temperature overrides and discuss firing adjustments in real time.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the ceramic firing use case (e.g.\n\"You are a senior ceramic firing mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for ceramic firing.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$3B — ceramics and sculptural arts supplies",
      "som": "$85M — kiln monitoring and studio safety tech"
    },
    {
      "id": "visual-art-wash-water-guide-18",
      "theme": "visual-art",
      "title": "Wash Water Guide",
      "pitch": "Lovable AI predicts drying times so watercolorists layer washes seamlessly; ElevenLabs speaks moisture cues to prevent muddy colors.",
      "subDiscipline": "watercolor technique",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Watercolor layering requires a conversational agent to listen to paper wetness observations and advise on the next wash.",
      "megaPrompt": "Build \"Wash Water Guide\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI predicts drying times so watercolorists layer washes seamlessly; ElevenLabs speaks moisture cues to prevent muddy colors.\nDiscipline: Visual Art (watercolor technique).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Watercolor layering requires a conversational agent to listen to paper wetness observations and advise on the next wash.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the watercolor technique use case (e.g.\n\"You are a senior watercolor technique mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for watercolor technique.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$4B — watercolor and paper art materials",
      "som": "$90M — technique aids for watercolor painters"
    },
    {
      "id": "visual-art-price-tag-whisper-19",
      "theme": "visual-art",
      "title": "Price Tag Whisper",
      "pitch": "Lovable AI analyzes market comps so artists price confidently; ElevenLabs speaks the pricing rationale during private studio visits.",
      "subDiscipline": "art valuation",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Pricing art requires a conversational agent to debate market variables and negotiate final numbers with the artist.",
      "megaPrompt": "Build \"Price Tag Whisper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI analyzes market comps so artists price confidently; ElevenLabs speaks the pricing rationale during private studio visits.\nDiscipline: Visual Art (art valuation).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Pricing art requires a conversational agent to debate market variables and negotiate final numbers with the artist.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the art valuation use case (e.g.\n\"You are a senior art valuation mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for art valuation.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$11B — art advisory and valuation services",
      "som": "$220M — pricing analytics for independent artists"
    },
    {
      "id": "visual-art-sketchbook-confidant-20",
      "theme": "visual-art",
      "title": "Sketchbook Confidant",
      "pitch": "Lovable AI reflects on daily doodles so artists process creative blocks; ElevenLabs speaks empathetic insights during quiet studio hours.",
      "subDiscipline": "visual journaling",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Journaling requires a conversational agent to listen to emotional reflections and ask empathetic, probing follow-up questions.",
      "megaPrompt": "Build \"Sketchbook Confidant\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI reflects on daily doodles so artists process creative blocks; ElevenLabs speaks empathetic insights during quiet studio hours.\nDiscipline: Visual Art (visual journaling).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Journaling requires a conversational agent to listen to emotional reflections and ask empathetic, probing follow-up questions.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the visual journaling use case (e.g.\n\"You are a senior visual journaling mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for visual journaling.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$2B — art therapy and creative wellness",
      "som": "$50M — digital journaling and mindfulness apps for creatives"
    },
    {
      "id": "visual-art-nib-rhythm-keeper-21",
      "theme": "visual-art",
      "title": "Nib Rhythm Keeper",
      "pitch": "Lovable AI generates metronome patterns so calligraphers maintain stroke consistency; ElevenLabs speaks the rhythmic beats while hands are inked.",
      "subDiscipline": "calligraphy",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Calligraphy requires a conversational agent to listen to stroke rhythm feedback and adjust the spoken metronome dynamically.",
      "megaPrompt": "Build \"Nib Rhythm Keeper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI generates metronome patterns so calligraphers maintain stroke consistency; ElevenLabs speaks the rhythmic beats while hands are inked.\nDiscipline: Visual Art (calligraphy).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Calligraphy requires a conversational agent to listen to stroke rhythm feedback and adjust the spoken metronome dynamically.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the calligraphy use case (e.g.\n\"You are a senior calligraphy mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for calligraphy.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$1B — calligraphy and lettering supplies",
      "som": "$25M — practice aids for typographic artists"
    },
    {
      "id": "visual-art-light-meter-muse-22",
      "theme": "visual-art",
      "title": "Light Meter Muse",
      "pitch": "Lovable AI calculates exposure ratios so photographers shape dramatic portraits; ElevenLabs speaks f-stop adjustments while adjusting physical modifiers.",
      "subDiscipline": "studio lighting",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Adjusting lights requires a conversational agent to listen to shadow descriptions and suggest real-time modifier changes.",
      "megaPrompt": "Build \"Light Meter Muse\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI calculates exposure ratios so photographers shape dramatic portraits; ElevenLabs speaks f-stop adjustments while adjusting physical modifiers.\nDiscipline: Visual Art (studio lighting).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Adjusting lights requires a conversational agent to listen to shadow descriptions and suggest real-time modifier changes.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the studio lighting use case (e.g.\n\"You are a senior studio lighting mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for studio lighting.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$14B — photography and lighting equipment",
      "som": "$350M — studio lighting calculation and planning tools"
    },
    {
      "id": "visual-art-varnish-doctor-23",
      "theme": "visual-art",
      "title": "Varnish Doctor",
      "pitch": "Lovable AI diagnoses canvas degradation so conservators choose safe solvents; ElevenLabs speaks chemical warnings during delicate restoration work.",
      "subDiscipline": "art conservation",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Restoration requires a conversational agent to listen to solvent reactions and instantly advise on chemical neutralization.",
      "megaPrompt": "Build \"Varnish Doctor\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI diagnoses canvas degradation so conservators choose safe solvents; ElevenLabs speaks chemical warnings during delicate restoration work.\nDiscipline: Visual Art (art conservation).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Restoration requires a conversational agent to listen to solvent reactions and instantly advise on chemical neutralization.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the art conservation use case (e.g.\n\"You are a senior art conservation mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for art conservation.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$2B — art conservation and restoration services",
      "som": "$70M — diagnostic software for art conservators"
    },
    {
      "id": "visual-art-space-wire-rigger-24",
      "theme": "visual-art",
      "title": "Space Wire Rigger",
      "pitch": "Lovable AI calculates load-bearing tensions so installation artists hang safely; ElevenLabs speaks rigging instructions while working on ladders.",
      "subDiscipline": "installation art",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Rigging requires a conversational agent to listen to tension feedback from the artist and adjust load calculations verbally.",
      "megaPrompt": "Build \"Space Wire Rigger\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI calculates load-bearing tensions so installation artists hang safely; ElevenLabs speaks rigging instructions while working on ladders.\nDiscipline: Visual Art (installation art).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Rigging requires a conversational agent to listen to tension feedback from the artist and adjust load calculations verbally.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the installation art use case (e.g.\n\"You are a senior installation art mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for installation art.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market",
      "sam": "$3B — contemporary installation and spatial art",
      "som": "$65M — structural planning tools for installation artists"
    },
    {
      "id": "visual-art-canvas-whisper-journal-0",
      "theme": "visual-art",
      "title": "Canvas Whisper Journal",
      "pitch": "Lovable AI structures messy thoughts so painters draft statements; ElevenLabs transcribes hands-free studio audio.",
      "subDiscipline": "studio journaling",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT allows painters to journal continuously without washing paint off their hands.",
      "megaPrompt": "Build \"Canvas Whisper Journal\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI structures messy thoughts so painters draft statements; ElevenLabs transcribes hands-free studio audio.\nDiscipline: Visual Art (studio journaling).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT allows painters to journal continuously without washing paint off their hands.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for studio journaling:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken studio journaling notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for studio journaling.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global art supply and studio software market",
      "sam": "$500M — professional painter productivity tools",
      "som": "$15M — indie painter journaling apps"
    },
    {
      "id": "visual-art-prompt-alchemy-dictation-1",
      "theme": "visual-art",
      "title": "Prompt Alchemy Dictation",
      "pitch": "Lovable AI refines spoken descriptions so generative artists get perfect prompts; ElevenLabs captures rapid visual brainstorming.",
      "subDiscipline": "generative art prompting",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT lets artists speak complex visual concepts much faster than typing them.",
      "megaPrompt": "Build \"Prompt Alchemy Dictation\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI refines spoken descriptions so generative artists get perfect prompts; ElevenLabs captures rapid visual brainstorming.\nDiscipline: Visual Art (generative art prompting).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT lets artists speak complex visual concepts much faster than typing them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for generative art prompting:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken generative art prompting notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for generative art prompting.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global generative AI software market",
      "sam": "$800M — AI image generation tools",
      "som": "$25M — prompt engineering utilities"
    },
    {
      "id": "visual-art-placard-prose-weaver-2",
      "theme": "visual-art",
      "title": "Placard Prose Weaver",
      "pitch": "Lovable AI writes formal placards so gallerists save time; ElevenLabs transcribes spontaneous artwork observations.",
      "subDiscipline": "gallery curation",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT captures the gallerist's authentic, off-the-cuff passion for immediate text refinement.",
      "megaPrompt": "Build \"Placard Prose Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI writes formal placards so gallerists save time; ElevenLabs transcribes spontaneous artwork observations.\nDiscipline: Visual Art (gallery curation).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT captures the gallerist's authentic, off-the-cuff passion for immediate text refinement.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for gallery curation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken gallery curation notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for gallery curation.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global fine art market",
      "sam": "$4B — gallery management and exhibition software",
      "som": "$50M — exhibition catalog publishing tools"
    },
    {
      "id": "visual-art-studio-critique-catcher-3",
      "theme": "visual-art",
      "title": "Studio Critique Catcher",
      "pitch": "Lovable AI formats feedback so art teachers give better notes; ElevenLabs captures walk-around studio critiques.",
      "subDiscipline": "art education",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT allows teachers to maintain eye contact with students while dictating feedback.",
      "megaPrompt": "Build \"Studio Critique Catcher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI formats feedback so art teachers give better notes; ElevenLabs captures walk-around studio critiques.\nDiscipline: Visual Art (art education).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT allows teachers to maintain eye contact with students while dictating feedback.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for art education:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken art education notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for art education.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — global art education and edtech market",
      "sam": "$2B — higher education art department software",
      "som": "$40M — studio critique management platforms"
    },
    {
      "id": "visual-art-pigment-ratio-scribe-4",
      "theme": "visual-art",
      "title": "Pigment Ratio Scribe",
      "pitch": "Lovable AI logs recipes so painters reproduce exact colors; ElevenLabs transcribes messy-hands pigment ratios.",
      "subDiscipline": "color mixing",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT is essential because painters cannot touch keyboards while mixing oils.",
      "megaPrompt": "Build \"Pigment Ratio Scribe\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI logs recipes so painters reproduce exact colors; ElevenLabs transcribes messy-hands pigment ratios.\nDiscipline: Visual Art (color mixing).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT is essential because painters cannot touch keyboards while mixing oils.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for color mixing:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken color mixing notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for color mixing.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$12B — global paint and coating manufacturing market",
      "sam": "$1B — professional artist paint and accessory sales",
      "som": "$20M — color theory and mixing software"
    },
    {
      "id": "visual-art-brief-extraction-whisper-5",
      "theme": "visual-art",
      "title": "Brief Extraction Whisper",
      "pitch": "Lovable AI extracts requirements so illustrators get clear briefs; ElevenLabs captures client discovery calls.",
      "subDiscipline": "illustration commissions",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT accurately logs nuanced client requests during fast-paced verbal negotiations.",
      "megaPrompt": "Build \"Brief Extraction Whisper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI extracts requirements so illustrators get clear briefs; ElevenLabs captures client discovery calls.\nDiscipline: Visual Art (illustration commissions).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT accurately logs nuanced client requests during fast-paced verbal negotiations.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for illustration commissions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken illustration commissions notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for illustration commissions.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — global freelance and gig economy platforms",
      "sam": "$1.5B — commercial illustration and design market",
      "som": "$30M — freelance illustrator CRM tools"
    },
    {
      "id": "visual-art-plein-air-scene-scribe-6",
      "theme": "visual-art",
      "title": "Plein Air Scene Scribe",
      "pitch": "Lovable AI writes scene essays so landscape painters document light; ElevenLabs transcribes outdoor weather observations.",
      "subDiscipline": "landscape painting",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT lets artists dictate shifting light conditions without looking away from the canvas.",
      "megaPrompt": "Build \"Plein Air Scene Scribe\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI writes scene essays so landscape painters document light; ElevenLabs transcribes outdoor weather observations.\nDiscipline: Visual Art (landscape painting).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT lets artists dictate shifting light conditions without looking away from the canvas.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for landscape painting:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken landscape painting notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for landscape painting.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — global outdoor recreation and plein air market",
      "sam": "$500M — landscape art supplies and workshops",
      "som": "$10M — plein air painting companion apps"
    },
    {
      "id": "visual-art-armature-blueprint-dictation-7",
      "theme": "visual-art",
      "title": "Armature Blueprint Dictation",
      "pitch": "Lovable AI drafts build plans so sculptors organize materials; ElevenLabs captures spatial measurement dictations.",
      "subDiscipline": "sculpture planning",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT allows sculptors to call out dimensions while physically manipulating heavy materials.",
      "megaPrompt": "Build \"Armature Blueprint Dictation\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI drafts build plans so sculptors organize materials; ElevenLabs captures spatial measurement dictations.\nDiscipline: Visual Art (sculpture planning).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT allows sculptors to call out dimensions while physically manipulating heavy materials.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for sculpture planning:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken sculpture planning notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for sculpture planning.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — global sculpture and public art market",
      "sam": "$600M — sculpting tools and fabrication services",
      "som": "$15M — 3D fabrication and sculpting planning software"
    },
    {
      "id": "visual-art-booth-pitch-rehearsal-8",
      "theme": "visual-art",
      "title": "Booth Pitch Rehearsal",
      "pitch": "Lovable AI critiques pacing so artists sell better; ElevenLabs transcribes spoken art fair pitches.",
      "subDiscipline": "art fair sales",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT captures the exact cadence and pauses of a spoken sales pitch.",
      "megaPrompt": "Build \"Booth Pitch Rehearsal\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI critiques pacing so artists sell better; ElevenLabs transcribes spoken art fair pitches.\nDiscipline: Visual Art (art fair sales).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT captures the exact cadence and pauses of a spoken sales pitch.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for art fair sales:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken art fair sales notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for art fair sales.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global art fair and exhibition market",
      "sam": "$400M — art fair booth management and sales",
      "som": "$12M — artist sales coaching and pitch tools"
    },
    {
      "id": "visual-art-canvas-inventory-scribe-9",
      "theme": "visual-art",
      "title": "Canvas Inventory Scribe",
      "pitch": "Lovable AI updates databases so artists track supplies; ElevenLabs captures spoken inventory dimensions.",
      "subDiscipline": "studio management",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT enables hands-free logging of physical artwork dimensions and framing supplies.",
      "megaPrompt": "Build \"Canvas Inventory Scribe\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI updates databases so artists track supplies; ElevenLabs captures spoken inventory dimensions.\nDiscipline: Visual Art (studio management).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT enables hands-free logging of physical artwork dimensions and framing supplies.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for studio management:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken studio management notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for studio management.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — global art inventory and collection management market",
      "sam": "$800M — working artist studio management software",
      "som": "$25M — independent artist inventory trackers"
    },
    {
      "id": "visual-art-mural-wall-mapper-10",
      "theme": "visual-art",
      "title": "Mural Wall Mapper",
      "pitch": "Lovable AI generates checklists so muralists prep surfaces; ElevenLabs transcribes on-site wall condition notes.",
      "subDiscipline": "public art prep",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT lets artists dictate structural flaws while climbing scaffolding or lifts.",
      "megaPrompt": "Build \"Mural Wall Mapper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI generates checklists so muralists prep surfaces; ElevenLabs transcribes on-site wall condition notes.\nDiscipline: Visual Art (public art prep).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT lets artists dictate structural flaws while climbing scaffolding or lifts.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for public art prep:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken public art prep notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for public art prep.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global mural and street art commission market",
      "sam": "$300M — public art project management software",
      "som": "$8M — muralist site prep and logistics tools"
    },
    {
      "id": "visual-art-proofing-state-logger-11",
      "theme": "visual-art",
      "title": "Proofing State Logger",
      "pitch": "Lovable AI logs state changes so printmakers track editions; ElevenLabs captures spoken proofing adjustments.",
      "subDiscipline": "printmaking",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT allows printmakers to dictate ink viscosity changes while operating heavy presses.",
      "megaPrompt": "Build \"Proofing State Logger\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI logs state changes so printmakers track editions; ElevenLabs captures spoken proofing adjustments.\nDiscipline: Visual Art (printmaking).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT allows printmakers to dictate ink viscosity changes while operating heavy presses.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for printmaking:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken printmaking notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for printmaking.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$900M — global fine art printmaking market",
      "sam": "$200M — printmaking studio equipment and software",
      "som": "$5M — edition tracking and print state logs"
    },
    {
      "id": "visual-art-wash-layer-timeline-12",
      "theme": "visual-art",
      "title": "Wash Layer Timeline",
      "pitch": "Lovable AI creates timelines so painters master drying; ElevenLabs transcribes spoken wetness observations.",
      "subDiscipline": "watercolor technique",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT captures immediate visual feedback on water absorption without interrupting the brushstroke.",
      "megaPrompt": "Build \"Wash Layer Timeline\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI creates timelines so painters master drying; ElevenLabs transcribes spoken wetness observations.\nDiscipline: Visual Art (watercolor technique).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT captures immediate visual feedback on water absorption without interrupting the brushstroke.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for watercolor technique:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken watercolor technique notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for watercolor technique.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.2B — global watercolor and paper art market",
      "sam": "$300M — watercolor instruction and technique software",
      "som": "$7M — watercolor drying and timing aids"
    },
    {
      "id": "visual-art-exhibition-thread-weaver-13",
      "theme": "visual-art",
      "title": "Exhibition Thread Weaver",
      "pitch": "Lovable AI writes essays so curators link themes; ElevenLabs transcribes brainstormed artwork connections.",
      "subDiscipline": "curatorial writing",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT captures the rapid, associative thinking required to connect disparate visual artworks.",
      "megaPrompt": "Build \"Exhibition Thread Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI writes essays so curators link themes; ElevenLabs transcribes brainstormed artwork connections.\nDiscipline: Visual Art (curatorial writing).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT captures the rapid, associative thinking required to connect disparate visual artworks.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for curatorial writing:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken curatorial writing notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for curatorial writing.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global museum and curatorial services market",
      "sam": "$1B — exhibition design and catalog publishing",
      "som": "$30M — curatorial brainstorming and essay drafting tools"
    },
    {
      "id": "visual-art-gesture-anatomy-scribe-14",
      "theme": "visual-art",
      "title": "Gesture Anatomy Scribe",
      "pitch": "Lovable AI compiles guides so artists learn anatomy; ElevenLabs captures spoken pose observations.",
      "subDiscipline": "figure drawing",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT lets artists dictate muscle tensions during fleeting, short-duration live model poses.",
      "megaPrompt": "Build \"Gesture Anatomy Scribe\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI compiles guides so artists learn anatomy; ElevenLabs captures spoken pose observations.\nDiscipline: Visual Art (figure drawing).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT lets artists dictate muscle tensions during fleeting, short-duration live model poses.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for figure drawing:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken figure drawing notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for figure drawing.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global life drawing and figure art market",
      "sam": "$400M — anatomy instruction and reference software",
      "som": "$10M — live figure drawing study aids"
    },
    {
      "id": "visual-art-kiln-schedule-dictation-15",
      "theme": "visual-art",
      "title": "Kiln Schedule Dictation",
      "pitch": "Lovable AI formats schedules so potters fire safely; ElevenLabs transcribes spoken glaze recipes.",
      "subDiscipline": "ceramic glazing",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT allows potters to dictate chemical ratios while their hands are covered in clay.",
      "megaPrompt": "Build \"Kiln Schedule Dictation\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI formats schedules so potters fire safely; ElevenLabs transcribes spoken glaze recipes.\nDiscipline: Visual Art (ceramic glazing).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT allows potters to dictate chemical ratios while their hands are covered in clay.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for ceramic glazing:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken ceramic glazing notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for ceramic glazing.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — global ceramics and pottery equipment market",
      "sam": "$500M — ceramic studio management and glaze software",
      "som": "$12M — independent potter firing schedule tools"
    },
    {
      "id": "visual-art-blockage-breaker-coach-16",
      "theme": "visual-art",
      "title": "Blockage Breaker Coach",
      "pitch": "Lovable AI offers coaching so painters overcome frustration; ElevenLabs transcribes spoken creative blockages.",
      "subDiscipline": "artist mindset",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT captures the raw, emotional venting that precedes a creative breakthrough.",
      "megaPrompt": "Build \"Blockage Breaker Coach\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI offers coaching so painters overcome frustration; ElevenLabs transcribes spoken creative blockages.\nDiscipline: Visual Art (artist mindset).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT captures the raw, emotional venting that precedes a creative breakthrough.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for artist mindset:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken artist mindset notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for artist mindset.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global creative therapy and coaching market",
      "sam": "$300M — artist wellness and mindset programs",
      "som": "$8M — creative block journaling and coaching apps"
    },
    {
      "id": "visual-art-grant-vision-drafter-17",
      "theme": "visual-art",
      "title": "Grant Vision Drafter",
      "pitch": "Lovable AI writes proposals so artists win grants; ElevenLabs captures spoken project impacts.",
      "subDiscipline": "arts funding",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT lets artists passionately articulate their vision without being constrained by formal typing.",
      "megaPrompt": "Build \"Grant Vision Drafter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI writes proposals so artists win grants; ElevenLabs captures spoken project impacts.\nDiscipline: Visual Art (arts funding).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT lets artists passionately articulate their vision without being constrained by formal typing.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for arts funding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken arts funding notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for arts funding.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — global arts grant and foundation funding market",
      "sam": "$800M — grant writing and arts administration software",
      "som": "$20M — independent artist grant application tools"
    },
    {
      "id": "visual-art-residency-interview-prep-18",
      "theme": "visual-art",
      "title": "Residency Interview Prep",
      "pitch": "Lovable AI evaluates clarity so artists ace interviews; ElevenLabs transcribes spoken residency answers.",
      "subDiscipline": "career development",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT captures the natural hesitations and verbal tics that need refining for interviews.",
      "megaPrompt": "Build \"Residency Interview Prep\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI evaluates clarity so artists ace interviews; ElevenLabs transcribes spoken residency answers.\nDiscipline: Visual Art (career development).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT captures the natural hesitations and verbal tics that need refining for interviews.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for career development:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken career development notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for career development.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$500M — global artist residency and fellowship market",
      "sam": "$100M — arts career development and coaching",
      "som": "$3M — residency interview rehearsal platforms"
    },
    {
      "id": "visual-art-slide-lecture-catcher-19",
      "theme": "visual-art",
      "title": "Slide Lecture Catcher",
      "pitch": "Lovable AI builds guides so professors teach better; ElevenLabs transcribes spontaneous lecture insights.",
      "subDiscipline": "art history teaching",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT captures the unscripted, passionate tangents that make art history lectures engaging.",
      "megaPrompt": "Build \"Slide Lecture Catcher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI builds guides so professors teach better; ElevenLabs transcribes spontaneous lecture insights.\nDiscipline: Visual Art (art history teaching).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT captures the unscripted, passionate tangents that make art history lectures engaging.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for art history teaching:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken art history teaching notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for art history teaching.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — global higher education humanities market",
      "sam": "$1.5B — university art history department software",
      "som": "$35M — lecture capture and study guide generators"
    },
    {
      "id": "visual-art-value-proposition-scribe-20",
      "theme": "visual-art",
      "title": "Value Proposition Scribe",
      "pitch": "Lovable AI drafts emails so artists negotiate confidently; ElevenLabs captures spoken pricing justifications.",
      "subDiscipline": "art pricing",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT helps artists verbalize their self-worth and pricing logic before sending formal quotes.",
      "megaPrompt": "Build \"Value Proposition Scribe\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI drafts emails so artists negotiate confidently; ElevenLabs captures spoken pricing justifications.\nDiscipline: Visual Art (art pricing).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT helps artists verbalize their self-worth and pricing logic before sending formal quotes.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for art pricing:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken art pricing notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for art pricing.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$6B — global art advisory and private sales market",
      "sam": "$1.2B — independent artist sales and pricing tools",
      "som": "$25M — art commission negotiation and invoicing apps"
    },
    {
      "id": "visual-art-storyboard-arc-dictation-21",
      "theme": "visual-art",
      "title": "Storyboard Arc Dictation",
      "pitch": "Lovable AI structures storyboards so illustrators plan narratives; ElevenLabs transcribes spoken plot beats.",
      "subDiscipline": "graphic narrative",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT allows illustrators to act out dialogue and pacing while sketching rough panels.",
      "megaPrompt": "Build \"Storyboard Arc Dictation\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI structures storyboards so illustrators plan narratives; ElevenLabs transcribes spoken plot beats.\nDiscipline: Visual Art (graphic narrative).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT allows illustrators to act out dialogue and pacing while sketching rough panels.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for graphic narrative:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken graphic narrative notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for graphic narrative.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — global comic and graphic novel publishing market",
      "sam": "$600M — digital illustration and storyboarding software",
      "som": "$15M — graphic novel plotting and pacing tools"
    },
    {
      "id": "visual-art-rigging-safety-scribe-22",
      "theme": "visual-art",
      "title": "Rigging Safety Scribe",
      "pitch": "Lovable AI generates manuals so installers work safely; ElevenLabs captures spoken rigging checks.",
      "subDiscipline": "installation art",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT lets installation artists dictate safety protocols while physically inspecting heavy rigging.",
      "megaPrompt": "Build \"Rigging Safety Scribe\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI generates manuals so installers work safely; ElevenLabs captures spoken rigging checks.\nDiscipline: Visual Art (installation art).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT lets installation artists dictate safety protocols while physically inspecting heavy rigging.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for installation art:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken installation art notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for installation art.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — global large-scale installation and public art market",
      "sam": "$200M — art logistics and installation management",
      "som": "$5M — installation safety and compliance checklists"
    },
    {
      "id": "visual-art-crate-handler-guide-23",
      "theme": "visual-art",
      "title": "Crate Handler Guide",
      "pitch": "Lovable AI writes instructions so shippers avoid damage; ElevenLabs transcribes spoken packaging details.",
      "subDiscipline": "art shipping",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT allows artists to dictate fragile points while actively building custom shipping crates.",
      "megaPrompt": "Build \"Crate Handler Guide\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI writes instructions so shippers avoid damage; ElevenLabs transcribes spoken packaging details.\nDiscipline: Visual Art (art shipping).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT allows artists to dictate fragile points while actively building custom shipping crates.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for art shipping:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken art shipping notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for art shipping.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global fine art logistics and shipping market",
      "sam": "$400M — art packaging and crating services",
      "som": "$10M — independent artist shipping instruction generators"
    },
    {
      "id": "visual-art-portfolio-thread-weaver-24",
      "theme": "visual-art",
      "title": "Portfolio Thread Weaver",
      "pitch": "Lovable AI writes introductions so artists present cohesively; ElevenLabs transcribes spoken portfolio narratives.",
      "subDiscipline": "portfolio review",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT captures the overarching conceptual story an artist tells when reviewing their body of work.",
      "megaPrompt": "Build \"Portfolio Thread Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI writes introductions so artists present cohesively; ElevenLabs transcribes spoken portfolio narratives.\nDiscipline: Visual Art (portfolio review).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT captures the overarching conceptual story an artist tells when reviewing their body of work.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for portfolio review:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken portfolio review notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for portfolio review.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global art portfolio and admissions market",
      "sam": "$300M — professional artist website and portfolio builders",
      "som": "$8M — portfolio narrative and review prep tools"
    },
    {
      "id": "visual-art-canvas-breeze-synth-0",
      "theme": "visual-art",
      "title": "Canvas Breeze Synth",
      "pitch": "Lovable AI analyzes your landscape sketch so you feel immersed; ElevenLabs scores it.",
      "subDiscipline": "plein air painting",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Generative SFX creates immersive environmental audio matching the painted scenery.",
      "megaPrompt": "Build \"Canvas Breeze Synth\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI analyzes your landscape sketch so you feel immersed; ElevenLabs scores it.\nDiscipline: Visual Art (plein air painting).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Generative SFX creates immersive environmental audio matching the painted scenery.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for plein air painting. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for plein air painting.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global outdoor art supplies",
      "sam": "$500M — professional plein air painters",
      "som": "$15M — digital companion apps for painters"
    },
    {
      "id": "visual-art-brushstroke-tempo-engine-1",
      "theme": "visual-art",
      "title": "Brushstroke Tempo Engine",
      "pitch": "Lovable AI tracks your painting pace so you maintain deep focus; ElevenLabs scores it.",
      "subDiscipline": "studio flow state",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Generative music adapts BPM to the artist's physical brushstroke rhythm.",
      "megaPrompt": "Build \"Brushstroke Tempo Engine\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI tracks your painting pace so you maintain deep focus; ElevenLabs scores it.\nDiscipline: Visual Art (studio flow state).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Generative music adapts BPM to the artist's physical brushstroke rhythm.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for studio flow state. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for studio flow state.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global art studio equipment",
      "sam": "$1B — professional studio painters",
      "som": "$20M — focus and productivity audio tools"
    },
    {
      "id": "visual-art-chromatic-chord-mapper-2",
      "theme": "visual-art",
      "title": "Chromatic Chord Mapper",
      "pitch": "Lovable AI translates your palette choices so you understand harmony; ElevenLabs sounds it.",
      "subDiscipline": "color theory study",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation maps specific color frequencies and temperatures to audio chords.",
      "megaPrompt": "Build \"Chromatic Chord Mapper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI translates your palette choices so you understand harmony; ElevenLabs sounds it.\nDiscipline: Visual Art (color theory study).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation maps specific color frequencies and temperatures to audio chords.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for color theory study. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for color theory study.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — art education software",
      "sam": "$200M — color theory students",
      "som": "$5M — synesthesia learning tools"
    },
    {
      "id": "visual-art-white-cube-resonator-3",
      "theme": "visual-art",
      "title": "White Cube Resonator",
      "pitch": "Lovable AI designs spatial audio concepts so gallerists enhance installations; ElevenLabs scores it.",
      "subDiscipline": "gallery exhibition curation",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Studio track generation provides room-filling ambient soundscapes for physical art spaces.",
      "megaPrompt": "Build \"White Cube Resonator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI designs spatial audio concepts so gallerists enhance installations; ElevenLabs scores it.\nDiscipline: Visual Art (gallery exhibition curation).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Studio track generation provides room-filling ambient soundscapes for physical art spaces.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for gallery exhibition curation. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for gallery exhibition curation.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — global art gallery operations",
      "sam": "$2B — contemporary exhibition spaces",
      "som": "$30M — gallery sensory enhancement tech"
    },
    {
      "id": "visual-art-splatter-impact-foley-4",
      "theme": "visual-art",
      "title": "Splatter Impact Foley",
      "pitch": "Lovable AI interprets your gestural movements so you feel the canvas; ElevenLabs sounds it.",
      "subDiscipline": "abstract expressionism",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Short SFX generation provides percussive feedback for aggressive, physical paint application.",
      "megaPrompt": "Build \"Splatter Impact Foley\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI interprets your gestural movements so you feel the canvas; ElevenLabs sounds it.\nDiscipline: Visual Art (abstract expressionism).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Short SFX generation provides percussive feedback for aggressive, physical paint application.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for abstract expressionism. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for abstract expressionism.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — modern art materials",
      "sam": "$500M — abstract expressionist painters",
      "som": "$10M — interactive studio feedback tools"
    },
    {
      "id": "visual-art-kiln-rhythm-spinner-5",
      "theme": "visual-art",
      "title": "Kiln Rhythm Spinner",
      "pitch": "Lovable AI plans your clay pacing so you center the wheel; ElevenLabs scores it.",
      "subDiscipline": "ceramic sculpting",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Generative music creates hypnotic, looping beats synchronized to pottery wheel rotation.",
      "megaPrompt": "Build \"Kiln Rhythm Spinner\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI plans your clay pacing so you center the wheel; ElevenLabs scores it.\nDiscipline: Visual Art (ceramic sculpting).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Generative music creates hypnotic, looping beats synchronized to pottery wheel rotation.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for ceramic sculpting. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for ceramic sculpting.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — ceramics and sculpting supplies",
      "sam": "$800M — professional ceramicists",
      "som": "$12M — studio pacing and rhythm aids"
    },
    {
      "id": "visual-art-layered-canvas-crescendo-6",
      "theme": "visual-art",
      "title": "Layered Canvas Crescendo",
      "pitch": "Lovable AI structures your drawing layers so the timelapse flows; ElevenLabs scores it.",
      "subDiscipline": "digital illustration",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Studio music generation builds dynamic soundtracks that evolve with digital drawing complexity.",
      "megaPrompt": "Build \"Layered Canvas Crescendo\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI structures your drawing layers so the timelapse flows; ElevenLabs scores it.\nDiscipline: Visual Art (digital illustration).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Studio music generation builds dynamic soundtracks that evolve with digital drawing complexity.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for digital illustration. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for digital illustration.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — digital art software market",
      "sam": "$2B — professional digital illustrators",
      "som": "$40M — timelapse and portfolio video tools"
    },
    {
      "id": "visual-art-critique-tension-resolver-7",
      "theme": "visual-art",
      "title": "Critique Tension Resolver",
      "pitch": "Lovable AI drafts constructive feedback so you improve your pieces; ElevenLabs scores it.",
      "subDiscipline": "portfolio review",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Music generation uses tension and resolution to emotionally frame the critique process.",
      "megaPrompt": "Build \"Critique Tension Resolver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI drafts constructive feedback so you improve your pieces; ElevenLabs scores it.\nDiscipline: Visual Art (portfolio review).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Music generation uses tension and resolution to emotionally frame the critique process.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for portfolio review. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for portfolio review.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — art school and mentoring",
      "sam": "$400M — emerging visual artists",
      "som": "$8M — AI portfolio coaching platforms"
    },
    {
      "id": "visual-art-prompt-harmony-checker-8",
      "theme": "visual-art",
      "title": "Prompt Harmony Checker",
      "pitch": "Lovable AI evaluates your text prompts so you get better outputs; ElevenLabs sounds it.",
      "subDiscipline": "generative AI art",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX provides immediate, non-verbal audio cues indicating prompt balance and complexity.",
      "megaPrompt": "Build \"Prompt Harmony Checker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI evaluates your text prompts so you get better outputs; ElevenLabs sounds it.\nDiscipline: Visual Art (generative AI art).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX provides immediate, non-verbal audio cues indicating prompt balance and complexity.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for generative AI art. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for generative AI art.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — generative AI software",
      "sam": "$1B — AI visual artists",
      "som": "$25M — prompt engineering helper tools"
    },
    {
      "id": "visual-art-figure-pose-metronome-9",
      "theme": "visual-art",
      "title": "Figure Pose Metronome",
      "pitch": "Lovable AI times the model's poses so you capture proportions; ElevenLabs scores it.",
      "subDiscipline": "life drawing",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Ambient music generation provides subtle, shifting audio cues for timed drawing sessions.",
      "megaPrompt": "Build \"Figure Pose Metronome\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI times the model's poses so you capture proportions; ElevenLabs scores it.\nDiscipline: Visual Art (life drawing).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Ambient music generation provides subtle, shifting audio cues for timed drawing sessions.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for life drawing. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for life drawing.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — life drawing studios",
      "sam": "$200M — figure drawing students",
      "som": "$4M — timed session management apps"
    },
    {
      "id": "visual-art-acid-bath-industrialist-10",
      "theme": "visual-art",
      "title": "Acid Bath Industrialist",
      "pitch": "Lovable AI sequences your press steps so you avoid mistakes; ElevenLabs sounds it.",
      "subDiscipline": "etching and printmaking",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation creates rhythmic, industrial audio matching the physical printmaking process.",
      "megaPrompt": "Build \"Acid Bath Industrialist\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI sequences your press steps so you avoid mistakes; ElevenLabs sounds it.\nDiscipline: Visual Art (etching and printmaking).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation creates rhythmic, industrial audio matching the physical printmaking process.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for etching and printmaking. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for etching and printmaking.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$500M — printmaking equipment",
      "sam": "$100M — professional printmakers",
      "som": "$2M — traditional craft workflow aids"
    },
    {
      "id": "visual-art-wet-wash-fluidity-11",
      "theme": "visual-art",
      "title": "Wet Wash Fluidity",
      "pitch": "Lovable AI predicts drying times so you layer washes perfectly; ElevenLabs scores it.",
      "subDiscipline": "watercolor painting",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Generative music uses fluid, harp-like tones to guide wet-on-wet timing.",
      "megaPrompt": "Build \"Wet Wash Fluidity\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI predicts drying times so you layer washes perfectly; ElevenLabs scores it.\nDiscipline: Visual Art (watercolor painting).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Generative music uses fluid, harp-like tones to guide wet-on-wet timing.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for watercolor painting. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for watercolor painting.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — watercolor supplies",
      "sam": "$300M — watercolor specialists",
      "som": "$6M — medium-specific painting guides"
    },
    {
      "id": "visual-art-epoch-echo-immersion-12",
      "theme": "visual-art",
      "title": "Epoch Echo Immersion",
      "pitch": "Lovable AI summarizes historical contexts so you paint with authenticity; ElevenLabs scores it.",
      "subDiscipline": "art history research",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Studio track generation produces era-accurate ambient music for historical immersion.",
      "megaPrompt": "Build \"Epoch Echo Immersion\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI summarizes historical contexts so you paint with authenticity; ElevenLabs scores it.\nDiscipline: Visual Art (art history research).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Studio track generation produces era-accurate ambient music for historical immersion.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for art history research. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for art history research.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — academic art history",
      "sam": "$500M — historical painters and researchers",
      "som": "$10M — immersive research companions"
    },
    {
      "id": "visual-art-stretcher-bar-asmr-13",
      "theme": "visual-art",
      "title": "Stretcher Bar ASMR",
      "pitch": "Lovable AI guides your stretching technique so the canvas is taut; ElevenLabs sounds it.",
      "subDiscipline": "canvas preparation",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation provides satisfying, tactile audio feedback for physical studio labor.",
      "megaPrompt": "Build \"Stretcher Bar ASMR\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI guides your stretching technique so the canvas is taut; ElevenLabs sounds it.\nDiscipline: Visual Art (canvas preparation).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation provides satisfying, tactile audio feedback for physical studio labor.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for canvas preparation. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for canvas preparation.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$800M — canvas and framing supplies",
      "sam": "$150M — painters who prep their own canvases",
      "som": "$3M — studio setup and prep guides"
    },
    {
      "id": "visual-art-scaffolding-epic-scorer-14",
      "theme": "visual-art",
      "title": "Scaffolding Epic Scorer",
      "pitch": "Lovable AI breaks down large compositions so you conquer the wall; ElevenLabs scores it.",
      "subDiscipline": "mural painting",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Cinematic music generation provides motivating, large-scale audio for monumental physical work.",
      "megaPrompt": "Build \"Scaffolding Epic Scorer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI breaks down large compositions so you conquer the wall; ElevenLabs scores it.\nDiscipline: Visual Art (mural painting).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Cinematic music generation provides motivating, large-scale audio for monumental physical work.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for mural painting. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for mural painting.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — public art and murals",
      "sam": "$200M — professional muralists",
      "som": "$5M — large-scale project management tools"
    },
    {
      "id": "visual-art-pigment-ratio-tuner-15",
      "theme": "visual-art",
      "title": "Pigment Ratio Tuner",
      "pitch": "Lovable AI calculates color proportions so you mix the perfect hue; ElevenLabs sounds it.",
      "subDiscipline": "palette mixing",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Generative SFX uses resolving audio chords to signal when color ratios are balanced.",
      "megaPrompt": "Build \"Pigment Ratio Tuner\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI calculates color proportions so you mix the perfect hue; ElevenLabs sounds it.\nDiscipline: Visual Art (palette mixing).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Generative SFX uses resolving audio chords to signal when color ratios are balanced.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for palette mixing. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for palette mixing.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — artist paints and pigments",
      "sam": "$400M — traditional painters",
      "som": "$8M — color mixing utility apps"
    },
    {
      "id": "visual-art-blank-canvas-unwinder-16",
      "theme": "visual-art",
      "title": "Blank Canvas Unwinder",
      "pitch": "Lovable AI reframes your creative anxieties so you overcome art block; ElevenLabs scores it.",
      "subDiscipline": "creative mindset",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Meditative music generation creates calming soundscapes derived from journaling prompts.",
      "megaPrompt": "Build \"Blank Canvas Unwinder\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI reframes your creative anxieties so you overcome art block; ElevenLabs scores it.\nDiscipline: Visual Art (creative mindset).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Meditative music generation creates calming soundscapes derived from journaling prompts.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for creative mindset. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for creative mindset.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — mental wellness and coaching",
      "sam": "$800M — creative professionals",
      "som": "$15M — mindfulness tools for artists"
    },
    {
      "id": "visual-art-graphite-texture-scraper-17",
      "theme": "visual-art",
      "title": "Graphite Texture Scraper",
      "pitch": "Lovable AI analyzes your value scales so you deepen the shadows; ElevenLabs sounds it.",
      "subDiscipline": "charcoal drawing",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation produces scratchy, textured audio responding to shading pressure and density.",
      "megaPrompt": "Build \"Graphite Texture Scraper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI analyzes your value scales so you deepen the shadows; ElevenLabs sounds it.\nDiscipline: Visual Art (charcoal drawing).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation produces scratchy, textured audio responding to shading pressure and density.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for charcoal drawing. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for charcoal drawing.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$600M — drawing and sketching supplies",
      "sam": "$120M — charcoal and graphite artists",
      "som": "$2.5M — drawing technique feedback tools"
    },
    {
      "id": "visual-art-vernissage-vibe-weaver-18",
      "theme": "visual-art",
      "title": "Vernissage Vibe Weaver",
      "pitch": "Lovable AI profiles the featured artist so gallerists set the mood; ElevenLabs scores it.",
      "subDiscipline": "gallery opening events",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Studio track generation creates custom, sophisticated lobby music for exhibition openings.",
      "megaPrompt": "Build \"Vernissage Vibe Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI profiles the featured artist so gallerists set the mood; ElevenLabs scores it.\nDiscipline: Visual Art (gallery opening events).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Studio track generation creates custom, sophisticated lobby music for exhibition openings.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for gallery opening events. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for gallery opening events.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — event and hospitality management",
      "sam": "$1B — art gallery events",
      "som": "$20M — bespoke event atmosphere tools"
    },
    {
      "id": "visual-art-micro-detail-focus-19",
      "theme": "visual-art",
      "title": "Micro Detail Focus",
      "pitch": "Lovable AI highlights degradation patterns so you restore safely; ElevenLabs scores it.",
      "subDiscipline": "art conservation",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Delicate ambient music generation maintains deep, unbroken focus during microscopic work.",
      "megaPrompt": "Build \"Micro Detail Focus\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI highlights degradation patterns so you restore safely; ElevenLabs scores it.\nDiscipline: Visual Art (art conservation).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Delicate ambient music generation maintains deep, unbroken focus during microscopic work.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for art conservation. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for art conservation.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — art conservation and restoration",
      "sam": "$400M — professional conservators",
      "som": "$8M — precision focus and analysis tools"
    },
    {
      "id": "visual-art-worldbuilding-ambience-forge-20",
      "theme": "visual-art",
      "title": "Worldbuilding Ambience Forge",
      "pitch": "Lovable AI expands your lore notes so you visualize the environment; ElevenLabs sounds it.",
      "subDiscipline": "concept art",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Environmental SFX generation builds the auditory world before the artist draws it.",
      "megaPrompt": "Build \"Worldbuilding Ambience Forge\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI expands your lore notes so you visualize the environment; ElevenLabs sounds it.\nDiscipline: Visual Art (concept art).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Environmental SFX generation builds the auditory world before the artist draws it.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for concept art. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for concept art.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — game and film pre-production",
      "sam": "$2B — concept artists",
      "som": "$40M — worldbuilding and ideation software"
    },
    {
      "id": "visual-art-kerning-cadence-tracker-21",
      "theme": "visual-art",
      "title": "Kerning Cadence Tracker",
      "pitch": "Lovable AI evaluates your stroke consistency so your letters align; ElevenLabs scores it.",
      "subDiscipline": "hand lettering",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Rhythmic music generation matches the physical cadence and flow of pen strokes.",
      "megaPrompt": "Build \"Kerning Cadence Tracker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI evaluates your stroke consistency so your letters align; ElevenLabs scores it.\nDiscipline: Visual Art (hand lettering).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Rhythmic music generation matches the physical cadence and flow of pen strokes.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for hand lettering. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for hand lettering.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — typography and calligraphy",
      "sam": "$300M — hand lettering artists",
      "som": "$6M — lettering practice and pacing aids"
    },
    {
      "id": "visual-art-mixed-media-organizer-22",
      "theme": "visual-art",
      "title": "Mixed Media Organizer",
      "pitch": "Lovable AI suggests compositional balance so your chaotic elements unify; ElevenLabs sounds it.",
      "subDiscipline": "collage art",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Layered SFX generation starts chaotic and organizes into harmony as the composition balances.",
      "megaPrompt": "Build \"Mixed Media Organizer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI suggests compositional balance so your chaotic elements unify; ElevenLabs sounds it.\nDiscipline: Visual Art (collage art).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Layered SFX generation starts chaotic and organizes into harmony as the composition balances.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for collage art. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for collage art.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$800M — mixed media and craft supplies",
      "sam": "$150M — collage and assemblage artists",
      "som": "$3M — compositional layout tools"
    },
    {
      "id": "visual-art-mortar-pestle-muller-23",
      "theme": "visual-art",
      "title": "Mortar Pestle Muller",
      "pitch": "Lovable AI tracks your grinding consistency so you achieve the right texture; ElevenLabs sounds it.",
      "subDiscipline": "paint making",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Raw, earthy SFX generation complements the tactile, physical process of milling pigments.",
      "megaPrompt": "Build \"Mortar Pestle Muller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI tracks your grinding consistency so you achieve the right texture; ElevenLabs sounds it.\nDiscipline: Visual Art (paint making).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Raw, earthy SFX generation complements the tactile, physical process of milling pigments.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for paint making. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for paint making.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$300M — raw art pigments and binders",
      "sam": "$50M — bespoke paint makers",
      "som": "$1M — traditional material crafting aids"
    },
    {
      "id": "visual-art-final-coat-triumph-24",
      "theme": "visual-art",
      "title": "Final Coat Triumph",
      "pitch": "Lovable AI verifies your drying times so you apply the varnish safely; ElevenLabs scores it.",
      "subDiscipline": "painting finishing",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Triumphant studio track generation provides a satisfying audio reward for completing a piece.",
      "megaPrompt": "Build \"Final Coat Triumph\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI verifies your drying times so you apply the varnish safely; ElevenLabs scores it.\nDiscipline: Visual Art (painting finishing).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Triumphant studio track generation provides a satisfying audio reward for completing a piece.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for painting finishing. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for painting finishing.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$500M — art finishing and framing supplies",
      "sam": "$100M — professional painters",
      "som": "$2M — studio workflow completion tools"
    }
  ]
}