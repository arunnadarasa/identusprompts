{
  "theme": {
    "slug": "theater",
    "name": "Theater & Live Performance",
    "emoji": "🎭",
    "audience": "directors, playwrights, performers, lighting and stage designers",
    "market_anchor": "the live performance market (~$30B globally) with >100K active companies"
  },
  "ideas": [
    {
      "id": "theater-scriptchain-ledger-0",
      "theme": "theater",
      "title": "ScriptChain audio logs",
      "pitch": "Securely track and timestamp script drafts through an immutable ElevenLabs audio logs.",
      "subDiscipline": "playwright collaboration",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Streaming text-to-speech turns every playwright collaboration surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.",
      "megaPrompt": "Build \"ScriptChain audio logs\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSecurely track and timestamp script drafts through an immutable ElevenLabs audio logs.\nDiscipline: Theater & Live Performance (playwright collaboration).\nVoice primitive: ElevenLabs Text-to-Speech (streaming voice).\nWhy this primitive: Streaming text-to-speech turns every playwright collaboration surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/tts.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speak = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ text: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: {\n          \"xi-api-key\": process.env.ELEVENLABS_API_KEY!,\n          \"Content-Type\": \"application/json\",\n        },\n        body: JSON.stringify({ text: data.text, model_id: \"eleven_turbo_v2_5\" }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speak } from \"@/lib/tts.functions\";\n\nconst tts = useServerFn(speak);\nconst play = async (text: string) => {\n  const { audio } = await tts({ data: { text } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVOICE: George (`JBFqnCBsd6RMkjVDRZzb`) is the default; swap for any voice id from\nhttps://elevenlabs.io/voice-library. Use `eleven_multilingual_v2` instead of\n`eleven_turbo_v2_5` when broadcast quality matters more than first-token latency.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for playwright collaboration.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — global digital scriptwriting tools market",
      "sam": "$200M — playwright collaboration software segment",
      "som": "$50M — early-adopting theater companies using voice-app for IP protection"
    },
    {
      "id": "theater-roleauction-platform-1",
      "theme": "theater",
      "title": "RoleAuction Platform",
      "pitch": "shared audition bids let performers compete transparently for roles via voice prompts.",
      "subDiscipline": "casting marketplace",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Streaming text-to-speech turns every casting marketplace surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.",
      "megaPrompt": "Build \"RoleAuction Platform\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nshared audition bids let performers compete transparently for roles via voice prompts.\nDiscipline: Theater & Live Performance (casting marketplace).\nVoice primitive: ElevenLabs Text-to-Speech (streaming voice).\nWhy this primitive: Streaming text-to-speech turns every casting marketplace surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/tts.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speak = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ text: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: {\n          \"xi-api-key\": process.env.ELEVENLABS_API_KEY!,\n          \"Content-Type\": \"application/json\",\n        },\n        body: JSON.stringify({ text: data.text, model_id: \"eleven_turbo_v2_5\" }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speak } from \"@/lib/tts.functions\";\n\nconst tts = useServerFn(speak);\nconst play = async (text: string) => {\n  const { audio } = await tts({ data: { text } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVOICE: George (`JBFqnCBsd6RMkjVDRZzb`) is the default; swap for any voice id from\nhttps://elevenlabs.io/voice-library. Use `eleven_multilingual_v2` instead of\n`eleven_turbo_v2_5` when broadcast quality matters more than first-token latency.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for casting marketplace.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — global casting agency market",
      "sam": "$500M — online casting platforms",
      "som": "$100M — theater companies adopting voice-app casting tools"
    },
    {
      "id": "theater-stagelight-token-2",
      "theme": "theater",
      "title": "StageLight voice clips",
      "pitch": "Issue and trade usage rights for lighting designs using on-record voice clips to protect creators.",
      "subDiscipline": "lighting rights management",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Streaming text-to-speech turns every lighting rights management surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.",
      "megaPrompt": "Build \"StageLight voice clips\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nIssue and trade usage rights for lighting designs using on-record voice clips to protect creators.\nDiscipline: Theater & Live Performance (lighting rights management).\nVoice primitive: ElevenLabs Text-to-Speech (streaming voice).\nWhy this primitive: Streaming text-to-speech turns every lighting rights management surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/tts.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speak = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ text: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: {\n          \"xi-api-key\": process.env.ELEVENLABS_API_KEY!,\n          \"Content-Type\": \"application/json\",\n        },\n        body: JSON.stringify({ text: data.text, model_id: \"eleven_turbo_v2_5\" }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speak } from \"@/lib/tts.functions\";\n\nconst tts = useServerFn(speak);\nconst play = async (text: string) => {\n  const { audio } = await tts({ data: { text } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVOICE: George (`JBFqnCBsd6RMkjVDRZzb`) is the default; swap for any voice id from\nhttps://elevenlabs.io/voice-library. Use `eleven_multilingual_v2` instead of\n`eleven_turbo_v2_5` when broadcast quality matters more than first-token latency.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for lighting rights management.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — theater lighting equipment market",
      "sam": "$400M — lighting design intellectual property segment",
      "som": "$80M — digital rights management adoption in live performance"
    },
    {
      "id": "theater-promptchain-impro-3",
      "theme": "theater",
      "title": "PromptChain Impro",
      "pitch": "Generate and track custom improv prompts securely stored and shared via voice prompts.",
      "subDiscipline": "improvisation prompts",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Streaming text-to-speech turns every improvisation prompts surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.",
      "megaPrompt": "Build \"PromptChain Impro\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nGenerate and track custom improv prompts securely stored and shared via voice prompts.\nDiscipline: Theater & Live Performance (improvisation prompts).\nVoice primitive: ElevenLabs Text-to-Speech (streaming voice).\nWhy this primitive: Streaming text-to-speech turns every improvisation prompts surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/tts.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speak = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ text: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: {\n          \"xi-api-key\": process.env.ELEVENLABS_API_KEY!,\n          \"Content-Type\": \"application/json\",\n        },\n        body: JSON.stringify({ text: data.text, model_id: \"eleven_turbo_v2_5\" }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speak } from \"@/lib/tts.functions\";\n\nconst tts = useServerFn(speak);\nconst play = async (text: string) => {\n  const { audio } = await tts({ data: { text } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVOICE: George (`JBFqnCBsd6RMkjVDRZzb`) is the default; swap for any voice id from\nhttps://elevenlabs.io/voice-library. Use `eleven_multilingual_v2` instead of\n`eleven_turbo_v2_5` when broadcast quality matters more than first-token latency.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for improvisation prompts.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$500M — improv theater training market",
      "sam": "$100M — improv prompt tools and apps",
      "som": "$20M — improv troupes experimenting with voice-app prompts"
    },
    {
      "id": "theater-tickettrust-nft-4",
      "theme": "theater",
      "title": "TicketTrust voice asset",
      "pitch": "Use ElevenLabs to publish tamper-proof voice asset tickets preventing counterfeiting for live performances.",
      "subDiscipline": "ticket authenticity",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Streaming text-to-speech turns every ticket authenticity surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.",
      "megaPrompt": "Build \"TicketTrust voice asset\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nUse ElevenLabs to publish tamper-proof voice asset tickets preventing counterfeiting for live performances.\nDiscipline: Theater & Live Performance (ticket authenticity).\nVoice primitive: ElevenLabs Text-to-Speech (streaming voice).\nWhy this primitive: Streaming text-to-speech turns every ticket authenticity surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/tts.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speak = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ text: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: {\n          \"xi-api-key\": process.env.ELEVENLABS_API_KEY!,\n          \"Content-Type\": \"application/json\",\n        },\n        body: JSON.stringify({ text: data.text, model_id: \"eleven_turbo_v2_5\" }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speak } from \"@/lib/tts.functions\";\n\nconst tts = useServerFn(speak);\nconst play = async (text: string) => {\n  const { audio } = await tts({ data: { text } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVOICE: George (`JBFqnCBsd6RMkjVDRZzb`) is the default; swap for any voice id from\nhttps://elevenlabs.io/voice-library. Use `eleven_multilingual_v2` instead of\n`eleven_turbo_v2_5` when broadcast quality matters more than first-token latency.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for ticket authenticity.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$15B — global ticketing market",
      "sam": "$3B — digital ticketing platforms",
      "som": "$600M — voice-app ticket adoption in theaters and venues"
    },
    {
      "id": "theater-propchain-registry-5",
      "theme": "theater",
      "title": "PropChain Registry",
      "pitch": "Record and transfer ownership history of stage props transparently on a ElevenLabs registry.",
      "subDiscipline": "prop ownership",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Streaming text-to-speech turns every prop ownership surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.",
      "megaPrompt": "Build \"PropChain Registry\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nRecord and transfer ownership history of stage props transparently on a ElevenLabs registry.\nDiscipline: Theater & Live Performance (prop ownership).\nVoice primitive: ElevenLabs Text-to-Speech (streaming voice).\nWhy this primitive: Streaming text-to-speech turns every prop ownership surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/tts.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speak = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ text: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: {\n          \"xi-api-key\": process.env.ELEVENLABS_API_KEY!,\n          \"Content-Type\": \"application/json\",\n        },\n        body: JSON.stringify({ text: data.text, model_id: \"eleven_turbo_v2_5\" }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speak } from \"@/lib/tts.functions\";\n\nconst tts = useServerFn(speak);\nconst play = async (text: string) => {\n  const { audio } = await tts({ data: { text } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVOICE: George (`JBFqnCBsd6RMkjVDRZzb`) is the default; swap for any voice id from\nhttps://elevenlabs.io/voice-library. Use `eleven_multilingual_v2` instead of\n`eleven_turbo_v2_5` when broadcast quality matters more than first-token latency.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for prop ownership.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — theatrical prop rental and sales market",
      "sam": "$200M — prop inventory management systems",
      "som": "$40M — prop companies using voice-app asset tracking"
    },
    {
      "id": "theater-scriptroyalty-split-6",
      "theme": "theater",
      "title": "ScriptRoyalty Split",
      "pitch": "Automate fair payouts to playwrights, directors, and actors using on-record credit splits splits.",
      "subDiscipline": "royalty distribution",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Streaming text-to-speech turns every royalty distribution surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.",
      "megaPrompt": "Build \"ScriptRoyalty Split\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAutomate fair payouts to playwrights, directors, and actors using on-record credit splits splits.\nDiscipline: Theater & Live Performance (royalty distribution).\nVoice primitive: ElevenLabs Text-to-Speech (streaming voice).\nWhy this primitive: Streaming text-to-speech turns every royalty distribution surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/tts.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speak = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ text: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: {\n          \"xi-api-key\": process.env.ELEVENLABS_API_KEY!,\n          \"Content-Type\": \"application/json\",\n        },\n        body: JSON.stringify({ text: data.text, model_id: \"eleven_turbo_v2_5\" }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speak } from \"@/lib/tts.functions\";\n\nconst tts = useServerFn(speak);\nconst play = async (text: string) => {\n  const { audio } = await tts({ data: { text } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVOICE: George (`JBFqnCBsd6RMkjVDRZzb`) is the default; swap for any voice id from\nhttps://elevenlabs.io/voice-library. Use `eleven_multilingual_v2` instead of\n`eleven_turbo_v2_5` when broadcast quality matters more than first-token latency.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for royalty distribution.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — global live performance revenue",
      "sam": "$2B — royalty management software market",
      "som": "$400M — theater companies seeking automated royalty solutions"
    },
    {
      "id": "theater-stagecrew-dao-7",
      "theme": "theater",
      "title": "StageCrew voice collective",
      "pitch": "Decentralize stage crew decisions and resource allocation through a transparent voice collective governed by voice prompts.",
      "subDiscipline": "crew coordination",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Streaming text-to-speech turns every crew coordination surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.",
      "megaPrompt": "Build \"StageCrew voice collective\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDecentralize stage crew decisions and resource allocation through a transparent voice collective governed by voice prompts.\nDiscipline: Theater & Live Performance (crew coordination).\nVoice primitive: ElevenLabs Text-to-Speech (streaming voice).\nWhy this primitive: Streaming text-to-speech turns every crew coordination surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/tts.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speak = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ text: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: {\n          \"xi-api-key\": process.env.ELEVENLABS_API_KEY!,\n          \"Content-Type\": \"application/json\",\n        },\n        body: JSON.stringify({ text: data.text, model_id: \"eleven_turbo_v2_5\" }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speak } from \"@/lib/tts.functions\";\n\nconst tts = useServerFn(speak);\nconst play = async (text: string) => {\n  const { audio } = await tts({ data: { text } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVOICE: George (`JBFqnCBsd6RMkjVDRZzb`) is the default; swap for any voice id from\nhttps://elevenlabs.io/voice-library. Use `eleven_multilingual_v2` instead of\n`eleven_turbo_v2_5` when broadcast quality matters more than first-token latency.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for crew coordination.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$500M — stage crew management software",
      "sam": "$100M — decentralized team coordination tools",
      "som": "$25M — theater companies piloting DAO-based crew workflows"
    },
    {
      "id": "theater-playmint-nft-8",
      "theme": "theater",
      "title": "PlayMint voice asset",
      "pitch": "publish unique digital editions of scripts as voice assets to protect intellectual property and track sales.",
      "subDiscipline": "script NFTs",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Streaming text-to-speech turns every script NFTs surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.",
      "megaPrompt": "Build \"PlayMint voice asset\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\npublish unique digital editions of scripts as voice assets to protect intellectual property and track sales.\nDiscipline: Theater & Live Performance (script NFTs).\nVoice primitive: ElevenLabs Text-to-Speech (streaming voice).\nWhy this primitive: Streaming text-to-speech turns every script NFTs surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/tts.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speak = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ text: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: {\n          \"xi-api-key\": process.env.ELEVENLABS_API_KEY!,\n          \"Content-Type\": \"application/json\",\n        },\n        body: JSON.stringify({ text: data.text, model_id: \"eleven_turbo_v2_5\" }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speak } from \"@/lib/tts.functions\";\n\nconst tts = useServerFn(speak);\nconst play = async (text: string) => {\n  const { audio } = await tts({ data: { text } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVOICE: George (`JBFqnCBsd6RMkjVDRZzb`) is the default; swap for any voice id from\nhttps://elevenlabs.io/voice-library. Use `eleven_multilingual_v2` instead of\n`eleven_turbo_v2_5` when broadcast quality matters more than first-token latency.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for script NFTs.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$800M — digital publishing for theater",
      "sam": "$150M — NFT market for creative scripts",
      "som": "$30M — theatrical NFT experiments in IP protection"
    },
    {
      "id": "theater-actorstake-platform-9",
      "theme": "theater",
      "title": "ActorStake Platform",
      "pitch": "Allow fans to stake voice clips on actors’ performances, rewarding success with ElevenLabs incentives.",
      "subDiscipline": "performance staking",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Streaming text-to-speech turns every performance staking surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.",
      "megaPrompt": "Build \"ActorStake Platform\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAllow fans to stake voice clips on actors’ performances, rewarding success with ElevenLabs incentives.\nDiscipline: Theater & Live Performance (performance staking).\nVoice primitive: ElevenLabs Text-to-Speech (streaming voice).\nWhy this primitive: Streaming text-to-speech turns every performance staking surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/tts.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speak = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ text: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: {\n          \"xi-api-key\": process.env.ELEVENLABS_API_KEY!,\n          \"Content-Type\": \"application/json\",\n        },\n        body: JSON.stringify({ text: data.text, model_id: \"eleven_turbo_v2_5\" }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speak } from \"@/lib/tts.functions\";\n\nconst tts = useServerFn(speak);\nconst play = async (text: string) => {\n  const { audio } = await tts({ data: { text } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVOICE: George (`JBFqnCBsd6RMkjVDRZzb`) is the default; swap for any voice id from\nhttps://elevenlabs.io/voice-library. Use `eleven_multilingual_v2` instead of\n`eleven_turbo_v2_5` when broadcast quality matters more than first-token latency.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for performance staking.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — live performance fan engagement market",
      "sam": "$400M — voice-app fan staking platforms",
      "som": "$80M — theaters exploring tokenized fan incentives"
    },
    {
      "id": "theater-lightcue-automation-10",
      "theme": "theater",
      "title": "LightCue Automation",
      "pitch": "Record and trigger lighting cues on-record to verify show timing and enable automated playback.",
      "subDiscipline": "lighting cue logs",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Streaming text-to-speech turns every lighting cue logs surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.",
      "megaPrompt": "Build \"LightCue Automation\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nRecord and trigger lighting cues on-record to verify show timing and enable automated playback.\nDiscipline: Theater & Live Performance (lighting cue logs).\nVoice primitive: ElevenLabs Text-to-Speech (streaming voice).\nWhy this primitive: Streaming text-to-speech turns every lighting cue logs surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/tts.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speak = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ text: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: {\n          \"xi-api-key\": process.env.ELEVENLABS_API_KEY!,\n          \"Content-Type\": \"application/json\",\n        },\n        body: JSON.stringify({ text: data.text, model_id: \"eleven_turbo_v2_5\" }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speak } from \"@/lib/tts.functions\";\n\nconst tts = useServerFn(speak);\nconst play = async (text: string) => {\n  const { audio } = await tts({ data: { text } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVOICE: George (`JBFqnCBsd6RMkjVDRZzb`) is the default; swap for any voice id from\nhttps://elevenlabs.io/voice-library. Use `eleven_multilingual_v2` instead of\n`eleven_turbo_v2_5` when broadcast quality matters more than first-token latency.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for lighting cue logs.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — live lighting control market",
      "sam": "$300M — automated lighting cue systems",
      "som": "$60M — theaters integrating voice-app control for lights"
    },
    {
      "id": "theater-scenechange-nfts-11",
      "theme": "theater",
      "title": "SceneChange voice assets",
      "pitch": "Artists publish voice asset certificates of original scene designs for resale and licensing.",
      "subDiscipline": "scene ownership",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Streaming text-to-speech turns every scene ownership surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.",
      "megaPrompt": "Build \"SceneChange voice assets\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nArtists publish voice asset certificates of original scene designs for resale and licensing.\nDiscipline: Theater & Live Performance (scene ownership).\nVoice primitive: ElevenLabs Text-to-Speech (streaming voice).\nWhy this primitive: Streaming text-to-speech turns every scene ownership surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/tts.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speak = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ text: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: {\n          \"xi-api-key\": process.env.ELEVENLABS_API_KEY!,\n          \"Content-Type\": \"application/json\",\n        },\n        body: JSON.stringify({ text: data.text, model_id: \"eleven_turbo_v2_5\" }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speak } from \"@/lib/tts.functions\";\n\nconst tts = useServerFn(speak);\nconst play = async (text: string) => {\n  const { audio } = await tts({ data: { text } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVOICE: George (`JBFqnCBsd6RMkjVDRZzb`) is the default; swap for any voice id from\nhttps://elevenlabs.io/voice-library. Use `eleven_multilingual_v2` instead of\n`eleven_turbo_v2_5` when broadcast quality matters more than first-token latency.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for scene ownership.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$700M — stage design market",
      "sam": "$150M — digital licensing of scene concepts",
      "som": "$35M — scene designers trialing voice-app rights"
    },
    {
      "id": "theater-playbill-ledger-12",
      "theme": "theater",
      "title": "Playbill audio logs",
      "pitch": "Securely record production credits and changes transparently on a public audio logs.",
      "subDiscipline": "production documentation",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Streaming text-to-speech turns every production documentation surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.",
      "megaPrompt": "Build \"Playbill audio logs\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSecurely record production credits and changes transparently on a public audio logs.\nDiscipline: Theater & Live Performance (production documentation).\nVoice primitive: ElevenLabs Text-to-Speech (streaming voice).\nWhy this primitive: Streaming text-to-speech turns every production documentation surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/tts.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speak = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ text: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: {\n          \"xi-api-key\": process.env.ELEVENLABS_API_KEY!,\n          \"Content-Type\": \"application/json\",\n        },\n        body: JSON.stringify({ text: data.text, model_id: \"eleven_turbo_v2_5\" }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speak } from \"@/lib/tts.functions\";\n\nconst tts = useServerFn(speak);\nconst play = async (text: string) => {\n  const { audio } = await tts({ data: { text } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVOICE: George (`JBFqnCBsd6RMkjVDRZzb`) is the default; swap for any voice id from\nhttps://elevenlabs.io/voice-library. Use `eleven_multilingual_v2` instead of\n`eleven_turbo_v2_5` when broadcast quality matters more than first-token latency.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for production documentation.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$400M — playbill and program publishing",
      "sam": "$80M — production documentation services",
      "som": "$15M — theaters innovating version control with voice-app "
    },
    {
      "id": "theater-audiencevote-dao-13",
      "theme": "theater",
      "title": "AudienceVote voice collective",
      "pitch": "Collect and tally audience votes on performances in a transparent, tamper-proof voice collective system.",
      "subDiscipline": "live feedback",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Streaming text-to-speech turns every live feedback surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.",
      "megaPrompt": "Build \"AudienceVote voice collective\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCollect and tally audience votes on performances in a transparent, tamper-proof voice collective system.\nDiscipline: Theater & Live Performance (live feedback).\nVoice primitive: ElevenLabs Text-to-Speech (streaming voice).\nWhy this primitive: Streaming text-to-speech turns every live feedback surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/tts.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speak = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ text: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: {\n          \"xi-api-key\": process.env.ELEVENLABS_API_KEY!,\n          \"Content-Type\": \"application/json\",\n        },\n        body: JSON.stringify({ text: data.text, model_id: \"eleven_turbo_v2_5\" }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speak } from \"@/lib/tts.functions\";\n\nconst tts = useServerFn(speak);\nconst play = async (text: string) => {\n  const { audio } = await tts({ data: { text } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVOICE: George (`JBFqnCBsd6RMkjVDRZzb`) is the default; swap for any voice id from\nhttps://elevenlabs.io/voice-library. Use `eleven_multilingual_v2` instead of\n`eleven_turbo_v2_5` when broadcast quality matters more than first-token latency.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for live feedback.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — audience engagement tools",
      "sam": "$200M — live voting and polling platforms",
      "som": "$40M — theaters adopting voice-app audience feedback"
    },
    {
      "id": "theater-backstage-token-14",
      "theme": "theater",
      "title": "Backstage voice clips",
      "pitch": "Issue voice clips granting secure, on-record backstage access for staff and VIPs.",
      "subDiscipline": "access control",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Streaming text-to-speech turns every access control surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.",
      "megaPrompt": "Build \"Backstage voice clips\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nIssue voice clips granting secure, on-record backstage access for staff and VIPs.\nDiscipline: Theater & Live Performance (access control).\nVoice primitive: ElevenLabs Text-to-Speech (streaming voice).\nWhy this primitive: Streaming text-to-speech turns every access control surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/tts.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speak = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ text: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: {\n          \"xi-api-key\": process.env.ELEVENLABS_API_KEY!,\n          \"Content-Type\": \"application/json\",\n        },\n        body: JSON.stringify({ text: data.text, model_id: \"eleven_turbo_v2_5\" }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speak } from \"@/lib/tts.functions\";\n\nconst tts = useServerFn(speak);\nconst play = async (text: string) => {\n  const { audio } = await tts({ data: { text } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVOICE: George (`JBFqnCBsd6RMkjVDRZzb`) is the default; swap for any voice id from\nhttps://elevenlabs.io/voice-library. Use `eleven_multilingual_v2` instead of\n`eleven_turbo_v2_5` when broadcast quality matters more than first-token latency.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for access control.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$800M — access management market",
      "sam": "$160M — digital credentialing in live events",
      "som": "$32M — theaters piloting token-based backstage passes"
    },
    {
      "id": "theater-rehearsallog-chain-15",
      "theme": "theater",
      "title": "RehearsalLog Chain",
      "pitch": "Immutably store rehearsal attendance and notes to improve production accountability.",
      "subDiscipline": "rehearsal tracking",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Streaming text-to-speech turns every rehearsal tracking surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.",
      "megaPrompt": "Build \"RehearsalLog Chain\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nImmutably store rehearsal attendance and notes to improve production accountability.\nDiscipline: Theater & Live Performance (rehearsal tracking).\nVoice primitive: ElevenLabs Text-to-Speech (streaming voice).\nWhy this primitive: Streaming text-to-speech turns every rehearsal tracking surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/tts.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speak = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ text: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: {\n          \"xi-api-key\": process.env.ELEVENLABS_API_KEY!,\n          \"Content-Type\": \"application/json\",\n        },\n        body: JSON.stringify({ text: data.text, model_id: \"eleven_turbo_v2_5\" }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speak } from \"@/lib/tts.functions\";\n\nconst tts = useServerFn(speak);\nconst play = async (text: string) => {\n  const { audio } = await tts({ data: { text } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVOICE: George (`JBFqnCBsd6RMkjVDRZzb`) is the default; swap for any voice id from\nhttps://elevenlabs.io/voice-library. Use `eleven_multilingual_v2` instead of\n`eleven_turbo_v2_5` when broadcast quality matters more than first-token latency.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for rehearsal tracking.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$300M — rehearsal management tools",
      "sam": "$60M — production workflow tracking software",
      "som": "$10M — voice-app adoption for rehearsal logs"
    },
    {
      "id": "theater-scriptlens-dao-16",
      "theme": "theater",
      "title": "ScriptLens voice collective",
      "pitch": "Enable playwrights and directors to co-own scripts via shared governance and revenue sharing.",
      "subDiscipline": "script co-creation",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Streaming text-to-speech turns every script co-creation surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.",
      "megaPrompt": "Build \"ScriptLens voice collective\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nEnable playwrights and directors to co-own scripts via shared governance and revenue sharing.\nDiscipline: Theater & Live Performance (script co-creation).\nVoice primitive: ElevenLabs Text-to-Speech (streaming voice).\nWhy this primitive: Streaming text-to-speech turns every script co-creation surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/tts.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speak = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ text: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: {\n          \"xi-api-key\": process.env.ELEVENLABS_API_KEY!,\n          \"Content-Type\": \"application/json\",\n        },\n        body: JSON.stringify({ text: data.text, model_id: \"eleven_turbo_v2_5\" }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speak } from \"@/lib/tts.functions\";\n\nconst tts = useServerFn(speak);\nconst play = async (text: string) => {\n  const { audio } = await tts({ data: { text } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVOICE: George (`JBFqnCBsd6RMkjVDRZzb`) is the default; swap for any voice id from\nhttps://elevenlabs.io/voice-library. Use `eleven_multilingual_v2` instead of\n`eleven_turbo_v2_5` when broadcast quality matters more than first-token latency.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for script co-creation.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — collaborative writing software",
      "sam": "$200M — decentralized creative platforms",
      "som": "$40M — theater groups experimenting with co-creation DAOs"
    },
    {
      "id": "theater-sceneswap-marketplace-17",
      "theme": "theater",
      "title": "SceneSwap Marketplace",
      "pitch": "Trade and license scene and set designs through a verified on-record marketplace.",
      "subDiscipline": "design exchange",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Streaming text-to-speech turns every design exchange surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.",
      "megaPrompt": "Build \"SceneSwap Marketplace\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTrade and license scene and set designs through a verified on-record marketplace.\nDiscipline: Theater & Live Performance (design exchange).\nVoice primitive: ElevenLabs Text-to-Speech (streaming voice).\nWhy this primitive: Streaming text-to-speech turns every design exchange surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/tts.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speak = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ text: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: {\n          \"xi-api-key\": process.env.ELEVENLABS_API_KEY!,\n          \"Content-Type\": \"application/json\",\n        },\n        body: JSON.stringify({ text: data.text, model_id: \"eleven_turbo_v2_5\" }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speak } from \"@/lib/tts.functions\";\n\nconst tts = useServerFn(speak);\nconst play = async (text: string) => {\n  const { audio } = await tts({ data: { text } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVOICE: George (`JBFqnCBsd6RMkjVDRZzb`) is the default; swap for any voice id from\nhttps://elevenlabs.io/voice-library. Use `eleven_multilingual_v2` instead of\n`eleven_turbo_v2_5` when broadcast quality matters more than first-token latency.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for design exchange.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.2B — set design and rental market",
      "sam": "$250M — digital creative marketplaces",
      "som": "$50M — early adopters in voice-app design exchange"
    },
    {
      "id": "theater-performerbadge-nft-18",
      "theme": "theater",
      "title": "PerformerBadge voice asset",
      "pitch": "publish verifiable voice asset badges certifying performer skills and achievements.",
      "subDiscipline": "credentials verification",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Streaming text-to-speech turns every credentials verification surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.",
      "megaPrompt": "Build \"PerformerBadge voice asset\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\npublish verifiable voice asset badges certifying performer skills and achievements.\nDiscipline: Theater & Live Performance (credentials verification).\nVoice primitive: ElevenLabs Text-to-Speech (streaming voice).\nWhy this primitive: Streaming text-to-speech turns every credentials verification surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/tts.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speak = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ text: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: {\n          \"xi-api-key\": process.env.ELEVENLABS_API_KEY!,\n          \"Content-Type\": \"application/json\",\n        },\n        body: JSON.stringify({ text: data.text, model_id: \"eleven_turbo_v2_5\" }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speak } from \"@/lib/tts.functions\";\n\nconst tts = useServerFn(speak);\nconst play = async (text: string) => {\n  const { audio } = await tts({ data: { text } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVOICE: George (`JBFqnCBsd6RMkjVDRZzb`) is the default; swap for any voice id from\nhttps://elevenlabs.io/voice-library. Use `eleven_multilingual_v2` instead of\n`eleven_turbo_v2_5` when broadcast quality matters more than first-token latency.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for credentials verification.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$700M — talent credentialing market",
      "sam": "$140M — digital certification platforms",
      "som": "$28M — talent agencies piloting voice-app verification"
    },
    {
      "id": "theater-lightfx-tokenize-19",
      "theme": "theater",
      "title": "LightFX Tokenize",
      "pitch": "Tokenize and license stage special effects designs to productions worldwide securely.",
      "subDiscipline": "special effects licensing",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Streaming text-to-speech turns every special effects licensing surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.",
      "megaPrompt": "Build \"LightFX Tokenize\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTokenize and license stage special effects designs to productions worldwide securely.\nDiscipline: Theater & Live Performance (special effects licensing).\nVoice primitive: ElevenLabs Text-to-Speech (streaming voice).\nWhy this primitive: Streaming text-to-speech turns every special effects licensing surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/tts.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speak = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ text: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: {\n          \"xi-api-key\": process.env.ELEVENLABS_API_KEY!,\n          \"Content-Type\": \"application/json\",\n        },\n        body: JSON.stringify({ text: data.text, model_id: \"eleven_turbo_v2_5\" }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speak } from \"@/lib/tts.functions\";\n\nconst tts = useServerFn(speak);\nconst play = async (text: string) => {\n  const { audio } = await tts({ data: { text } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVOICE: George (`JBFqnCBsd6RMkjVDRZzb`) is the default; swap for any voice id from\nhttps://elevenlabs.io/voice-library. Use `eleven_multilingual_v2` instead of\n`eleven_turbo_v2_5` when broadcast quality matters more than first-token latency.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for special effects licensing.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$900M — live performance special effects market",
      "sam": "$180M — digital licensing of effects designs",
      "som": "$36M — special effects designers adopting voice-app "
    },
    {
      "id": "theater-caststake-dao-20",
      "theme": "theater",
      "title": "CastStake voice collective",
      "pitch": "Decentralize theater production funding by letting stakeholders vote and invest through voice collective voice clips.",
      "subDiscipline": "fundraising",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Streaming text-to-speech turns every fundraising surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.",
      "megaPrompt": "Build \"CastStake voice collective\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDecentralize theater production funding by letting stakeholders vote and invest through voice collective voice clips.\nDiscipline: Theater & Live Performance (fundraising).\nVoice primitive: ElevenLabs Text-to-Speech (streaming voice).\nWhy this primitive: Streaming text-to-speech turns every fundraising surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/tts.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speak = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ text: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: {\n          \"xi-api-key\": process.env.ELEVENLABS_API_KEY!,\n          \"Content-Type\": \"application/json\",\n        },\n        body: JSON.stringify({ text: data.text, model_id: \"eleven_turbo_v2_5\" }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speak } from \"@/lib/tts.functions\";\n\nconst tts = useServerFn(speak);\nconst play = async (text: string) => {\n  const { audio } = await tts({ data: { text } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVOICE: George (`JBFqnCBsd6RMkjVDRZzb`) is the default; swap for any voice id from\nhttps://elevenlabs.io/voice-library. Use `eleven_multilingual_v2` instead of\n`eleven_turbo_v2_5` when broadcast quality matters more than first-token latency.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for fundraising.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — theater production financing market",
      "sam": "$1B — crowdfunding platforms for arts",
      "som": "$200M — voice-app powered arts fundraising pilots"
    },
    {
      "id": "theater-propstoken-swap-21",
      "theme": "theater",
      "title": "PropsToken Swap",
      "pitch": "Create a tokenized system for lending and borrowing props between theater companies.",
      "subDiscipline": "collaborative prop lending",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Streaming text-to-speech turns every collaborative prop lending surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.",
      "megaPrompt": "Build \"PropsToken Swap\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCreate a tokenized system for lending and borrowing props between theater companies.\nDiscipline: Theater & Live Performance (collaborative prop lending).\nVoice primitive: ElevenLabs Text-to-Speech (streaming voice).\nWhy this primitive: Streaming text-to-speech turns every collaborative prop lending surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/tts.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speak = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ text: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: {\n          \"xi-api-key\": process.env.ELEVENLABS_API_KEY!,\n          \"Content-Type\": \"application/json\",\n        },\n        body: JSON.stringify({ text: data.text, model_id: \"eleven_turbo_v2_5\" }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speak } from \"@/lib/tts.functions\";\n\nconst tts = useServerFn(speak);\nconst play = async (text: string) => {\n  const { audio } = await tts({ data: { text } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVOICE: George (`JBFqnCBsd6RMkjVDRZzb`) is the default; swap for any voice id from\nhttps://elevenlabs.io/voice-library. Use `eleven_multilingual_v2` instead of\n`eleven_turbo_v2_5` when broadcast quality matters more than first-token latency.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for collaborative prop lending.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — theatrical prop rental market",
      "sam": "$200M — shared asset management platforms",
      "som": "$40M — prop rental companies using voice-app tokens"
    },
    {
      "id": "theater-stagesound-dao-22",
      "theme": "theater",
      "title": "StageSound voice collective",
      "pitch": "Coordinate and monetize collective sound design projects with transparent voice collectives and revenue splits.",
      "subDiscipline": "sound design collaboration",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Streaming text-to-speech turns every sound design collaboration surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.",
      "megaPrompt": "Build \"StageSound voice collective\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCoordinate and monetize collective sound design projects with transparent voice collectives and revenue splits.\nDiscipline: Theater & Live Performance (sound design collaboration).\nVoice primitive: ElevenLabs Text-to-Speech (streaming voice).\nWhy this primitive: Streaming text-to-speech turns every sound design collaboration surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/tts.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speak = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ text: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: {\n          \"xi-api-key\": process.env.ELEVENLABS_API_KEY!,\n          \"Content-Type\": \"application/json\",\n        },\n        body: JSON.stringify({ text: data.text, model_id: \"eleven_turbo_v2_5\" }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speak } from \"@/lib/tts.functions\";\n\nconst tts = useServerFn(speak);\nconst play = async (text: string) => {\n  const { audio } = await tts({ data: { text } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVOICE: George (`JBFqnCBsd6RMkjVDRZzb`) is the default; swap for any voice id from\nhttps://elevenlabs.io/voice-library. Use `eleven_multilingual_v2` instead of\n`eleven_turbo_v2_5` when broadcast quality matters more than first-token latency.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for sound design collaboration.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.2B — live sound design market",
      "sam": "$250M — collaborative music and sound platforms",
      "som": "$50M — sound designers testing voice-app collaboration"
    },
    {
      "id": "theater-encore-reward-23",
      "theme": "theater",
      "title": "Encore Reward",
      "pitch": "Tokenize audience rewards to incentivize repeat attendance and engagement at live performances.",
      "subDiscipline": "audience loyalty",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Streaming text-to-speech turns every audience loyalty surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.",
      "megaPrompt": "Build \"Encore Reward\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTokenize audience rewards to incentivize repeat attendance and engagement at live performances.\nDiscipline: Theater & Live Performance (audience loyalty).\nVoice primitive: ElevenLabs Text-to-Speech (streaming voice).\nWhy this primitive: Streaming text-to-speech turns every audience loyalty surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/tts.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speak = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ text: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: {\n          \"xi-api-key\": process.env.ELEVENLABS_API_KEY!,\n          \"Content-Type\": \"application/json\",\n        },\n        body: JSON.stringify({ text: data.text, model_id: \"eleven_turbo_v2_5\" }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speak } from \"@/lib/tts.functions\";\n\nconst tts = useServerFn(speak);\nconst play = async (text: string) => {\n  const { audio } = await tts({ data: { text } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVOICE: George (`JBFqnCBsd6RMkjVDRZzb`) is the default; swap for any voice id from\nhttps://elevenlabs.io/voice-library. Use `eleven_multilingual_v2` instead of\n`eleven_turbo_v2_5` when broadcast quality matters more than first-token latency.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for audience loyalty.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — audience loyalty programs in live events",
      "sam": "$600M — voice-app loyalty reward platforms",
      "som": "$120M — theaters integrating token-based rewards"
    },
    {
      "id": "theater-setpiece-provenance-24",
      "theme": "theater",
      "title": "SetPiece Provenance",
      "pitch": "Archive and authenticate historical set pieces’ origin and usage on an immutable ElevenLabs record.",
      "subDiscipline": "historical archival",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Streaming text-to-speech turns every historical archival surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.",
      "megaPrompt": "Build \"SetPiece Provenance\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nArchive and authenticate historical set pieces’ origin and usage on an immutable ElevenLabs record.\nDiscipline: Theater & Live Performance (historical archival).\nVoice primitive: ElevenLabs Text-to-Speech (streaming voice).\nWhy this primitive: Streaming text-to-speech turns every historical archival surface in Theater & Live Performance into something audible, so the user gets a presentation-grade voiceover instead of static text.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/tts.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speak = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ text: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: {\n          \"xi-api-key\": process.env.ELEVENLABS_API_KEY!,\n          \"Content-Type\": \"application/json\",\n        },\n        body: JSON.stringify({ text: data.text, model_id: \"eleven_turbo_v2_5\" }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speak } from \"@/lib/tts.functions\";\n\nconst tts = useServerFn(speak);\nconst play = async (text: string) => {\n  const { audio } = await tts({ data: { text } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVOICE: George (`JBFqnCBsd6RMkjVDRZzb`) is the default; swap for any voice id from\nhttps://elevenlabs.io/voice-library. Use `eleven_multilingual_v2` instead of\n`eleven_turbo_v2_5` when broadcast quality matters more than first-token latency.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for historical archival.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$400M — theatrical artifact market",
      "sam": "$80M — digital archiving solutions",
      "som": "$16M — museums and theaters adopting voice-app provenance"
    },
    {
      "id": "theater-stagelight-archive-0",
      "theme": "theater",
      "title": "StageLight Archive",
      "pitch": "Pin and access dynamic stage lighting setups with permanent audio storage storage for reliable reuse and sharing.",
      "subDiscipline": "lighting design",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "On-demand music + SFX matches lighting design because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.",
      "megaPrompt": "Build \"StageLight Archive\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPin and access dynamic stage lighting setups with permanent audio storage storage for reliable reuse and sharing.\nDiscipline: Theater & Live Performance (lighting design).\nVoice primitive: Generative Music & SFX (audio generation).\nWhy this primitive: On-demand music + SFX matches lighting design because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/music.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const conjureSfx = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    prompt: z.string().min(1),\n    seconds: z.number().min(0.5).max(22).optional(),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"sfx\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: data.prompt, duration_seconds: data.seconds ?? 30 }\n      : { text: data.prompt, duration_seconds: data.seconds ?? 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT:\n```tsx\nconst conjure = useServerFn(conjureSfx);\nconst play = async (prompt: string, kind: \"sfx\" | \"music\" = \"sfx\") => {\n  const { audio } = await conjure({ data: { prompt, kind } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nTIP: the music endpoint uses `prompt`, the SFX endpoint uses `text` — the\nserver fn above already maps them. Keep SFX under 22 seconds; cap music at ~30s\nfor the demo so generation finishes inside the pitch slot.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for lighting design.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — global theatrical lighting market",
      "sam": "$500M — digital lighting design software",
      "som": "$50M — niche stage lighting archival tools"
    },
    {
      "id": "theater-playscript-vault-1",
      "theme": "theater",
      "title": "PlayScript Vault",
      "pitch": "Securely save, share, and version play scripts on audio storage to preserve authorship and facilitate collaboration.",
      "subDiscipline": "playwriting",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "On-demand music + SFX matches playwriting because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.",
      "megaPrompt": "Build \"PlayScript Vault\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSecurely save, share, and version play scripts on audio storage to preserve authorship and facilitate collaboration.\nDiscipline: Theater & Live Performance (playwriting).\nVoice primitive: Generative Music & SFX (audio generation).\nWhy this primitive: On-demand music + SFX matches playwriting because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/music.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const conjureSfx = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    prompt: z.string().min(1),\n    seconds: z.number().min(0.5).max(22).optional(),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"sfx\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: data.prompt, duration_seconds: data.seconds ?? 30 }\n      : { text: data.prompt, duration_seconds: data.seconds ?? 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT:\n```tsx\nconst conjure = useServerFn(conjureSfx);\nconst play = async (prompt: string, kind: \"sfx\" | \"music\" = \"sfx\") => {\n  const { audio } = await conjure({ data: { prompt, kind } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nTIP: the music endpoint uses `prompt`, the SFX endpoint uses `text` — the\nserver fn above already maps them. Keep SFX under 22 seconds; cap music at ~30s\nfor the demo so generation finishes inside the pitch slot.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for playwriting.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — global playwriting and publishing",
      "sam": "$700M — digital script collaboration tools",
      "som": "$70M — playwright-focused IPFS script repositories"
    },
    {
      "id": "theater-costumemood-board-2",
      "theme": "theater",
      "title": "CostumeMood Board",
      "pitch": "Pin and share costume sketches and mood boards on audio storage for collaborative costume creation.",
      "subDiscipline": "costume design",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "On-demand music + SFX matches costume design because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.",
      "megaPrompt": "Build \"CostumeMood Board\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPin and share costume sketches and mood boards on audio storage for collaborative costume creation.\nDiscipline: Theater & Live Performance (costume design).\nVoice primitive: Generative Music & SFX (audio generation).\nWhy this primitive: On-demand music + SFX matches costume design because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/music.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const conjureSfx = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    prompt: z.string().min(1),\n    seconds: z.number().min(0.5).max(22).optional(),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"sfx\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: data.prompt, duration_seconds: data.seconds ?? 30 }\n      : { text: data.prompt, duration_seconds: data.seconds ?? 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT:\n```tsx\nconst conjure = useServerFn(conjureSfx);\nconst play = async (prompt: string, kind: \"sfx\" | \"music\" = \"sfx\") => {\n  const { audio } = await conjure({ data: { prompt, kind } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nTIP: the music endpoint uses `prompt`, the SFX endpoint uses `text` — the\nserver fn above already maps them. Keep SFX under 22 seconds; cap music at ~30s\nfor the demo so generation finishes inside the pitch slot.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for costume design.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — theatrical costume industry",
      "sam": "$400M — digital costume design platforms",
      "som": "$30M — costume-specific IPFS mood board tools"
    },
    {
      "id": "theater-actorportfolio-hub-3",
      "theme": "theater",
      "title": "ActorPortfolio Hub",
      "pitch": "Actors upload and pin portfolios including headshots and performance clips permanently on audio storage for casting accessibility.",
      "subDiscipline": "performance showcase",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "On-demand music + SFX matches performance showcase because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.",
      "megaPrompt": "Build \"ActorPortfolio Hub\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nActors upload and pin portfolios including headshots and performance clips permanently on audio storage for casting accessibility.\nDiscipline: Theater & Live Performance (performance showcase).\nVoice primitive: Generative Music & SFX (audio generation).\nWhy this primitive: On-demand music + SFX matches performance showcase because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/music.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const conjureSfx = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    prompt: z.string().min(1),\n    seconds: z.number().min(0.5).max(22).optional(),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"sfx\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: data.prompt, duration_seconds: data.seconds ?? 30 }\n      : { text: data.prompt, duration_seconds: data.seconds ?? 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT:\n```tsx\nconst conjure = useServerFn(conjureSfx);\nconst play = async (prompt: string, kind: \"sfx\" | \"music\" = \"sfx\") => {\n  const { audio } = await conjure({ data: { prompt, kind } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nTIP: the music endpoint uses `prompt`, the SFX endpoint uses `text` — the\nserver fn above already maps them. Keep SFX under 22 seconds; cap music at ~30s\nfor the demo so generation finishes inside the pitch slot.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for performance showcase.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3.5B — global talent management",
      "sam": "$600M — digital actor portfolios",
      "som": "$40M — IPFS-based actor profile services"
    },
    {
      "id": "theater-setdesign-manifest-4",
      "theme": "theater",
      "title": "SetDesign Manifest",
      "pitch": "Store and share complex 3D set designs and manifests immutably on audio storage for production coordination.",
      "subDiscipline": "set design",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "On-demand music + SFX matches set design because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.",
      "megaPrompt": "Build \"SetDesign Manifest\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nStore and share complex 3D set designs and manifests immutably on audio storage for production coordination.\nDiscipline: Theater & Live Performance (set design).\nVoice primitive: Generative Music & SFX (audio generation).\nWhy this primitive: On-demand music + SFX matches set design because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/music.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const conjureSfx = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    prompt: z.string().min(1),\n    seconds: z.number().min(0.5).max(22).optional(),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"sfx\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: data.prompt, duration_seconds: data.seconds ?? 30 }\n      : { text: data.prompt, duration_seconds: data.seconds ?? 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT:\n```tsx\nconst conjure = useServerFn(conjureSfx);\nconst play = async (prompt: string, kind: \"sfx\" | \"music\" = \"sfx\") => {\n  const { audio } = await conjure({ data: { prompt, kind } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nTIP: the music endpoint uses `prompt`, the SFX endpoint uses `text` — the\nserver fn above already maps them. Keep SFX under 22 seconds; cap music at ~30s\nfor the demo so generation finishes inside the pitch slot.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for set design.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.5B — stage set production",
      "sam": "$450M — digital set design software",
      "som": "$35M — set design IPFS manifest platforms"
    },
    {
      "id": "theater-lightingcues-ledger-5",
      "theme": "theater",
      "title": "LightingCues audio logs",
      "pitch": "Save and distribute lighting cue sequences as pinned JSON on audio storage for synchronized performances.",
      "subDiscipline": "lighting cues",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "On-demand music + SFX matches lighting cues because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.",
      "megaPrompt": "Build \"LightingCues audio logs\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSave and distribute lighting cue sequences as pinned JSON on audio storage for synchronized performances.\nDiscipline: Theater & Live Performance (lighting cues).\nVoice primitive: Generative Music & SFX (audio generation).\nWhy this primitive: On-demand music + SFX matches lighting cues because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/music.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const conjureSfx = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    prompt: z.string().min(1),\n    seconds: z.number().min(0.5).max(22).optional(),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"sfx\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: data.prompt, duration_seconds: data.seconds ?? 30 }\n      : { text: data.prompt, duration_seconds: data.seconds ?? 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT:\n```tsx\nconst conjure = useServerFn(conjureSfx);\nconst play = async (prompt: string, kind: \"sfx\" | \"music\" = \"sfx\") => {\n  const { audio } = await conjure({ data: { prompt, kind } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nTIP: the music endpoint uses `prompt`, the SFX endpoint uses `text` — the\nserver fn above already maps them. Keep SFX under 22 seconds; cap music at ~30s\nfor the demo so generation finishes inside the pitch slot.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for lighting cues.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — theatrical lighting management",
      "sam": "$300M — digital cueing tools",
      "som": "$25M — IPFS-based lighting cue management"
    },
    {
      "id": "theater-playpromo-kit-6",
      "theme": "theater",
      "title": "PlayPromo Kit",
      "pitch": "Pin play promo images and metadata on audio storage to ensure immutable, globally accessible marketing assets.",
      "subDiscipline": "marketing collateral",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "On-demand music + SFX matches marketing collateral because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.",
      "megaPrompt": "Build \"PlayPromo Kit\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPin play promo images and metadata on audio storage to ensure immutable, globally accessible marketing assets.\nDiscipline: Theater & Live Performance (marketing collateral).\nVoice primitive: Generative Music & SFX (audio generation).\nWhy this primitive: On-demand music + SFX matches marketing collateral because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/music.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const conjureSfx = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    prompt: z.string().min(1),\n    seconds: z.number().min(0.5).max(22).optional(),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"sfx\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: data.prompt, duration_seconds: data.seconds ?? 30 }\n      : { text: data.prompt, duration_seconds: data.seconds ?? 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT:\n```tsx\nconst conjure = useServerFn(conjureSfx);\nconst play = async (prompt: string, kind: \"sfx\" | \"music\" = \"sfx\") => {\n  const { audio } = await conjure({ data: { prompt, kind } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nTIP: the music endpoint uses `prompt`, the SFX endpoint uses `text` — the\nserver fn above already maps them. Keep SFX under 22 seconds; cap music at ~30s\nfor the demo so generation finishes inside the pitch slot.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for marketing collateral.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — live performance marketing",
      "sam": "$800M — digital promotion platforms",
      "som": "$50M — IPFS-backed theatrical promo kits"
    },
    {
      "id": "theater-scriptrevision-chain-7",
      "theme": "theater",
      "title": "ScriptRevision Chain",
      "pitch": "Immutable version control for script edits stored and shared pinned on audio storage for playwrights and directors.",
      "subDiscipline": "script editing",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "On-demand music + SFX matches script editing because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.",
      "megaPrompt": "Build \"ScriptRevision Chain\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nImmutable version control for script edits stored and shared pinned on audio storage for playwrights and directors.\nDiscipline: Theater & Live Performance (script editing).\nVoice primitive: Generative Music & SFX (audio generation).\nWhy this primitive: On-demand music + SFX matches script editing because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/music.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const conjureSfx = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    prompt: z.string().min(1),\n    seconds: z.number().min(0.5).max(22).optional(),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"sfx\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: data.prompt, duration_seconds: data.seconds ?? 30 }\n      : { text: data.prompt, duration_seconds: data.seconds ?? 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT:\n```tsx\nconst conjure = useServerFn(conjureSfx);\nconst play = async (prompt: string, kind: \"sfx\" | \"music\" = \"sfx\") => {\n  const { audio } = await conjure({ data: { prompt, kind } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nTIP: the music endpoint uses `prompt`, the SFX endpoint uses `text` — the\nserver fn above already maps them. Keep SFX under 22 seconds; cap music at ~30s\nfor the demo so generation finishes inside the pitch slot.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for script editing.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — script development market",
      "sam": "$600M — digital script editing",
      "som": "$45M — IPFS-based script version tracking"
    },
    {
      "id": "theater-setprops-catalog-8",
      "theme": "theater",
      "title": "SetProps Catalog",
      "pitch": "Pin and index prop images and descriptions on audio storage for collaborative rental and inventory management.",
      "subDiscipline": "props management",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "On-demand music + SFX matches props management because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.",
      "megaPrompt": "Build \"SetProps Catalog\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPin and index prop images and descriptions on audio storage for collaborative rental and inventory management.\nDiscipline: Theater & Live Performance (props management).\nVoice primitive: Generative Music & SFX (audio generation).\nWhy this primitive: On-demand music + SFX matches props management because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/music.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const conjureSfx = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    prompt: z.string().min(1),\n    seconds: z.number().min(0.5).max(22).optional(),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"sfx\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: data.prompt, duration_seconds: data.seconds ?? 30 }\n      : { text: data.prompt, duration_seconds: data.seconds ?? 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT:\n```tsx\nconst conjure = useServerFn(conjureSfx);\nconst play = async (prompt: string, kind: \"sfx\" | \"music\" = \"sfx\") => {\n  const { audio } = await conjure({ data: { prompt, kind } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nTIP: the music endpoint uses `prompt`, the SFX endpoint uses `text` — the\nserver fn above already maps them. Keep SFX under 22 seconds; cap music at ~30s\nfor the demo so generation finishes inside the pitch slot.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for props management.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.8B — theatrical prop rental",
      "sam": "$350M — digital prop databases",
      "som": "$20M — IPFS prop catalog solutions"
    },
    {
      "id": "theater-soundscape-archive-9",
      "theme": "theater",
      "title": "Soundscape Archive",
      "pitch": "Pin ambient and effect sound files with metadata on audio storage to ensure lasting availability and sharing.",
      "subDiscipline": "sound design",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "On-demand music + SFX matches sound design because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.",
      "megaPrompt": "Build \"Soundscape Archive\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPin ambient and effect sound files with metadata on audio storage to ensure lasting availability and sharing.\nDiscipline: Theater & Live Performance (sound design).\nVoice primitive: Generative Music & SFX (audio generation).\nWhy this primitive: On-demand music + SFX matches sound design because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/music.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const conjureSfx = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    prompt: z.string().min(1),\n    seconds: z.number().min(0.5).max(22).optional(),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"sfx\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: data.prompt, duration_seconds: data.seconds ?? 30 }\n      : { text: data.prompt, duration_seconds: data.seconds ?? 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT:\n```tsx\nconst conjure = useServerFn(conjureSfx);\nconst play = async (prompt: string, kind: \"sfx\" | \"music\" = \"sfx\") => {\n  const { audio } = await conjure({ data: { prompt, kind } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nTIP: the music endpoint uses `prompt`, the SFX endpoint uses `text` — the\nserver fn above already maps them. Keep SFX under 22 seconds; cap music at ~30s\nfor the demo so generation finishes inside the pitch slot.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for sound design.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — live sound design",
      "sam": "$400M — digital sound libraries",
      "som": "$30M — IPFS-based sound archival tools"
    },
    {
      "id": "theater-performancereview-ledger-10",
      "theme": "theater",
      "title": "PerformanceReview audio logs",
      "pitch": "Store aggregated audience reviews as pinned JSON on audio storage guaranteeing unalterable feedback records.",
      "subDiscipline": "audience feedback",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "On-demand music + SFX matches audience feedback because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.",
      "megaPrompt": "Build \"PerformanceReview audio logs\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nStore aggregated audience reviews as pinned JSON on audio storage guaranteeing unalterable feedback records.\nDiscipline: Theater & Live Performance (audience feedback).\nVoice primitive: Generative Music & SFX (audio generation).\nWhy this primitive: On-demand music + SFX matches audience feedback because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/music.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const conjureSfx = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    prompt: z.string().min(1),\n    seconds: z.number().min(0.5).max(22).optional(),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"sfx\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: data.prompt, duration_seconds: data.seconds ?? 30 }\n      : { text: data.prompt, duration_seconds: data.seconds ?? 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT:\n```tsx\nconst conjure = useServerFn(conjureSfx);\nconst play = async (prompt: string, kind: \"sfx\" | \"music\" = \"sfx\") => {\n  const { audio } = await conjure({ data: { prompt, kind } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nTIP: the music endpoint uses `prompt`, the SFX endpoint uses `text` — the\nserver fn above already maps them. Keep SFX under 22 seconds; cap music at ~30s\nfor the demo so generation finishes inside the pitch slot.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for audience feedback.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — live performance feedback",
      "sam": "$500M — digital review platforms",
      "som": "$25M — IPFS audience review services"
    },
    {
      "id": "theater-lightingpalette-exchange-11",
      "theme": "theater",
      "title": "LightingPalette Exchange",
      "pitch": "Pin and trade curated lighting color palettes as images and data on audio storage for designers to reuse.",
      "subDiscipline": "color study",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "On-demand music + SFX matches color study because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.",
      "megaPrompt": "Build \"LightingPalette Exchange\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPin and trade curated lighting color palettes as images and data on audio storage for designers to reuse.\nDiscipline: Theater & Live Performance (color study).\nVoice primitive: Generative Music & SFX (audio generation).\nWhy this primitive: On-demand music + SFX matches color study because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/music.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const conjureSfx = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    prompt: z.string().min(1),\n    seconds: z.number().min(0.5).max(22).optional(),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"sfx\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: data.prompt, duration_seconds: data.seconds ?? 30 }\n      : { text: data.prompt, duration_seconds: data.seconds ?? 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT:\n```tsx\nconst conjure = useServerFn(conjureSfx);\nconst play = async (prompt: string, kind: \"sfx\" | \"music\" = \"sfx\") => {\n  const { audio } = await conjure({ data: { prompt, kind } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nTIP: the music endpoint uses `prompt`, the SFX endpoint uses `text` — the\nserver fn above already maps them. Keep SFX under 22 seconds; cap music at ~30s\nfor the demo so generation finishes inside the pitch slot.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for color study.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.2B — theatrical lighting color tools",
      "sam": "$250M — digital lighting palette apps",
      "som": "$15M — IPFS palette sharing platforms"
    },
    {
      "id": "theater-venuemap-index-12",
      "theme": "theater",
      "title": "VenueMap Index",
      "pitch": "Pin detailed venue and stage layout manifests on audio storage for directors and designers to coordinate setups.",
      "subDiscipline": "stage layout",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "On-demand music + SFX matches stage layout because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.",
      "megaPrompt": "Build \"VenueMap Index\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPin detailed venue and stage layout manifests on audio storage for directors and designers to coordinate setups.\nDiscipline: Theater & Live Performance (stage layout).\nVoice primitive: Generative Music & SFX (audio generation).\nWhy this primitive: On-demand music + SFX matches stage layout because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/music.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const conjureSfx = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    prompt: z.string().min(1),\n    seconds: z.number().min(0.5).max(22).optional(),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"sfx\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: data.prompt, duration_seconds: data.seconds ?? 30 }\n      : { text: data.prompt, duration_seconds: data.seconds ?? 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT:\n```tsx\nconst conjure = useServerFn(conjureSfx);\nconst play = async (prompt: string, kind: \"sfx\" | \"music\" = \"sfx\") => {\n  const { audio } = await conjure({ data: { prompt, kind } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nTIP: the music endpoint uses `prompt`, the SFX endpoint uses `text` — the\nserver fn above already maps them. Keep SFX under 22 seconds; cap music at ~30s\nfor the demo so generation finishes inside the pitch slot.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for stage layout.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.3B — venue management software",
      "sam": "$400M — digital stage layout tools",
      "som": "$20M — IPFS venue mapping services"
    },
    {
      "id": "theater-monologue-archive-13",
      "theme": "theater",
      "title": "Monologue Archive",
      "pitch": "Pin curated monologue scripts and performance notes on audio storage preserving artistic intent unchangeably.",
      "subDiscipline": "performance repertoire",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "On-demand music + SFX matches performance repertoire because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.",
      "megaPrompt": "Build \"Monologue Archive\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPin curated monologue scripts and performance notes on audio storage preserving artistic intent unchangeably.\nDiscipline: Theater & Live Performance (performance repertoire).\nVoice primitive: Generative Music & SFX (audio generation).\nWhy this primitive: On-demand music + SFX matches performance repertoire because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/music.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const conjureSfx = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    prompt: z.string().min(1),\n    seconds: z.number().min(0.5).max(22).optional(),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"sfx\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: data.prompt, duration_seconds: data.seconds ?? 30 }\n      : { text: data.prompt, duration_seconds: data.seconds ?? 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT:\n```tsx\nconst conjure = useServerFn(conjureSfx);\nconst play = async (prompt: string, kind: \"sfx\" | \"music\" = \"sfx\") => {\n  const { audio } = await conjure({ data: { prompt, kind } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nTIP: the music endpoint uses `prompt`, the SFX endpoint uses `text` — the\nserver fn above already maps them. Keep SFX under 22 seconds; cap music at ~30s\nfor the demo so generation finishes inside the pitch slot.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for performance repertoire.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — acting training resources",
      "sam": "$300M — digital monologue libraries",
      "som": "$18M — IPFS-based monologue archives"
    },
    {
      "id": "theater-costumetexture-bank-14",
      "theme": "theater",
      "title": "CostumeTexture Bank",
      "pitch": "Pin high-resolution fabric texture images and details on audio storage for costume designers’ resource sharing.",
      "subDiscipline": "fabric study",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "On-demand music + SFX matches fabric study because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.",
      "megaPrompt": "Build \"CostumeTexture Bank\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPin high-resolution fabric texture images and details on audio storage for costume designers’ resource sharing.\nDiscipline: Theater & Live Performance (fabric study).\nVoice primitive: Generative Music & SFX (audio generation).\nWhy this primitive: On-demand music + SFX matches fabric study because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/music.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const conjureSfx = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    prompt: z.string().min(1),\n    seconds: z.number().min(0.5).max(22).optional(),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"sfx\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: data.prompt, duration_seconds: data.seconds ?? 30 }\n      : { text: data.prompt, duration_seconds: data.seconds ?? 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT:\n```tsx\nconst conjure = useServerFn(conjureSfx);\nconst play = async (prompt: string, kind: \"sfx\" | \"music\" = \"sfx\") => {\n  const { audio } = await conjure({ data: { prompt, kind } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nTIP: the music endpoint uses `prompt`, the SFX endpoint uses `text` — the\nserver fn above already maps them. Keep SFX under 22 seconds; cap music at ~30s\nfor the demo so generation finishes inside the pitch slot.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for fabric study.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.8B — costume material sourcing",
      "sam": "$350M — digital fabric repositories",
      "som": "$22M — IPFS costume texture platforms"
    },
    {
      "id": "theater-propblueprint-cache-15",
      "theme": "theater",
      "title": "PropBlueprint Cache",
      "pitch": "Store and share detailed prop blueprints and assembly guides pinned immutably on audio storage for artisans.",
      "subDiscipline": "prop design",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "On-demand music + SFX matches prop design because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.",
      "megaPrompt": "Build \"PropBlueprint Cache\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nStore and share detailed prop blueprints and assembly guides pinned immutably on audio storage for artisans.\nDiscipline: Theater & Live Performance (prop design).\nVoice primitive: Generative Music & SFX (audio generation).\nWhy this primitive: On-demand music + SFX matches prop design because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/music.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const conjureSfx = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    prompt: z.string().min(1),\n    seconds: z.number().min(0.5).max(22).optional(),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"sfx\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: data.prompt, duration_seconds: data.seconds ?? 30 }\n      : { text: data.prompt, duration_seconds: data.seconds ?? 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT:\n```tsx\nconst conjure = useServerFn(conjureSfx);\nconst play = async (prompt: string, kind: \"sfx\" | \"music\" = \"sfx\") => {\n  const { audio } = await conjure({ data: { prompt, kind } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nTIP: the music endpoint uses `prompt`, the SFX endpoint uses `text` — the\nserver fn above already maps them. Keep SFX under 22 seconds; cap music at ~30s\nfor the demo so generation finishes inside the pitch slot.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for prop design.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.7B — prop manufacturing",
      "sam": "$320M — digital prop design platforms",
      "som": "$18M — IPFS-based blueprint archives"
    },
    {
      "id": "theater-directorstoryboard-hub-16",
      "theme": "theater",
      "title": "DirectorStoryboard Hub",
      "pitch": "Pin storyboards as image sequences on audio storage to maintain integrity and facilitate collaboration across teams.",
      "subDiscipline": "storyboarding",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "On-demand music + SFX matches storyboarding because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.",
      "megaPrompt": "Build \"DirectorStoryboard Hub\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPin storyboards as image sequences on audio storage to maintain integrity and facilitate collaboration across teams.\nDiscipline: Theater & Live Performance (storyboarding).\nVoice primitive: Generative Music & SFX (audio generation).\nWhy this primitive: On-demand music + SFX matches storyboarding because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/music.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const conjureSfx = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    prompt: z.string().min(1),\n    seconds: z.number().min(0.5).max(22).optional(),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"sfx\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: data.prompt, duration_seconds: data.seconds ?? 30 }\n      : { text: data.prompt, duration_seconds: data.seconds ?? 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT:\n```tsx\nconst conjure = useServerFn(conjureSfx);\nconst play = async (prompt: string, kind: \"sfx\" | \"music\" = \"sfx\") => {\n  const { audio } = await conjure({ data: { prompt, kind } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nTIP: the music endpoint uses `prompt`, the SFX endpoint uses `text` — the\nserver fn above already maps them. Keep SFX under 22 seconds; cap music at ~30s\nfor the demo so generation finishes inside the pitch slot.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for storyboarding.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — theatrical directing tools",
      "sam": "$450M — digital storyboarding software",
      "som": "$25M — IPFS storyboard sharing platforms"
    },
    {
      "id": "theater-lightingfixture-db-17",
      "theme": "theater",
      "title": "LightingFixture DB",
      "pitch": "Pin detailed lighting fixture profiles and manuals on audio storage for technicians and designers to access anytime.",
      "subDiscipline": "equipment catalog",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "On-demand music + SFX matches equipment catalog because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.",
      "megaPrompt": "Build \"LightingFixture DB\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPin detailed lighting fixture profiles and manuals on audio storage for technicians and designers to access anytime.\nDiscipline: Theater & Live Performance (equipment catalog).\nVoice primitive: Generative Music & SFX (audio generation).\nWhy this primitive: On-demand music + SFX matches equipment catalog because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/music.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const conjureSfx = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    prompt: z.string().min(1),\n    seconds: z.number().min(0.5).max(22).optional(),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"sfx\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: data.prompt, duration_seconds: data.seconds ?? 30 }\n      : { text: data.prompt, duration_seconds: data.seconds ?? 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT:\n```tsx\nconst conjure = useServerFn(conjureSfx);\nconst play = async (prompt: string, kind: \"sfx\" | \"music\" = \"sfx\") => {\n  const { audio } = await conjure({ data: { prompt, kind } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nTIP: the music endpoint uses `prompt`, the SFX endpoint uses `text` — the\nserver fn above already maps them. Keep SFX under 22 seconds; cap music at ~30s\nfor the demo so generation finishes inside the pitch slot.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for equipment catalog.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — theatrical equipment databases",
      "sam": "$600M — lighting tech catalog apps",
      "som": "$40M — IPFS-backed fixture info services"
    },
    {
      "id": "theater-virtualstage-seeds-18",
      "theme": "theater",
      "title": "VirtualStage Seeds",
      "pitch": "Pin virtual stage assets and scene manifests on audio storage for seamless integration in live and hybrid performances.",
      "subDiscipline": "virtual set design",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "On-demand music + SFX matches virtual set design because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.",
      "megaPrompt": "Build \"VirtualStage Seeds\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPin virtual stage assets and scene manifests on audio storage for seamless integration in live and hybrid performances.\nDiscipline: Theater & Live Performance (virtual set design).\nVoice primitive: Generative Music & SFX (audio generation).\nWhy this primitive: On-demand music + SFX matches virtual set design because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/music.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const conjureSfx = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    prompt: z.string().min(1),\n    seconds: z.number().min(0.5).max(22).optional(),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"sfx\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: data.prompt, duration_seconds: data.seconds ?? 30 }\n      : { text: data.prompt, duration_seconds: data.seconds ?? 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT:\n```tsx\nconst conjure = useServerFn(conjureSfx);\nconst play = async (prompt: string, kind: \"sfx\" | \"music\" = \"sfx\") => {\n  const { audio } = await conjure({ data: { prompt, kind } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nTIP: the music endpoint uses `prompt`, the SFX endpoint uses `text` — the\nserver fn above already maps them. Keep SFX under 22 seconds; cap music at ~30s\nfor the demo so generation finishes inside the pitch slot.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for virtual set design.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.5B — virtual performance tech",
      "sam": "$500M — virtual set design software",
      "som": "$35M — IPFS virtual staging tools"
    },
    {
      "id": "theater-lightingpattern-archive-19",
      "theme": "theater",
      "title": "LightingPattern Archive",
      "pitch": "Pin and share choreographed lighting patterns and sequences immutably on audio storage for repeatable productions.",
      "subDiscipline": "lighting choreography",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "On-demand music + SFX matches lighting choreography because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.",
      "megaPrompt": "Build \"LightingPattern Archive\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPin and share choreographed lighting patterns and sequences immutably on audio storage for repeatable productions.\nDiscipline: Theater & Live Performance (lighting choreography).\nVoice primitive: Generative Music & SFX (audio generation).\nWhy this primitive: On-demand music + SFX matches lighting choreography because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/music.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const conjureSfx = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    prompt: z.string().min(1),\n    seconds: z.number().min(0.5).max(22).optional(),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"sfx\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: data.prompt, duration_seconds: data.seconds ?? 30 }\n      : { text: data.prompt, duration_seconds: data.seconds ?? 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT:\n```tsx\nconst conjure = useServerFn(conjureSfx);\nconst play = async (prompt: string, kind: \"sfx\" | \"music\" = \"sfx\") => {\n  const { audio } = await conjure({ data: { prompt, kind } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nTIP: the music endpoint uses `prompt`, the SFX endpoint uses `text` — the\nserver fn above already maps them. Keep SFX under 22 seconds; cap music at ~30s\nfor the demo so generation finishes inside the pitch slot.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for lighting choreography.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — theatrical lighting choreography",
      "sam": "$300M — lighting sequence software",
      "som": "$20M — IPFS-based lighting pattern archives"
    },
    {
      "id": "theater-performancemanifesto-20",
      "theme": "theater",
      "title": "PerformanceManifesto",
      "pitch": "Pin performance manifestos and artistic statements as permanent JSON files on audio storage for posterity and access.",
      "subDiscipline": "creative documentation",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "On-demand music + SFX matches creative documentation because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.",
      "megaPrompt": "Build \"PerformanceManifesto\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPin performance manifestos and artistic statements as permanent JSON files on audio storage for posterity and access.\nDiscipline: Theater & Live Performance (creative documentation).\nVoice primitive: Generative Music & SFX (audio generation).\nWhy this primitive: On-demand music + SFX matches creative documentation because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/music.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const conjureSfx = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    prompt: z.string().min(1),\n    seconds: z.number().min(0.5).max(22).optional(),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"sfx\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: data.prompt, duration_seconds: data.seconds ?? 30 }\n      : { text: data.prompt, duration_seconds: data.seconds ?? 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT:\n```tsx\nconst conjure = useServerFn(conjureSfx);\nconst play = async (prompt: string, kind: \"sfx\" | \"music\" = \"sfx\") => {\n  const { audio } = await conjure({ data: { prompt, kind } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nTIP: the music endpoint uses `prompt`, the SFX endpoint uses `text` — the\nserver fn above already maps them. Keep SFX under 22 seconds; cap music at ~30s\nfor the demo so generation finishes inside the pitch slot.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for creative documentation.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — artistic documentation market",
      "sam": "$300M — digital artist statement platforms",
      "som": "$15M — IPFS manifesto hosting services"
    },
    {
      "id": "theater-stagesafety-logs-21",
      "theme": "theater",
      "title": "StageSafety Logs",
      "pitch": "Pin safety inspection reports and compliance logs on audio storage ensuring tamper-proof records for venues and crews.",
      "subDiscipline": "safety compliance",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "On-demand music + SFX matches safety compliance because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.",
      "megaPrompt": "Build \"StageSafety Logs\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPin safety inspection reports and compliance logs on audio storage ensuring tamper-proof records for venues and crews.\nDiscipline: Theater & Live Performance (safety compliance).\nVoice primitive: Generative Music & SFX (audio generation).\nWhy this primitive: On-demand music + SFX matches safety compliance because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/music.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const conjureSfx = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    prompt: z.string().min(1),\n    seconds: z.number().min(0.5).max(22).optional(),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"sfx\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: data.prompt, duration_seconds: data.seconds ?? 30 }\n      : { text: data.prompt, duration_seconds: data.seconds ?? 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT:\n```tsx\nconst conjure = useServerFn(conjureSfx);\nconst play = async (prompt: string, kind: \"sfx\" | \"music\" = \"sfx\") => {\n  const { audio } = await conjure({ data: { prompt, kind } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nTIP: the music endpoint uses `prompt`, the SFX endpoint uses `text` — the\nserver fn above already maps them. Keep SFX under 22 seconds; cap music at ~30s\nfor the demo so generation finishes inside the pitch slot.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for safety compliance.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — theatrical safety management",
      "sam": "$200M — digital safety compliance tools",
      "som": "$12M — IPFS-based safety log platforms"
    },
    {
      "id": "theater-actorlinetracker-22",
      "theme": "theater",
      "title": "ActorLineTracker",
      "pitch": "Pin annotated lines and rehearsal notes as immutable JSON to audio storage for actors’ reliable study aids.",
      "subDiscipline": "line memorization",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "On-demand music + SFX matches line memorization because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.",
      "megaPrompt": "Build \"ActorLineTracker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPin annotated lines and rehearsal notes as immutable JSON to audio storage for actors’ reliable study aids.\nDiscipline: Theater & Live Performance (line memorization).\nVoice primitive: Generative Music & SFX (audio generation).\nWhy this primitive: On-demand music + SFX matches line memorization because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/music.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const conjureSfx = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    prompt: z.string().min(1),\n    seconds: z.number().min(0.5).max(22).optional(),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"sfx\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: data.prompt, duration_seconds: data.seconds ?? 30 }\n      : { text: data.prompt, duration_seconds: data.seconds ?? 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT:\n```tsx\nconst conjure = useServerFn(conjureSfx);\nconst play = async (prompt: string, kind: \"sfx\" | \"music\" = \"sfx\") => {\n  const { audio } = await conjure({ data: { prompt, kind } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nTIP: the music endpoint uses `prompt`, the SFX endpoint uses `text` — the\nserver fn above already maps them. Keep SFX under 22 seconds; cap music at ~30s\nfor the demo so generation finishes inside the pitch slot.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for line memorization.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.2B — acting education market",
      "sam": "$250M — digital memorization tools",
      "som": "$15M — IPFS actor rehearsal aids"
    },
    {
      "id": "theater-setlighting-simulator-23",
      "theme": "theater",
      "title": "SetLighting Simulator",
      "pitch": "Pin lighting simulation data and images on audio storage for real-time, distributed production previews.",
      "subDiscipline": "previsualization",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "On-demand music + SFX matches previsualization because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.",
      "megaPrompt": "Build \"SetLighting Simulator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPin lighting simulation data and images on audio storage for real-time, distributed production previews.\nDiscipline: Theater & Live Performance (previsualization).\nVoice primitive: Generative Music & SFX (audio generation).\nWhy this primitive: On-demand music + SFX matches previsualization because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/music.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const conjureSfx = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    prompt: z.string().min(1),\n    seconds: z.number().min(0.5).max(22).optional(),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"sfx\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: data.prompt, duration_seconds: data.seconds ?? 30 }\n      : { text: data.prompt, duration_seconds: data.seconds ?? 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT:\n```tsx\nconst conjure = useServerFn(conjureSfx);\nconst play = async (prompt: string, kind: \"sfx\" | \"music\" = \"sfx\") => {\n  const { audio } = await conjure({ data: { prompt, kind } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nTIP: the music endpoint uses `prompt`, the SFX endpoint uses `text` — the\nserver fn above already maps them. Keep SFX under 22 seconds; cap music at ~30s\nfor the demo so generation finishes inside the pitch slot.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for previsualization.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — performance previsualization",
      "sam": "$400M — digital lighting simulator platforms",
      "som": "$25M — IPFS stage lighting simulation"
    },
    {
      "id": "theater-playbill-archive-24",
      "theme": "theater",
      "title": "PlayBill Archive",
      "pitch": "Pin historic and current playbill designs on audio storage for theatrical heritage and design reference.",
      "subDiscipline": "program design",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "On-demand music + SFX matches program design because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.",
      "megaPrompt": "Build \"PlayBill Archive\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPin historic and current playbill designs on audio storage for theatrical heritage and design reference.\nDiscipline: Theater & Live Performance (program design).\nVoice primitive: Generative Music & SFX (audio generation).\nWhy this primitive: On-demand music + SFX matches program design because Theater & Live Performance lives or dies on the soundbed — the demo needs a sound, generated in seconds, not a Freesound library trawl.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/music.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const conjureSfx = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    prompt: z.string().min(1),\n    seconds: z.number().min(0.5).max(22).optional(),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"sfx\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: data.prompt, duration_seconds: data.seconds ?? 30 }\n      : { text: data.prompt, duration_seconds: data.seconds ?? 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT:\n```tsx\nconst conjure = useServerFn(conjureSfx);\nconst play = async (prompt: string, kind: \"sfx\" | \"music\" = \"sfx\") => {\n  const { audio } = await conjure({ data: { prompt, kind } });\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nTIP: the music endpoint uses `prompt`, the SFX endpoint uses `text` — the\nserver fn above already maps them. Keep SFX under 22 seconds; cap music at ~30s\nfor the demo so generation finishes inside the pitch slot.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for program design.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — theatrical print design",
      "sam": "$150M — digital program archives",
      "som": "$10M — IPFS playbill archival platforms"
    },
    {
      "id": "theater-smartstage-access-0",
      "theme": "theater",
      "title": "SmartStage Access",
      "pitch": "Enable seamless, latency-free ticketing and VIP experiences via social login and sponsored voice events.",
      "subDiscipline": "audience engagement",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "A conversational ElevenLabs agent fits audience engagement because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.",
      "megaPrompt": "Build \"SmartStage Access\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nEnable seamless, latency-free ticketing and VIP experiences via social login and sponsored voice events.\nDiscipline: Theater & Live Performance (audience engagement).\nVoice primitive: Conversational Voice Agent (live conversation).\nWhy this primitive: A conversational ElevenLabs agent fits audience engagement because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/agent.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation();\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async () => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({ conversationToken: token, connectionType: \"webrtc\" });\n};\n```\n\nAGENT SETUP: create the agent in https://elevenlabs.io/app/agents, give it a\nsystem prompt fit for the audience engagement use case, copy its agent id, and expose it as\n`VITE_AGENT_ID` in the project (public env, not a secret).\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for audience engagement.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — global live event ticketing market",
      "sam": "$2B — digital ticketing for theater performances",
      "som": "$300M — theaters adopting voice-app ticketing"
    },
    {
      "id": "theater-scriptshare-vault-1",
      "theme": "theater",
      "title": "ScriptShare Vault",
      "pitch": "Facilitate secure, collaborative script revisions with on-record identity and no-fee voice events.",
      "subDiscipline": "script collaboration",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "A conversational ElevenLabs agent fits script collaboration because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.",
      "megaPrompt": "Build \"ScriptShare Vault\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFacilitate secure, collaborative script revisions with on-record identity and no-fee voice events.\nDiscipline: Theater & Live Performance (script collaboration).\nVoice primitive: Conversational Voice Agent (live conversation).\nWhy this primitive: A conversational ElevenLabs agent fits script collaboration because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/agent.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation();\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async () => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({ conversationToken: token, connectionType: \"webrtc\" });\n};\n```\n\nAGENT SETUP: create the agent in https://elevenlabs.io/app/agents, give it a\nsystem prompt fit for the script collaboration use case, copy its agent id, and expose it as\n`VITE_AGENT_ID` in the project (public env, not a secret).\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for script collaboration.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$500M — global playwriting markets",
      "sam": "$120M — digital script collaboration tools",
      "som": "$25M — theater groups using voice-app collaboration"
    },
    {
      "id": "theater-propchain-ledger-2",
      "theme": "theater",
      "title": "PropChain audio logs",
      "pitch": "Track and verify theater prop history and ownership with private social access and free voice events.",
      "subDiscipline": "prop provenance",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "A conversational ElevenLabs agent fits prop provenance because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.",
      "megaPrompt": "Build \"PropChain audio logs\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTrack and verify theater prop history and ownership with private social access and free voice events.\nDiscipline: Theater & Live Performance (prop provenance).\nVoice primitive: Conversational Voice Agent (live conversation).\nWhy this primitive: A conversational ElevenLabs agent fits prop provenance because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/agent.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation();\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async () => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({ conversationToken: token, connectionType: \"webrtc\" });\n};\n```\n\nAGENT SETUP: create the agent in https://elevenlabs.io/app/agents, give it a\nsystem prompt fit for the prop provenance use case, copy its agent id, and expose it as\n`VITE_AGENT_ID` in the project (public env, not a secret).\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for prop provenance.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$300M — global theater prop markets",
      "sam": "$80M — prop rental and sales digitization",
      "som": "$15M — voice-app prop tracking adoption"
    },
    {
      "id": "theater-lightcue-sync-3",
      "theme": "theater",
      "title": "LightCue Sync",
      "pitch": "Collaborate on lighting cues live with instant, gasless on-record updates through social login.",
      "subDiscipline": "lighting design",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "A conversational ElevenLabs agent fits lighting design because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.",
      "megaPrompt": "Build \"LightCue Sync\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCollaborate on lighting cues live with instant, gasless on-record updates through social login.\nDiscipline: Theater & Live Performance (lighting design).\nVoice primitive: Conversational Voice Agent (live conversation).\nWhy this primitive: A conversational ElevenLabs agent fits lighting design because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/agent.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation();\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async () => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({ conversationToken: token, connectionType: \"webrtc\" });\n};\n```\n\nAGENT SETUP: create the agent in https://elevenlabs.io/app/agents, give it a\nsystem prompt fit for the lighting design use case, copy its agent id, and expose it as\n`VITE_AGENT_ID` in the project (public env, not a secret).\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for lighting design.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.2B — global stage lighting equipment and design",
      "sam": "$300M — digital lighting design collaboration",
      "som": "$50M — theaters using voice-app for lighting"
    },
    {
      "id": "theater-actortrust-ledger-4",
      "theme": "theater",
      "title": "ActorTrust audio logs",
      "pitch": "Store and verify actor resumes and roles securely with social login and no latency fees.",
      "subDiscipline": "performance credentials",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "A conversational ElevenLabs agent fits performance credentials because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.",
      "megaPrompt": "Build \"ActorTrust audio logs\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nStore and verify actor resumes and roles securely with social login and no latency fees.\nDiscipline: Theater & Live Performance (performance credentials).\nVoice primitive: Conversational Voice Agent (live conversation).\nWhy this primitive: A conversational ElevenLabs agent fits performance credentials because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/agent.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation();\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async () => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({ conversationToken: token, connectionType: \"webrtc\" });\n};\n```\n\nAGENT SETUP: create the agent in https://elevenlabs.io/app/agents, give it a\nsystem prompt fit for the performance credentials use case, copy its agent id, and expose it as\n`VITE_AGENT_ID` in the project (public env, not a secret).\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for performance credentials.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$15B — global actor management and casting market",
      "sam": "$3B — digital acting portfolios",
      "som": "$400M — digital credential verification for actors"
    },
    {
      "id": "theater-sceneswap-marketplace-5",
      "theme": "theater",
      "title": "SceneSwap Marketplace",
      "pitch": "Enable latency-free peer-to-peer trading of set designs via social voiceprints and sponsored tx.",
      "subDiscipline": "set design exchange",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "A conversational ElevenLabs agent fits set design exchange because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.",
      "megaPrompt": "Build \"SceneSwap Marketplace\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nEnable latency-free peer-to-peer trading of set designs via social voiceprints and sponsored tx.\nDiscipline: Theater & Live Performance (set design exchange).\nVoice primitive: Conversational Voice Agent (live conversation).\nWhy this primitive: A conversational ElevenLabs agent fits set design exchange because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/agent.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation();\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async () => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({ conversationToken: token, connectionType: \"webrtc\" });\n};\n```\n\nAGENT SETUP: create the agent in https://elevenlabs.io/app/agents, give it a\nsystem prompt fit for the set design exchange use case, copy its agent id, and expose it as\n`VITE_AGENT_ID` in the project (public env, not a secret).\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for set design exchange.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$700M — global set design market",
      "sam": "$200M — online set design marketplaces",
      "som": "$30M — voice-app enabled design exchanges"
    },
    {
      "id": "theater-playbill-nfts-6",
      "theme": "theater",
      "title": "Playbill voice assets",
      "pitch": "Create and distribute collectible digital playbills with embedded social voiceprints and free publishing.",
      "subDiscipline": "memorabilia",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "A conversational ElevenLabs agent fits memorabilia because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.",
      "megaPrompt": "Build \"Playbill voice assets\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCreate and distribute collectible digital playbills with embedded social voiceprints and free publishing.\nDiscipline: Theater & Live Performance (memorabilia).\nVoice primitive: Conversational Voice Agent (live conversation).\nWhy this primitive: A conversational ElevenLabs agent fits memorabilia because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/agent.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation();\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async () => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({ conversationToken: token, connectionType: \"webrtc\" });\n};\n```\n\nAGENT SETUP: create the agent in https://elevenlabs.io/app/agents, give it a\nsystem prompt fit for the memorabilia use case, copy its agent id, and expose it as\n`VITE_AGENT_ID` in the project (public env, not a secret).\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for memorabilia.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — theater memorabilia market",
      "sam": "$300M — digital collectibles sector",
      "som": "$50M — NFT playbill startups"
    },
    {
      "id": "theater-audiencevote-live-7",
      "theme": "theater",
      "title": "AudienceVote Live",
      "pitch": "Let audiences vote live on performance elements using social login and gasless on-record votes.",
      "subDiscipline": "interactive performances",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "A conversational ElevenLabs agent fits interactive performances because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.",
      "megaPrompt": "Build \"AudienceVote Live\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLet audiences vote live on performance elements using social login and gasless on-record votes.\nDiscipline: Theater & Live Performance (interactive performances).\nVoice primitive: Conversational Voice Agent (live conversation).\nWhy this primitive: A conversational ElevenLabs agent fits interactive performances because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/agent.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation();\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async () => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({ conversationToken: token, connectionType: \"webrtc\" });\n};\n```\n\nAGENT SETUP: create the agent in https://elevenlabs.io/app/agents, give it a\nsystem prompt fit for the interactive performances use case, copy its agent id, and expose it as\n`VITE_AGENT_ID` in the project (public env, not a secret).\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for interactive performances.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — global interactive theater experiences",
      "sam": "$1B — digital audience participation tech",
      "som": "$150M — voice-app powered voting in shows"
    },
    {
      "id": "theater-backstage-pass-8",
      "theme": "theater",
      "title": "Backstage Pass",
      "pitch": "Offer backstage digital passes with seamless social voiceprints login and free sponsorship tx.",
      "subDiscipline": "exclusive access",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "A conversational ElevenLabs agent fits exclusive access because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.",
      "megaPrompt": "Build \"Backstage Pass\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nOffer backstage digital passes with seamless social voiceprints login and free sponsorship tx.\nDiscipline: Theater & Live Performance (exclusive access).\nVoice primitive: Conversational Voice Agent (live conversation).\nWhy this primitive: A conversational ElevenLabs agent fits exclusive access because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/agent.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation();\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async () => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({ conversationToken: token, connectionType: \"webrtc\" });\n};\n```\n\nAGENT SETUP: create the agent in https://elevenlabs.io/app/agents, give it a\nsystem prompt fit for the exclusive access use case, copy its agent id, and expose it as\n`VITE_AGENT_ID` in the project (public env, not a secret).\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for exclusive access.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — live event VIP access market",
      "sam": "$500M — digital VIP experiences",
      "som": "$80M — theaters using voice-app access passes"
    },
    {
      "id": "theater-rehearsalrecord-9",
      "theme": "theater",
      "title": "RehearsalRecord",
      "pitch": "Securely archive rehearsal footage and notes with social voiceprints authentication and free updates.",
      "subDiscipline": "performance archives",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "A conversational ElevenLabs agent fits performance archives because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.",
      "megaPrompt": "Build \"RehearsalRecord\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSecurely archive rehearsal footage and notes with social voiceprints authentication and free updates.\nDiscipline: Theater & Live Performance (performance archives).\nVoice primitive: Conversational Voice Agent (live conversation).\nWhy this primitive: A conversational ElevenLabs agent fits performance archives because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/agent.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation();\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async () => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({ conversationToken: token, connectionType: \"webrtc\" });\n};\n```\n\nAGENT SETUP: create the agent in https://elevenlabs.io/app/agents, give it a\nsystem prompt fit for the performance archives use case, copy its agent id, and expose it as\n`VITE_AGENT_ID` in the project (public env, not a secret).\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for performance archives.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$600M — theater production documentation market",
      "sam": "$150M — digital rehearsal tools",
      "som": "$25M — voice-app rehearsal archiving"
    },
    {
      "id": "theater-sponsorspotlight-10",
      "theme": "theater",
      "title": "SponsorSpotlight",
      "pitch": "Connect sponsors and shows with on-record contracts signed via social login and no latency fees.",
      "subDiscipline": "performance sponsorship",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "A conversational ElevenLabs agent fits performance sponsorship because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.",
      "megaPrompt": "Build \"SponsorSpotlight\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nConnect sponsors and shows with on-record contracts signed via social login and no latency fees.\nDiscipline: Theater & Live Performance (performance sponsorship).\nVoice primitive: Conversational Voice Agent (live conversation).\nWhy this primitive: A conversational ElevenLabs agent fits performance sponsorship because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/agent.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation();\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async () => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({ conversationToken: token, connectionType: \"webrtc\" });\n};\n```\n\nAGENT SETUP: create the agent in https://elevenlabs.io/app/agents, give it a\nsystem prompt fit for the performance sponsorship use case, copy its agent id, and expose it as\n`VITE_AGENT_ID` in the project (public env, not a secret).\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for performance sponsorship.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global event sponsorship market",
      "sam": "$1.5B — theater sponsorship deals",
      "som": "$200M — voice-app based sponsorships"
    },
    {
      "id": "theater-roleswap-network-11",
      "theme": "theater",
      "title": "RoleSwap Network",
      "pitch": "Facilitate secure actor role exchanges and confirmations with social voiceprints and sponsored voice events.",
      "subDiscipline": "cast management",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "A conversational ElevenLabs agent fits cast management because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.",
      "megaPrompt": "Build \"RoleSwap Network\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFacilitate secure actor role exchanges and confirmations with social voiceprints and sponsored voice events.\nDiscipline: Theater & Live Performance (cast management).\nVoice primitive: Conversational Voice Agent (live conversation).\nWhy this primitive: A conversational ElevenLabs agent fits cast management because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/agent.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation();\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async () => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({ conversationToken: token, connectionType: \"webrtc\" });\n};\n```\n\nAGENT SETUP: create the agent in https://elevenlabs.io/app/agents, give it a\nsystem prompt fit for the cast management use case, copy its agent id, and expose it as\n`VITE_AGENT_ID` in the project (public env, not a secret).\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for cast management.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — global casting and personnel management",
      "sam": "$3B — digital casting platforms",
      "som": "$400M — voice-app casting exchanges"
    },
    {
      "id": "theater-stagedesign-dao-12",
      "theme": "theater",
      "title": "StageDesign voice collective",
      "pitch": "Create shared design collectives with gasless voting and social voiceprints onboarding.",
      "subDiscipline": "design collaboration",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "A conversational ElevenLabs agent fits design collaboration because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.",
      "megaPrompt": "Build \"StageDesign voice collective\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCreate shared design collectives with gasless voting and social voiceprints onboarding.\nDiscipline: Theater & Live Performance (design collaboration).\nVoice primitive: Conversational Voice Agent (live conversation).\nWhy this primitive: A conversational ElevenLabs agent fits design collaboration because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/agent.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation();\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async () => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({ conversationToken: token, connectionType: \"webrtc\" });\n};\n```\n\nAGENT SETUP: create the agent in https://elevenlabs.io/app/agents, give it a\nsystem prompt fit for the design collaboration use case, copy its agent id, and expose it as\n`VITE_AGENT_ID` in the project (public env, not a secret).\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for design collaboration.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — theater design collaboration",
      "sam": "$300M — digital creative DAOs",
      "som": "$40M — voice-app DAOs in live arts"
    },
    {
      "id": "theater-scripttoken-rights-13",
      "theme": "theater",
      "title": "ScriptToken Rights",
      "pitch": "Manage and transfer script rights securely via social voiceprints and sponsored on-record voice events.",
      "subDiscipline": "rights management",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "A conversational ElevenLabs agent fits rights management because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.",
      "megaPrompt": "Build \"ScriptToken Rights\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nManage and transfer script rights securely via social voiceprints and sponsored on-record voice events.\nDiscipline: Theater & Live Performance (rights management).\nVoice primitive: Conversational Voice Agent (live conversation).\nWhy this primitive: A conversational ElevenLabs agent fits rights management because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/agent.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation();\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async () => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({ conversationToken: token, connectionType: \"webrtc\" });\n};\n```\n\nAGENT SETUP: create the agent in https://elevenlabs.io/app/agents, give it a\nsystem prompt fit for the rights management use case, copy its agent id, and expose it as\n`VITE_AGENT_ID` in the project (public env, not a secret).\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for rights management.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$800M — global script rights market",
      "sam": "$250M — digital IP management",
      "som": "$30M — voice-app rights transfers"
    },
    {
      "id": "theater-cuepoint-tracker-14",
      "theme": "theater",
      "title": "CuePoint Tracker",
      "pitch": "Provide directors with gasless on-record cue tracking and adjustments via social login.",
      "subDiscipline": "live direction",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "A conversational ElevenLabs agent fits live direction because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.",
      "megaPrompt": "Build \"CuePoint Tracker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nProvide directors with gasless on-record cue tracking and adjustments via social login.\nDiscipline: Theater & Live Performance (live direction).\nVoice primitive: Conversational Voice Agent (live conversation).\nWhy this primitive: A conversational ElevenLabs agent fits live direction because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/agent.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation();\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async () => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({ conversationToken: token, connectionType: \"webrtc\" });\n};\n```\n\nAGENT SETUP: create the agent in https://elevenlabs.io/app/agents, give it a\nsystem prompt fit for the live direction use case, copy its agent id, and expose it as\n`VITE_AGENT_ID` in the project (public env, not a secret).\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for live direction.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$900M — live direction tools",
      "sam": "$300M — digital stage direction software",
      "som": "$45M — voice-app powered cue tracking"
    },
    {
      "id": "theater-actormint-ledger-15",
      "theme": "theater",
      "title": "ActorMint audio logs",
      "pitch": "publish and distribute unique actor voice assets representing roles and performances with gasless publishing.",
      "subDiscipline": "performance NFTs",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "A conversational ElevenLabs agent fits performance NFTs because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.",
      "megaPrompt": "Build \"ActorMint audio logs\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\npublish and distribute unique actor voice assets representing roles and performances with gasless publishing.\nDiscipline: Theater & Live Performance (performance NFTs).\nVoice primitive: Conversational Voice Agent (live conversation).\nWhy this primitive: A conversational ElevenLabs agent fits performance NFTs because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/agent.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation();\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async () => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({ conversationToken: token, connectionType: \"webrtc\" });\n};\n```\n\nAGENT SETUP: create the agent in https://elevenlabs.io/app/agents, give it a\nsystem prompt fit for the performance NFTs use case, copy its agent id, and expose it as\n`VITE_AGENT_ID` in the project (public env, not a secret).\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for performance NFTs.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global NFT entertainment market",
      "sam": "$500M — performance-related NFTs",
      "som": "$70M — voice-app adoption among actors"
    },
    {
      "id": "theater-setbuild-contracts-16",
      "theme": "theater",
      "title": "SetBuild Contracts",
      "pitch": "Automate set construction contracts with social voiceprints signing and no-fee on-record voice events.",
      "subDiscipline": "production agreements",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "A conversational ElevenLabs agent fits production agreements because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.",
      "megaPrompt": "Build \"SetBuild Contracts\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAutomate set construction contracts with social voiceprints signing and no-fee on-record voice events.\nDiscipline: Theater & Live Performance (production agreements).\nVoice primitive: Conversational Voice Agent (live conversation).\nWhy this primitive: A conversational ElevenLabs agent fits production agreements because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/agent.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation();\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async () => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({ conversationToken: token, connectionType: \"webrtc\" });\n};\n```\n\nAGENT SETUP: create the agent in https://elevenlabs.io/app/agents, give it a\nsystem prompt fit for the production agreements use case, copy its agent id, and expose it as\n`VITE_AGENT_ID` in the project (public env, not a secret).\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for production agreements.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — theater production services",
      "sam": "$600M — digital contract tools",
      "som": "$90M — voice-app contract adoption"
    },
    {
      "id": "theater-lightingtoken-rewards-17",
      "theme": "theater",
      "title": "LightingToken Rewards",
      "pitch": "Issue tokenized rewards to lighting crews with social voiceprints onboarding and sponsored tx.",
      "subDiscipline": "crew incentives",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "A conversational ElevenLabs agent fits crew incentives because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.",
      "megaPrompt": "Build \"LightingToken Rewards\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nIssue tokenized rewards to lighting crews with social voiceprints onboarding and sponsored tx.\nDiscipline: Theater & Live Performance (crew incentives).\nVoice primitive: Conversational Voice Agent (live conversation).\nWhy this primitive: A conversational ElevenLabs agent fits crew incentives because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/agent.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation();\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async () => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({ conversationToken: token, connectionType: \"webrtc\" });\n};\n```\n\nAGENT SETUP: create the agent in https://elevenlabs.io/app/agents, give it a\nsystem prompt fit for the crew incentives use case, copy its agent id, and expose it as\n`VITE_AGENT_ID` in the project (public env, not a secret).\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for crew incentives.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$500M — stage crew management",
      "sam": "$150M — digital workforce incentives",
      "som": "$20M — voice-app driven crew rewards"
    },
    {
      "id": "theater-playwright-guild-18",
      "theme": "theater",
      "title": "Playwright Guild",
      "pitch": "Create tokenized playwright guilds with seamless social login and gasless member voice events.",
      "subDiscipline": "community membership",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "A conversational ElevenLabs agent fits community membership because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.",
      "megaPrompt": "Build \"Playwright Guild\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCreate tokenized playwright guilds with seamless social login and gasless member voice events.\nDiscipline: Theater & Live Performance (community membership).\nVoice primitive: Conversational Voice Agent (live conversation).\nWhy this primitive: A conversational ElevenLabs agent fits community membership because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/agent.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation();\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async () => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({ conversationToken: token, connectionType: \"webrtc\" });\n};\n```\n\nAGENT SETUP: create the agent in https://elevenlabs.io/app/agents, give it a\nsystem prompt fit for the community membership use case, copy its agent id, and expose it as\n`VITE_AGENT_ID` in the project (public env, not a secret).\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for community membership.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$400M — playwright associations",
      "sam": "$100M — digital community platforms",
      "som": "$15M — voice-app based guilds"
    },
    {
      "id": "theater-audiencetip-jar-19",
      "theme": "theater",
      "title": "AudienceTip Jar",
      "pitch": "Let audiences tip performers instantly with social voiceprints and sponsored voice events gaslessness.",
      "subDiscipline": "fan monetization",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "A conversational ElevenLabs agent fits fan monetization because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.",
      "megaPrompt": "Build \"AudienceTip Jar\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLet audiences tip performers instantly with social voiceprints and sponsored voice events gaslessness.\nDiscipline: Theater & Live Performance (fan monetization).\nVoice primitive: Conversational Voice Agent (live conversation).\nWhy this primitive: A conversational ElevenLabs agent fits fan monetization because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/agent.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation();\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async () => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({ conversationToken: token, connectionType: \"webrtc\" });\n};\n```\n\nAGENT SETUP: create the agent in https://elevenlabs.io/app/agents, give it a\nsystem prompt fit for the fan monetization use case, copy its agent id, and expose it as\n`VITE_AGENT_ID` in the project (public env, not a secret).\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for fan monetization.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — live performance monetization",
      "sam": "$700M — digital tipping platforms",
      "som": "$100M — voice-app tip solutions"
    },
    {
      "id": "theater-promochain-posters-20",
      "theme": "theater",
      "title": "PromoChain Posters",
      "pitch": "Distribute limited-edition promo posters as voice assets with easy social voiceprints publishing and free tx.",
      "subDiscipline": "marketing assets",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "A conversational ElevenLabs agent fits marketing assets because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.",
      "megaPrompt": "Build \"PromoChain Posters\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDistribute limited-edition promo posters as voice assets with easy social voiceprints publishing and free tx.\nDiscipline: Theater & Live Performance (marketing assets).\nVoice primitive: Conversational Voice Agent (live conversation).\nWhy this primitive: A conversational ElevenLabs agent fits marketing assets because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/agent.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation();\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async () => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({ conversationToken: token, connectionType: \"webrtc\" });\n};\n```\n\nAGENT SETUP: create the agent in https://elevenlabs.io/app/agents, give it a\nsystem prompt fit for the marketing assets use case, copy its agent id, and expose it as\n`VITE_AGENT_ID` in the project (public env, not a secret).\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for marketing assets.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — live show marketing",
      "sam": "$300M — digital promotional collectibles",
      "som": "$40M — NFT promo innovations"
    },
    {
      "id": "theater-scriptarchive-dao-21",
      "theme": "theater",
      "title": "ScriptArchive voice collective",
      "pitch": "Preserve scripts on-record with collaborative voice collective management and gasless voice events via social voiceprints.",
      "subDiscipline": "preservation",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "A conversational ElevenLabs agent fits preservation because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.",
      "megaPrompt": "Build \"ScriptArchive voice collective\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPreserve scripts on-record with collaborative voice collective management and gasless voice events via social voiceprints.\nDiscipline: Theater & Live Performance (preservation).\nVoice primitive: Conversational Voice Agent (live conversation).\nWhy this primitive: A conversational ElevenLabs agent fits preservation because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/agent.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation();\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async () => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({ conversationToken: token, connectionType: \"webrtc\" });\n};\n```\n\nAGENT SETUP: create the agent in https://elevenlabs.io/app/agents, give it a\nsystem prompt fit for the preservation use case, copy its agent id, and expose it as\n`VITE_AGENT_ID` in the project (public env, not a secret).\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for preservation.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$600M — script preservation",
      "sam": "$200M — digital literary archives",
      "som": "$30M — voice-app archival DAOs"
    },
    {
      "id": "theater-virtualstage-access-22",
      "theme": "theater",
      "title": "VirtualStage Access",
      "pitch": "Grant gasless access to hybrid live/virtual shows via social voiceprints login and sponsored tx.",
      "subDiscipline": "hybrid performances",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "A conversational ElevenLabs agent fits hybrid performances because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.",
      "megaPrompt": "Build \"VirtualStage Access\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nGrant gasless access to hybrid live/virtual shows via social voiceprints login and sponsored tx.\nDiscipline: Theater & Live Performance (hybrid performances).\nVoice primitive: Conversational Voice Agent (live conversation).\nWhy this primitive: A conversational ElevenLabs agent fits hybrid performances because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/agent.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation();\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async () => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({ conversationToken: token, connectionType: \"webrtc\" });\n};\n```\n\nAGENT SETUP: create the agent in https://elevenlabs.io/app/agents, give it a\nsystem prompt fit for the hybrid performances use case, copy its agent id, and expose it as\n`VITE_AGENT_ID` in the project (public env, not a secret).\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for hybrid performances.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — hybrid live/virtual performances",
      "sam": "$1.5B — digital event access",
      "som": "$200M — voice-app ticketing for hybrid shows"
    },
    {
      "id": "theater-critiquechain-feedback-23",
      "theme": "theater",
      "title": "CritiqueChain Feedback",
      "pitch": "Collect verified audience critiques on-record with social login and no-fee feedback voice events.",
      "subDiscipline": "performance reviews",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "A conversational ElevenLabs agent fits performance reviews because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.",
      "megaPrompt": "Build \"CritiqueChain Feedback\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCollect verified audience critiques on-record with social login and no-fee feedback voice events.\nDiscipline: Theater & Live Performance (performance reviews).\nVoice primitive: Conversational Voice Agent (live conversation).\nWhy this primitive: A conversational ElevenLabs agent fits performance reviews because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/agent.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation();\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async () => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({ conversationToken: token, connectionType: \"webrtc\" });\n};\n```\n\nAGENT SETUP: create the agent in https://elevenlabs.io/app/agents, give it a\nsystem prompt fit for the performance reviews use case, copy its agent id, and expose it as\n`VITE_AGENT_ID` in the project (public env, not a secret).\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for performance reviews.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$700M — performance feedback market",
      "sam": "$200M — digital critique platforms",
      "som": "$30M — voice-app feedback adoption"
    },
    {
      "id": "theater-setinventory-nfts-24",
      "theme": "theater",
      "title": "SetInventory voice assets",
      "pitch": "Tokenize set inventory for easy rental and ownership tracking with social voiceprints and free tx.",
      "subDiscipline": "asset tokenization",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "A conversational ElevenLabs agent fits asset tokenization because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.",
      "megaPrompt": "Build \"SetInventory voice assets\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTokenize set inventory for easy rental and ownership tracking with social voiceprints and free tx.\nDiscipline: Theater & Live Performance (asset tokenization).\nVoice primitive: Conversational Voice Agent (live conversation).\nWhy this primitive: A conversational ElevenLabs agent fits asset tokenization because the work is dialogic — the user wants to talk to Theater & Live Performance, not click through a form.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/agent.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation();\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async () => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({ conversationToken: token, connectionType: \"webrtc\" });\n};\n```\n\nAGENT SETUP: create the agent in https://elevenlabs.io/app/agents, give it a\nsystem prompt fit for the asset tokenization use case, copy its agent id, and expose it as\n`VITE_AGENT_ID` in the project (public env, not a secret).\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for asset tokenization.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$900M — set asset market",
      "sam": "$300M — digital asset tokenization",
      "som": "$40M — voice-app asset management"
    },
    {
      "id": "theater-scene-script-provenance-0",
      "theme": "theater",
      "title": "Scene Script Provenance",
      "pitch": "Securely publish and track original play scripts as creator-owned voice assets.",
      "subDiscipline": "playwriting",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime scribe is the right primitive for playwriting: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.",
      "megaPrompt": "Build \"Scene Script Provenance\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSecurely publish and track original play scripts as creator-owned voice assets.\nDiscipline: Theater & Live Performance (playwriting).\nVoice primitive: Realtime Speech-to-Text (live transcription).\nWhy this primitive: Realtime scribe is the right primitive for playwriting: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/scribe.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintScribeToken } from \"@/lib/scribe.functions\";\n\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\nconst getToken = useServerFn(mintScribeToken);\n\nconst start = async () => {\n  const { token } = await getToken();\n  await scribe.connect({\n    token,\n    microphone: { echoCancellation: true, noiseSuppression: true },\n  });\n};\n```\n\nRENDER: show `scribe.partialTranscript` live, and append each\n`scribe.committedTranscripts[i].text` to the finalized list as VAD detects the\npause. Perfect for playwriting where the user is speaking the work into the page.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for playwriting.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global digital script distribution market",
      "sam": "$500M — playwrights adopting digital rights management",
      "som": "$50M — playwrights using NFT-based copyright tools"
    },
    {
      "id": "theater-lighting-cue-chain-1",
      "theme": "theater",
      "title": "Lighting Cue Chain",
      "pitch": "Record and verify lighting cue sequences as unique voice assets for live performances.",
      "subDiscipline": "lighting design",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime scribe is the right primitive for lighting design: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.",
      "megaPrompt": "Build \"Lighting Cue Chain\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nRecord and verify lighting cue sequences as unique voice assets for live performances.\nDiscipline: Theater & Live Performance (lighting design).\nVoice primitive: Realtime Speech-to-Text (live transcription).\nWhy this primitive: Realtime scribe is the right primitive for lighting design: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/scribe.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintScribeToken } from \"@/lib/scribe.functions\";\n\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\nconst getToken = useServerFn(mintScribeToken);\n\nconst start = async () => {\n  const { token } = await getToken();\n  await scribe.connect({\n    token,\n    microphone: { echoCancellation: true, noiseSuppression: true },\n  });\n};\n```\n\nRENDER: show `scribe.partialTranscript` live, and append each\n`scribe.committedTranscripts[i].text` to the finalized list as VAD detects the\npause. Perfect for lighting design where the user is speaking the work into the page.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for lighting design.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — global stage lighting equipment and software market",
      "sam": "$200M — digital lighting design tools",
      "som": "$20M — NFT adoption by lighting professionals"
    },
    {
      "id": "theater-actor-rehearsal-logs-2",
      "theme": "theater",
      "title": "Actor Rehearsal Logs",
      "pitch": "Actors publish daily rehearsal videos as voice assets to prove practice and progress.",
      "subDiscipline": "performance tracking",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Onchain minting timestamps rehearsal footage with verified ownership by performers.",
      "megaPrompt": "Build \"Actor Rehearsal Logs\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nActors publish daily rehearsal videos as voice assets to prove practice and progress.\nDiscipline: Theater & Live Performance (performance tracking).\nVoice primitive: Realtime Speech-to-Text (live transcription).\nWhy this primitive: Onchain minting timestamps rehearsal footage with verified ownership by performers.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/scribe.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintScribeToken } from \"@/lib/scribe.functions\";\n\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\nconst getToken = useServerFn(mintScribeToken);\n\nconst start = async () => {\n  const { token } = await getToken();\n  await scribe.connect({\n    token,\n    microphone: { echoCancellation: true, noiseSuppression: true },\n  });\n};\n```\n\nRENDER: show `scribe.partialTranscript` live, and append each\n`scribe.committedTranscripts[i].text` to the finalized list as VAD detects the\npause. Perfect for performance tracking where the user is speaking the work into the page.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for performance tracking.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — actor training and rehearsal platforms",
      "sam": "$300M — digital performance coaching tools",
      "som": "$30M — NFT solutions for performance documentation"
    },
    {
      "id": "theater-set-design-archives-3",
      "theme": "theater",
      "title": "Set Design Archives",
      "pitch": "publish 3D set designs as voice assets for secure sharing and historical provenance.",
      "subDiscipline": "scenic design",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime scribe is the right primitive for scenic design: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.",
      "megaPrompt": "Build \"Set Design Archives\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\npublish 3D set designs as voice assets for secure sharing and historical provenance.\nDiscipline: Theater & Live Performance (scenic design).\nVoice primitive: Realtime Speech-to-Text (live transcription).\nWhy this primitive: Realtime scribe is the right primitive for scenic design: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/scribe.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintScribeToken } from \"@/lib/scribe.functions\";\n\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\nconst getToken = useServerFn(mintScribeToken);\n\nconst start = async () => {\n  const { token } = await getToken();\n  await scribe.connect({\n    token,\n    microphone: { echoCancellation: true, noiseSuppression: true },\n  });\n};\n```\n\nRENDER: show `scribe.partialTranscript` live, and append each\n`scribe.committedTranscripts[i].text` to the finalized list as VAD detects the\npause. Perfect for scenic design where the user is speaking the work into the page.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for scenic design.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$800M — global set design and prop market",
      "sam": "$150M — digital scenic design software",
      "som": "$15M — NFT-based archival tools for set designers"
    },
    {
      "id": "theater-playwright-feedback-chain-4",
      "theme": "theater",
      "title": "Playwright Feedback Chain",
      "pitch": "publish feedback and revisions on scripts as voice assets for transparent version control.",
      "subDiscipline": "script development",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime scribe is the right primitive for script development: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.",
      "megaPrompt": "Build \"Playwright Feedback Chain\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\npublish feedback and revisions on scripts as voice assets for transparent version control.\nDiscipline: Theater & Live Performance (script development).\nVoice primitive: Realtime Speech-to-Text (live transcription).\nWhy this primitive: Realtime scribe is the right primitive for script development: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/scribe.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintScribeToken } from \"@/lib/scribe.functions\";\n\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\nconst getToken = useServerFn(mintScribeToken);\n\nconst start = async () => {\n  const { token } = await getToken();\n  await scribe.connect({\n    token,\n    microphone: { echoCancellation: true, noiseSuppression: true },\n  });\n};\n```\n\nRENDER: show `scribe.partialTranscript` live, and append each\n`scribe.committedTranscripts[i].text` to the finalized list as VAD detects the\npause. Perfect for script development where the user is speaking the work into the page.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for script development.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.2B — script editing and collaboration tools",
      "sam": "$250M — playwright collaboration platforms",
      "som": "$25M — NFT-enabled revision management"
    },
    {
      "id": "theater-performance-nft-tickets-5",
      "theme": "theater",
      "title": "Performance voice asset Tickets",
      "pitch": "Issue voice asset tickets proving legitimate access and unique attendance experience.",
      "subDiscipline": "audience engagement",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime scribe is the right primitive for audience engagement: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.",
      "megaPrompt": "Build \"Performance voice asset Tickets\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nIssue voice asset tickets proving legitimate access and unique attendance experience.\nDiscipline: Theater & Live Performance (audience engagement).\nVoice primitive: Realtime Speech-to-Text (live transcription).\nWhy this primitive: Realtime scribe is the right primitive for audience engagement: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/scribe.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintScribeToken } from \"@/lib/scribe.functions\";\n\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\nconst getToken = useServerFn(mintScribeToken);\n\nconst start = async () => {\n  const { token } = await getToken();\n  await scribe.connect({\n    token,\n    microphone: { echoCancellation: true, noiseSuppression: true },\n  });\n};\n```\n\nRENDER: show `scribe.partialTranscript` live, and append each\n`scribe.committedTranscripts[i].text` to the finalized list as VAD detects the\npause. Perfect for audience engagement where the user is speaking the work into the page.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for audience engagement.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — global event ticketing market",
      "sam": "$1B — digital ticketing solutions",
      "som": "$100M — NFT ticket use in live performance"
    },
    {
      "id": "theater-costume-provenance-chain-6",
      "theme": "theater",
      "title": "Costume Provenance Chain",
      "pitch": "publish costume designs and ownership as voice assets to protect designer rights and provenance.",
      "subDiscipline": "costume design",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime scribe is the right primitive for costume design: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.",
      "megaPrompt": "Build \"Costume Provenance Chain\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\npublish costume designs and ownership as voice assets to protect designer rights and provenance.\nDiscipline: Theater & Live Performance (costume design).\nVoice primitive: Realtime Speech-to-Text (live transcription).\nWhy this primitive: Realtime scribe is the right primitive for costume design: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/scribe.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintScribeToken } from \"@/lib/scribe.functions\";\n\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\nconst getToken = useServerFn(mintScribeToken);\n\nconst start = async () => {\n  const { token } = await getToken();\n  await scribe.connect({\n    token,\n    microphone: { echoCancellation: true, noiseSuppression: true },\n  });\n};\n```\n\nRENDER: show `scribe.partialTranscript` live, and append each\n`scribe.committedTranscripts[i].text` to the finalized list as VAD detects the\npause. Perfect for costume design where the user is speaking the work into the page.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for costume design.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$600M — costume and wardrobe market",
      "sam": "$120M — digital costume design assets",
      "som": "$12M — NFT adoption in costume provenance"
    },
    {
      "id": "theater-monologue-minting-hub-7",
      "theme": "theater",
      "title": "Monologue publishing Hub",
      "pitch": "Actors publish monologues as voice assets to showcase and monetize original performances.",
      "subDiscipline": "acting",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime scribe is the right primitive for acting: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.",
      "megaPrompt": "Build \"Monologue publishing Hub\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nActors publish monologues as voice assets to showcase and monetize original performances.\nDiscipline: Theater & Live Performance (acting).\nVoice primitive: Realtime Speech-to-Text (live transcription).\nWhy this primitive: Realtime scribe is the right primitive for acting: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/scribe.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintScribeToken } from \"@/lib/scribe.functions\";\n\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\nconst getToken = useServerFn(mintScribeToken);\n\nconst start = async () => {\n  const { token } = await getToken();\n  await scribe.connect({\n    token,\n    microphone: { echoCancellation: true, noiseSuppression: true },\n  });\n};\n```\n\nRENDER: show `scribe.partialTranscript` live, and append each\n`scribe.committedTranscripts[i].text` to the finalized list as VAD detects the\npause. Perfect for acting where the user is speaking the work into the page.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for acting.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$700M — actor content creation market",
      "sam": "$140M — digital acting portfolios",
      "som": "$14M — NFT monologue platforms"
    },
    {
      "id": "theater-soundscape-provenance-8",
      "theme": "theater",
      "title": "Soundscape Provenance",
      "pitch": "publish live performance soundscapes as voice assets to authenticate original audio environments.",
      "subDiscipline": "sound design",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime scribe is the right primitive for sound design: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.",
      "megaPrompt": "Build \"Soundscape Provenance\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\npublish live performance soundscapes as voice assets to authenticate original audio environments.\nDiscipline: Theater & Live Performance (sound design).\nVoice primitive: Realtime Speech-to-Text (live transcription).\nWhy this primitive: Realtime scribe is the right primitive for sound design: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/scribe.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintScribeToken } from \"@/lib/scribe.functions\";\n\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\nconst getToken = useServerFn(mintScribeToken);\n\nconst start = async () => {\n  const { token } = await getToken();\n  await scribe.connect({\n    token,\n    microphone: { echoCancellation: true, noiseSuppression: true },\n  });\n};\n```\n\nRENDER: show `scribe.partialTranscript` live, and append each\n`scribe.committedTranscripts[i].text` to the finalized list as VAD detects the\npause. Perfect for sound design where the user is speaking the work into the page.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for sound design.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$900M — live sound engineering market",
      "sam": "$180M — digital sound design tools",
      "som": "$18M — NFT soundscape authentication"
    },
    {
      "id": "theater-director-s-vision-ledger-9",
      "theme": "theater",
      "title": "Director's Vision audio logs",
      "pitch": "Directors publish annotated scripts as voice assets capturing their unique vision and notes.",
      "subDiscipline": "direction",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime scribe is the right primitive for direction: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.",
      "megaPrompt": "Build \"Director's Vision audio logs\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDirectors publish annotated scripts as voice assets capturing their unique vision and notes.\nDiscipline: Theater & Live Performance (direction).\nVoice primitive: Realtime Speech-to-Text (live transcription).\nWhy this primitive: Realtime scribe is the right primitive for direction: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/scribe.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintScribeToken } from \"@/lib/scribe.functions\";\n\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\nconst getToken = useServerFn(mintScribeToken);\n\nconst start = async () => {\n  const { token } = await getToken();\n  await scribe.connect({\n    token,\n    microphone: { echoCancellation: true, noiseSuppression: true },\n  });\n};\n```\n\nRENDER: show `scribe.partialTranscript` live, and append each\n`scribe.committedTranscripts[i].text` to the finalized list as VAD detects the\npause. Perfect for direction where the user is speaking the work into the page.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for direction.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — digital script annotation market",
      "sam": "$200M — director collaboration software",
      "som": "$20M — NFT-based vision tracking"
    },
    {
      "id": "theater-interactive-stage-nfts-10",
      "theme": "theater",
      "title": "Interactive Stage voice assets",
      "pitch": "publish interactive performance elements as voice assets to prove creative ownership.",
      "subDiscipline": "stage interaction",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime scribe is the right primitive for stage interaction: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.",
      "megaPrompt": "Build \"Interactive Stage voice assets\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\npublish interactive performance elements as voice assets to prove creative ownership.\nDiscipline: Theater & Live Performance (stage interaction).\nVoice primitive: Realtime Speech-to-Text (live transcription).\nWhy this primitive: Realtime scribe is the right primitive for stage interaction: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/scribe.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintScribeToken } from \"@/lib/scribe.functions\";\n\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\nconst getToken = useServerFn(mintScribeToken);\n\nconst start = async () => {\n  const { token } = await getToken();\n  await scribe.connect({\n    token,\n    microphone: { echoCancellation: true, noiseSuppression: true },\n  });\n};\n```\n\nRENDER: show `scribe.partialTranscript` live, and append each\n`scribe.committedTranscripts[i].text` to the finalized list as VAD detects the\npause. Perfect for stage interaction where the user is speaking the work into the page.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for stage interaction.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$500M — interactive theater technology",
      "sam": "$100M — interactive stage asset markets",
      "som": "$10M — NFT use in interactive performances"
    },
    {
      "id": "theater-improv-moment-mint-11",
      "theme": "theater",
      "title": "Improv Moment publish",
      "pitch": "Actors publish spontaneous improv performances as voice assets to preserve unique moments.",
      "subDiscipline": "improvisation",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime scribe is the right primitive for improvisation: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.",
      "megaPrompt": "Build \"Improv Moment publish\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nActors publish spontaneous improv performances as voice assets to preserve unique moments.\nDiscipline: Theater & Live Performance (improvisation).\nVoice primitive: Realtime Speech-to-Text (live transcription).\nWhy this primitive: Realtime scribe is the right primitive for improvisation: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/scribe.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintScribeToken } from \"@/lib/scribe.functions\";\n\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\nconst getToken = useServerFn(mintScribeToken);\n\nconst start = async () => {\n  const { token } = await getToken();\n  await scribe.connect({\n    token,\n    microphone: { echoCancellation: true, noiseSuppression: true },\n  });\n};\n```\n\nRENDER: show `scribe.partialTranscript` live, and append each\n`scribe.committedTranscripts[i].text` to the finalized list as VAD detects the\npause. Perfect for improvisation where the user is speaking the work into the page.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for improvisation.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$300M — improv and live performance recording",
      "sam": "$60M — digital improv content",
      "som": "$6M — NFT improv minting platforms"
    },
    {
      "id": "theater-choreography-chain-12",
      "theme": "theater",
      "title": "Choreography Chain",
      "pitch": "publish choreography sequences as voice assets ensuring original dance and movement rights.",
      "subDiscipline": "movement design",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime scribe is the right primitive for movement design: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.",
      "megaPrompt": "Build \"Choreography Chain\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\npublish choreography sequences as voice assets ensuring original dance and movement rights.\nDiscipline: Theater & Live Performance (movement design).\nVoice primitive: Realtime Speech-to-Text (live transcription).\nWhy this primitive: Realtime scribe is the right primitive for movement design: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/scribe.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintScribeToken } from \"@/lib/scribe.functions\";\n\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\nconst getToken = useServerFn(mintScribeToken);\n\nconst start = async () => {\n  const { token } = await getToken();\n  await scribe.connect({\n    token,\n    microphone: { echoCancellation: true, noiseSuppression: true },\n  });\n};\n```\n\nRENDER: show `scribe.partialTranscript` live, and append each\n`scribe.committedTranscripts[i].text` to the finalized list as VAD detects the\npause. Perfect for movement design where the user is speaking the work into the page.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for movement design.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.8B — dance and choreography market",
      "sam": "$360M — digital choreography tools",
      "som": "$36M — NFT-based choreography protection"
    },
    {
      "id": "theater-stage-props-provenance-13",
      "theme": "theater",
      "title": "Stage Props Provenance",
      "pitch": "publish digital twins of stage props as voice assets for ownership and rental tracking.",
      "subDiscipline": "prop management",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime scribe is the right primitive for prop management: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.",
      "megaPrompt": "Build \"Stage Props Provenance\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\npublish digital twins of stage props as voice assets for ownership and rental tracking.\nDiscipline: Theater & Live Performance (prop management).\nVoice primitive: Realtime Speech-to-Text (live transcription).\nWhy this primitive: Realtime scribe is the right primitive for prop management: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/scribe.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintScribeToken } from \"@/lib/scribe.functions\";\n\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\nconst getToken = useServerFn(mintScribeToken);\n\nconst start = async () => {\n  const { token } = await getToken();\n  await scribe.connect({\n    token,\n    microphone: { echoCancellation: true, noiseSuppression: true },\n  });\n};\n```\n\nRENDER: show `scribe.partialTranscript` live, and append each\n`scribe.committedTranscripts[i].text` to the finalized list as VAD detects the\npause. Perfect for prop management where the user is speaking the work into the page.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for prop management.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$700M — theatrical prop rental and sales",
      "sam": "$140M — prop rental platforms",
      "som": "$14M — NFT prop provenance solutions"
    },
    {
      "id": "theater-script-translation-nfts-14",
      "theme": "theater",
      "title": "Script Translation voice assets",
      "pitch": "publish translated scripts as voice assets to protect translators' intellectual property.",
      "subDiscipline": "translation",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime scribe is the right primitive for translation: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.",
      "megaPrompt": "Build \"Script Translation voice assets\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\npublish translated scripts as voice assets to protect translators' intellectual property.\nDiscipline: Theater & Live Performance (translation).\nVoice primitive: Realtime Speech-to-Text (live transcription).\nWhy this primitive: Realtime scribe is the right primitive for translation: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/scribe.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintScribeToken } from \"@/lib/scribe.functions\";\n\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\nconst getToken = useServerFn(mintScribeToken);\n\nconst start = async () => {\n  const { token } = await getToken();\n  await scribe.connect({\n    token,\n    microphone: { echoCancellation: true, noiseSuppression: true },\n  });\n};\n```\n\nRENDER: show `scribe.partialTranscript` live, and append each\n`scribe.committedTranscripts[i].text` to the finalized list as VAD detects the\npause. Perfect for translation where the user is speaking the work into the page.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for translation.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$500M — script translation and localization market",
      "sam": "$100M — digital translation services",
      "som": "$10M — NFT translation copyright tools"
    },
    {
      "id": "theater-audience-experience-logs-15",
      "theme": "theater",
      "title": "Audience Experience Logs",
      "pitch": "publish audience reaction recordings as voice assets to capture genuine engagement data.",
      "subDiscipline": "audience analytics",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime scribe is the right primitive for audience analytics: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.",
      "megaPrompt": "Build \"Audience Experience Logs\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\npublish audience reaction recordings as voice assets to capture genuine engagement data.\nDiscipline: Theater & Live Performance (audience analytics).\nVoice primitive: Realtime Speech-to-Text (live transcription).\nWhy this primitive: Realtime scribe is the right primitive for audience analytics: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/scribe.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintScribeToken } from \"@/lib/scribe.functions\";\n\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\nconst getToken = useServerFn(mintScribeToken);\n\nconst start = async () => {\n  const { token } = await getToken();\n  await scribe.connect({\n    token,\n    microphone: { echoCancellation: true, noiseSuppression: true },\n  });\n};\n```\n\nRENDER: show `scribe.partialTranscript` live, and append each\n`scribe.committedTranscripts[i].text` to the finalized list as VAD detects the\npause. Perfect for audience analytics where the user is speaking the work into the page.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for audience analytics.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$800M — audience engagement analytics market",
      "sam": "$160M — digital feedback tools",
      "som": "$16M — NFT feedback platforms"
    },
    {
      "id": "theater-virtual-backstage-pass-16",
      "theme": "theater",
      "title": "Virtual Backstage Pass",
      "pitch": "Issue voice asset backstage passes granting exclusive digital and live experience.",
      "subDiscipline": "fan engagement",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime scribe is the right primitive for fan engagement: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.",
      "megaPrompt": "Build \"Virtual Backstage Pass\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nIssue voice asset backstage passes granting exclusive digital and live experience.\nDiscipline: Theater & Live Performance (fan engagement).\nVoice primitive: Realtime Speech-to-Text (live transcription).\nWhy this primitive: Realtime scribe is the right primitive for fan engagement: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/scribe.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintScribeToken } from \"@/lib/scribe.functions\";\n\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\nconst getToken = useServerFn(mintScribeToken);\n\nconst start = async () => {\n  const { token } = await getToken();\n  await scribe.connect({\n    token,\n    microphone: { echoCancellation: true, noiseSuppression: true },\n  });\n};\n```\n\nRENDER: show `scribe.partialTranscript` live, and append each\n`scribe.committedTranscripts[i].text` to the finalized list as VAD detects the\npause. Perfect for fan engagement where the user is speaking the work into the page.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for fan engagement.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — fan engagement and merchandise market",
      "sam": "$600M — digital fan access platforms",
      "som": "$60M — NFT backstage pass adoption"
    },
    {
      "id": "theater-playbill-provenance-17",
      "theme": "theater",
      "title": "Playbill Provenance",
      "pitch": "publish digital playbills as voice assets to preserve historical performance records.",
      "subDiscipline": "program design",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime scribe is the right primitive for program design: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.",
      "megaPrompt": "Build \"Playbill Provenance\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\npublish digital playbills as voice assets to preserve historical performance records.\nDiscipline: Theater & Live Performance (program design).\nVoice primitive: Realtime Speech-to-Text (live transcription).\nWhy this primitive: Realtime scribe is the right primitive for program design: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/scribe.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintScribeToken } from \"@/lib/scribe.functions\";\n\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\nconst getToken = useServerFn(mintScribeToken);\n\nconst start = async () => {\n  const { token } = await getToken();\n  await scribe.connect({\n    token,\n    microphone: { echoCancellation: true, noiseSuppression: true },\n  });\n};\n```\n\nRENDER: show `scribe.partialTranscript` live, and append each\n`scribe.committedTranscripts[i].text` to the finalized list as VAD detects the\npause. Perfect for program design where the user is speaking the work into the page.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for program design.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$400M — theater program and souvenir market",
      "sam": "$80M — digital playbill platforms",
      "som": "$8M — NFT archival playbill solutions"
    },
    {
      "id": "theater-set-lighting-automation-nfts-18",
      "theme": "theater",
      "title": "Set Lighting Automation voice assets",
      "pitch": "publish automated lighting sequences as voice assets to secure ownership and resale rights.",
      "subDiscipline": "lighting tech",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime scribe is the right primitive for lighting tech: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.",
      "megaPrompt": "Build \"Set Lighting Automation voice assets\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\npublish automated lighting sequences as voice assets to secure ownership and resale rights.\nDiscipline: Theater & Live Performance (lighting tech).\nVoice primitive: Realtime Speech-to-Text (live transcription).\nWhy this primitive: Realtime scribe is the right primitive for lighting tech: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/scribe.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintScribeToken } from \"@/lib/scribe.functions\";\n\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\nconst getToken = useServerFn(mintScribeToken);\n\nconst start = async () => {\n  const { token } = await getToken();\n  await scribe.connect({\n    token,\n    microphone: { echoCancellation: true, noiseSuppression: true },\n  });\n};\n```\n\nRENDER: show `scribe.partialTranscript` live, and append each\n`scribe.committedTranscripts[i].text` to the finalized list as VAD detects the\npause. Perfect for lighting tech where the user is speaking the work into the page.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for lighting tech.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.2B — automated stage lighting market",
      "sam": "$240M — digital lighting control software",
      "som": "$24M — NFT automation asset market"
    },
    {
      "id": "theater-drama-therapy-journals-19",
      "theme": "theater",
      "title": "Drama Therapy Journals",
      "pitch": "publish personal drama therapy session outputs as voice assets to secure privacy and ownership.",
      "subDiscipline": "therapeutic performance",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime scribe is the right primitive for therapeutic performance: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.",
      "megaPrompt": "Build \"Drama Therapy Journals\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\npublish personal drama therapy session outputs as voice assets to secure privacy and ownership.\nDiscipline: Theater & Live Performance (therapeutic performance).\nVoice primitive: Realtime Speech-to-Text (live transcription).\nWhy this primitive: Realtime scribe is the right primitive for therapeutic performance: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/scribe.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintScribeToken } from \"@/lib/scribe.functions\";\n\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\nconst getToken = useServerFn(mintScribeToken);\n\nconst start = async () => {\n  const { token } = await getToken();\n  await scribe.connect({\n    token,\n    microphone: { echoCancellation: true, noiseSuppression: true },\n  });\n};\n```\n\nRENDER: show `scribe.partialTranscript` live, and append each\n`scribe.committedTranscripts[i].text` to the finalized list as VAD detects the\npause. Perfect for therapeutic performance where the user is speaking the work into the page.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for therapeutic performance.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$600M — drama therapy and wellness market",
      "sam": "$120M — digital therapy documentation",
      "som": "$12M — NFT-secured therapy journals"
    },
    {
      "id": "theater-costume-rental-nft-20",
      "theme": "theater",
      "title": "Costume Rental voice asset",
      "pitch": "publish voice assets representing costume rental agreements to streamline voice events.",
      "subDiscipline": "rental services",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime scribe is the right primitive for rental services: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.",
      "megaPrompt": "Build \"Costume Rental voice asset\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\npublish voice assets representing costume rental agreements to streamline voice events.\nDiscipline: Theater & Live Performance (rental services).\nVoice primitive: Realtime Speech-to-Text (live transcription).\nWhy this primitive: Realtime scribe is the right primitive for rental services: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/scribe.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintScribeToken } from \"@/lib/scribe.functions\";\n\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\nconst getToken = useServerFn(mintScribeToken);\n\nconst start = async () => {\n  const { token } = await getToken();\n  await scribe.connect({\n    token,\n    microphone: { echoCancellation: true, noiseSuppression: true },\n  });\n};\n```\n\nRENDER: show `scribe.partialTranscript` live, and append each\n`scribe.committedTranscripts[i].text` to the finalized list as VAD detects the\npause. Perfect for rental services where the user is speaking the work into the page.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for rental services.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$900M — costume rental market",
      "sam": "$180M — digital rental management platforms",
      "som": "$18M — NFT rental solutions"
    },
    {
      "id": "theater-playwright-royalty-nfts-21",
      "theme": "theater",
      "title": "Playwright credit splits voice assets",
      "pitch": "publish credit splits shares as voice assets to transparently track playwriting revenue splits.",
      "subDiscipline": "royalty management",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime scribe is the right primitive for royalty management: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.",
      "megaPrompt": "Build \"Playwright credit splits voice assets\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\npublish credit splits shares as voice assets to transparently track playwriting revenue splits.\nDiscipline: Theater & Live Performance (royalty management).\nVoice primitive: Realtime Speech-to-Text (live transcription).\nWhy this primitive: Realtime scribe is the right primitive for royalty management: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/scribe.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintScribeToken } from \"@/lib/scribe.functions\";\n\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\nconst getToken = useServerFn(mintScribeToken);\n\nconst start = async () => {\n  const { token } = await getToken();\n  await scribe.connect({\n    token,\n    microphone: { echoCancellation: true, noiseSuppression: true },\n  });\n};\n```\n\nRENDER: show `scribe.partialTranscript` live, and append each\n`scribe.committedTranscripts[i].text` to the finalized list as VAD detects the\npause. Perfect for royalty management where the user is speaking the work into the page.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for royalty management.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — playwriting royalty market",
      "sam": "$300M — digital royalty tracking",
      "som": "$30M — NFT royalty platforms"
    },
    {
      "id": "theater-performance-highlight-reels-22",
      "theme": "theater",
      "title": "Performance Highlight Reels",
      "pitch": "Actors publish highlight reels as voice assets to prove originality and attract bookings.",
      "subDiscipline": "promotion",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime scribe is the right primitive for promotion: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.",
      "megaPrompt": "Build \"Performance Highlight Reels\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nActors publish highlight reels as voice assets to prove originality and attract bookings.\nDiscipline: Theater & Live Performance (promotion).\nVoice primitive: Realtime Speech-to-Text (live transcription).\nWhy this primitive: Realtime scribe is the right primitive for promotion: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/scribe.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintScribeToken } from \"@/lib/scribe.functions\";\n\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\nconst getToken = useServerFn(mintScribeToken);\n\nconst start = async () => {\n  const { token } = await getToken();\n  await scribe.connect({\n    token,\n    microphone: { echoCancellation: true, noiseSuppression: true },\n  });\n};\n```\n\nRENDER: show `scribe.partialTranscript` live, and append each\n`scribe.committedTranscripts[i].text` to the finalized list as VAD detects the\npause. Perfect for promotion where the user is speaking the work into the page.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for promotion.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — digital actor marketing market",
      "sam": "$200M — highlight reel platforms",
      "som": "$20M — NFT promotional tools"
    },
    {
      "id": "theater-virtual-set-nfts-23",
      "theme": "theater",
      "title": "Virtual Set voice assets",
      "pitch": "publish virtual set designs as voice assets for use in augmented and virtual performances.",
      "subDiscipline": "digital scenography",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime scribe is the right primitive for digital scenography: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.",
      "megaPrompt": "Build \"Virtual Set voice assets\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\npublish virtual set designs as voice assets for use in augmented and virtual performances.\nDiscipline: Theater & Live Performance (digital scenography).\nVoice primitive: Realtime Speech-to-Text (live transcription).\nWhy this primitive: Realtime scribe is the right primitive for digital scenography: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/scribe.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintScribeToken } from \"@/lib/scribe.functions\";\n\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\nconst getToken = useServerFn(mintScribeToken);\n\nconst start = async () => {\n  const { token } = await getToken();\n  await scribe.connect({\n    token,\n    microphone: { echoCancellation: true, noiseSuppression: true },\n  });\n};\n```\n\nRENDER: show `scribe.partialTranscript` live, and append each\n`scribe.committedTranscripts[i].text` to the finalized list as VAD detects the\npause. Perfect for digital scenography where the user is speaking the work into the page.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for digital scenography.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.3B — virtual and augmented theater market",
      "sam": "$260M — digital scenography platforms",
      "som": "$26M — NFT virtual set marketplaces"
    },
    {
      "id": "theater-script-ownership-registry-24",
      "theme": "theater",
      "title": "Script Ownership Registry",
      "pitch": "publish scripts as voice assets to create an immutable ownership registry for playwrights.",
      "subDiscipline": "legal protection",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime scribe is the right primitive for legal protection: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.",
      "megaPrompt": "Build \"Script Ownership Registry\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\npublish scripts as voice assets to create an immutable ownership registry for playwrights.\nDiscipline: Theater & Live Performance (legal protection).\nVoice primitive: Realtime Speech-to-Text (live transcription).\nWhy this primitive: Realtime scribe is the right primitive for legal protection: the user speaks the work into existence and Theater & Live Performance needs the captions to land as the words land.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/*.functions.ts` that proxies ElevenLabs\n  and keeps `ELEVENLABS_API_KEY` on the server. No other server fns.\n- ONE client surface (a button, a mic, or a prompt box) wired to that server fn.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Use ONLY libraries already in the template plus, if the kernel requires it,\n  a single `bun add @elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app (the index route).\n- All ElevenLabs calls go through a `createServerFn` so the API key stays on the server.\n- Client surface fits the kernel: a `<button>` for TTS / music / SFX, a mic for agents, a live caption strip for scribe.\n- Tailwind + shadcn for the UI. Keep it editorial: gold accent on a dark or warm-cream background, generous type.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nSERVER FUNCTION (src/lib/scribe.functions.ts):\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintScribeToken } from \"@/lib/scribe.functions\";\n\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\nconst getToken = useServerFn(mintScribeToken);\n\nconst start = async () => {\n  const { token } = await getToken();\n  await scribe.connect({\n    token,\n    microphone: { echoCancellation: true, noiseSuppression: true },\n  });\n};\n```\n\nRENDER: show `scribe.partialTranscript` live, and append each\n`scribe.committedTranscripts[i].text` to the finalized list as VAD detects the\npause. Perfect for legal protection where the user is speaking the work into the page.\n\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on page; the headline previews what the demo does for legal protection.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. After the audio plays / the conversation ends / the transcript commits, the result stays on screen and can be retried or shared.\n\nREQUIRED SECRET — `ELEVENLABS_API_KEY` (server-side only):\nSingle key for TTS, voice agents, scribe, music and SFX.\n\nBEST PATH (recommended): use the Lovable ElevenLabs standard connector.\nIt syncs `ELEVENLABS_API_KEY` into the project automatically and rotates\nfrom the dashboard. In Lovable, just say:\n    \"Connect the ElevenLabs connector\"\nand accept the prompt. No copy-pasting keys.\n\nFALLBACK (manual): grab a key from https://elevenlabs.io/app/settings/api-keys\nand add it as `ELEVENLABS_API_KEY` in Project Settings -> Secrets. Free tier\nis enough for a hackathon weekend.\n\nEither way, read it only on the server (`process.env.ELEVENLABS_API_KEY`\ninside a `createServerFn` handler). Never expose it to the client and never\nprefix it with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global script copyright management",
      "sam": "$400M — playwright legal services",
      "som": "$40M — NFT copyright registries"
    }
  ]
}