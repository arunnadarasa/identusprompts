{
  "theme": {
    "slug": "theater",
    "name": "Theater & Live Performance",
    "emoji": "🎭",
    "audience": "directors, playwrights, performers, lighting and stage designers",
    "market_anchor": "the live performance market (~$30B globally) with >100K active companies"
  },
  "ideas": [
    {
      "id": "theater-verified-playwright-credits-0",
      "theme": "theater",
      "title": "Verified Playwright Credits",
      "pitch": "Verified Playwright Credits mints a did:prism for playwright recognition so identity travels with the person, not the platform.",
      "concept": "Playwrights claim their work credits which are verified by theaters to ensure rightful authorship during productions.",
      "subDiscipline": "playwright recognition",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits playwright recognition in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Verified Playwright Credits\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPlaywrights claim their work credits which are verified by theaters to ensure rightful authorship during productions.\nDiscipline: Theater & Live Performance (playwright recognition).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits playwright recognition in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance and theater market · SAM $5B — professional playwriting and script development software · SOM $200M — indie and regional theater script licensing\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance and theater market",
      "sam": "$5B — professional playwriting and script development software",
      "som": "$200M — indie and regional theater script licensing"
    },
    {
      "id": "theater-audition-credentialing-0",
      "theme": "theater",
      "title": "Audition Credentialing",
      "pitch": "Audition Credentialing pairs two wallets over DIDComm so cast auditions can exchange trusted claims on a private channel.",
      "concept": "Actors prove their audition credentials to directors through secure invites, ensuring verified expertise and experience.",
      "subDiscipline": "cast auditions",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits cast auditions in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Audition Credentialing\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nActors prove their audition credentials to directors through secure invites, ensuring verified expertise and experience.\nDiscipline: Theater & Live Performance (cast auditions).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits cast auditions in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $4B — theater production management and directing tools · SOM $150M — educational and community theater directing resources\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$4B — theater production management and directing tools",
      "som": "$150M — educational and community theater directing resources"
    },
    {
      "id": "theater-director-credentials-0",
      "theme": "theater",
      "title": "Director Credentials",
      "pitch": "Director Credentials issues a signed verifiable credential for theater directing that the holder keeps and reuses anywhere.",
      "concept": "Directors can issue verifiable credentials to prove their experience and previous projects to casting agents.",
      "subDiscipline": "theater directing",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches theater directing in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Director Credentials\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDirectors can issue verifiable credentials to prove their experience and previous projects to casting agents.\nDiscipline: Theater & Live Performance (theater directing).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches theater directing in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $3B — actor training and rehearsal applications · SOM $100M — drama school and conservatory student tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$3B — actor training and rehearsal applications",
      "som": "$100M — drama school and conservatory student tools"
    },
    {
      "id": "theater-actor-credentials-hub-0",
      "theme": "theater",
      "title": "Actor Credentials Hub",
      "pitch": "Actor Credentials Hub verifies a credential at the gate for casting — proof without a phone call to the issuer.",
      "concept": "Actors present verified acting credits to directors, ensuring authenticity and trust in their experience and training.",
      "subDiscipline": "casting",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for casting in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Actor Credentials Hub\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nActors present verified acting credits to directors, ensuring authenticity and trust in their experience and training.\nDiscipline: Theater & Live Performance (casting).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for casting in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $2B — stage lighting and visual design software · SOM $80M — freelance lighting designer productivity tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$2B — stage lighting and visual design software",
      "som": "$80M — freelance lighting designer productivity tools"
    },
    {
      "id": "theater-stage-designer-identity-0",
      "theme": "theater",
      "title": "Stage Designer Identity",
      "pitch": "Stage Designer Identity mints a did:prism for set design so identity travels with the person, not the platform.",
      "concept": "Stage designers publish their identity and past project credentials to validate their expertise for hiring by production companies.",
      "subDiscipline": "set design",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits set design in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Stage Designer Identity\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nStage designers publish their identity and past project credentials to validate their expertise for hiring by production companies.\nDiscipline: Theater & Live Performance (set design).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits set design in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $1.5B — stage management and production scheduling software · SOM $50M — regional theater stage management operations\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$1.5B — stage management and production scheduling software",
      "som": "$50M — regional theater stage management operations"
    },
    {
      "id": "theater-backstage-access-control-0",
      "theme": "theater",
      "title": "Backstage Access Control",
      "pitch": "Backstage Access Control pairs two wallets over DIDComm so event security can exchange trusted claims on a private channel.",
      "concept": "Crew members receive verified access invitations to restricted areas, ensuring safety and authenticity of personnel on site.",
      "subDiscipline": "event security",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits event security in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Backstage Access Control\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCrew members receive verified access invitations to restricted areas, ensuring safety and authenticity of personnel on site.\nDiscipline: Theater & Live Performance (event security).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits event security in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $2.5B — performer health and vocal coaching market · SOM $120M — musical theater and opera vocal maintenance\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$2.5B — performer health and vocal coaching market",
      "som": "$120M — musical theater and opera vocal maintenance"
    },
    {
      "id": "theater-playwright-verification-0",
      "theme": "theater",
      "title": "Playwright Verification",
      "pitch": "Playwright Verification issues a signed verifiable credential for playwriting that the holder keeps and reuses anywhere.",
      "concept": "Playwrights receive signed credentials to authenticate their works, ensuring proper attribution when submitting scripts to theaters.",
      "subDiscipline": "playwriting",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches playwriting in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Playwright Verification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPlaywrights receive signed credentials to authenticate their works, ensuring proper attribution when submitting scripts to theaters.\nDiscipline: Theater & Live Performance (playwriting).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches playwriting in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $4B — global scriptwriting and localization services · SOM $90M — international theater festival script development\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$4B — global scriptwriting and localization services",
      "som": "$90M — international theater festival script development"
    },
    {
      "id": "theater-stage-access-passport-0",
      "theme": "theater",
      "title": "Stage Access Passport",
      "pitch": "Stage Access Passport verifies a credential at the gate for backstage access — proof without a phone call to the issuer.",
      "concept": "Technicians and performers prove their backstage privileges to venue staff via verified credentials for secure access.",
      "subDiscipline": "backstage access",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for backstage access in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Stage Access Passport\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTechnicians and performers prove their backstage privileges to venue staff via verified credentials for secure access.\nDiscipline: Theater & Live Performance (backstage access).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for backstage access in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $3B — theatrical directing and dramaturgy tools · SOM $75M — professional theater company rehearsal software\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$3B — theatrical directing and dramaturgy tools",
      "som": "$75M — professional theater company rehearsal software"
    },
    {
      "id": "theater-performer-accreditation-hub-0",
      "theme": "theater",
      "title": "Performer Accreditation Hub",
      "pitch": "Performer Accreditation Hub mints a did:prism for performer guilds so identity travels with the person, not the platform.",
      "concept": "Actors can prove their membership in industry guilds to casting directors for eligibility in union-required roles.",
      "subDiscipline": "performer guilds",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits performer guilds in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Performer Accreditation Hub\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nActors can prove their membership in industry guilds to casting directors for eligibility in union-required roles.\nDiscipline: Theater & Live Performance (performer guilds).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits performer guilds in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $2B — scenic design and theatrical architecture market · SOM $60M — immersive and site-specific theater design\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$2B — scenic design and theatrical architecture market",
      "som": "$60M — immersive and site-specific theater design"
    },
    {
      "id": "theater-performance-membership-validation-0",
      "theme": "theater",
      "title": "Performance Membership Validation",
      "pitch": "Performance Membership Validation pairs two wallets over DIDComm so theater associations can exchange trusted claims on a private channel.",
      "concept": "Members can invite officials to verify their membership status, ensuring eligibility for benefits and opportunities.",
      "subDiscipline": "theater associations",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits theater associations in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Performance Membership Validation\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nMembers can invite officials to verify their membership status, ensuring eligibility for benefits and opportunities.\nDiscipline: Theater & Live Performance (theater associations).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits theater associations in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $5B — acting coaching and psychological preparation market · SOM $150M — professional film and stage acting studios\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$5B — acting coaching and psychological preparation market",
      "som": "$150M — professional film and stage acting studios"
    },
    {
      "id": "theater-lighting-designer-license-0",
      "theme": "theater",
      "title": "Lighting Designer License",
      "pitch": "Lighting Designer License issues a signed verifiable credential for lighting design that the holder keeps and reuses anywhere.",
      "concept": "Lighting designers can obtain credentials confirming their training and certifications, needed for employment on professional projects.",
      "subDiscipline": "lighting design",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches lighting design in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Lighting Designer License\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLighting designers can obtain credentials confirming their training and certifications, needed for employment on professional projects.\nDiscipline: Theater & Live Performance (lighting design).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches lighting design in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $1B — stage combat and stunt choreography training · SOM $40M — university theater and regional action productions\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$1B — stage combat and stunt choreography training",
      "som": "$40M — university theater and regional action productions"
    },
    {
      "id": "theater-director-s-validation-tool-0",
      "theme": "theater",
      "title": "Director's Validation Tool",
      "pitch": "Director's Validation Tool verifies a credential at the gate for directing — proof without a phone call to the issuer.",
      "concept": "Directors can request producers to verify their past successful productions without revealing full details of their contracts.",
      "subDiscipline": "directing",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for directing in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Director's Validation Tool\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDirectors can request producers to verify their past successful productions without revealing full details of their contracts.\nDiscipline: Theater & Live Performance (directing).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for directing in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $1.5B — theatrical costume and wardrobe management · SOM $50M — large-scale musical theater wardrobe operations\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$1.5B — theatrical costume and wardrobe management",
      "som": "$50M — large-scale musical theater wardrobe operations"
    },
    {
      "id": "theater-authentic-script-repository-0",
      "theme": "theater",
      "title": "Authentic Script Repository",
      "pitch": "Authentic Script Repository mints a did:prism for script authenticity so identity travels with the person, not the platform.",
      "concept": "Playwrights can prove the authenticity of their scripts to theaters, preventing unauthorized adaptations and ensuring original content.",
      "subDiscipline": "script authenticity",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits script authenticity in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Authentic Script Repository\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPlaywrights can prove the authenticity of their scripts to theaters, preventing unauthorized adaptations and ensuring original content.\nDiscipline: Theater & Live Performance (script authenticity).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits script authenticity in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $800M — comedy clubs and improv theater operations · SOM $30M — independent improv troupe training and shows\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$800M — comedy clubs and improv theater operations",
      "som": "$30M — independent improv troupe training and shows"
    },
    {
      "id": "theater-director-collaborations-hub-0",
      "theme": "theater",
      "title": "Director Collaborations Hub",
      "pitch": "Director Collaborations Hub pairs two wallets over DIDComm so creative partnerships can exchange trusted claims on a private channel.",
      "concept": "Directors can prove their collaborative project involvement to production companies using secure connections.",
      "subDiscipline": "creative partnerships",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits creative partnerships in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Director Collaborations Hub\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDirectors can prove their collaborative project involvement to production companies using secure connections.\nDiscipline: Theater & Live Performance (creative partnerships).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits creative partnerships in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $2B — live event accessibility and inclusion services · SOM $100M — Broadway and West End accessibility compliance\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$2B — live event accessibility and inclusion services",
      "som": "$100M — Broadway and West End accessibility compliance"
    },
    {
      "id": "theater-stage-manager-id-0",
      "theme": "theater",
      "title": "Stage Manager ID",
      "pitch": "Stage Manager ID issues a signed verifiable credential for stage management that the holder keeps and reuses anywhere.",
      "concept": "Stage managers can issue credentials to verify their roles and experience, streamlining hiring for productions.",
      "subDiscipline": "stage management",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches stage management in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Stage Manager ID\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nStage managers can issue credentials to verify their roles and experience, streamlining hiring for productions.\nDiscipline: Theater & Live Performance (stage management).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches stage management in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $4B — scriptwriting software and creative writing tools · SOM $120M — professional playwright guilds and fellowships\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$4B — scriptwriting software and creative writing tools",
      "som": "$120M — professional playwright guilds and fellowships"
    },
    {
      "id": "theater-performance-license-verifier-0",
      "theme": "theater",
      "title": "Performance License Verifier",
      "pitch": "Performance License Verifier verifies a credential at the gate for licensing — proof without a phone call to the issuer.",
      "concept": "Theater companies submit proof of performance licenses to venues, ensuring they are authorized to stage a particular play.",
      "subDiscipline": "licensing",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for licensing in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Performance License Verifier\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTheater companies submit proof of performance licenses to venues, ensuring they are authorized to stage a particular play.\nDiscipline: Theater & Live Performance (licensing).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for licensing in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $3B — theatrical directing and cast management software · SOM $90M — professional repertory theater company operations\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$3B — theatrical directing and cast management software",
      "som": "$90M — professional repertory theater company operations"
    },
    {
      "id": "theater-director-s-verified-portfolio-0",
      "theme": "theater",
      "title": "Director's Verified Portfolio",
      "pitch": "Director's Verified Portfolio mints a did:prism for directing so identity travels with the person, not the platform.",
      "concept": "Directors present verified portfolios of their past productions to producers, simplifying the audition process for new projects.",
      "subDiscipline": "directing",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits directing in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Director's Verified Portfolio\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDirectors present verified portfolios of their past productions to producers, simplifying the audition process for new projects.\nDiscipline: Theater & Live Performance (directing).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits directing in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $2B — performer wellness and mental health coaching · SOM $70M — professional touring cast wellness programs\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$2B — performer wellness and mental health coaching",
      "som": "$70M — professional touring cast wellness programs"
    },
    {
      "id": "theater-playwright-rights-management-0",
      "theme": "theater",
      "title": "Playwright Rights Management",
      "pitch": "Playwright Rights Management pairs two wallets over DIDComm so intellectual property can exchange trusted claims on a private channel.",
      "concept": "Playwrights invite producers to verify their rights to scripts, ensuring legal protection and recognition.",
      "subDiscipline": "intellectual property",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits intellectual property in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Playwright Rights Management\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPlaywrights invite producers to verify their rights to scripts, ensuring legal protection and recognition.\nDiscipline: Theater & Live Performance (intellectual property).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits intellectual property in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $1.8B — live sound engineering and design software · SOM $60M — regional theater sound design contracting\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$1.8B — live sound engineering and design software",
      "som": "$60M — regional theater sound design contracting"
    },
    {
      "id": "theater-union-membership-pass-0",
      "theme": "theater",
      "title": "Union Membership Pass",
      "pitch": "Union Membership Pass issues a signed verifiable credential for professional guilds that the holder keeps and reuses anywhere.",
      "concept": "Members receive verifiable credentials validating their union status, required for access to certain gigs and performances.",
      "subDiscipline": "professional guilds",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches professional guilds in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Union Membership Pass\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nMembers receive verifiable credentials validating their union status, required for access to certain gigs and performances.\nDiscipline: Theater & Live Performance (professional guilds).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches professional guilds in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $5B — performing arts education and drama schools · SOM $200M — university theater department curriculum tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$5B — performing arts education and drama schools",
      "som": "$200M — university theater department curriculum tools"
    },
    {
      "id": "theater-choreographer-s-record-keeper-0",
      "theme": "theater",
      "title": "Choreographer's Record Keeper",
      "pitch": "Choreographer's Record Keeper verifies a credential at the gate for choreography — proof without a phone call to the issuer.",
      "concept": "Choreographers present their credentials and previous works to dance companies to validate their expertise.",
      "subDiscipline": "choreography",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for choreography in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Choreographer's Record Keeper\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nChoreographers present their credentials and previous works to dance companies to validate their expertise.\nDiscipline: Theater & Live Performance (choreography).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for choreography in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $1.5B — venue management and backstage operations · SOM $40M — large-scale touring production stage management\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$1.5B — venue management and backstage operations",
      "som": "$40M — large-scale touring production stage management"
    },
    {
      "id": "theater-age-verification-for-performances-0",
      "theme": "theater",
      "title": "Age Verification for Performances",
      "pitch": "Age Verification for Performances mints a did:prism for audience access so identity travels with the person, not the platform.",
      "concept": "The app verifies the age of ticket holders in real-time, ensuring compliance with age-restricted performances.",
      "subDiscipline": "audience access",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits audience access in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Age Verification for Performances\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nThe app verifies the age of ticket holders in real-time, ensuring compliance with age-restricted performances.\nDiscipline: Theater & Live Performance (audience access).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits audience access in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $3B — theatrical adaptation and dramaturgy services · SOM $80M — educational theater classic play adaptations\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$3B — theatrical adaptation and dramaturgy services",
      "som": "$80M — educational theater classic play adaptations"
    },
    {
      "id": "theater-stage-designer-portfolio-proofs-0",
      "theme": "theater",
      "title": "Stage Designer Portfolio Proofs",
      "pitch": "Stage Designer Portfolio Proofs pairs two wallets over DIDComm so design accreditation can exchange trusted claims on a private channel.",
      "concept": "Stage designers prove their past project contributions to potential clients via secure invitations.",
      "subDiscipline": "design accreditation",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits design accreditation in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Stage Designer Portfolio Proofs\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nStage designers prove their past project contributions to potential clients via secure invitations.\nDiscipline: Theater & Live Performance (design accreditation).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits design accreditation in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $2B — dialect coaching and actor training market · SOM $90M — professional film and stage dialect prep\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$2B — dialect coaching and actor training market",
      "som": "$90M — professional film and stage dialect prep"
    },
    {
      "id": "theater-child-performer-consent-0",
      "theme": "theater",
      "title": "Child Performer Consent",
      "pitch": "Child Performer Consent issues a signed verifiable credential for youth theater that the holder keeps and reuses anywhere.",
      "concept": "Parents can issue credentials to grant consent for child performers, ensuring compliance with labor laws.",
      "subDiscipline": "youth theater",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches youth theater in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Child Performer Consent\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nParents can issue credentials to grant consent for child performers, ensuring compliance with labor laws.\nDiscipline: Theater & Live Performance (youth theater).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches youth theater in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $3B — theatrical directing and movement choreography · SOM $75M — experimental and physical theater companies\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$3B — theatrical directing and movement choreography",
      "som": "$75M — experimental and physical theater companies"
    },
    {
      "id": "theater-guild-membership-checker-0",
      "theme": "theater",
      "title": "Guild Membership Checker",
      "pitch": "Guild Membership Checker verifies a credential at the gate for union membership — proof without a phone call to the issuer.",
      "concept": "Performers prove their guild membership status to producers, ensuring compliance with industry standards.",
      "subDiscipline": "union membership",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for union membership in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Guild Membership Checker\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPerformers prove their guild membership status to producers, ensuring compliance with industry standards.\nDiscipline: Theater & Live Performance (union membership).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for union membership in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $1B — creative journaling and artist development tools · SOM $30M — professional acting union wellness resources\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$1B — creative journaling and artist development tools",
      "som": "$30M — professional acting union wellness resources"
    },
    {
      "id": "theater-lighting-designer-credentials-0",
      "theme": "theater",
      "title": "Lighting Designer Credentials",
      "pitch": "Lighting Designer Credentials mints a did:prism for lighting design so identity travels with the person, not the platform.",
      "concept": "Lighting designers publish their qualifications and past works to validate their hiring for performances.",
      "subDiscipline": "lighting design",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits lighting design in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Lighting Designer Credentials\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLighting designers publish their qualifications and past works to validate their hiring for performances.\nDiscipline: Theater & Live Performance (lighting design).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits lighting design in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $2B — stage rigging and automated lighting hardware · SOM $100M — concert touring and arena lighting operations\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$2B — stage rigging and automated lighting hardware",
      "som": "$100M — concert touring and arena lighting operations"
    },
    {
      "id": "theater-casting-call-verification-0",
      "theme": "theater",
      "title": "Casting Call Verification",
      "pitch": "Casting Call Verification pairs two wallets over DIDComm so audition process can exchange trusted claims on a private channel.",
      "concept": "Casting agents invite talents to verify their qualifications and past roles, streamlining the selection process.",
      "subDiscipline": "audition process",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits audition process in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Casting Call Verification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCasting agents invite talents to verify their qualifications and past roles, streamlining the selection process.\nDiscipline: Theater & Live Performance (audition process).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits audition process in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $2B — theatrical publishing and script development · SOM $50M — independent playwright software tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$2B — theatrical publishing and script development",
      "som": "$50M — independent playwright software tools"
    },
    {
      "id": "theater-actor-skill-endorsement-0",
      "theme": "theater",
      "title": "Actor Skill Endorsement",
      "pitch": "Actor Skill Endorsement issues a signed verifiable credential for actor training that the holder keeps and reuses anywhere.",
      "concept": "Actors can receive credentials from trainers verifying specific skills, aiding casting directors in their selection process.",
      "subDiscipline": "actor training",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches actor training in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Actor Skill Endorsement\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nActors can receive credentials from trainers verifying specific skills, aiding casting directors in their selection process.\nDiscipline: Theater & Live Performance (actor training).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches actor training in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $3B — actor training and rehearsal services · SOM $100M — digital memorization aids\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$3B — actor training and rehearsal services",
      "som": "$100M — digital memorization aids"
    },
    {
      "id": "theater-age-verification-stage-0",
      "theme": "theater",
      "title": "Age Verification Stage",
      "pitch": "Age Verification Stage verifies a credential at the gate for youth theater — proof without a phone call to the issuer.",
      "concept": "Young performers present age verification without revealing their exact birth dates to access youth programs.",
      "subDiscipline": "youth theater",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for youth theater in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Age Verification Stage\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nYoung performers present age verification without revealing their exact birth dates to access youth programs.\nDiscipline: Theater & Live Performance (youth theater).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for youth theater in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $4B — theater production and direction tools · SOM $80M — digital staging assistants\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$4B — theater production and direction tools",
      "som": "$80M — digital staging assistants"
    },
    {
      "id": "theater-audition-consent-tracker-0",
      "theme": "theater",
      "title": "Audition Consent Tracker",
      "pitch": "Audition Consent Tracker mints a did:prism for casting so identity travels with the person, not the platform.",
      "concept": "Actors submit their consent for auditions that producers can verify, ensuring transparency and respect for performer rights.",
      "subDiscipline": "casting",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits casting in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Audition Consent Tracker\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nActors submit their consent for auditions that producers can verify, ensuring transparency and respect for performer rights.\nDiscipline: Theater & Live Performance (casting).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits casting in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $1B — stage management and crew operations · SOM $40M — technical rehearsal software\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$1B — stage management and crew operations",
      "som": "$40M — technical rehearsal software"
    },
    {
      "id": "theater-production-crew-identity-cards-0",
      "theme": "theater",
      "title": "Production Crew Identity Cards",
      "pitch": "Production Crew Identity Cards pairs two wallets over DIDComm so crew management can exchange trusted claims on a private channel.",
      "concept": "Production crew members can verify their roles and certifications to the project leads through invitation channels.",
      "subDiscipline": "crew management",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits crew management in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Production Crew Identity Cards\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nProduction crew members can verify their roles and certifications to the project leads through invitation channels.\nDiscipline: Theater & Live Performance (crew management).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits crew management in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $2B — theatrical lighting and equipment · SOM $60M — lighting design ideation software\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$2B — theatrical lighting and equipment",
      "som": "$60M — lighting design ideation software"
    },
    {
      "id": "theater-casting-director-references-0",
      "theme": "theater",
      "title": "Casting Director References",
      "pitch": "Casting Director References issues a signed verifiable credential for casting that the holder keeps and reuses anywhere.",
      "concept": "Casting directors can issue credentials as references for actors, proving their prior work and credibility in the industry.",
      "subDiscipline": "casting",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches casting in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Casting Director References\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCasting directors can issue credentials as references for actors, proving their prior work and credibility in the industry.\nDiscipline: Theater & Live Performance (casting).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches casting in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $3B — scenic construction and design · SOM $75M — set design collaboration tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$3B — scenic construction and design",
      "som": "$75M — set design collaboration tools"
    },
    {
      "id": "theater-playwright-authenticator-0",
      "theme": "theater",
      "title": "Playwright Authenticator",
      "pitch": "Playwright Authenticator verifies a credential at the gate for playwriting — proof without a phone call to the issuer.",
      "concept": "Playwrights validate the provenance of their scripts to theaters, confirming original authorship and rights.",
      "subDiscipline": "playwriting",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for playwriting in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Playwright Authenticator\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPlaywrights validate the provenance of their scripts to theaters, confirming original authorship and rights.\nDiscipline: Theater & Live Performance (playwriting).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for playwriting in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $500M — improv and comedy club operations · SOM $20M — improv training applications\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$500M — improv and comedy club operations",
      "som": "$20M — improv training applications"
    },
    {
      "id": "theater-backstage-pass-verification-0",
      "theme": "theater",
      "title": "Backstage Pass Verification",
      "pitch": "Backstage Pass Verification mints a did:prism for access control so identity travels with the person, not the platform.",
      "concept": "Stage managers verify backstage pass holders' identities to control access and enhance security during live performances.",
      "subDiscipline": "access control",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits access control in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Backstage Pass Verification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nStage managers verify backstage pass holders' identities to control access and enhance security during live performances.\nDiscipline: Theater & Live Performance (access control).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits access control in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $800M — academic and theatrical research · SOM $30M — dramaturgy reference tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$800M — academic and theatrical research",
      "som": "$30M — dramaturgy reference tools"
    },
    {
      "id": "theater-ticket-resale-authentication-0",
      "theme": "theater",
      "title": "Ticket Resale Authentication",
      "pitch": "Ticket Resale Authentication pairs two wallets over DIDComm so event access can exchange trusted claims on a private channel.",
      "concept": "Ticket holders prove authenticity of their tickets to event organizers before accessing performances.",
      "subDiscipline": "event access",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits event access in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Ticket Resale Authentication\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTicket holders prove authenticity of their tickets to event organizers before accessing performances.\nDiscipline: Theater & Live Performance (event access).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits event access in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $2B — performer health and vocal coaching · SOM $90M — vocal wellness apps\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$2B — performer health and vocal coaching",
      "som": "$90M — vocal wellness apps"
    },
    {
      "id": "theater-performance-access-pass-0",
      "theme": "theater",
      "title": "Performance Access Pass",
      "pitch": "Performance Access Pass issues a signed verifiable credential for audience engagement that the holder keeps and reuses anywhere.",
      "concept": "Audience members can obtain credentials granting access to backstage experiences, verified through their purchases.",
      "subDiscipline": "audience engagement",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches audience engagement in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Performance Access Pass\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAudience members can obtain credentials granting access to backstage experiences, verified through their purchases.\nDiscipline: Theater & Live Performance (audience engagement).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches audience engagement in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $400M — stage combat and stunt training · SOM $15M — choreography pacing tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$400M — stage combat and stunt training",
      "som": "$15M — choreography pacing tools"
    },
    {
      "id": "theater-lighting-technician-credentials-0",
      "theme": "theater",
      "title": "Lighting Technician Credentials",
      "pitch": "Lighting Technician Credentials verifies a credential at the gate for technical theater — proof without a phone call to the issuer.",
      "concept": "Lighting designers present verified qualifications and past projects to gain trust from production teams.",
      "subDiscipline": "technical theater",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for technical theater in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Lighting Technician Credentials\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLighting designers present verified qualifications and past projects to gain trust from production teams.\nDiscipline: Theater & Live Performance (technical theater).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for technical theater in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $2B — theatrical costume and wardrobe · SOM $50M — costume design software\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$2B — theatrical costume and wardrobe",
      "som": "$50M — costume design software"
    },
    {
      "id": "theater-provenance-for-costumes-0",
      "theme": "theater",
      "title": "Provenance for Costumes",
      "pitch": "Provenance for Costumes mints a did:prism for costume history so identity travels with the person, not the platform.",
      "concept": "Costume designers prove the historical authenticity of costumes to directors, ensuring fidelity to the production's era.",
      "subDiscipline": "costume history",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits costume history in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Provenance for Costumes\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCostume designers prove the historical authenticity of costumes to directors, ensuring fidelity to the production's era.\nDiscipline: Theater & Live Performance (costume history).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits costume history in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $1B — live event sound engineering · SOM $45M — soundscape ideation tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$1B — live event sound engineering",
      "som": "$45M — soundscape ideation tools"
    },
    {
      "id": "theater-age-verification-for-performances-1",
      "theme": "theater",
      "title": "Age Verification for Performances",
      "pitch": "Age Verification for Performances pairs two wallets over DIDComm so audience access can exchange trusted claims on a private channel.",
      "concept": "Event organizers invite audience members to verify their age, ensuring compliance with age restrictions for certain performances.",
      "subDiscipline": "audience access",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits audience access in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Age Verification for Performances\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nEvent organizers invite audience members to verify their age, ensuring compliance with age restrictions for certain performances.\nDiscipline: Theater & Live Performance (audience access).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits audience access in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $300M — theater safety and compliance · SOM $10M — intimacy coordination resources\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$300M — theater safety and compliance",
      "som": "$10M — intimacy coordination resources"
    },
    {
      "id": "theater-technical-staff-credentials-0",
      "theme": "theater",
      "title": "Technical Staff Credentials",
      "pitch": "Technical Staff Credentials issues a signed verifiable credential for production crew that the holder keeps and reuses anywhere.",
      "concept": "Technical crew members receive credentials to showcase their expertise and experience to potential employers.",
      "subDiscipline": "production crew",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches production crew in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Technical Staff Credentials\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTechnical crew members receive credentials to showcase their expertise and experience to potential employers.\nDiscipline: Theater & Live Performance (production crew).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches production crew in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $5B — musical theater and live music · SOM $120M — lyric writing assistants\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$5B — musical theater and live music",
      "som": "$120M — lyric writing assistants"
    },
    {
      "id": "theater-costume-designer-proof-0",
      "theme": "theater",
      "title": "Costume Designer Proof",
      "pitch": "Costume Designer Proof verifies a credential at the gate for costume design — proof without a phone call to the issuer.",
      "concept": "Costume designers prove their previous work experience and credentials to theaters seeking trusted collaborators.",
      "subDiscipline": "costume design",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for costume design in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Costume Designer Proof\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCostume designers prove their previous work experience and credentials to theaters seeking trusted collaborators.\nDiscipline: Theater & Live Performance (costume design).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for costume design in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $200M — puppetry and physical theater · SOM $8M — puppetry rehearsal aids\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$200M — puppetry and physical theater",
      "som": "$8M — puppetry rehearsal aids"
    },
    {
      "id": "theater-talent-agent-delegation-0",
      "theme": "theater",
      "title": "Talent Agent Delegation",
      "pitch": "Talent Agent Delegation mints a did:prism for agent representation so identity travels with the person, not the platform.",
      "concept": "Actors can prove their agent’s authorization to manage bookings directly to casting directors, avoiding misrepresentation.",
      "subDiscipline": "agent representation",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits agent representation in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Talent Agent Delegation\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nActors can prove their agent’s authorization to manage bookings directly to casting directors, avoiding misrepresentation.\nDiscipline: Theater & Live Performance (agent representation).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits agent representation in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $1B — live event accessibility services · SOM $40M — audio description drafting tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$1B — live event accessibility services",
      "som": "$40M — audio description drafting tools"
    },
    {
      "id": "theater-accredited-actor-networks-0",
      "theme": "theater",
      "title": "Accredited Actor Networks",
      "pitch": "Accredited Actor Networks pairs two wallets over DIDComm so actor training can exchange trusted claims on a private channel.",
      "concept": "Actors prove their training status and memberships to casting directors through secure connections.",
      "subDiscipline": "actor training",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits actor training in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Accredited Actor Networks\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nActors prove their training status and memberships to casting directors through secure connections.\nDiscipline: Theater & Live Performance (actor training).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits actor training in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $800M — arts journalism and media · SOM $25M — review dictation software\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$800M — arts journalism and media",
      "som": "$25M — review dictation software"
    },
    {
      "id": "theater-theater-sponsorship-proof-0",
      "theme": "theater",
      "title": "Theater Sponsorship Proof",
      "pitch": "Theater Sponsorship Proof issues a signed verifiable credential for sponsorship that the holder keeps and reuses anywhere.",
      "concept": "Sponsors can issue credentials verifying their financial contributions to productions, enhancing transparency and trust.",
      "subDiscipline": "sponsorship",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches sponsorship in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Theater Sponsorship Proof\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSponsors can issue credentials verifying their financial contributions to productions, enhancing transparency and trust.\nDiscipline: Theater & Live Performance (sponsorship).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches sponsorship in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $2B — casting and talent agency operations · SOM $70M — audition preparation apps\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$2B — casting and talent agency operations",
      "som": "$70M — audition preparation apps"
    },
    {
      "id": "theater-production-team-validator-0",
      "theme": "theater",
      "title": "Production Team Validator",
      "pitch": "Production Team Validator verifies a credential at the gate for production management — proof without a phone call to the issuer.",
      "concept": "Production managers present verified team members' credentials to stakeholders, ensuring experienced personnel are involved.",
      "subDiscipline": "production management",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for production management in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Production Team Validator\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nProduction managers present verified team members' credentials to stakeholders, ensuring experienced personnel are involved.\nDiscipline: Theater & Live Performance (production management).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for production management in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $4B — performing arts education · SOM $150M — digital acting coaches\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$4B — performing arts education",
      "som": "$150M — digital acting coaches"
    },
    {
      "id": "theater-choreographer-identity-registry-0",
      "theme": "theater",
      "title": "Choreographer Identity Registry",
      "pitch": "Choreographer Identity Registry mints a did:prism for dance so identity travels with the person, not the platform.",
      "concept": "Choreographers publish their identities and previous works to prove credentials to dance companies seeking collaborations.",
      "subDiscipline": "dance",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits dance in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Choreographer Identity Registry\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nChoreographers publish their identities and previous works to prove credentials to dance companies seeking collaborations.\nDiscipline: Theater & Live Performance (dance).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits dance in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $1B — venue operations and customer service · SOM $30M — staff training simulators\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$1B — venue operations and customer service",
      "som": "$30M — staff training simulators"
    },
    {
      "id": "theater-play-authenticity-tracking-0",
      "theme": "theater",
      "title": "Play Authenticity Tracking",
      "pitch": "Play Authenticity Tracking pairs two wallets over DIDComm so script validation can exchange trusted claims on a private channel.",
      "concept": "Theater companies invite critics to verify the authenticity of a play's script before reviews are published.",
      "subDiscipline": "script validation",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits script validation in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Play Authenticity Tracking\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTheater companies invite critics to verify the authenticity of a play's script before reviews are published.\nDiscipline: Theater & Live Performance (script validation).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits script validation in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $600M — theatrical props and set dressing · SOM $20M — prop inventory and sourcing tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$600M — theatrical props and set dressing",
      "som": "$20M — prop inventory and sourcing tools"
    },
    {
      "id": "theater-production-rights-certificate-0",
      "theme": "theater",
      "title": "Production Rights Certificate",
      "pitch": "Production Rights Certificate issues a signed verifiable credential for intellectual property that the holder keeps and reuses anywhere.",
      "concept": "Writers can receive credentials proving their rights to perform adaptations, safeguarding against unauthorized productions.",
      "subDiscipline": "intellectual property",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches intellectual property in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Production Rights Certificate\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nWriters can receive credentials proving their rights to perform adaptations, safeguarding against unauthorized productions.\nDiscipline: Theater & Live Performance (intellectual property).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches intellectual property in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $2B — entertainment PR and marketing · SOM $60M — media training applications\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$2B — entertainment PR and marketing",
      "som": "$60M — media training applications"
    },
    {
      "id": "theater-audience-consent-tracker-0",
      "theme": "theater",
      "title": "Audience Consent Tracker",
      "pitch": "Audience Consent Tracker verifies a credential at the gate for audience engagement — proof without a phone call to the issuer.",
      "concept": "Audiences provide verified consent to theaters for data usage while maintaining personal privacy.",
      "subDiscipline": "audience engagement",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for audience engagement in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Audience Consent Tracker\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAudiences provide verified consent to theaters for data usage while maintaining personal privacy.\nDiscipline: Theater & Live Performance (audience engagement).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for audience engagement in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $500M — backstage wardrobe and crew ops · SOM $15M — quick change optimization tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$500M — backstage wardrobe and crew ops",
      "som": "$15M — quick change optimization tools"
    },
    {
      "id": "theater-proven-rights-for-music-0",
      "theme": "theater",
      "title": "Proven Rights for Music",
      "pitch": "Proven Rights for Music mints a did:prism for musical theater so identity travels with the person, not the platform.",
      "concept": "Musicians verify their rights to score compositions used in theater productions, ensuring proper credit and compensation.",
      "subDiscipline": "musical theater",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits musical theater in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Proven Rights for Music\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nMusicians verify their rights to score compositions used in theater productions, ensuring proper credit and compensation.\nDiscipline: Theater & Live Performance (musical theater).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits musical theater in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $900M — live interpretation and translation · SOM $35M — ASL theatrical pacing aids\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$900M — live interpretation and translation",
      "som": "$35M — ASL theatrical pacing aids"
    },
    {
      "id": "theater-union-membership-proof-0",
      "theme": "theater",
      "title": "Union Membership Proof",
      "pitch": "Union Membership Proof pairs two wallets over DIDComm so guild compliance can exchange trusted claims on a private channel.",
      "concept": "Actors can invite producers to verify their union membership status during contract negotiations.",
      "subDiscipline": "guild compliance",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits guild compliance in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Union Membership Proof\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nActors can invite producers to verify their union membership status during contract negotiations.\nDiscipline: Theater & Live Performance (guild compliance).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits guild compliance in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $1B — solo shows and fringe theater · SOM $40M — character analysis software\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$1B — solo shows and fringe theater",
      "som": "$40M — character analysis software"
    },
    {
      "id": "theater-set-designer-portfolio-0",
      "theme": "theater",
      "title": "Set Designer Portfolio",
      "pitch": "Set Designer Portfolio issues a signed verifiable credential for set design that the holder keeps and reuses anywhere.",
      "concept": "Set designers can issue credentials showcasing their previous projects, helping them secure future contracts.",
      "subDiscipline": "set design",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches set design in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Set Designer Portfolio\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSet designers can issue credentials showcasing their previous projects, helping them secure future contracts.\nDiscipline: Theater & Live Performance (set design).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches set design in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance and theater market · SAM $2B — global playwriting and script software · SOM $100M — AI script dictation tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance and theater market",
      "sam": "$2B — global playwriting and script software",
      "som": "$100M — AI script dictation tools"
    },
    {
      "id": "theater-backstage-safety-certifier-0",
      "theme": "theater",
      "title": "Backstage Safety Certifier",
      "pitch": "Backstage Safety Certifier verifies a credential at the gate for safety training — proof without a phone call to the issuer.",
      "concept": "Technicians verify their safety training certificates before being allowed into hazardous production areas.",
      "subDiscipline": "safety training",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for safety training in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Backstage Safety Certifier\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTechnicians verify their safety training certificates before being allowed into hazardous production areas.\nDiscipline: Theater & Live Performance (safety training).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for safety training in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance and theater market · SAM $3B — stage management and prompt-book software · SOM $150M — AI rehearsal logging tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance and theater market",
      "sam": "$3B — stage management and prompt-book software",
      "som": "$150M — AI rehearsal logging tools"
    },
    {
      "id": "theater-production-design-validation-0",
      "theme": "theater",
      "title": "Production Design Validation",
      "pitch": "Production Design Validation mints a did:prism for production design so identity travels with the person, not the platform.",
      "concept": "Production designers validate their past work to producers seeking to hire for upcoming theatre productions.",
      "subDiscipline": "production design",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits production design in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Production Design Validation\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nProduction designers validate their past work to producers seeking to hire for upcoming theatre productions.\nDiscipline: Theater & Live Performance (production design).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits production design in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance and theater market · SAM $1B — improv and comedy club software · SOM $50M — AI improv transcription tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance and theater market",
      "sam": "$1B — improv and comedy club software",
      "som": "$50M — AI improv transcription tools"
    },
    {
      "id": "theater-collaborative-rehearsal-spaces-0",
      "theme": "theater",
      "title": "Collaborative Rehearsal Spaces",
      "pitch": "Collaborative Rehearsal Spaces pairs two wallets over DIDComm so rehearsal logistics can exchange trusted claims on a private channel.",
      "concept": "Collaborators can validate their project involvement when booking rehearsal spaces through secure invites.",
      "subDiscipline": "rehearsal logistics",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits rehearsal logistics in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Collaborative Rehearsal Spaces\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCollaborators can validate their project involvement when booking rehearsal spaces through secure invites.\nDiscipline: Theater & Live Performance (rehearsal logistics).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits rehearsal logistics in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance and theater market · SAM $2.5B — directing and production management tools · SOM $120M — AI director note-taking apps\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance and theater market",
      "sam": "$2.5B — directing and production management tools",
      "som": "$120M — AI director note-taking apps"
    },
    {
      "id": "theater-costume-authenticity-0",
      "theme": "theater",
      "title": "Costume Authenticity",
      "pitch": "Costume Authenticity issues a signed verifiable credential for costume design that the holder keeps and reuses anywhere.",
      "concept": "Costume designers can issue credentials proving the authenticity and originality of their designs for productions.",
      "subDiscipline": "costume design",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches costume design in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Costume Authenticity\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCostume designers can issue credentials proving the authenticity and originality of their designs for productions.\nDiscipline: Theater & Live Performance (costume design).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches costume design in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance and theater market · SAM $1.5B — actor training and coaching software · SOM $80M — AI dialect coaching tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance and theater market",
      "sam": "$1.5B — actor training and coaching software",
      "som": "$80M — AI dialect coaching tools"
    },
    {
      "id": "theater-provenance-stage-app-0",
      "theme": "theater",
      "title": "Provenance Stage App",
      "pitch": "Provenance Stage App verifies a credential at the gate for artistic integrity — proof without a phone call to the issuer.",
      "concept": "Artists present evidence of their past work to validate claims of experience and authenticity within theater.",
      "subDiscipline": "artistic integrity",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for artistic integrity in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Provenance Stage App\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nArtists present evidence of their past work to validate claims of experience and authenticity within theater.\nDiscipline: Theater & Live Performance (artistic integrity).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for artistic integrity in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance and theater market · SAM $2B — rehearsal and casting software · SOM $90M — AI script analysis tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance and theater market",
      "sam": "$2B — rehearsal and casting software",
      "som": "$90M — AI script analysis tools"
    },
    {
      "id": "theater-event-accessibility-certification-0",
      "theme": "theater",
      "title": "Event Accessibility Certification",
      "pitch": "Event Accessibility Certification mints a did:prism for accessibility so identity travels with the person, not the platform.",
      "concept": "The app verifies the accessibility certifications of venues to ensure compliance with regulations for live performances.",
      "subDiscipline": "accessibility",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits accessibility in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Event Accessibility Certification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nThe app verifies the accessibility certifications of venues to ensure compliance with regulations for live performances.\nDiscipline: Theater & Live Performance (accessibility).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits accessibility in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance and theater market · SAM $1.2B — choreography and blocking software · SOM $60M — AI stage mapping tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance and theater market",
      "sam": "$1.2B — choreography and blocking software",
      "som": "$60M — AI stage mapping tools"
    },
    {
      "id": "theater-costume-archive-verification-0",
      "theme": "theater",
      "title": "Costume Archive Verification",
      "pitch": "Costume Archive Verification pairs two wallets over DIDComm so wardrobe integrity can exchange trusted claims on a private channel.",
      "concept": "Costume designers invite production teams to verify the provenance of costumes for authenticity.",
      "subDiscipline": "wardrobe integrity",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits wardrobe integrity in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Costume Archive Verification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCostume designers invite production teams to verify the provenance of costumes for authenticity.\nDiscipline: Theater & Live Performance (wardrobe integrity).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits wardrobe integrity in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance and theater market · SAM $800M — performer health and wellness apps · SOM $40M — AI vocal tracking tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance and theater market",
      "sam": "$800M — performer health and wellness apps",
      "som": "$40M — AI vocal tracking tools"
    },
    {
      "id": "theater-performance-eligibility-badge-0",
      "theme": "theater",
      "title": "Performance Eligibility Badge",
      "pitch": "Performance Eligibility Badge issues a signed verifiable credential for community theater that the holder keeps and reuses anywhere.",
      "concept": "Performers can validate their eligibility status for auditions through verifiable credentials, easing the selection process.",
      "subDiscipline": "community theater",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches community theater in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Performance Eligibility Badge\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPerformers can validate their eligibility status for auditions through verifiable credentials, easing the selection process.\nDiscipline: Theater & Live Performance (community theater).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches community theater in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance and theater market · SAM $500M — accessibility and inclusion software · SOM $25M — AI live captioning tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance and theater market",
      "sam": "$500M — accessibility and inclusion software",
      "som": "$25M — AI live captioning tools"
    },
    {
      "id": "theater-sponsorship-proof-hub-0",
      "theme": "theater",
      "title": "Sponsorship Proof Hub",
      "pitch": "Sponsorship Proof Hub verifies a credential at the gate for fundraising — proof without a phone call to the issuer.",
      "concept": "Performers verify their sponsorship agreements to potential investors, ensuring transparency in funding sources.",
      "subDiscipline": "fundraising",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for fundraising in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Sponsorship Proof Hub\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPerformers verify their sponsorship agreements to potential investors, ensuring transparency in funding sources.\nDiscipline: Theater & Live Performance (fundraising).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for fundraising in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance and theater market · SAM $1.8B — audience engagement and ticketing software · SOM $75M — AI feedback analysis tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance and theater market",
      "sam": "$1.8B — audience engagement and ticketing software",
      "som": "$75M — AI feedback analysis tools"
    },
    {
      "id": "theater-festival-performer-verification-0",
      "theme": "theater",
      "title": "Festival Performer Verification",
      "pitch": "Festival Performer Verification mints a did:prism for theater festivals so identity travels with the person, not the platform.",
      "concept": "Actors submit verified identities to festival organizers, ensuring proper representation and eligibility for participation.",
      "subDiscipline": "theater festivals",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits theater festivals in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Festival Performer Verification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nActors submit verified identities to festival organizers, ensuring proper representation and eligibility for participation.\nDiscipline: Theater & Live Performance (theater festivals).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits theater festivals in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance and theater market · SAM $1B — actor training and rehearsal apps · SOM $55M — AI memorization aids\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance and theater market",
      "sam": "$1B — actor training and rehearsal apps",
      "som": "$55M — AI memorization aids"
    },
    {
      "id": "theater-performer-s-rights-assertion-0",
      "theme": "theater",
      "title": "Performer's Rights Assertion",
      "pitch": "Performer's Rights Assertion pairs two wallets over DIDComm so performance rights can exchange trusted claims on a private channel.",
      "concept": "Performers can prove their rights to royalty payouts to producers through secure invitation connections.",
      "subDiscipline": "performance rights",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits performance rights in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Performer's Rights Assertion\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPerformers can prove their rights to royalty payouts to producers through secure invitation connections.\nDiscipline: Theater & Live Performance (performance rights).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits performance rights in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance and theater market · SAM $2.2B — stage lighting and design software · SOM $110M — AI lighting programming tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance and theater market",
      "sam": "$2.2B — stage lighting and design software",
      "som": "$110M — AI lighting programming tools"
    },
    {
      "id": "theater-stage-combat-certification-0",
      "theme": "theater",
      "title": "Stage Combat Certification",
      "pitch": "Stage Combat Certification issues a signed verifiable credential for stage combat that the holder keeps and reuses anywhere.",
      "concept": "Actors can obtain credentials certifying their stage combat training, required for specific roles in productions.",
      "subDiscipline": "stage combat",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches stage combat in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Stage Combat Certification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nActors can obtain credentials certifying their stage combat training, required for specific roles in productions.\nDiscipline: Theater & Live Performance (stage combat).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches stage combat in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance and theater market · SAM $1.5B — theatrical sound design software · SOM $70M — AI foley brainstorming tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance and theater market",
      "sam": "$1.5B — theatrical sound design software",
      "som": "$70M — AI foley brainstorming tools"
    },
    {
      "id": "theater-collaborative-project-validator-0",
      "theme": "theater",
      "title": "Collaborative Project Validator",
      "pitch": "Collaborative Project Validator verifies a credential at the gate for co-productions — proof without a phone call to the issuer.",
      "concept": "Theaters verify co-production agreements with each other, ensuring all parties have legitimated interest in the project.",
      "subDiscipline": "co-productions",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for co-productions in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Collaborative Project Validator\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTheaters verify co-production agreements with each other, ensuring all parties have legitimated interest in the project.\nDiscipline: Theater & Live Performance (co-productions).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for co-productions in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance and theater market · SAM $400M — stage combat and safety software · SOM $20M — AI safety logging tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance and theater market",
      "sam": "$400M — stage combat and safety software",
      "som": "$20M — AI safety logging tools"
    },
    {
      "id": "theater-union-membership-proof-1",
      "theme": "theater",
      "title": "Union Membership Proof",
      "pitch": "Union Membership Proof mints a did:prism for labor relations so identity travels with the person, not the platform.",
      "concept": "Performers prove their union memberships to producers for participation in union-specific projects, ensuring compliance.",
      "subDiscipline": "labor relations",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits labor relations in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Union Membership Proof\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPerformers prove their union memberships to producers for participation in union-specific projects, ensuring compliance.\nDiscipline: Theater & Live Performance (labor relations).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits labor relations in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance and theater market · SAM $300M — puppetry and physical theater tools · SOM $15M — AI puppetry timing aids\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance and theater market",
      "sam": "$300M — puppetry and physical theater tools",
      "som": "$15M — AI puppetry timing aids"
    },
    {
      "id": "theater-live-streaming-access-control-0",
      "theme": "theater",
      "title": "Live Streaming Access Control",
      "pitch": "Live Streaming Access Control pairs two wallets over DIDComm so digital events can exchange trusted claims on a private channel.",
      "concept": "Live performance producers invite attendees to validate their access to digital streaming events.",
      "subDiscipline": "digital events",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits digital events in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Live Streaming Access Control\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLive performance producers invite attendees to validate their access to digital streaming events.\nDiscipline: Theater & Live Performance (digital events).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits digital events in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance and theater market · SAM $1.1B — set design and props software · SOM $50M — AI prop sourcing tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance and theater market",
      "sam": "$1.1B — set design and props software",
      "som": "$50M — AI prop sourcing tools"
    },
    {
      "id": "theater-community-engagement-award-0",
      "theme": "theater",
      "title": "Community Engagement Award",
      "pitch": "Community Engagement Award issues a signed verifiable credential for audience outreach that the holder keeps and reuses anywhere.",
      "concept": "Theater companies can issue credentials to recognize audience members' participation in community engagement initiatives.",
      "subDiscipline": "audience outreach",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches audience outreach in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Community Engagement Award\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTheater companies can issue credentials to recognize audience members' participation in community engagement initiatives.\nDiscipline: Theater & Live Performance (audience outreach).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches audience outreach in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance and theater market · SAM $900M — costume and wardrobe management software · SOM $45M — AI alteration logging tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance and theater market",
      "sam": "$900M — costume and wardrobe management software",
      "som": "$45M — AI alteration logging tools"
    },
    {
      "id": "theater-application-talent-showcase-0",
      "theme": "theater",
      "title": "Application Talent Showcase",
      "pitch": "Application Talent Showcase verifies a credential at the gate for auditions — proof without a phone call to the issuer.",
      "concept": "Auditionees prove their training and experience to casting directors without sharing sensitive personal information.",
      "subDiscipline": "auditions",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for auditions in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Application Talent Showcase\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAuditionees prove their training and experience to casting directors without sharing sensitive personal information.\nDiscipline: Theater & Live Performance (auditions).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for auditions in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance and theater market · SAM $600M — dramaturgy and research databases · SOM $30M — AI historical context tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance and theater market",
      "sam": "$600M — dramaturgy and research databases",
      "som": "$30M — AI historical context tools"
    },
    {
      "id": "theater-audience-feedback-authenticity-0",
      "theme": "theater",
      "title": "Audience Feedback Authenticity",
      "pitch": "Audience Feedback Authenticity mints a did:prism for community engagement so identity travels with the person, not the platform.",
      "concept": "Audience members can verify their attendance at performances to provide authentic feedback for producers and critics.",
      "subDiscipline": "community engagement",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits community engagement in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Audience Feedback Authenticity\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAudience members can verify their attendance at performances to provide authentic feedback for producers and critics.\nDiscipline: Theater & Live Performance (community engagement).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits community engagement in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance and theater market · SAM $200M — intimacy and HR compliance software · SOM $10M — AI consent documentation tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance and theater market",
      "sam": "$200M — intimacy and HR compliance software",
      "som": "$10M — AI consent documentation tools"
    },
    {
      "id": "theater-mentorship-program-validation-0",
      "theme": "theater",
      "title": "Mentorship Program Validation",
      "pitch": "Mentorship Program Validation pairs two wallets over DIDComm so actor mentorship can exchange trusted claims on a private channel.",
      "concept": "Mentors and mentees can verify participation in mentorship programs through secure invitations.",
      "subDiscipline": "actor mentorship",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits actor mentorship in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Mentorship Program Validation\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nMentors and mentees can verify participation in mentorship programs through secure invitations.\nDiscipline: Theater & Live Performance (actor mentorship).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits actor mentorship in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance and theater market · SAM $1.4B — musical theater and scoring software · SOM $65M — AI vocal arrangement tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance and theater market",
      "sam": "$1.4B — musical theater and scoring software",
      "som": "$65M — AI vocal arrangement tools"
    },
    {
      "id": "theater-audience-age-verification-0",
      "theme": "theater",
      "title": "Audience Age Verification",
      "pitch": "Audience Age Verification issues a signed verifiable credential for age restrictions that the holder keeps and reuses anywhere.",
      "concept": "Venues can issue credentials to verify audience members' ages for access to age-restricted performances.",
      "subDiscipline": "age restrictions",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches age restrictions in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Audience Age Verification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nVenues can issue credentials to verify audience members' ages for access to age-restricted performances.\nDiscipline: Theater & Live Performance (age restrictions).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches age restrictions in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance and theater market · SAM $500M — theatrical makeup and effects software · SOM $25M — AI continuity logging tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance and theater market",
      "sam": "$500M — theatrical makeup and effects software",
      "som": "$25M — AI continuity logging tools"
    },
    {
      "id": "theater-incubator-program-validator-0",
      "theme": "theater",
      "title": "Incubator Program Validator",
      "pitch": "Incubator Program Validator verifies a credential at the gate for theater education — proof without a phone call to the issuer.",
      "concept": "Emerging artists present verified acceptance into incubator programs to industry professionals for mentorship opportunities.",
      "subDiscipline": "theater education",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for theater education in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Incubator Program Validator\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nEmerging artists present verified acceptance into incubator programs to industry professionals for mentorship opportunities.\nDiscipline: Theater & Live Performance (theater education).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for theater education in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance and theater market · SAM $2.5B — arts marketing and ticketing software · SOM $125M — AI copywriting tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance and theater market",
      "sam": "$2.5B — arts marketing and ticketing software",
      "som": "$125M — AI copywriting tools"
    },
    {
      "id": "theater-collaborator-identity-network-0",
      "theme": "theater",
      "title": "Collaborator Identity Network",
      "pitch": "Collaborator Identity Network mints a did:prism for creative teams so identity travels with the person, not the platform.",
      "concept": "Creative teams publish their identities and roles to verify collaborators for new theater projects, enhancing project integrity.",
      "subDiscipline": "creative teams",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits creative teams in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Collaborator Identity Network\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCreative teams publish their identities and roles to verify collaborators for new theater projects, enhancing project integrity.\nDiscipline: Theater & Live Performance (creative teams).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits creative teams in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance and theater market · SAM $700M — mental health and wellness apps · SOM $35M — AI performance psychology tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance and theater market",
      "sam": "$700M — mental health and wellness apps",
      "som": "$35M — AI performance psychology tools"
    },
    {
      "id": "theater-script-submission-proofs-0",
      "theme": "theater",
      "title": "Script Submission Proofs",
      "pitch": "Script Submission Proofs pairs two wallets over DIDComm so play submissions can exchange trusted claims on a private channel.",
      "concept": "Playwrights can invite theaters to verify the submission timestamp of their scripts.",
      "subDiscipline": "play submissions",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits play submissions in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Script Submission Proofs\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPlaywrights can invite theaters to verify the submission timestamp of their scripts.\nDiscipline: Theater & Live Performance (play submissions).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits play submissions in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance and theater market · SAM $1.8B — production crew and comms software · SOM $85M — AI backstage logging tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance and theater market",
      "sam": "$1.8B — production crew and comms software",
      "som": "$85M — AI backstage logging tools"
    },
    {
      "id": "theater-collaborative-project-proof-0",
      "theme": "theater",
      "title": "Collaborative Project Proof",
      "pitch": "Collaborative Project Proof issues a signed verifiable credential for joint productions that the holder keeps and reuses anywhere.",
      "concept": "Artists involved in collaborative projects can receive credentials documenting their contributions and roles within joint productions.",
      "subDiscipline": "joint productions",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches joint productions in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Collaborative Project Proof\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nArtists involved in collaborative projects can receive credentials documenting their contributions and roles within joint productions.\nDiscipline: Theater & Live Performance (joint productions).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches joint productions in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance and theater market · SAM $1B — event logistics and strike software · SOM $50M — AI load-out management tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance and theater market",
      "sam": "$1B — event logistics and strike software",
      "som": "$50M — AI load-out management tools"
    },
    {
      "id": "theater-theater-review-verifier-0",
      "theme": "theater",
      "title": "Theater Review Verifier",
      "pitch": "Theater Review Verifier verifies a credential at the gate for critique — proof without a phone call to the issuer.",
      "concept": "Critics validate their credentials and affiliations to theaters to build trust around their feedback.",
      "subDiscipline": "critique",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for critique in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Theater Review Verifier\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCritics validate their credentials and affiliations to theaters to build trust around their feedback.\nDiscipline: Theater & Live Performance (critique).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for critique in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $5B — live performance production and design software · SOM $500M — indie and regional theater sound design budgets\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$5B — live performance production and design software",
      "som": "$500M — indie and regional theater sound design budgets"
    },
    {
      "id": "theater-backstage-safety-verification-0",
      "theme": "theater",
      "title": "Backstage Safety Verification",
      "pitch": "Backstage Safety Verification mints a did:prism for safety protocols so identity travels with the person, not the platform.",
      "concept": "Crew members verify their safety training certifications to production managers, ensuring a safe environment during live events.",
      "subDiscipline": "safety protocols",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits safety protocols in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Backstage Safety Verification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCrew members verify their safety training certifications to production managers, ensuring a safe environment during live events.\nDiscipline: Theater & Live Performance (safety protocols).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits safety protocols in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $5B — live performance production and design software · SOM $500M — indie and regional theater sound design budgets\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$5B — live performance production and design software",
      "som": "$500M — indie and regional theater sound design budgets"
    },
    {
      "id": "theater-talent-agency-accreditation-0",
      "theme": "theater",
      "title": "Talent Agency Accreditation",
      "pitch": "Talent Agency Accreditation pairs two wallets over DIDComm so agent proof can exchange trusted claims on a private channel.",
      "concept": "Talent agencies prove their accreditation status to clients through secure connections.",
      "subDiscipline": "agent proof",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits agent proof in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Talent Agency Accreditation\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTalent agencies prove their accreditation status to clients through secure connections.\nDiscipline: Theater & Live Performance (agent proof).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits agent proof in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $5B — live performance production and design software · SOM $500M — indie and regional theater sound design budgets\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$5B — live performance production and design software",
      "som": "$500M — indie and regional theater sound design budgets"
    },
    {
      "id": "theater-performance-credit-recognition-0",
      "theme": "theater",
      "title": "Performance Credit Recognition",
      "pitch": "Performance Credit Recognition issues a signed verifiable credential for credits that the holder keeps and reuses anywhere.",
      "concept": "Actors can receive credentials that validate their roles in productions, enhancing their professional reputations.",
      "subDiscipline": "credits",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches credits in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Performance Credit Recognition\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nActors can receive credentials that validate their roles in productions, enhancing their professional reputations.\nDiscipline: Theater & Live Performance (credits).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches credits in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $5B — live performance production and design software · SOM $500M — indie and regional theater sound design budgets\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$5B — live performance production and design software",
      "som": "$500M — indie and regional theater sound design budgets"
    },
    {
      "id": "theater-script-submission-tracker-0",
      "theme": "theater",
      "title": "Script Submission Tracker",
      "pitch": "Script Submission Tracker verifies a credential at the gate for play submission — proof without a phone call to the issuer.",
      "concept": "Playwrights submit scripts with proof of prior submissions to manage their portfolios securely.",
      "subDiscipline": "play submission",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for play submission in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Script Submission Tracker\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPlaywrights submit scripts with proof of prior submissions to manage their portfolios securely.\nDiscipline: Theater & Live Performance (play submission).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for play submission in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $5B — live performance production and design software · SOM $500M — indie and regional theater sound design budgets\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$5B — live performance production and design software",
      "som": "$500M — indie and regional theater sound design budgets"
    },
    {
      "id": "theater-playwright-rights-tracker-0",
      "theme": "theater",
      "title": "Playwright Rights Tracker",
      "pitch": "Playwright Rights Tracker mints a did:prism for rights management so identity travels with the person, not the platform.",
      "concept": "Playwrights verify their ownership rights when licensing plays to ensure proper usage and protection against infringement.",
      "subDiscipline": "rights management",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits rights management in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Playwright Rights Tracker\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPlaywrights verify their ownership rights when licensing plays to ensure proper usage and protection against infringement.\nDiscipline: Theater & Live Performance (rights management).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits rights management in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $5B — live performance production and design software · SOM $500M — indie and regional theater sound design budgets\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$5B — live performance production and design software",
      "som": "$500M — indie and regional theater sound design budgets"
    },
    {
      "id": "theater-production-budget-transparency-0",
      "theme": "theater",
      "title": "Production Budget Transparency",
      "pitch": "Production Budget Transparency pairs two wallets over DIDComm so financial accountability can exchange trusted claims on a private channel.",
      "concept": "Producers can invite stakeholders to verify budget expenditures and allocations in real-time.",
      "subDiscipline": "financial accountability",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits financial accountability in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Production Budget Transparency\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nProducers can invite stakeholders to verify budget expenditures and allocations in real-time.\nDiscipline: Theater & Live Performance (financial accountability).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits financial accountability in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $5B — live performance production and design software · SOM $500M — indie and regional theater sound design budgets\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$5B — live performance production and design software",
      "som": "$500M — indie and regional theater sound design budgets"
    },
    {
      "id": "theater-workshop-participation-badge-0",
      "theme": "theater",
      "title": "Workshop Participation Badge",
      "pitch": "Workshop Participation Badge issues a signed verifiable credential for training workshops that the holder keeps and reuses anywhere.",
      "concept": "Participants in theater workshops can earn credentials confirming their attendance and skills acquired during the sessions.",
      "subDiscipline": "training workshops",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches training workshops in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Workshop Participation Badge\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nParticipants in theater workshops can earn credentials confirming their attendance and skills acquired during the sessions.\nDiscipline: Theater & Live Performance (training workshops).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches training workshops in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $5B — live performance production and design software · SOM $500M — indie and regional theater sound design budgets\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$5B — live performance production and design software",
      "som": "$500M — indie and regional theater sound design budgets"
    },
    {
      "id": "theater-rehearsal-attendance-checker-0",
      "theme": "theater",
      "title": "Rehearsal Attendance Checker",
      "pitch": "Rehearsal Attendance Checker verifies a credential at the gate for rehearsal — proof without a phone call to the issuer.",
      "concept": "Actors verify their rehearsal attendance to directors without sharing full attendance records.",
      "subDiscipline": "rehearsal",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for rehearsal in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Rehearsal Attendance Checker\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nActors verify their rehearsal attendance to directors without sharing full attendance records.\nDiscipline: Theater & Live Performance (rehearsal).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for rehearsal in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $5B — live performance production and design software · SOM $500M — indie and regional theater sound design budgets\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$5B — live performance production and design software",
      "som": "$500M — indie and regional theater sound design budgets"
    },
    {
      "id": "theater-audition-record-validation-0",
      "theme": "theater",
      "title": "Audition Record Validation",
      "pitch": "Audition Record Validation mints a did:prism for casting calls so identity travels with the person, not the platform.",
      "concept": "Actors verify their audition records to casting directors, simplifying the hiring process for new roles.",
      "subDiscipline": "casting calls",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits casting calls in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Audition Record Validation\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nActors verify their audition records to casting directors, simplifying the hiring process for new roles.\nDiscipline: Theater & Live Performance (casting calls).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits casting calls in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $5B — live performance production and design software · SOM $500M — indie and regional theater sound design budgets\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$5B — live performance production and design software",
      "som": "$500M — indie and regional theater sound design budgets"
    },
    {
      "id": "theater-performance-review-authentication-0",
      "theme": "theater",
      "title": "Performance Review Authentication",
      "pitch": "Performance Review Authentication pairs two wallets over DIDComm so critic engagements can exchange trusted claims on a private channel.",
      "concept": "Critics can prove their review credentials to theater companies through invitation-based verification.",
      "subDiscipline": "critic engagements",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits critic engagements in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Performance Review Authentication\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCritics can prove their review credentials to theater companies through invitation-based verification.\nDiscipline: Theater & Live Performance (critic engagements).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits critic engagements in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $5B — live performance production and design software · SOM $500M — indie and regional theater sound design budgets\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$5B — live performance production and design software",
      "som": "$500M — indie and regional theater sound design budgets"
    },
    {
      "id": "theater-backstage-access-credential-0",
      "theme": "theater",
      "title": "Backstage Access Credential",
      "pitch": "Backstage Access Credential issues a signed verifiable credential for event security that the holder keeps and reuses anywhere.",
      "concept": "Crew members can issue credentials to audience members granting them backstage access during special performances.",
      "subDiscipline": "event security",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches event security in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Backstage Access Credential\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCrew members can issue credentials to audience members granting them backstage access during special performances.\nDiscipline: Theater & Live Performance (event security).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches event security in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $5B — live performance production and design software · SOM $500M — indie and regional theater sound design budgets\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$5B — live performance production and design software",
      "som": "$500M — indie and regional theater sound design budgets"
    },
    {
      "id": "theater-festival-participation-proof-0",
      "theme": "theater",
      "title": "Festival Participation Proof",
      "pitch": "Festival Participation Proof verifies a credential at the gate for festivals — proof without a phone call to the issuer.",
      "concept": "Theaters prove their participation in festivals with verified credentials, enhancing their reputation and opportunities.",
      "subDiscipline": "festivals",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for festivals in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Festival Participation Proof\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTheaters prove their participation in festivals with verified credentials, enhancing their reputation and opportunities.\nDiscipline: Theater & Live Performance (festivals).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for festivals in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $5B — live performance production and design software · SOM $500M — indie and regional theater sound design budgets\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$5B — live performance production and design software",
      "som": "$500M — indie and regional theater sound design budgets"
    },
    {
      "id": "theater-performance-attendance-confirmation-0",
      "theme": "theater",
      "title": "Performance Attendance Confirmation",
      "pitch": "Performance Attendance Confirmation mints a did:prism for audience management so identity travels with the person, not the platform.",
      "concept": "Ticket holders prove their identities and attendance to receive exclusive offers after completing performances.",
      "subDiscipline": "audience management",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits audience management in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Performance Attendance Confirmation\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTicket holders prove their identities and attendance to receive exclusive offers after completing performances.\nDiscipline: Theater & Live Performance (audience management).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits audience management in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $5B — live performance production and design software · SOM $500M — indie and regional theater sound design budgets\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$5B — live performance production and design software",
      "som": "$500M — indie and regional theater sound design budgets"
    },
    {
      "id": "theater-stage-license-verification-0",
      "theme": "theater",
      "title": "Stage License Verification",
      "pitch": "Stage License Verification pairs two wallets over DIDComm so performance rights can exchange trusted claims on a private channel.",
      "concept": "Theater productions invite officials to verify stage licenses before performances commence.",
      "subDiscipline": "performance rights",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits performance rights in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Stage License Verification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTheater productions invite officials to verify stage licenses before performances commence.\nDiscipline: Theater & Live Performance (performance rights).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits performance rights in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $5B — live performance production and design software · SOM $500M — indie and regional theater sound design budgets\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$5B — live performance production and design software",
      "som": "$500M — indie and regional theater sound design budgets"
    },
    {
      "id": "theater-artistic-collaboration-verification-0",
      "theme": "theater",
      "title": "Artistic Collaboration Verification",
      "pitch": "Artistic Collaboration Verification issues a signed verifiable credential for collaboration that the holder keeps and reuses anywhere.",
      "concept": "Artists collaborating on projects can issue credentials to confirm their involvement, securing recognition for joint efforts.",
      "subDiscipline": "collaboration",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches collaboration in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Artistic Collaboration Verification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nArtists collaborating on projects can issue credentials to confirm their involvement, securing recognition for joint efforts.\nDiscipline: Theater & Live Performance (collaboration).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches collaboration in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $5B — live performance production and design software · SOM $500M — indie and regional theater sound design budgets\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$5B — live performance production and design software",
      "som": "$500M — indie and regional theater sound design budgets"
    },
    {
      "id": "theater-educational-credential-presenter-0",
      "theme": "theater",
      "title": "Educational Credential Presenter",
      "pitch": "Educational Credential Presenter verifies a credential at the gate for theater education — proof without a phone call to the issuer.",
      "concept": "Educators present verified teaching credentials to institutions for job applications without detailed personal data.",
      "subDiscipline": "theater education",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for theater education in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Educational Credential Presenter\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nEducators present verified teaching credentials to institutions for job applications without detailed personal data.\nDiscipline: Theater & Live Performance (theater education).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for theater education in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $5B — live performance production and design software · SOM $500M — indie and regional theater sound design budgets\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$5B — live performance production and design software",
      "som": "$500M — indie and regional theater sound design budgets"
    },
    {
      "id": "theater-rehearsal-access-pass-0",
      "theme": "theater",
      "title": "Rehearsal Access Pass",
      "pitch": "Rehearsal Access Pass mints a did:prism for rehearsal management so identity travels with the person, not the platform.",
      "concept": "Actors verify their identities to gain access to private rehearsal sessions, ensuring only authorized personnel are present.",
      "subDiscipline": "rehearsal management",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits rehearsal management in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Rehearsal Access Pass\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nActors verify their identities to gain access to private rehearsal sessions, ensuring only authorized personnel are present.\nDiscipline: Theater & Live Performance (rehearsal management).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits rehearsal management in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $5B — live performance production and design software · SOM $500M — indie and regional theater sound design budgets\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$5B — live performance production and design software",
      "som": "$500M — indie and regional theater sound design budgets"
    },
    {
      "id": "theater-audience-feedback-verification-0",
      "theme": "theater",
      "title": "Audience Feedback Verification",
      "pitch": "Audience Feedback Verification pairs two wallets over DIDComm so engagement metrics can exchange trusted claims on a private channel.",
      "concept": "Audience members can verify their attendance for feedback submissions through secure invites.",
      "subDiscipline": "engagement metrics",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits engagement metrics in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Audience Feedback Verification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAudience members can verify their attendance for feedback submissions through secure invites.\nDiscipline: Theater & Live Performance (engagement metrics).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits engagement metrics in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $5B — live performance production and design software · SOM $500M — indie and regional theater sound design budgets\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$5B — live performance production and design software",
      "som": "$500M — indie and regional theater sound design budgets"
    },
    {
      "id": "theater-production-timeline-tracker-0",
      "theme": "theater",
      "title": "Production Timeline Tracker",
      "pitch": "Production Timeline Tracker issues a signed verifiable credential for project management that the holder keeps and reuses anywhere.",
      "concept": "Productions can issue credentials documenting key milestones and contributions, assisting in project management and accountability.",
      "subDiscipline": "project management",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches project management in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Production Timeline Tracker\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nProductions can issue credentials documenting key milestones and contributions, assisting in project management and accountability.\nDiscipline: Theater & Live Performance (project management).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches project management in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $5B — live performance production and design software · SOM $500M — indie and regional theater sound design budgets\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$5B — live performance production and design software",
      "som": "$500M — indie and regional theater sound design budgets"
    },
    {
      "id": "theater-audience-feedback-identity-0",
      "theme": "theater",
      "title": "Audience Feedback Identity",
      "pitch": "Audience Feedback Identity verifies a credential at the gate for feedback collection — proof without a phone call to the issuer.",
      "concept": "Audience members validate their feedback contributions to theaters while maintaining anonymity with verified identities.",
      "subDiscipline": "feedback collection",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for feedback collection in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Audience Feedback Identity\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAudience members validate their feedback contributions to theaters while maintaining anonymity with verified identities.\nDiscipline: Theater & Live Performance (feedback collection).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for feedback collection in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $5B — live performance production and design software · SOM $500M — indie and regional theater sound design budgets\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$5B — live performance production and design software",
      "som": "$500M — indie and regional theater sound design budgets"
    },
    {
      "id": "theater-showcase-participation-proof-0",
      "theme": "theater",
      "title": "Showcase Participation Proof",
      "pitch": "Showcase Participation Proof mints a did:prism for showcase events so identity travels with the person, not the platform.",
      "concept": "Artists prove their participation in showcases to festival organizers, solidifying their credentials for consideration in future events.",
      "subDiscipline": "showcase events",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits showcase events in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Showcase Participation Proof\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nArtists prove their participation in showcases to festival organizers, solidifying their credentials for consideration in future events.\nDiscipline: Theater & Live Performance (showcase events).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits showcase events in Theater & Live Performance because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $5B — live performance production and design software · SOM $500M — indie and regional theater sound design budgets\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$5B — live performance production and design software",
      "som": "$500M — indie and regional theater sound design budgets"
    },
    {
      "id": "theater-event-organizer-credibility-0",
      "theme": "theater",
      "title": "Event Organizer Credibility",
      "pitch": "Event Organizer Credibility pairs two wallets over DIDComm so event management can exchange trusted claims on a private channel.",
      "concept": "Event organizers can prove their credentials to potential partners using verified connections.",
      "subDiscipline": "event management",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits event management in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Event Organizer Credibility\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nEvent organizers can prove their credentials to potential partners using verified connections.\nDiscipline: Theater & Live Performance (event management).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits event management in Theater & Live Performance because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $5B — live performance production and design software · SOM $500M — indie and regional theater sound design budgets\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$5B — live performance production and design software",
      "som": "$500M — indie and regional theater sound design budgets"
    },
    {
      "id": "theater-patron-loyalty-credential-0",
      "theme": "theater",
      "title": "Patron Loyalty Credential",
      "pitch": "Patron Loyalty Credential issues a signed verifiable credential for audience loyalty that the holder keeps and reuses anywhere.",
      "concept": "Theater patrons can obtain credentials recognizing their loyalty, providing perks for longstanding support of the company.",
      "subDiscipline": "audience loyalty",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches audience loyalty in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Patron Loyalty Credential\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTheater patrons can obtain credentials recognizing their loyalty, providing perks for longstanding support of the company.\nDiscipline: Theater & Live Performance (audience loyalty).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches audience loyalty in Theater & Live Performance because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $5B — live performance production and design software · SOM $500M — indie and regional theater sound design budgets\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$5B — live performance production and design software",
      "som": "$500M — indie and regional theater sound design budgets"
    },
    {
      "id": "theater-regional-equity-validator-0",
      "theme": "theater",
      "title": "Regional Equity Validator",
      "pitch": "Regional Equity Validator verifies a credential at the gate for equity compliance — proof without a phone call to the issuer.",
      "concept": "Theaters prove compliance with regional equity regulations by presenting verified workforce data.",
      "subDiscipline": "equity compliance",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for equity compliance in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Regional Equity Validator\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTheaters prove compliance with regional equity regulations by presenting verified workforce data.\nDiscipline: Theater & Live Performance (equity compliance).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for equity compliance in Theater & Live Performance because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $30B — global live performance market · SAM $5B — live performance production and design software · SOM $500M — indie and regional theater sound design budgets\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$30B — global live performance market",
      "sam": "$5B — live performance production and design software",
      "som": "$500M — indie and regional theater sound design budgets"
    }
  ]
}
