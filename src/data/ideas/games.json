{
  "theme": {
    "slug": "games",
    "name": "Game Design & Interactive Media",
    "emoji": "🎮",
    "audience": "game designers, interactive artists, XR creators",
    "market_anchor": "the game industry (~$200B) and >3M indie developers"
  },
  "ideas": [
    {
      "id": "games-codex-whisperer-0",
      "theme": "games",
      "title": "Codex Whisperer",
      "pitch": "Codex Whisperer spins up a fresh public Sprite for each session so narrative lore drafting gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "narrative lore drafting",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits narrative lore drafting in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Codex Whisperer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCodex Whisperer spins up a fresh public Sprite for each session so narrative lore drafting gets its own shareable sandbox URL in seconds.\nDiscipline: Game Design & Interactive Media (narrative lore drafting).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits narrative lore drafting in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `narrative-lore-drafting-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for narrative lore drafting.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$15B — narrative and RPG game development",
      "som": "$500M — indie RPG worldbuilding tools"
    },
    {
      "id": "games-playtest-prompter-1",
      "theme": "games",
      "title": "Playtest Prompter",
      "pitch": "Playtest Prompter composes live user testing in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "live user testing",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits live user testing in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Playtest Prompter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPlaytest Prompter composes live user testing in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Game Design & Interactive Media (live user testing).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits live user testing in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `live-user-testing-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for live user testing (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for live user testing.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$10B — game QA and playtesting services",
      "som": "$300M — indie user research tools"
    },
    {
      "id": "games-tavern-chatterbox-2",
      "theme": "games",
      "title": "Tavern Chatterbox",
      "pitch": "Tavern Chatterbox boots a long-running service inside a Sprite for ambient NPC barks — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "ambient NPC barks",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches ambient NPC barks in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Tavern Chatterbox\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTavern Chatterbox boots a long-running service inside a Sprite for ambient NPC barks — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Game Design & Interactive Media (ambient NPC barks).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches ambient NPC barks in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `ambient-npc-barks-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for ambient NPC barks.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits ambient NPC barks — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for ambient NPC barks.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$12B — game audio and narrative production",
      "som": "$400M — procedural NPC dialogue tools"
    },
    {
      "id": "games-locomotion-soother-3",
      "theme": "games",
      "title": "Locomotion Soother",
      "pitch": "Locomotion Soother runs a one-shot shell command inside a Sprite for XR comfort design and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "XR comfort design",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits XR comfort design in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Locomotion Soother\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLocomotion Soother runs a one-shot shell command inside a Sprite for XR comfort design and streams the stdout back so the user watches the work happen live.\nDiscipline: Game Design & Interactive Media (XR comfort design).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits XR comfort design in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `xr-comfort-design-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a XR comfort design-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for XR comfort design.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$8B — virtual and mixed reality software",
      "som": "$200M — XR accessibility and comfort plugins"
    },
    {
      "id": "games-onboarding-metronome-4",
      "theme": "games",
      "title": "Onboarding Metronome",
      "pitch": "Onboarding Metronome spins up a fresh public Sprite for each session so tutorial flow design gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "tutorial flow design",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits tutorial flow design in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Onboarding Metronome\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nOnboarding Metronome spins up a fresh public Sprite for each session so tutorial flow design gets its own shareable sandbox URL in seconds.\nDiscipline: Game Design & Interactive Media (tutorial flow design).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits tutorial flow design in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `tutorial-flow-design-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for tutorial flow design.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$20B — casual and mobile game development",
      "som": "$600M — UX and onboarding design tools"
    },
    {
      "id": "games-jam-pitch-polisher-5",
      "theme": "games",
      "title": "Jam Pitch Polisher",
      "pitch": "Jam Pitch Polisher composes game jam presentations in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "game jam presentations",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits game jam presentations in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Jam Pitch Polisher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nJam Pitch Polisher composes game jam presentations in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Game Design & Interactive Media (game jam presentations).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits game jam presentations in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `game-jam-presentations-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for game jam presentations (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for game jam presentations.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$5B — game jam and indie showcase events",
      "som": "$150M — pitch preparation software"
    },
    {
      "id": "games-blindside-navigator-6",
      "theme": "games",
      "title": "Blindside Navigator",
      "pitch": "Blindside Navigator boots a long-running service inside a Sprite for visual impairment accessibility — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "visual impairment accessibility",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches visual impairment accessibility in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Blindside Navigator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nBlindside Navigator boots a long-running service inside a Sprite for visual impairment accessibility — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Game Design & Interactive Media (visual impairment accessibility).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches visual impairment accessibility in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `visual-impairment-access-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for visual impairment accessibility.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits visual impairment accessibility — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for visual impairment accessibility.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$6B — game accessibility software and consulting",
      "som": "$250M — UI screen-reader integration tools"
    },
    {
      "id": "games-devlog-dictation-7",
      "theme": "games",
      "title": "Devlog Dictation",
      "pitch": "Devlog Dictation runs a one-shot shell command inside a Sprite for indie marketing and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "indie marketing",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits indie marketing in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Devlog Dictation\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDevlog Dictation runs a one-shot shell command inside a Sprite for indie marketing and streams the stdout back so the user watches the work happen live.\nDiscipline: Game Design & Interactive Media (indie marketing).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits indie marketing in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `indie-marketing-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a indie marketing-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for indie marketing.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$15B — game marketing and community management",
      "som": "$450M — indie creator content tools"
    },
    {
      "id": "games-corridor-tension-tuner-8",
      "theme": "games",
      "title": "Corridor Tension Tuner",
      "pitch": "Corridor Tension Tuner spins up a fresh public Sprite for each session so level design pacing gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "level design pacing",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits level design pacing in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Corridor Tension Tuner\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCorridor Tension Tuner spins up a fresh public Sprite for each session so level design pacing gets its own shareable sandbox URL in seconds.\nDiscipline: Game Design & Interactive Media (level design pacing).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits level design pacing in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `level-design-pacing-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for level design pacing.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$18B — horror and action game development",
      "som": "$550M — level design and pacing software"
    },
    {
      "id": "games-polyglot-playtester-9",
      "theme": "games",
      "title": "Polyglot Playtester",
      "pitch": "Polyglot Playtester composes localization QA in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "localization QA",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits localization QA in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Polyglot Playtester\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPolyglot Playtester composes localization QA in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Game Design & Interactive Media (localization QA).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits localization QA in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `localization-qa-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for localization QA (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for localization QA.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$10B — game localization and translation services",
      "som": "$300M — automated localization QA tools"
    },
    {
      "id": "games-empathy-scenario-spinner-10",
      "theme": "games",
      "title": "Empathy Scenario Spinner",
      "pitch": "Empathy Scenario Spinner boots a long-running service inside a Sprite for serious game design — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "serious game design",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches serious game design in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Empathy Scenario Spinner\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nEmpathy Scenario Spinner boots a long-running service inside a Sprite for serious game design — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Game Design & Interactive Media (serious game design).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches serious game design in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `serious-game-design-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for serious game design.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits serious game design — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for serious game design.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$12B — serious games and simulation software",
      "som": "$400M — medical training simulators"
    },
    {
      "id": "games-soundscape-storyboarder-11",
      "theme": "games",
      "title": "Soundscape Storyboarder",
      "pitch": "Soundscape Storyboarder runs a one-shot shell command inside a Sprite for game audio conceptualization and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "game audio conceptualization",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits game audio conceptualization in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Soundscape Storyboarder\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSoundscape Storyboarder runs a one-shot shell command inside a Sprite for game audio conceptualization and streams the stdout back so the user watches the work happen live.\nDiscipline: Game Design & Interactive Media (game audio conceptualization).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits game audio conceptualization in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `game-audio-conceptualiza-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a game audio conceptualization-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for game audio conceptualization.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$12B — game audio and narrative production",
      "som": "$350M — audio direction and storyboarding tools"
    },
    {
      "id": "games-launch-postmortem-mirror-12",
      "theme": "games",
      "title": "Launch Postmortem Mirror",
      "pitch": "Launch Postmortem Mirror spins up a fresh public Sprite for each session so team reflection gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "team reflection",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits team reflection in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Launch Postmortem Mirror\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLaunch Postmortem Mirror spins up a fresh public Sprite for each session so team reflection gets its own shareable sandbox URL in seconds.\nDiscipline: Game Design & Interactive Media (team reflection).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits team reflection in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `team-reflection-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for team reflection.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$25B — game studio management and operations",
      "som": "$700M — agile and retrospective software"
    },
    {
      "id": "games-menu-friction-finder-13",
      "theme": "games",
      "title": "Menu Friction Finder",
      "pitch": "Menu Friction Finder composes UI UX testing in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "UI UX testing",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits UI UX testing in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Menu Friction Finder\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nMenu Friction Finder composes UI UX testing in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Game Design & Interactive Media (UI UX testing).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits UI UX testing in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `ui-ux-testing-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for UI UX testing (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for UI UX testing.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$20B — game UI and UX design",
      "som": "$600M — prototyping and usability testing tools"
    },
    {
      "id": "games-twitch-hype-announcer-14",
      "theme": "games",
      "title": "Twitch Hype Announcer",
      "pitch": "Twitch Hype Announcer boots a long-running service inside a Sprite for streamer integration design — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "streamer integration design",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches streamer integration design in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Twitch Hype Announcer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTwitch Hype Announcer boots a long-running service inside a Sprite for streamer integration design — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Game Design & Interactive Media (streamer integration design).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches streamer integration design in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `streamer-integration-des-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for streamer integration design.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits streamer integration design — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for streamer integration design.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$8B — game streaming and creator economy",
      "som": "$250M — stream overlay and integration plugins"
    },
    {
      "id": "games-mocap-emotion-prompter-15",
      "theme": "games",
      "title": "Mocap Emotion Prompter",
      "pitch": "Mocap Emotion Prompter runs a one-shot shell command inside a Sprite for animation direction and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "animation direction",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits animation direction in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Mocap Emotion Prompter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nMocap Emotion Prompter runs a one-shot shell command inside a Sprite for animation direction and streams the stdout back so the user watches the work happen live.\nDiscipline: Game Design & Interactive Media (animation direction).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits animation direction in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `animation-direction-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a animation direction-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for animation direction.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$15B — 3D animation and motion capture",
      "som": "$450M — virtual production direction tools"
    },
    {
      "id": "games-math-quest-narrator-16",
      "theme": "games",
      "title": "Math Quest Narrator",
      "pitch": "Math Quest Narrator spins up a fresh public Sprite for each session so educational game design gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "educational game design",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits educational game design in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Math Quest Narrator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nMath Quest Narrator spins up a fresh public Sprite for each session so educational game design gets its own shareable sandbox URL in seconds.\nDiscipline: Game Design & Interactive Media (educational game design).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits educational game design in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `educational-game-design-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for educational game design.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$10B — educational games and edtech",
      "som": "$300M — gamified learning platforms"
    },
    {
      "id": "games-discord-patchnotes-bard-17",
      "theme": "games",
      "title": "Discord Patchnotes Bard",
      "pitch": "Discord Patchnotes Bard composes community management in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "community management",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits community management in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Discord Patchnotes Bard\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDiscord Patchnotes Bard composes community management in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Game Design & Interactive Media (community management).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits community management in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `community-management-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for community management (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for community management.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$15B — game marketing and community management",
      "som": "$400M — Discord bot and community tools"
    },
    {
      "id": "games-glitch-repro-storyteller-18",
      "theme": "games",
      "title": "Glitch Repro Storyteller",
      "pitch": "Glitch Repro Storyteller boots a long-running service inside a Sprite for QA bug reporting — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "QA bug reporting",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches QA bug reporting in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Glitch Repro Storyteller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nGlitch Repro Storyteller boots a long-running service inside a Sprite for QA bug reporting — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Game Design & Interactive Media (QA bug reporting).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches QA bug reporting in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `qa-bug-reporting-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for QA bug reporting.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits QA bug reporting — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for QA bug reporting.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$10B — game QA and playtesting services",
      "som": "$350M — issue tracking and bug management"
    },
    {
      "id": "games-choice-consequence-weaver-19",
      "theme": "games",
      "title": "Choice Consequence Weaver",
      "pitch": "Choice Consequence Weaver runs a one-shot shell command inside a Sprite for interactive fiction and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "interactive fiction",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits interactive fiction in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Choice Consequence Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nChoice Consequence Weaver runs a one-shot shell command inside a Sprite for interactive fiction and streams the stdout back so the user watches the work happen live.\nDiscipline: Game Design & Interactive Media (interactive fiction).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits interactive fiction in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `interactive-fiction-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a interactive fiction-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for interactive fiction.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$12B — interactive fiction and visual novels",
      "som": "$400M — narrative design software"
    },
    {
      "id": "games-hologram-audio-director-20",
      "theme": "games",
      "title": "Hologram Audio Director",
      "pitch": "Hologram Audio Director spins up a fresh public Sprite for each session so XR spatial audio gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "XR spatial audio",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits XR spatial audio in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Hologram Audio Director\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nHologram Audio Director spins up a fresh public Sprite for each session so XR spatial audio gets its own shareable sandbox URL in seconds.\nDiscipline: Game Design & Interactive Media (XR spatial audio).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits XR spatial audio in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `xr-spatial-audio-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for XR spatial audio.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$8B — virtual and mixed reality software",
      "som": "$250M — spatial audio design tools"
    },
    {
      "id": "games-crunch-burnout-shield-21",
      "theme": "games",
      "title": "Crunch Burnout Shield",
      "pitch": "Crunch Burnout Shield composes developer wellness in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "developer wellness",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits developer wellness in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Crunch Burnout Shield\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCrunch Burnout Shield composes developer wellness in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Game Design & Interactive Media (developer wellness).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits developer wellness in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `developer-wellness-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for developer wellness (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for developer wellness.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$25B — game studio management and operations",
      "som": "$600M — corporate wellness and ergonomics software"
    },
    {
      "id": "games-gold-sink-balancer-22",
      "theme": "games",
      "title": "Gold Sink Balancer",
      "pitch": "Gold Sink Balancer boots a long-running service inside a Sprite for game economy design — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "game economy design",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches game economy design in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Gold Sink Balancer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nGold Sink Balancer boots a long-running service inside a Sprite for game economy design — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Game Design & Interactive Media (game economy design).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches game economy design in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `game-economy-design-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for game economy design.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits game economy design — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for game economy design.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$30B — live-ops and in-game economies",
      "som": "$800M — game economy balancing tools"
    },
    {
      "id": "games-publisher-hook-sharpener-23",
      "theme": "games",
      "title": "Publisher Hook Sharpener",
      "pitch": "Publisher Hook Sharpener runs a one-shot shell command inside a Sprite for publisher pitching and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "publisher pitching",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits publisher pitching in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Publisher Hook Sharpener\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPublisher Hook Sharpener runs a one-shot shell command inside a Sprite for publisher pitching and streams the stdout back so the user watches the work happen live.\nDiscipline: Game Design & Interactive Media (publisher pitching).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits publisher pitching in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `publisher-pitching-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a publisher pitching-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for publisher pitching.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$5B — game publishing and investment",
      "som": "$150M — pitch preparation software"
    },
    {
      "id": "games-modder-api-guide-24",
      "theme": "games",
      "title": "Modder API Guide",
      "pitch": "Modder API Guide spins up a fresh public Sprite for each session so modding documentation gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "modding documentation",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits modding documentation in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Modder API Guide\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nModder API Guide spins up a fresh public Sprite for each session so modding documentation gets its own shareable sandbox URL in seconds.\nDiscipline: Game Design & Interactive Media (modding documentation).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits modding documentation in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `modding-documentation-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for modding documentation.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$10B — UGC and modding ecosystems",
      "som": "$300M — developer documentation platforms"
    },
    {
      "id": "games-phantom-dialogue-sparring-0",
      "theme": "games",
      "title": "Phantom Dialogue Sparring",
      "pitch": "Phantom Dialogue Sparring composes NPC voice acting in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "NPC voice acting",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits NPC voice acting in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Phantom Dialogue Sparring\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhantom Dialogue Sparring composes NPC voice acting in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Game Design & Interactive Media (NPC voice acting).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits NPC voice acting in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `npc-voice-acting-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for NPC voice acting (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for NPC voice acting.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — global game development software market",
      "sam": "$1.5B — narrative design and dialogue tools",
      "som": "$40M — AI voice rehearsal assistants for indie studios"
    },
    {
      "id": "games-blindfolded-level-architect-1",
      "theme": "games",
      "title": "Blindfolded Level Architect",
      "pitch": "Blindfolded Level Architect boots a long-running service inside a Sprite for spatial layout drafting — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "spatial layout drafting",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches spatial layout drafting in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Blindfolded Level Architect\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nBlindfolded Level Architect boots a long-running service inside a Sprite for spatial layout drafting — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Game Design & Interactive Media (spatial layout drafting).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches spatial layout drafting in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `spatial-layout-drafting-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for spatial layout drafting.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits spatial layout drafting — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for spatial layout drafting.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — game engines and level editors market",
      "sam": "$2B — indie level design and prototyping tools",
      "som": "$50M — AI spatial drafting companions"
    },
    {
      "id": "games-indie-sprint-therapist-2",
      "theme": "games",
      "title": "Indie Sprint Therapist",
      "pitch": "Indie Sprint Therapist runs a one-shot shell command inside a Sprite for developer mental health and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "developer mental health",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits developer mental health in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Indie Sprint Therapist\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nIndie Sprint Therapist runs a one-shot shell command inside a Sprite for developer mental health and streams the stdout back so the user watches the work happen live.\nDiscipline: Game Design & Interactive Media (developer mental health).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits developer mental health in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `developer-mental-health-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a developer mental health-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for developer mental health.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$15B — project management and productivity software",
      "sam": "$4B — game development agile and scoping tools",
      "som": "$80M — AI mental health and sprint coaching for solo devs"
    },
    {
      "id": "games-lore-inquisitor-chamber-3",
      "theme": "games",
      "title": "Lore Inquisitor Chamber",
      "pitch": "Lore Inquisitor Chamber spins up a fresh public Sprite for each session so worldbuilding consistency gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "worldbuilding consistency",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits worldbuilding consistency in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Lore Inquisitor Chamber\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLore Inquisitor Chamber spins up a fresh public Sprite for each session so worldbuilding consistency gets its own shareable sandbox URL in seconds.\nDiscipline: Game Design & Interactive Media (worldbuilding consistency).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits worldbuilding consistency in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `worldbuilding-consistenc-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for worldbuilding consistency.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — global game development software market",
      "sam": "$1.2B — worldbuilding and lore management platforms",
      "som": "$35M — AI lore consistency checkers"
    },
    {
      "id": "games-spatial-soundscape-conductor-4",
      "theme": "games",
      "title": "Spatial Soundscape Conductor",
      "pitch": "Spatial Soundscape Conductor composes XR audio prototyping in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "XR audio prototyping",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits XR audio prototyping in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Spatial Soundscape Conductor\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSpatial Soundscape Conductor composes XR audio prototyping in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Game Design & Interactive Media (XR audio prototyping).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits XR audio prototyping in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `xr-audio-prototyping-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for XR audio prototyping (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for XR audio prototyping.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$12B — game audio and XR middleware market",
      "sam": "$3B — spatial audio plugins and XR tools",
      "som": "$60M — AI voice-directed audio prototyping"
    },
    {
      "id": "games-tabletop-encounter-weaver-5",
      "theme": "games",
      "title": "Tabletop Encounter Weaver",
      "pitch": "Tabletop Encounter Weaver boots a long-running service inside a Sprite for TTRPG campaign prep — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "TTRPG campaign prep",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches TTRPG campaign prep in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Tabletop Encounter Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTabletop Encounter Weaver boots a long-running service inside a Sprite for TTRPG campaign prep — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Game Design & Interactive Media (TTRPG campaign prep).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches TTRPG campaign prep in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `ttrpg-campaign-prep-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for TTRPG campaign prep.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits TTRPG campaign prep — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for TTRPG campaign prep.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — tabletop RPG and digital companion market",
      "sam": "$1B — digital GM tools and encounter builders",
      "som": "$25M — AI campaign prep assistants"
    },
    {
      "id": "games-meta-balance-debater-6",
      "theme": "games",
      "title": "Meta Balance Debater",
      "pitch": "Meta Balance Debater runs a one-shot shell command inside a Sprite for combat economy tuning and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "combat economy tuning",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits combat economy tuning in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Meta Balance Debater\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nMeta Balance Debater runs a one-shot shell command inside a Sprite for combat economy tuning and streams the stdout back so the user watches the work happen live.\nDiscipline: Game Design & Interactive Media (combat economy tuning).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits combat economy tuning in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `combat-economy-tuning-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a combat economy tuning-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for combat economy tuning.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — game engines and analytics platforms",
      "sam": "$2.5B — game balancing and economy design tools",
      "som": "$45M — AI meta-balancing debaters"
    },
    {
      "id": "games-screenreader-flow-ghost-7",
      "theme": "games",
      "title": "Screenreader Flow Ghost",
      "pitch": "Screenreader Flow Ghost spins up a fresh public Sprite for each session so UI accessibility testing gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "UI accessibility testing",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits UI accessibility testing in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Screenreader Flow Ghost\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nScreenreader Flow Ghost spins up a fresh public Sprite for each session so UI accessibility testing gets its own shareable sandbox URL in seconds.\nDiscipline: Game Design & Interactive Media (UI accessibility testing).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits UI accessibility testing in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `ui-accessibility-testing-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for UI accessibility testing.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$9B — game testing and QA software market",
      "sam": "$2.5B — automated UX and accessibility testing",
      "som": "$45M — AI accessibility simulators for game UI"
    },
    {
      "id": "games-choice-tree-pruner-8",
      "theme": "games",
      "title": "Choice Tree Pruner",
      "pitch": "Choice Tree Pruner composes interactive fiction pacing in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "interactive fiction pacing",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits interactive fiction pacing in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Choice Tree Pruner\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nChoice Tree Pruner composes interactive fiction pacing in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Game Design & Interactive Media (interactive fiction pacing).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits interactive fiction pacing in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `interactive-fiction-paci-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for interactive fiction pacing (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for interactive fiction pacing.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — global game development software market",
      "sam": "$1.5B — interactive fiction and branching narrative tools",
      "som": "$40M — AI narrative tree pruners"
    },
    {
      "id": "games-emotional-arc-surgeon-9",
      "theme": "games",
      "title": "Emotional Arc Surgeon",
      "pitch": "Emotional Arc Surgeon boots a long-running service inside a Sprite for level pacing critique — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "level pacing critique",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches level pacing critique in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Emotional Arc Surgeon\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nEmotional Arc Surgeon boots a long-running service inside a Sprite for level pacing critique — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Game Design & Interactive Media (level pacing critique).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches level pacing critique in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `level-pacing-critique-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for level pacing critique.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits level pacing critique — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for level pacing critique.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — game engines and level editors market",
      "sam": "$2B — level design and pacing analytics tools",
      "som": "$50M — AI pacing critique companions"
    },
    {
      "id": "games-confused-rookie-simulator-10",
      "theme": "games",
      "title": "Confused Rookie Simulator",
      "pitch": "Confused Rookie Simulator runs a one-shot shell command inside a Sprite for onboarding UX testing and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "onboarding UX testing",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits onboarding UX testing in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Confused Rookie Simulator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nConfused Rookie Simulator runs a one-shot shell command inside a Sprite for onboarding UX testing and streams the stdout back so the user watches the work happen live.\nDiscipline: Game Design & Interactive Media (onboarding UX testing).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits onboarding UX testing in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `onboarding-ux-testing-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a onboarding UX testing-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for onboarding UX testing.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$9B — game testing and QA software market",
      "sam": "$2B — user onboarding and tutorial testing tools",
      "som": "$35M — AI UX rookie simulators"
    },
    {
      "id": "games-foley-scene-painter-11",
      "theme": "games",
      "title": "Foley Scene Painter",
      "pitch": "Foley Scene Painter spins up a fresh public Sprite for each session so ambient sound direction gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "ambient sound direction",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits ambient sound direction in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Foley Scene Painter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFoley Scene Painter spins up a fresh public Sprite for each session so ambient sound direction gets its own shareable sandbox URL in seconds.\nDiscipline: Game Design & Interactive Media (ambient sound direction).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits ambient sound direction in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `ambient-sound-direction-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for ambient sound direction.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$12B — game audio and XR middleware market",
      "sam": "$2.5B — foley and sound effect libraries",
      "som": "$50M — AI foley direction assistants"
    },
    {
      "id": "games-esports-caster-hype-12",
      "theme": "games",
      "title": "Esports Caster Hype",
      "pitch": "Esports Caster Hype composes trailer narrative rehearsal in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "trailer narrative rehearsal",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits trailer narrative rehearsal in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Esports Caster Hype\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nEsports Caster Hype composes trailer narrative rehearsal in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Game Design & Interactive Media (trailer narrative rehearsal).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits trailer narrative rehearsal in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `trailer-narrative-rehear-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for trailer narrative rehearsal (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for trailer narrative rehearsal.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$6B — esports and game marketing software",
      "sam": "$1.5B — trailer production and narrative tools",
      "som": "$30M — AI shoutcast rehearsal coaches"
    },
    {
      "id": "games-vestibular-sickness-oracle-13",
      "theme": "games",
      "title": "Vestibular Sickness Oracle",
      "pitch": "Vestibular Sickness Oracle boots a long-running service inside a Sprite for VR locomotion safety — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "VR locomotion safety",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches VR locomotion safety in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Vestibular Sickness Oracle\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nVestibular Sickness Oracle boots a long-running service inside a Sprite for VR locomotion safety — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Game Design & Interactive Media (VR locomotion safety).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches VR locomotion safety in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `vr-locomotion-safety-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for VR locomotion safety.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits VR locomotion safety — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for VR locomotion safety.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$12B — game audio and XR middleware market",
      "sam": "$3B — VR locomotion and comfort plugins",
      "som": "$60M — AI VR safety predictors"
    },
    {
      "id": "games-tavern-keeper-mask-14",
      "theme": "games",
      "title": "Tavern Keeper Mask",
      "pitch": "Tavern Keeper Mask runs a one-shot shell command inside a Sprite for quest giver characterization and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "quest giver characterization",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits quest giver characterization in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Tavern Keeper Mask\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTavern Keeper Mask runs a one-shot shell command inside a Sprite for quest giver characterization and streams the stdout back so the user watches the work happen live.\nDiscipline: Game Design & Interactive Media (quest giver characterization).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits quest giver characterization in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `quest-giver-characteriza-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a quest giver characterization-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for quest giver characterization.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — global game development software market",
      "sam": "$1.5B — narrative design and dialogue tools",
      "som": "$40M — AI NPC persona actors"
    },
    {
      "id": "games-storefront-hook-polisher-15",
      "theme": "games",
      "title": "Storefront Hook Polisher",
      "pitch": "Storefront Hook Polisher spins up a fresh public Sprite for each session so indie game marketing gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "indie game marketing",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits indie game marketing in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Storefront Hook Polisher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nStorefront Hook Polisher spins up a fresh public Sprite for each session so indie game marketing gets its own shareable sandbox URL in seconds.\nDiscipline: Game Design & Interactive Media (indie game marketing).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits indie game marketing in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `indie-game-marketing-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for indie game marketing.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$6B — esports and game marketing software",
      "sam": "$1.2B — indie game storefront optimization tools",
      "som": "$25M — AI pitch rehearsal companions"
    },
    {
      "id": "games-parry-timing-metronome-16",
      "theme": "games",
      "title": "Parry Timing Metronome",
      "pitch": "Parry Timing Metronome composes action combat choreography in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "action combat choreography",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits action combat choreography in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Parry Timing Metronome\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nParry Timing Metronome composes action combat choreography in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Game Design & Interactive Media (action combat choreography).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits action combat choreography in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `action-combat-choreograp-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for action combat choreography (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for action combat choreography.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — game engines and animation middleware",
      "sam": "$2.5B — combat design and choreography tools",
      "som": "$45M — AI combat rhythm callers"
    },
    {
      "id": "games-cultural-nuance-translator-17",
      "theme": "games",
      "title": "Cultural Nuance Translator",
      "pitch": "Cultural Nuance Translator boots a long-running service inside a Sprite for dialogue localization QA — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "dialogue localization QA",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches dialogue localization QA in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Cultural Nuance Translator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCultural Nuance Translator boots a long-running service inside a Sprite for dialogue localization QA — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Game Design & Interactive Media (dialogue localization QA).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches dialogue localization QA in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `dialogue-localization-qa-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for dialogue localization QA.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits dialogue localization QA — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for dialogue localization QA.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — global game development software market",
      "sam": "$2B — game localization and translation QA",
      "som": "$50M — AI localization tone checkers"
    },
    {
      "id": "games-roguelike-dungeon-caller-18",
      "theme": "games",
      "title": "Roguelike Dungeon Caller",
      "pitch": "Roguelike Dungeon Caller runs a one-shot shell command inside a Sprite for procedural generation narrative and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "procedural generation narrative",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits procedural generation narrative in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Roguelike Dungeon Caller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nRoguelike Dungeon Caller runs a one-shot shell command inside a Sprite for procedural generation narrative and streams the stdout back so the user watches the work happen live.\nDiscipline: Game Design & Interactive Media (procedural generation narrative).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits procedural generation narrative in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `procedural-generation-na-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a procedural generation narrative-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for procedural generation narrative.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — game engines and level editors market",
      "sam": "$1.5B — procedural generation and roguelike tools",
      "som": "$35M — AI dungeon flow narrators"
    },
    {
      "id": "games-casual-gamer-proxy-19",
      "theme": "games",
      "title": "Casual Gamer Proxy",
      "pitch": "Casual Gamer Proxy spins up a fresh public Sprite for each session so player demographic simulation gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "player demographic simulation",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits player demographic simulation in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Casual Gamer Proxy\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCasual Gamer Proxy spins up a fresh public Sprite for each session so player demographic simulation gets its own shareable sandbox URL in seconds.\nDiscipline: Game Design & Interactive Media (player demographic simulation).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits player demographic simulation in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `player-demographic-simul-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for player demographic simulation.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$9B — game testing and QA software market",
      "sam": "$2B — player demographic and difficulty testing",
      "som": "$40M — AI player empathy simulators"
    },
    {
      "id": "games-glitch-route-navigator-20",
      "theme": "games",
      "title": "Glitch Route Navigator",
      "pitch": "Glitch Route Navigator composes speedrun sequence planning in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "speedrun sequence planning",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits speedrun sequence planning in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Glitch Route Navigator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nGlitch Route Navigator composes speedrun sequence planning in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Game Design & Interactive Media (speedrun sequence planning).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits speedrun sequence planning in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `speedrun-sequence-planni-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for speedrun sequence planning (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for speedrun sequence planning.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — esports and streaming companion market",
      "sam": "$1B — speedrunning and routing tools",
      "som": "$20M — AI speedrun route callers"
    },
    {
      "id": "games-prose-pacing-director-21",
      "theme": "games",
      "title": "Prose Pacing Director",
      "pitch": "Prose Pacing Director boots a long-running service inside a Sprite for text adventure answer — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "text adventure answer",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches text adventure answer in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Prose Pacing Director\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nProse Pacing Director boots a long-running service inside a Sprite for text adventure answer — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Game Design & Interactive Media (text adventure answer).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches text adventure answer in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `text-adventure-answer-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for text adventure answer.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits text adventure answer — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for text adventure answer.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — global game development software market",
      "sam": "$1B — interactive fiction and text adventure engines",
      "som": "$25M — AI prose pacing directors"
    },
    {
      "id": "games-voice-chat-referee-22",
      "theme": "games",
      "title": "Voice Chat Referee",
      "pitch": "Voice Chat Referee runs a one-shot shell command inside a Sprite for multiplayer moderation training and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "multiplayer moderation training",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits multiplayer moderation training in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Voice Chat Referee\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nVoice Chat Referee runs a one-shot shell command inside a Sprite for multiplayer moderation training and streams the stdout back so the user watches the work happen live.\nDiscipline: Game Design & Interactive Media (multiplayer moderation training).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits multiplayer moderation training in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `multiplayer-moderation-t-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a multiplayer moderation training-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for multiplayer moderation training.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$9B — game testing and QA software market",
      "sam": "$2.5B — multiplayer moderation and safety tools",
      "som": "$50M — AI toxicity simulation referees"
    },
    {
      "id": "games-jam-scope-guillotine-23",
      "theme": "games",
      "title": "Jam Scope Guillotine",
      "pitch": "Jam Scope Guillotine spins up a fresh public Sprite for each session so game jam feature cutting gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "game jam feature cutting",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits game jam feature cutting in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Jam Scope Guillotine\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nJam Scope Guillotine spins up a fresh public Sprite for each session so game jam feature cutting gets its own shareable sandbox URL in seconds.\nDiscipline: Game Design & Interactive Media (game jam feature cutting).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits game jam feature cutting in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `game-jam-feature-cutting-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for game jam feature cutting.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$15B — project management and productivity software",
      "sam": "$3B — game jam and rapid prototyping tools",
      "som": "$60M — AI scope management cops"
    },
    {
      "id": "games-launch-autopsy-interviewer-24",
      "theme": "games",
      "title": "Launch Autopsy Interviewer",
      "pitch": "Launch Autopsy Interviewer composes development post-mortem in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "development post-mortem",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits development post-mortem in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Launch Autopsy Interviewer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLaunch Autopsy Interviewer composes development post-mortem in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Game Design & Interactive Media (development post-mortem).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits development post-mortem in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `development-post-mortem-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for development post-mortem (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for development post-mortem.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$15B — project management and productivity software",
      "sam": "$4B — game development analytics and post-mortem tools",
      "som": "$80M — AI launch autopsy interviewers"
    },
    {
      "id": "games-playtest-whisperer-0",
      "theme": "games",
      "title": "Playtest Whisperer",
      "pitch": "Playtest Whisperer boots a long-running service inside a Sprite for UX research — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "UX research",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches UX research in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Playtest Whisperer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPlaytest Whisperer boots a long-running service inside a Sprite for UX research — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Game Design & Interactive Media (UX research).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches UX research in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `ux-research-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for UX research.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits UX research — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for UX research.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — global game UX research",
      "sam": "$2B — indie playtesting tools",
      "som": "$50M — think-aloud protocol software"
    },
    {
      "id": "games-dungeon-scribe-1",
      "theme": "games",
      "title": "Dungeon Live Notes",
      "pitch": "Dungeon Live Notes runs a one-shot shell command inside a Sprite for tabletop campaign logging and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "tabletop campaign logging",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits tabletop campaign logging in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Dungeon Live Notes\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDungeon Live Notes runs a one-shot shell command inside a Sprite for tabletop campaign logging and streams the stdout back so the user watches the work happen live.\nDiscipline: Game Design & Interactive Media (tabletop campaign logging).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits tabletop campaign logging in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `tabletop-campaign-loggin-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a tabletop campaign logging-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for tabletop campaign logging.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$15B — tabletop gaming market",
      "sam": "$3B — digital TTRPG companions",
      "som": "$100M — session transcription apps"
    },
    {
      "id": "games-split-catcher-2",
      "theme": "games",
      "title": "Split Catcher",
      "pitch": "Split Catcher spins up a fresh public Sprite for each session so esports broadcasting gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "esports broadcasting",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits esports broadcasting in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Split Catcher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSplit Catcher spins up a fresh public Sprite for each session so esports broadcasting gets its own shareable sandbox URL in seconds.\nDiscipline: Game Design & Interactive Media (esports broadcasting).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits esports broadcasting in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `esports-broadcasting-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for esports broadcasting.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — esports broadcasting software",
      "sam": "$800M — speedrunning community tools",
      "som": "$20M — VOD editing assistants"
    },
    {
      "id": "games-indie-monologue-3",
      "theme": "games",
      "title": "Indie Monologue",
      "pitch": "Indie Monologue composes developer marketing in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "developer marketing",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits developer marketing in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Indie Monologue\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nIndie Monologue composes developer marketing in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Game Design & Interactive Media (developer marketing).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits developer marketing in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `developer-marketing-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for developer marketing (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for developer marketing.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$12B — creator economy software",
      "sam": "$1B — indie dev marketing tools",
      "som": "$40M — devlog generation platforms"
    },
    {
      "id": "games-spatial-echo-4",
      "theme": "games",
      "title": "Spatial Echo",
      "pitch": "Spatial Echo boots a long-running service inside a Sprite for XR interaction design — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "XR interaction design",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches XR interaction design in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Spatial Echo\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSpatial Echo boots a long-running service inside a Sprite for XR interaction design — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Game Design & Interactive Media (XR interaction design).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches XR interaction design in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `xr-interaction-design-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for XR interaction design.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits XR interaction design — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for XR interaction design.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$25B — XR development software",
      "sam": "$5B — spatial UI prototyping",
      "som": "$150M — voice interaction datasets"
    },
    {
      "id": "games-barksmith-jam-5",
      "theme": "games",
      "title": "Barksmith Jam",
      "pitch": "Barksmith Jam runs a one-shot shell command inside a Sprite for narrative design and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "narrative design",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits narrative design in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Barksmith Jam\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nBarksmith Jam runs a one-shot shell command inside a Sprite for narrative design and streams the stdout back so the user watches the work happen live.\nDiscipline: Game Design & Interactive Media (narrative design).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits narrative design in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `narrative-design-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a narrative design-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for narrative design.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — game narrative tools",
      "sam": "$2B — indie dialogue editors",
      "som": "$60M — voice-to-text game engines"
    },
    {
      "id": "games-walkthrough-whisper-6",
      "theme": "games",
      "title": "Walkthrough Whisper",
      "pitch": "Walkthrough Whisper spins up a fresh public Sprite for each session so environment art gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "environment art",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits environment art in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Walkthrough Whisper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nWalkthrough Whisper spins up a fresh public Sprite for each session so environment art gets its own shareable sandbox URL in seconds.\nDiscipline: Game Design & Interactive Media (environment art).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits environment art in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `environment-art-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for environment art.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$6B — 3D level design software",
      "sam": "$1.5B — environment art tools",
      "som": "$30M — spatial pacing analytics"
    },
    {
      "id": "games-foley-mimic-7",
      "theme": "games",
      "title": "Foley Mimic",
      "pitch": "Foley Mimic composes audio engineering in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "audio engineering",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits audio engineering in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Foley Mimic\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFoley Mimic composes audio engineering in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Game Design & Interactive Media (audio engineering).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits audio engineering in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `audio-engineering-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for audio engineering (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for audio engineering.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — game audio middleware",
      "sam": "$800M — indie sound design tools",
      "som": "$25M — foley tagging software"
    },
    {
      "id": "games-jam-huddle-8",
      "theme": "games",
      "title": "Jam Huddle",
      "pitch": "Jam Huddle boots a long-running service inside a Sprite for game production — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "game production",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches game production in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Jam Huddle\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nJam Huddle boots a long-running service inside a Sprite for game production — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Game Design & Interactive Media (game production).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches game production in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `game-production-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for game production.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits game production — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for game production.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — project management software",
      "sam": "$500M — game jam coordination",
      "som": "$15M — hackathon scoping tools"
    },
    {
      "id": "games-mythos-murmur-9",
      "theme": "games",
      "title": "Mythos Murmur",
      "pitch": "Mythos Murmur runs a one-shot shell command inside a Sprite for worldbuilding and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "worldbuilding",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits worldbuilding in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Mythos Murmur\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nMythos Murmur runs a one-shot shell command inside a Sprite for worldbuilding and streams the stdout back so the user watches the work happen live.\nDiscipline: Game Design & Interactive Media (worldbuilding).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits worldbuilding in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `worldbuilding-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a worldbuilding-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for worldbuilding.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — creative writing software",
      "sam": "$1B — game lore wikis",
      "som": "$35M — worldbuilding assistants"
    },
    {
      "id": "games-onboarding-echo-10",
      "theme": "games",
      "title": "Onboarding Echo",
      "pitch": "Onboarding Echo spins up a fresh public Sprite for each session so UX onboarding gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "UX onboarding",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits UX onboarding in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Onboarding Echo\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nOnboarding Echo spins up a fresh public Sprite for each session so UX onboarding gets its own shareable sandbox URL in seconds.\nDiscipline: Game Design & Interactive Media (UX onboarding).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits UX onboarding in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `ux-onboarding-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for UX onboarding.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$7B — game analytics platforms",
      "sam": "$1.2B — tutorial optimization tools",
      "som": "$40M — onboarding UX software"
    },
    {
      "id": "games-branch-weaver-11",
      "theme": "games",
      "title": "Branch Weaver",
      "pitch": "Branch Weaver composes text adventure design in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "text adventure design",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits text adventure design in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Branch Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nBranch Weaver composes text adventure design in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Game Design & Interactive Media (text adventure design).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits text adventure design in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `text-adventure-design-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for text adventure design (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for text adventure design.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — interactive fiction engines",
      "sam": "$800M — narrative game tools",
      "som": "$20M — branching dialogue mappers"
    },
    {
      "id": "games-rulebook-referee-12",
      "theme": "games",
      "title": "Rulebook Referee",
      "pitch": "Rulebook Referee boots a long-running service inside a Sprite for tabletop design — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "tabletop design",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches tabletop design in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Rulebook Referee\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nRulebook Referee boots a long-running service inside a Sprite for tabletop design — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Game Design & Interactive Media (tabletop design).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches tabletop design in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `tabletop-design-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for tabletop design.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits tabletop design — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for tabletop design.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$12B — board game publishing",
      "sam": "$2B — tabletop design software",
      "som": "$50M — playtesting feedback tools"
    },
    {
      "id": "games-debrief-scribe-13",
      "theme": "games",
      "title": "Debrief Live Notes",
      "pitch": "Debrief Live Notes runs a one-shot shell command inside a Sprite for edtech simulation and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "edtech simulation",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits edtech simulation in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Debrief Live Notes\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDebrief Live Notes runs a one-shot shell command inside a Sprite for edtech simulation and streams the stdout back so the user watches the work happen live.\nDiscipline: Game Design & Interactive Media (edtech simulation).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits edtech simulation in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `edtech-simulation-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a edtech simulation-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for edtech simulation.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$20B — serious games market",
      "sam": "$4B — corporate training sims",
      "som": "$100M — simulation debrief software"
    },
    {
      "id": "games-hitstop-hustle-14",
      "theme": "games",
      "title": "Hitstop Hustle",
      "pitch": "Hitstop Hustle spins up a fresh public Sprite for each session so game feel tuning gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "game feel tuning",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits game feel tuning in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Hitstop Hustle\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nHitstop Hustle spins up a fresh public Sprite for each session so game feel tuning gets its own shareable sandbox URL in seconds.\nDiscipline: Game Design & Interactive Media (game feel tuning).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits game feel tuning in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `game-feel-tuning-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for game feel tuning.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$9B — game engine plugins",
      "sam": "$1.5B — combat design tools",
      "som": "$30M — game feel analytics"
    },
    {
      "id": "games-zen-prompter-15",
      "theme": "games",
      "title": "Zen Prompter",
      "pitch": "Zen Prompter composes wellness XR in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "wellness XR",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits wellness XR in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Zen Prompter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nZen Prompter composes wellness XR in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Game Design & Interactive Media (wellness XR).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits wellness XR in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `wellness-xr-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for wellness XR (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for wellness XR.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$6B — digital wellness apps",
      "sam": "$1B — VR meditation software",
      "som": "$25M — biofeedback XR tools"
    },
    {
      "id": "games-puzzle-prompt-16",
      "theme": "games",
      "title": "Puzzle Prompt",
      "pitch": "Puzzle Prompt boots a long-running service inside a Sprite for live ops design — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "live ops design",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches live ops design in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Puzzle Prompt\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPuzzle Prompt boots a long-running service inside a Sprite for live ops design — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Game Design & Interactive Media (live ops design).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches live ops design in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `live-ops-design-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for live ops design.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits live ops design — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for live ops design.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — escape room software",
      "sam": "$600M — live puzzle operations",
      "som": "$15M — automated hint systems"
    },
    {
      "id": "games-seed-whisperer-17",
      "theme": "games",
      "title": "Seed Whisperer",
      "pitch": "Seed Whisperer runs a one-shot shell command inside a Sprite for technical design and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "technical design",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits technical design in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Seed Whisperer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSeed Whisperer runs a one-shot shell command inside a Sprite for technical design and streams the stdout back so the user watches the work happen live.\nDiscipline: Game Design & Interactive Media (technical design).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits technical design in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `technical-design-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a technical design-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for technical design.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — procedural generation tools",
      "sam": "$1B — indie terrain editors",
      "som": "$20M — AI seed generators"
    },
    {
      "id": "games-rumble-dictation-18",
      "theme": "games",
      "title": "Rumble Dictation",
      "pitch": "Rumble Dictation spins up a fresh public Sprite for each session so hardware UX gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "hardware UX",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits hardware UX in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Rumble Dictation\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nRumble Dictation spins up a fresh public Sprite for each session so hardware UX gets its own shareable sandbox URL in seconds.\nDiscipline: Game Design & Interactive Media (hardware UX).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits hardware UX in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `hardware-ux-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for hardware UX.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — game peripheral software",
      "sam": "$800M — haptic design tools",
      "som": "$25M — controller feedback mappers"
    },
    {
      "id": "games-chat-conductor-19",
      "theme": "games",
      "title": "Chat Conductor",
      "pitch": "Chat Conductor composes live broadcasting in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "live broadcasting",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits live broadcasting in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Chat Conductor\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nChat Conductor composes live broadcasting in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Game Design & Interactive Media (live broadcasting).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits live broadcasting in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `live-broadcasting-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for live broadcasting (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for live broadcasting.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — streaming software market",
      "sam": "$2B — Twitch extension tools",
      "som": "$60M — live audience interaction"
    },
    {
      "id": "games-a11y-advocate-20",
      "theme": "games",
      "title": "A11y Advocate",
      "pitch": "A11y Advocate boots a long-running service inside a Sprite for inclusive design — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "inclusive design",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches inclusive design in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"A11y Advocate\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nA11y Advocate boots a long-running service inside a Sprite for inclusive design — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Game Design & Interactive Media (inclusive design).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches inclusive design in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `inclusive-design-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for inclusive design.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits inclusive design — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for inclusive design.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — game accessibility tools",
      "sam": "$1.5B — inclusive design software",
      "som": "$40M — audio description mapping"
    },
    {
      "id": "games-lip-sync-scribe-21",
      "theme": "games",
      "title": "Lip Sync Live Notes",
      "pitch": "Lip Sync Live Notes runs a one-shot shell command inside a Sprite for translation VO and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "translation VO",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits translation VO in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Lip Sync Live Notes\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLip Sync Live Notes runs a one-shot shell command inside a Sprite for translation VO and streams the stdout back so the user watches the work happen live.\nDiscipline: Game Design & Interactive Media (translation VO).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits translation VO in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `translation-vo-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a translation VO-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for translation VO.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$7B — game localization market",
      "sam": "$1.2B — VO sync software",
      "som": "$35M — lip flap timing tools"
    },
    {
      "id": "games-harmony-warden-22",
      "theme": "games",
      "title": "Harmony Warden",
      "pitch": "Harmony Warden spins up a fresh public Sprite for each session so community safety gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "community safety",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits community safety in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Harmony Warden\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nHarmony Warden spins up a fresh public Sprite for each session so community safety gets its own shareable sandbox URL in seconds.\nDiscipline: Game Design & Interactive Media (community safety).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits community safety in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `community-safety-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for community safety.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — gaming moderation tools",
      "sam": "$1B — multiplayer safety software",
      "som": "$30M — voice toxicity filters"
    },
    {
      "id": "games-mocap-murmur-23",
      "theme": "games",
      "title": "Mocap Murmur",
      "pitch": "Mocap Murmur composes animation pipeline in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "animation pipeline",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits animation pipeline in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Mocap Murmur\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nMocap Murmur composes animation pipeline in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Game Design & Interactive Media (animation pipeline).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits animation pipeline in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `animation-pipeline-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for animation pipeline (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for animation pipeline.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$12B — 3D animation software",
      "sam": "$3B — game mocap pipelines",
      "som": "$80M — facial sync automation"
    },
    {
      "id": "games-strat-scribe-24",
      "theme": "games",
      "title": "Strat Live Notes",
      "pitch": "Strat Live Notes boots a long-running service inside a Sprite for competitive coaching — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "competitive coaching",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches competitive coaching in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Strat Live Notes\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nStrat Live Notes boots a long-running service inside a Sprite for competitive coaching — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Game Design & Interactive Media (competitive coaching).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches competitive coaching in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `competitive-coaching-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for competitive coaching.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits competitive coaching — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for competitive coaching.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$6B — esports coaching platforms",
      "sam": "$1.5B — competitive VOD review",
      "som": "$45M — team comms analytics"
    },
    {
      "id": "games-biome-breath-crafter-0",
      "theme": "games",
      "title": "Biome Breath Crafter",
      "pitch": "Biome Breath Crafter runs a one-shot shell command inside a Sprite for environmental soundscape design and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "environmental soundscape design",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits environmental soundscape design in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Biome Breath Crafter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nBiome Breath Crafter runs a one-shot shell command inside a Sprite for environmental soundscape design and streams the stdout back so the user watches the work happen live.\nDiscipline: Game Design & Interactive Media (environmental soundscape design).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits environmental soundscape design in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `environmental-soundscape-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a environmental soundscape design-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for environmental soundscape design.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$5B — game audio middleware",
      "som": "$50M — indie environmental art tools"
    },
    {
      "id": "games-menu-click-alchemist-1",
      "theme": "games",
      "title": "Menu Click Alchemist",
      "pitch": "Menu Click Alchemist spins up a fresh public Sprite for each session so interface audio feedback gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "interface audio feedback",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits interface audio feedback in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Menu Click Alchemist\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nMenu Click Alchemist spins up a fresh public Sprite for each session so interface audio feedback gets its own shareable sandbox URL in seconds.\nDiscipline: Game Design & Interactive Media (interface audio feedback).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits interface audio feedback in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `interface-audio-feedback-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for interface audio feedback.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$2B — UI/UX design software",
      "som": "$20M — indie game UI toolkits"
    },
    {
      "id": "games-sword-clash-composer-2",
      "theme": "games",
      "title": "Sword Clash Composer",
      "pitch": "Sword Clash Composer composes combat foley generation in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "combat foley generation",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits combat foley generation in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Sword Clash Composer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSword Clash Composer composes combat foley generation in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Game Design & Interactive Media (combat foley generation).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits combat foley generation in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `combat-foley-generation-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for combat foley generation (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for combat foley generation.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$4B — game engine asset stores",
      "som": "$40M — indie action game dev tools"
    },
    {
      "id": "games-boss-theme-weaver-3",
      "theme": "games",
      "title": "Boss Theme Weaver",
      "pitch": "Boss Theme Weaver boots a long-running service inside a Sprite for adaptive battle scoring — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "adaptive battle scoring",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches adaptive battle scoring in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Boss Theme Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nBoss Theme Weaver boots a long-running service inside a Sprite for adaptive battle scoring — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Game Design & Interactive Media (adaptive battle scoring).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches adaptive battle scoring in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `adaptive-battle-scoring-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for adaptive battle scoring.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits adaptive battle scoring — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for adaptive battle scoring.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$3B — game audio production",
      "som": "$30M — indie RPG music licensing"
    },
    {
      "id": "games-dread-pulse-architect-4",
      "theme": "games",
      "title": "Dread Pulse Architect",
      "pitch": "Dread Pulse Architect runs a one-shot shell command inside a Sprite for horror tension building and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "horror tension building",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits horror tension building in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Dread Pulse Architect\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDread Pulse Architect runs a one-shot shell command inside a Sprite for horror tension building and streams the stdout back so the user watches the work happen live.\nDiscipline: Game Design & Interactive Media (horror tension building).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits horror tension building in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `horror-tension-building-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a horror tension building-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for horror tension building.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$1B — horror game development",
      "som": "$15M — indie horror audio assets"
    },
    {
      "id": "games-neon-drive-synth-5",
      "theme": "games",
      "title": "Neon Drive Synth",
      "pitch": "Neon Drive Synth spins up a fresh public Sprite for each session so sci-fi vehicle audio gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "sci-fi vehicle audio",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits sci-fi vehicle audio in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Neon Drive Synth\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nNeon Drive Synth spins up a fresh public Sprite for each session so sci-fi vehicle audio gets its own shareable sandbox URL in seconds.\nDiscipline: Game Design & Interactive Media (sci-fi vehicle audio).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits sci-fi vehicle audio in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `sci-fi-vehicle-audio-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for sci-fi vehicle audio.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$2B — sci-fi game assets",
      "som": "$25M — racing game sound design"
    },
    {
      "id": "games-spellcast-resonance-6",
      "theme": "games",
      "title": "Spellcast Resonance",
      "pitch": "Spellcast Resonance composes magic VFX audio in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "magic VFX audio",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits magic VFX audio in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Spellcast Resonance\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSpellcast Resonance composes magic VFX audio in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Game Design & Interactive Media (magic VFX audio).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits magic VFX audio in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `magic-vfx-audio-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for magic VFX audio (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for magic VFX audio.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$5B — fantasy game development",
      "som": "$60M — indie magic system toolkits"
    },
    {
      "id": "games-blind-navigator-chime-7",
      "theme": "games",
      "title": "Blind Navigator Chime",
      "pitch": "Blind Navigator Chime boots a long-running service inside a Sprite for accessible spatial audio — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "accessible spatial audio",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches accessible spatial audio in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Blind Navigator Chime\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nBlind Navigator Chime boots a long-running service inside a Sprite for accessible spatial audio — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Game Design & Interactive Media (accessible spatial audio).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches accessible spatial audio in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `accessible-spatial-audio-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for accessible spatial audio.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits accessible spatial audio — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for accessible spatial audio.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$500M — accessibility software",
      "som": "$10M — inclusive design plugins"
    },
    {
      "id": "games-hearthside-loop-maker-8",
      "theme": "games",
      "title": "Hearthside Loop Maker",
      "pitch": "Hearthside Loop Maker runs a one-shot shell command inside a Sprite for cozy game ambience and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "cozy game ambience",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits cozy game ambience in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Hearthside Loop Maker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nHearthside Loop Maker runs a one-shot shell command inside a Sprite for cozy game ambience and streams the stdout back so the user watches the work happen live.\nDiscipline: Game Design & Interactive Media (cozy game ambience).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits cozy game ambience in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `cozy-game-ambience-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a cozy game ambience-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for cozy game ambience.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$3B — cozy game market",
      "som": "$35M — simulation game audio"
    },
    {
      "id": "games-haptic-ghost-echo-9",
      "theme": "games",
      "title": "Haptic Ghost Echo",
      "pitch": "Haptic Ghost Echo spins up a fresh public Sprite for each session so VR spatial audio gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "VR spatial audio",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits VR spatial audio in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Haptic Ghost Echo\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nHaptic Ghost Echo spins up a fresh public Sprite for each session so VR spatial audio gets its own shareable sandbox URL in seconds.\nDiscipline: Game Design & Interactive Media (VR spatial audio).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits VR spatial audio in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `vr-spatial-audio-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for VR spatial audio.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$50B — XR hardware and software",
      "sam": "$5B — VR game development",
      "som": "$50M — VR haptic audio tools"
    },
    {
      "id": "games-pixel-crunch-oscillator-10",
      "theme": "games",
      "title": "Pixel Crunch Oscillator",
      "pitch": "Pixel Crunch Oscillator composes retro chiptune scoring in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "retro chiptune scoring",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits retro chiptune scoring in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Pixel Crunch Oscillator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPixel Crunch Oscillator composes retro chiptune scoring in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Game Design & Interactive Media (retro chiptune scoring).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits retro chiptune scoring in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `retro-chiptune-scoring-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for retro chiptune scoring (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for retro chiptune scoring.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$2B — retro indie game market",
      "som": "$20M — chiptune asset stores"
    },
    {
      "id": "games-logic-lock-clicker-11",
      "theme": "games",
      "title": "Logic Lock Clicker",
      "pitch": "Logic Lock Clicker boots a long-running service inside a Sprite for puzzle feedback audio — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "puzzle feedback audio",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches puzzle feedback audio in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Logic Lock Clicker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLogic Lock Clicker boots a long-running service inside a Sprite for puzzle feedback audio — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Game Design & Interactive Media (puzzle feedback audio).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches puzzle feedback audio in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `puzzle-feedback-audio-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for puzzle feedback audio.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits puzzle feedback audio — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for puzzle feedback audio.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$4B — puzzle game market",
      "som": "$40M — mobile puzzle audio"
    },
    {
      "id": "games-beast-roar-forge-12",
      "theme": "games",
      "title": "Beast Roar Forge",
      "pitch": "Beast Roar Forge runs a one-shot shell command inside a Sprite for creature vocalization and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "creature vocalization",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits creature vocalization in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Beast Roar Forge\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nBeast Roar Forge runs a one-shot shell command inside a Sprite for creature vocalization and streams the stdout back so the user watches the work happen live.\nDiscipline: Game Design & Interactive Media (creature vocalization).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits creature vocalization in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `creature-vocalization-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a creature vocalization-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for creature vocalization.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$3B — creature design software",
      "som": "$30M — monster taming game assets"
    },
    {
      "id": "games-wasteland-radio-dj-13",
      "theme": "games",
      "title": "Wasteland Radio DJ",
      "pitch": "Wasteland Radio DJ spins up a fresh public Sprite for each session so diegetic radio scoring gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "diegetic radio scoring",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits diegetic radio scoring in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Wasteland Radio DJ\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nWasteland Radio DJ spins up a fresh public Sprite for each session so diegetic radio scoring gets its own shareable sandbox URL in seconds.\nDiscipline: Game Design & Interactive Media (diegetic radio scoring).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits diegetic radio scoring in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `diegetic-radio-scoring-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for diegetic radio scoring.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$6B — narrative game development",
      "som": "$60M — open-world audio assets"
    },
    {
      "id": "games-stadium-swarm-dynamics-14",
      "theme": "games",
      "title": "Stadium Swarm Dynamics",
      "pitch": "Stadium Swarm Dynamics composes sports crowd audio in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "sports crowd audio",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits sports crowd audio in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Stadium Swarm Dynamics\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nStadium Swarm Dynamics composes sports crowd audio in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Game Design & Interactive Media (sports crowd audio).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits sports crowd audio in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `sports-crowd-audio-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for sports crowd audio (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for sports crowd audio.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$8B — sports game market",
      "som": "$80M — sports audio middleware"
    },
    {
      "id": "games-shadow-step-foley-15",
      "theme": "games",
      "title": "Shadow Step Foley",
      "pitch": "Shadow Step Foley boots a long-running service inside a Sprite for stealth movement audio — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "stealth movement audio",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches stealth movement audio in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Shadow Step Foley\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nShadow Step Foley boots a long-running service inside a Sprite for stealth movement audio — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Game Design & Interactive Media (stealth movement audio).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches stealth movement audio in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `stealth-movement-audio-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for stealth movement audio.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits stealth movement audio — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for stealth movement audio.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$2B — stealth game market",
      "som": "$25M — indie stealth foley packs"
    },
    {
      "id": "games-beatmap-drop-forger-16",
      "theme": "games",
      "title": "Beatmap Drop Forger",
      "pitch": "Beatmap Drop Forger runs a one-shot shell command inside a Sprite for rhythm game scoring and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "rhythm game scoring",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits rhythm game scoring in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Beatmap Drop Forger\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nBeatmap Drop Forger runs a one-shot shell command inside a Sprite for rhythm game scoring and streams the stdout back so the user watches the work happen live.\nDiscipline: Game Design & Interactive Media (rhythm game scoring).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits rhythm game scoring in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `rhythm-game-scoring-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a rhythm game scoring-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for rhythm game scoring.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$1B — rhythm game market",
      "som": "$15M — indie rhythm game audio"
    },
    {
      "id": "games-campfire-crackler-17",
      "theme": "games",
      "title": "Campfire Crackler",
      "pitch": "Campfire Crackler spins up a fresh public Sprite for each session so survival crafting audio gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "survival crafting audio",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits survival crafting audio in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Campfire Crackler\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCampfire Crackler spins up a fresh public Sprite for each session so survival crafting audio gets its own shareable sandbox URL in seconds.\nDiscipline: Game Design & Interactive Media (survival crafting audio).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits survival crafting audio in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `survival-crafting-audio-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for survival crafting audio.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$4B — survival game market",
      "som": "$40M — crafting sim audio assets"
    },
    {
      "id": "games-bouncy-spring-synth-18",
      "theme": "games",
      "title": "Bouncy Spring Synth",
      "pitch": "Bouncy Spring Synth composes platformer movement audio in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "platformer movement audio",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits platformer movement audio in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Bouncy Spring Synth\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nBouncy Spring Synth composes platformer movement audio in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Game Design & Interactive Media (platformer movement audio).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits platformer movement audio in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `platformer-movement-audi-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for platformer movement audio (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for platformer movement audio.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$3B — platformer game market",
      "som": "$30M — 2D indie game audio"
    },
    {
      "id": "games-epic-loot-shimmer-19",
      "theme": "games",
      "title": "Epic Loot Shimmer",
      "pitch": "Epic Loot Shimmer boots a long-running service inside a Sprite for reward psychology audio — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "reward psychology audio",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches reward psychology audio in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Epic Loot Shimmer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nEpic Loot Shimmer boots a long-running service inside a Sprite for reward psychology audio — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Game Design & Interactive Media (reward psychology audio).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches reward psychology audio in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `reward-psychology-audio-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for reward psychology audio.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits reward psychology audio — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for reward psychology audio.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$10B — live-service game economy",
      "som": "$100M — gacha audio design"
    },
    {
      "id": "games-mentor-chime-guide-20",
      "theme": "games",
      "title": "Mentor Chime Guide",
      "pitch": "Mentor Chime Guide runs a one-shot shell command inside a Sprite for tutorial audio feedback and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "tutorial audio feedback",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits tutorial audio feedback in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Mentor Chime Guide\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nMentor Chime Guide runs a one-shot shell command inside a Sprite for tutorial audio feedback and streams the stdout back so the user watches the work happen live.\nDiscipline: Game Design & Interactive Media (tutorial audio feedback).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits tutorial audio feedback in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `tutorial-audio-feedback-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a tutorial audio feedback-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for tutorial audio feedback.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$1B — UX onboarding tools",
      "som": "$10M — game tutorial asset packs"
    },
    {
      "id": "games-sonic-boom-weaver-21",
      "theme": "games",
      "title": "Sonic Boom Weaver",
      "pitch": "Sonic Boom Weaver spins up a fresh public Sprite for each session so flight sim audio gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "flight sim audio",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits flight sim audio in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Sonic Boom Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSonic Boom Weaver spins up a fresh public Sprite for each session so flight sim audio gets its own shareable sandbox URL in seconds.\nDiscipline: Game Design & Interactive Media (flight sim audio).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits flight sim audio in Game Design & Interactive Media because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `flight-sim-audio-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for flight sim audio.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$2B — simulation game market",
      "som": "$20M — flight sim audio mods"
    },
    {
      "id": "games-lobby-vibe-setter-22",
      "theme": "games",
      "title": "Lobby Vibe Setter",
      "pitch": "Lobby Vibe Setter composes multiplayer lobby music in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "multiplayer lobby music",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits multiplayer lobby music in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Lobby Vibe Setter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLobby Vibe Setter composes multiplayer lobby music in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Game Design & Interactive Media (multiplayer lobby music).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits multiplayer lobby music in Game Design & Interactive Media because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `multiplayer-lobby-music-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for multiplayer lobby music (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for multiplayer lobby music.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$15B — multiplayer game market",
      "som": "$150M — esports lobby audio"
    },
    {
      "id": "games-factory-hum-architect-23",
      "theme": "games",
      "title": "Factory Hum Architect",
      "pitch": "Factory Hum Architect boots a long-running service inside a Sprite for base building ambience — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "base building ambience",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches base building ambience in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Factory Hum Architect\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFactory Hum Architect boots a long-running service inside a Sprite for base building ambience — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Game Design & Interactive Media (base building ambience).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches base building ambience in Game Design & Interactive Media because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `base-building-ambience-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for base building ambience.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits base building ambience — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for base building ambience.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$3B — strategy game market",
      "som": "$30M — automation game audio"
    },
    {
      "id": "games-defeat-dirge-drafter-24",
      "theme": "games",
      "title": "Defeat Dirge Drafter",
      "pitch": "Defeat Dirge Drafter runs a one-shot shell command inside a Sprite for game over scoring and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "game over scoring",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits game over scoring in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Defeat Dirge Drafter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDefeat Dirge Drafter runs a one-shot shell command inside a Sprite for game over scoring and streams the stdout back so the user watches the work happen live.\nDiscipline: Game Design & Interactive Media (game over scoring).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits game over scoring in Game Design & Interactive Media because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `game-over-scoring-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a game over scoring-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for game over scoring.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$6B — narrative game development",
      "som": "$60M — indie game soundtracks"
    }
  ]
}