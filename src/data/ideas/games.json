{
  "theme": {
    "slug": "games",
    "name": "Game Design & Interactive Media",
    "emoji": "🎮",
    "audience": "game designers, interactive artists, XR creators",
    "market_anchor": "the game industry (~$200B) and >3M indie developers"
  },
  "ideas": [
    {
      "id": "games-dynamic-credits-ledger-0",
      "theme": "games",
      "title": "Dynamic Credits Ledger",
      "pitch": "Dynamic Credits Ledger mints a did:prism for credit tracking so identity travels with the person, not the platform.",
      "concept": "A web app that tracks and verifies game credits, allowing contributors to prove their involvement and receive proper attribution.",
      "subDiscipline": "credit tracking",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits credit tracking in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Dynamic Credits Ledger\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nA web app that tracks and verifies game credits, allowing contributors to prove their involvement and receive proper attribution.\nDiscipline: Game Design & Interactive Media (credit tracking).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits credit tracking in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global video game industry · SAM $15B — narrative and RPG game development · SOM $500M — indie RPG worldbuilding tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global video game industry",
      "sam": "$15B — narrative and RPG game development",
      "som": "$500M — indie RPG worldbuilding tools"
    },
    {
      "id": "games-collaborative-credits-0",
      "theme": "games",
      "title": "Collaborative Credits",
      "pitch": "Collaborative Credits pairs two wallets over DIDComm so game teams can exchange trusted claims on a private channel.",
      "concept": "Team members can share and verify their roles and contributions on a project through a secure DIDComm connection.",
      "subDiscipline": "game teams",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits game teams in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Collaborative Credits\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTeam members can share and verify their roles and contributions on a project through a secure DIDComm connection.\nDiscipline: Game Design & Interactive Media (game teams).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits game teams in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global video game industry · SAM $10B — game QA and playtesting services · SOM $300M — indie user research tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global video game industry",
      "sam": "$10B — game QA and playtesting services",
      "som": "$300M — indie user research tools"
    },
    {
      "id": "games-creator-attribution-hub-0",
      "theme": "games",
      "title": "Creator Attribution Hub",
      "pitch": "Creator Attribution Hub issues a signed verifiable credential for credits management that the holder keeps and reuses anywhere.",
      "concept": "Game developers receive verifiable credentials proving their contributions to projects, ensuring proper attribution in game credits.",
      "subDiscipline": "credits management",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches credits management in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Creator Attribution Hub\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nGame developers receive verifiable credentials proving their contributions to projects, ensuring proper attribution in game credits.\nDiscipline: Game Design & Interactive Media (credits management).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches credits management in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global video game industry · SAM $12B — game audio and narrative production · SOM $400M — procedural NPC dialogue tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global video game industry",
      "sam": "$12B — game audio and narrative production",
      "som": "$400M — procedural NPC dialogue tools"
    },
    {
      "id": "games-game-tester-validation-0",
      "theme": "games",
      "title": "Game Tester Validation",
      "pitch": "Game Tester Validation verifies a credential at the gate for user testing — proof without a phone call to the issuer.",
      "concept": "Gamers prove their identity and experience level to game developers to access exclusive beta testing opportunities.",
      "subDiscipline": "user testing",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for user testing in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Game Tester Validation\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nGamers prove their identity and experience level to game developers to access exclusive beta testing opportunities.\nDiscipline: Game Design & Interactive Media (user testing).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for user testing in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global video game industry · SAM $8B — virtual and mixed reality software · SOM $200M — XR accessibility and comfort plugins\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global video game industry",
      "sam": "$8B — virtual and mixed reality software",
      "som": "$200M — XR accessibility and comfort plugins"
    },
    {
      "id": "games-artistic-ownership-proof-0",
      "theme": "games",
      "title": "Artistic Ownership Proof",
      "pitch": "Artistic Ownership Proof mints a did:prism for interactive art so identity travels with the person, not the platform.",
      "concept": "This app enables artists to register their artwork's provenance, providing proof of ownership to galleries and collectors.",
      "subDiscipline": "interactive art",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits interactive art in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Artistic Ownership Proof\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nThis app enables artists to register their artwork's provenance, providing proof of ownership to galleries and collectors.\nDiscipline: Game Design & Interactive Media (interactive art).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits interactive art in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global video game industry · SAM $20B — casual and mobile game development · SOM $600M — UX and onboarding design tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global video game industry",
      "sam": "$20B — casual and mobile game development",
      "som": "$600M — UX and onboarding design tools"
    },
    {
      "id": "games-artistic-attribution-0",
      "theme": "games",
      "title": "Artistic Attribution",
      "pitch": "Artistic Attribution pairs two wallets over DIDComm so interactive art can exchange trusted claims on a private channel.",
      "concept": "Artists can prove the originality of their work to galleries or clients using verifiable credentials exchanged via DIDComm.",
      "subDiscipline": "interactive art",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits interactive art in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Artistic Attribution\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nArtists can prove the originality of their work to galleries or clients using verifiable credentials exchanged via DIDComm.\nDiscipline: Game Design & Interactive Media (interactive art).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits interactive art in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global video game industry · SAM $5B — game jam and indie showcase events · SOM $150M — pitch preparation software\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global video game industry",
      "sam": "$5B — game jam and indie showcase events",
      "som": "$150M — pitch preparation software"
    },
    {
      "id": "games-playtester-validation-0",
      "theme": "games",
      "title": "Playtester Validation",
      "pitch": "Playtester Validation issues a signed verifiable credential for user testing that the holder keeps and reuses anywhere.",
      "concept": "Playtesters can prove their eligibility and experience through verifiable credentials, allowing developers to select qualified testers for their games.",
      "subDiscipline": "user testing",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches user testing in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Playtester Validation\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPlaytesters can prove their eligibility and experience through verifiable credentials, allowing developers to select qualified testers for their games.\nDiscipline: Game Design & Interactive Media (user testing).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches user testing in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global video game industry · SAM $6B — game accessibility software and consulting · SOM $250M — UI screen-reader integration tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global video game industry",
      "sam": "$6B — game accessibility software and consulting",
      "som": "$250M — UI screen-reader integration tools"
    },
    {
      "id": "games-artistic-collaboration-proof-0",
      "theme": "games",
      "title": "Artistic Collaboration Proof",
      "pitch": "Artistic Collaboration Proof verifies a credential at the gate for collaborative projects — proof without a phone call to the issuer.",
      "concept": "Artists submit proof of their contributions to projects, ensuring proper credit is given in collaborative game development.",
      "subDiscipline": "collaborative projects",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for collaborative projects in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Artistic Collaboration Proof\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nArtists submit proof of their contributions to projects, ensuring proper credit is given in collaborative game development.\nDiscipline: Game Design & Interactive Media (collaborative projects).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for collaborative projects in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global video game industry · SAM $15B — game marketing and community management · SOM $450M — indie creator content tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global video game industry",
      "sam": "$15B — game marketing and community management",
      "som": "$450M — indie creator content tools"
    },
    {
      "id": "games-membership-validator-0",
      "theme": "games",
      "title": "Membership Validator",
      "pitch": "Membership Validator mints a did:prism for game associations so identity travels with the person, not the platform.",
      "concept": "A solution for game developers to prove their membership in professional associations, ensuring access to exclusive resources and events.",
      "subDiscipline": "game associations",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits game associations in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Membership Validator\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nA solution for game developers to prove their membership in professional associations, ensuring access to exclusive resources and events.\nDiscipline: Game Design & Interactive Media (game associations).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits game associations in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global video game industry · SAM $18B — horror and action game development · SOM $550M — level design and pacing software\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global video game industry",
      "sam": "$18B — horror and action game development",
      "som": "$550M — level design and pacing software"
    },
    {
      "id": "games-access-passes-0",
      "theme": "games",
      "title": "Access Passes",
      "pitch": "Access Passes pairs two wallets over DIDComm so event management can exchange trusted claims on a private channel.",
      "concept": "Event organizers can distribute digital access passes to participants, verified through DIDComm connections for exclusive experiences.",
      "subDiscipline": "event management",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits event management in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Access Passes\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nEvent organizers can distribute digital access passes to participants, verified through DIDComm connections for exclusive experiences.\nDiscipline: Game Design & Interactive Media (event management).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits event management in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global video game industry · SAM $10B — game localization and translation services · SOM $300M — automated localization QA tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global video game industry",
      "sam": "$10B — game localization and translation services",
      "som": "$300M — automated localization QA tools"
    },
    {
      "id": "games-licensing-verification-0",
      "theme": "games",
      "title": "Licensing Verification",
      "pitch": "Licensing Verification issues a signed verifiable credential for rights management that the holder keeps and reuses anywhere.",
      "concept": "Game creators can issue credentials that verify licensing rights for assets, ensuring legal compliance when using third-party content.",
      "subDiscipline": "rights management",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches rights management in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Licensing Verification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nGame creators can issue credentials that verify licensing rights for assets, ensuring legal compliance when using third-party content.\nDiscipline: Game Design & Interactive Media (rights management).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches rights management in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global video game industry · SAM $12B — serious games and simulation software · SOM $400M — medical training simulators\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global video game industry",
      "sam": "$12B — serious games and simulation software",
      "som": "$400M — medical training simulators"
    },
    {
      "id": "games-license-validator-0",
      "theme": "games",
      "title": "License Validator",
      "pitch": "License Validator verifies a credential at the gate for indie rights — proof without a phone call to the issuer.",
      "concept": "Indie developers present proof of licensing agreements to publishers ensuring their work is properly attributed and legally protected.",
      "subDiscipline": "indie rights",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for indie rights in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"License Validator\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nIndie developers present proof of licensing agreements to publishers ensuring their work is properly attributed and legally protected.\nDiscipline: Game Design & Interactive Media (indie rights).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for indie rights in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global video game industry · SAM $12B — game audio and narrative production · SOM $350M — audio direction and storyboarding tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global video game industry",
      "sam": "$12B — game audio and narrative production",
      "som": "$350M — audio direction and storyboarding tools"
    },
    {
      "id": "games-age-verification-tool-0",
      "theme": "games",
      "title": "Age Verification Tool",
      "pitch": "Age Verification Tool mints a did:prism for player safety so identity travels with the person, not the platform.",
      "concept": "A simple interface that lets players verify their age for game content eligibility without revealing personal details.",
      "subDiscipline": "player safety",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits player safety in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Age Verification Tool\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nA simple interface that lets players verify their age for game content eligibility without revealing personal details.\nDiscipline: Game Design & Interactive Media (player safety).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits player safety in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global video game industry · SAM $25B — game studio management and operations · SOM $700M — agile and retrospective software\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global video game industry",
      "sam": "$25B — game studio management and operations",
      "som": "$700M — agile and retrospective software"
    },
    {
      "id": "games-game-tester-guild-0",
      "theme": "games",
      "title": "Game Tester Guild",
      "pitch": "Game Tester Guild pairs two wallets over DIDComm so playtesting can exchange trusted claims on a private channel.",
      "concept": "Playtesters can join a guild and verify their membership status to access exclusive beta tests via DIDComm invitations.",
      "subDiscipline": "playtesting",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits playtesting in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Game Tester Guild\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPlaytesters can join a guild and verify their membership status to access exclusive beta tests via DIDComm invitations.\nDiscipline: Game Design & Interactive Media (playtesting).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits playtesting in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global video game industry · SAM $20B — game UI and UX design · SOM $600M — prototyping and usability testing tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global video game industry",
      "sam": "$20B — game UI and UX design",
      "som": "$600M — prototyping and usability testing tools"
    },
    {
      "id": "games-talent-agency-portal-0",
      "theme": "games",
      "title": "Talent Agency Portal",
      "pitch": "Talent Agency Portal issues a signed verifiable credential for representative networking that the holder keeps and reuses anywhere.",
      "concept": "Talent agencies can provide credentials for their clients, proving talent availability and skills to game studios looking for collaborators.",
      "subDiscipline": "representative networking",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches representative networking in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Talent Agency Portal\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTalent agencies can provide credentials for their clients, proving talent availability and skills to game studios looking for collaborators.\nDiscipline: Game Design & Interactive Media (representative networking).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches representative networking in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global video game industry · SAM $8B — game streaming and creator economy · SOM $250M — stream overlay and integration plugins\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global video game industry",
      "sam": "$8B — game streaming and creator economy",
      "som": "$250M — stream overlay and integration plugins"
    },
    {
      "id": "games-membership-access-pass-0",
      "theme": "games",
      "title": "Membership Access Pass",
      "pitch": "Membership Access Pass verifies a credential at the gate for industry guilds — proof without a phone call to the issuer.",
      "concept": "Members of game design guilds prove their status to gain exclusive access to resources and workshops.",
      "subDiscipline": "industry guilds",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for industry guilds in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Membership Access Pass\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nMembers of game design guilds prove their status to gain exclusive access to resources and workshops.\nDiscipline: Game Design & Interactive Media (industry guilds).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for industry guilds in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global video game industry · SAM $15B — 3D animation and motion capture · SOM $450M — virtual production direction tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global video game industry",
      "sam": "$15B — 3D animation and motion capture",
      "som": "$450M — virtual production direction tools"
    },
    {
      "id": "games-content-authenticity-checker-0",
      "theme": "games",
      "title": "Content Authenticity Checker",
      "pitch": "Content Authenticity Checker mints a did:prism for game journalism so identity travels with the person, not the platform.",
      "concept": "An application for journalists to prove the authenticity of game reviews and articles by registering their identity and affiliation.",
      "subDiscipline": "game journalism",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits game journalism in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Content Authenticity Checker\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAn application for journalists to prove the authenticity of game reviews and articles by registering their identity and affiliation.\nDiscipline: Game Design & Interactive Media (game journalism).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits game journalism in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global video game industry · SAM $10B — educational games and edtech · SOM $300M — gamified learning platforms\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global video game industry",
      "sam": "$10B — educational games and edtech",
      "som": "$300M — gamified learning platforms"
    },
    {
      "id": "games-safe-age-verification-0",
      "theme": "games",
      "title": "Safe Age Verification",
      "pitch": "Safe Age Verification pairs two wallets over DIDComm so youth games can exchange trusted claims on a private channel.",
      "concept": "Games can verify players' ages through trusted connections to ensure compliance with content regulations without sharing personal data.",
      "subDiscipline": "youth games",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits youth games in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Safe Age Verification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nGames can verify players' ages through trusted connections to ensure compliance with content regulations without sharing personal data.\nDiscipline: Game Design & Interactive Media (youth games).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits youth games in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global video game industry · SAM $15B — game marketing and community management · SOM $400M — Discord bot and community tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global video game industry",
      "sam": "$15B — game marketing and community management",
      "som": "$400M — Discord bot and community tools"
    },
    {
      "id": "games-safe-space-access-0",
      "theme": "games",
      "title": "Safe Space Access",
      "pitch": "Safe Space Access issues a signed verifiable credential for community safety that the holder keeps and reuses anywhere.",
      "concept": "Participants in game events receive credentials proving their commitment to safe community practices, accessing exclusive spaces at conventions.",
      "subDiscipline": "community safety",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches community safety in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Safe Space Access\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nParticipants in game events receive credentials proving their commitment to safe community practices, accessing exclusive spaces at conventions.\nDiscipline: Game Design & Interactive Media (community safety).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches community safety in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global video game industry · SAM $10B — game QA and playtesting services · SOM $350M — issue tracking and bug management\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global video game industry",
      "sam": "$10B — game QA and playtesting services",
      "som": "$350M — issue tracking and bug management"
    },
    {
      "id": "games-age-verification-gate-0",
      "theme": "games",
      "title": "Age Verification Gate",
      "pitch": "Age Verification Gate verifies a credential at the gate for game ratings — proof without a phone call to the issuer.",
      "concept": "Players verify their age to access age-restricted game content and features directly to the developers.",
      "subDiscipline": "game ratings",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for game ratings in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Age Verification Gate\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPlayers verify their age to access age-restricted game content and features directly to the developers.\nDiscipline: Game Design & Interactive Media (game ratings).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for game ratings in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global video game industry · SAM $12B — interactive fiction and visual novels · SOM $400M — narrative design software\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global video game industry",
      "sam": "$12B — interactive fiction and visual novels",
      "som": "$400M — narrative design software"
    },
    {
      "id": "games-unity-asset-provenance-0",
      "theme": "games",
      "title": "Unity Asset Provenance",
      "pitch": "Unity Asset Provenance mints a did:prism for asset management so identity travels with the person, not the platform.",
      "concept": "This app allows developers to verify the origins of game assets, ensuring they have the right to use them in their projects.",
      "subDiscipline": "asset management",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits asset management in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Unity Asset Provenance\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nThis app allows developers to verify the origins of game assets, ensuring they have the right to use them in their projects.\nDiscipline: Game Design & Interactive Media (asset management).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits asset management in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global video game industry · SAM $8B — virtual and mixed reality software · SOM $250M — spatial audio design tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global video game industry",
      "sam": "$8B — virtual and mixed reality software",
      "som": "$250M — spatial audio design tools"
    },
    {
      "id": "games-content-license-checker-0",
      "theme": "games",
      "title": "Content License Checker",
      "pitch": "Content License Checker pairs two wallets over DIDComm so legal compliance can exchange trusted claims on a private channel.",
      "concept": "Developers can verify content licenses of assets in use, ensuring compliance through DIDComm-based verification with original creators.",
      "subDiscipline": "legal compliance",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits legal compliance in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Content License Checker\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDevelopers can verify content licenses of assets in use, ensuring compliance through DIDComm-based verification with original creators.\nDiscipline: Game Design & Interactive Media (legal compliance).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits legal compliance in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global video game industry · SAM $25B — game studio management and operations · SOM $600M — corporate wellness and ergonomics software\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global video game industry",
      "sam": "$25B — game studio management and operations",
      "som": "$600M — corporate wellness and ergonomics software"
    },
    {
      "id": "games-workshop-certification-0",
      "theme": "games",
      "title": "Workshop Certification",
      "pitch": "Workshop Certification issues a signed verifiable credential for training programs that the holder keeps and reuses anywhere.",
      "concept": "Individuals who complete game design workshops receive verifiable credentials, showcasing their skills to future employers or collaborators.",
      "subDiscipline": "training programs",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches training programs in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Workshop Certification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nIndividuals who complete game design workshops receive verifiable credentials, showcasing their skills to future employers or collaborators.\nDiscipline: Game Design & Interactive Media (training programs).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches training programs in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global video game industry · SAM $30B — live-ops and in-game economies · SOM $800M — game economy balancing tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global video game industry",
      "sam": "$30B — live-ops and in-game economies",
      "som": "$800M — game economy balancing tools"
    },
    {
      "id": "games-creative-portfolio-showcase-0",
      "theme": "games",
      "title": "Creative Portfolio Showcase",
      "pitch": "Creative Portfolio Showcase verifies a credential at the gate for portfolio curation — proof without a phone call to the issuer.",
      "concept": "Artists present verified credentials and past works to potential collaborators seeking authentic talent.",
      "subDiscipline": "portfolio curation",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for portfolio curation in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Creative Portfolio Showcase\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nArtists present verified credentials and past works to potential collaborators seeking authentic talent.\nDiscipline: Game Design & Interactive Media (portfolio curation).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for portfolio curation in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global video game industry · SAM $5B — game publishing and investment · SOM $150M — pitch preparation software\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global video game industry",
      "sam": "$5B — game publishing and investment",
      "som": "$150M — pitch preparation software"
    },
    {
      "id": "games-achievement-ownership-registry-0",
      "theme": "games",
      "title": "Achievement Ownership Registry",
      "pitch": "Achievement Ownership Registry mints a did:prism for leaderboards so identity travels with the person, not the platform.",
      "concept": "A platform where gamers can register their achievements and prove them to others, enhancing their credibility within the gaming community.",
      "subDiscipline": "leaderboards",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits leaderboards in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Achievement Ownership Registry\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nA platform where gamers can register their achievements and prove them to others, enhancing their credibility within the gaming community.\nDiscipline: Game Design & Interactive Media (leaderboards).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits leaderboards in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global video game industry · SAM $10B — UGC and modding ecosystems · SOM $300M — developer documentation platforms\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global video game industry",
      "sam": "$10B — UGC and modding ecosystems",
      "som": "$300M — developer documentation platforms"
    },
    {
      "id": "games-exclusive-backstage-0",
      "theme": "games",
      "title": "Exclusive Backstage",
      "pitch": "Exclusive Backstage pairs two wallets over DIDComm so live events can exchange trusted claims on a private channel.",
      "concept": "Creators can invite fans to exclusive backstage experiences, confirming attendance through secure DIDComm connections.",
      "subDiscipline": "live events",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits live events in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Exclusive Backstage\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCreators can invite fans to exclusive backstage experiences, confirming attendance through secure DIDComm connections.\nDiscipline: Game Design & Interactive Media (live events).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits live events in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $8B — global game development software market · SAM $1.5B — narrative design and dialogue tools · SOM $40M — AI voice rehearsal assistants for indie studios\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$8B — global game development software market",
      "sam": "$1.5B — narrative design and dialogue tools",
      "som": "$40M — AI voice rehearsal assistants for indie studios"
    },
    {
      "id": "games-game-jam-participation-0",
      "theme": "games",
      "title": "Game Jam Participation",
      "pitch": "Game Jam Participation issues a signed verifiable credential for event participation that the holder keeps and reuses anywhere.",
      "concept": "Participants in game jams receive credentials verifying their involvement, allowing them to demonstrate experience and collaboration to potential employers.",
      "subDiscipline": "event participation",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches event participation in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Game Jam Participation\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nParticipants in game jams receive credentials verifying their involvement, allowing them to demonstrate experience and collaboration to potential employers.\nDiscipline: Game Design & Interactive Media (event participation).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches event participation in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $10B — game engines and level editors market · SAM $2B — indie level design and prototyping tools · SOM $50M — AI spatial drafting companions\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$10B — game engines and level editors market",
      "sam": "$2B — indie level design and prototyping tools",
      "som": "$50M — AI spatial drafting companions"
    },
    {
      "id": "games-feedback-credibility-score-0",
      "theme": "games",
      "title": "Feedback Credibility Score",
      "pitch": "Feedback Credibility Score verifies a credential at the gate for game reviews — proof without a phone call to the issuer.",
      "concept": "Reviewers present verified identities to game developers, ensuring feedback comes from credible sources which enhances trust.",
      "subDiscipline": "game reviews",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for game reviews in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Feedback Credibility Score\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nReviewers present verified identities to game developers, ensuring feedback comes from credible sources which enhances trust.\nDiscipline: Game Design & Interactive Media (game reviews).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for game reviews in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $15B — project management and productivity software · SAM $4B — game development agile and scoping tools · SOM $80M — AI mental health and sprint coaching for solo devs\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$15B — project management and productivity software",
      "sam": "$4B — game development agile and scoping tools",
      "som": "$80M — AI mental health and sprint coaching for solo devs"
    },
    {
      "id": "games-game-jam-credentialer-0",
      "theme": "games",
      "title": "Game Jam Credentialer",
      "pitch": "Game Jam Credentialer mints a did:prism for event management so identity travels with the person, not the platform.",
      "concept": "Developers can register their participation in game jams, providing verifiable proof for future collaborations and career opportunities.",
      "subDiscipline": "event management",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits event management in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Game Jam Credentialer\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDevelopers can register their participation in game jams, providing verifiable proof for future collaborations and career opportunities.\nDiscipline: Game Design & Interactive Media (event management).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits event management in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $8B — global game development software market · SAM $1.2B — worldbuilding and lore management platforms · SOM $35M — AI lore consistency checkers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$8B — global game development software market",
      "sam": "$1.2B — worldbuilding and lore management platforms",
      "som": "$35M — AI lore consistency checkers"
    },
    {
      "id": "games-collaborative-projects-hub-0",
      "theme": "games",
      "title": "Collaborative Projects Hub",
      "pitch": "Collaborative Projects Hub pairs two wallets over DIDComm so team collaboration can exchange trusted claims on a private channel.",
      "concept": "Game developers can form project teams and verify each member's roles using DIDComm for secure identity exchange.",
      "subDiscipline": "team collaboration",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits team collaboration in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Collaborative Projects Hub\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nGame developers can form project teams and verify each member's roles using DIDComm for secure identity exchange.\nDiscipline: Game Design & Interactive Media (team collaboration).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits team collaboration in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $12B — game audio and XR middleware market · SAM $3B — spatial audio plugins and XR tools · SOM $60M — AI voice-directed audio prototyping\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$12B — game audio and XR middleware market",
      "sam": "$3B — spatial audio plugins and XR tools",
      "som": "$60M — AI voice-directed audio prototyping"
    },
    {
      "id": "games-artistic-collaboration-proof-1",
      "theme": "games",
      "title": "Artistic Collaboration Proof",
      "pitch": "Artistic Collaboration Proof issues a signed verifiable credential for cross-disciplinary projects that the holder keeps and reuses anywhere.",
      "concept": "Artists involved in game projects can prove their contributions via verifiable credentials, promoting recognition across different creative fields.",
      "subDiscipline": "cross-disciplinary projects",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches cross-disciplinary projects in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Artistic Collaboration Proof\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nArtists involved in game projects can prove their contributions via verifiable credentials, promoting recognition across different creative fields.\nDiscipline: Game Design & Interactive Media (cross-disciplinary projects).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches cross-disciplinary projects in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — tabletop RPG and digital companion market · SAM $1B — digital GM tools and encounter builders · SOM $25M — AI campaign prep assistants\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — tabletop RPG and digital companion market",
      "sam": "$1B — digital GM tools and encounter builders",
      "som": "$25M — AI campaign prep assistants"
    },
    {
      "id": "games-beta-access-validation-0",
      "theme": "games",
      "title": "Beta Access Validation",
      "pitch": "Beta Access Validation verifies a credential at the gate for early access — proof without a phone call to the issuer.",
      "concept": "Players prove their identity and connection to a game studio to unlock early access to new titles.",
      "subDiscipline": "early access",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for early access in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Beta Access Validation\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPlayers prove their identity and connection to a game studio to unlock early access to new titles.\nDiscipline: Game Design & Interactive Media (early access).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for early access in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $10B — game engines and analytics platforms · SAM $2.5B — game balancing and economy design tools · SOM $45M — AI meta-balancing debaters\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$10B — game engines and analytics platforms",
      "sam": "$2.5B — game balancing and economy design tools",
      "som": "$45M — AI meta-balancing debaters"
    },
    {
      "id": "games-exclusivity-pass-system-0",
      "theme": "games",
      "title": "Exclusivity Pass System",
      "pitch": "Exclusivity Pass System mints a did:prism for events access so identity travels with the person, not the platform.",
      "concept": "Participants can prove their eligibility for exclusive game events by registering unique passes linked to their identities.",
      "subDiscipline": "events access",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits events access in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Exclusivity Pass System\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nParticipants can prove their eligibility for exclusive game events by registering unique passes linked to their identities.\nDiscipline: Game Design & Interactive Media (events access).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits events access in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $9B — game testing and QA software market · SAM $2.5B — automated UX and accessibility testing · SOM $45M — AI accessibility simulators for game UI\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$9B — game testing and QA software market",
      "sam": "$2.5B — automated UX and accessibility testing",
      "som": "$45M — AI accessibility simulators for game UI"
    },
    {
      "id": "games-skill-verification-hub-0",
      "theme": "games",
      "title": "Skill Verification Hub",
      "pitch": "Skill Verification Hub pairs two wallets over DIDComm so professional networking can exchange trusted claims on a private channel.",
      "concept": "Creators can showcase verified skills and experiences to potential collaborators or employers through DIDComm connections.",
      "subDiscipline": "professional networking",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits professional networking in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Skill Verification Hub\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCreators can showcase verified skills and experiences to potential collaborators or employers through DIDComm connections.\nDiscipline: Game Design & Interactive Media (professional networking).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits professional networking in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $8B — global game development software market · SAM $1.5B — interactive fiction and branching narrative tools · SOM $40M — AI narrative tree pruners\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$8B — global game development software market",
      "sam": "$1.5B — interactive fiction and branching narrative tools",
      "som": "$40M — AI narrative tree pruners"
    },
    {
      "id": "games-mentorship-verification-0",
      "theme": "games",
      "title": "Mentorship Verification",
      "pitch": "Mentorship Verification issues a signed verifiable credential for professional development that the holder keeps and reuses anywhere.",
      "concept": "Mentors provide credentials that verify their authority and experience, connecting them with mentees in the game design community.",
      "subDiscipline": "professional development",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches professional development in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Mentorship Verification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nMentors provide credentials that verify their authority and experience, connecting them with mentees in the game design community.\nDiscipline: Game Design & Interactive Media (professional development).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches professional development in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $10B — game engines and level editors market · SAM $2B — level design and pacing analytics tools · SOM $50M — AI pacing critique companions\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$10B — game engines and level editors market",
      "sam": "$2B — level design and pacing analytics tools",
      "som": "$50M — AI pacing critique companions"
    },
    {
      "id": "games-artwork-authenticity-check-0",
      "theme": "games",
      "title": "Artwork Authenticity Check",
      "pitch": "Artwork Authenticity Check verifies a credential at the gate for art provenance — proof without a phone call to the issuer.",
      "concept": "Artists verify the authenticity of their digital artwork, ensuring buyers know they are purchasing original creations.",
      "subDiscipline": "art provenance",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for art provenance in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Artwork Authenticity Check\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nArtists verify the authenticity of their digital artwork, ensuring buyers know they are purchasing original creations.\nDiscipline: Game Design & Interactive Media (art provenance).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for art provenance in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $9B — game testing and QA software market · SAM $2B — user onboarding and tutorial testing tools · SOM $35M — AI UX rookie simulators\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$9B — game testing and QA software market",
      "sam": "$2B — user onboarding and tutorial testing tools",
      "som": "$35M — AI UX rookie simulators"
    },
    {
      "id": "games-rights-management-hub-0",
      "theme": "games",
      "title": "Rights Management Hub",
      "pitch": "Rights Management Hub mints a did:prism for content rights so identity travels with the person, not the platform.",
      "concept": "A tool that helps creators register and manage their game content rights, proving ownership to publishers and platforms.",
      "subDiscipline": "content rights",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits content rights in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Rights Management Hub\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nA tool that helps creators register and manage their game content rights, proving ownership to publishers and platforms.\nDiscipline: Game Design & Interactive Media (content rights).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits content rights in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $12B — game audio and XR middleware market · SAM $2.5B — foley and sound effect libraries · SOM $50M — AI foley direction assistants\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$12B — game audio and XR middleware market",
      "sam": "$2.5B — foley and sound effect libraries",
      "som": "$50M — AI foley direction assistants"
    },
    {
      "id": "games-merit-badge-platform-0",
      "theme": "games",
      "title": "Merit Badge Platform",
      "pitch": "Merit Badge Platform pairs two wallets over DIDComm so educational games can exchange trusted claims on a private channel.",
      "concept": "Educators can issue verifiable badges to learners for completed skills or achievements through secure DIDComm interactions.",
      "subDiscipline": "educational games",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits educational games in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Merit Badge Platform\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nEducators can issue verifiable badges to learners for completed skills or achievements through secure DIDComm interactions.\nDiscipline: Game Design & Interactive Media (educational games).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits educational games in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $6B — esports and game marketing software · SAM $1.5B — trailer production and narrative tools · SOM $30M — AI shoutcast rehearsal coaches\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$6B — esports and game marketing software",
      "sam": "$1.5B — trailer production and narrative tools",
      "som": "$30M — AI shoutcast rehearsal coaches"
    },
    {
      "id": "games-age-restricted-access-0",
      "theme": "games",
      "title": "Age-Restricted Access",
      "pitch": "Age-Restricted Access issues a signed verifiable credential for content regulation that the holder keeps and reuses anywhere.",
      "concept": "Players can receive age-verifiable credentials that grant them access to age-restricted content in games and events.",
      "subDiscipline": "content regulation",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches content regulation in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Age-Restricted Access\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPlayers can receive age-verifiable credentials that grant them access to age-restricted content in games and events.\nDiscipline: Game Design & Interactive Media (content regulation).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches content regulation in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $12B — game audio and XR middleware market · SAM $3B — VR locomotion and comfort plugins · SOM $60M — AI VR safety predictors\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$12B — game audio and XR middleware market",
      "sam": "$3B — VR locomotion and comfort plugins",
      "som": "$60M — AI VR safety predictors"
    },
    {
      "id": "games-event-participation-proof-0",
      "theme": "games",
      "title": "Event Participation Proof",
      "pitch": "Event Participation Proof verifies a credential at the gate for conferences — proof without a phone call to the issuer.",
      "concept": "Participants verify attendance at industry events to gain access to exclusive post-event resources and networking opportunities.",
      "subDiscipline": "conferences",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for conferences in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Event Participation Proof\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nParticipants verify attendance at industry events to gain access to exclusive post-event resources and networking opportunities.\nDiscipline: Game Design & Interactive Media (conferences).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for conferences in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $8B — global game development software market · SAM $1.5B — narrative design and dialogue tools · SOM $40M — AI NPC persona actors\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$8B — global game development software market",
      "sam": "$1.5B — narrative design and dialogue tools",
      "som": "$40M — AI NPC persona actors"
    },
    {
      "id": "games-user-consent-ledger-0",
      "theme": "games",
      "title": "User Consent Ledger",
      "pitch": "User Consent Ledger mints a did:prism for data privacy so identity travels with the person, not the platform.",
      "concept": "This app allows players to manage and prove their data consent for personalized gaming experiences while maintaining anonymity.",
      "subDiscipline": "data privacy",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits data privacy in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"User Consent Ledger\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nThis app allows players to manage and prove their data consent for personalized gaming experiences while maintaining anonymity.\nDiscipline: Game Design & Interactive Media (data privacy).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits data privacy in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $6B — esports and game marketing software · SAM $1.2B — indie game storefront optimization tools · SOM $25M — AI pitch rehearsal companions\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$6B — esports and game marketing software",
      "sam": "$1.2B — indie game storefront optimization tools",
      "som": "$25M — AI pitch rehearsal companions"
    },
    {
      "id": "games-union-membership-checker-0",
      "theme": "games",
      "title": "Union Membership Checker",
      "pitch": "Union Membership Checker pairs two wallets over DIDComm so industry representation can exchange trusted claims on a private channel.",
      "concept": "Game workers can prove their union membership status to access industry resources through DIDComm connections.",
      "subDiscipline": "industry representation",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits industry representation in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Union Membership Checker\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nGame workers can prove their union membership status to access industry resources through DIDComm connections.\nDiscipline: Game Design & Interactive Media (industry representation).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits industry representation in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $10B — game engines and animation middleware · SAM $2.5B — combat design and choreography tools · SOM $45M — AI combat rhythm callers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$10B — game engines and animation middleware",
      "sam": "$2.5B — combat design and choreography tools",
      "som": "$45M — AI combat rhythm callers"
    },
    {
      "id": "games-guild-membership-status-0",
      "theme": "games",
      "title": "Guild Membership Status",
      "pitch": "Guild Membership Status issues a signed verifiable credential for community building that the holder keeps and reuses anywhere.",
      "concept": "Game developers can verify membership in guilds or union affiliations through credentials, facilitating access to exclusive resources and opportunities.",
      "subDiscipline": "community building",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches community building in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Guild Membership Status\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nGame developers can verify membership in guilds or union affiliations through credentials, facilitating access to exclusive resources and opportunities.\nDiscipline: Game Design & Interactive Media (community building).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches community building in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $8B — global game development software market · SAM $2B — game localization and translation QA · SOM $50M — AI localization tone checkers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$8B — global game development software market",
      "sam": "$2B — game localization and translation QA",
      "som": "$50M — AI localization tone checkers"
    },
    {
      "id": "games-behind-the-scenes-access-0",
      "theme": "games",
      "title": "Behind-the-Scenes Access",
      "pitch": "Behind-the-Scenes Access verifies a credential at the gate for game production — proof without a phone call to the issuer.",
      "concept": "Creators prove their identity to access exclusive behind-the-scenes content of ongoing game productions.",
      "subDiscipline": "game production",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for game production in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Behind-the-Scenes Access\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCreators prove their identity to access exclusive behind-the-scenes content of ongoing game productions.\nDiscipline: Game Design & Interactive Media (game production).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for game production in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $10B — game engines and level editors market · SAM $1.5B — procedural generation and roguelike tools · SOM $35M — AI dungeon flow narrators\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$10B — game engines and level editors market",
      "sam": "$1.5B — procedural generation and roguelike tools",
      "som": "$35M — AI dungeon flow narrators"
    },
    {
      "id": "games-game-design-portfolio-proof-0",
      "theme": "games",
      "title": "Game Design Portfolio Proof",
      "pitch": "Game Design Portfolio Proof mints a did:prism for professional identity so identity travels with the person, not the platform.",
      "concept": "Designers can use this app to register their portfolios, allowing them to prove their work to potential employers or collaborators.",
      "subDiscipline": "professional identity",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits professional identity in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Game Design Portfolio Proof\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDesigners can use this app to register their portfolios, allowing them to prove their work to potential employers or collaborators.\nDiscipline: Game Design & Interactive Media (professional identity).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits professional identity in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $9B — game testing and QA software market · SAM $2B — player demographic and difficulty testing · SOM $40M — AI player empathy simulators\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$9B — game testing and QA software market",
      "sam": "$2B — player demographic and difficulty testing",
      "som": "$40M — AI player empathy simulators"
    },
    {
      "id": "games-digital-consent-forms-0",
      "theme": "games",
      "title": "Digital Consent Forms",
      "pitch": "Digital Consent Forms pairs two wallets over DIDComm so consent management can exchange trusted claims on a private channel.",
      "concept": "Creators can secure consent from collaborators for project usage through verifiable digital forms exchanged via DIDComm.",
      "subDiscipline": "consent management",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits consent management in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Digital Consent Forms\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCreators can secure consent from collaborators for project usage through verifiable digital forms exchanged via DIDComm.\nDiscipline: Game Design & Interactive Media (consent management).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits consent management in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — esports and streaming companion market · SAM $1B — speedrunning and routing tools · SOM $20M — AI speedrun route callers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — esports and streaming companion market",
      "sam": "$1B — speedrunning and routing tools",
      "som": "$20M — AI speedrun route callers"
    },
    {
      "id": "games-collaborator-endorsements-0",
      "theme": "games",
      "title": "Collaborator Endorsements",
      "pitch": "Collaborator Endorsements issues a signed verifiable credential for networking that the holder keeps and reuses anywhere.",
      "concept": "Collaborators in game development can issue and receive credentials that endorse their teamwork and skills, enhancing visibility in the industry.",
      "subDiscipline": "networking",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches networking in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Collaborator Endorsements\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCollaborators in game development can issue and receive credentials that endorse their teamwork and skills, enhancing visibility in the industry.\nDiscipline: Game Design & Interactive Media (networking).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches networking in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $8B — global game development software market · SAM $1B — interactive fiction and text adventure engines · SOM $25M — AI prose pacing directors\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$8B — global game development software market",
      "sam": "$1B — interactive fiction and text adventure engines",
      "som": "$25M — AI prose pacing directors"
    },
    {
      "id": "games-creator-rights-tracker-0",
      "theme": "games",
      "title": "Creator Rights Tracker",
      "pitch": "Creator Rights Tracker verifies a credential at the gate for royalties — proof without a phone call to the issuer.",
      "concept": "Game creators present proof of contributions to ensure fair royalties distribution based on verified work done.",
      "subDiscipline": "royalties",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for royalties in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Creator Rights Tracker\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nGame creators present proof of contributions to ensure fair royalties distribution based on verified work done.\nDiscipline: Game Design & Interactive Media (royalties).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for royalties in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $9B — game testing and QA software market · SAM $2.5B — multiplayer moderation and safety tools · SOM $50M — AI toxicity simulation referees\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$9B — game testing and QA software market",
      "sam": "$2.5B — multiplayer moderation and safety tools",
      "som": "$50M — AI toxicity simulation referees"
    },
    {
      "id": "games-prototype-validator-0",
      "theme": "games",
      "title": "Prototype Validator",
      "pitch": "Prototype Validator mints a did:prism for testing phases so identity travels with the person, not the platform.",
      "concept": "A platform for designers to submit prototypes and receive verified feedback from testers while maintaining their intellectual property rights.",
      "subDiscipline": "testing phases",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits testing phases in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Prototype Validator\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nA platform for designers to submit prototypes and receive verified feedback from testers while maintaining their intellectual property rights.\nDiscipline: Game Design & Interactive Media (testing phases).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits testing phases in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $15B — project management and productivity software · SAM $3B — game jam and rapid prototyping tools · SOM $60M — AI scope management cops\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$15B — project management and productivity software",
      "sam": "$3B — game jam and rapid prototyping tools",
      "som": "$60M — AI scope management cops"
    },
    {
      "id": "games-artwork-provenance-0",
      "theme": "games",
      "title": "Artwork Provenance",
      "pitch": "Artwork Provenance pairs two wallets over DIDComm so art archives can exchange trusted claims on a private channel.",
      "concept": "Artists can prove the history of their works to buyers and galleries using DIDComm to exchange provenance credentials.",
      "subDiscipline": "art archives",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits art archives in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Artwork Provenance\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nArtists can prove the history of their works to buyers and galleries using DIDComm to exchange provenance credentials.\nDiscipline: Game Design & Interactive Media (art archives).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits art archives in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $15B — project management and productivity software · SAM $4B — game development analytics and post-mortem tools · SOM $80M — AI launch autopsy interviewers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$15B — project management and productivity software",
      "sam": "$4B — game development analytics and post-mortem tools",
      "som": "$80M — AI launch autopsy interviewers"
    },
    {
      "id": "games-game-asset-provenance-0",
      "theme": "games",
      "title": "Game Asset Provenance",
      "pitch": "Game Asset Provenance issues a signed verifiable credential for asset authenticity that the holder keeps and reuses anywhere.",
      "concept": "Creators receive credentials that trace the provenance of their game assets, ensuring authenticity and preventing copyright issues.",
      "subDiscipline": "asset authenticity",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches asset authenticity in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Game Asset Provenance\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCreators receive credentials that trace the provenance of their game assets, ensuring authenticity and preventing copyright issues.\nDiscipline: Game Design & Interactive Media (asset authenticity).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches asset authenticity in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $8B — global game UX research · SAM $2B — indie playtesting tools · SOM $50M — think-aloud protocol software\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$8B — global game UX research",
      "sam": "$2B — indie playtesting tools",
      "som": "$50M — think-aloud protocol software"
    },
    {
      "id": "games-interactive-workshop-pass-0",
      "theme": "games",
      "title": "Interactive Workshop Pass",
      "pitch": "Interactive Workshop Pass verifies a credential at the gate for skills training — proof without a phone call to the issuer.",
      "concept": "Participants prove their enrollment in workshops to receive access to interactive training sessions with industry experts.",
      "subDiscipline": "skills training",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for skills training in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Interactive Workshop Pass\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nParticipants prove their enrollment in workshops to receive access to interactive training sessions with industry experts.\nDiscipline: Game Design & Interactive Media (skills training).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for skills training in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $15B — tabletop gaming market · SAM $3B — digital TTRPG companions · SOM $100M — session transcription apps\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$15B — tabletop gaming market",
      "sam": "$3B — digital TTRPG companions",
      "som": "$100M — session transcription apps"
    },
    {
      "id": "games-collaborative-credits-system-0",
      "theme": "games",
      "title": "Collaborative Credits System",
      "pitch": "Collaborative Credits System mints a did:prism for team projects so identity travels with the person, not the platform.",
      "concept": "A tool for game teams to verify and track contributions, ensuring every member's work is acknowledged and credited correctly.",
      "subDiscipline": "team projects",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits team projects in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Collaborative Credits System\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nA tool for game teams to verify and track contributions, ensuring every member's work is acknowledged and credited correctly.\nDiscipline: Game Design & Interactive Media (team projects).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits team projects in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $4B — esports broadcasting software · SAM $800M — speedrunning community tools · SOM $20M — VOD editing assistants\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$4B — esports broadcasting software",
      "sam": "$800M — speedrunning community tools",
      "som": "$20M — VOD editing assistants"
    },
    {
      "id": "games-interactive-portfolio-share-0",
      "theme": "games",
      "title": "Interactive Portfolio Share",
      "pitch": "Interactive Portfolio Share pairs two wallets over DIDComm so self-promotion can exchange trusted claims on a private channel.",
      "concept": "Creators can share and verify their portfolios with potential clients through secure invitations and connections via DIDComm.",
      "subDiscipline": "self-promotion",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits self-promotion in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Interactive Portfolio Share\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCreators can share and verify their portfolios with potential clients through secure invitations and connections via DIDComm.\nDiscipline: Game Design & Interactive Media (self-promotion).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits self-promotion in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $12B — creator economy software · SAM $1B — indie dev marketing tools · SOM $40M — devlog generation platforms\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$12B — creator economy software",
      "sam": "$1B — indie dev marketing tools",
      "som": "$40M — devlog generation platforms"
    },
    {
      "id": "games-content-creator-recognition-0",
      "theme": "games",
      "title": "Content Creator Recognition",
      "pitch": "Content Creator Recognition issues a signed verifiable credential for influencer partnerships that the holder keeps and reuses anywhere.",
      "concept": "Content creators receive credentials verifying their influence and reach, which they can showcase to game companies for partnerships.",
      "subDiscipline": "influencer partnerships",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches influencer partnerships in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Content Creator Recognition\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nContent creators receive credentials verifying their influence and reach, which they can showcase to game companies for partnerships.\nDiscipline: Game Design & Interactive Media (influencer partnerships).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches influencer partnerships in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $25B — XR development software · SAM $5B — spatial UI prototyping · SOM $150M — voice interaction datasets\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$25B — XR development software",
      "sam": "$5B — spatial UI prototyping",
      "som": "$150M — voice interaction datasets"
    },
    {
      "id": "games-content-creator-verification-0",
      "theme": "games",
      "title": "Content Creator Verification",
      "pitch": "Content Creator Verification verifies a credential at the gate for influencer partnerships — proof without a phone call to the issuer.",
      "concept": "Content creators prove their verified status to partner with game studios for marketing campaigns.",
      "subDiscipline": "influencer partnerships",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for influencer partnerships in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Content Creator Verification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nContent creators prove their verified status to partner with game studios for marketing campaigns.\nDiscipline: Game Design & Interactive Media (influencer partnerships).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for influencer partnerships in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $10B — game narrative tools · SAM $2B — indie dialogue editors · SOM $60M — voice-to-text game engines\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$10B — game narrative tools",
      "sam": "$2B — indie dialogue editors",
      "som": "$60M — voice-to-text game engines"
    },
    {
      "id": "games-backstage-access-control-0",
      "theme": "games",
      "title": "Backstage Access Control",
      "pitch": "Backstage Access Control mints a did:prism for event access so identity travels with the person, not the platform.",
      "concept": "Users can register for backstage access at game conventions by proving their industry affiliation through a secure identifier.",
      "subDiscipline": "event access",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits event access in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Backstage Access Control\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nUsers can register for backstage access at game conventions by proving their industry affiliation through a secure identifier.\nDiscipline: Game Design & Interactive Media (event access).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits event access in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $6B — 3D level design software · SAM $1.5B — environment art tools · SOM $30M — spatial pacing analytics\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$6B — 3D level design software",
      "sam": "$1.5B — environment art tools",
      "som": "$30M — spatial pacing analytics"
    },
    {
      "id": "games-live-performance-roster-0",
      "theme": "games",
      "title": "Live Performance Roster",
      "pitch": "Live Performance Roster pairs two wallets over DIDComm so event planning can exchange trusted claims on a private channel.",
      "concept": "Performers can verify their availability and participation status for events using DIDComm connections for scheduling.",
      "subDiscipline": "event planning",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits event planning in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Live Performance Roster\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPerformers can verify their availability and participation status for events using DIDComm connections for scheduling.\nDiscipline: Game Design & Interactive Media (event planning).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits event planning in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $3B — game audio middleware · SAM $800M — indie sound design tools · SOM $25M — foley tagging software\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$3B — game audio middleware",
      "sam": "$800M — indie sound design tools",
      "som": "$25M — foley tagging software"
    },
    {
      "id": "games-feedback-contributor-credential-0",
      "theme": "games",
      "title": "Feedback Contributor Credential",
      "pitch": "Feedback Contributor Credential issues a signed verifiable credential for audience interaction that the holder keeps and reuses anywhere.",
      "concept": "Gamers providing valuable feedback can receive credentials acknowledging their contributions, boosting their credibility in the gaming community.",
      "subDiscipline": "audience interaction",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches audience interaction in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Feedback Contributor Credential\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nGamers providing valuable feedback can receive credentials acknowledging their contributions, boosting their credibility in the gaming community.\nDiscipline: Game Design & Interactive Media (audience interaction).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches audience interaction in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — project management software · SAM $500M — game jam coordination · SOM $15M — hackathon scoping tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — project management software",
      "sam": "$500M — game jam coordination",
      "som": "$15M — hackathon scoping tools"
    },
    {
      "id": "games-community-moderator-verification-0",
      "theme": "games",
      "title": "Community Moderator Verification",
      "pitch": "Community Moderator Verification verifies a credential at the gate for online forums — proof without a phone call to the issuer.",
      "concept": "Moderators prove their role within gaming communities, ensuring trusted management of online discussions and content.",
      "subDiscipline": "online forums",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for online forums in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Community Moderator Verification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nModerators prove their role within gaming communities, ensuring trusted management of online discussions and content.\nDiscipline: Game Design & Interactive Media (online forums).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for online forums in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $8B — creative writing software · SAM $1B — game lore wikis · SOM $35M — worldbuilding assistants\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$8B — creative writing software",
      "sam": "$1B — game lore wikis",
      "som": "$35M — worldbuilding assistants"
    },
    {
      "id": "games-edition-authentication-service-0",
      "theme": "games",
      "title": "Edition Authentication Service",
      "pitch": "Edition Authentication Service mints a did:prism for collectibles so identity travels with the person, not the platform.",
      "concept": "Creators can register their limited game editions, providing proof of authenticity to collectors and preventing counterfeits.",
      "subDiscipline": "collectibles",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits collectibles in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Edition Authentication Service\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCreators can register their limited game editions, providing proof of authenticity to collectors and preventing counterfeits.\nDiscipline: Game Design & Interactive Media (collectibles).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits collectibles in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $7B — game analytics platforms · SAM $1.2B — tutorial optimization tools · SOM $40M — onboarding UX software\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$7B — game analytics platforms",
      "sam": "$1.2B — tutorial optimization tools",
      "som": "$40M — onboarding UX software"
    },
    {
      "id": "games-digital-signature-verification-0",
      "theme": "games",
      "title": "Digital Signature Verification",
      "pitch": "Digital Signature Verification pairs two wallets over DIDComm so authenticity checks can exchange trusted claims on a private channel.",
      "concept": "Composers can authenticate their compositions through verifiable digital signatures exchanged via DIDComm.",
      "subDiscipline": "authenticity checks",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits authenticity checks in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Digital Signature Verification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nComposers can authenticate their compositions through verifiable digital signatures exchanged via DIDComm.\nDiscipline: Game Design & Interactive Media (authenticity checks).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits authenticity checks in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $4B — interactive fiction engines · SAM $800M — narrative game tools · SOM $20M — branching dialogue mappers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$4B — interactive fiction engines",
      "sam": "$800M — narrative game tools",
      "som": "$20M — branching dialogue mappers"
    },
    {
      "id": "games-interactive-exhibit-access-0",
      "theme": "games",
      "title": "Interactive Exhibit Access",
      "pitch": "Interactive Exhibit Access issues a signed verifiable credential for exhibition management that the holder keeps and reuses anywhere.",
      "concept": "Visitors to interactive media exhibits can receive credentials that prove their access and participation in exclusive events or workshops.",
      "subDiscipline": "exhibition management",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches exhibition management in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Interactive Exhibit Access\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nVisitors to interactive media exhibits can receive credentials that prove their access and participation in exclusive events or workshops.\nDiscipline: Game Design & Interactive Media (exhibition management).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches exhibition management in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $12B — board game publishing · SAM $2B — tabletop design software · SOM $50M — playtesting feedback tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$12B — board game publishing",
      "sam": "$2B — tabletop design software",
      "som": "$50M — playtesting feedback tools"
    },
    {
      "id": "games-playable-demo-access-0",
      "theme": "games",
      "title": "Playable Demo Access",
      "pitch": "Playable Demo Access verifies a credential at the gate for public testing — proof without a phone call to the issuer.",
      "concept": "Players verify eligibility and interest to access limited playable demos of upcoming games before public release.",
      "subDiscipline": "public testing",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for public testing in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Playable Demo Access\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPlayers verify eligibility and interest to access limited playable demos of upcoming games before public release.\nDiscipline: Game Design & Interactive Media (public testing).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for public testing in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $20B — serious games market · SAM $4B — corporate training sims · SOM $100M — simulation debrief software\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$20B — serious games market",
      "sam": "$4B — corporate training sims",
      "som": "$100M — simulation debrief software"
    },
    {
      "id": "games-mentorship-validation-app-0",
      "theme": "games",
      "title": "Mentorship Validation App",
      "pitch": "Mentorship Validation App mints a did:prism for career development so identity travels with the person, not the platform.",
      "concept": "Aspiring game developers can prove their mentorship status within the industry, enhancing their credentials for job applications.",
      "subDiscipline": "career development",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits career development in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Mentorship Validation App\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAspiring game developers can prove their mentorship status within the industry, enhancing their credentials for job applications.\nDiscipline: Game Design & Interactive Media (career development).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits career development in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $9B — game engine plugins · SAM $1.5B — combat design tools · SOM $30M — game feel analytics\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$9B — game engine plugins",
      "sam": "$1.5B — combat design tools",
      "som": "$30M — game feel analytics"
    },
    {
      "id": "games-membership-rewards-0",
      "theme": "games",
      "title": "Membership Rewards",
      "pitch": "Membership Rewards pairs two wallets over DIDComm so fan engagement can exchange trusted claims on a private channel.",
      "concept": "Fans can verify their membership status to receive exclusive rewards through DIDComm invitations from creators.",
      "subDiscipline": "fan engagement",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits fan engagement in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Membership Rewards\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFans can verify their membership status to receive exclusive rewards through DIDComm invitations from creators.\nDiscipline: Game Design & Interactive Media (fan engagement).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits fan engagement in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $6B — digital wellness apps · SAM $1B — VR meditation software · SOM $25M — biofeedback XR tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$6B — digital wellness apps",
      "sam": "$1B — VR meditation software",
      "som": "$25M — biofeedback XR tools"
    },
    {
      "id": "games-exclusive-beta-access-0",
      "theme": "games",
      "title": "Exclusive Beta Access",
      "pitch": "Exclusive Beta Access issues a signed verifiable credential for testing frameworks that the holder keeps and reuses anywhere.",
      "concept": "Beta testers can obtain credentials confirming their participation in closed testing phases of games, establishing their experience to developers.",
      "subDiscipline": "testing frameworks",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches testing frameworks in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Exclusive Beta Access\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nBeta testers can obtain credentials confirming their participation in closed testing phases of games, establishing their experience to developers.\nDiscipline: Game Design & Interactive Media (testing frameworks).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches testing frameworks in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $3B — escape room software · SAM $600M — live puzzle operations · SOM $15M — automated hint systems\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$3B — escape room software",
      "sam": "$600M — live puzzle operations",
      "som": "$15M — automated hint systems"
    },
    {
      "id": "games-game-jam-registration-0",
      "theme": "games",
      "title": "Game Jam Registration",
      "pitch": "Game Jam Registration verifies a credential at the gate for hackathons — proof without a phone call to the issuer.",
      "concept": "Participants present their identity and gaming experience to register for competitive game design events.",
      "subDiscipline": "hackathons",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for hackathons in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Game Jam Registration\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nParticipants present their identity and gaming experience to register for competitive game design events.\nDiscipline: Game Design & Interactive Media (hackathons).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for hackathons in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — procedural generation tools · SAM $1B — indie terrain editors · SOM $20M — AI seed generators\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — procedural generation tools",
      "sam": "$1B — indie terrain editors",
      "som": "$20M — AI seed generators"
    },
    {
      "id": "games-game-development-accreditations-0",
      "theme": "games",
      "title": "Game Development Accreditations",
      "pitch": "Game Development Accreditations mints a did:prism for education so identity travels with the person, not the platform.",
      "concept": "This application allows institutions to verify and issue credentials for game design courses attended by students.",
      "subDiscipline": "education",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits education in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Game Development Accreditations\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nThis application allows institutions to verify and issue credentials for game design courses attended by students.\nDiscipline: Game Design & Interactive Media (education).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits education in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $4B — game peripheral software · SAM $800M — haptic design tools · SOM $25M — controller feedback mappers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$4B — game peripheral software",
      "sam": "$800M — haptic design tools",
      "som": "$25M — controller feedback mappers"
    },
    {
      "id": "games-playtesting-credentials-0",
      "theme": "games",
      "title": "Playtesting Credentials",
      "pitch": "Playtesting Credentials pairs two wallets over DIDComm so user feedback can exchange trusted claims on a private channel.",
      "concept": "User researchers can verify the credibility of playtesters through DIDComm connections to ensure quality feedback.",
      "subDiscipline": "user feedback",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits user feedback in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Playtesting Credentials\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nUser researchers can verify the credibility of playtesters through DIDComm connections to ensure quality feedback.\nDiscipline: Game Design & Interactive Media (user feedback).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits user feedback in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $10B — streaming software market · SAM $2B — Twitch extension tools · SOM $60M — live audience interaction\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$10B — streaming software market",
      "sam": "$2B — Twitch extension tools",
      "som": "$60M — live audience interaction"
    },
    {
      "id": "games-dev-conference-accreditation-0",
      "theme": "games",
      "title": "Dev Conference Accreditation",
      "pitch": "Dev Conference Accreditation issues a signed verifiable credential for event participation that the holder keeps and reuses anywhere.",
      "concept": "Attendees of development conferences receive credentials that authenticate their participation, enabling access to exclusive sessions and networking opportunities.",
      "subDiscipline": "event participation",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches event participation in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Dev Conference Accreditation\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAttendees of development conferences receive credentials that authenticate their participation, enabling access to exclusive sessions and networking opportunities.\nDiscipline: Game Design & Interactive Media (event participation).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches event participation in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $8B — game accessibility tools · SAM $1.5B — inclusive design software · SOM $40M — audio description mapping\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$8B — game accessibility tools",
      "sam": "$1.5B — inclusive design software",
      "som": "$40M — audio description mapping"
    },
    {
      "id": "games-guild-member-recognition-0",
      "theme": "games",
      "title": "Guild Member Recognition",
      "pitch": "Guild Member Recognition verifies a credential at the gate for clan status — proof without a phone call to the issuer.",
      "concept": "Players present proof of guild membership to unlock exclusive in-game benefits and rewards.",
      "subDiscipline": "clan status",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for clan status in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Guild Member Recognition\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPlayers present proof of guild membership to unlock exclusive in-game benefits and rewards.\nDiscipline: Game Design & Interactive Media (clan status).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for clan status in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $7B — game localization market · SAM $1.2B — VO sync software · SOM $35M — lip flap timing tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$7B — game localization market",
      "sam": "$1.2B — VO sync software",
      "som": "$35M — lip flap timing tools"
    },
    {
      "id": "games-digital-rights-tracker-0",
      "theme": "games",
      "title": "Digital Rights Tracker",
      "pitch": "Digital Rights Tracker mints a did:prism for licensing so identity travels with the person, not the platform.",
      "concept": "Creators can register digital rights agreements, allowing them to prove licensing terms to collaborators and publishers effectively.",
      "subDiscipline": "licensing",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits licensing in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Digital Rights Tracker\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCreators can register digital rights agreements, allowing them to prove licensing terms to collaborators and publishers effectively.\nDiscipline: Game Design & Interactive Media (licensing).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits licensing in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5B — gaming moderation tools · SAM $1B — multiplayer safety software · SOM $30M — voice toxicity filters\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5B — gaming moderation tools",
      "sam": "$1B — multiplayer safety software",
      "som": "$30M — voice toxicity filters"
    },
    {
      "id": "games-rights-management-portal-0",
      "theme": "games",
      "title": "Rights Management Portal",
      "pitch": "Rights Management Portal pairs two wallets over DIDComm so asset management can exchange trusted claims on a private channel.",
      "concept": "Content creators can track and verify the rights associated with their digital assets using DIDComm for transparency.",
      "subDiscipline": "asset management",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits asset management in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Rights Management Portal\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nContent creators can track and verify the rights associated with their digital assets using DIDComm for transparency.\nDiscipline: Game Design & Interactive Media (asset management).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits asset management in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $12B — 3D animation software · SAM $3B — game mocap pipelines · SOM $80M — facial sync automation\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$12B — 3D animation software",
      "sam": "$3B — game mocap pipelines",
      "som": "$80M — facial sync automation"
    },
    {
      "id": "games-interactive-narrative-credentials-0",
      "theme": "games",
      "title": "Interactive Narrative Credentials",
      "pitch": "Interactive Narrative Credentials issues a signed verifiable credential for story development that the holder keeps and reuses anywhere.",
      "concept": "Writers involved in interactive narratives can receive credentials that validate their contributions and storytelling techniques to game developers.",
      "subDiscipline": "story development",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches story development in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Interactive Narrative Credentials\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nWriters involved in interactive narratives can receive credentials that validate their contributions and storytelling techniques to game developers.\nDiscipline: Game Design & Interactive Media (story development).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches story development in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $6B — esports coaching platforms · SAM $1.5B — competitive VOD review · SOM $45M — team comms analytics\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$6B — esports coaching platforms",
      "sam": "$1.5B — competitive VOD review",
      "som": "$45M — team comms analytics"
    },
    {
      "id": "games-skill-level-assessment-0",
      "theme": "games",
      "title": "Skill Level Assessment",
      "pitch": "Skill Level Assessment verifies a credential at the gate for game matchmaking — proof without a phone call to the issuer.",
      "concept": "Players verify their gaming skills to enhance matchmaking for competitive gaming environments.",
      "subDiscipline": "game matchmaking",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for game matchmaking in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Skill Level Assessment\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPlayers verify their gaming skills to enhance matchmaking for competitive gaming environments.\nDiscipline: Game Design & Interactive Media (game matchmaking).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for game matchmaking in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global gaming market · SAM $5B — game audio middleware · SOM $50M — indie environmental art tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global gaming market",
      "sam": "$5B — game audio middleware",
      "som": "$50M — indie environmental art tools"
    },
    {
      "id": "games-content-usage-proof-0",
      "theme": "games",
      "title": "Content Usage Proof",
      "pitch": "Content Usage Proof mints a did:prism for content sharing so identity travels with the person, not the platform.",
      "concept": "A service for game developers to prove their right to use third-party content in games, safeguarding against legal disputes.",
      "subDiscipline": "content sharing",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits content sharing in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Content Usage Proof\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nA service for game developers to prove their right to use third-party content in games, safeguarding against legal disputes.\nDiscipline: Game Design & Interactive Media (content sharing).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits content sharing in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global gaming market · SAM $2B — UI/UX design software · SOM $20M — indie game UI toolkits\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global gaming market",
      "sam": "$2B — UI/UX design software",
      "som": "$20M — indie game UI toolkits"
    },
    {
      "id": "games-team-formation-engine-0",
      "theme": "games",
      "title": "Team Formation Engine",
      "pitch": "Team Formation Engine pairs two wallets over DIDComm so collaborative tools can exchange trusted claims on a private channel.",
      "concept": "Developers can form teams for projects based on verified skills and availability through secure DIDComm connections.",
      "subDiscipline": "collaborative tools",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits collaborative tools in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Team Formation Engine\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDevelopers can form teams for projects based on verified skills and availability through secure DIDComm connections.\nDiscipline: Game Design & Interactive Media (collaborative tools).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits collaborative tools in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global gaming market · SAM $4B — game engine asset stores · SOM $40M — indie action game dev tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global gaming market",
      "sam": "$4B — game engine asset stores",
      "som": "$40M — indie action game dev tools"
    },
    {
      "id": "games-recognition-for-diversity-0",
      "theme": "games",
      "title": "Recognition for Diversity",
      "pitch": "Recognition for Diversity issues a signed verifiable credential for inclusivity initiatives that the holder keeps and reuses anywhere.",
      "concept": "Creators participating in diversity programs can receive credentials acknowledging their commitment to inclusivity in game design.",
      "subDiscipline": "inclusivity initiatives",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches inclusivity initiatives in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Recognition for Diversity\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCreators participating in diversity programs can receive credentials acknowledging their commitment to inclusivity in game design.\nDiscipline: Game Design & Interactive Media (inclusivity initiatives).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches inclusivity initiatives in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global gaming market · SAM $3B — game audio production · SOM $30M — indie RPG music licensing\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global gaming market",
      "sam": "$3B — game audio production",
      "som": "$30M — indie RPG music licensing"
    },
    {
      "id": "games-feedback-access-credentials-0",
      "theme": "games",
      "title": "Feedback Access Credentials",
      "pitch": "Feedback Access Credentials verifies a credential at the gate for consumer research — proof without a phone call to the issuer.",
      "concept": "Players prove their identity to access a curated feedback platform for sharing game experiences with developers.",
      "subDiscipline": "consumer research",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for consumer research in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Feedback Access Credentials\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPlayers prove their identity to access a curated feedback platform for sharing game experiences with developers.\nDiscipline: Game Design & Interactive Media (consumer research).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for consumer research in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global gaming market · SAM $1B — horror game development · SOM $15M — indie horror audio assets\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global gaming market",
      "sam": "$1B — horror game development",
      "som": "$15M — indie horror audio assets"
    },
    {
      "id": "games-game-beta-tester-identity-0",
      "theme": "games",
      "title": "Game Beta Tester Identity",
      "pitch": "Game Beta Tester Identity mints a did:prism for testing community so identity travels with the person, not the platform.",
      "concept": "Beta testers can register their participation in game testing, proving their credentials to developers while preserving their privacy.",
      "subDiscipline": "testing community",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits testing community in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Game Beta Tester Identity\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nBeta testers can register their participation in game testing, proving their credentials to developers while preserving their privacy.\nDiscipline: Game Design & Interactive Media (testing community).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits testing community in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global gaming market · SAM $2B — sci-fi game assets · SOM $25M — racing game sound design\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global gaming market",
      "sam": "$2B — sci-fi game assets",
      "som": "$25M — racing game sound design"
    },
    {
      "id": "games-game-show-auditions-0",
      "theme": "games",
      "title": "Game Show Auditions",
      "pitch": "Game Show Auditions pairs two wallets over DIDComm so live casting can exchange trusted claims on a private channel.",
      "concept": "Applicants can verify their audition submissions and qualifications through DIDComm connections for game show opportunities.",
      "subDiscipline": "live casting",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits live casting in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Game Show Auditions\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nApplicants can verify their audition submissions and qualifications through DIDComm connections for game show opportunities.\nDiscipline: Game Design & Interactive Media (live casting).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits live casting in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global gaming market · SAM $5B — fantasy game development · SOM $60M — indie magic system toolkits\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global gaming market",
      "sam": "$5B — fantasy game development",
      "som": "$60M — indie magic system toolkits"
    },
    {
      "id": "games-historical-game-validation-0",
      "theme": "games",
      "title": "Historical Game Validation",
      "pitch": "Historical Game Validation issues a signed verifiable credential for cultural heritage that the holder keeps and reuses anywhere.",
      "concept": "Games based on historical events receive credentials that verify their authenticity and accuracy, enhancing trust among users.",
      "subDiscipline": "cultural heritage",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches cultural heritage in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Historical Game Validation\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nGames based on historical events receive credentials that verify their authenticity and accuracy, enhancing trust among users.\nDiscipline: Game Design & Interactive Media (cultural heritage).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches cultural heritage in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global gaming market · SAM $500M — accessibility software · SOM $10M — inclusive design plugins\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global gaming market",
      "sam": "$500M — accessibility software",
      "som": "$10M — inclusive design plugins"
    },
    {
      "id": "games-design-challenge-validity-0",
      "theme": "games",
      "title": "Design Challenge Validity",
      "pitch": "Design Challenge Validity verifies a credential at the gate for competitions — proof without a phone call to the issuer.",
      "concept": "Participants verify their past designs to enter high-stakes game design challenges.",
      "subDiscipline": "competitions",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for competitions in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Design Challenge Validity\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nParticipants verify their past designs to enter high-stakes game design challenges.\nDiscipline: Game Design & Interactive Media (competitions).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for competitions in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global gaming market · SAM $3B — cozy game market · SOM $35M — simulation game audio\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global gaming market",
      "sam": "$3B — cozy game market",
      "som": "$35M — simulation game audio"
    },
    {
      "id": "games-sponsorship-validation-tool-0",
      "theme": "games",
      "title": "Sponsorship Validation Tool",
      "pitch": "Sponsorship Validation Tool mints a did:prism for marketing so identity travels with the person, not the platform.",
      "concept": "This app allows game developers to verify sponsorship agreements and prove compliance to sponsors in a secure manner.",
      "subDiscipline": "marketing",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits marketing in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Sponsorship Validation Tool\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nThis app allows game developers to verify sponsorship agreements and prove compliance to sponsors in a secure manner.\nDiscipline: Game Design & Interactive Media (marketing).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits marketing in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $50B — XR hardware and software · SAM $5B — VR game development · SOM $50M — VR haptic audio tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$50B — XR hardware and software",
      "sam": "$5B — VR game development",
      "som": "$50M — VR haptic audio tools"
    },
    {
      "id": "games-feedback-circles-0",
      "theme": "games",
      "title": "Feedback Circles",
      "pitch": "Feedback Circles pairs two wallets over DIDComm so iterative design can exchange trusted claims on a private channel.",
      "concept": "Designers can form feedback loops by verifying member identities and roles through DIDComm for constructive input.",
      "subDiscipline": "iterative design",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits iterative design in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Feedback Circles\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDesigners can form feedback loops by verifying member identities and roles through DIDComm for constructive input.\nDiscipline: Game Design & Interactive Media (iterative design).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits iterative design in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global gaming market · SAM $2B — retro indie game market · SOM $20M — chiptune asset stores\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global gaming market",
      "sam": "$2B — retro indie game market",
      "som": "$20M — chiptune asset stores"
    },
    {
      "id": "games-access-to-game-assets-0",
      "theme": "games",
      "title": "Access to Game Assets",
      "pitch": "Access to Game Assets issues a signed verifiable credential for resource sharing that the holder keeps and reuses anywhere.",
      "concept": "Developers can issue credentials that grant access to shared game assets within collaborative projects, streamlining resource management.",
      "subDiscipline": "resource sharing",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches resource sharing in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Access to Game Assets\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDevelopers can issue credentials that grant access to shared game assets within collaborative projects, streamlining resource management.\nDiscipline: Game Design & Interactive Media (resource sharing).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches resource sharing in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global gaming market · SAM $4B — puzzle game market · SOM $40M — mobile puzzle audio\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global gaming market",
      "sam": "$4B — puzzle game market",
      "som": "$40M — mobile puzzle audio"
    },
    {
      "id": "games-sponsorship-proof-0",
      "theme": "games",
      "title": "Sponsorship Proof",
      "pitch": "Sponsorship Proof verifies a credential at the gate for funding — proof without a phone call to the issuer.",
      "concept": "Creators present verified sponsorship from organizations to ensure honest representation of financial support.",
      "subDiscipline": "funding",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for funding in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Sponsorship Proof\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCreators present verified sponsorship from organizations to ensure honest representation of financial support.\nDiscipline: Game Design & Interactive Media (funding).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for funding in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global gaming market · SAM $3B — creature design software · SOM $30M — monster taming game assets\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global gaming market",
      "sam": "$3B — creature design software",
      "som": "$30M — monster taming game assets"
    },
    {
      "id": "games-cultural-heritage-game-prototypes-0",
      "theme": "games",
      "title": "Cultural Heritage Game Prototypes",
      "pitch": "Cultural Heritage Game Prototypes mints a did:prism for cultural games so identity travels with the person, not the platform.",
      "concept": "Developers can register prototypes focused on cultural heritage, enhancing their credibility when seeking funding or partnerships.",
      "subDiscipline": "cultural games",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits cultural games in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Cultural Heritage Game Prototypes\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDevelopers can register prototypes focused on cultural heritage, enhancing their credibility when seeking funding or partnerships.\nDiscipline: Game Design & Interactive Media (cultural games).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits cultural games in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global gaming market · SAM $6B — narrative game development · SOM $60M — open-world audio assets\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global gaming market",
      "sam": "$6B — narrative game development",
      "som": "$60M — open-world audio assets"
    },
    {
      "id": "games-collective-licensing-0",
      "theme": "games",
      "title": "Collective Licensing",
      "pitch": "Collective Licensing pairs two wallets over DIDComm so shared projects can exchange trusted claims on a private channel.",
      "concept": "Groups can collectively manage and verify licenses for shared assets, ensuring transparency via DIDComm connections.",
      "subDiscipline": "shared projects",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits shared projects in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Collective Licensing\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nGroups can collectively manage and verify licenses for shared assets, ensuring transparency via DIDComm connections.\nDiscipline: Game Design & Interactive Media (shared projects).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits shared projects in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global gaming market · SAM $8B — sports game market · SOM $80M — sports audio middleware\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global gaming market",
      "sam": "$8B — sports game market",
      "som": "$80M — sports audio middleware"
    },
    {
      "id": "games-completionist-credentials-0",
      "theme": "games",
      "title": "Completionist Credentials",
      "pitch": "Completionist Credentials issues a signed verifiable credential for player engagement that the holder keeps and reuses anywhere.",
      "concept": "Players can earn credentials showcasing their achievements in games, enhancing their profiles within communities and potentially unlocking rewards.",
      "subDiscipline": "player engagement",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches player engagement in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Completionist Credentials\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPlayers can earn credentials showcasing their achievements in games, enhancing their profiles within communities and potentially unlocking rewards.\nDiscipline: Game Design & Interactive Media (player engagement).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches player engagement in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global gaming market · SAM $2B — stealth game market · SOM $25M — indie stealth foley packs\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global gaming market",
      "sam": "$2B — stealth game market",
      "som": "$25M — indie stealth foley packs"
    },
    {
      "id": "games-access-to-development-kits-0",
      "theme": "games",
      "title": "Access to Development Kits",
      "pitch": "Access to Development Kits verifies a credential at the gate for developer resources — proof without a phone call to the issuer.",
      "concept": "Developers prove their identity to gain access to specialized game development kits for innovative projects.",
      "subDiscipline": "developer resources",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for developer resources in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Access to Development Kits\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDevelopers prove their identity to gain access to specialized game development kits for innovative projects.\nDiscipline: Game Design & Interactive Media (developer resources).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for developer resources in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global gaming market · SAM $1B — rhythm game market · SOM $15M — indie rhythm game audio\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global gaming market",
      "sam": "$1B — rhythm game market",
      "som": "$15M — indie rhythm game audio"
    },
    {
      "id": "games-agent-delegation-registry-0",
      "theme": "games",
      "title": "Agent Delegation Registry",
      "pitch": "Agent Delegation Registry mints a did:prism for representation so identity travels with the person, not the platform.",
      "concept": "Game creators can register agents to represent them, proving this relationship to publishers and collaborators.",
      "subDiscipline": "representation",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits representation in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Agent Delegation Registry\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nGame creators can register agents to represent them, proving this relationship to publishers and collaborators.\nDiscipline: Game Design & Interactive Media (representation).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits representation in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global gaming market · SAM $4B — survival game market · SOM $40M — crafting sim audio assets\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global gaming market",
      "sam": "$4B — survival game market",
      "som": "$40M — crafting sim audio assets"
    },
    {
      "id": "games-interactive-workshop-access-0",
      "theme": "games",
      "title": "Interactive Workshop Access",
      "pitch": "Interactive Workshop Access pairs two wallets over DIDComm so talent development can exchange trusted claims on a private channel.",
      "concept": "Participants can prove their registration and qualification for workshops through DIDComm connections for seamless entry.",
      "subDiscipline": "talent development",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits talent development in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Interactive Workshop Access\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nParticipants can prove their registration and qualification for workshops through DIDComm connections for seamless entry.\nDiscipline: Game Design & Interactive Media (talent development).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits talent development in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global gaming market · SAM $3B — platformer game market · SOM $30M — 2D indie game audio\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global gaming market",
      "sam": "$3B — platformer game market",
      "som": "$30M — 2D indie game audio"
    },
    {
      "id": "games-real-world-experience-0",
      "theme": "games",
      "title": "Real World Experience",
      "pitch": "Real World Experience issues a signed verifiable credential for industry engagement that the holder keeps and reuses anywhere.",
      "concept": "Interns at game studios receive credentials verifying their roles and contributions, aiding their future job applications.",
      "subDiscipline": "industry engagement",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches industry engagement in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Real World Experience\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nInterns at game studios receive credentials verifying their roles and contributions, aiding their future job applications.\nDiscipline: Game Design & Interactive Media (industry engagement).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches industry engagement in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global gaming market · SAM $10B — live-service game economy · SOM $100M — gacha audio design\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global gaming market",
      "sam": "$10B — live-service game economy",
      "som": "$100M — gacha audio design"
    },
    {
      "id": "games-integration-testers-credentials-0",
      "theme": "games",
      "title": "Integration Testers' Credentials",
      "pitch": "Integration Testers' Credentials verifies a credential at the gate for software testing — proof without a phone call to the issuer.",
      "concept": "Testers prove their expertise to participate in integration testing for games across various platforms.",
      "subDiscipline": "software testing",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for software testing in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Integration Testers' Credentials\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTesters prove their expertise to participate in integration testing for games across various platforms.\nDiscipline: Game Design & Interactive Media (software testing).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for software testing in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global gaming market · SAM $1B — UX onboarding tools · SOM $10M — game tutorial asset packs\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global gaming market",
      "sam": "$1B — UX onboarding tools",
      "som": "$10M — game tutorial asset packs"
    },
    {
      "id": "games-skill-set-validator-0",
      "theme": "games",
      "title": "Skill Set Validator",
      "pitch": "Skill Set Validator mints a did:prism for talent matching so identity travels with the person, not the platform.",
      "concept": "Job seekers can verify their game design skills, allowing companies to trust their abilities before hiring.",
      "subDiscipline": "talent matching",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits talent matching in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Skill Set Validator\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nJob seekers can verify their game design skills, allowing companies to trust their abilities before hiring.\nDiscipline: Game Design & Interactive Media (talent matching).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits talent matching in Game Design & Interactive Media because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global gaming market · SAM $2B — simulation game market · SOM $20M — flight sim audio mods\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global gaming market",
      "sam": "$2B — simulation game market",
      "som": "$20M — flight sim audio mods"
    },
    {
      "id": "games-creator-collaboration-gateway-0",
      "theme": "games",
      "title": "Creator Collaboration Gateway",
      "pitch": "Creator Collaboration Gateway pairs two wallets over DIDComm so network building can exchange trusted claims on a private channel.",
      "concept": "Content creators can establish trusted collaborations by exchanging verifiable identities through DIDComm invitations.",
      "subDiscipline": "network building",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits network building in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Creator Collaboration Gateway\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nContent creators can establish trusted collaborations by exchanging verifiable identities through DIDComm invitations.\nDiscipline: Game Design & Interactive Media (network building).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits network building in Game Design & Interactive Media because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global gaming market · SAM $15B — multiplayer game market · SOM $150M — esports lobby audio\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global gaming market",
      "sam": "$15B — multiplayer game market",
      "som": "$150M — esports lobby audio"
    },
    {
      "id": "games-ledger-credit-0",
      "theme": "games",
      "title": "Ledger Credit",
      "pitch": "Ledger Credit issues a signed verifiable credential for credit attribution that the holder keeps and reuses anywhere.",
      "concept": "A game design app where game designers use Credential Issuance to establish who made which part of the work.",
      "subDiscipline": "credit attribution",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches credit attribution in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Ledger Credit\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nA game design app where game designers use Credential Issuance to establish who made which part of the work.\nDiscipline: Game Design & Interactive Media (credit attribution).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches credit attribution in Game Design & Interactive Media because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global gaming market · SAM $3B — strategy game market · SOM $30M — automation game audio\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global gaming market",
      "sam": "$3B — strategy game market",
      "som": "$30M — automation game audio"
    },
    {
      "id": "games-exclusive-content-verification-0",
      "theme": "games",
      "title": "Exclusive Content Verification",
      "pitch": "Exclusive Content Verification verifies a credential at the gate for fan clubs — proof without a phone call to the issuer.",
      "concept": "Fans present proof of membership to unlock exclusive in-game content and experiences.",
      "subDiscipline": "fan clubs",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for fan clubs in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Exclusive Content Verification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFans present proof of membership to unlock exclusive in-game content and experiences.\nDiscipline: Game Design & Interactive Media (fan clubs).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for fan clubs in Game Design & Interactive Media because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $200B — global gaming market · SAM $6B — narrative game development · SOM $60M — indie game soundtracks\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$200B — global gaming market",
      "sam": "$6B — narrative game development",
      "som": "$60M — indie game soundtracks"
    }
  ]
}
