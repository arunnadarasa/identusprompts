{
  "theme": {
    "slug": "games",
    "name": "Game Design & Interactive Media",
    "emoji": "🎮",
    "audience": "game designers, interactive artists, XR creators",
    "market_anchor": "the game industry (~$200B) and >3M indie developers"
  },
  "ideas": [
    {
      "id": "games-codex-whisperer-0",
      "theme": "games",
      "title": "Codex Whisperer",
      "pitch": "Lovable AI generates deep faction histories so you can worldbuild; AIsa narrates it while you commute.",
      "subDiscipline": "narrative lore drafting",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "Text-to-speech allows designers to absorb dense, complex lore hands-free during downtime.",
      "megaPrompt": "Build \"Codex Whisperer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI generates deep faction histories so you can worldbuild; AIsa narrates it while you commute.\nDiscipline: Game Design & Interactive Media (narrative lore drafting).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: Text-to-speech allows designers to absorb dense, complex lore hands-free during downtime.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for narrative lore drafting. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for narrative lore drafting.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$15B — narrative and RPG game development",
      "som": "$500M — indie RPG worldbuilding tools"
    },
    {
      "id": "games-playtest-prompter-1",
      "theme": "games",
      "title": "Playtest Prompter",
      "pitch": "Lovable AI scripts unbiased observation questions so you stay focused; AIsa writes it during live playtests.",
      "subDiscipline": "live user testing",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "Audio prompts keep the tester's eyes on the player rather than a clipboard.",
      "megaPrompt": "Build \"Playtest Prompter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI scripts unbiased observation questions so you stay focused; AIsa writes it during live playtests.\nDiscipline: Game Design & Interactive Media (live user testing).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: Audio prompts keep the tester's eyes on the player rather than a clipboard.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for live user testing. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for live user testing.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$10B — game QA and playtesting services",
      "som": "$300M — indie user research tools"
    },
    {
      "id": "games-tavern-chatterbox-2",
      "theme": "games",
      "title": "Tavern Chatterbox",
      "pitch": "Lovable AI writes contextual background chatter so your village feels alive; AIsa voices it for instant auditioning.",
      "subDiscipline": "ambient NPC barks",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "Hearing generated barks aloud immediately validates the tone and pacing of ambient dialogue.",
      "megaPrompt": "Build \"Tavern Chatterbox\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI writes contextual background chatter so your village feels alive; AIsa voices it for instant auditioning.\nDiscipline: Game Design & Interactive Media (ambient NPC barks).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: Hearing generated barks aloud immediately validates the tone and pacing of ambient dialogue.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for ambient NPC barks. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for ambient NPC barks.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$12B — game audio and narrative production",
      "som": "$400M — procedural NPC dialogue tools"
    },
    {
      "id": "games-locomotion-soother-3",
      "theme": "games",
      "title": "Locomotion Soother",
      "pitch": "Lovable AI drafts calming grounding scripts so panicked users relax; AIsa reads it to ease VR vertigo.",
      "subDiscipline": "XR comfort design",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for XR comfort design in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Locomotion Soother\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI drafts calming grounding scripts so panicked users relax; AIsa reads it to ease VR vertigo.\nDiscipline: Game Design & Interactive Media (XR comfort design).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for XR comfort design in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for XR comfort design. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for XR comfort design.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$8B — virtual and mixed reality software",
      "som": "$200M — XR accessibility and comfort plugins"
    },
    {
      "id": "games-onboarding-metronome-4",
      "theme": "games",
      "title": "Onboarding Metronome",
      "pitch": "Lovable AI structures step-by-step mechanics introductions so tutorials flow better; AIsa writes it to test rhythmic pacing.",
      "subDiscipline": "tutorial flow design",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for tutorial flow design in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Onboarding Metronome\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI structures step-by-step mechanics introductions so tutorials flow better; AIsa writes it to test rhythmic pacing.\nDiscipline: Game Design & Interactive Media (tutorial flow design).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for tutorial flow design in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for tutorial flow design. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for tutorial flow design.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$20B — casual and mobile game development",
      "som": "$600M — UX and onboarding design tools"
    },
    {
      "id": "games-jam-pitch-polisher-5",
      "theme": "games",
      "title": "Jam Pitch Polisher",
      "pitch": "Lovable AI refines your chaotic game jam pitch so you sound professional; AIsa narrates it for seamless rehearsal.",
      "subDiscipline": "game jam presentations",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "Audio playback helps solo devs memorize and time their rapid-fire presentations perfectly.",
      "megaPrompt": "Build \"Jam Pitch Polisher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI refines your chaotic game jam pitch so you sound professional; AIsa narrates it for seamless rehearsal.\nDiscipline: Game Design & Interactive Media (game jam presentations).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: Audio playback helps solo devs memorize and time their rapid-fire presentations perfectly.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for game jam presentations. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for game jam presentations.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$5B — game jam and indie showcase events",
      "som": "$150M — pitch preparation software"
    },
    {
      "id": "games-blindside-navigator-6",
      "theme": "games",
      "title": "Blindside Navigator",
      "pitch": "Lovable AI translates complex UI menus into descriptive routes so blind players navigate easily; AIsa writes it.",
      "subDiscipline": "visual impairment accessibility",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for visual impairment accessibility in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Blindside Navigator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI translates complex UI menus into descriptive routes so blind players navigate easily; AIsa writes it.\nDiscipline: Game Design & Interactive Media (visual impairment accessibility).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for visual impairment accessibility in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for visual impairment accessibility. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for visual impairment accessibility.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$6B — game accessibility software and consulting",
      "som": "$250M — UI screen-reader integration tools"
    },
    {
      "id": "games-devlog-dictation-7",
      "theme": "games",
      "title": "Devlog Dictation",
      "pitch": "Lovable AI turns messy commit logs into engaging stories so you market consistently; AIsa voices it weekly.",
      "subDiscipline": "indie marketing",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for indie marketing in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Devlog Dictation\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI turns messy commit logs into engaging stories so you market consistently; AIsa voices it weekly.\nDiscipline: Game Design & Interactive Media (indie marketing).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for indie marketing in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for indie marketing. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for indie marketing.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$15B — game marketing and community management",
      "som": "$450M — indie creator content tools"
    },
    {
      "id": "games-corridor-tension-tuner-8",
      "theme": "games",
      "title": "Corridor Tension Tuner",
      "pitch": "Lovable AI writes emotional beats for your horror hallways so you map dread; AIsa shows it.",
      "subDiscipline": "level design pacing",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "Hearing the emotional shifts aloud helps designers place lighting and enemy spawns accurately.",
      "megaPrompt": "Build \"Corridor Tension Tuner\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI writes emotional beats for your horror hallways so you map dread; AIsa shows it.\nDiscipline: Game Design & Interactive Media (level design pacing).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: Hearing the emotional shifts aloud helps designers place lighting and enemy spawns accurately.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for level design pacing. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for level design pacing.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$18B — horror and action game development",
      "som": "$550M — level design and pacing software"
    },
    {
      "id": "games-polyglot-playtester-9",
      "theme": "games",
      "title": "Polyglot Playtester",
      "pitch": "Lovable AI flags culturally awkward idioms so your translations feel natural; AIsa writes it to check cadence.",
      "subDiscipline": "localization QA",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for localization QA in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Polyglot Playtester\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI flags culturally awkward idioms so your translations feel natural; AIsa writes it to check cadence.\nDiscipline: Game Design & Interactive Media (localization QA).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for localization QA in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for localization QA. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for localization QA.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$10B — game localization and translation services",
      "som": "$300M — automated localization QA tools"
    },
    {
      "id": "games-empathy-scenario-spinner-10",
      "theme": "games",
      "title": "Empathy Scenario Spinner",
      "pitch": "Lovable AI generates sensitive dialogue trees so medical sims feel real; AIsa voices it for bedside practice.",
      "subDiscipline": "serious game design",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for serious game design in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Empathy Scenario Spinner\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI generates sensitive dialogue trees so medical sims feel real; AIsa voices it for bedside practice.\nDiscipline: Game Design & Interactive Media (serious game design).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for serious game design in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for serious game design. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for serious game design.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$12B — serious games and simulation software",
      "som": "$400M — medical training simulators"
    },
    {
      "id": "games-soundscape-storyboarder-11",
      "theme": "games",
      "title": "Soundscape Storyboarder",
      "pitch": "Lovable AI describes layered ambient soundscapes so audio engineers visualize the mix; AIsa reads it before DAW work.",
      "subDiscipline": "game audio conceptualization",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "Spoken audio briefs help sound designers internalize the mood before opening their software.",
      "megaPrompt": "Build \"Soundscape Storyboarder\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI describes layered ambient soundscapes so audio engineers visualize the mix; AIsa reads it before DAW work.\nDiscipline: Game Design & Interactive Media (game audio conceptualization).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: Spoken audio briefs help sound designers internalize the mood before opening their software.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for game audio conceptualization. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for game audio conceptualization.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$12B — game audio and narrative production",
      "som": "$350M — audio direction and storyboarding tools"
    },
    {
      "id": "games-launch-postmortem-mirror-12",
      "theme": "games",
      "title": "Launch Postmortem Mirror",
      "pitch": "Lovable AI synthesizes chaotic launch feedback into lessons so your team reflects calmly; AIsa narrates it today.",
      "subDiscipline": "team reflection",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "Audio summaries foster a relaxed, campfire-style atmosphere for difficult team retrospectives.",
      "megaPrompt": "Build \"Launch Postmortem Mirror\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI synthesizes chaotic launch feedback into lessons so your team reflects calmly; AIsa narrates it today.\nDiscipline: Game Design & Interactive Media (team reflection).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: Audio summaries foster a relaxed, campfire-style atmosphere for difficult team retrospectives.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for team reflection. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for team reflection.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$25B — game studio management and operations",
      "som": "$700M — agile and retrospective software"
    },
    {
      "id": "games-menu-friction-finder-13",
      "theme": "games",
      "title": "Menu Friction Finder",
      "pitch": "Lovable AI critiques your inventory screen's cognitive load so you improve UX; AIsa writes it during blind navigation.",
      "subDiscipline": "UI UX testing",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "Eyes-free audio feedback forces designers to evaluate UI intuitiveness purely by memory.",
      "megaPrompt": "Build \"Menu Friction Finder\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI critiques your inventory screen's cognitive load so you improve UX; AIsa writes it during blind navigation.\nDiscipline: Game Design & Interactive Media (UI UX testing).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: Eyes-free audio feedback forces designers to evaluate UI intuitiveness purely by memory.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for UI UX testing. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for UI UX testing.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$20B — game UI and UX design",
      "som": "$600M — prototyping and usability testing tools"
    },
    {
      "id": "games-twitch-hype-announcer-14",
      "theme": "games",
      "title": "Twitch Hype Announcer",
      "pitch": "Lovable AI generates witty shoutouts for top channel point redeemers so streams stay energetic; AIsa writes it live.",
      "subDiscipline": "streamer integration design",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for streamer integration design in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Twitch Hype Announcer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI generates witty shoutouts for top channel point redeemers so streams stay energetic; AIsa writes it live.\nDiscipline: Game Design & Interactive Media (streamer integration design).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for streamer integration design in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for streamer integration design. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for streamer integration design.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$8B — game streaming and creator economy",
      "som": "$250M — stream overlay and integration plugins"
    },
    {
      "id": "games-mocap-emotion-prompter-15",
      "theme": "games",
      "title": "Mocap Emotion Prompter",
      "pitch": "Lovable AI writes nuanced emotional subtext so motion capture actors stay in character; AIsa reads it between takes.",
      "subDiscipline": "animation direction",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "Quick audio playback of character motivations keeps actors in the zone without reading paper.",
      "megaPrompt": "Build \"Mocap Emotion Prompter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI writes nuanced emotional subtext so motion capture actors stay in character; AIsa reads it between takes.\nDiscipline: Game Design & Interactive Media (animation direction).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: Quick audio playback of character motivations keeps actors in the zone without reading paper.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for animation direction. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for animation direction.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$15B — 3D animation and motion capture",
      "som": "$450M — virtual production direction tools"
    },
    {
      "id": "games-math-quest-narrator-16",
      "theme": "games",
      "title": "Math Quest Narrator",
      "pitch": "Lovable AI adapts complex algebra problems into fantasy quests so young learners stay engaged; AIsa voices it playfully.",
      "subDiscipline": "educational game design",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for educational game design in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Math Quest Narrator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI adapts complex algebra problems into fantasy quests so young learners stay engaged; AIsa voices it playfully.\nDiscipline: Game Design & Interactive Media (educational game design).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for educational game design in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for educational game design. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for educational game design.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$10B — educational games and edtech",
      "som": "$300M — gamified learning platforms"
    },
    {
      "id": "games-discord-patchnotes-bard-17",
      "theme": "games",
      "title": "Discord Patchnotes Bard",
      "pitch": "Lovable AI translates dry technical changelogs into epic announcements so your community stays hyped; AIsa writes it loudly.",
      "subDiscipline": "community management",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for community management in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Discord Patchnotes Bard\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI translates dry technical changelogs into epic announcements so your community stays hyped; AIsa writes it loudly.\nDiscipline: Game Design & Interactive Media (community management).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for community management in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for community management. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for community management.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$15B — game marketing and community management",
      "som": "$400M — Discord bot and community tools"
    },
    {
      "id": "games-glitch-repro-storyteller-18",
      "theme": "games",
      "title": "Glitch Repro Storyteller",
      "pitch": "Lovable AI structures messy tester notes into clear reproduction steps so programmers code efficiently; AIsa shows it.",
      "subDiscipline": "QA bug reporting",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "Audio bug reports allow developers to absorb complex edge-case logic without breaking flow.",
      "megaPrompt": "Build \"Glitch Repro Storyteller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI structures messy tester notes into clear reproduction steps so programmers code efficiently; AIsa shows it.\nDiscipline: Game Design & Interactive Media (QA bug reporting).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: Audio bug reports allow developers to absorb complex edge-case logic without breaking flow.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for QA bug reporting. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for QA bug reporting.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$10B — game QA and playtesting services",
      "som": "$350M — issue tracking and bug management"
    },
    {
      "id": "games-choice-consequence-weaver-19",
      "theme": "games",
      "title": "Choice Consequence Weaver",
      "pitch": "Lovable AI outlines the emotional fallout of dialogue choices so you verify branching logic; AIsa narrates it clearly.",
      "subDiscipline": "interactive fiction",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "Hearing the consequences spoken aloud highlights tonal inconsistencies in complex narrative trees.",
      "megaPrompt": "Build \"Choice Consequence Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI outlines the emotional fallout of dialogue choices so you verify branching logic; AIsa narrates it clearly.\nDiscipline: Game Design & Interactive Media (interactive fiction).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: Hearing the consequences spoken aloud highlights tonal inconsistencies in complex narrative trees.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for interactive fiction. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for interactive fiction.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$12B — interactive fiction and visual novels",
      "som": "$400M — narrative design software"
    },
    {
      "id": "games-hologram-audio-director-20",
      "theme": "games",
      "title": "Hologram Audio Director",
      "pitch": "Lovable AI plans directional audio cues for your mixed reality app so you choreograph attention; AIsa writes it.",
      "subDiscipline": "XR spatial audio",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "Spoken spatial scripts help XR directors choreograph user attention before implementing 3D engines.",
      "megaPrompt": "Build \"Hologram Audio Director\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI plans directional audio cues for your mixed reality app so you choreograph attention; AIsa writes it.\nDiscipline: Game Design & Interactive Media (XR spatial audio).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: Spoken spatial scripts help XR directors choreograph user attention before implementing 3D engines.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for XR spatial audio. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for XR spatial audio.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$8B — virtual and mixed reality software",
      "som": "$250M — spatial audio design tools"
    },
    {
      "id": "games-crunch-burnout-shield-21",
      "theme": "games",
      "title": "Crunch Burnout Shield",
      "pitch": "Lovable AI designs micro-break routines tailored to your dev milestone so you prevent wrist strain; AIsa reads it.",
      "subDiscipline": "developer wellness",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "Audio-guided physical breaks are essential for solo devs glued to monitors during crunch.",
      "megaPrompt": "Build \"Crunch Burnout Shield\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI designs micro-break routines tailored to your dev milestone so you prevent wrist strain; AIsa reads it.\nDiscipline: Game Design & Interactive Media (developer wellness).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: Audio-guided physical breaks are essential for solo devs glued to monitors during crunch.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for developer wellness. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for developer wellness.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$25B — game studio management and operations",
      "som": "$600M — corporate wellness and ergonomics software"
    },
    {
      "id": "games-gold-sink-balancer-22",
      "theme": "games",
      "title": "Gold Sink Balancer",
      "pitch": "Lovable AI simulates player spending behaviors across your late-game economy so you spot inflation; AIsa writes it daily.",
      "subDiscipline": "game economy design",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "Audio summaries of complex spreadsheet data help designers grasp macro-economic flaws intuitively.",
      "megaPrompt": "Build \"Gold Sink Balancer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI simulates player spending behaviors across your late-game economy so you spot inflation; AIsa writes it daily.\nDiscipline: Game Design & Interactive Media (game economy design).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: Audio summaries of complex spreadsheet data help designers grasp macro-economic flaws intuitively.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for game economy design. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for game economy design.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$30B — live-ops and in-game economies",
      "som": "$800M — game economy balancing tools"
    },
    {
      "id": "games-publisher-hook-sharpener-23",
      "theme": "games",
      "title": "Publisher Hook Sharpener",
      "pitch": "Lovable AI rewrites your unique selling points into punchy hooks so you pitch confidently; AIsa narrates it perfectly.",
      "subDiscipline": "publisher pitching",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for publisher pitching in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Publisher Hook Sharpener\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI rewrites your unique selling points into punchy hooks so you pitch confidently; AIsa narrates it perfectly.\nDiscipline: Game Design & Interactive Media (publisher pitching).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for publisher pitching in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for publisher pitching. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for publisher pitching.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$5B — game publishing and investment",
      "som": "$150M — pitch preparation software"
    },
    {
      "id": "games-modder-api-guide-24",
      "theme": "games",
      "title": "Modder API Guide",
      "pitch": "Lovable AI translates dense engine API updates into accessible tutorials so modders learn easily; AIsa writes it smoothly.",
      "subDiscipline": "modding documentation",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "Audio tutorials let community modders follow along in their IDE without constantly alt-tabbing.",
      "megaPrompt": "Build \"Modder API Guide\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI translates dense engine API updates into accessible tutorials so modders learn easily; AIsa writes it smoothly.\nDiscipline: Game Design & Interactive Media (modding documentation).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: Audio tutorials let community modders follow along in their IDE without constantly alt-tabbing.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for modding documentation. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for modding documentation.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global video game industry",
      "sam": "$10B — UGC and modding ecosystems",
      "som": "$300M — developer documentation platforms"
    },
    {
      "id": "games-phantom-dialogue-sparring-0",
      "theme": "games",
      "title": "Phantom Dialogue Sparring",
      "pitch": "Lovable AI generates dynamic NPC responses so designers rehearse lines; AIsa writes it.",
      "subDiscipline": "NPC voice acting",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for NPC voice acting in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Phantom Dialogue Sparring\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI generates dynamic NPC responses so designers rehearse lines; AIsa writes it.\nDiscipline: Game Design & Interactive Media (NPC voice acting).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for NPC voice acting in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for NPC voice acting. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for NPC voice acting.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — global game development software market",
      "sam": "$1.5B — narrative design and dialogue tools",
      "som": "$40M — AI voice rehearsal assistants for indie studios"
    },
    {
      "id": "games-blindfolded-level-architect-1",
      "theme": "games",
      "title": "Blindfolded Level Architect",
      "pitch": "Lovable AI critiques spatial flow so designers refine layouts; AIsa writes it.",
      "subDiscipline": "spatial layout drafting",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for spatial layout drafting in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Blindfolded Level Architect\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI critiques spatial flow so designers refine layouts; AIsa writes it.\nDiscipline: Game Design & Interactive Media (spatial layout drafting).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for spatial layout drafting in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for spatial layout drafting. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for spatial layout drafting.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — game engines and level editors market",
      "sam": "$2B — indie level design and prototyping tools",
      "som": "$50M — AI spatial drafting companions"
    },
    {
      "id": "games-indie-sprint-therapist-2",
      "theme": "games",
      "title": "Indie Sprint Therapist",
      "pitch": "Lovable AI plans sustainable dev sprints so solo creators avoid burnout; AIsa writes it.",
      "subDiscipline": "developer mental health",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for developer mental health in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Indie Sprint Therapist\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI plans sustainable dev sprints so solo creators avoid burnout; AIsa writes it.\nDiscipline: Game Design & Interactive Media (developer mental health).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for developer mental health in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for developer mental health. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for developer mental health.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$15B — project management and productivity software",
      "sam": "$4B — game development agile and scoping tools",
      "som": "$80M — AI mental health and sprint coaching for solo devs"
    },
    {
      "id": "games-lore-inquisitor-chamber-3",
      "theme": "games",
      "title": "Lore Inquisitor Chamber",
      "pitch": "Lovable AI cross-references worldbuilding bibles so writers fix plot holes; AIsa writes it.",
      "subDiscipline": "worldbuilding consistency",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for worldbuilding consistency in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Lore Inquisitor Chamber\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI cross-references worldbuilding bibles so writers fix plot holes; AIsa writes it.\nDiscipline: Game Design & Interactive Media (worldbuilding consistency).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for worldbuilding consistency in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for worldbuilding consistency. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for worldbuilding consistency.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — global game development software market",
      "sam": "$1.2B — worldbuilding and lore management platforms",
      "som": "$35M — AI lore consistency checkers"
    },
    {
      "id": "games-spatial-soundscape-conductor-4",
      "theme": "games",
      "title": "Spatial Soundscape Conductor",
      "pitch": "Lovable AI designs 3D audio cues so XR creators map soundscapes; AIsa writes it.",
      "subDiscipline": "XR audio prototyping",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for XR audio prototyping in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Spatial Soundscape Conductor\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI designs 3D audio cues so XR creators map soundscapes; AIsa writes it.\nDiscipline: Game Design & Interactive Media (XR audio prototyping).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for XR audio prototyping in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for XR audio prototyping. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for XR audio prototyping.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$12B — game audio and XR middleware market",
      "sam": "$3B — spatial audio plugins and XR tools",
      "som": "$60M — AI voice-directed audio prototyping"
    },
    {
      "id": "games-tabletop-encounter-weaver-5",
      "theme": "games",
      "title": "Tabletop Encounter Weaver",
      "pitch": "Lovable AI brainstorms dynamic encounters so game masters prep sessions; AIsa writes it.",
      "subDiscipline": "TTRPG campaign prep",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for TTRPG campaign prep in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Tabletop Encounter Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI brainstorms dynamic encounters so game masters prep sessions; AIsa writes it.\nDiscipline: Game Design & Interactive Media (TTRPG campaign prep).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for TTRPG campaign prep in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for TTRPG campaign prep. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for TTRPG campaign prep.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — tabletop RPG and digital companion market",
      "sam": "$1B — digital GM tools and encounter builders",
      "som": "$25M — AI campaign prep assistants"
    },
    {
      "id": "games-meta-balance-debater-6",
      "theme": "games",
      "title": "Meta Balance Debater",
      "pitch": "Lovable AI argues weapon statistics so designers balance combat loops; AIsa writes it.",
      "subDiscipline": "combat economy tuning",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for combat economy tuning in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Meta Balance Debater\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI argues weapon statistics so designers balance combat loops; AIsa writes it.\nDiscipline: Game Design & Interactive Media (combat economy tuning).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for combat economy tuning in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for combat economy tuning. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for combat economy tuning.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — game engines and analytics platforms",
      "sam": "$2.5B — game balancing and economy design tools",
      "som": "$45M — AI meta-balancing debaters"
    },
    {
      "id": "games-screenreader-flow-ghost-7",
      "theme": "games",
      "title": "Screenreader Flow Ghost",
      "pitch": "Lovable AI simulates visual impairment so designers test menu navigation; AIsa writes it.",
      "subDiscipline": "UI accessibility testing",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for UI accessibility testing in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Screenreader Flow Ghost\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI simulates visual impairment so designers test menu navigation; AIsa writes it.\nDiscipline: Game Design & Interactive Media (UI accessibility testing).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for UI accessibility testing in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for UI accessibility testing. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for UI accessibility testing.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$9B — game testing and QA software market",
      "sam": "$2.5B — automated UX and accessibility testing",
      "som": "$45M — AI accessibility simulators for game UI"
    },
    {
      "id": "games-choice-tree-pruner-8",
      "theme": "games",
      "title": "Choice Tree Pruner",
      "pitch": "Lovable AI evaluates branching consequences so writers tighten narrative paths; AIsa writes it.",
      "subDiscipline": "interactive fiction pacing",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for interactive fiction pacing in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Choice Tree Pruner\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI evaluates branching consequences so writers tighten narrative paths; AIsa writes it.\nDiscipline: Game Design & Interactive Media (interactive fiction pacing).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for interactive fiction pacing in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for interactive fiction pacing. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for interactive fiction pacing.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — global game development software market",
      "sam": "$1.5B — interactive fiction and branching narrative tools",
      "som": "$40M — AI narrative tree pruners"
    },
    {
      "id": "games-emotional-arc-surgeon-9",
      "theme": "games",
      "title": "Emotional Arc Surgeon",
      "pitch": "Lovable AI analyzes tension curves so designers adjust level pacing; AIsa writes it.",
      "subDiscipline": "level pacing critique",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for level pacing critique in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Emotional Arc Surgeon\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI analyzes tension curves so designers adjust level pacing; AIsa writes it.\nDiscipline: Game Design & Interactive Media (level pacing critique).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for level pacing critique in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for level pacing critique. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for level pacing critique.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — game engines and level editors market",
      "sam": "$2B — level design and pacing analytics tools",
      "som": "$50M — AI pacing critique companions"
    },
    {
      "id": "games-confused-rookie-simulator-10",
      "theme": "games",
      "title": "Confused Rookie Simulator",
      "pitch": "Lovable AI roleplays a lost player so designers refine tutorials; AIsa writes it.",
      "subDiscipline": "onboarding UX testing",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for onboarding UX testing in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Confused Rookie Simulator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI roleplays a lost player so designers refine tutorials; AIsa writes it.\nDiscipline: Game Design & Interactive Media (onboarding UX testing).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for onboarding UX testing in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for onboarding UX testing. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for onboarding UX testing.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$9B — game testing and QA software market",
      "sam": "$2B — user onboarding and tutorial testing tools",
      "som": "$35M — AI UX rookie simulators"
    },
    {
      "id": "games-foley-scene-painter-11",
      "theme": "games",
      "title": "Foley Scene Painter",
      "pitch": "Lovable AI describes environmental acoustics so audio engineers gather foley; AIsa writes it.",
      "subDiscipline": "ambient sound direction",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for ambient sound direction in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Foley Scene Painter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI describes environmental acoustics so audio engineers gather foley; AIsa writes it.\nDiscipline: Game Design & Interactive Media (ambient sound direction).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for ambient sound direction in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for ambient sound direction. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for ambient sound direction.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$12B — game audio and XR middleware market",
      "sam": "$2.5B — foley and sound effect libraries",
      "som": "$50M — AI foley direction assistants"
    },
    {
      "id": "games-esports-caster-hype-12",
      "theme": "games",
      "title": "Esports Caster Hype",
      "pitch": "Lovable AI writes energetic match commentary so creators rehearse trailers; AIsa writes it.",
      "subDiscipline": "trailer narrative rehearsal",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for trailer narrative rehearsal in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Esports Caster Hype\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI writes energetic match commentary so creators rehearse trailers; AIsa writes it.\nDiscipline: Game Design & Interactive Media (trailer narrative rehearsal).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for trailer narrative rehearsal in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for trailer narrative rehearsal. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for trailer narrative rehearsal.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$6B — esports and game marketing software",
      "sam": "$1.5B — trailer production and narrative tools",
      "som": "$30M — AI shoutcast rehearsal coaches"
    },
    {
      "id": "games-vestibular-sickness-oracle-13",
      "theme": "games",
      "title": "Vestibular Sickness Oracle",
      "pitch": "Lovable AI predicts motion sickness triggers so VR devs adjust locomotion; AIsa writes it.",
      "subDiscipline": "VR locomotion safety",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for VR locomotion safety in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Vestibular Sickness Oracle\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI predicts motion sickness triggers so VR devs adjust locomotion; AIsa writes it.\nDiscipline: Game Design & Interactive Media (VR locomotion safety).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for VR locomotion safety in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for VR locomotion safety. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for VR locomotion safety.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$12B — game audio and XR middleware market",
      "sam": "$3B — VR locomotion and comfort plugins",
      "som": "$60M — AI VR safety predictors"
    },
    {
      "id": "games-tavern-keeper-mask-14",
      "theme": "games",
      "title": "Tavern Keeper Mask",
      "pitch": "Lovable AI embodies grumpy merchants so writers test dialogue tone; AIsa writes it.",
      "subDiscipline": "quest giver characterization",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for quest giver characterization in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Tavern Keeper Mask\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI embodies grumpy merchants so writers test dialogue tone; AIsa writes it.\nDiscipline: Game Design & Interactive Media (quest giver characterization).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for quest giver characterization in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for quest giver characterization. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for quest giver characterization.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — global game development software market",
      "sam": "$1.5B — narrative design and dialogue tools",
      "som": "$40M — AI NPC persona actors"
    },
    {
      "id": "games-storefront-hook-polisher-15",
      "theme": "games",
      "title": "Storefront Hook Polisher",
      "pitch": "Lovable AI sharpens elevator pitches so developers record store trailers; AIsa writes it.",
      "subDiscipline": "indie game marketing",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for indie game marketing in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Storefront Hook Polisher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI sharpens elevator pitches so developers record store trailers; AIsa writes it.\nDiscipline: Game Design & Interactive Media (indie game marketing).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for indie game marketing in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for indie game marketing. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for indie game marketing.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$6B — esports and game marketing software",
      "sam": "$1.2B — indie game storefront optimization tools",
      "som": "$25M — AI pitch rehearsal companions"
    },
    {
      "id": "games-parry-timing-metronome-16",
      "theme": "games",
      "title": "Parry Timing Metronome",
      "pitch": "Lovable AI calculates hit-stop frames so animators choreograph combat; AIsa writes it.",
      "subDiscipline": "action combat choreography",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for action combat choreography in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Parry Timing Metronome\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI calculates hit-stop frames so animators choreograph combat; AIsa writes it.\nDiscipline: Game Design & Interactive Media (action combat choreography).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for action combat choreography in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for action combat choreography. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for action combat choreography.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — game engines and animation middleware",
      "sam": "$2.5B — combat design and choreography tools",
      "som": "$45M — AI combat rhythm callers"
    },
    {
      "id": "games-cultural-nuance-translator-17",
      "theme": "games",
      "title": "Cultural Nuance Translator",
      "pitch": "Lovable AI checks translated idioms so localization teams preserve emotional weight; AIsa writes it.",
      "subDiscipline": "dialogue localization QA",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for dialogue localization QA in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Cultural Nuance Translator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI checks translated idioms so localization teams preserve emotional weight; AIsa writes it.\nDiscipline: Game Design & Interactive Media (dialogue localization QA).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for dialogue localization QA in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for dialogue localization QA. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for dialogue localization QA.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — global game development software market",
      "sam": "$2B — game localization and translation QA",
      "som": "$50M — AI localization tone checkers"
    },
    {
      "id": "games-roguelike-dungeon-caller-18",
      "theme": "games",
      "title": "Roguelike Dungeon Caller",
      "pitch": "Lovable AI narrates generated room layouts so designers test roguelike flow; AIsa writes it.",
      "subDiscipline": "procedural generation narrative",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for procedural generation narrative in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Roguelike Dungeon Caller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI narrates generated room layouts so designers test roguelike flow; AIsa writes it.\nDiscipline: Game Design & Interactive Media (procedural generation narrative).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for procedural generation narrative in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for procedural generation narrative. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for procedural generation narrative.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — game engines and level editors market",
      "sam": "$1.5B — procedural generation and roguelike tools",
      "som": "$35M — AI dungeon flow narrators"
    },
    {
      "id": "games-casual-gamer-proxy-19",
      "theme": "games",
      "title": "Casual Gamer Proxy",
      "pitch": "Lovable AI mimics casual players so hardcore designers test difficulty; AIsa writes it.",
      "subDiscipline": "player demographic simulation",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for player demographic simulation in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Casual Gamer Proxy\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI mimics casual players so hardcore designers test difficulty; AIsa writes it.\nDiscipline: Game Design & Interactive Media (player demographic simulation).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for player demographic simulation in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for player demographic simulation. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for player demographic simulation.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$9B — game testing and QA software market",
      "sam": "$2B — player demographic and difficulty testing",
      "som": "$40M — AI player empathy simulators"
    },
    {
      "id": "games-glitch-route-navigator-20",
      "theme": "games",
      "title": "Glitch Route Navigator",
      "pitch": "Lovable AI maps optimal movement paths so speedrunners plan routes; AIsa writes it.",
      "subDiscipline": "speedrun sequence planning",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for speedrun sequence planning in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Glitch Route Navigator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI maps optimal movement paths so speedrunners plan routes; AIsa writes it.\nDiscipline: Game Design & Interactive Media (speedrun sequence planning).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for speedrun sequence planning in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for speedrun sequence planning. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for speedrun sequence planning.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — esports and streaming companion market",
      "sam": "$1B — speedrunning and routing tools",
      "som": "$20M — AI speedrun route callers"
    },
    {
      "id": "games-prose-pacing-director-21",
      "theme": "games",
      "title": "Prose Pacing Director",
      "pitch": "Lovable AI edits descriptive paragraphs so interactive fiction authors improve readability; AIsa writes it.",
      "subDiscipline": "text adventure answer",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for text adventure narration in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Prose Pacing Director\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI edits descriptive paragraphs so interactive fiction authors improve readability; AIsa writes it.\nDiscipline: Game Design & Interactive Media (text adventure answer).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for text adventure narration in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for text adventure answer. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for text adventure answer.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — global game development software market",
      "sam": "$1B — interactive fiction and text adventure engines",
      "som": "$25M — AI prose pacing directors"
    },
    {
      "id": "games-voice-chat-referee-22",
      "theme": "games",
      "title": "Voice Chat Referee",
      "pitch": "Lovable AI generates toxic banter so developers train moderation filters; AIsa writes it.",
      "subDiscipline": "multiplayer moderation training",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for multiplayer moderation training in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Voice Chat Referee\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI generates toxic banter so developers train moderation filters; AIsa writes it.\nDiscipline: Game Design & Interactive Media (multiplayer moderation training).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for multiplayer moderation training in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for multiplayer moderation training. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for multiplayer moderation training.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$9B — game testing and QA software market",
      "sam": "$2.5B — multiplayer moderation and safety tools",
      "som": "$50M — AI toxicity simulation referees"
    },
    {
      "id": "games-jam-scope-guillotine-23",
      "theme": "games",
      "title": "Jam Scope Guillotine",
      "pitch": "Lovable AI ruthlessly cuts features so jam teams finish on time; AIsa writes it.",
      "subDiscipline": "game jam feature cutting",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for game jam feature cutting in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Jam Scope Guillotine\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI ruthlessly cuts features so jam teams finish on time; AIsa writes it.\nDiscipline: Game Design & Interactive Media (game jam feature cutting).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for game jam feature cutting in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for game jam feature cutting. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for game jam feature cutting.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$15B — project management and productivity software",
      "sam": "$3B — game jam and rapid prototyping tools",
      "som": "$60M — AI scope management cops"
    },
    {
      "id": "games-launch-autopsy-interviewer-24",
      "theme": "games",
      "title": "Launch Autopsy Interviewer",
      "pitch": "Lovable AI asks probing review questions so teams analyze launch failures; AIsa writes it.",
      "subDiscipline": "development post-mortem",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for development post-mortem in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Launch Autopsy Interviewer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI asks probing review questions so teams analyze launch failures; AIsa writes it.\nDiscipline: Game Design & Interactive Media (development post-mortem).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for development post-mortem in Game Design & Interactive Media — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for development post-mortem. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nSwap the model to `anthropic/claude-3-5-sonnet`, `google/gemini-2.5-flash`, or\n`qwen/qwen2.5-72b` from https://aisa.one/models — same endpoint, same body.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for development post-mortem.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$15B — project management and productivity software",
      "sam": "$4B — game development analytics and post-mortem tools",
      "som": "$80M — AI launch autopsy interviewers"
    },
    {
      "id": "games-playtest-whisperer-0",
      "theme": "games",
      "title": "Playtest Whisperer",
      "pitch": "Lovable AI analyzes player feedback so designers fix friction; AIsa captures it.",
      "subDiscipline": "UX research",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Realtime STT logs exact moments players express confusion during live playtests.",
      "megaPrompt": "Build \"Playtest Whisperer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI analyzes player feedback so designers fix friction; AIsa captures it.\nDiscipline: Game Design & Interactive Media (UX research).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Realtime STT logs exact moments players express confusion during live playtests.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for UX research.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for UX research. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for UX research.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — global game UX research",
      "sam": "$2B — indie playtesting tools",
      "som": "$50M — think-aloud protocol software"
    },
    {
      "id": "games-dungeon-scribe-1",
      "theme": "games",
      "title": "Dungeon Live Notes",
      "pitch": "Lovable AI organizes campaign lore so game masters prep faster; AIsa captures it.",
      "subDiscipline": "tabletop campaign logging",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Realtime STT records improvised GM dialogue and player actions mid-session.",
      "megaPrompt": "Build \"Dungeon Live Notes\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI organizes campaign lore so game masters prep faster; AIsa captures it.\nDiscipline: Game Design & Interactive Media (tabletop campaign logging).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Realtime STT records improvised GM dialogue and player actions mid-session.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for tabletop campaign logging.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for tabletop campaign logging. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for tabletop campaign logging.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$15B — tabletop gaming market",
      "sam": "$3B — digital TTRPG companions",
      "som": "$100M — session transcription apps"
    },
    {
      "id": "games-split-catcher-2",
      "theme": "games",
      "title": "Split Catcher",
      "pitch": "Lovable AI highlights peak hype moments so editors cut VODs; AIsa captures it.",
      "subDiscipline": "esports broadcasting",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Realtime STT timestamps vocal spikes to auto-mark speedrun highlights.",
      "megaPrompt": "Build \"Split Catcher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI highlights peak hype moments so editors cut VODs; AIsa captures it.\nDiscipline: Game Design & Interactive Media (esports broadcasting).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Realtime STT timestamps vocal spikes to auto-mark speedrun highlights.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for esports broadcasting.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for esports broadcasting. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for esports broadcasting.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — esports broadcasting software",
      "sam": "$800M — speedrunning community tools",
      "som": "$20M — VOD editing assistants"
    },
    {
      "id": "games-indie-monologue-3",
      "theme": "games",
      "title": "Indie Monologue",
      "pitch": "Lovable AI structures daily rants so devs publish polished blogs; AIsa captures it.",
      "subDiscipline": "developer marketing",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "AIsa Skills (web / scholar / trends / market data) ground developer marketing in Game Design & Interactive Media with real-world signal so the LLM's output is sourced, not invented.",
      "megaPrompt": "Build \"Indie Monologue\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI structures daily rants so devs publish polished blogs; AIsa captures it.\nDiscipline: Game Design & Interactive Media (developer marketing).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: AIsa Skills (web / scholar / trends / market data) ground developer marketing in Game Design & Interactive Media with real-world signal so the LLM's output is sourced, not invented.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for developer marketing.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for developer marketing. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for developer marketing.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$12B — creator economy software",
      "sam": "$1B — indie dev marketing tools",
      "som": "$40M — devlog generation platforms"
    },
    {
      "id": "games-spatial-echo-4",
      "theme": "games",
      "title": "Spatial Echo",
      "pitch": "Lovable AI maps voice intents so XR devs build menus; AIsa captures it.",
      "subDiscipline": "XR interaction design",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "AIsa Skills (web / scholar / trends / market data) ground XR interaction design in Game Design & Interactive Media with real-world signal so the LLM's output is sourced, not invented.",
      "megaPrompt": "Build \"Spatial Echo\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI maps voice intents so XR devs build menus; AIsa captures it.\nDiscipline: Game Design & Interactive Media (XR interaction design).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: AIsa Skills (web / scholar / trends / market data) ground XR interaction design in Game Design & Interactive Media with real-world signal so the LLM's output is sourced, not invented.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for XR interaction design.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for XR interaction design. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for XR interaction design.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$25B — XR development software",
      "sam": "$5B — spatial UI prototyping",
      "som": "$150M — voice interaction datasets"
    },
    {
      "id": "games-barksmith-jam-5",
      "theme": "games",
      "title": "Barksmith Jam",
      "pitch": "Lovable AI formats improv lines so writers populate NPC barks; AIsa captures it.",
      "subDiscipline": "narrative design",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "AIsa Skills (web / scholar / trends / market data) ground narrative design in Game Design & Interactive Media with real-world signal so the LLM's output is sourced, not invented.",
      "megaPrompt": "Build \"Barksmith Jam\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI formats improv lines so writers populate NPC barks; AIsa captures it.\nDiscipline: Game Design & Interactive Media (narrative design).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: AIsa Skills (web / scholar / trends / market data) ground narrative design in Game Design & Interactive Media with real-world signal so the LLM's output is sourced, not invented.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for narrative design.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for narrative design. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for narrative design.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — game narrative tools",
      "sam": "$2B — indie dialogue editors",
      "som": "$60M — voice-to-text game engines"
    },
    {
      "id": "games-walkthrough-whisper-6",
      "theme": "games",
      "title": "Walkthrough Whisper",
      "pitch": "Lovable AI critiques spatial flow so level designers adjust pacing; AIsa captures it.",
      "subDiscipline": "environment art",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Realtime STT records designer commentary while physically walking through greybox levels.",
      "megaPrompt": "Build \"Walkthrough Whisper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI critiques spatial flow so level designers adjust pacing; AIsa captures it.\nDiscipline: Game Design & Interactive Media (environment art).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Realtime STT records designer commentary while physically walking through greybox levels.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for environment art.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for environment art. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for environment art.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$6B — 3D level design software",
      "sam": "$1.5B — environment art tools",
      "som": "$30M — spatial pacing analytics"
    },
    {
      "id": "games-foley-mimic-7",
      "theme": "games",
      "title": "Foley Mimic",
      "pitch": "Lovable AI categorizes vocalized sound effects so audio devs tag libraries; AIsa captures it.",
      "subDiscipline": "audio engineering",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "AIsa Skills (web / scholar / trends / market data) ground audio engineering in Game Design & Interactive Media with real-world signal so the LLM's output is sourced, not invented.",
      "megaPrompt": "Build \"Foley Mimic\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI categorizes vocalized sound effects so audio devs tag libraries; AIsa captures it.\nDiscipline: Game Design & Interactive Media (audio engineering).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: AIsa Skills (web / scholar / trends / market data) ground audio engineering in Game Design & Interactive Media with real-world signal so the LLM's output is sourced, not invented.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for audio engineering.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for audio engineering. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for audio engineering.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — game audio middleware",
      "sam": "$800M — indie sound design tools",
      "som": "$25M — foley tagging software"
    },
    {
      "id": "games-jam-huddle-8",
      "theme": "games",
      "title": "Jam Huddle",
      "pitch": "Lovable AI extracts core mechanics so jam teams align scopes; AIsa captures it.",
      "subDiscipline": "game production",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Realtime STT logs chaotic brainstorming to isolate actionable game design pillars.",
      "megaPrompt": "Build \"Jam Huddle\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI extracts core mechanics so jam teams align scopes; AIsa captures it.\nDiscipline: Game Design & Interactive Media (game production).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Realtime STT logs chaotic brainstorming to isolate actionable game design pillars.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for game production.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for game production. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for game production.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — project management software",
      "sam": "$500M — game jam coordination",
      "som": "$15M — hackathon scoping tools"
    },
    {
      "id": "games-mythos-murmur-9",
      "theme": "games",
      "title": "Mythos Murmur",
      "pitch": "Lovable AI cross-references dictated history so writers maintain canon; AIsa captures it.",
      "subDiscipline": "worldbuilding",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Realtime STT allows worldbuilders to speak lore while sketching concept art.",
      "megaPrompt": "Build \"Mythos Murmur\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI cross-references dictated history so writers maintain canon; AIsa captures it.\nDiscipline: Game Design & Interactive Media (worldbuilding).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Realtime STT allows worldbuilders to speak lore while sketching concept art.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for worldbuilding.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for worldbuilding. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for worldbuilding.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — creative writing software",
      "sam": "$1B — game lore wikis",
      "som": "$35M — worldbuilding assistants"
    },
    {
      "id": "games-onboarding-echo-10",
      "theme": "games",
      "title": "Onboarding Echo",
      "pitch": "Lovable AI identifies confusing steps so designers rewrite tutorials; AIsa captures it.",
      "subDiscipline": "UX onboarding",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Realtime STT catches exact moments players vocalize frustration during onboarding.",
      "megaPrompt": "Build \"Onboarding Echo\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI identifies confusing steps so designers rewrite tutorials; AIsa captures it.\nDiscipline: Game Design & Interactive Media (UX onboarding).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Realtime STT catches exact moments players vocalize frustration during onboarding.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for UX onboarding.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for UX onboarding. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for UX onboarding.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$7B — game analytics platforms",
      "sam": "$1.2B — tutorial optimization tools",
      "som": "$40M — onboarding UX software"
    },
    {
      "id": "games-branch-weaver-11",
      "theme": "games",
      "title": "Branch Weaver",
      "pitch": "Lovable AI structures written choices so authors build branching trees; AIsa captures it.",
      "subDiscipline": "text adventure design",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Realtime STT maps vocalized what-if scenarios directly into node-based logic.",
      "megaPrompt": "Build \"Branch Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI structures written choices so authors build branching trees; AIsa captures it.\nDiscipline: Game Design & Interactive Media (text adventure design).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Realtime STT maps vocalized what-if scenarios directly into node-based logic.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for text adventure design.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for text adventure design. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for text adventure design.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — interactive fiction engines",
      "sam": "$800M — narrative game tools",
      "som": "$20M — branching dialogue mappers"
    },
    {
      "id": "games-rulebook-referee-12",
      "theme": "games",
      "title": "Rulebook Referee",
      "pitch": "Lovable AI clarifies ambiguous mechanics so designers rewrite rulebooks; AIsa captures it.",
      "subDiscipline": "tabletop design",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Realtime STT records player arguments over rules to identify design flaws.",
      "megaPrompt": "Build \"Rulebook Referee\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI clarifies ambiguous mechanics so designers rewrite rulebooks; AIsa captures it.\nDiscipline: Game Design & Interactive Media (tabletop design).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Realtime STT records player arguments over rules to identify design flaws.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for tabletop design.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for tabletop design. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for tabletop design.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$12B — board game publishing",
      "sam": "$2B — tabletop design software",
      "som": "$50M — playtesting feedback tools"
    },
    {
      "id": "games-debrief-scribe-13",
      "theme": "games",
      "title": "Debrief Live Notes",
      "pitch": "Lovable AI summarizes learning outcomes so facilitators grade trainees; AIsa captures it.",
      "subDiscipline": "edtech simulation",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Realtime STT logs post-simulation discussions for automated educational assessment.",
      "megaPrompt": "Build \"Debrief Live Notes\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI summarizes learning outcomes so facilitators grade trainees; AIsa captures it.\nDiscipline: Game Design & Interactive Media (edtech simulation).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Realtime STT logs post-simulation discussions for automated educational assessment.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for edtech simulation.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for edtech simulation. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for edtech simulation.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$20B — serious games market",
      "sam": "$4B — corporate training sims",
      "som": "$100M — simulation debrief software"
    },
    {
      "id": "games-hitstop-hustle-14",
      "theme": "games",
      "title": "Hitstop Hustle",
      "pitch": "Lovable AI correlates vocal reactions so designers tweak hitstop frames; AIsa captures it.",
      "subDiscipline": "game feel tuning",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Realtime STT timestamps player grunts and cheers to measure combat impact.",
      "megaPrompt": "Build \"Hitstop Hustle\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI correlates vocal reactions so designers tweak hitstop frames; AIsa captures it.\nDiscipline: Game Design & Interactive Media (game feel tuning).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Realtime STT timestamps player grunts and cheers to measure combat impact.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for game feel tuning.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for game feel tuning. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for game feel tuning.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$9B — game engine plugins",
      "sam": "$1.5B — combat design tools",
      "som": "$30M — game feel analytics"
    },
    {
      "id": "games-zen-prompter-15",
      "theme": "games",
      "title": "Zen Prompter",
      "pitch": "Lovable AI adapts calming scripts so XR guides pace breathwork; AIsa captures it.",
      "subDiscipline": "wellness XR",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Realtime STT monitors user breathing and spoken affirmations to adjust VR pacing.",
      "megaPrompt": "Build \"Zen Prompter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI adapts calming scripts so XR guides pace breathwork; AIsa captures it.\nDiscipline: Game Design & Interactive Media (wellness XR).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Realtime STT monitors user breathing and spoken affirmations to adjust VR pacing.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for wellness XR.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for wellness XR. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for wellness XR.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$6B — digital wellness apps",
      "sam": "$1B — VR meditation software",
      "som": "$25M — biofeedback XR tools"
    },
    {
      "id": "games-puzzle-prompt-16",
      "theme": "games",
      "title": "Puzzle Prompt",
      "pitch": "Lovable AI generates contextual clues so game masters assist players; AIsa captures it.",
      "subDiscipline": "live ops design",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Realtime STT listens to player theories to trigger perfectly timed hints.",
      "megaPrompt": "Build \"Puzzle Prompt\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI generates contextual clues so game masters assist players; AIsa captures it.\nDiscipline: Game Design & Interactive Media (live ops design).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Realtime STT listens to player theories to trigger perfectly timed hints.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for live ops design.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for live ops design. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for live ops design.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — escape room software",
      "sam": "$600M — live puzzle operations",
      "som": "$15M — automated hint systems"
    },
    {
      "id": "games-seed-whisperer-17",
      "theme": "games",
      "title": "Seed Whisperer",
      "pitch": "Lovable AI translates written metaphors so devs generate terrain seeds; AIsa captures it.",
      "subDiscipline": "technical design",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Realtime STT converts abstract vocal descriptions into procedural generation parameters.",
      "megaPrompt": "Build \"Seed Whisperer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI translates written metaphors so devs generate terrain seeds; AIsa captures it.\nDiscipline: Game Design & Interactive Media (technical design).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Realtime STT converts abstract vocal descriptions into procedural generation parameters.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for technical design.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for technical design. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for technical design.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — procedural generation tools",
      "sam": "$1B — indie terrain editors",
      "som": "$20M — AI seed generators"
    },
    {
      "id": "games-rumble-dictation-18",
      "theme": "games",
      "title": "Rumble Dictation",
      "pitch": "Lovable AI maps vocalized rhythms so devs program haptic feedback; AIsa captures it.",
      "subDiscipline": "hardware UX",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Realtime STT captures beatboxed vibration patterns for controller rumble timelines.",
      "megaPrompt": "Build \"Rumble Dictation\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI maps vocalized rhythms so devs program haptic feedback; AIsa captures it.\nDiscipline: Game Design & Interactive Media (hardware UX).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Realtime STT captures beatboxed vibration patterns for controller rumble timelines.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for hardware UX.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for hardware UX. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for hardware UX.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — game peripheral software",
      "sam": "$800M — haptic design tools",
      "som": "$25M — controller feedback mappers"
    },
    {
      "id": "games-chat-conductor-19",
      "theme": "games",
      "title": "Chat Conductor",
      "pitch": "Lovable AI tallies written votes so streamers run live polls; AIsa captures it.",
      "subDiscipline": "live broadcasting",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Realtime STT parses audience shout-outs and verbal votes during chaotic streams.",
      "megaPrompt": "Build \"Chat Conductor\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI tallies written votes so streamers run live polls; AIsa captures it.\nDiscipline: Game Design & Interactive Media (live broadcasting).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Realtime STT parses audience shout-outs and verbal votes during chaotic streams.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for live broadcasting.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for live broadcasting. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for live broadcasting.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — streaming software market",
      "sam": "$2B — Twitch extension tools",
      "som": "$60M — live audience interaction"
    },
    {
      "id": "games-a11y-advocate-20",
      "theme": "games",
      "title": "A11y Advocate",
      "pitch": "Lovable AI flags inaccessible moments so devs add visual cues; AIsa captures it.",
      "subDiscipline": "inclusive design",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Realtime STT logs blind playtester audio descriptions to find visual gaps.",
      "megaPrompt": "Build \"A11y Advocate\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI flags inaccessible moments so devs add visual cues; AIsa captures it.\nDiscipline: Game Design & Interactive Media (inclusive design).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Realtime STT logs blind playtester audio descriptions to find visual gaps.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for inclusive design.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for inclusive design. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for inclusive design.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — game accessibility tools",
      "sam": "$1.5B — inclusive design software",
      "som": "$40M — audio description mapping"
    },
    {
      "id": "games-lip-sync-scribe-21",
      "theme": "games",
      "title": "Lip Sync Live Notes",
      "pitch": "Lovable AI adjusts translated phrasing so animators match lip flaps; AIsa captures it.",
      "subDiscipline": "translation VO",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Realtime STT times native speaker syllables to guide localization mouth animations.",
      "megaPrompt": "Build \"Lip Sync Live Notes\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI adjusts translated phrasing so animators match lip flaps; AIsa captures it.\nDiscipline: Game Design & Interactive Media (translation VO).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Realtime STT times native speaker syllables to guide localization mouth animations.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for translation VO.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for translation VO. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for translation VO.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$7B — game localization market",
      "sam": "$1.2B — VO sync software",
      "som": "$35M — lip flap timing tools"
    },
    {
      "id": "games-harmony-warden-22",
      "theme": "games",
      "title": "Harmony Warden",
      "pitch": "Lovable AI coaches positive phrasing so moderators de-escalate lobbies; AIsa captures it.",
      "subDiscipline": "community safety",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Realtime STT detects rising vocal aggression to trigger automated cooldowns.",
      "megaPrompt": "Build \"Harmony Warden\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI coaches positive phrasing so moderators de-escalate lobbies; AIsa captures it.\nDiscipline: Game Design & Interactive Media (community safety).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Realtime STT detects rising vocal aggression to trigger automated cooldowns.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for community safety.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for community safety. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for community safety.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — gaming moderation tools",
      "sam": "$1B — multiplayer safety software",
      "som": "$30M — voice toxicity filters"
    },
    {
      "id": "games-mocap-murmur-23",
      "theme": "games",
      "title": "Mocap Murmur",
      "pitch": "Lovable AI aligns dialogue takes so animators sync facial rigs; AIsa captures it.",
      "subDiscipline": "animation pipeline",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Realtime STT provides word-level timestamps to auto-sync mocap facial data.",
      "megaPrompt": "Build \"Mocap Murmur\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI aligns dialogue takes so animators sync facial rigs; AIsa captures it.\nDiscipline: Game Design & Interactive Media (animation pipeline).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Realtime STT provides word-level timestamps to auto-sync mocap facial data.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for animation pipeline.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for animation pipeline. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for animation pipeline.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$12B — 3D animation software",
      "sam": "$3B — game mocap pipelines",
      "som": "$80M — facial sync automation"
    },
    {
      "id": "games-strat-scribe-24",
      "theme": "games",
      "title": "Strat Live Notes",
      "pitch": "Lovable AI extracts tactical callouts so coaches build VOD reviews; AIsa captures it.",
      "subDiscipline": "competitive coaching",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Realtime STT logs in-game team comms to analyze strategic communication flaws.",
      "megaPrompt": "Build \"Strat Live Notes\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI extracts tactical callouts so coaches build VOD reviews; AIsa captures it.\nDiscipline: Game Design & Interactive Media (competitive coaching).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Realtime STT logs in-game team comms to analyze strategic communication flaws.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for competitive coaching.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"openai/gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for competitive coaching. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for competitive coaching.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$6B — esports coaching platforms",
      "sam": "$1.5B — competitive VOD review",
      "som": "$45M — team comms analytics"
    },
    {
      "id": "games-biome-breath-crafter-0",
      "theme": "games",
      "title": "Biome Breath Crafter",
      "pitch": "Lovable AI designs layered forest ambience prompts so level artists; AIsa scores it.",
      "subDiscipline": "environmental soundscape design",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "Generative SFX excels at blending organic environmental textures into seamless loops.",
      "megaPrompt": "Build \"Biome Breath Crafter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI designs layered forest ambience prompts so level artists; AIsa scores it.\nDiscipline: Game Design & Interactive Media (environmental soundscape design).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: Generative SFX excels at blending organic environmental textures into seamless loops.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"bytedance/seedream-3.0\",\n        prompt: `Concept art for environmental soundscape design: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nOther models: `openai/gpt-image-1` (text-fidelity), `bytedance/seedream-3.0`\n(default, fast + artistic). Browse https://aisa.one/models for the live list.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for environmental soundscape design.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$5B — game audio middleware",
      "som": "$50M — indie environmental art tools"
    },
    {
      "id": "games-menu-click-alchemist-1",
      "theme": "games",
      "title": "Menu Click Alchemist",
      "pitch": "Lovable AI invents satisfying menu interaction cues so UX designers; AIsa scores it.",
      "subDiscipline": "interface audio feedback",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "SFX generation rapidly prototypes micro-interaction sounds crucial for tactile UI feel.",
      "megaPrompt": "Build \"Menu Click Alchemist\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI invents satisfying menu interaction cues so UX designers; AIsa scores it.\nDiscipline: Game Design & Interactive Media (interface audio feedback).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: SFX generation rapidly prototypes micro-interaction sounds crucial for tactile UI feel.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"bytedance/seedream-3.0\",\n        prompt: `Concept art for interface audio feedback: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nOther models: `openai/gpt-image-1` (text-fidelity), `bytedance/seedream-3.0`\n(default, fast + artistic). Browse https://aisa.one/models for the live list.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for interface audio feedback.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$2B — UI/UX design software",
      "som": "$20M — indie game UI toolkits"
    },
    {
      "id": "games-sword-clash-composer-2",
      "theme": "games",
      "title": "Sword Clash Composer",
      "pitch": "Lovable AI choreographs weapon impact sequences so animators; AIsa scores it.",
      "subDiscipline": "combat foley generation",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "Text-to-SFX translates kinetic combat descriptions into precise, punchy audio hits.",
      "megaPrompt": "Build \"Sword Clash Composer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI choreographs weapon impact sequences so animators; AIsa scores it.\nDiscipline: Game Design & Interactive Media (combat foley generation).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: Text-to-SFX translates kinetic combat descriptions into precise, punchy audio hits.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"bytedance/seedream-3.0\",\n        prompt: `Concept art for combat foley generation: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nOther models: `openai/gpt-image-1` (text-fidelity), `bytedance/seedream-3.0`\n(default, fast + artistic). Browse https://aisa.one/models for the live list.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for combat foley generation.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$4B — game engine asset stores",
      "som": "$40M — indie action game dev tools"
    },
    {
      "id": "games-boss-theme-weaver-3",
      "theme": "games",
      "title": "Boss Theme Weaver",
      "pitch": "Lovable AI structures multi-phase boss encounter music so composers; AIsa scores it.",
      "subDiscipline": "adaptive battle scoring",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "Music generation creates distinct, high-energy stems needed for adaptive combat systems.",
      "megaPrompt": "Build \"Boss Theme Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI structures multi-phase boss encounter music so composers; AIsa scores it.\nDiscipline: Game Design & Interactive Media (adaptive battle scoring).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: Music generation creates distinct, high-energy stems needed for adaptive combat systems.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"bytedance/seedream-3.0\",\n        prompt: `Concept art for adaptive battle scoring: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nOther models: `openai/gpt-image-1` (text-fidelity), `bytedance/seedream-3.0`\n(default, fast + artistic). Browse https://aisa.one/models for the live list.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for adaptive battle scoring.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$3B — game audio production",
      "som": "$30M — indie RPG music licensing"
    },
    {
      "id": "games-dread-pulse-architect-4",
      "theme": "games",
      "title": "Dread Pulse Architect",
      "pitch": "Lovable AI maps psychological horror soundscapes so narrative designers; AIsa scores it.",
      "subDiscipline": "horror tension building",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "SFX generation specializes in eerie, unpredictable textures that trigger player anxiety.",
      "megaPrompt": "Build \"Dread Pulse Architect\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI maps psychological horror soundscapes so narrative designers; AIsa scores it.\nDiscipline: Game Design & Interactive Media (horror tension building).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: SFX generation specializes in eerie, unpredictable textures that trigger player anxiety.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"bytedance/seedream-3.0\",\n        prompt: `Concept art for horror tension building: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nOther models: `openai/gpt-image-1` (text-fidelity), `bytedance/seedream-3.0`\n(default, fast + artistic). Browse https://aisa.one/models for the live list.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for horror tension building.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$1B — horror game development",
      "som": "$15M — indie horror audio assets"
    },
    {
      "id": "games-neon-drive-synth-5",
      "theme": "games",
      "title": "Neon Drive Synth",
      "pitch": "Lovable AI engineers futuristic engine hums so vehicle designers; AIsa scores it.",
      "subDiscipline": "sci-fi vehicle audio",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "Generative audio synthesizes complex, layered mechanical drones impossible to record naturally.",
      "megaPrompt": "Build \"Neon Drive Synth\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI engineers futuristic engine hums so vehicle designers; AIsa scores it.\nDiscipline: Game Design & Interactive Media (sci-fi vehicle audio).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: Generative audio synthesizes complex, layered mechanical drones impossible to record naturally.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"bytedance/seedream-3.0\",\n        prompt: `Concept art for sci-fi vehicle audio: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nOther models: `openai/gpt-image-1` (text-fidelity), `bytedance/seedream-3.0`\n(default, fast + artistic). Browse https://aisa.one/models for the live list.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for sci-fi vehicle audio.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$2B — sci-fi game assets",
      "som": "$25M — racing game sound design"
    },
    {
      "id": "games-spellcast-resonance-6",
      "theme": "games",
      "title": "Spellcast Resonance",
      "pitch": "Lovable AI describes elemental magic bursts so VFX artists; AIsa scores it.",
      "subDiscipline": "magic VFX audio",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "SFX models easily blend elemental concepts like fire and glass into magical impacts.",
      "megaPrompt": "Build \"Spellcast Resonance\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI describes elemental magic bursts so VFX artists; AIsa scores it.\nDiscipline: Game Design & Interactive Media (magic VFX audio).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: SFX models easily blend elemental concepts like fire and glass into magical impacts.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"bytedance/seedream-3.0\",\n        prompt: `Concept art for magic VFX audio: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nOther models: `openai/gpt-image-1` (text-fidelity), `bytedance/seedream-3.0`\n(default, fast + artistic). Browse https://aisa.one/models for the live list.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for magic VFX audio.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$5B — fantasy game development",
      "som": "$60M — indie magic system toolkits"
    },
    {
      "id": "games-blind-navigator-chime-7",
      "theme": "games",
      "title": "Blind Navigator Chime",
      "pitch": "Lovable AI designs distinct navigational earcons so accessibility leads; AIsa scores it.",
      "subDiscipline": "accessible spatial audio",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "Clean SFX generation ensures high-contrast, recognizable audio cues for visually impaired players.",
      "megaPrompt": "Build \"Blind Navigator Chime\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI designs distinct navigational earcons so accessibility leads; AIsa scores it.\nDiscipline: Game Design & Interactive Media (accessible spatial audio).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: Clean SFX generation ensures high-contrast, recognizable audio cues for visually impaired players.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"bytedance/seedream-3.0\",\n        prompt: `Concept art for accessible spatial audio: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nOther models: `openai/gpt-image-1` (text-fidelity), `bytedance/seedream-3.0`\n(default, fast + artistic). Browse https://aisa.one/models for the live list.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for accessible spatial audio.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$500M — accessibility software",
      "som": "$10M — inclusive design plugins"
    },
    {
      "id": "games-hearthside-loop-maker-8",
      "theme": "games",
      "title": "Hearthside Loop Maker",
      "pitch": "Lovable AI plans relaxing tavern background layers so indie devs; AIsa scores it.",
      "subDiscipline": "cozy game ambience",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "Music generation excels at producing warm, non-intrusive acoustic loops for idle states.",
      "megaPrompt": "Build \"Hearthside Loop Maker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI plans relaxing tavern background layers so indie devs; AIsa scores it.\nDiscipline: Game Design & Interactive Media (cozy game ambience).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: Music generation excels at producing warm, non-intrusive acoustic loops for idle states.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"bytedance/seedream-3.0\",\n        prompt: `Concept art for cozy game ambience: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nOther models: `openai/gpt-image-1` (text-fidelity), `bytedance/seedream-3.0`\n(default, fast + artistic). Browse https://aisa.one/models for the live list.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for cozy game ambience.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$3B — cozy game market",
      "som": "$35M — simulation game audio"
    },
    {
      "id": "games-haptic-ghost-echo-9",
      "theme": "games",
      "title": "Haptic Ghost Echo",
      "pitch": "Lovable AI scripts proximity-based ghost whispers so XR creators; AIsa scores it.",
      "subDiscipline": "VR spatial audio",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "Short SFX bursts are perfectly timed to trigger VR haptic feedback vests.",
      "megaPrompt": "Build \"Haptic Ghost Echo\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI scripts proximity-based ghost whispers so XR creators; AIsa scores it.\nDiscipline: Game Design & Interactive Media (VR spatial audio).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: Short SFX bursts are perfectly timed to trigger VR haptic feedback vests.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"bytedance/seedream-3.0\",\n        prompt: `Concept art for VR spatial audio: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nOther models: `openai/gpt-image-1` (text-fidelity), `bytedance/seedream-3.0`\n(default, fast + artistic). Browse https://aisa.one/models for the live list.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for VR spatial audio.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$50B — XR hardware and software",
      "sam": "$5B — VR game development",
      "som": "$50M — VR haptic audio tools"
    },
    {
      "id": "games-pixel-crunch-oscillator-10",
      "theme": "games",
      "title": "Pixel Crunch Oscillator",
      "pitch": "Lovable AI outlines 8-bit arcade motifs so retro developers; AIsa scores it.",
      "subDiscipline": "retro chiptune scoring",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "Music generation can emulate vintage synthesizer constraints for authentic retro soundtracks.",
      "megaPrompt": "Build \"Pixel Crunch Oscillator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI outlines 8-bit arcade motifs so retro developers; AIsa scores it.\nDiscipline: Game Design & Interactive Media (retro chiptune scoring).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: Music generation can emulate vintage synthesizer constraints for authentic retro soundtracks.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"bytedance/seedream-3.0\",\n        prompt: `Concept art for retro chiptune scoring: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nOther models: `openai/gpt-image-1` (text-fidelity), `bytedance/seedream-3.0`\n(default, fast + artistic). Browse https://aisa.one/models for the live list.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for retro chiptune scoring.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$2B — retro indie game market",
      "som": "$20M — chiptune asset stores"
    },
    {
      "id": "games-logic-lock-clicker-11",
      "theme": "games",
      "title": "Logic Lock Clicker",
      "pitch": "Lovable AI invents rewarding puzzle-solve chimes so level designers; AIsa scores it.",
      "subDiscipline": "puzzle feedback audio",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "SFX generation creates satisfying, dopamine-triggering micro-sounds for cognitive reward loops.",
      "megaPrompt": "Build \"Logic Lock Clicker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI invents rewarding puzzle-solve chimes so level designers; AIsa scores it.\nDiscipline: Game Design & Interactive Media (puzzle feedback audio).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: SFX generation creates satisfying, dopamine-triggering micro-sounds for cognitive reward loops.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"bytedance/seedream-3.0\",\n        prompt: `Concept art for puzzle feedback audio: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nOther models: `openai/gpt-image-1` (text-fidelity), `bytedance/seedream-3.0`\n(default, fast + artistic). Browse https://aisa.one/models for the live list.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for puzzle feedback audio.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$4B — puzzle game market",
      "som": "$40M — mobile puzzle audio"
    },
    {
      "id": "games-beast-roar-forge-12",
      "theme": "games",
      "title": "Beast Roar Forge",
      "pitch": "Lovable AI details monstrous vocal anatomy so creature artists; AIsa scores it.",
      "subDiscipline": "creature vocalization",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "SFX models blend animal samples and distortion to create unique alien roars.",
      "megaPrompt": "Build \"Beast Roar Forge\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI details monstrous vocal anatomy so creature artists; AIsa scores it.\nDiscipline: Game Design & Interactive Media (creature vocalization).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: SFX models blend animal samples and distortion to create unique alien roars.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"bytedance/seedream-3.0\",\n        prompt: `Concept art for creature vocalization: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nOther models: `openai/gpt-image-1` (text-fidelity), `bytedance/seedream-3.0`\n(default, fast + artistic). Browse https://aisa.one/models for the live list.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for creature vocalization.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$3B — creature design software",
      "som": "$30M — monster taming game assets"
    },
    {
      "id": "games-wasteland-radio-dj-13",
      "theme": "games",
      "title": "Wasteland Radio DJ",
      "pitch": "Lovable AI writes post-apocalyptic broadcast transitions so writers; AIsa scores it.",
      "subDiscipline": "diegetic radio scoring",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "Music generation produces lo-fi, degraded tracks perfect for diegetic in-game radios.",
      "megaPrompt": "Build \"Wasteland Radio DJ\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI writes post-apocalyptic broadcast transitions so writers; AIsa scores it.\nDiscipline: Game Design & Interactive Media (diegetic radio scoring).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: Music generation produces lo-fi, degraded tracks perfect for diegetic in-game radios.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"bytedance/seedream-3.0\",\n        prompt: `Concept art for diegetic radio scoring: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nOther models: `openai/gpt-image-1` (text-fidelity), `bytedance/seedream-3.0`\n(default, fast + artistic). Browse https://aisa.one/models for the live list.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for diegetic radio scoring.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$6B — narrative game development",
      "som": "$60M — open-world audio assets"
    },
    {
      "id": "games-stadium-swarm-dynamics-14",
      "theme": "games",
      "title": "Stadium Swarm Dynamics",
      "pitch": "Lovable AI models reactive crowd surges so sports simulators; AIsa scores it.",
      "subDiscipline": "sports crowd audio",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "SFX generation handles complex, multi-layered crowd noise that reacts to game states.",
      "megaPrompt": "Build \"Stadium Swarm Dynamics\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI models reactive crowd surges so sports simulators; AIsa scores it.\nDiscipline: Game Design & Interactive Media (sports crowd audio).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: SFX generation handles complex, multi-layered crowd noise that reacts to game states.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"bytedance/seedream-3.0\",\n        prompt: `Concept art for sports crowd audio: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nOther models: `openai/gpt-image-1` (text-fidelity), `bytedance/seedream-3.0`\n(default, fast + artistic). Browse https://aisa.one/models for the live list.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for sports crowd audio.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$8B — sports game market",
      "som": "$80M — sports audio middleware"
    },
    {
      "id": "games-shadow-step-foley-15",
      "theme": "games",
      "title": "Shadow Step Foley",
      "pitch": "Lovable AI choreographs surface-specific footfalls so stealth designers; AIsa scores it.",
      "subDiscipline": "stealth movement audio",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "SFX generation provides nuanced, material-specific sounds critical for stealth gameplay.",
      "megaPrompt": "Build \"Shadow Step Foley\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI choreographs surface-specific footfalls so stealth designers; AIsa scores it.\nDiscipline: Game Design & Interactive Media (stealth movement audio).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: SFX generation provides nuanced, material-specific sounds critical for stealth gameplay.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"bytedance/seedream-3.0\",\n        prompt: `Concept art for stealth movement audio: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nOther models: `openai/gpt-image-1` (text-fidelity), `bytedance/seedream-3.0`\n(default, fast + artistic). Browse https://aisa.one/models for the live list.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for stealth movement audio.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$2B — stealth game market",
      "som": "$25M — indie stealth foley packs"
    },
    {
      "id": "games-beatmap-drop-forger-16",
      "theme": "games",
      "title": "Beatmap Drop Forger",
      "pitch": "Lovable AI structures escalating EDM drops so rhythm designers; AIsa scores it.",
      "subDiscipline": "rhythm game scoring",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "Music generation creates precise, high-BPM electronic tracks ideal for beatmapping.",
      "megaPrompt": "Build \"Beatmap Drop Forger\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI structures escalating EDM drops so rhythm designers; AIsa scores it.\nDiscipline: Game Design & Interactive Media (rhythm game scoring).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: Music generation creates precise, high-BPM electronic tracks ideal for beatmapping.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"bytedance/seedream-3.0\",\n        prompt: `Concept art for rhythm game scoring: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nOther models: `openai/gpt-image-1` (text-fidelity), `bytedance/seedream-3.0`\n(default, fast + artistic). Browse https://aisa.one/models for the live list.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for rhythm game scoring.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$1B — rhythm game market",
      "som": "$15M — indie rhythm game audio"
    },
    {
      "id": "games-campfire-crackler-17",
      "theme": "games",
      "title": "Campfire Crackler",
      "pitch": "Lovable AI designs dynamic fire and wind layers so survival devs; AIsa scores it.",
      "subDiscipline": "survival crafting audio",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "SFX generation loops organic survival elements seamlessly without auditory fatigue.",
      "megaPrompt": "Build \"Campfire Crackler\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI designs dynamic fire and wind layers so survival devs; AIsa scores it.\nDiscipline: Game Design & Interactive Media (survival crafting audio).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: SFX generation loops organic survival elements seamlessly without auditory fatigue.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"bytedance/seedream-3.0\",\n        prompt: `Concept art for survival crafting audio: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nOther models: `openai/gpt-image-1` (text-fidelity), `bytedance/seedream-3.0`\n(default, fast + artistic). Browse https://aisa.one/models for the live list.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for survival crafting audio.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$4B — survival game market",
      "som": "$40M — crafting sim audio assets"
    },
    {
      "id": "games-bouncy-spring-synth-18",
      "theme": "games",
      "title": "Bouncy Spring Synth",
      "pitch": "Lovable AI invents whimsical jump and landing cues so platformer devs; AIsa scores it.",
      "subDiscipline": "platformer movement audio",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "SFX generation produces the exaggerated, cartoony sounds essential for platforming feel.",
      "megaPrompt": "Build \"Bouncy Spring Synth\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI invents whimsical jump and landing cues so platformer devs; AIsa scores it.\nDiscipline: Game Design & Interactive Media (platformer movement audio).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: SFX generation produces the exaggerated, cartoony sounds essential for platforming feel.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"bytedance/seedream-3.0\",\n        prompt: `Concept art for platformer movement audio: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nOther models: `openai/gpt-image-1` (text-fidelity), `bytedance/seedream-3.0`\n(default, fast + artistic). Browse https://aisa.one/models for the live list.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for platformer movement audio.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$3B — platformer game market",
      "som": "$30M — 2D indie game audio"
    },
    {
      "id": "games-epic-loot-shimmer-19",
      "theme": "games",
      "title": "Epic Loot Shimmer",
      "pitch": "Lovable AI engineers dopamine-sparking rarity chimes so economy designers; AIsa scores it.",
      "subDiscipline": "reward psychology audio",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "SFX generation crafts the exact frequency sweeps needed to trigger player reward responses.",
      "megaPrompt": "Build \"Epic Loot Shimmer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI engineers dopamine-sparking rarity chimes so economy designers; AIsa scores it.\nDiscipline: Game Design & Interactive Media (reward psychology audio).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: SFX generation crafts the exact frequency sweeps needed to trigger player reward responses.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"bytedance/seedream-3.0\",\n        prompt: `Concept art for reward psychology audio: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nOther models: `openai/gpt-image-1` (text-fidelity), `bytedance/seedream-3.0`\n(default, fast + artistic). Browse https://aisa.one/models for the live list.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for reward psychology audio.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$10B — live-service game economy",
      "som": "$100M — gacha audio design"
    },
    {
      "id": "games-mentor-chime-guide-20",
      "theme": "games",
      "title": "Mentor Chime Guide",
      "pitch": "Lovable AI scripts gentle corrective and success tones so onboarding designers; AIsa scores it.",
      "subDiscipline": "tutorial audio feedback",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "Clean SFX generation ensures tutorial cues are helpful without being annoying.",
      "megaPrompt": "Build \"Mentor Chime Guide\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI scripts gentle corrective and success tones so onboarding designers; AIsa scores it.\nDiscipline: Game Design & Interactive Media (tutorial audio feedback).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: Clean SFX generation ensures tutorial cues are helpful without being annoying.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"bytedance/seedream-3.0\",\n        prompt: `Concept art for tutorial audio feedback: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nOther models: `openai/gpt-image-1` (text-fidelity), `bytedance/seedream-3.0`\n(default, fast + artistic). Browse https://aisa.one/models for the live list.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for tutorial audio feedback.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$1B — UX onboarding tools",
      "som": "$10M — game tutorial asset packs"
    },
    {
      "id": "games-sonic-boom-weaver-21",
      "theme": "games",
      "title": "Sonic Boom Weaver",
      "pitch": "Lovable AI calculates aerodynamic stress and wind shear so flight simulators; AIsa scores it.",
      "subDiscipline": "flight sim audio",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "SFX generation simulates complex physics-based audio like sonic booms and cockpit rattles.",
      "megaPrompt": "Build \"Sonic Boom Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI calculates aerodynamic stress and wind shear so flight simulators; AIsa scores it.\nDiscipline: Game Design & Interactive Media (flight sim audio).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: SFX generation simulates complex physics-based audio like sonic booms and cockpit rattles.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"bytedance/seedream-3.0\",\n        prompt: `Concept art for flight sim audio: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nOther models: `openai/gpt-image-1` (text-fidelity), `bytedance/seedream-3.0`\n(default, fast + artistic). Browse https://aisa.one/models for the live list.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for flight sim audio.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$2B — simulation game market",
      "som": "$20M — flight sim audio mods"
    },
    {
      "id": "games-lobby-vibe-setter-22",
      "theme": "games",
      "title": "Lobby Vibe Setter",
      "pitch": "Lovable AI curates hype-building pre-match playlists so multiplayer producers; AIsa scores it.",
      "subDiscipline": "multiplayer lobby music",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "Music generation creates continuous, non-distracting background tracks for matchmaking queues.",
      "megaPrompt": "Build \"Lobby Vibe Setter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI curates hype-building pre-match playlists so multiplayer producers; AIsa scores it.\nDiscipline: Game Design & Interactive Media (multiplayer lobby music).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: Music generation creates continuous, non-distracting background tracks for matchmaking queues.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"bytedance/seedream-3.0\",\n        prompt: `Concept art for multiplayer lobby music: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nOther models: `openai/gpt-image-1` (text-fidelity), `bytedance/seedream-3.0`\n(default, fast + artistic). Browse https://aisa.one/models for the live list.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for multiplayer lobby music.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$15B — multiplayer game market",
      "som": "$150M — esports lobby audio"
    },
    {
      "id": "games-factory-hum-architect-23",
      "theme": "games",
      "title": "Factory Hum Architect",
      "pitch": "Lovable AI designs industrial machinery rhythms so RTS designers; AIsa scores it.",
      "subDiscipline": "base building ambience",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "SFX generation layers mechanical loops to convey scale and productivity in base builders.",
      "megaPrompt": "Build \"Factory Hum Architect\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI designs industrial machinery rhythms so RTS designers; AIsa scores it.\nDiscipline: Game Design & Interactive Media (base building ambience).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: SFX generation layers mechanical loops to convey scale and productivity in base builders.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"bytedance/seedream-3.0\",\n        prompt: `Concept art for base building ambience: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nOther models: `openai/gpt-image-1` (text-fidelity), `bytedance/seedream-3.0`\n(default, fast + artistic). Browse https://aisa.one/models for the live list.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for base building ambience.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$3B — strategy game market",
      "som": "$30M — automation game audio"
    },
    {
      "id": "games-defeat-dirge-drafter-24",
      "theme": "games",
      "title": "Defeat Dirge Drafter",
      "pitch": "Lovable AI composes emotionally resonant failure motifs so narrative directors; AIsa scores it.",
      "subDiscipline": "game over scoring",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "Music generation produces somber, reflective tracks that soften the blow of player defeat.",
      "megaPrompt": "Build \"Defeat Dirge Drafter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI composes emotionally resonant failure motifs so narrative directors; AIsa scores it.\nDiscipline: Game Design & Interactive Media (game over scoring).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: Music generation produces somber, reflective tracks that soften the blow of player defeat.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"bytedance/seedream-3.0\",\n        prompt: `Concept art for game over scoring: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nOther models: `openai/gpt-image-1` (text-fidelity), `bytedance/seedream-3.0`\n(default, fast + artistic). Browse https://aisa.one/models for the live list.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for game over scoring.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200B — global gaming market",
      "sam": "$6B — narrative game development",
      "som": "$60M — indie game soundtracks"
    }
  ]
}