{
  "theme": {
    "slug": "photography",
    "name": "Photography",
    "emoji": "📷",
    "audience": "photographers, photo editors, photojournalists",
    "market_anchor": "the photo software market (~$2.4B) and >15M pro photographers"
  },
  "ideas": [
    {
      "id": "photography-provenance-passport-0",
      "theme": "photography",
      "title": "Provenance Passport",
      "pitch": "Provenance Passport mints a did:prism for art documentation so identity travels with the person, not the platform.",
      "concept": "Track and verify the provenance of artworks through documented histories proven by photographers to galleries and collectors.",
      "subDiscipline": "art documentation",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits art documentation in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Provenance Passport\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTrack and verify the provenance of artworks through documented histories proven by photographers to galleries and collectors.\nDiscipline: Photography (art documentation).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits art documentation in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $400M — landscape photography apps · SOM $15M — premium scouting tool subscriptions\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$400M — landscape photography apps",
      "som": "$15M — premium scouting tool subscriptions"
    },
    {
      "id": "photography-credible-credits-0",
      "theme": "photography",
      "title": "Credible Credits",
      "pitch": "Credible Credits pairs two wallets over DIDComm so photojournalism can exchange trusted claims on a private channel.",
      "concept": "Photographers can send connection invitations to editors to verify their photo credits and ownership rights after an assignment.",
      "subDiscipline": "photojournalism",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits photojournalism in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Credible Credits\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can send connection invitations to editors to verify their photo credits and ownership rights after an assignment.\nDiscipline: Photography (photojournalism).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits photojournalism in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $800M — portrait studio software · SOM $25M — posing guide app revenues\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$800M — portrait studio software",
      "som": "$25M — posing guide app revenues"
    },
    {
      "id": "photography-photo-provenance-tracker-0",
      "theme": "photography",
      "title": "Photo Provenance Tracker",
      "pitch": "Photo Provenance Tracker issues a signed verifiable credential for art documentation that the holder keeps and reuses anywhere.",
      "concept": "Photographers can register their works' provenance, proving authorship and authenticity to galleries and buyers through verifiable credentials.",
      "subDiscipline": "art documentation",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches art documentation in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Photo Provenance Tracker\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can register their works' provenance, proving authorship and authenticity to galleries and buyers through verifiable credentials.\nDiscipline: Photography (art documentation).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches art documentation in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $300M — journalism field tools · SOM $10M — photojournalist productivity apps\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$300M — journalism field tools",
      "som": "$10M — photojournalist productivity apps"
    },
    {
      "id": "photography-portfolio-integrity-0",
      "theme": "photography",
      "title": "Portfolio Integrity",
      "pitch": "Portfolio Integrity verifies a credential at the gate for personal branding — proof without a phone call to the issuer.",
      "concept": "Photographers can present verified portfolios to clients showing their true work history and credentials without intermediaries.",
      "subDiscipline": "personal branding",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for personal branding in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Portfolio Integrity\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can present verified portfolios to clients showing their true work history and credentials without intermediaries.\nDiscipline: Photography (personal branding).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for personal branding in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $150M — camera inventory management · SOM $5M — gear tracking subscriptions\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$150M — camera inventory management",
      "som": "$5M — gear tracking subscriptions"
    },
    {
      "id": "photography-credited-clicks-0",
      "theme": "photography",
      "title": "Credited Clicks",
      "pitch": "Credited Clicks mints a did:prism for photojournalism so identity travels with the person, not the platform.",
      "concept": "Photographers receive automated credits for their published work, verified by editors to ensure proper attribution and payment.",
      "subDiscipline": "photojournalism",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits photojournalism in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Credited Clicks\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers receive automated credits for their published work, verified by editors to ensure proper attribution and payment.\nDiscipline: Photography (photojournalism).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits photojournalism in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $100M — analog photography supplies · SOM $8M — darkroom companion apps\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$100M — analog photography supplies",
      "som": "$8M — darkroom companion apps"
    },
    {
      "id": "photography-verified-prints-0",
      "theme": "photography",
      "title": "Verified Prints",
      "pitch": "Verified Prints pairs two wallets over DIDComm so fine art can exchange trusted claims on a private channel.",
      "concept": "Artists share a connection link with galleries to authenticate limited edition prints and validate artist signatures.",
      "subDiscipline": "fine art",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits fine art in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Verified Prints\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nArtists share a connection link with galleries to authenticate limited edition prints and validate artist signatures.\nDiscipline: Photography (fine art).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits fine art in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $900M — photo editing software · SOM $30M — workflow optimization plugins\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$900M — photo editing software",
      "som": "$30M — workflow optimization plugins"
    },
    {
      "id": "photography-credited-collaborators-0",
      "theme": "photography",
      "title": "Credited Collaborators",
      "pitch": "Credited Collaborators issues a signed verifiable credential for team projects that the holder keeps and reuses anywhere.",
      "concept": "Photographers can issue verifiable credentials to collaborators for their contributions, ensuring proper credit is recorded and easily shared with clients.",
      "subDiscipline": "team projects",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches team projects in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Credited Collaborators\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can issue verifiable credentials to collaborators for their contributions, ensuring proper credit is recorded and easily shared with clients.\nDiscipline: Photography (team projects).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches team projects in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $500M — studio management tools · SOM $12M — lighting tutorial platforms\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$500M — studio management tools",
      "som": "$12M — lighting tutorial platforms"
    },
    {
      "id": "photography-event-access-verifier-0",
      "theme": "photography",
      "title": "Event Access Verifier",
      "pitch": "Event Access Verifier verifies a credential at the gate for event photography — proof without a phone call to the issuer.",
      "concept": "Photographers can prove their authorized access to events using verified credentials, ensuring authenticity when shooting behind-the-scenes.",
      "subDiscipline": "event photography",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for event photography in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Event Access Verifier\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can prove their authorized access to events using verified credentials, ensuring authenticity when shooting behind-the-scenes.\nDiscipline: Photography (event photography).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for event photography in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $200M — nature photography guides · SOM $7M — wildlife tracking apps\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$200M — nature photography guides",
      "som": "$7M — wildlife tracking apps"
    },
    {
      "id": "photography-membership-validator-0",
      "theme": "photography",
      "title": "Membership Validator",
      "pitch": "Membership Validator mints a did:prism for photography guilds so identity travels with the person, not the platform.",
      "concept": "Members of photography guilds are verified through unique DIDs to access exclusive events and resources.",
      "subDiscipline": "photography guilds",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits photography guilds in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Membership Validator\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nMembers of photography guilds are verified through unique DIDs to access exclusive events and resources.\nDiscipline: Photography (photography guilds).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits photography guilds in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $120M — astronomy photography tools · SOM $4M — night sky planning apps\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$120M — astronomy photography tools",
      "som": "$4M — night sky planning apps"
    },
    {
      "id": "photography-membership-passport-0",
      "theme": "photography",
      "title": "Membership Passport",
      "pitch": "Membership Passport pairs two wallets over DIDComm so photo unions can exchange trusted claims on a private channel.",
      "concept": "Union members can prove their eligibility and membership status to event organizers using a DIDComm connection invitation.",
      "subDiscipline": "photo unions",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits photo unions in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Membership Passport\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nUnion members can prove their eligibility and membership status to event organizers using a DIDComm connection invitation.\nDiscipline: Photography (photo unions).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits photo unions in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $350M — commercial photography software · SOM $11M — food styling resources\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$350M — commercial photography software",
      "som": "$11M — food styling resources"
    },
    {
      "id": "photography-union-membership-verifier-0",
      "theme": "photography",
      "title": "Union Membership Verifier",
      "pitch": "Union Membership Verifier issues a signed verifiable credential for professional accreditation that the holder keeps and reuses anywhere.",
      "concept": "Freelance photographers can verify their union status through a credential, facilitating access to union benefits and job opportunities.",
      "subDiscipline": "professional accreditation",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches professional accreditation in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Union Membership Verifier\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFreelance photographers can verify their union status through a credential, facilitating access to union benefits and job opportunities.\nDiscipline: Photography (professional accreditation).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches professional accreditation in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $600M — real estate media tools · SOM $20M — property photography apps\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$600M — real estate media tools",
      "som": "$20M — property photography apps"
    },
    {
      "id": "photography-membership-validator-1",
      "theme": "photography",
      "title": "Membership Validator",
      "pitch": "Membership Validator verifies a credential at the gate for professional associations — proof without a phone call to the issuer.",
      "concept": "Photojournalists can present proof of membership in professional organizations to access exclusive resources and opportunities.",
      "subDiscipline": "professional associations",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for professional associations in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Membership Validator\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotojournalists can present proof of membership in professional organizations to access exclusive resources and opportunities.\nDiscipline: Photography (professional associations).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for professional associations in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $180M — specialized photography tools · SOM $6M — macro focusing aids\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$180M — specialized photography tools",
      "som": "$6M — macro focusing aids"
    },
    {
      "id": "photography-access-pass-0",
      "theme": "photography",
      "title": "Access Pass",
      "pitch": "Access Pass mints a did:prism for event photography so identity travels with the person, not the platform.",
      "concept": "Event photographers receive digital access passes verified by event organizers to access exclusive areas and backstage.",
      "subDiscipline": "event photography",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits event photography in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Access Pass\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nEvent photographers receive digital access passes verified by event organizers to access exclusive areas and backstage.\nDiscipline: Photography (event photography).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits event photography in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $700M — wedding photography software · SOM $25M — event timeline apps\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$700M — wedding photography software",
      "som": "$25M — event timeline apps"
    },
    {
      "id": "photography-authenticity-checker-0",
      "theme": "photography",
      "title": "Authenticity Checker",
      "pitch": "Authenticity Checker pairs two wallets over DIDComm so documentary can exchange trusted claims on a private channel.",
      "concept": "Photojournalists can invite stakeholders to verify the provenance of images through a secure connection for authenticity checks.",
      "subDiscipline": "documentary",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits documentary in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Authenticity Checker\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotojournalists can invite stakeholders to verify the provenance of images through a secure connection for authenticity checks.\nDiscipline: Photography (documentary).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits documentary in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $250M — street photography resources · SOM $9M — travel photography guides\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$250M — street photography resources",
      "som": "$9M — travel photography guides"
    },
    {
      "id": "photography-client-trust-badge-0",
      "theme": "photography",
      "title": "Client Trust Badge",
      "pitch": "Client Trust Badge issues a signed verifiable credential for client relations that the holder keeps and reuses anywhere.",
      "concept": "Photographers can obtain a verifiable badge of trust from past clients, helping to enhance their credibility and attract new clients.",
      "subDiscipline": "client relations",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches client relations in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Client Trust Badge\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can obtain a verifiable badge of trust from past clients, helping to enhance their credibility and attract new clients.\nDiscipline: Photography (client relations).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches client relations in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $100M — dive photography gear · SOM $3M — scuba logging apps\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$100M — dive photography gear",
      "som": "$3M — scuba logging apps"
    },
    {
      "id": "photography-age-restricted-content-0",
      "theme": "photography",
      "title": "Age-Restricted Content",
      "pitch": "Age-Restricted Content verifies a credential at the gate for youth photography — proof without a phone call to the issuer.",
      "concept": "Photographers can verify their age when submitting work for youth-focused projects to comply with legal requirements.",
      "subDiscipline": "youth photography",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for youth photography in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Age-Restricted Content\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can verify their age when submitting work for youth-focused projects to comply with legal requirements.\nDiscipline: Photography (youth photography).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for youth photography in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $450M — drone flight software · SOM $15M — aerial cinematography tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$450M — drone flight software",
      "som": "$15M — aerial cinematography tools"
    },
    {
      "id": "photography-license-ledger-0",
      "theme": "photography",
      "title": "License Ledger",
      "pitch": "License Ledger mints a did:prism for stock photography so identity travels with the person, not the platform.",
      "concept": "Stock photographers can publish and prove licensing agreements directly to clients, ensuring transparent usage rights.",
      "subDiscipline": "stock photography",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits stock photography in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"License Ledger\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nStock photographers can publish and prove licensing agreements directly to clients, ensuring transparent usage rights.\nDiscipline: Photography (stock photography).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits stock photography in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $800M — photography business tools · SOM $30M — CRM and pitching software\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$800M — photography business tools",
      "som": "$30M — CRM and pitching software"
    },
    {
      "id": "photography-access-passes-0",
      "theme": "photography",
      "title": "Access Passes",
      "pitch": "Access Passes pairs two wallets over DIDComm so event photography can exchange trusted claims on a private channel.",
      "concept": "Photographers send connection invitations to event organizers to receive exclusive backstage access based on credentials.",
      "subDiscipline": "event photography",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits event photography in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Access Passes\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers send connection invitations to event organizers to receive exclusive backstage access based on credentials.\nDiscipline: Photography (event photography).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits event photography in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $150M — legal tech for creatives · SOM $5M — contract management apps\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$150M — legal tech for creatives",
      "som": "$5M — contract management apps"
    },
    {
      "id": "photography-event-access-pass-0",
      "theme": "photography",
      "title": "Event Access Pass",
      "pitch": "Event Access Pass issues a signed verifiable credential for event photography that the holder keeps and reuses anywhere.",
      "concept": "Photographers can issue verifiable event access passes for crew members, ensuring only authorized individuals gain entry to special events.",
      "subDiscipline": "event photography",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches event photography in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Event Access Pass\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can issue verifiable event access passes for crew members, ensuring only authorized individuals gain entry to special events.\nDiscipline: Photography (event photography).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches event photography in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $900M — photo editing software · SOM $40M — LUT and grading packs\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$900M — photo editing software",
      "som": "$40M — LUT and grading packs"
    },
    {
      "id": "photography-client-payment-authenticator-0",
      "theme": "photography",
      "title": "Client Payment Authenticator",
      "pitch": "Client Payment Authenticator verifies a credential at the gate for freelance — proof without a phone call to the issuer.",
      "concept": "Freelancers can show verified client payments and contracts to prove their eligibility for bidding on high-value projects.",
      "subDiscipline": "freelance",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for freelance in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Client Payment Authenticator\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFreelancers can show verified client payments and contracts to prove their eligibility for bidding on high-value projects.\nDiscipline: Photography (freelance).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for freelance in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $300M — sports media tools · SOM $10M — action photography aids\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$300M — sports media tools",
      "som": "$10M — action photography aids"
    },
    {
      "id": "photography-exhibit-authenticator-0",
      "theme": "photography",
      "title": "Exhibit Authenticator",
      "pitch": "Exhibit Authenticator mints a did:prism for gallery exhibitions so identity travels with the person, not the platform.",
      "concept": "Exhibiting photographers can prove their participation and works via verified DIDs to current and potential art buyers.",
      "subDiscipline": "gallery exhibitions",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits gallery exhibitions in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Exhibit Authenticator\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nExhibiting photographers can prove their participation and works via verified DIDs to current and potential art buyers.\nDiscipline: Photography (gallery exhibitions).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits gallery exhibitions in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $500M — fashion production software · SOM $18M — creative direction tools\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$500M — fashion production software",
      "som": "$18M — creative direction tools"
    },
    {
      "id": "photography-rights-management-hub-0",
      "theme": "photography",
      "title": "Rights Management Hub",
      "pitch": "Rights Management Hub pairs two wallets over DIDComm so licensing can exchange trusted claims on a private channel.",
      "concept": "Photographers can establish a connection to share licensing rights with agencies for seamless use of their images.",
      "subDiscipline": "licensing",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits licensing in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Rights Management Hub\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can establish a connection to share licensing rights with agencies for seamless use of their images.\nDiscipline: Photography (licensing).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits licensing in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $400M — e-commerce photography tools · SOM $14M — product styling software\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$400M — e-commerce photography tools",
      "som": "$14M — product styling software"
    },
    {
      "id": "photography-exhibition-entry-credentials-0",
      "theme": "photography",
      "title": "Exhibition Entry Credentials",
      "pitch": "Exhibition Entry Credentials issues a signed verifiable credential for gallery submissions that the holder keeps and reuses anywhere.",
      "concept": "Artists can receive verifiable credentials proving their eligibility for exhibitions, simplifying the submission process for galleries.",
      "subDiscipline": "gallery submissions",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches gallery submissions in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Exhibition Entry Credentials\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nArtists can receive verifiable credentials proving their eligibility for exhibitions, simplifying the submission process for galleries.\nDiscipline: Photography (gallery submissions).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches gallery submissions in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $350M — family photography tools · SOM $12M — newborn posing guides\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$350M — family photography tools",
      "som": "$12M — newborn posing guides"
    },
    {
      "id": "photography-consent-tracker-0",
      "theme": "photography",
      "title": "Consent Tracker",
      "pitch": "Consent Tracker verifies a credential at the gate for event photography — proof without a phone call to the issuer.",
      "concept": "Photographers can present verified consent from subjects to ensure privacy and usage rights for photos used in publications.",
      "subDiscipline": "event photography",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for event photography in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Consent Tracker\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can present verified consent from subjects to ensure privacy and usage rights for photos used in publications.\nDiscipline: Photography (event photography).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for event photography in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $250M — pet photography resources · SOM $8M — animal training aids\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$250M — pet photography resources",
      "som": "$8M — animal training aids"
    },
    {
      "id": "photography-collaborator-credential-0",
      "theme": "photography",
      "title": "Collaborator Credential",
      "pitch": "Collaborator Credential mints a did:prism for photo collaborations so identity travels with the person, not the platform.",
      "concept": "Photographers and editors confirm collaborative projects through verified identities, ensuring accountability and recognition.",
      "subDiscipline": "photo collaborations",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits photo collaborations in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Collaborator Credential\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers and editors confirm collaborative projects through verified identities, ensuring accountability and recognition.\nDiscipline: Photography (photo collaborations).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits photo collaborations in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $800M — photography business tools · SOM $25M — pricing calculator apps\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$800M — photography business tools",
      "som": "$25M — pricing calculator apps"
    },
    {
      "id": "photography-age-verification-tool-0",
      "theme": "photography",
      "title": "Age Verification Tool",
      "pitch": "Age Verification Tool pairs two wallets over DIDComm so youth photography can exchange trusted claims on a private channel.",
      "concept": "Photographers can invite guardians to verify the age of child models for compliance with industry regulations.",
      "subDiscipline": "youth photography",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits youth photography in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Age Verification Tool\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can invite guardians to verify the age of child models for compliance with industry regulations.\nDiscipline: Photography (youth photography).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits youth photography in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo editing and studio management software · SAM $800M — professional portrait and studio photography tools · SOM $45M — independent studio photographers adopting AI assistants\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo editing and studio management software",
      "sam": "$800M — professional portrait and studio photography tools",
      "som": "$45M — independent studio photographers adopting AI assistants"
    },
    {
      "id": "photography-license-proofer-0",
      "theme": "photography",
      "title": "License Proofer",
      "pitch": "License Proofer issues a signed verifiable credential for licensing that the holder keeps and reuses anywhere.",
      "concept": "Photographers can issue credentials that confirm licensing agreements to clients, protecting their rights and clarifying usage terms.",
      "subDiscipline": "licensing",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches licensing in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"License Proofer\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can issue credentials that confirm licensing agreements to clients, protecting their rights and clarifying usage terms.\nDiscipline: Photography (licensing).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches licensing in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software and creative services market · SAM $600M — portrait and headshot photography software · SOM $30M — freelance portrait photographers seeking posing aids\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software and creative services market",
      "sam": "$600M — portrait and headshot photography software",
      "som": "$30M — freelance portrait photographers seeking posing aids"
    },
    {
      "id": "photography-exhibition-rights-0",
      "theme": "photography",
      "title": "Exhibition Rights",
      "pitch": "Exhibition Rights verifies a credential at the gate for gallery shows — proof without a phone call to the issuer.",
      "concept": "Photographers can present verifiable rights to their works when applying for exhibitions, ensuring authenticity and ownership.",
      "subDiscipline": "gallery shows",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for gallery shows in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Exhibition Rights\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can present verifiable rights to their works when applying for exhibitions, ensuring authenticity and ownership.\nDiscipline: Photography (gallery shows).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for gallery shows in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $3.5B — global journalism and editorial media software · SAM $400M — photojournalism and documentary field tools · SOM $25M — freelance photojournalists and NGO documentarians\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$3.5B — global journalism and editorial media software",
      "sam": "$400M — photojournalism and documentary field tools",
      "som": "$25M — freelance photojournalists and NGO documentarians"
    },
    {
      "id": "photography-age-verification-0",
      "theme": "photography",
      "title": "Age Verification",
      "pitch": "Age Verification mints a did:prism for youth photography so identity travels with the person, not the platform.",
      "concept": "Photographers working with minors can easily verify age consent through trusted DIDs for legal protection.",
      "subDiscipline": "youth photography",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits youth photography in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Age Verification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers working with minors can easily verify age consent through trusted DIDs for legal protection.\nDiscipline: Photography (youth photography).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits youth photography in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software and planning applications · SAM $350M — landscape and outdoor photography planning tools · SOM $20M — professional landscape and travel photographers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software and planning applications",
      "sam": "$350M — landscape and outdoor photography planning tools",
      "som": "$20M — professional landscape and travel photographers"
    },
    {
      "id": "photography-collaborative-editing-0",
      "theme": "photography",
      "title": "Collaborative Editing",
      "pitch": "Collaborative Editing pairs two wallets over DIDComm so photo editing can exchange trusted claims on a private channel.",
      "concept": "Editors send connection invitations to photographers to confirm consent for collaborative editing sessions on shared projects.",
      "subDiscipline": "photo editing",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits photo editing in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Collaborative Editing\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nEditors send connection invitations to photographers to confirm consent for collaborative editing sessions on shared projects.\nDiscipline: Photography (photo editing).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits photo editing in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo editing and culling software · SAM $900M — high-volume wedding and event culling tools · SOM $50M — high-volume event photographers managing burnout\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo editing and culling software",
      "sam": "$900M — high-volume wedding and event culling tools",
      "som": "$50M — high-volume event photographers managing burnout"
    },
    {
      "id": "photography-age-verified-access-0",
      "theme": "photography",
      "title": "Age-Verified Access",
      "pitch": "Age-Verified Access issues a signed verifiable credential for content restrictions that the holder keeps and reuses anywhere.",
      "concept": "Photographers can verify the age of subjects for shoots, ensuring compliance with legal requirements through secure credentials.",
      "subDiscipline": "content restrictions",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches content restrictions in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Age-Verified Access\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can verify the age of subjects for shoots, ensuring compliance with legal requirements through secure credentials.\nDiscipline: Photography (content restrictions).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches content restrictions in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5.0B — global freelance business and CRM software · SAM $800M — photography studio management and sales tools · SOM $40M — independent studio owners refining sales pitches\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5.0B — global freelance business and CRM software",
      "sam": "$800M — photography studio management and sales tools",
      "som": "$40M — independent studio owners refining sales pitches"
    },
    {
      "id": "photography-accreditation-proof-0",
      "theme": "photography",
      "title": "Accreditation Proof",
      "pitch": "Accreditation Proof verifies a credential at the gate for educational institutions — proof without a phone call to the issuer.",
      "concept": "Photography students can verify their enrollment and course completion to gain access to special workshops and internships.",
      "subDiscipline": "educational institutions",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for educational institutions in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Accreditation Proof\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotography students can verify their enrollment and course completion to gain access to special workshops and internships.\nDiscipline: Photography (educational institutions).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for educational institutions in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $1.2B — global accessibility and assistive technology software · SAM $200M — creative arts accessibility tools · SOM $15M — visually impaired creators and inclusive art programs\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$1.2B — global accessibility and assistive technology software",
      "sam": "$200M — creative arts accessibility tools",
      "som": "$15M — visually impaired creators and inclusive art programs"
    },
    {
      "id": "photography-edition-certifier-0",
      "theme": "photography",
      "title": "Edition Certifier",
      "pitch": "Edition Certifier mints a did:prism for fine art photography so identity travels with the person, not the platform.",
      "concept": "Limited edition photographers can verify the authenticity and number of their works through unique DIDs to buyers.",
      "subDiscipline": "fine art photography",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits fine art photography in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Edition Certifier\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLimited edition photographers can verify the authenticity and number of their works through unique DIDs to buyers.\nDiscipline: Photography (fine art photography).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits fine art photography in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software and gear maintenance services · SAM $150M — camera repair and diagnostic software · SOM $10M — field photographers and indie repair technicians\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software and gear maintenance services",
      "sam": "$150M — camera repair and diagnostic software",
      "som": "$10M — field photographers and indie repair technicians"
    },
    {
      "id": "photography-exhibition-invitation-0",
      "theme": "photography",
      "title": "Exhibition Invitation",
      "pitch": "Exhibition Invitation pairs two wallets over DIDComm so exhibitions can exchange trusted claims on a private channel.",
      "concept": "Curators invite photographers to prove their portfolio's eligibility for exhibitions through a secure connection.",
      "subDiscipline": "exhibitions",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits exhibitions in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Exhibition Invitation\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCurators invite photographers to prove their portfolio's eligibility for exhibitions through a secure connection.\nDiscipline: Photography (exhibitions).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits exhibitions in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software and pre-production tools · SAM $500M — commercial and conceptual photography planning · SOM $35M — commercial photographers and creative directors\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software and pre-production tools",
      "sam": "$500M — commercial and conceptual photography planning",
      "som": "$35M — commercial photographers and creative directors"
    },
    {
      "id": "photography-authenticity-certificate-0",
      "theme": "photography",
      "title": "Authenticity Certificate",
      "pitch": "Authenticity Certificate issues a signed verifiable credential for art sales that the holder keeps and reuses anywhere.",
      "concept": "Photographers can issue authentication certificates for limited edition prints, assuring buyers of their authenticity and provenance.",
      "subDiscipline": "art sales",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches art sales in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Authenticity Certificate\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can issue authentication certificates for limited edition prints, assuring buyers of their authenticity and provenance.\nDiscipline: Photography (art sales).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches art sales in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software and analog supplies market · SAM $150M — fine art analog darkroom equipment and tools · SOM $12M — fine art printers and analog photography educators\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software and analog supplies market",
      "sam": "$150M — fine art analog darkroom equipment and tools",
      "som": "$12M — fine art printers and analog photography educators"
    },
    {
      "id": "photography-work-provenance-checker-0",
      "theme": "photography",
      "title": "Work Provenance Checker",
      "pitch": "Work Provenance Checker verifies a credential at the gate for art history — proof without a phone call to the issuer.",
      "concept": "Collectibles can be proven authentic by showing verified history of ownership and rights as documented by the original photographers.",
      "subDiscipline": "art history",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for art history in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Work Provenance Checker\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCollectibles can be proven authentic by showing verified history of ownership and rights as documented by the original photographers.\nDiscipline: Photography (art history).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for art history in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software and outdoor gear market · SAM $300M — wildlife and nature photography planning tools · SOM $20M — professional wildlife photographers and ecotourism guides\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software and outdoor gear market",
      "sam": "$300M — wildlife and nature photography planning tools",
      "som": "$20M — professional wildlife photographers and ecotourism guides"
    },
    {
      "id": "photography-rights-tracker-0",
      "theme": "photography",
      "title": "Rights Tracker",
      "pitch": "Rights Tracker mints a did:prism for image rights so identity travels with the person, not the platform.",
      "concept": "Photographers can track and prove usage rights for their images, verified by clients or stakeholders, ensuring compliance.",
      "subDiscipline": "image rights",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits image rights in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Rights Tracker\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can track and prove usage rights for their images, verified by clients or stakeholders, ensuring compliance.\nDiscipline: Photography (image rights).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits image rights in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software and culinary media market · SAM $250M — food photography and styling software · SOM $18M — professional food photographers and prop stylists\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software and culinary media market",
      "sam": "$250M — food photography and styling software",
      "som": "$18M — professional food photographers and prop stylists"
    },
    {
      "id": "photography-credentialed-feedback-0",
      "theme": "photography",
      "title": "Credentialed Feedback",
      "pitch": "Credentialed Feedback pairs two wallets over DIDComm so education can exchange trusted claims on a private channel.",
      "concept": "Students can invite instructors to verify their photography project submissions for feedback and assessment.",
      "subDiscipline": "education",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits education in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Credentialed Feedback\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nStudents can invite instructors to verify their photography project submissions for feedback and assessment.\nDiscipline: Photography (education).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits education in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software and astronomy planning apps · SAM $180M — astrophotography and night sky tools · SOM $15M — dedicated astrophotographers and dark sky guides\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software and astronomy planning apps",
      "sam": "$180M — astrophotography and night sky tools",
      "som": "$15M — dedicated astrophotographers and dark sky guides"
    },
    {
      "id": "photography-image-consent-manager-0",
      "theme": "photography",
      "title": "Image Consent Manager",
      "pitch": "Image Consent Manager issues a signed verifiable credential for model rights that the holder keeps and reuses anywhere.",
      "concept": "Photographers can manage consent documents for models, issuing verifiable credentials to prove consent for image usage.",
      "subDiscipline": "model rights",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches model rights in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Image Consent Manager\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can manage consent documents for models, issuing verifiable credentials to prove consent for image usage.\nDiscipline: Photography (model rights).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches model rights in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $4.0B — global wedding services and photography software · SAM $1.2B — wedding photojournalism and editing tools · SOM $60M — high-end wedding photographers and studio owners\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$4.0B — global wedding services and photography software",
      "sam": "$1.2B — wedding photojournalism and editing tools",
      "som": "$60M — high-end wedding photographers and studio owners"
    },
    {
      "id": "photography-editorial-credentials-0",
      "theme": "photography",
      "title": "Editorial Credentials",
      "pitch": "Editorial Credentials verifies a credential at the gate for photojournalism — proof without a phone call to the issuer.",
      "concept": "Journalists can prove their editorial credentials to gain access for credible reporting at newsworthy events.",
      "subDiscipline": "photojournalism",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for photojournalism in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Editorial Credentials\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nJournalists can prove their editorial credentials to gain access for credible reporting at newsworthy events.\nDiscipline: Photography (photojournalism).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for photojournalism in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software and architectural visualization · SAM $400M — real estate and architectural photography tools · SOM $30M — commercial architectural and interior photographers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software and architectural visualization",
      "sam": "$400M — real estate and architectural photography tools",
      "som": "$30M — commercial architectural and interior photographers"
    },
    {
      "id": "photography-delegated-authority-0",
      "theme": "photography",
      "title": "Delegated Authority",
      "pitch": "Delegated Authority mints a did:prism for agent representation so identity travels with the person, not the platform.",
      "concept": "Agents can verify their authority to represent photographers via unique DIDs to agencies and potential clients.",
      "subDiscipline": "agent representation",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits agent representation in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Delegated Authority\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAgents can verify their authority to represent photographers via unique DIDs to agencies and potential clients.\nDiscipline: Photography (agent representation).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits agent representation in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software and scientific imaging · SAM $120M — macro and product photography tools · SOM $10M — macro nature and high-end product photographers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software and scientific imaging",
      "sam": "$120M — macro and product photography tools",
      "som": "$10M — macro nature and high-end product photographers"
    },
    {
      "id": "photography-model-release-verifier-0",
      "theme": "photography",
      "title": "Model Release Verifier",
      "pitch": "Model Release Verifier pairs two wallets over DIDComm so portraiture can exchange trusted claims on a private channel.",
      "concept": "Photographers can establish a connection with models to verify and share signed model release forms.",
      "subDiscipline": "portraiture",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits portraiture in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Model Release Verifier\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can establish a connection with models to verify and share signed model release forms.\nDiscipline: Photography (portraiture).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits portraiture in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software and mobile photography apps · SAM $200M — street and documentary photography tools · SOM $15M — fine art street photographers and urban explorers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software and mobile photography apps",
      "sam": "$200M — street and documentary photography tools",
      "som": "$15M — fine art street photographers and urban explorers"
    },
    {
      "id": "photography-guild-status-checker-0",
      "theme": "photography",
      "title": "Guild Status Checker",
      "pitch": "Guild Status Checker issues a signed verifiable credential for professional groups that the holder keeps and reuses anywhere.",
      "concept": "Photographers can verify their membership in professional guilds, providing credibility and access to exclusive resources.",
      "subDiscipline": "professional groups",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches professional groups in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Guild Status Checker\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can verify their membership in professional guilds, providing credibility and access to exclusive resources.\nDiscipline: Photography (professional groups).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches professional groups in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software and dive equipment market · SAM $150M — underwater photography and lighting tools · SOM $12M — professional underwater and marine life photographers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software and dive equipment market",
      "sam": "$150M — underwater photography and lighting tools",
      "som": "$12M — professional underwater and marine life photographers"
    },
    {
      "id": "photography-collaborative-authorship-0",
      "theme": "photography",
      "title": "Collaborative Authorship",
      "pitch": "Collaborative Authorship verifies a credential at the gate for team projects — proof without a phone call to the issuer.",
      "concept": "Photographers collaborating on projects can present verified contributions for shared credit and financial arrangements.",
      "subDiscipline": "team projects",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for team projects in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Collaborative Authorship\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers collaborating on projects can present verified contributions for shared credit and financial arrangements.\nDiscipline: Photography (team projects).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for team projects in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $3.0B — global fashion tech and photography software · SAM $600M — editorial and fashion photography tools · SOM $40M — fashion photographers and editorial producers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$3.0B — global fashion tech and photography software",
      "sam": "$600M — editorial and fashion photography tools",
      "som": "$40M — fashion photographers and editorial producers"
    },
    {
      "id": "photography-credential-showcase-0",
      "theme": "photography",
      "title": "Credential Showcase",
      "pitch": "Credential Showcase mints a did:prism for professional accreditation so identity travels with the person, not the platform.",
      "concept": "Photographers can display their professional accreditations and training history, verified by educational institutions, to clients.",
      "subDiscipline": "professional accreditation",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits professional accreditation in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Credential Showcase\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can display their professional accreditations and training history, verified by educational institutions, to clients.\nDiscipline: Photography (professional accreditation).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits professional accreditation in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software and sports media market · SAM $450M — sports and action photography tools · SOM $35M — professional sports and extreme action photographers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software and sports media market",
      "sam": "$450M — sports and action photography tools",
      "som": "$35M — professional sports and extreme action photographers"
    },
    {
      "id": "photography-event-documenter-0",
      "theme": "photography",
      "title": "Event Documenter",
      "pitch": "Event Documenter pairs two wallets over DIDComm so event photography can exchange trusted claims on a private channel.",
      "concept": "Photographers invite organizers to confirm their duty of documenting events with a verified connection.",
      "subDiscipline": "event photography",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits event photography in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Event Documenter\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers invite organizers to confirm their duty of documenting events with a verified connection.\nDiscipline: Photography (event photography).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits event photography in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $1.5B — global mental health and creative coaching apps · SAM $300M — creative professional wellness and coaching · SOM $20M — freelance photographers seeking mindset coaching\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$1.5B — global mental health and creative coaching apps",
      "sam": "$300M — creative professional wellness and coaching",
      "som": "$20M — freelance photographers seeking mindset coaching"
    },
    {
      "id": "photography-session-transfer-agreement-0",
      "theme": "photography",
      "title": "Session Transfer Agreement",
      "pitch": "Session Transfer Agreement issues a signed verifiable credential for freelancing that the holder keeps and reuses anywhere.",
      "concept": "Freelancers can issue credentials that confirm session transfer agreements, protecting both parties and clarifying terms of service.",
      "subDiscipline": "freelancing",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches freelancing in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Session Transfer Agreement\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFreelancers can issue credentials that confirm session transfer agreements, protecting both parties and clarifying terms of service.\nDiscipline: Photography (freelancing).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches freelancing in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $5.0B — global commercial drone and flight software · SAM $1.2B — aerial photography and drone mapping tools · SOM $60M — licensed drone photographers and cinematographers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$5.0B — global commercial drone and flight software",
      "sam": "$1.2B — aerial photography and drone mapping tools",
      "som": "$60M — licensed drone photographers and cinematographers"
    },
    {
      "id": "photography-license-validator-0",
      "theme": "photography",
      "title": "License Validator",
      "pitch": "License Validator verifies a credential at the gate for stock photography — proof without a phone call to the issuer.",
      "concept": "Photographers can show verified licenses for their work, ensuring legal compliance when distributing images.",
      "subDiscipline": "stock photography",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for stock photography in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"License Validator\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can show verified licenses for their work, ensuring legal compliance when distributing images.\nDiscipline: Photography (stock photography).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for stock photography in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo and video editing software · SAM $800M — advanced color grading and post-production tools · SOM $45M — high-end retouchers and colorists\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo and video editing software",
      "sam": "$800M — advanced color grading and post-production tools",
      "som": "$45M — high-end retouchers and colorists"
    },
    {
      "id": "photography-consent-collector-0",
      "theme": "photography",
      "title": "Consent Collector",
      "pitch": "Consent Collector mints a did:prism for model photography so identity travels with the person, not the platform.",
      "concept": "Photographers can manage and verify model consent forms using DIDs for legal protection and professional integrity.",
      "subDiscipline": "model photography",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits model photography in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Consent Collector\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can manage and verify model consent forms using DIDs for legal protection and professional integrity.\nDiscipline: Photography (model photography).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits model photography in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software and art market services · SAM $200M — portfolio building and gallery submission tools · SOM $15M — fine art photographers preparing for exhibitions\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software and art market services",
      "sam": "$200M — portfolio building and gallery submission tools",
      "som": "$15M — fine art photographers preparing for exhibitions"
    },
    {
      "id": "photography-provenance-trail-0",
      "theme": "photography",
      "title": "Provenance Trail",
      "pitch": "Provenance Trail pairs two wallets over DIDComm so art photography can exchange trusted claims on a private channel.",
      "concept": "Artists can send connection invitations to collectors to authenticate the history and ownership of their artwork.",
      "subDiscipline": "art photography",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits art photography in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Provenance Trail\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nArtists can send connection invitations to collectors to authenticate the history and ownership of their artwork.\nDiscipline: Photography (art photography).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits art photography in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $1.5B — global health tracking and productivity software · SAM $250M — creative professional wellness and scheduling tools · SOM $20M — high-volume event and wedding photographers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$1.5B — global health tracking and productivity software",
      "sam": "$250M — creative professional wellness and scheduling tools",
      "som": "$20M — high-volume event and wedding photographers"
    },
    {
      "id": "photography-portfolio-verification-tool-0",
      "theme": "photography",
      "title": "Portfolio Verification Tool",
      "pitch": "Portfolio Verification Tool issues a signed verifiable credential for portfolio building that the holder keeps and reuses anywhere.",
      "concept": "Photographers can receive verified credentials for showcased works, assisting in validating their portfolios to potential clients.",
      "subDiscipline": "portfolio building",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches portfolio building in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Portfolio Verification Tool\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can receive verified credentials for showcased works, assisting in validating their portfolios to potential clients.\nDiscipline: Photography (portfolio building).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches portfolio building in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $120M — location scouting and pre-production apps · SOM $5M — AI scout assistants\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$120M — location scouting and pre-production apps",
      "som": "$5M — AI scout assistants"
    },
    {
      "id": "photography-trust-score-revealer-0",
      "theme": "photography",
      "title": "Trust Score Revealer",
      "pitch": "Trust Score Revealer verifies a credential at the gate for client relations — proof without a phone call to the issuer.",
      "concept": "Photographers can present verified trust scores from past clients to establish credibility when bidding for new projects.",
      "subDiscipline": "client relations",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for client relations in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Trust Score Revealer\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can present verified trust scores from past clients to establish credibility when bidding for new projects.\nDiscipline: Photography (client relations).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for client relations in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $300M — studio lighting and grip gear software · SOM $15M — AI lighting assistants\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$300M — studio lighting and grip gear software",
      "som": "$15M — AI lighting assistants"
    },
    {
      "id": "photography-guild-status-verifier-0",
      "theme": "photography",
      "title": "Guild Status Verifier",
      "pitch": "Guild Status Verifier mints a did:prism for photography unions so identity travels with the person, not the platform.",
      "concept": "Union members can verify their membership status to access benefits and services, ensuring proper representation.",
      "subDiscipline": "photography unions",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits photography unions in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Guild Status Verifier\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nUnion members can verify their membership status to access benefits and services, ensuring proper representation.\nDiscipline: Photography (photography unions).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits photography unions in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $500M — photojournalism and editorial tools · SOM $25M — AI field logging\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$500M — photojournalism and editorial tools",
      "som": "$25M — AI field logging"
    },
    {
      "id": "photography-accreditation-checker-0",
      "theme": "photography",
      "title": "Accreditation Checker",
      "pitch": "Accreditation Checker pairs two wallets over DIDComm so journalism can exchange trusted claims on a private channel.",
      "concept": "Freelance photographers invite publication editors to verify their accreditation status for press events.",
      "subDiscipline": "journalism",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits journalism in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Accreditation Checker\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFreelance photographers invite publication editors to verify their accreditation status for press events.\nDiscipline: Photography (journalism).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits journalism in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $400M — portrait studio management software · SOM $18M — AI posing coaches\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$400M — portrait studio management software",
      "som": "$18M — AI posing coaches"
    },
    {
      "id": "photography-provenance-pathway-0",
      "theme": "photography",
      "title": "Provenance Pathway",
      "pitch": "Provenance Pathway issues a signed verifiable credential for art history that the holder keeps and reuses anywhere.",
      "concept": "Art photographers can create a verified path of their artwork's ownership, enhancing its value and authenticity for collectors.",
      "subDiscipline": "art history",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches art history in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Provenance Pathway\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nArt photographers can create a verified path of their artwork's ownership, enhancing its value and authenticity for collectors.\nDiscipline: Photography (art history).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches art history in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $850M — professional workflow and DAM tools · SOM $40M — AI metadata taggers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$850M — professional workflow and DAM tools",
      "som": "$40M — AI metadata taggers"
    },
    {
      "id": "photography-guild-membership-proof-0",
      "theme": "photography",
      "title": "Guild Membership Proof",
      "pitch": "Guild Membership Proof verifies a credential at the gate for union work — proof without a phone call to the issuer.",
      "concept": "Photographers can easily verify their union membership status to access guild benefits and resources.",
      "subDiscipline": "union work",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for union work in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Guild Membership Proof\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can easily verify their union membership status to access guild benefits and resources.\nDiscipline: Photography (union work).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for union work in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $90M — nature and wildlife logging apps · SOM $4M — AI blind trackers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$90M — nature and wildlife logging apps",
      "som": "$4M — AI blind trackers"
    },
    {
      "id": "photography-publication-proof-0",
      "theme": "photography",
      "title": "Publication Proof",
      "pitch": "Publication Proof mints a did:prism for photo publishing so identity travels with the person, not the platform.",
      "concept": "Photographers can verify their submissions and published works to publishers and clients, ensuring acknowledgment.",
      "subDiscipline": "photo publishing",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits photo publishing in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Publication Proof\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can verify their submissions and published works to publishers and clients, ensuring acknowledgment.\nDiscipline: Photography (photo publishing).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits photo publishing in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $60M — astrophotography planning software · SOM $3M — AI night sky assistants\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$60M — astrophotography planning software",
      "som": "$3M — AI night sky assistants"
    },
    {
      "id": "photography-peer-review-network-0",
      "theme": "photography",
      "title": "Peer Review Network",
      "pitch": "Peer Review Network pairs two wallets over DIDComm so photo critique can exchange trusted claims on a private channel.",
      "concept": "Photographers can send invitations to fellow artists for peer review sessions to validate their work before exhibitions.",
      "subDiscipline": "photo critique",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits photo critique in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Peer Review Network\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can send invitations to fellow artists for peer review sessions to validate their work before exhibitions.\nDiscipline: Photography (photo critique).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits photo critique in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $350M — wedding photography CRM and tools · SOM $20M — AI wedding coordinators\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$350M — wedding photography CRM and tools",
      "som": "$20M — AI wedding coordinators"
    },
    {
      "id": "photography-skill-level-credentialing-0",
      "theme": "photography",
      "title": "Skill Level Credentialing",
      "pitch": "Skill Level Credentialing issues a signed verifiable credential for professional development that the holder keeps and reuses anywhere.",
      "concept": "Photographers can issue credentials certifying their skills and training, helping them stand out in competitive job markets.",
      "subDiscipline": "professional development",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches professional development in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Skill Level Credentialing\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can issue credentials certifying their skills and training, helping them stand out in competitive job markets.\nDiscipline: Photography (professional development).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches professional development in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $110M — commercial food photography tools · SOM $6M — AI food styling aides\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$110M — commercial food photography tools",
      "som": "$6M — AI food styling aides"
    },
    {
      "id": "photography-print-edition-verification-0",
      "theme": "photography",
      "title": "Print Edition Verification",
      "pitch": "Print Edition Verification verifies a credential at the gate for fine art — proof without a phone call to the issuer.",
      "concept": "Artists can provide verified proof of edition numbers for prints, ensuring authenticity for collectors and galleries.",
      "subDiscipline": "fine art",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for fine art in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Print Edition Verification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nArtists can provide verified proof of edition numbers for prints, ensuring authenticity for collectors and galleries.\nDiscipline: Photography (fine art).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for fine art in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $75M — macro and scientific imaging software · SOM $4M — AI focus stackers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$75M — macro and scientific imaging software",
      "som": "$4M — AI focus stackers"
    },
    {
      "id": "photography-workshop-enrollment-0",
      "theme": "photography",
      "title": "Workshop Enrollment",
      "pitch": "Workshop Enrollment mints a did:prism for photography education so identity travels with the person, not the platform.",
      "concept": "Participants in photography workshops can verify their enrollment and attendance through DIDs for credentialing.",
      "subDiscipline": "photography education",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits photography education in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Workshop Enrollment\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nParticipants in photography workshops can verify their enrollment and attendance through DIDs for credentialing.\nDiscipline: Photography (photography education).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits photography education in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $200M — architectural and real estate media tools · SOM $12M — AI perspective critics\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$200M — architectural and real estate media tools",
      "som": "$12M — AI perspective critics"
    },
    {
      "id": "photography-safe-image-distribution-0",
      "theme": "photography",
      "title": "Safe Image Distribution",
      "pitch": "Safe Image Distribution pairs two wallets over DIDComm so media sharing can exchange trusted claims on a private channel.",
      "concept": "Photographers use connection links to share images securely with clients, proving ownership and rights in real time.",
      "subDiscipline": "media sharing",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits media sharing in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Safe Image Distribution\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers use connection links to share images securely with clients, proving ownership and rights in real time.\nDiscipline: Photography (media sharing).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits media sharing in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $80M — street photography community and legal apps · SOM $5M — AI ethics journalers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$80M — street photography community and legal apps",
      "som": "$5M — AI ethics journalers"
    },
    {
      "id": "photography-event-documentation-proof-0",
      "theme": "photography",
      "title": "Event Documentation Proof",
      "pitch": "Event Documentation Proof issues a signed verifiable credential for photojournalism that the holder keeps and reuses anywhere.",
      "concept": "Journalists can issue credentials verifying their presence at events, ensuring the authenticity of their photographic documentation.",
      "subDiscipline": "photojournalism",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches photojournalism in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Event Documentation Proof\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nJournalists can issue credentials verifying their presence at events, ensuring the authenticity of their photographic documentation.\nDiscipline: Photography (photojournalism).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches photojournalism in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $450M — drone mapping and flight software · SOM $22M — AI drone co-pilots\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$450M — drone mapping and flight software",
      "som": "$22M — AI drone co-pilots"
    },
    {
      "id": "photography-location-access-checks-0",
      "theme": "photography",
      "title": "Location Access Checks",
      "pitch": "Location Access Checks verifies a credential at the gate for landscape photography — proof without a phone call to the issuer.",
      "concept": "Photographers can verify permissions for shooting in restricted areas to secure access and avoid legal issues.",
      "subDiscipline": "landscape photography",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for landscape photography in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Location Access Checks\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can verify permissions for shooting in restricted areas to secure access and avoid legal issues.\nDiscipline: Photography (landscape photography).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for landscape photography in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $40M — dive logging and underwater housing software · SOM $2M — AI reef loggers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$40M — dive logging and underwater housing software",
      "som": "$2M — AI reef loggers"
    },
    {
      "id": "photography-award-submission-0",
      "theme": "photography",
      "title": "Award Submission",
      "pitch": "Award Submission mints a did:prism for photo contests so identity travels with the person, not the platform.",
      "concept": "Photographers can authenticate their entries in contests via DIDs to ensure fair evaluation and transparency.",
      "subDiscipline": "photo contests",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits photo contests in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Award Submission\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can authenticate their entries in contests via DIDs to ensure fair evaluation and transparency.\nDiscipline: Photography (photo contests).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits photo contests in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $150M — equipment rental and inventory management · SOM $8M — AI gear packers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$150M — equipment rental and inventory management",
      "som": "$8M — AI gear packers"
    },
    {
      "id": "photography-feedback-loop-0",
      "theme": "photography",
      "title": "Feedback Loop",
      "pitch": "Feedback Loop pairs two wallets over DIDComm so portfolio reviews can exchange trusted claims on a private channel.",
      "concept": "Photographers invite mentors to verify feedback on their portfolios through a certified connection process.",
      "subDiscipline": "portfolio reviews",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits portfolio reviews in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Feedback Loop\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers invite mentors to verify feedback on their portfolios through a certified connection process.\nDiscipline: Photography (portfolio reviews).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits portfolio reviews in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $60M — analog film and darkroom tools · SOM $3M — AI darkroom timers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$60M — analog film and darkroom tools",
      "som": "$3M — AI darkroom timers"
    },
    {
      "id": "photography-rights-management-system-0",
      "theme": "photography",
      "title": "Rights Management System",
      "pitch": "Rights Management System issues a signed verifiable credential for copyright that the holder keeps and reuses anywhere.",
      "concept": "Photographers can issue credentials that track and manage rights for their works, simplifying negotiations with licenses and clients.",
      "subDiscipline": "copyright",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches copyright in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Rights Management System\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can issue credentials that track and manage rights for their works, simplifying negotiations with licenses and clients.\nDiscipline: Photography (copyright).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches copyright in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $280M — sports and action photography software · SOM $14M — AI panning trackers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$280M — sports and action photography software",
      "som": "$14M — AI panning trackers"
    },
    {
      "id": "photography-model-release-proof-0",
      "theme": "photography",
      "title": "Model Release Proof",
      "pitch": "Model Release Proof verifies a credential at the gate for portraiture — proof without a phone call to the issuer.",
      "concept": "Photographers can present verified model release forms to prove consent for using subjects’ images in various media.",
      "subDiscipline": "portraiture",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for portraiture in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Model Release Proof\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can present verified model release forms to prove consent for using subjects’ images in various media.\nDiscipline: Photography (portraiture).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for portraiture in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $400M — client gallery and proofing platforms · SOM $20M — AI proof narrators\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$400M — client gallery and proofing platforms",
      "som": "$20M — AI proof narrators"
    },
    {
      "id": "photography-image-authentication-0",
      "theme": "photography",
      "title": "Image Authentication",
      "pitch": "Image Authentication mints a did:prism for photo forensics so identity travels with the person, not the platform.",
      "concept": "Photographers can prove the authenticity of their images to clients and agencies through verified IDs.",
      "subDiscipline": "photo forensics",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits photo forensics in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Image Authentication\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can prove the authenticity of their images to clients and agencies through verified IDs.\nDiscipline: Photography (photo forensics).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits photo forensics in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $180M — e-commerce and product photography tools · SOM $10M — AI glare sweepers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$180M — e-commerce and product photography tools",
      "som": "$10M — AI glare sweepers"
    },
    {
      "id": "photography-shared-studio-access-0",
      "theme": "photography",
      "title": "Shared Studio Access",
      "pitch": "Shared Studio Access pairs two wallets over DIDComm so studio rentals can exchange trusted claims on a private channel.",
      "concept": "Photographers can send studio managers connection invitations to verify their booking and usage rights.",
      "subDiscipline": "studio rentals",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits studio rentals in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Shared Studio Access\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can send studio managers connection invitations to verify their booking and usage rights.\nDiscipline: Photography (studio rentals).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits studio rentals in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $90M — legal and release management apps · SOM $5M — AI consent drafters\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$90M — legal and release management apps",
      "som": "$5M — AI consent drafters"
    },
    {
      "id": "photography-edition-verification-app-0",
      "theme": "photography",
      "title": "Edition Verification App",
      "pitch": "Edition Verification App issues a signed verifiable credential for fine art that the holder keeps and reuses anywhere.",
      "concept": "Artists can issue credentials verifying the limited nature of their prints, reassuring buyers of their edition authenticity.",
      "subDiscipline": "fine art",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches fine art in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Edition Verification App\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nArtists can issue credentials verifying the limited nature of their prints, reassuring buyers of their edition authenticity.\nDiscipline: Photography (fine art).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches fine art in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $600M — color grading and post-production software · SOM $30M — AI grade whisperers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$600M — color grading and post-production software",
      "som": "$30M — AI grade whisperers"
    },
    {
      "id": "photography-award-proof-presenter-0",
      "theme": "photography",
      "title": "Award Proof Presenter",
      "pitch": "Award Proof Presenter verifies a credential at the gate for competitions — proof without a phone call to the issuer.",
      "concept": "Photographers can present verified proof of awards won through recognized competitions to enhance reputation.",
      "subDiscipline": "competitions",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for competitions in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Award Proof Presenter\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can present verified proof of awards won through recognized competitions to enhance reputation.\nDiscipline: Photography (competitions).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for competitions in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $220M — event and corporate photography tools · SOM $11M — AI VIP spotters\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$220M — event and corporate photography tools",
      "som": "$11M — AI VIP spotters"
    },
    {
      "id": "photography-event-participation-0",
      "theme": "photography",
      "title": "Event Participation",
      "pitch": "Event Participation mints a did:prism for photography festivals so identity travels with the person, not the platform.",
      "concept": "Photographers can prove their participation in festivals or events through unique DIDs issued by organizers.",
      "subDiscipline": "photography festivals",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits photography festivals in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Event Participation\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can prove their participation in festivals or events through unique DIDs issued by organizers.\nDiscipline: Photography (photography festivals).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits photography festivals in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $130M — pet and family portrait studio software · SOM $7M — AI pet wranglers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$130M — pet and family portrait studio software",
      "som": "$7M — AI pet wranglers"
    },
    {
      "id": "photography-collaborative-copyright-0",
      "theme": "photography",
      "title": "Collaborative Copyright",
      "pitch": "Collaborative Copyright pairs two wallets over DIDComm so commercial photography can exchange trusted claims on a private channel.",
      "concept": "Photographers invite models to agree on copyright terms through a verified connection before shooting.",
      "subDiscipline": "commercial photography",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits commercial photography in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Collaborative Copyright\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers invite models to agree on copyright terms through a verified connection before shooting.\nDiscipline: Photography (commercial photography).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits commercial photography in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $310M — real estate media and virtual tour software · SOM $16M — AI room flow narrators\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$310M — real estate media and virtual tour software",
      "som": "$16M — AI room flow narrators"
    },
    {
      "id": "photography-mentorship-credential-exchange-0",
      "theme": "photography",
      "title": "Mentorship Credential Exchange",
      "pitch": "Mentorship Credential Exchange issues a signed verifiable credential for education that the holder keeps and reuses anywhere.",
      "concept": "Photographers can issue credentials that recognize mentorships provided, enhancing the professional stature of both mentors and mentees.",
      "subDiscipline": "education",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches education in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Mentorship Credential Exchange\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can issue credentials that recognize mentorships provided, enhancing the professional stature of both mentors and mentees.\nDiscipline: Photography (education).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches education in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $70M — camera repair and maintenance logging tools · SOM $4M — AI sensor dust mappers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$70M — camera repair and maintenance logging tools",
      "som": "$4M — AI sensor dust mappers"
    },
    {
      "id": "photography-patronage-verifier-0",
      "theme": "photography",
      "title": "Patronage Verifier",
      "pitch": "Patronage Verifier verifies a credential at the gate for art funding — proof without a phone call to the issuer.",
      "concept": "Photographers can show verified patronage agreements to secure funding for projects while ensuring trust and transparency.",
      "subDiscipline": "art funding",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for art funding in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Patronage Verifier\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can show verified patronage agreements to secure funding for projects while ensuring trust and transparency.\nDiscipline: Photography (art funding).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for art funding in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $500M — editorial and photojournalism software · SOM $15M — AI pacing tools for photo essays\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$500M — editorial and photojournalism software",
      "som": "$15M — AI pacing tools for photo essays"
    },
    {
      "id": "photography-portfolio-validator-0",
      "theme": "photography",
      "title": "Portfolio Validator",
      "pitch": "Portfolio Validator mints a did:prism for freelance photography so identity travels with the person, not the platform.",
      "concept": "Freelancers can verify their portfolio work to clients with DIDs to ensure authenticity and protect rights.",
      "subDiscipline": "freelance photography",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits freelance photography in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Portfolio Validator\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFreelancers can verify their portfolio work to clients with DIDs to ensure authenticity and protect rights.\nDiscipline: Photography (freelance photography).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits freelance photography in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $300M — portrait studio management tools · SOM $10M — AI subject relaxation audio aids\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$300M — portrait studio management tools",
      "som": "$10M — AI subject relaxation audio aids"
    },
    {
      "id": "photography-survey-feedback-access-0",
      "theme": "photography",
      "title": "Survey Feedback Access",
      "pitch": "Survey Feedback Access pairs two wallets over DIDComm so event photography can exchange trusted claims on a private channel.",
      "concept": "Photographers invite event attendees to share feedback on their work through a verified connection for future improvement.",
      "subDiscipline": "event photography",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits event photography in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Survey Feedback Access\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers invite event attendees to share feedback on their work through a verified connection for future improvement.\nDiscipline: Photography (event photography).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits event photography in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $800M — professional editing and workflow tools · SOM $25M — AI focus aids for photo editors\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$800M — professional editing and workflow tools",
      "som": "$25M — AI focus aids for photo editors"
    },
    {
      "id": "photography-access-levels-creator-0",
      "theme": "photography",
      "title": "Access Levels Creator",
      "pitch": "Access Levels Creator issues a signed verifiable credential for content distribution that the holder keeps and reuses anywhere.",
      "concept": "Photographers can create tiered access credentials for shared resources, controlling who can view or use their work.",
      "subDiscipline": "content distribution",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches content distribution in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Access Levels Creator\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can create tiered access credentials for shared resources, controlling who can view or use their work.\nDiscipline: Photography (content distribution).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches content distribution in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $150M — wildlife and nature photography gear · SOM $5M — AI patience aids for nature shooters\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$150M — wildlife and nature photography gear",
      "som": "$5M — AI patience aids for nature shooters"
    },
    {
      "id": "photography-workshop-certification-0",
      "theme": "photography",
      "title": "Workshop Certification",
      "pitch": "Workshop Certification verifies a credential at the gate for education — proof without a phone call to the issuer.",
      "concept": "Instructors can present verified credentials for teaching workshops to ensure quality education for participants.",
      "subDiscipline": "education",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for education in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Workshop Certification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nInstructors can present verified credentials for teaching workshops to ensure quality education for participants.\nDiscipline: Photography (education).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for education in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $400M — wedding photography and videography software · SOM $20M — AI audio scoring for wedding slideshows\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$400M — wedding photography and videography software",
      "som": "$20M — AI audio scoring for wedding slideshows"
    },
    {
      "id": "photography-collaboration-contract-0",
      "theme": "photography",
      "title": "Collaboration Contract",
      "pitch": "Collaboration Contract mints a did:prism for creative partnerships so identity travels with the person, not the platform.",
      "concept": "Partnership agreements between photographers can be verified through DIDs to ensure accountability and trust.",
      "subDiscipline": "creative partnerships",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits creative partnerships in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Collaboration Contract\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPartnership agreements between photographers can be verified through DIDs to ensure accountability and trust.\nDiscipline: Photography (creative partnerships).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits creative partnerships in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $100M — street photography apps and guides · SOM $8M — AI pacing tools for street photographers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$100M — street photography apps and guides",
      "som": "$8M — AI pacing tools for street photographers"
    },
    {
      "id": "photography-event-credentials-hub-0",
      "theme": "photography",
      "title": "Event Credentials Hub",
      "pitch": "Event Credentials Hub pairs two wallets over DIDComm so conventions can exchange trusted claims on a private channel.",
      "concept": "Photographers send invitations to convention organizers to prove their attendance and access based on credentials.",
      "subDiscipline": "conventions",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits conventions in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Event Credentials Hub\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers send invitations to convention organizers to prove their attendance and access based on credentials.\nDiscipline: Photography (conventions).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits conventions in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $250M — commercial product photography tools · SOM $12M — AI Foley generators for product reels\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$250M — commercial product photography tools",
      "som": "$12M — AI Foley generators for product reels"
    },
    {
      "id": "photography-competition-entrant-proof-0",
      "theme": "photography",
      "title": "Competition Entrant Proof",
      "pitch": "Competition Entrant Proof issues a signed verifiable credential for contests that the holder keeps and reuses anywhere.",
      "concept": "Photographers can issue credentials to verify their entry in competitions, establishing authenticity of submissions.",
      "subDiscipline": "contests",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches contests in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Competition Entrant Proof\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can issue credentials to verify their entry in competitions, establishing authenticity of submissions.\nDiscipline: Photography (contests).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches contests in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $50M — astrophotography planning software · SOM $3M — AI audio timers for night shooters\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$50M — astrophotography planning software",
      "som": "$3M — AI audio timers for night shooters"
    },
    {
      "id": "photography-curation-approval-0",
      "theme": "photography",
      "title": "Curation Approval",
      "pitch": "Curation Approval verifies a credential at the gate for gallery submissions — proof without a phone call to the issuer.",
      "concept": "Photographers can show verified approvals from curators for their works to improve chances of being featured in exhibitions.",
      "subDiscipline": "gallery submissions",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for gallery submissions in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Curation Approval\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can show verified approvals from curators for their works to improve chances of being featured in exhibitions.\nDiscipline: Photography (gallery submissions).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for gallery submissions in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $120M — food photography and styling tools · SOM $6M — AI sensory audio for food creators\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$120M — food photography and styling tools",
      "som": "$6M — AI sensory audio for food creators"
    },
    {
      "id": "photography-content-rights-manager-0",
      "theme": "photography",
      "title": "Content Rights Manager",
      "pitch": "Content Rights Manager mints a did:prism for digital media so identity travels with the person, not the platform.",
      "concept": "Content creators can manage and prove rights to their photos through easily verifiable DIDs for end-users.",
      "subDiscipline": "digital media",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits digital media in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Content Rights Manager\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nContent creators can manage and prove rights to their photos through easily verifiable DIDs for end-users.\nDiscipline: Photography (digital media).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits digital media in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $200M — architectural photography and rendering tools · SOM $10M — AI spatial audio for architecture tours\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$200M — architectural photography and rendering tools",
      "som": "$10M — AI spatial audio for architecture tours"
    },
    {
      "id": "photography-quality-check-network-0",
      "theme": "photography",
      "title": "Quality Check Network",
      "pitch": "Quality Check Network pairs two wallets over DIDComm so product photography can exchange trusted claims on a private channel.",
      "concept": "Product photographers invite brands to verify image quality and compliance with marketing standards via secure connections.",
      "subDiscipline": "product photography",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits product photography in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Quality Check Network\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nProduct photographers invite brands to verify image quality and compliance with marketing standards via secure connections.\nDiscipline: Photography (product photography).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits product photography in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $180M — fashion photography production software · SOM $9M — AI mood-setters for fashion shoots\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$180M — fashion photography production software",
      "som": "$9M — AI mood-setters for fashion shoots"
    },
    {
      "id": "photography-collaborative-project-credentialing-0",
      "theme": "photography",
      "title": "Collaborative Project Credentialing",
      "pitch": "Collaborative Project Credentialing issues a signed verifiable credential for collective work that the holder keeps and reuses anywhere.",
      "concept": "Photographers can obtain credentials that confirm participation in collaborations, facilitating credit sharing among all involved.",
      "subDiscipline": "collective work",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches collective work in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Collaborative Project Credentialing\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can obtain credentials that confirm participation in collaborations, facilitating credit sharing among all involved.\nDiscipline: Photography (collective work).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches collective work in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $75M — macro and close-up photography tools · SOM $4M — AI soundscapes for macro exhibitions\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$75M — macro and close-up photography tools",
      "som": "$4M — AI soundscapes for macro exhibitions"
    },
    {
      "id": "photography-content-usage-tracker-0",
      "theme": "photography",
      "title": "Content Usage Tracker",
      "pitch": "Content Usage Tracker verifies a credential at the gate for digital media — proof without a phone call to the issuer.",
      "concept": "Photographers can verify where and how their work is being used online to ensure proper attribution and licensing.",
      "subDiscipline": "digital media",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for digital media in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Content Usage Tracker\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can verify where and how their work is being used online to ensure proper attribution and licensing.\nDiscipline: Photography (digital media).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for digital media in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $350M — drone and aerial photography software · SOM $15M — AI flight-scoring tools for drone pilots\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$350M — drone and aerial photography software",
      "som": "$15M — AI flight-scoring tools for drone pilots"
    },
    {
      "id": "photography-agent-endorsement-0",
      "theme": "photography",
      "title": "Agent Endorsement",
      "pitch": "Agent Endorsement mints a did:prism for artist representation so identity travels with the person, not the platform.",
      "concept": "Photographers’ agents can publicly endorse their clients' works using verified DIDs to attract potential buyers.",
      "subDiscipline": "artist representation",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits artist representation in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Agent Endorsement\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers’ agents can publicly endorse their clients' works using verified DIDs to attract potential buyers.\nDiscipline: Photography (artist representation).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits artist representation in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $220M — sports photography and editing tools · SOM $11M — AI hype tracks for sports editors\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$220M — sports photography and editing tools",
      "som": "$11M — AI hype tracks for sports editors"
    },
    {
      "id": "photography-contest-entry-verifier-0",
      "theme": "photography",
      "title": "Contest Entry Verifier",
      "pitch": "Contest Entry Verifier pairs two wallets over DIDComm so competitions can exchange trusted claims on a private channel.",
      "concept": "Photographers can invite contest organizers to verify their entries and eligibility for participation.",
      "subDiscipline": "competitions",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits competitions in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Contest Entry Verifier\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can invite contest organizers to verify their entries and eligibility for participation.\nDiscipline: Photography (competitions).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits competitions in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $40M — analog and film photography apps · SOM $2M — AI audio aids for darkroom printers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$40M — analog and film photography apps",
      "som": "$2M — AI audio aids for darkroom printers"
    },
    {
      "id": "photography-creative-commons-tracker-0",
      "theme": "photography",
      "title": "Creative Commons Tracker",
      "pitch": "Creative Commons Tracker issues a signed verifiable credential for shared content that the holder keeps and reuses anywhere.",
      "concept": "Photographers can register their works under Creative Commons using verifiable credentials, ensuring adherence to usage rights.",
      "subDiscipline": "shared content",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches shared content in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Creative Commons Tracker\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can register their works under Creative Commons using verifiable credentials, ensuring adherence to usage rights.\nDiscipline: Photography (shared content).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches shared content in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $500M — editorial and photojournalism software · SOM $12M — AI privacy audio for field reporters\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$500M — editorial and photojournalism software",
      "som": "$12M — AI privacy audio for field reporters"
    },
    {
      "id": "photography-reference-verification-0",
      "theme": "photography",
      "title": "Reference Verification",
      "pitch": "Reference Verification verifies a credential at the gate for freelance work — proof without a phone call to the issuer.",
      "concept": "Freelance photographers can present verified references from clients to enhance credibility in new job applications.",
      "subDiscipline": "freelance work",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for freelance work in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Reference Verification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFreelance photographers can present verified references from clients to enhance credibility in new job applications.\nDiscipline: Photography (freelance work).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for freelance work in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $160M — landscape photography planning tools · SOM $7M — AI mindfulness audio for landscape shooters\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$160M — landscape photography planning tools",
      "som": "$7M — AI mindfulness audio for landscape shooters"
    },
    {
      "id": "photography-reputation-tracker-0",
      "theme": "photography",
      "title": "Reputation Tracker",
      "pitch": "Reputation Tracker mints a did:prism for client reviews so identity travels with the person, not the platform.",
      "concept": "Photographers can collect and display verified reviews from clients using DIDs to build trust in their services.",
      "subDiscipline": "client reviews",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits client reviews in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Reputation Tracker\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can collect and display verified reviews from clients using DIDs to build trust in their services.\nDiscipline: Photography (client reviews).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits client reviews in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $90M — event and concert photography tools · SOM $5M — AI rhythm aids for music photographers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$90M — event and concert photography tools",
      "som": "$5M — AI rhythm aids for music photographers"
    },
    {
      "id": "photography-portfolio-showcase-0",
      "theme": "photography",
      "title": "Portfolio Showcase",
      "pitch": "Portfolio Showcase pairs two wallets over DIDComm so online galleries can exchange trusted claims on a private channel.",
      "concept": "Photographers send invitations to gallery owners to authenticate their portfolio submissions for online showcases.",
      "subDiscipline": "online galleries",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits online galleries in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Portfolio Showcase\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers send invitations to gallery owners to authenticate their portfolio submissions for online showcases.\nDiscipline: Photography (online galleries).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits online galleries in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $280M — real estate photography software · SOM $14M — AI audio tours for property photographers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$280M — real estate photography software",
      "som": "$14M — AI audio tours for property photographers"
    },
    {
      "id": "photography-client-referral-program-0",
      "theme": "photography",
      "title": "Client Referral Program",
      "pitch": "Client Referral Program issues a signed verifiable credential for lead generation that the holder keeps and reuses anywhere.",
      "concept": "Photographers can issue credentials to clients for referrals, providing proof of previous work and increasing trust in new clients.",
      "subDiscipline": "lead generation",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches lead generation in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Client Referral Program\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can issue credentials to clients for referrals, providing proof of previous work and increasing trust in new clients.\nDiscipline: Photography (lead generation).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches lead generation in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $60M — pet photography studio tools · SOM $3M — AI engagement audio for pet photographers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$60M — pet photography studio tools",
      "som": "$3M — AI engagement audio for pet photographers"
    },
    {
      "id": "photography-mentorship-validation-0",
      "theme": "photography",
      "title": "Mentorship Validation",
      "pitch": "Mentorship Validation verifies a credential at the gate for career development — proof without a phone call to the issuer.",
      "concept": "Veteran photographers can verify mentorship status to prove their role in guiding emerging talents.",
      "subDiscipline": "career development",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for career development in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Mentorship Validation\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nVeteran photographers can verify mentorship status to prove their role in guiding emerging talents.\nDiscipline: Photography (career development).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for career development in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $110M — fine art and gallery software · SOM $6M — AI curation audio for gallery spaces\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$110M — fine art and gallery software",
      "som": "$6M — AI curation audio for gallery spaces"
    },
    {
      "id": "photography-project-history-log-0",
      "theme": "photography",
      "title": "Project History Log",
      "pitch": "Project History Log mints a did:prism for portfolio management so identity travels with the person, not the platform.",
      "concept": "Photographers can maintain a verifiable history of all projects completed, accessible through their DIDs.",
      "subDiscipline": "portfolio management",
      "quantumHook": "DID Registrar",
      "quantumHookId": "identus-did",
      "quantumTag": "publish a did:prism",
      "quantumRationale": "DID Registrar fits portfolio management in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.",
      "megaPrompt": "Build \"Project History Log\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can maintain a verifiable history of all projects completed, accessible through their DIDs.\nDiscipline: Photography (portfolio management).\nRecipe: DID Registrar (publish a did:prism) as the single Hyperledger Identus primitive.\nWhy Identus: DID Registrar fits portfolio management in Photography because the people and works involved need a portable identifier they own — not a row in someone else's platform database that disappears when the platform does.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DID Registrar (create + publish a did:prism)\n```ts\n// src/lib/identus.functions.ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nexport const mintDid = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const headers = { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! };\n\n    const created = await fetch(`${base}/did-registrar/dids`, {\n      method: \"POST\", headers,\n      body: JSON.stringify({\n        documentTemplate: {\n          publicKeys: [\n            { id: \"auth-1\", purpose: \"authentication\", curve: \"secp256k1\" },\n            { id: \"assert-1\", purpose: \"assertionMethod\", curve: \"secp256k1\" },\n          ],\n          services: [],\n        },\n      }),\n    }).then((r) => r.json());\n\n    await fetch(`${base}/did-registrar/dids/${created.longFormDid}/publications`, {\n      method: \"POST\", headers,\n    });\n\n    // Poll until the DID reports PUBLISHED, then resolve the document.\n    return { did: created.longFormDid, label: data.label };\n  });\n```\n\nUSER FLOW (build exactly this, nothing more)\n1. The user types a label (their name, a company, a work).\n2. Press \"Mint identity\" -> server function creates and publishes a did:prism.\n3. Show the DID, its status (`CREATED` -> `PUBLISHED`), and the resolved document JSON.\n4. A copy button for the DID string, and a short plain-English explainer of what it is.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $45M — underwater photography gear and apps · SOM $2M — AI breathing aids for dive photographers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$45M — underwater photography gear and apps",
      "som": "$2M — AI breathing aids for dive photographers"
    },
    {
      "id": "photography-backup-verification-0",
      "theme": "photography",
      "title": "Backup Verification",
      "pitch": "Backup Verification pairs two wallets over DIDComm so image safety can exchange trusted claims on a private channel.",
      "concept": "Photographers can create a connection to ensure clients verify image backups and access rights across platforms.",
      "subDiscipline": "image safety",
      "quantumHook": "DIDComm Connection",
      "quantumHookId": "identus-connection",
      "quantumTag": "invitation → peer channel",
      "quantumRationale": "A DIDComm Connection suits image safety in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.",
      "megaPrompt": "Build \"Backup Verification\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can create a connection to ensure clients verify image backups and access rights across platforms.\nDiscipline: Photography (image safety).\nRecipe: DIDComm Connection (invitation → peer channel) as the single Hyperledger Identus primitive.\nWhy Identus: A DIDComm Connection suits image safety in Photography because two parties have to establish a private, mutually-authenticated channel before anything sensitive — a credit, a licence, a booking — can pass between them.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — DIDComm Connection (invitation -> peer channel)\n```ts\nexport const createInvitation = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/connections`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({ label: data.label, goalCode: \"connect\", goal: data.label }),\n    });\n    const conn = await res.json();\n    // conn.invitation.invitationUrl -> render as a QR code for the wallet\n    return { connectionId: conn.connectionId, invitationUrl: conn.invitation.invitationUrl, state: conn.state };\n  });\n\nexport const connectionState = createServerFn({ method: \"GET\" })\n  .inputValidator((d) => z.object({ connectionId: z.string() }).parse(d))\n  .handler(async ({ data }) => { /* GET /connections/{id} -> { state } */ });\n```\nPoll `connectionState` every 2s in the client until `ConnectionResponseSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user presses \"Create invitation\" -> server function returns an invitation URL.\n2. Render it as a QR code plus a copyable link.\n3. Poll the connection state every 2s and show a live status pill.\n4. When the state reaches `ConnectionResponseSent`, reveal the paired peer DID.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $130M — automotive photography and video tools · SOM $8M — AI engine audio for car content creators\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$130M — automotive photography and video tools",
      "som": "$8M — AI engine audio for car content creators"
    },
    {
      "id": "photography-personal-brand-verifier-0",
      "theme": "photography",
      "title": "Personal Brand Verifier",
      "pitch": "Personal Brand Verifier issues a signed verifiable credential for branding that the holder keeps and reuses anywhere.",
      "concept": "Photographers can issue credentials that affirm their personal brand identity, enhancing their marketability to clients.",
      "subDiscipline": "branding",
      "quantumHook": "Credential Issuance",
      "quantumHookId": "identus-credential",
      "quantumTag": "signed verifiable credential",
      "quantumRationale": "Credential Issuance matches branding in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.",
      "megaPrompt": "Build \"Personal Brand Verifier\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can issue credentials that affirm their personal brand identity, enhancing their marketability to clients.\nDiscipline: Photography (branding).\nRecipe: Credential Issuance (signed verifiable credential) as the single Hyperledger Identus primitive.\nWhy Identus: Credential Issuance matches branding in Photography because the claim at the centre of the workflow is a fact somebody must vouch for — and a signed credential lets the holder carry that proof anywhere without asking the issuer again.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Credential Issuance (signed verifiable credential)\n```ts\nexport const offerCredential = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    issuingDID: z.string(), claims: z.record(z.string(), z.string()),\n    connectionId: z.string().optional(),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/issue-credentials/credential-offers`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        claims: data.claims,\n        issuingDID: data.issuingDID,          // MUST be published + assertionMethod\n        credentialFormat: \"JWT\",\n        automaticIssuance: true,\n        ...(data.connectionId\n          ? { connectionId: data.connectionId }\n          : { goalCode: \"issue-vc\", goal: \"Claim your credential\" }), // connectionless\n      }),\n    });\n    const rec = await res.json();\n    return { recordId: rec.recordId, state: rec.protocolState, invitationUrl: rec.invitation?.invitationUrl };\n  });\n```\nPoll `GET /issue-credentials/records/{recordId}` until `CredentialSent`.\n\nUSER FLOW (build exactly this, nothing more)\n1. The user fills a tiny claims form (2-4 fields for the scenario below).\n2. Press \"Issue credential\" -> server function creates the offer against the published issuer DID.\n3. Poll the record until `CredentialSent`; show the state machine as it advances.\n4. Show the credential JSON + the invitation URL/QR so a wallet can accept it.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $85M — newborn and maternity photography tools · SOM $4M — AI soothing audio for newborn photographers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$85M — newborn and maternity photography tools",
      "som": "$4M — AI soothing audio for newborn photographers"
    },
    {
      "id": "photography-digital-archive-proof-0",
      "theme": "photography",
      "title": "Digital Archive Proof",
      "pitch": "Digital Archive Proof verifies a credential at the gate for historical photography — proof without a phone call to the issuer.",
      "concept": "Photographers can present verified claims about the historical significance of their works to archivists and collectors.",
      "subDiscipline": "historical photography",
      "quantumHook": "Proof Presentation",
      "quantumHookId": "identus-verify",
      "quantumTag": "verify without the middleman",
      "quantumRationale": "Proof Presentation is right for historical photography in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.",
      "megaPrompt": "Build \"Digital Archive Proof\" as a ONE-SHOT Lovable build.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPhotographers can present verified claims about the historical significance of their works to archivists and collectors.\nDiscipline: Photography (historical photography).\nRecipe: Proof Presentation (verify without the middleman) as the single Hyperledger Identus primitive.\nWhy Identus: Proof Presentation is right for historical photography in Photography because the job is checking a claim at a gate — fast, offline-friendly, and revealing only the fact that matters instead of copying personal data into another database.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThis prompt MUST ship a working demo on the FIRST message with zero follow-ups.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No auth, no nav.\n- ONE server module `src/lib/identus.functions.ts` with `createServerFn` wrappers.\n- ONE client surface wired to it (form + result panel).\n- NO database, NO Lovable Cloud, NO file uploads, NO extra integrations.\n- Libraries: template defaults + `zod`. Nothing else.\n- If a feature is not in the user flow below, do not build it.\n\nIDENTUS PRIMER (assume the reader has never used Identus)\n- Hyperledger Identus is open-source self-sovereign identity: an issuer signs a\n  W3C Verifiable Credential, a holder keeps it in their wallet, and a verifier\n  checks it without phoning the issuer.\n- The Cloud Agent is a REST service. Everything below is plain HTTP + JSON.\n  Docs: https://identus.io/documentation/develop/ ·\n  https://github.com/hyperledger-identus/cloud-agent\n- Identifiers are `did:prism:<hex>` — created unpublished, then published so\n  others can resolve them. Only a PUBLISHED DID carrying an `assertionMethod`\n  key can sign a credential.\n- Agents talk to each other over DIDComm; a Mediator relays messages to wallets\n  that are not always online (https://github.com/hyperledger-identus/mediator).\n- Browser/mobile holders use the TypeScript SDK\n  (https://github.com/hyperledger-identus/sdk-ts) — not needed for this build.\n- Auth to the agent is the `apikey: <AGENT_API_KEY>` header. Server-side only.\n\nRUNTIME CONVENTIONS (this template — follow exactly)\n- Stack: TanStack Start v1 + React 19 + Vite 7 + Tailwind v4 (`src/styles.css`).\n  There is NO `src/App.tsx`, no react-router-dom, no `src/pages`.\n- Routes live in `src/routes`; the home page is `src/routes/index.tsx` and uses\n  `createFileRoute(\"/\")({ component: Page, head: () => ({ meta: [...] }) })`.\n  Never edit `src/routeTree.gen.ts`.\n- Server logic: `import { createServerFn } from \"@tanstack/react-start\";` in a\n  client-safe module such as `src/lib/identus.functions.ts`. Shape is\n  `createServerFn({ method: \"POST\" }).inputValidator((d) => schema.parse(d)).handler(async ({ data }) => {...})`.\n  Call it from the client with `useServerFn(fn)` or directly inside an event handler.\n- Read `process.env.AGENT_BASE_URL` / `process.env.AGENT_API_KEY` INSIDE the\n  handler — never at module scope (env is injected at call time).\n- The server runtime is a Cloudflare-style Worker: use `fetch`, `crypto.randomUUID()`,\n  `Buffer`. No child_process, no sharp, no native modules.\n- Toasts: `sonner` (`import { toast } from \"sonner\"`), and render `<Toaster />`\n  once in `src/routes/__root.tsx`. `@/hooks/use-toast` does NOT exist here.\n- Colours come from semantic tokens in `src/styles.css` — no hardcoded\n  `text-white` / `bg-black` / `bg-[#hex]` in components.\n- Give `src/routes/index.tsx` its own `head()` with a real title and description.\n\n<<MODE_BLOCK>>\n\nCLOUD AGENT API REFERENCE (everything you need — no other docs required)\nAll calls: base URL `AGENT_BASE_URL`, headers\n`{ \"content-type\": \"application/json\", apikey: AGENT_API_KEY }`.\n\nGET  /_system/health                      -> { version } (use for a status pill)\n\nPOST /did-registrar/dids\n  body { documentTemplate: { publicKeys: [{ id, purpose: \"authentication\"|\"assertionMethod\", curve: \"secp256k1\" }], services: [] } }\n  -> { longFormDid, status: \"CREATED\" }\nPOST /did-registrar/dids/{didRef}/publications  -> { scheduledOperation: { id, didRef } }\nGET  /did-registrar/dids/{didRef}         -> { did, longFormDid, status: \"CREATED\"|\"PUBLICATION_PENDING\"|\"PUBLISHED\" }\nGET  /dids/{did}                          -> resolved DID document\n\nPOST /connections\n  body { label, goalCode: \"connect\", goal }\n  -> { connectionId, state: \"InvitationGenerated\", invitation: { invitationUrl, id } }\nPOST /connection-invitations\n  body { invitation: \"<oob base64url string from the invitationUrl ?_oob= param>\" }\nGET  /connections/{connectionId}\n  -> { state } : InvitationGenerated -> ConnectionRequestReceived -> ConnectionResponseSent\nGET  /connections                         -> { contents: [...] }\n\nPOST /issue-credentials/credential-offers\n  body { claims: { ... }, issuingDID, credentialFormat: \"JWT\", automaticIssuance: true,\n         connectionId? | (goalCode + goal for connectionless) }\n  -> { recordId, protocolState: \"OfferSent\", invitation?: { invitationUrl } }\nGET  /issue-credentials/records/{recordId}\n  -> { protocolState } : OfferSent -> RequestReceived -> CredentialSent, plus `credential` (JWT string)\nGET  /issue-credentials/records           -> { contents: [...] }\n\nPOST /present-proof/presentations\n  body { connectionId, proofs: [], options: { challenge, domain },\n         claims: { \"<attr>\": {} } }   // or anoncredPresentationRequest for ZK predicates\n  -> { presentationId, status: \"RequestSent\" }\nGET  /present-proof/presentations/{presentationId}\n  -> { status } : RequestSent -> PresentationReceived -> PresentationVerified | PresentationVerificationFailed\n  plus `data` (the disclosed claims)\n\nErrors are RFC-7807 JSON: { status, title, detail }. Surface `detail` in the UI —\nit names the real problem (unpublished DID, missing connectionId, bad apikey).\n\nSERVER SNIPPET — Proof Presentation (verify a credential)\n```ts\nexport const requestProof = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ connectionId: z.string(), attributes: z.array(z.string()) }).parse(d))\n  .handler(async ({ data }) => {\n    const base = process.env.AGENT_BASE_URL!.replace(/\\/$/, \"\");\n    const res = await fetch(`${base}/present-proof/presentations`, {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\", apikey: process.env.AGENT_API_KEY! },\n      body: JSON.stringify({\n        connectionId: data.connectionId,\n        proofs: [],\n        options: { challenge: crypto.randomUUID(), domain: \"https://example.app\" },\n        claims: Object.fromEntries(data.attributes.map((a) => [a, {}])),\n      }),\n    });\n    const rec = await res.json();\n    return { presentationId: rec.presentationId, state: rec.status };\n  });\n```\nPoll `GET /present-proof/presentations/{id}` until `PresentationVerified`, then\nshow a green/red gate. Reveal only the predicate you needed — never store the\nholder's full credential.\n\nUSER FLOW (build exactly this, nothing more)\n1. The verifier picks which attributes they demand (checkboxes).\n2. Press \"Request proof\" -> server function creates the presentation request.\n3. Poll until `PresentationVerified` and show a large PASS / FAIL gate.\n4. List ONLY the disclosed attributes, with a note that nothing else was revealed.\n\nDESIGN\nEditorial, high-contrast, generous whitespace. One accent colour used sparingly.\nShow the protocol honestly: render the state machine and the raw JSON envelope in\na collapsible panel so a judge can see the real Identus record. Truncate DIDs and\nJWTs in prose (first 12 + last 6 chars) with the full value behind a copy button.\nMobile first — a judge will open this on a phone.\n\nMARKET (for the pitch slide, not the UI)\nTAM $2.4B — global photo software market · SAM $190M — travel photography and journaling apps · SOM $9M — AI memory audio for travel photographers\n\nGOTCHAS (universal — apply to every agent call in this build)\n- Only a PUBLISHED DID with an `assertionMethod` key can sign a credential\n  offer. Create -> publish -> wait for `PUBLISHED` before issuing, or you get a\n  cryptic 422/500.\n- On a direct Fly deploy the agent serves at the app ROOT — strip any trailing\n  `/cloud-agent` from the base URL. On the local docker compose stack, KEEP it.\n- Every issuance/presentation endpoint is asynchronous: you POST, then POLL the\n  record's `protocolState` (`OfferSent` -> `CredentialSent`,\n  `RequestSent` -> `PresentationVerified`). Never assume the POST finished the job.\n- Connectionless issuance omits `connectionId` and needs a `goalCode`; if you\n  send neither a `connectionId` nor a `goalCode` you get \"Missing connectionId\".\n- The human principal and any AI agent acting for them are DIFFERENT DIDs.\n  Compare principal to credential subject, agent to mandate subject — never cross them.\n- First boot of a real agent migrates four databases: allow ~5 minutes and >= 4 GB\n  of memory before deciding it is broken.\n- Never call the agent from the browser. Every fetch lives inside a\n  `createServerFn` handler so `AGENT_API_KEY` stays server-side.\n\nREFERENCE MATERIAL (if you need more than the above)\n- Identus docs: https://identus.io/documentation/develop/\n- Cloud Agent (OpenAPI + compose examples): https://github.com/hyperledger-identus/cloud-agent\n- TypeScript SDK (browser/wallet holders): https://github.com/hyperledger-identus/sdk-ts\n- Mediator (DIDComm relay for offline wallets): https://github.com/hyperledger-identus/mediator\n- Umbrella repo: https://github.com/hyperledger-identus/hyperledger-identus\n- Reference console built with this stack: https://github.com/arunnadarasa/identus\n- Full machine-readable brief for your own LLM: https://identusprompts.lovable.app/llms-full.txt\n\nDELIVERABLE\nA working single-page demo where the flow above completes end-to-end, plus a\none-paragraph README explaining which Identus primitive is used and how to point\nthe app at a real Cloud Agent.\nBuilt for the Hyperledger Identus Catalyst — organised by StreetKode Fam during Indian Krump Festival 14.",
      "tam": "$2.4B — global photo software market",
      "sam": "$190M — travel photography and journaling apps",
      "som": "$9M — AI memory audio for travel photographers"
    }
  ]
}
